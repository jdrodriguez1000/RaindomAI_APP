# Informe de auditoria — S-006

| Campo | Valor |
|---|---|
| Sesion | S-006 |
| Fecha | 2026-09-02 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | ver `git log -1 -- _audit/S-006.md` (se rellena tras el commit de este cierre) |

## 0. Respuesta a la auditoria anterior

`R-005` (sobre `S-005`, commit `510d580`) abrio cuatro hallazgos nuevos: `F-011`, `F-012`, `F-013`,
`F-014`. Ademas quedaba abierto `F-010` (de `R-004`, sobre `S-004`, commit `c70b757`), que `S-005`
habia aceptado pero no podia corregir por un punto muerto de autoria que `R-005` senalo
explicitamente en su seccion 5. Esta sesion evaluo los cinco.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-010 — Una convencion vigente de `_audit/findings.md` cita un archivo que ya no existe | Aceptado — pendiente | `T-018`, `Implementada` en este commit (linea de la seccion «Convenciones» corregida bajo la autorizacion nueva de `D-027`) |
| F-011 — La nota nueva de `A-001` vuelve a registrar un recuento sobre `HEAD` que no se reproduce sobre su commit | Aceptado — pendiente | `T-014`, `Implementada` en este commit (dos notas fechadas ancladas a `e61454b`, bajo `A-001` en `assumptions.md` y bajo `T-012` en `tasks.md`) |
| F-012 — La segunda excepcion de `D-025` no llego a los tres sitios que siguen diciendo «es la unica excepcion» | Aceptado — pendiente | `T-015`, `Implementada` en este commit (los tres sitios reescritos: `.claude/agents/session-closer.md`, `.claude/skills/protocol-close/SKILL.md`, `_phases/000_preproject.md`) |
| F-013 — `D-023` conserva vigente una advertencia que `D-026` desmiente en el mismo commit | Aceptado — pendiente | `T-016`, `Implementada` en este commit (nota fechada bajo `D-023` en `decisions.md`, que ademas corrige la descripcion del ambito anterior) |
| F-014 — El «Avance de la etapa» de `progress.md` atribuye mal la procedencia de los hallazgos y cuenta `F-006` dos veces | Aceptado — pendiente | `T-017`, `Implementada` en este commit (secciones 1 y 2 de `progress.md` reescritas; la bitacora de `S-005`, historica, lleva nota fechada) |

Nota de lectura, igual que en `S-005`: **la correccion ya esta escrita en este commit**, pero el
veredicto que la cierra —`Implementado`— solo lo puede dar esta auditoria en su siguiente pasada,
verificando sobre `HEAD` que cada nota o correccion realmente esta ahi y dice lo que su `T-XXX`
afirma. `manager` no cierra sus propios hallazgos, y tampoco los que corrige por autorizacion de
`D-027`.

## 1. Que se hizo

Archivos tocados en este commit (via `git status --porcelain` mas `git show --stat --name-only
--format= <commit>` una vez exista el hash):

- `.claude/agents/session-closer.md` — reescribe la seccion de la segunda excepcion de escritura de
  `manager` sobre `tasks.md`, por su cita (`D-XXX`/`F-NNN`) y no por su numero (`T-015`, `F-012`)
- `.claude/skills/protocol-audit/SKILL.md` — el patron del control (c) suma `_methodology` al
  ambito (`D-028`)
- `.claude/skills/protocol-close/SKILL.md` — el mismo ambito nuevo en el Paso 1b (`D-028`); y el
  Paso 6 gana la comprobacion del ambito temporal de los bloques de verificacion (`T-019`, `D-022`)
- `CLAUDE.md` — `_methodology/` entra en la lista de lo que tiene que poder copiarse tal cual y que
  el Paso 1b comprueba (`D-028`)
- `_audit/findings.md` — las cinco filas (`F-010` a `F-014`) pasan a `Aceptado — pendiente` citando
  su `T-XXX`; cada entrada de detalle lleva su bloque «Que se hizo» con comando y salida cruda; la
  linea de la seccion «Convenciones» que citaba `debtec.md` se corrige (`T-018`, bajo `D-027`)
- `_audit/index.md` — fila nueva `S-006` (este cierre, ver mas abajo)
- `_persistence/assumptions.md` — nota fechada bajo `A-001` que ancla a `e61454b` el recuento sobre
  `HEAD` sin fecha (`T-014`)
- `_persistence/constraints.md` — entrada nueva `C-007` (las fuentes de la guia de metodo no se
  editan)
- `_persistence/decisions.md` — nota fechada bajo `D-023` (`T-016`); entradas nuevas `D-027` a
  `D-031`
- `_persistence/lessons.md` — entrada nueva `L-009` (un hallazgo acota su ejemplo, no el defecto)
- `_persistence/progress.md` — secciones 1, 2 y 3 sobrescritas; nota fechada bajo la bitacora de
  `S-005` (`T-017`); entrada nueva `S-006` y su fila en el indice de sesiones (este cierre)
- `_persistence/tasks.md` — nota fechada bajo `T-012` (`T-014`); tareas nuevas `T-014` a `T-019`
  (`Origen: report_auditor`, `Sesion: S-006`)
- `_phases/000_preproject.md` — mapa de carpetas y tabla de escritura por archivo actualizados con
  `_methodology/` y las dos excepciones de `tasks.md` (`D-028`, `T-015`)
- `project.md` — declara `_methodology/` en «Carpetas propias»; nota sobre que la guia no declara
  etapas; propuesta de codigos de producto del metodo (`D-028`, `D-029`, `D-030`)
- `_methodology/` (nuevo, sin trackear hasta este commit) — `000_method.md` (documento canonico del
  metodo) y `sources/` con las tres fuentes intactas (`005_vertical.md`, `010_prototyping.md`,
  `015_evolution.md`), aportados por el usuario y adoptados por `D-028`, `D-029`, `D-030`, `D-031`

Y del propio cierre de sesion (no de contenido de la jornada): `_persistence/progress.md` (entrada
`S-006` y secciones 1-3, ya contadas arriba), `_audit/S-006.md` (este archivo) y su fila en
`_audit/index.md`.

**Resumen por bloque:**

1. **Los cuatro hallazgos de `R-005` (`F-011`-`F-014`).** Dos son reincidencias del patron de
   `D-022` —recuento sobre `HEAD` sin ancla, dentro de una correccion que ya existia para corregir
   ese mismo patron (`F-011`)— y una excepcion de `D-025` que no llego a los tres sitios donde se
   enuncia la regla para quien la ejecuta (`F-012`, reincidencia de `F-007`). Los otros dos son
   texto vigente que quedo desmentido por una decision del mismo commit (`F-013`) y un recuento
   erroneo en el archivo que se lee siempre al arrancar (`F-014`). En los cuatro casos la
   verificacion amplio el alcance mas alla de lo que el hallazgo citaba literalmente —de ahi nace
   `L-009`—.
2. **`F-010`, desatascado.** Estaba `Aceptado — pendiente` desde `S-005` sin que nadie pudiera
   tocar el texto senalado: el auditor tiene prohibido corregir y `manager` tenia prohibido
   escribir en `_audit/findings.md` mas alla de la fila de estado. `D-027` decide que el texto que
   senala un hallazgo aceptado lo corrige `manager`, aunque el archivo pertenezca a otro agente,
   con limites explicitos sobre que se puede tocar (registro vivo si, documentos entregados no).
3. **Mecanismo para `D-022`.** `D-022` llevaba desde `S-005` como regla sin nada que la aplicara —
   la propia `R-005` lo senalo como recomendacion sin hallazgo. `T-019` anade al Paso 6 de
   `protocol-close` la comprobacion del ambito temporal de los bloques de verificacion.
4. **`_methodology/`.** El usuario aporto el metodo de desarrollo consolidado y sus tres fuentes.
   Entra al repositorio como carpeta agnostica dentro del ambito del Paso 1b (`D-028`); se declara
   guia vigente que no declara etapas del proyecto (`D-029`); sus codigos de producto colisionaban
   con `F-NNN`/`S-XXX` del registro y se renombran dentro del documento (`D-030`); y la seccion del
   Gate del metodo pasa de una terminal auditora de un esquema ya revocado a dos firmas —veredicto
   tecnico de `report_auditor`, Gate declarado por el usuario— (`D-031`).

## 2. Que NO se hizo, y por que

Del indice de `tasks.md`, filtrando `No implementada` (las mismas tres desde `S-001`, sin tocar
esta sesion):

- `T-001` — Definir alcance y objetivo del proyecto. Sin tocar; etapa `005_discovery`.
- `T-002` — Declarar las etapas posteriores a `000_preproject`. Sin tocar; etapa `005_discovery`.
- `T-003` — Verificar si el historico de la fuente oficial es obtenible (`A-003`). Sin tocar, etapa
  `000_preproject`, bloqueante.

Ademas, una recomendacion de `R-005` sigue sin codigo que la agende: **`005_discovery` esta
declarada en `project.md` desde `D-024` y todavia no tiene su archivo en `_phases/`**. `D-024` ya lo
anotaba como advertencia; `R-005` lo repitio como recomendacion sin hallazgo; esta sesion no la
convirtio en `T-XXX`. Va en «Sin resolver» del reporte del cierre.

## 3. Decisiones tomadas

- **`D-027` — El texto que senala un hallazgo aceptado lo corrige `manager`, aunque el archivo sea
  de otro.** Origen: report_auditor (a partir de la seccion 5 de `R-005`). Alternativas
  descartadas: ampliar el mandato del auditor a las convenciones de `findings.md` (rompe
  independencia en la pasada siguiente); declarar la seccion como de `manager` sin mas (deja el
  caso simetrico de `F-014` en `progress.md` igual de atascado); dejarlo sin zanjar (deja `F-010`
  sin poder cerrarlo nadie).
- **`D-028` — `_methodology/` entra al repositorio como carpeta agnostica, dentro del control de
  fuga.** Origen: usuario. Alternativas descartadas: dejarla fuera del repositorio; anadirla a
  `.gitignore`; meterla dentro de `CLAUDE.md`; declararla sin control que la compruebe.
- **`D-029` — `000_method.md` es guia de metodo vigente, y no declara las etapas del proyecto.**
  Origen: usuario. Alternativas descartadas: adoptar el ciclo completo del metodo y declarar todas
  las etapas; dejar el documento sin bloque de encuadre; no incorporar el documento hasta declarar
  las etapas.
- **`D-030` — Los codigos de producto del metodo se renombran a `FT-` y `SC-`.** Origen: manager.
  Alternativas descartadas: renombrar los del registro (`F-NNN`, `S-XXX`, ya auditados); aplazar
  con una entrada de deuda tecnica; usar `FEAT-`/`SCEN-`.
- **`D-031` — Un Gate lo declara el usuario, sobre el veredicto tecnico de `report_auditor`.**
  Origen: usuario. Alternativas descartadas: dejar que el auditor declare el Gate (amplia su
  mandato y rompe separacion de roles); dejar la seccion como principio negativo sin dueno.

## 4. Supuestos vigentes y riesgos

- **`A-001`** — Un auditor lanzado por el auditado conserva independencia suficiente. Sigue
  `Abierto`. Esta sesion solo le ancla a su hash el recuento de la nota anadida en `S-005` (`T-014`,
  `F-011`); no se toco el fondo del supuesto.
- **`A-003`** — Si el historico de la fuente oficial es obtenible. Sigue sin verificar (`T-003`,
  bloqueante). De el depende el ciclo entero del producto.
- **Riesgo sin codigo:** `005_discovery` declarada sin archivo en `_phases/` (ver seccion 2). No
  tiene `T-XXX` ni `DT-XXX` que lo agende.
- **Riesgo que la propia `L-009` deja abierto:** la leccion dice que la cita de un hallazgo es «una
  muestra, no un inventario», pero no fija un mecanismo que obligue a barrer el ambito completo en
  la proxima correccion — depende de que quien corrija se acuerde de aplicarla. Es el mismo patron
  que `L-006`/`L-008` ya describieron para otro defecto, y esta sesion no le dio a `L-009` un
  mecanismo equivalente al que `T-019` le dio a `D-022`.

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto a partir de `_brief/client_brief.md`. Importancia
`Alta`, urgencia `Bloqueante`, etapa `005_discovery`. Sigue siendo la primera accion que de verdad
abre esa etapa.

## 6. Que pedimos auditar

- **Que las notas y correcciones de esta sesion (`T-014` a `T-018`) realmente esten en `HEAD` con
  el comando y la salida cruda que sus tareas afirman.** Son correcciones sobre correcciones de un
  patron que ya fallo cinco veces (`L-008`, `L-009`); el riesgo de una sexta repeticion dentro de
  esta misma correccion es real y no teorico.
- **Que la ampliacion nueva de `D-027`** —`manager` corrige texto en archivos de otro agente cuando
  un hallazgo aceptado lo senala— **se haya aplicado dentro de sus limites**: solo lo que la propia
  decision marca como registro vivo (`_audit/findings.md`, secciones reescribibles de `progress.md`),
  nunca documentos entregados (`_audit/R-XXX.md`, `_audit/S-XXX.md`) ni la bitacora historica.
- **Que `_methodology/` sea de verdad agnostica** — sin nombre del proyecto, ruta de disco ni host
  del remoto, ahora que entra por primera vez al ambito del Paso 1b junto con `.claude`, `CLAUDE.md`
  y `_phases`. El control de esta sesion dio cero sobre el arbol de trabajo (no sobre un commit,
  porque la carpeta todavia no estaba trackeada); conviene una segunda mirada sobre el commit ya
  cerrado.
- **Que el mecanismo nuevo del Paso 6 de `protocol-close` (`T-019`, para `D-022`) sea aplicable en
  la practica** y no solo teoricamente correcto: es la primera vez que se prueba, y todavia no ha
  pasado por un cierre real que lo ejecute sobre sus propias entradas.
- **El riesgo sin codigo senalado en la seccion 4** — que `005_discovery` siga declarada sin archivo
  en `_phases/` y sin `T-XXX` que lo agende, arrastrado ya desde `D-024` y repetido por `R-005` como
  recomendacion sin hallazgo.
