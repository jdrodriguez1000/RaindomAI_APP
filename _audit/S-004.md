# Informe de auditoria — S-004

| Campo | Valor |
|---|---|
| Sesion | S-004 |
| Fecha | 2026-09-01 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-004.md`) |

## 0. Respuesta a la auditoria anterior

`R-003` (sobre `S-003`, commit `ea0b850`) devolvio veredicto **Con hallazgos (3)**: `F-005`,
`F-006`, `F-007`. Esta sesion solo evaluo `F-007`; `F-005` y `F-006` no se tocaron.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-005 — El bloque de verificacion de la observacion nueva de `A-001` no se reproduce sobre su propio commit | Abierto | No evaluado esta sesion. Sigue como lo dejo `R-003` |
| F-006 — La nota de ambito de `D-016` registra recuentos que no cuadran con el commit que la contiene | Abierto | No evaluado esta sesion. Sigue como lo dejo `R-003` |
| F-007 — `D-020` declara una excepcion que la convencion de `tasks.md` no recoge, y la tension queda sin registrar | Aceptado — pendiente | `T-008`, `Implementada`. `manager` verifico contra `HEAD` (`ea0b850`, comando en `_audit/findings.md`, entrada `F-007`) que el defecto seguia vivo, y escribio la excepcion dentro de la convencion de `_persistence/tasks.md`, reflejandola tambien en `.claude/skills/protocol-close/SKILL.md` y en `.claude/agents/session-closer.md`. El estado sigue `Aceptado — pendiente` y no `Implementado` porque quien cierra un hallazgo es la auditoria siguiente, nunca `manager` |

## 1. Que se hizo

Archivos tocados esta sesion, segun `git status --porcelain` (el commit de este cierre aun no
existia al generar esta lista; verificar con `git show --stat --name-only HEAD` una vez commiteado):

- `.claude/agents/session-closer.md` — se anadio el parrafo de la excepcion mecanica de `D-020`
  (que `manager`, no el closer, puede escribir la `T-XXX` de un hallazgo aceptado) y se corrigio
  `debtec.md` → `techdebt.md` en la descripcion del actor y en el limite del Paso 5.
- `.claude/skills/protocol-audit/SKILL.md` — `debtec` → `techdebt` en el bucle de archivos del
  control de indice ↔ detalle.
- `.claude/skills/protocol-close/SKILL.md` — mismo parrafo de la excepcion de `D-020` que en
  `session-closer.md`, mas `debtec` → `techdebt` en el bucle del Paso 2b, en la tabla de codigos,
  en el titulo del Paso 5, en la referencia del Paso 6 y en la plantilla del reporte del Paso 8.
- `.claude/skills/protocol-start/SKILL.md` — `debtec` → `techdebt` en el bucle de comprobacion, en
  la tabla de lectura a demanda, en la seccion "Como se leen estos archivos", en los `grep` de
  ejemplo y en la tabla de estados que no se reportan como abiertos.
- `CLAUDE.md` — `debtec.md` → `techdebt.md` en la seccion "Cierre de sesion".
- `_audit/findings.md` — la fila de `F-007` en el indice pasa de `Abierto` a `Aceptado — pendiente`;
  la entrada de detalle de `F-007` se completa con "Registrado en: T-008" y el relato de la
  evaluacion de `manager`.
- `_persistence/decisions.md` — se anade a `D-020` una nota fechada que cierra su tension («la
  excepcion pasa a estar escrita dentro de la convencion de `tasks.md`»), y se registra `D-021`
  (`debtec.md` pasa a llamarse `techdebt.md`), con su verificacion de dos comandos (`git grep`
  sobre el ambito vivo, y un recuento de las referencias que quedan fuera de ese ambito a
  proposito).
- `_persistence/lessons.md` — se registra `L-007` ("Una excepcion se escribe donde esta la regla,
  no donde se decidio"), con `Origen: report_auditor`.
- `_persistence/progress.md` — actualizacion obligatoria del cierre: `Estado general`, `Ultimo
  realizado`, `Siguiente paso` sobrescritos, y entrada nueva `S-004` en la Bitacora con su fila de
  indice. La tabla de estructura (seccion 6) pasa `debtec.md` → `techdebt.md`.
- `_persistence/tasks.md` — dentro de la convencion del archivo se escribe la excepcion que fija
  `D-020` (que `manager` puede escribir una `T-XXX` de origen `report_auditor` fuera del cierre), y
  se anade `T-008` con `Estado: Implementada`, `Origen: report_auditor`, `Sesion: S-004`. **Esta
  tarea la escribio `manager` durante la jornada, no este cierre** — es la excepcion mecanica que
  el propio `D-020` fija y que esta misma sesion documenta en la convencion.
- `_persistence/debtec.md` → `_persistence/techdebt.md` — renombrado con `git mv` (aparece en
  `git status` como `RM`, similaridad 100%). Contenido sin cambios salvo el encabezado (`# debtec.md`
  → `# techdebt.md`) y la tabla de indice/detalle de `DT-001` (`Estado: Implementada`,
  `Confirmacion: Confirmada`), mas una nota fechada que explica el pago de la deuda y remite a
  `D-021`.
- `project.md` — `debtec.md` → `techdebt.md` en la tabla de codigos.
- `_audit/S-004.md` (este archivo) y su fila en `_audit/index.md` — los anade este cierre.

## 2. Que NO se hizo, y por que

Tareas `No implementada` en el indice de `_persistence/tasks.md` al cierre de esta sesion:

- `T-001` — Definir alcance y objetivo del proyecto. Sin tocar esta sesion.
- `T-002` — Declarar las etapas posteriores a `000_preproject`. Sin tocar esta sesion.
- `T-003` — Verificar si el historico de la fuente oficial es obtenible (`A-003`). Sin tocar esta
  sesion.

Ademas, `F-005` y `F-006` de `R-003` siguen `Abierto` en `_audit/findings.md`: esta sesion evaluo
unicamente `F-007`. No hay `D-XXX` de rechazo ni `T-XXX` de aceptacion para ninguno de los dos —
siguen exactamente como los dejo `R-003`, y el motivo de por que no se tocaron no esta documentado
en ningun archivo mas alla de esta linea del informe.

## 3. Decisiones tomadas

- **`D-020` (nota de cierre, no una decision nueva):** el usuario, preguntado, confirmo que
  `manager` debe escribir en `_persistence/tasks.md` la `T-XXX` de un hallazgo aceptado en el
  momento de aceptarlo, sin esperar al cierre de sesion. Con eso, la tension que la propia `D-020`
  habia dejado declarada («si el usuario prefiere lo contrario, la regla de `tasks.md` deberia
  decirlo con la excepcion escrita dentro») se resuelve a favor de lo ya decidido, y la excepcion
  se escribe donde antes solo estaba prohibida en absoluto. No hubo alternativas descartadas
  distintas de las que ya listaba `D-020` al abrirse.
- **`D-021` — `debtec.md` pasa a llamarse `techdebt.md`:** el usuario pidio el renombrado
  revisando todas las referencias, en la misma peticion en que confirmo `DT-001`. Se hizo
  `git mv` y se reescribieron las referencias vivas en `.claude/`, `CLAUDE.md`, `project.md` y la
  tabla de estructura de `progress.md`. **Alternativas descartadas:** reescribir tambien todo
  `_audit/` para dejar el patron `debtec` en cero absoluto sobre el repositorio entero (se
  descarto: los informes y auditorias ya entregados citan el nombre que existia cuando se
  escribieron, y reescribirlos falsificaria un documento ya auditado — mismo criterio que
  `D-016`); no renombrar y mantener la deuda como propuesta (revocado por el propio usuario al
  pedir el pago).

## 4. Supuestos vigentes y riesgos

Ningun `A-XXX` se toco esta sesion. Siguen abiertos `A-001` (un auditor lanzado por el auditado
conserva independencia suficiente), `A-002` (el brief recibido es el encargo completo) y `A-003`
(el historico de la fuente oficial es obtenible de forma automatizable) — este ultimo bloqueante
para todo el ciclo de producto (`T-003`).

Riesgo propio de esta sesion, sin codigo abierto todavia: la nota de verificacion de `D-021`
("cero referencias `debtec` en el ambito vivo") se corrio **antes** de que este cierre escribiera
la entrada `S-004` de `_persistence/progress.md`. Esa entrada, al describir el propio renombrado,
necesariamente vuelve a citar `debtec.md` (para decir de donde vino el archivo). El recuento que
`D-021` registra para `progress.md` (2 referencias) ya no coincide con el estado final de este
commit (5 referencias). No hay `A-XXX`, `T-XXX` ni `DT-XXX` que registre esta discrepancia; queda
solo en la seccion 6 de este informe.

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto (`Alta`, `Bloqueante`), a partir de
`_brief/client_brief.md`, contrastado punto por punto (`A-002`). Es la que abre la etapa siguiente
a `000_preproject` y la que llevan pendiente `S-001`, `S-002`, `S-003` y ahora `S-004` sin tocar.

## 6. Que pedimos auditar

- **La discrepancia entre el recuento de `D-021` y el estado final del commit.** `D-021` afirma
  `_persistence/progress.md:2` para el patron `debtec`, comando corrido antes de que este cierre
  anadiera la entrada `S-004`. Esa entrada, al narrar el propio renombrado, sube el recuento real a
  5. La decision no queda "falsa" —describe correctamente lo que veia al momento de escribirse—,
  pero el numero que cita ya no se reproduce sobre el commit que lo contiene. Es el mismo patron de
  defecto que `F-001` y `F-006`: un enunciado de verificacion mas estrecho en el tiempo que el
  commit que lo termina conteniendo.
- **`F-005` y `F-006` siguen sin evaluar.** No hay decision de aceptarlos ni de rechazarlos, y esta
  sesion no explica por que se dejaron de lado en favor de `F-007` y del renombrado de `DT-001`.
  Si el criterio fue simplemente "el usuario trajo otro encargo (el renombrado) esta sesion", esa
  razon no quedo escrita en ningun archivo de `_persistence/`.
- **El titulo de `DT-001` y las citas historicas a `debtec.md` distribuidas por `_audit/` y por
  entradas cerradas de `decisions.md` y `progress.md`.** Es una decision deliberada (`D-021`, mismo
  criterio que `D-016`), pero significa que `git grep -n debtec -- .` sobre el repositorio entero
  nunca volvera a dar cero, y quien no conozca `D-016`/`D-021` puede leerlo como una fuga real. Vale
  la pena que la auditoria confirme si ese criterio de "cero solo en el ambito vivo, nunca en el
  repositorio entero" sigue pareciendole razonable ahora que hay dos decisiones que lo invocan.
