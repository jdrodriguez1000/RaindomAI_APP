# Informe de auditoria — S-036

| Campo | Valor |
|---|---|
| Sesion | S-036 |
| Fecha | 2026-09-11 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | `6fb6d39` |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`ce0ac4e`, que es la auditoria `R-035`), sobre el estado que dejo el commit
auditado `cce48e0`. Los dos hashes se derivaron asi:

```
$ git rev-parse --short HEAD
ce0ac4e
$ git log -1 --format='%h %s' HEAD
ce0ac4e auditoria R-035 sobre S-035 (cce48e0)
```

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-100 — la bitacora de `S-035` afirma que `project.md` gana dos filas nuevas, y el commit no toca `project.md` | Aceptado — corregido en este commit | `T-169`, `Implementada`; nota fechada en la propia entrada `S-035` de `_persistence/progress.md`, con las dos ordenes que muestran que ni el commit ni el archivo en `HEAD` tienen las filas |
| F-101 — el bloque de verificacion de `T-157` publica el resultado de un segundo barrido sin su patron ni su salida | Aceptado — corregido en este commit | `T-170`, `Implementada`; nota fechada en `_persistence/tasks.md` que rehace el barrido (no se pudo reconstruir el de entonces) y publica patron, ambito y salida cruda |

⚠️ **Los dos hallazgos se dejan `Aceptado — pendiente` en `_audit/findings.md`, no `Aceptado —
corregido en este commit`, siguiendo la misma convencion que `S-035` aplico a `F-098`/`F-099`.** La
correccion de fondo esta en este commit —las dos notas fechadas—, pero el estado `Implementado` en
`findings.md` lo escribe unicamente una auditoria posterior que lo verifique sobre `HEAD`. La columna
«Veredicto» de esta tabla describe lo que `manager` hizo con el hallazgo; la columna «Estado» de
`findings.md` describe lo que el auditor verifico, y son cosas distintas.

## 1. Que se hizo

El commit todavia no existe al escribir esta seccion, asi que la lista sale del area de staging con
la orden equivalente a `git show --stat --name-only --format= <commit>`:

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
.claude/skills/protocol-harvest/SKILL.md
.claude/skills/protocol-promote/SKILL.md
CLAUDE.md
_audit/findings.md
_persistence/assumptions.md
_persistence/constraints.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_persistence/techdebt.md
project.md
```

📌 **Esta lista sale del area de staging y se ancla al commit real en la NOTA DE CIERRE de la seccion
7, junto con la version anclada de `git show --stat --name-only --format=`** (Paso 7c).

Trece archivos. Que cambia en cada uno:

- **`.claude/skills/protocol-close/SKILL.md`** — nace el **Paso 2f** (barrido de desfase con el
  esqueleto, informa y no frena, con guarda de ruta ausente) y su seccion 9 de evidencia en la
  plantilla del informe, y la fila «Desfase con el esqueleto (2f)» en la plantilla del reporte de
  pantalla (`T-160`, `D-148`).
- **`.claude/skills/protocol-harvest/SKILL.md`** — una frase se corrige: deja de decir que la cosecha
  es «el unico protocolo del andamiaje que escribe fuera de este repositorio» y pasa a decir «uno de
  los dos», nombrando a `protocol-promote` (`T-161`).
- **`.claude/skills/protocol-promote/SKILL.md`** (nace) — la skill de promocion al esqueleto de
  arranque, con `manager` como unico ejecutor y su puerta antes de escribir (Paso 5), no antes del
  push (`T-161`, `D-146`).
- **`CLAUDE.md`** — la misma correccion que en `protocol-harvest`, en su propio parrafo sobre la
  cosecha de lecciones (`T-161`).
- **`_audit/findings.md`** — `F-100` y `F-101` pasan de `Abierto` a `Aceptado — pendiente`, citando
  `T-169` y `T-170` (esta sesion, seccion 0 de arriba).
- **`_persistence/assumptions.md`** — nace `A-019` (la skill de promocion existe y esta bien
  ordenada, pero no se ha ejecutado ni una vez); una nota (`T-158`) se anade a `A-018` con el primer
  ejercicio de su disparador, sin confirmarlo.
- **`_persistence/constraints.md`** — nace `C-010`: las barras invertidas se pierden al escribir por
  shell, incluso dentro de un heredoc citado, y quedan como caracter de retroceso real (`0x08`)
  invisible en pantalla.
- **`_persistence/decisions.md`** — nace `D-151` (los patrones «mangled» ya commiteados no se
  corrigen en masa; la regla rige hacia adelante); tres notas fechadas sobre `D-145` (criterio de
  cierre de `T-161` cumplido), `D-147` (criterio de cierre de `T-159` cumplido, y correccion de una
  cuarta cita cruzada en su cuerpo — `T-171`) y `D-148` (criterio de cierre de `T-160` cumplido).
- **`_persistence/lessons.md`** — nacen `L-056` (una orden que busca su propio rotulo en el archivo
  donde queda escrita se cuenta a si misma) y `L-057` (una barra invertida se pierde al escribir y
  convierte un patron publicado en uno que no reproduce).
- **`_persistence/progress.md`** — la entrada `S-035` de la bitacora gana la nota fechada de `F-100`
  (`T-169`); se sobrescriben «Estado general», «Ultimo realizado» y «Siguiente paso»; nace la entrada
  `S-036` en la bitacora, con su fila en el indice de sesiones.
- **`_persistence/tasks.md`** — `T-158` a `T-161` pasan de `No implementada` a `Implementada`, cada
  una con su bloque de «Verificacion»; nacen `T-169`, `T-170` y `T-171`, las tres `Implementada`.
  Ademas, **encontradas por el Paso 2d de este mismo cierre**, cinco notas fechadas mas que corrigen
  bloques de esta misma sesion cuyas ordenes dejaron de reproducir por ediciones posteriores dentro
  de la propia jornada (detalle completo en la seccion 7).
- **`_persistence/techdebt.md`** — nace `DT-007`: 26 ocurrencias heredadas del defecto de `C-010` en
  `tasks.md`, `assumptions.md` y `findings.md`, propuesta como `Propuesta (pendiente del usuario)`.
- **`project.md`** — gana las dos filas de «Esqueleto de arranque» (repositorio y remoto) y su nota
  del caso invertido; se actualiza el numero y la enumeracion de rutas absolutas excepcionales (de
  dos a tres).

## 2. Que NO se hizo, y por que

Lista derivada del indice de `tasks.md`, filtrando `No implementada`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-039](#t-039---normalizar-a-no-implementada-el-estado-pendiente-de-t-037-y-t-038-f-029) | Normalizar a `No implementada` el estado `Pendiente` de `T-037` y `T-038` (`F-029`) | Implementada | Media | No bloqueante | `000_preproject` |
| [T-128](#t-128---hacer-la-cosecha-de-las-lecciones-de-la-etapa-preparatoria-d-123) | Hacer la cosecha de las lecciones de la etapa preparatoria (`D-123`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-144](#t-144---dar-dueño-y-sitio-a-evaluacion-y-observabilidad-lg-54-d-134) | Dar dueño y sitio a evaluacion y observabilidad (`LG-54`, `D-134`) | No implementada | Media | No bloqueante | `005_discovery` |
| [T-156](#t-156---construir-el-esqueleto-reutilizable-de-arranque-de-proyecto-d-143) | Construir el esqueleto reutilizable de arranque de proyecto (`D-143`) | No implementada | Alta | Bloqueante | `000_preproject` |
| [T-162](#t-162---escribir-la-guia-de-arranque-del-esqueleto) | Escribir la guia de arranque del esqueleto | No implementada | Media | No bloqueante | `000_preproject` |
| [T-163](#t-163---resolver-los-dos-huecos-menores-del-esqueleto-brief-y-temporal) | Resolver los dos huecos menores del esqueleto: brief y `temporal/` | No implementada | Baja | No bloqueante | `000_preproject` |
| [T-167](#t-167---anadir-a-la-plantilla-de-projectmd-las-dos-filas-del-esqueleto-de-origen-d-150) | Anadir a la plantilla de `project.md` las dos filas del esqueleto de origen (`D-150`) | No implementada | Media | No bloqueante | `000_preproject` |
```

⚠️ **La linea de `T-039` es un falso positivo del `grep`**: coincide porque su titulo contiene el
texto «`No implementada`» dentro de la frase «Normalizar a `No implementada` el estado `Pendiente`»,
pero su columna de estado real dice `Implementada`. Se deja en la salida pegada, sin filtrar a mano,
para no seleccionar sobre la evidencia — y se aclara aqui.

De las siete tareas realmente abiertas: `T-156` (la tarea paraguas del esqueleto, `Bloqueante`),
`T-162` y `T-163` (los dos ultimos huecos del esqueleto), `T-167` (llevar a la plantilla de
`project.md` las dos filas de `D-150`), `T-001` y `T-144` (aplazadas hasta que `005_discovery` este
activa) y `T-128` (la primera cosecha real, se hace al cerrar la etapa).

## 3. Decisiones tomadas

**`D-151` - Los patrones «mangled» del registro no se corrigen en masa: la regla rige hacia
adelante.** Al medir el alcance de `C-010` aparecen 26 ocurrencias ya commiteadas del mismo defecto
(barras invertidas perdidas al escribir por shell, quedando como `0x08`). Se decide no reescribirlas:
hacerlo convertiria «evidencia que no reproduce» en «evidencia falsa», porque el bloque afirmaria que
aquel dia se corrio una orden que aquel dia no se corrio. La regla nueva —copiar de vuelta del
archivo y correr antes de dar por buena una orden (`C-010`)— rige **hacia adelante**; lo ya
commiteado se registra como deuda (`DT-007`).

- **Alternativas descartadas:** (a) corregir las 26 en el sitio — produce evidencia falsa; (b)
  corregirlas anadiendo una nota a cada una — 26 notas que no aportan nada sobre lo que ya dice
  `L-057`, y enterrarian el registro en ruido; (c) no registrar nada y dejarlo pasar — el defecto
  volveria a descubrirse desde cero, que es como se descubrio esta vez.
- **Reversible a criterio** — no se borra ni se reescribe nada; es una decision de no actuar.

## 4. Supuestos vigentes y riesgos

Codigos de los supuestos abiertos, derivados del indice de `assumptions.md`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep -oE '\[A-[0-9]+\]' | tr -d '[]' | sort -u
[A-001]
[A-002]
[A-003]
[A-004]
[A-005]
[A-006]
[A-007]
[A-008]
[A-009]
[A-011]
[A-012]
[A-013]
[A-014]
[A-015]
[A-018]
[A-019]
```

De ellos, dos nacen o se tocan esta sesion:

- **`A-019` (nace)** — que `protocol-promote` funciona de punta a punta nunca se ha ejecutado. Sobre
  el descansa que el mecanismo del esqueleto sea un mecanismo y no un documento; si la skill no hace
  lo que dice, el cierre sigue detectando desfase sin que nadie pueda cerrarlo. Se refuta con la
  primera ejecucion real que no haga lo que la skill dice.
- **`A-018` (se toca, sigue `Abierto`)** — el primer ejercicio real de su disparador (el barrido de
  `T-158`, ocho diferencias, 21 lineas «solo en el esqueleto» inspeccionadas una por una) no lo
  refuta, pero **un caso no confirma un universal**: sigue abierto por diseño, hasta que aparezca una
  linea que si exija conciliacion.

🚨 **Riesgo nombrado en el propio trabajo de esta sesion y sin codigo que lo sostenga:** el Paso 2d
de este cierre encontro que **un bloque de `assumptions.md`** (la nota del 2026-09-11 sobre `T-158`,
que sostiene `A-018`) publica una orden cuya salida ya no reproduce, por el mismo mecanismo que las
demas (el esqueleto se sincronizo despues de que la nota se escribiera). `assumptions.md` no es un
archivo que el cierre pueda editar — es de los cuatro del porque. Ver seccion 6.

## 5. Siguiente tarea propuesta

Escribir `T-162` (la guia de arranque del esqueleto) y `T-163` (los dos huecos menores: `_brief/` y
`temporal/`), que son lo unico que le falta a `T-156` para poder cerrarse. Importancia Media, no
bloqueante — ya estan registradas como tales.

## 6. Que pedimos auditar

- **Un bloque de `_persistence/assumptions.md` (nota del 2026-09-11 sobre `T-158`, bajo `A-018`) ya
  no reproduce**, y este cierre no lo pudo corregir porque ese archivo no es suyo. La orden
  `diff --strip-trailing-cr "$ESQ/$f" "$f" | grep -c '^<'` publicaba `5 3 7 4 1 0 1` para los siete
  archivos listados; corrida hoy sobre el esqueleto ya sincronizado (`T-158`, commit `1748f0a`) da
  `4 0 0 0 0 0 0`. La causa es la misma que documentan las notas de `tasks.md` (el esqueleto cambio
  de estado **dentro de la misma sesion** en la que la nota se escribio) y la correccion es la misma
  —una nota fechada—, pero `assumptions.md` la escribe `manager`, no el cierre. Queda senalado aqui
  para que se corrija en la proxima sesion, y **no se ha tocado**.
- **El Paso 2d de este mismo cierre encontro cinco bloques de `tasks.md`, escritos en esta misma
  sesion, cuyas ordenes dejaron de reproducir por ediciones posteriores dentro de la propia
  jornada** (detalle en la seccion 7). Los cinco se corrigieron con notas fechadas antes del commit,
  porque `tasks.md` si es del cierre — pero es la sexta vez que aparece esta forma de defecto (el
  registro ya documentaba siete casos previos de la misma familia), y esta vez aparecio **dentro de
  la propia sesion que los escribio**, no entre sesiones. Vale la pena que la auditoria revise si el
  patron merece un mecanismo nuevo en `protocol-close`, en vez de seguir dependiendo de que el cierre
  siguiente los encuentre a mano.
- **`DT-007` puede solapar con `DT-003` a `DT-006`.** La propia entrada lo dice: no se hizo la
  reconciliacion linea a linea entre el barrido nuevo (26 ocurrencias en tres archivos) y las cuatro
  deudas anteriores (que documentan casos puntuales ya conocidos en algunos de esos mismos archivos).
  Es posible que algunas de las 26 ya estuvieran contadas en `DT-004`/`DT-006`.
- **`T-171` corrige una cuarta cita cruzada que la pasada de `T-168` no vio**, buscando solo la forma
  «Lo implementa `T-XXX`». No hay garantia de que no quede una quinta en otra redaccion todavia no
  buscada; el barrido de `T-171` solo cubrio las dos formas que ya se conocian.

## 7. Evidencia del Paso 2d

**Orden anclada al area de staging** (el commit de esta sesion todavia no existe al escribir esta
seccion; la version anclada al commit real, con `S-036.md` excluido, va en la NOTA DE CIERRE de esta
misma seccion, puesta por el Paso 7c):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
     1	+$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in CLAUDE.md     .claude/skills/protocol-audit/SKILL.md .claude/skills/protocol-close/SKILL.md     .claude/skills/protocol-start/SKILL.md _phases/000_preproject.md     _phases/005_discovery.md _templates/000_preproject/005_project.md; do     printf '%3d  %s
     2	+$ grep -c 'unicamente `manager`' .claude/skills/protocol-promote/SKILL.md
     3	+$ grep -nE '^## Paso 5 |^## Paso 6 ' .claude/skills/protocol-promote/SKILL.md
     4	+$ grep -c "Esqueleto de arranque —" project.md
     5	+$ grep -c "el esqueleto salio de este proyecto" project.md
     6	+$ grep -cE "^## Paso 2f |^## 9\. Evidencia del Paso 2f$|^Desfase con el esqueleto \(2f\)" .claude/skills/protocol-close/SKILL.md
     7	+$ python -c "print(sum(open(f,encoding='utf-8',newline='').read().count(chr(8)) for f in ['_persistence/tasks.md','_persistence/assumptions.md','_audit/findings.md']))"
     8	+$ python -c "
     9	+$ git show --name-only --format= cce48e0 | grep -xc "project.md"
    10	+$ cd C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS && git log --oneline -1
    11	+$ grep -rnoE "\b(T|D|F|A|C|L|S|DT|R|H|N|VS|TC)-[0-9]{3}\b" . --include=*.md | wc -l
    12	+$ grep -rcE "\b(T|D|F|A|C|L|S|DT|R|H|N|VS|TC)-[0-9]{3}\b" . --include=*.md | grep -v ":0$" | sort
    13	+$ grep -rhoE "\b(T|D|F|A|C|L|S|DT|R|H|N|VS|TC)-[0-9]{3}\b" . --include=*.md | sort -u | tr '\n' ' '
    14	+$ grep -rnoE "\b(T|D|F|A|C|L|S|DT|R|H|N|VS|TC)-[0-9]{3}\b" . --include=*.md \
    15	+$ cd C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS && git log --oneline -1
    16	+$ cd C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS && grep -rnoE "\b(T|D|F|A|C|L|S|DT|R|H|N|VS|TC)-[0-9]{3}\b" . --include=*.md | awk -F: '{print $1":"$2}' | uniq -c | awk '$1>1'
    17	+$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
    18	+$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
    19	+$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
    20	+$ cd "$ESQ" && git log --oneline -2
    21	+$ git show --stat --name-only --format= 1748f0a
    22	+$ git status -sb
    23	+$ cd "$ESQ"
    24	+$ grep -rnE "RaindomAI|Raidom|Proyectos_TripleS|github\.com" .claude CLAUDE.md _phases _methodology _templates _workflow
    25	+$ grep -rnoE '\b[A-Z]{1,2}-[0-9]+\b' _phases _workflow | grep -vE ':PI-[0-9]+$'
    26	+$ grep -rnE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|vercel|supabase|Company_" .claude CLAUDE.md _phases _methodology _templates _workflow
    27	+$ grep -rnE '[A-Za-z]:[/\]|https?://|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' .claude CLAUDE.md _phases _methodology _templates _workflow
    28	+$ f=".claude/skills/protocol-audit/SKILL.md"
    29	+$ for p in "$f" "$ESQ/$f"; do printf '%s  CR=%s  LF=%s\n' "$p" "$(tr -dc '\r' < "$p" | wc -c)" "$(tr -dc '\n' < "$p" | wc -c)"; done
    30	+$ grep -nE "Esqueleto de arranque" project.md
    31	+$ grep -c "el esqueleto salio de este proyecto" project.md
    32	+$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
    33	+$ test -d "$ESQ" && echo SI || echo NO
    34	+$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
    35	+$ grep -nE "^## Paso 2f |^## 9\. Evidencia|^Desfase con el esqueleto" .claude/skills/protocol-close/SKILL.md
    36	+$ ESQ="C:/ruta/que/no/existe/SDAI_TripleS"
    37	+$ for d in .claude _phases; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; echo "  exit=$?"; done
    38	+$ git grep -nE "RaindomAI|Raidom|Proyectos_TripleS|github\.com" -- .claude CLAUDE.md _phases _methodology _templates _workflow
    39	+$ sed -n '/^## Paso 2f /,/^## Paso 3 /p' .claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b'
    40	+$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
    41	+$ if [ ! -d "$ESQ" ]; then echo "SIN COMPROBAR - ..."; else for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md; fi
    42	+$ grep -c 'unicamente `manager`' .claude/skills/protocol-promote/SKILL.md
    43	+$ grep -c 'No lo delegas en ningun agente' .claude/skills/protocol-promote/SKILL.md
    44	+$ grep -nE '^## Paso [0-9]+ —' .claude/skills/protocol-promote/SKILL.md
    45	+$ git grep -nE "RaindomAI|Raidom|Proyectos_TripleS|github[.]com" -- .claude CLAUDE.md _phases _methodology _templates _workflow
    46	+$ grep -noE '\b[A-Z]{1,2}-[0-9]+\b' .claude/skills/protocol-promote/SKILL.md
    47	+$ grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|vercel|supabase|Company_" .claude/skills/protocol-promote/SKILL.md
    48	+$ git grep -n "unico protocolo del\|unico del andamiaje que" -- CLAUDE.md .claude/
    49	+$ git show --name-only --format= cce48e0 | grep -xc "project.md"
    50	+$ grep -cE 'T-1[7]0`, hallazgo `F-101' _persistence/tasks.md
    51	+$ grep -cE 'T-1[6]9`, hallazgo `F-100' _persistence/progress.md
    52	+$ grep -nE "T-1[7]1" _persistence/decisions.md
    53	+$ grep -nE "T-1[7]1" _persistence/decisions.md
    54	+$ python -c "print(sum(open(f,encoding='utf-8',newline='').read().count(chr(8)) for f in ['_persistence/tasks.md','_persistence/assumptions.md','_audit/findings.md']))"
```

**Recuento de lineas devueltas: 54.**

**Lineas repetidas** (`sort | uniq -d` sobre la misma lista):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
+$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
+$ cd C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS && git log --oneline -1
+$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
+$ git show --name-only --format= cce48e0 | grep -xc "project.md"
+$ grep -c "el esqueleto salio de este proyecto" project.md
+$ grep -c 'unicamente `manager`' .claude/skills/protocol-promote/SKILL.md
+$ grep -nE "T-1[7]1" _persistence/decisions.md
+$ python -c "print(sum(open(f,encoding='utf-8',newline='').read().count(chr(8)) for f in ['_persistence/tasks.md','_persistence/assumptions.md','_audit/findings.md']))"
```

**Reejecucion, linea por linea** (por numero de `cat -n`; las multi-linea se reejecutaron completas,
no solo su primera linea):

| # | Reproduce | Nota |
|---|---|---|
| 1 | 🚨 NO | En `assumptions.md`, no editable por el cierre. Publicaba `5 3 7 4 1 0 1`; hoy da `4 0 0 0 0 0 0` (el esqueleto se sincronizo despues, `T-158`). Ver seccion 6 |
| 2, 42 | Si | `1` |
| 3 | Si | lineas 217/241 |
| 4 | Si | `2` |
| 5, 31 | Si | `1` |
| 6 | Si | `3` |
| 7, 54 | Si | `26` |
| 8 | Si | (multilinea) `71 archivos, 0 ocurrencias` |
| 9, 49 | Si | `0` |
| 10 | 🚨 NO | Publicaba `fa7da56 ...`; hoy da `1748f0a ...` (el esqueleto se sincronizo mas tarde en la misma sesion, `T-158`). **Corregido con nota fechada en `tasks.md`**, bajo el bloque de `T-170` |
| 11 | Si | `53` |
| 12 | Si | mismas ocho lineas |
| 13 | Si | mismas quince formas |
| 14 | 🚨 NO | Publicaba `CLAUDE.md:252`; hoy da `CLAUDE.md:256` (mismo motivo que la 10: `CLAUDE.md` crecio despues, dentro de la sesion). **Corregido con nota fechada**, junto a la 10 |
| 15 | Si | `1748f0a ...` — es la orden de mi propia nota correctiva, reproduce lo que ella misma publica |
| 16 | Si | `256 810×4 45×2 71×2` — es la orden de mi propia nota correctiva |
| 17 | Si | (asignacion, sin salida que comparar) |
| 18 | 🚨 NO por diseño | Es la snapshot «Antes» de `T-158` (8 diferencias historicas); hoy, ya sincronizado, da 4 lineas distintas. No se corrige: esta explicitamente fechada como «antes de sincronizar» |
| 19 | 🚨 NO | Es el «Despues» de `T-158`, que publicaba «sin salida»; hoy da 4 lineas (`T-160`/`T-161` volvieron a tocar `protocol-close` y `CLAUDE.md` despues). **Corregido con nota fechada**, bajo el bloque de `T-159` |
| 20 | Si | `1748f0a`, `fa7da56` |
| 21 | Si | mismos 8 archivos |
| 22 | Si | `## main...origin/main` |
| 23 | Si | (cd, sin salida) |
| 24 | Si | vacio |
| 25 | Si | vacio |
| 26 | Si | vacio |
| 27 | Si | mismas 4 lineas de atribucion |
| 28 | Si | (asignacion) |
| 29 | Si | reproduce el estado «despues» ya anticipado en la propia prosa (`CR=0 LF=402`); el bloque pegado en el archivo es explicitamente el «antes» (`CR=0 LF=374`), etiquetado como tal |
| 30 | Si | lineas 41/42/64 |
| 32 | Si | `SI` |
| 33 | 🚨 NO | Publicaba «sin salida»; hoy difiere (`T-161` volvio a tocar `CLAUDE.md`). **Corregido con nota fechada**, bajo el bloque de `T-159` (misma causa que la 19) |
| 34 | Si | lineas 616/1224/1970 |
| 35 | Si | mismos dos `No such file or directory`, `exit=2` |
| 36 | Si | vacio |
| 37 | Si | vacio |
| 38 | Si | (asignacion) |
| 39 | 🚨 NO | Publicaba 1 linea («el propio paso naciendo»); hoy da 4 (`T-161` se sumo despues). **Corregido con nota fechada**, bajo el bloque de `T-160` (misma causa que la 19 y la 33) |
| 40 | Si | `1` |
| 41 | Si | mismas 9 lineas `## Paso N —` |
| 43 | Si | vacio |
| 44 | Si | vacio |
| 45 | Si | vacio |
| 46 | Si | vacio |
| 47 | Si | `0` |
| 48 | Si | `1` |
| 50 | Si | `1` |
| 51 | Si | `1` |
| 52 | 🚨 NO | Publicaba `9489:...`; hoy da `9512:...` (otras notas se anadieron despues a `decisions.md`, dentro de la sesion). La orden **anclada** al lado (`git show ce0ac4e:...`) si reproduce exacta. **Corregido con nota fechada**, bajo el bloque de `T-171` |
| 53 | Igual que la 52 | misma orden, mismo resultado y misma correccion |

📌 **Cinco causas distintas, un solo mecanismo:** las lineas 1, 10/14, 19/33, 39 y 52/53 fallan por
la misma razon estructural — una orden **sin ancla a un commit**, publicada sobre un objeto (el
esqueleto externo, o un archivo de esta misma sesion) que **siguio cambiando dentro de la propia
jornada**, despues de que la orden se corriera y su salida se pegara. Las cuatro que viven en
`tasks.md` se corrigieron con nota fechada, antes del `git add`, porque `tasks.md` es del cierre. La
que vive en `assumptions.md` no se pudo corregir por la misma razon que las otras cuatro tampoco eran
del cierre en un primer momento — pero esa si lo es, y esta no: queda senalada en la seccion 6.

> ## NOTA DE CIERRE (Paso 7c, despues del commit)
>
> **El commit de la sesion es `6fb6d39`.** La seccion 1 prometia su version anclada aqui mismo:
>
> ```
> $ git show --stat --name-only --format= 6fb6d39
> .claude/skills/protocol-close/SKILL.md
> .claude/skills/protocol-harvest/SKILL.md
> .claude/skills/protocol-promote/SKILL.md
> CLAUDE.md
> _audit/S-036.md
> _audit/findings.md
> _audit/index.md
> _persistence/assumptions.md
> _persistence/constraints.md
> _persistence/decisions.md
> _persistence/lessons.md
> _persistence/progress.md
> _persistence/tasks.md
> _persistence/techdebt.md
> project.md
> ```
>
> Quince archivos: los trece de la lista de staging de la seccion 1, mas `_audit/S-036.md` (este
> mismo informe) y `_audit/index.md` (su fila), que no podian estar en staging cuando la seccion 1 se
> escribio porque todavia no existian.
>
> **BARRIDO DE ANCLAJE — salida:** el Paso 7c-bis busco ordenes de «Criterio de cierre» con `<hash>`
> pendiente, nacidas esta sesion, en `decisions.md` y `tasks.md`. Los dos barridos que exige el
> recuadro del Paso 7c:
>
> ```
> $ for f in $(git diff --name-only 6fb6d39^ 6fb6d39 -- _persistence _audit ":(exclude)_audit/S-036.md"); do n=$(git diff -U0 6fb6d39^ 6fb6d39 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
> (sin salida)
> ```
>
> **CONTROL vacio: cero ordenes que anclar.** Ninguna decision ni tarea nacida esta sesion dejo un
> «Criterio de cierre» con la forma `$ git show <hash>:` pendiente — `D-151` no lleva orden en su
> criterio de cierre, y `T-169`/`T-170`/`T-171` ya usan hashes reales (`ce0ac4e`, `cce48e0`) en sus
> propios bloques de «Verificacion», escritos en la misma sesion en que se cerraron. Por eso el Paso
> 7c-bis **no modifico ni `decisions.md` ni `tasks.md`** en este commit de anclaje, y no hay desglose
> por entrada que publicar: el total de ordenes ancladas por este paso, esta sesion, es **cero**.
>
> El CENSO universal (informativo, no tiene que salir vacio: cuenta lineas con `<hash>` heredadas en
> todo el registro, este commit incluido) confirma que lo unico que queda con `<hash>` es historico,
> anterior a esta sesion:
>
> ```
> $ for f in $(git ls-tree -r --name-only 6fb6d39 _persistence _audit | grep -v '_audit/S-036.md'); do n=$(git show 6fb6d39:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
> _audit/R-025.md: 2
> _audit/R-026.md: 7
> _audit/R-027.md: 1
> _audit/R-029.md: 2
> _audit/R-030.md: 3
> _audit/R-031.md: 2
> _audit/R-032.md: 2
> _audit/R-033.md: 3
> _audit/R-034.md: 2
> _audit/R-035.md: 1
> _audit/S-017.md: 1
> _audit/S-024.md: 1
> _audit/S-025.md: 17
> _audit/S-026.md: 8
> _audit/S-027.md: 17
> _audit/S-029.md: 2
> _audit/S-030.md: 2
> _persistence/assumptions.md: 9
> _persistence/decisions.md: 16
> _persistence/tasks.md: 23
> ```
>
> **No queda ninguna orden de esta sesion sin anclar**, porque esta sesion no dejo ninguna: las 20
> lineas de archivo que aparecen en el CENSO son todas de sesiones anteriores a `S-036`, ya
> commiteadas, y `D-019` prohibe reescribirlas. El CONTROL —que si es la condicion de parada— salio
> vacio.
>
> **CONTROL DE PROSA BORRADA — salida:**
>
> ```
> $ outside() { awk '/^```/{f=!f; next} !f'; }
> $ for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | outside > /tmp/prosa_antes; outside < "$f" > /tmp/prosa_despues; echo "== $f =="; diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'; done
> == _persistence/decisions.md ==
> == _persistence/tasks.md ==
> ```
>
> Ninguna linea de prosa desaparecio — coherente con que este paso no toco ninguno de los dos
> archivos (el CONTROL de arriba salio vacio).
>
> **SEGUNDA PASADA anclada del Paso 2e — salida:**
>
> ```
> $ for f in $(git diff --name-only --diff-filter=d 6fb6d39^ 6fb6d39); do n=$(git show 6fb6d39:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
> _audit/findings.md: 1
> _persistence/assumptions.md: 4
> _persistence/decisions.md: 7
> _persistence/tasks.md: 5
>
> $ git diff --name-only --diff-filter=d 6fb6d39^ 6fb6d39 | wc -l
> 15
>
> $ for f in $(git diff --name-only --diff-filter=d 6fb6d39^ 6fb6d39); do git show 6fb6d39:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
> 17
> ```
>
> El commit final lleva **15** archivos, dos mas que los **13** de la tabla de la seccion 8
> (`_audit/S-036.md` y `_audit/index.md`, los dos con `0` ocurrencias). Los dos contrastes anclados
> cuadran igual que los de staging: `15` filas, suma `17`. La brecha que la seccion 8 declaraba
> —«los archivos que el cierre escribe despues quedan fuera por construccion»— queda cerrada aqui, y
> no cambia ninguna cifra: los dos archivos nuevos no contienen caracteres de control.
>
> **CONTROL DE CIFRA ADYACENTE — salida:** corrido sobre este mismo informe antes del `git add`
> (Paso 6b), con una correccion aplicada antes de commitear (la cifra de `DT-007` no era comparable
> con la de esta seccion, y se reescribio la frase para no afirmar una relacion que no existe). Vuelto
> a correr ahora, sobre el archivo ya commiteado, para dejar constancia:
>
> ```
> $ git show 6fb6d39:_audit/S-036.md | awk '
>   /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
>   infence { next }
>   armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
> '
> -:58:  7, junto con la version anclada de `git show --stat --name-only --format=`** (Paso 7c).
> -:119:  ⚠️ **La linea de `T-039` es un falso positivo del `grep`**: coincide porque su titulo contiene el
> -:282:  **Recuento de lineas devueltas: 54.**
> -:397:  Esta sesion no anade ninguna linea nueva de caracter de control: las 17 lineas de arriba son las
> -:419:  13 filas en la tabla, coincide con las 13 filas del contraste 1. Suma de la columna `staged`
> -:420:  (1+4+7+5 = 17), coincide con el contraste 2. Cuadra.
> -:434:  `CLAUDE.md` cambiaron esta sesion (el Paso 2f nacio, y la correccion de «uno de los dos protocolos»);
> ```
>
> Las siete lineas se leyeron una por una contra su bloque precedente en la seccion 6b, antes del
> commit: las siete citan una cifra que sale de la salida cruda inmediatamente anterior. Ninguna
> exigio correccion en esta pasada; la unica correccion la aplico la propia redaccion del Paso 6b,
> antes de escribir esta nota.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Las cuatro cifras son **heredadas**, identicas antes y despues de esta sesion (comparadas contra
`HEAD`):

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"
done
.claude/skills/protocol-close/SKILL.md head=0 staged=0
.claude/skills/protocol-harvest/SKILL.md head=0 staged=0
.claude/skills/protocol-promote/SKILL.md head=0 staged=0
CLAUDE.md head=0 staged=0
_audit/findings.md head=1 staged=1
_persistence/assumptions.md head=4 staged=4
_persistence/constraints.md head=0 staged=0
_persistence/decisions.md head=7 staged=7
_persistence/lessons.md head=0 staged=0
_persistence/progress.md head=0 staged=0
_persistence/tasks.md head=5 staged=5
_persistence/techdebt.md head=0 staged=0
project.md head=0 staged=0
```

Esta sesion no anade ninguna linea nueva de caracter de control: las 17 lineas de arriba son las
mismas, con la misma cifra, que ya estaban en `HEAD`.

⚠️ **Esta cifra (17 lineas con al menos una coincidencia, en cuatro archivos) no es directamente
comparable con la de `DT-007` (26 ocurrencias del caracter `chr(8)`, en tres de esos archivos).**
Miden cosas distintas: esta cuenta **lineas** que contienen algun caracter de un rango mas amplio
(`grep -c`), `DT-007` cuenta **apariciones** de un caracter especifico (`str.count` en Python) y no
incluye `decisions.md`. Que una linea tenga mas de una ocurrencia, o que `decisions.md` aporte
ocurrencias fuera del alcance de `DT-007`, basta para que las dos cifras no cuadren entre si — y no
tienen por que.

**Los dos contrastes de la tabla:**

```
$ git diff --cached --name-only --diff-filter=d | wc -l
13

$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done \
  | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
```

13 filas en la tabla, coincide con las 13 filas del contraste 1. Suma de la columna `staged`
(1+4+7+5 = 17), coincide con el contraste 2. Cuadra.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ if [ ! -d "$ESQ" ]; then echo "SIN COMPROBAR - la ruta del esqueleto no existe en esta maquina: $ESQ"; else for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md; fi
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-harvest/SKILL.md and .claude/skills/protocol-harvest/SKILL.md differ
Only in .claude/skills: protocol-promote
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/CLAUDE.md and CLAUDE.md differ
```

**Cuatro lineas, y son promociones pendientes, no un fallo del cierre.** `protocol-close` y
`CLAUDE.md` cambiaron esta sesion (el Paso 2f nacio, y la correccion de «uno de los dos protocolos»);
`protocol-promote` es un archivo nuevo que el esqueleto todavia no tiene. Ninguna linea es `Only in`
el arbol del esqueleto, asi que no hay hallazgo: las cuatro son candidatas a la primera ejecucion
real de `protocol-promote` (`T-161`, `A-019`), que el usuario tiene que pedir.

**Recuento derivado de la propia salida:** 4 lineas.
