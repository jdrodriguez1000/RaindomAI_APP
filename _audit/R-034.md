# Auditoria R-034 — sobre S-034

| Campo | Valor |
|---|---|
| Auditoria | R-034 |
| Informe auditado | `_audit/S-034.md` |
| Commit auditado | `2cef150` |
| Fecha | 2026-09-11 |
| Veredicto | Con hallazgos (2) |

⚠️ **El commit auditado es el sustantivo, no el derivado.** `git log -1 --format=%h -- _audit/S-034.md`
devuelve `3e34c27`, que es el **commit de anclaje** del Paso 7c (tres archivos: el informe mas
`decisions.md` y `tasks.md`, los dos que el 7c-bis esta autorizado a tocar). Manda el literal de la
cabecera del informe:

```
$ git log -1 --format=%h -- _audit/S-034.md
3e34c27
$ git show 3e34c27:_audit/S-034.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `2cef150` |
$ git show --stat --name-only --format= 2cef150 | grep -c .
12
$ git show --stat --name-only --format= 3e34c27 | grep -c .
3
```

Los 12 archivos de `2cef150` coinciden con la lista de la seccion 1 del informe. El texto del informe
se lee en su version anclada (`3e34c27`), que es la publicada.

## 1. Que se comprobo

### a) Indice ↔ detalle en `_persistence/` — coherente

```
$ H=2cef150; for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show $H:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show $H:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete sin diferencias.

### b) Carpetas del arbol contra las declaradas — las dos diferencias conocidas, con motivo escrito

```
$ diff <(git ls-tree -d --name-only 2cef150 | sed 's|$|/|' | sort) \
       <(git show 2cef150:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos tienen su razon escrita en `project.md` (`010_prototype/` declarada por adelantado, `D-061`;
`temporal/` excluida en `.gitignore`). No es hallazgo.

### c) Fuga de datos propios en las seis areas agnosticas — cero lineas

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" 2cef150 -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida)
```

### d) Codigos instanciados en `_phases/` y `_workflow/` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 2cef150 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
(sin salida)
```

### e) Fecha escrita contra la del commit (Paso 7d) — coincide

```
$ git log -1 --format=%ad --date=short 2cef150
2026-09-11
$ git log -1 --format=%ad --date=short 3e34c27
2026-09-11
$ git show 3e34c27:_audit/S-034.md | grep -m1 '^| Fecha |'
| Fecha | 2026-09-11 |
```

### f) La seccion 1 contra el diff — respaldada archivo por archivo

```
$ git diff 2cef150^ 2cef150 -- _persistence/tasks.md | grep -oE '^\+### T-[0-9]+' | sort -u
+### T-152
+### T-153
+### T-154
+### T-155
+### T-156
+### T-157
+### T-158
+### T-159
+### T-160
+### T-161
+### T-162
+### T-163
$ git diff 2cef150^ 2cef150 -- _persistence/decisions.md | grep -oE '^\+### D-[0-9]+' | sort -u
+### D-144
+### D-145
+### D-146
+### D-147
+### D-148
$ git diff 2cef150^ 2cef150 -- _persistence/lessons.md | grep -oE '^\+### L-[0-9]+' | sort -u
+### L-052
+### L-053
+### L-054
$ git diff 2cef150^ 2cef150 -- _persistence/assumptions.md | grep -oE '^\+### A-[0-9]+' | sort -u
+### A-018
$ git diff 2cef150^ 2cef150 -- _persistence/constraints.md | grep -oE '^\+### C-[0-9]+' | sort -u
(sin salida)
$ git diff 2cef150^ 2cef150 -- _persistence/progress.md | grep -oE '^\+### S-[0-9]+' | sort -u
+### S-034
```

Coincide con lo que la seccion 1 enumera, incluido «no nace ningun `C-XXX` nuevo».

Recuento de hunks de la seccion 1, reejecutado contra el commit en vez del area de staging:

```
$ for f in _persistence/decisions.md _persistence/tasks.md _persistence/assumptions.md _persistence/constraints.md _persistence/lessons.md _audit/findings.md _audit/S-033.md project.md; do echo "$f: $(git diff 2cef150^ 2cef150 -- "$f" | grep -c '^@@')"; done
_persistence/decisions.md: 2
_persistence/tasks.md: 2
_persistence/assumptions.md: 3
_persistence/constraints.md: 2
_persistence/lessons.md: 2
_audit/findings.md: 4
_audit/S-033.md: 2
project.md: 2
```

Identico a lo publicado.

### g) Secciones 2 y 4 — las cifras cuadran

```
$ git show 2cef150:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -cE '\| No implementada \|'
11
$ git show 2cef150:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -E '^\| \[T-003\]' | cut -d'|' -f4
 Suspendida
$ git show 2cef150:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -cE '\| Abierto \|'
15
```

Once tareas `No implementada` (lo que el informe dice), `T-003` fuera por estar `Suspendida` (lo que
el informe dice), y quince supuestos `Abierto` — los quince que el bloque de la seccion 4 lista.

### h) El BARRIDO DE ANCLAJE de la NOTA DE CIERRE — reproduce entero

```
$ for f in $(git ls-tree -r --name-only 2cef150 _persistence _audit | grep -v '_audit/S-034.md'); do
  n=$(git show 2cef150:"$f" | grep -cE '^\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-025.md: 2
_audit/R-026.md: 7
_audit/R-027.md: 1
_audit/R-029.md: 2
_audit/R-030.md: 3
_audit/R-031.md: 2
_audit/R-032.md: 2
_audit/R-033.md: 3
_audit/S-017.md: 1
_audit/S-024.md: 1
_audit/S-025.md: 17
_audit/S-026.md: 8
_audit/S-027.md: 17
_audit/S-029.md: 2
_audit/S-030.md: 2
_persistence/assumptions.md: 9
_persistence/decisions.md: 20
_persistence/tasks.md: 34

$ for f in $(git diff --name-only 2cef150^ 2cef150 -- _persistence _audit ":(exclude)_audit/S-034.md"); do
  n=$(git diff -U0 2cef150^ 2cef150 -- "$f" | grep -cE '^\+\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
_persistence/decisions.md: 4
_persistence/tasks.md: 11

$ awk '/^### D-/{d=$2} /^### T-/{d=$2} /^\$ git show 2cef150:/{c[d]++} END{for (k in c) print k": "c[k]}' <(git show 3e34c27:_persistence/decisions.md) <(git show 3e34c27:_persistence/tasks.md) | sort
D-144: 4
T-152: 3
T-153: 3
T-154: 2
T-155: 3

$ awk '/^### D-144/{f=1} /^### D-149/{f=0} f' <(git show 3e34c27:_persistence/decisions.md) | grep -cE '^\$ .*<hash>'
0
$ awk '/^### T-152/{f=1} /^### T-164/{f=0} f' <(git show 3e34c27:_persistence/tasks.md) | grep -cE '^\$ .*<hash>'
0
```

Las salidas reproducen exactamente lo publicado, y `4+3+3+2+3=15` cuadra con el CONTROL.
**Ninguna orden de las entradas nacidas esta sesion queda sin anclar.** El commit de anclaje tampoco
introduce ninguna nueva:

```
$ git diff -U0 2cef150 3e34c27 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+$ git show --stat --name-only 2cef150
```

La unica linea es la orden de la seccion 1 — anclada, pero escrita de forma que el filtro no la
reconoce. Es el hallazgo `F-098`.

### i) CONTROL DE PROSA BORRADA, incluido el tramo del anclaje — sin lineas `<`

```
$ outside() { awk '/^```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show 2cef150:"$f" | outside > /tmp/pa; git show 3e34c27:"$f" | outside > /tmp/pd
  echo "== $f =="; diff /tmp/pa /tmp/pd | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

En `_audit/S-033.md` —un informe ya auditado— tampoco se borro nada:

```
$ git diff 2cef150^ 2cef150 -- _audit/S-033.md | grep -c '^-[^-]'
0
```

Las dos correcciones entraron **solo por nota fechada**, como declaran `T-153` y `T-154`.

### j) SEGUNDA PASADA del Paso 2e — reproduce

```
$ for f in $(git diff --name-only --diff-filter=d 2cef150^ 2cef150); do
  n=$(git show 2cef150:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5
$ git diff --name-only --diff-filter=d 2cef150^ 2cef150 | wc -l
12
$ for f in $(git diff --name-only --diff-filter=d 2cef150^ 2cef150); do git show 2cef150:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
```

`1+4+7+5=17` y 12 archivos: identico a lo publicado, y la brecha 11→12 esta declarada por el propio
informe.

### k) CONTROL DE CIFRA ADYACENTE — reproduce, y tambien sobre la version final

Sobre `2cef150`, las trece lineas publicadas salen identicas. Corrido ademas sobre la version
**anclada** (`3e34c27`), que es la que se publica y que la NOTA DE CIERRE advierte que ya no coincide:

```
$ git show 3e34c27:_audit/S-034.md > /tmp/s034f.md
$ awk '
  /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' /tmp/s034f.md
/tmp/s034f.md:41: Incluye `_audit/S-034.md`, este propio informe: se anadio al `git add` despues de generarse una
/tmp/s034f.md:84: `assumptions.md` tiene 3 hunks (indice + nota en `A-014` + entrada nueva `A-018`) y `findings.md`
/tmp/s034f.md:85: tiene 4 (la tabla + las tres entradas de detalle `F-095`/`F-096`/`F-097`); los demas son
/tmp/s034f.md:108: Once tareas abiertas. Las ocho de codigo `T-156` a `T-163` son nuevas de esta sesion: la decision de
/tmp/s034f.md:109: construir el esqueleto de arranque (`D-143`, sesion anterior) ya tiene ahora sus cuatro decisiones de
/tmp/s034f.md:184: `A-018` nace esta sesion, apoyado en `D-146`. No es una observacion: es una deduccion sobre una
/tmp/s034f.md:233: Lista COMPLETA, numerada por la propia orden (30 lineas, ninguna deduplicada):
/tmp/s034f.md:275: Son los pares (3,20) y (4,21): el bloque «Criterio de cierre» de `D-144` y el de `T-155`, que la
/tmp/s034f.md:473: coinciden en los 11 archivos que el commit toca hasta este punto):
/tmp/s034f.md:492: Estas cifras heredadas son las mismas que sostienen `DT-003` a `DT-006` (`No implementada`,
/tmp/s034f.md:505: La tabla tiene 11 filas (coincide con el contraste 1) y sus cifras suman `1+4+7+5=17` (coincide con
/tmp/s034f.md:506: el contraste 2). Cuadra.
```

Doce lineas, todas con su cifra derivada de la salida que las precede — las cifras (3 hunks, 4 hunks,
11 tareas, 30 lineas, 11 archivos, 17) verificadas arriba una por una. **El control pasa tambien
sobre la version publicada.**

### l) Los cuatro rotulos del Paso 7c-ter, sobre el informe publicado — los cuatro estan

```
$ for m in "**BARRIDO DE ANCLAJE — salida:**" "**CONTROL DE PROSA BORRADA — salida:**" "**SEGUNDA PASADA anclada del Paso 2e — salida:**" "**CONTROL DE CIFRA ADYACENTE — salida:**"; do
  echo "$(git show 3e34c27:_audit/S-034.md | grep -cF "$m")  $m"
done
2  **BARRIDO DE ANCLAJE — salida:**
2  **CONTROL DE PROSA BORRADA — salida:**
2  **SEGUNDA PASADA anclada del Paso 2e — salida:**
2  **CONTROL DE CIFRA ADYACENTE — salida:**
```

Dos ocurrencias porque el propio informe cita los cuatro rotulos dentro de la orden del Paso 2d
(linea 23 de su lista). El control del 7c-ter pasa en su estreno.

### m) El Paso 2d, reejecutado contra el commit — 30 lineas identicas

```
$ git diff -U0 2cef150^ 2cef150 -- _persistence _audit ":(exclude)_audit/S-034.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
30
```

La lista numerada coincide linea por linea con la publicada.

### n) Las ordenes de los criterios de cierre — los trece valores declarados reproducen

```
$ git show 2cef150:.claude/skills/protocol-close/SKILL.md | grep -c '^### 7c-ter'
1
$ git show 2cef150:.claude/skills/protocol-close/SKILL.md | grep -c 'FALTA en la NOTA DE CIERRE'
1
$ git show 2cef150:.claude/skills/protocol-close/SKILL.md | grep -c 'el Paso 7c-ter los busca por esa'
1
$ git show 2cef150:.claude/skills/protocol-close/SKILL.md | grep -coF '— salida:**'
5
$ git show 2cef150:.claude/skills/protocol-close/SKILL.md | grep -c 'el 7c-ter no deja commitear el anclaje sin ella'
1
$ git show 2cef150:project.md | grep -cE '^\| `(FT|SC)-XXX`.*la etapa no esta adoptada'
0
$ git show 2cef150:project.md | grep -cE '^\| `(FT|SC)-XXX`.*la etapa esta declarada, no iniciada'
2
$ git show 2cef150:project.md | grep -c 'Nota del 2026-09-11 (`F-095`, `T-152`)'
1
$ git show 2cef150:_audit/S-033.md | grep -c 'Nota del 2026-09-11 (`F-096`, `T-153`)'
1
$ git show 2cef150:_audit/S-033.md | grep -c '^91a59c8: 152'
1
$ git show 2cef150:_audit/S-033.md | grep -c '^91a59c8^: 148'
1
$ git show 2cef150:_audit/S-033.md | grep -c 'Nota del 2026-09-11 (`F-097`, `T-154`)'
1
$ git show 2cef150:_audit/S-033.md | grep -c 'contraste 2). Cuadra.'
2
```

Los trece valores son exactamente los que `D-144` y `T-152` a `T-155` declaran (`1,1,1,5` / `0,2,1` /
`1,1,1` / `1,2` / `1,1,1`).

El Paso 7c-ter existe con su condicion de parada, y **distingue los dos casos** — reejecutada la
prueba de `T-155`:

```
$ ROTULOS=("**BARRIDO DE ANCLAJE — salida:**" "**CONTROL DE PROSA BORRADA — salida:**" "**SEGUNDA PASADA anclada del Paso 2e — salida:**" "**CONTROL DE CIFRA ADYACENTE — salida:**")
$ for m in "${ROTULOS[@]}"; do grep -qF "$m" _audit/S-033.md || echo "FALTA en la NOTA DE CIERRE: $m"; done
FALTA en la NOTA DE CIERRE: **BARRIDO DE ANCLAJE — salida:**
FALTA en la NOTA DE CIERRE: **CONTROL DE PROSA BORRADA — salida:**
FALTA en la NOTA DE CIERRE: **SEGUNDA PASADA anclada del Paso 2e — salida:**
FALTA en la NOTA DE CIERRE: **CONTROL DE CIFRA ADYACENTE — salida:**
```

⚠️ **Nota:** el caso que falla sigue fallando sobre la version de `S-033` **ya corregida por notas
fechadas** en este mismo commit, porque `T-155` declara expresamente que el control rige hacia
adelante y los informes publicados no se reescriben para llevar los rotulos. Coherente.

### o) El estado del esqueleto (`T-156`, `D-145`, `D-148`) — reproduce hoy

```
$ S=C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS
$ R=C:/Users/USUARIO/Documents/Company_TripleS/Proyectos_TripleS/RaidomAI_App
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq "$S/$d" "$R/$d"; done; diff -q "$S/CLAUDE.md" "$R/CLAUDE.md"
Files .../SDAI_TripleS/.claude/skills/protocol-audit/SKILL.md and .../RaidomAI_App/.claude/skills/protocol-audit/SKILL.md differ
Files .../SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .../RaidomAI_App/.claude/skills/protocol-close/SKILL.md differ
Only in .../RaidomAI_App/.claude/skills: protocol-harvest
Files .../SDAI_TripleS/.claude/skills/protocol-start/SKILL.md and .../RaidomAI_App/.claude/skills/protocol-start/SKILL.md differ
Files .../SDAI_TripleS/_phases/000_preproject.md and .../RaidomAI_App/_phases/000_preproject.md differ
Files .../SDAI_TripleS/_phases/005_discovery.md and .../RaidomAI_App/_phases/005_discovery.md differ
Files .../SDAI_TripleS/_templates/000_preproject/005_project.md and .../RaidomAI_App/_templates/000_preproject/005_project.md differ
Files .../SDAI_TripleS/CLAUDE.md and .../RaidomAI_App/CLAUDE.md differ

$ (cd "$S" && git log --oneline -1) || echo "(sin git)"
fatal: not a git repository (or any of the parent directories): .git
(sin git)

$ (cd "$S" && grep -rniE "raidom|raindom|Proyectos_TripleS|github.com|vercel" --include=*.md .)
(sin salida)
```

Las ocho lineas son las mismas que `T-156` publica, y `T-157` sigue sin hacerse: el esqueleto no es
todavia un repositorio. Coherente con que las ocho tareas del esqueleto esten `No implementada`.

⚠️ **Estas ordenes leen un directorio en disco, no el commit.** Reproducen hoy porque nada las ha
tocado; no son evidencia anclada, y el propio informe lo declara en su seccion 6.

### p) `A-018` no se trata como hecho en ninguna parte

```
$ git grep -n 'A-018' 2cef150 -- _persistence project.md
2cef150:_persistence/assumptions.md:30:| [A-018](#a-018---...) | Toda diferencia entre las areas agnosticas del proyecto y las del esqueleto es una promocion pendiente | 2026-09-11 | Abierto |
2cef150:_persistence/assumptions.md:1068:### A-018 - Toda diferencia entre las areas agnosticas del proyecto y las del esqueleto es una promocion pendiente
2cef150:_persistence/decisions.md:9405:  hecho: ver `A-018`. Si algun dia aparece una diferencia que no se puede promover, la premisa cayo.
2cef150:_persistence/progress.md:142:Nace `A-018`: que toda diferencia que el barrido de `D-148` encuentre sea siempre una promocion
2cef150:_persistence/progress.md:155:  `A-006` a `A-009`, `A-011` a `A-015` y ahora `A-018` quedan abiertos; `A-016`/`A-017` siguen
2cef150:_persistence/progress.md:188:sigue sin comprobar de fondo, por muestreo. `A-018` no tiene un momento de comprobacion aparte: se
```

La unica mencion desde `D-146` lo nombra explicitamente como premisa que puede caer. La entrada
declara «el caso todavia no ha ocurrido ni una vez», y trae forma de refutacion y disparador.
**Correctamente registrado como supuesto.** Sin hallazgo.

### q) La seccion 0 no usa `Implementado` — correcto

Los tres veredictos son `Aceptado — corregido en este commit`, y `findings.md` los deja en
`Aceptado — pendiente`. Es exactamente lo que `F-094` pedia, y esta vez se cumple.

## 2. Hallazgos

### F-098 — La seccion 1 publica una orden que no es la que produjo la salida pegada
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-034.md`, seccion 1 |

- **Que se observo:** el informe abre su seccion 1 con `$ git show --stat --name-only 2cef150` y pega
  debajo los doce nombres de archivo, a secas. Esa orden no devuelve eso: devuelve ademas ocho lineas
  de cabecera del commit.

```
$ git show --stat --name-only 2cef150 | head -9
commit 2cef150aec49e66bb78a955175da27cc0be1406a
Author: Triple S <110043648+jdrodriguez1000@users.noreply.github.com>
Date:   Fri Sep 11 06:55:41 2026 -0500

    S-034: se aceptan F-095 a F-097 (T-152 a T-155) y nace el Paso 7c-ter (D-144); el esqueleto de arranque pasa a ser repositorio propio (D-145 a D-148, T-156 a T-163)

    Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_...

.claude/skills/protocol-close/SKILL.md
```

  La orden que si produce lo publicado es la que `protocol-close` prescribe, con `--format=`, y es la
  que usaron los cuatro informes anteriores:

```
$ git show 2cef150:.claude/skills/protocol-close/SKILL.md | grep -n 'show --stat --name-only --format='
941:git show --stat --name-only --format= <commit>          # seccion 1: archivos tocados
1067:<`git show --stat --name-only --format= <commit>`>
1436:| **Seccion 1** del informe, nota de cierre | la lista de archivos anclada al commit | `git show --stat --name-only --format= <hash>` |

$ for s in 030 031 032 033 034; do echo "S-$s: $(grep -m1 -oE 'git show --stat[^`|]*' _audit/S-$s.md)"; done
S-030: git show --stat --name-only --format= 9b3f9ee
S-031: git show --stat --name-only --format= 30ce070
S-032: git show --stat --name-only --format= a6279c7
S-033: git show --stat --name-only --format= 91a59c8
S-034: git show --stat --name-only 2cef150
```

- **Por que importa:** es la misma clase de defecto que `F-096` abrio la sesion anterior —una salida
  cruda que no reproduce— y que esta misma sesion acepto y corrigio, escribiendo en `T-153` que «una
  salida cruda recortada a mano deja de ser evidencia y pasa a ser una afirmacion con formato de
  evidencia». Aqui la salida es correcta y la orden no; el efecto para quien reejecuta es el mismo:
  obtiene algo distinto de lo publicado y tiene que averiguar por su cuenta si el informe escondio
  algo. Y es una **regresion**: los cuatro informes anteriores llevaban la orden buena. El fondo no
  se ve afectado —los doce archivos son los correctos, verificado en 1(f)—, y por eso es Media y no
  Alta.
- **Que lo corregiria:** una nota fechada en `_audit/S-034.md` con la orden completa y su salida, sin
  reescribir la prosa; y valorar si el Paso 7c debe fijar esa orden por cadena literal comprobable,
  como `D-144` acaba de hacer con los rotulos, en vez de dejarla a que se copie bien.

### F-099 — La seccion 1 describe el cambio de `findings.md` con un fragmento roto y una etiqueta falsa
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-034.md`, seccion 1, vineta de `_audit/findings.md` |

- **Que se observo:**

```
$ git show 3e34c27:_audit/S-034.md | sed -n '52,54p'
- **`_audit/findings.md`** — `F-095` (nace) no; **`F-095`, `F-096`, `F-097` (notas anadidas)**: las
  tres filas de la tabla pasan de `Abierto` a `Aceptado — pendiente`, y las tres entradas de detalle
  ganan su `Registrado en`.
```

  Dos defectos en la misma linea. «`F-095` (nace) no;» es un residuo de edicion sin significado —y
  afirma, hasta que la niega, que `F-095` nace en esta sesion, cuando nacio en `R-033`—. Y «(notas
  anadidas)» es falso: en `findings.md` no se anadio ninguna nota. Lo que ocurrio esta bien dicho en
  la frase que sigue, y el diff lo confirma:

```
$ git diff 2cef150^ 2cef150 -- _audit/findings.md | grep -cE '^\+.*Nota del'
0
$ git diff 2cef150^ 2cef150 -- _audit/findings.md | grep -cE '^\+\| Estado \| Aceptado — pendiente \|'
3
```

- **Por que importa:** la seccion 1 es el mapa que usa la auditoria siguiente para saber que mirar en
  cada archivo. Una etiqueta que dice «notas anadidas» donde hubo cambios de estado manda a buscar lo
  que no esta, y un «(nace) no» obliga al lector a decidir cual de las dos mitades de la frase vale.
  Es Baja porque la descripcion correcta esta en la misma vineta, dos palabras despues.
- **Que lo corregiria:** una nota fechada que fije la descripcion correcta —tres filas de `Abierto` a
  `Aceptado — pendiente` y tres `Registrado en`, ninguna nota—, sin reescribir la prosa publicada.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-095` | **Implementado** en `2cef150` | La razon caduca desaparece de las dos filas y la nota fechada esta: el patron viejo da `0`, el nuevo da `2`, y la nota da `1`. Verificado en 1(n) |
| `F-096` | **Implementado** en `2cef150` | La nota fechada del final de la seccion 2 de `_audit/S-033.md` publica las dos salidas derivadas, y las dos reproducen: filtro de texto libre `4` filas, filtro por columna `3`, y recuento `148` / `152`. Reejecutado abajo |
| `F-097` | **Implementado** en `2cef150` | La nota fechada del final de la seccion 7 de `_audit/S-033.md` publica la salida entera del CONTROL DE CIFRA ADYACENTE, y las trece lineas reproducen exactas. Ademas nace el mecanismo (`D-144`, `T-155`, Paso 7c-ter), verificado en 1(l) y 1(n) |

Reejecucion de lo que cierra `F-096` y `F-097`:

```
$ git show 91a59c8:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -c "No implementada"
4
$ git show 91a59c8:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -cE '\| No implementada \|'
3
$ for h in 91a59c8^ 91a59c8; do echo "$h: $(git show $h:_persistence/tasks.md | grep -c '^### T-')"; done
91a59c8^: 148
91a59c8: 152

$ git show 91a59c8:_audit/S-033.md > /tmp/s.md
$ awk '
  /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' /tmp/s.md | wc -l
13
```

Las cuatro cifras (`4`, `3`, `148`/`152`, `13`) son las que las dos notas fechadas publican, y las
trece lineas salen identicas a las pegadas en la nota de `T-154`.

⚠️ **Los tres se cierran por la correccion, no por la tarea.** `T-152` a `T-155` figuran
`Implementada`, pero lo que cierra un hallazgo es que la correccion este en el commit y reproduzca, y
eso es lo que se verifico arriba.

## 4. Lo que NO se pudo comprobar

Ningun control fallo. Dos matices, que no son un `SIN COMPROBAR` pero conviene que consten:

- Las ordenes sobre el esqueleto (`C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS`) leen un
  directorio en disco, fuera de este repositorio y sin historial. Reproducen hoy —verificado en
  1(o)—, pero **no son evidencia anclada** y dejaran de serlo en cuanto alguien toque esa carpeta. El
  propio informe lo declara en su seccion 6.
- El punto que la seccion 6 llama «nuestro punto mas debil» —que `D-145` a `D-148` sean decision pura
  sin una linea construida— **es cierto y no es un defecto**: no hay nada que auditar todavia porque
  no hay nada escrito. Lo unico verificable hoy es que las cuatro estan registradas con su
  clasificacion reversible/irreversible leida de `C-009`, y lo estan.

## 5. Recomendaciones sin hallazgo

- **La nota fechada de `C-009` quedo intercalada entre dos vinetas de la entrada.** Se inserto entre
  «Que la origina» y «Se levanta cuando», partiendo la lista de campos:

```
$ git show 2cef150:_persistence/constraints.md | sed -n '256,268p' | grep -nE '^- \*\*|^📌'
2:- **Que la origina:** `T-037`, y una leccion global que pide escribir el inventario **antes** de
5:📌 **Nota del 2026-09-11 (`D-145`): la primera tabla crece por primera vez, y por la via prevista.**
9:- **Se levanta cuando:** no se levanta por si sola; crece.
```

  No cambia lo que dice ni lo hace menos cierto; la convencion en el resto del registro es que la
  nota fechada va **al final** de la entrada. Vale la pena fijarlo como criterio — no reescribir
  esta.

- **`D-146` y `D-147` cierran con un criterio en prosa, sin orden escrita.** Es legitimo: su criterio
  mira algo que todavia no existe. Pero las otras tres decisiones de la misma tanda si dejaron la
  orden escrita aunque no se pudiera correr, y escribirla el dia que se toma la decision es mas
  barato que reconstruirla el dia que toque cerrarla.

- **`T-156` habla de «ocho archivos por detras» y la salida son ocho lineas, una de las cuales es una
  carpeta** (`Only in …/.claude/skills: protocol-harvest`). Imprecision de una palabra en una tarea
  que aun no se ha ejecutado; se corrige sola cuando `T-158` se haga.
