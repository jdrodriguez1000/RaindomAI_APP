# Informe de auditoria — S-039

| Campo | Valor |
|---|---|
| Sesion | S-039 |
| Fecha | 2026-09-14 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | PENDIENTE — se rellena en el Paso 7c con el hash literal del commit sustantivo de esta sesion |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`d35bc91`, el commit de la auditoria `R-038`), sobre el estado que
`e822ae3` (el commit sustantivo de `S-038`) dejo:

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-107 — la nota que corrige `F-105` publica, en `assumptions.md` y `tasks.md`, una cifra que ya no reproducia en su propio commit, y ademas dice evaluada una recomendacion sin evaluar | Aceptado — corregido en este commit | `T-180`, nota fechada bajo `A-018` y nota fechada bajo `T-176`, en este commit |
| F-108 — el bloque «Procedencia por archivo» de la seccion 7 de `S-038` contiene una linea que su orden no produce, y la tabla de reejecucion atribuye tres filas al archivo equivocado | Aceptado — corregido en este commit (la correccion); no se implementa (la regla nueva) | `T-181` para la correccion, en este commit; `D-157` para el rechazo de la mitad hacia adelante del hallazgo |

## 1. Que se hizo

Mientras el commit no existe, esta lista sale del area de staging (se ancla al hash en el Paso 7c):

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
_audit/S-038.md
_audit/S-039.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_templates/000_preproject/015_tasks.md
```

Con codigos y rutas, lo que muestra el diff:

- **`_persistence/decisions.md`** — 3 hunks (`git diff --cached -U0 -- _persistence/decisions.md | grep -c '^@@'`, corrido antes del commit). Nacen `D-157` y `D-158`; se edita la entrada existente `D-153` (nota fechada del 2026-09-14 sobre el criterio de cierre de la promocion); se anade la fila de ambas al indice.
- **`_persistence/tasks.md`** — 6 hunks. Nacen `T-180` y `T-181`; se editan las entradas existentes `T-176` (nota fechada bajo su verificacion) y `T-179` (pasa de `No implementada` a `Implementada`, con nota fechada de `D-158`); se anaden las filas nuevas y se actualiza la fila de `T-179` en el indice; la tabla de `Origen` gana el valor `session-closer`.
- **`_persistence/assumptions.md`** — 3 hunks. Nace `A-021`; se edita la entrada existente `A-018` (segunda nota fechada, corrigiendo la de `T-176`); se anade la fila de `A-021` al indice.
- **`_persistence/lessons.md`** — 1 hunk. No nace ninguna entrada; se edita la existente `L-056` con una nota de reincidencia (tres formas del mismo defecto en `T-180`).
- **`_audit/findings.md`** — 5 hunks. No nace ninguna entrada; se editan las fichas y filas de `F-107` y `F-108` (`Abierto` → `Aceptado — pendiente`, con su `Registrado en`).
- **`_audit/S-038.md`** — 1 hunk. Se anade una nota fechada en la seccion 7, antes de la NOTA DE CIERRE, con la procedencia real y la atribucion corregida (implementa `T-181`).
- **`.claude/skills/protocol-close/SKILL.md`** — edita el Paso 2d (nueva regla: abrir tarea cuando la orden vive en un archivo ajeno), el Paso 4 (nuevo valor de `Origen`) y la linea de controles del reporte del Paso 8. Implementa `D-158`.
- **`_templates/000_preproject/015_tasks.md`** — edita la tabla de `Origen` y su explicacion, igual que `tasks.md`, en la misma pasada.
- **`_persistence/progress.md`** — lo escribe este cierre: `Estado general`, «Ultimo realizado», «Siguiente paso» y la entrada `S-039` de la bitacora, mas su fila en el indice de sesiones.
- **`_audit/S-039.md`** (este informe) y **`_audit/index.md`** — los anade el propio cierre.

## 2. Que NO se hizo, y por que

Tareas `No implementada` en el indice de `_persistence/tasks.md` (filtrado por la columna `Estado`,
no por texto del titulo):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | awk -F'|' '{gsub(/^ +| +$/,"",$4)} $4=="No implementada"' | wc -l
6
```

- `T-001` — Definir alcance y objetivo del proyecto (`Alta`, `Bloqueante`, `005_discovery`). Aplazada
  por `D-143`; se retoma cuando `A-004` quede confirmado y `005_discovery` este activa.
- `T-128` — Hacer la cosecha de las lecciones de la etapa preparatoria (`Alta`, `No bloqueante`).
  Pendiente de que la etapa se vaya a cerrar de verdad.
- `T-144` — Dar dueno y sitio a evaluacion y observabilidad (`Media`, `No bloqueante`,
  `005_discovery`). Se retoma cuando esa etapa este activa.
- `T-156` — Construir el esqueleto reutilizable de arranque de proyecto (`Alta`, `Bloqueante`). Tarea
  paraguas; le quedan `T-162` y `T-163`.
- `T-162` — Escribir la guia de arranque del esqueleto (`Media`, `No bloqueante`). Bloqueada por
  `D-153` hasta que se promueva la plantilla `005_project.md`.
- `T-163` — Resolver los dos huecos menores del esqueleto: brief y `temporal/` (`Baja`, `No
  bloqueante`).

Nada de lo anterior se toco esta sesion: el trabajo de hoy fue integramente evaluar y registrar
`F-107` y `F-108`, y retomar `T-179`. El siguiente trabajo de fondo — la primera promocion real con
`protocol-promote` — sigue sin ejecutarse; solo puede pedirla el usuario.

## 3. Decisiones tomadas

- **`D-157`** — La mitad hacia adelante de `F-108` no se adopta: la regla que pedia («que ningun
  comentario vaya dentro de un bloque de salida») ya existe en el Paso 2d de `protocol-close`
  (derivar la procedencia del diff, pegarla con su salida cruda) y en la plantilla del informe
  («sin editar»). Lo que fallo fue cumplirla, no que faltara. **Alternativas descartadas:** (a)
  adoptarla tal cual como frase nueva — duplicaria la regla y mezclaria este cambio con el de
  `D-158` en la misma sesion, lo que dificulta auditar cada uno por separado; (b) un control
  mecanico que compare el bloque publicado con la salida anclada — el Paso 7c-bis tiene prohibido
  tocar prosa y bloques ajenos al criterio de cierre, y un control que solo avisa ya existe: es la
  auditoria. Reversible a criterio (no cambia ningun archivo del andamiaje).
- **`D-158`** — Se retoma `T-179` (aplazada en `S-038`) porque `F-107` materializo el riesgo que ese
  aplazamiento dejaba abierto: la cuarta vez que el Paso 2d detecta una orden que no reproduce en un
  archivo que el cierre no puede editar, el pendiente quedaba solo dicho en el informe y acababa en
  hallazgo de auditoria. Decide dos cosas: (1) que el cierre abra una `T-XXX` en `tasks.md` con el
  archivo, la orden literal y lo publicado frente a lo que da, citada en la seccion 6 del informe; y
  (2) el nuevo valor de `Origen`: **`session-closer`**, el unico origen que el propio cierre puede
  poner por iniciativa de un control suyo. **Alternativas descartadas:** (a) `Origen: manager` — el
  registro diria que `manager` detecto y decidio algo que no vio; (b) `Origen: report_auditor` — la
  convencion exige citar un `F-NNN` y no lo hay para este mecanismo; (c) mantener el aplazamiento
  hasta la promocion — es lo que se hizo, y `F-107` es el coste; (d) dar al cierre permiso para
  escribir la nota fechada en los archivos del porque — rompe la regla de que el porque es de
  `manager`; (e) un control mecanico que compare los `No` de la tabla del Paso 2d con las tareas
  nacidas — la atribucion de archivo puede salir mal (lo muestra la propia seccion 7 de `S-038`); se
  prefiere probar primero la regla simple, con su supuesto `A-021`. Reversible a criterio.

## 4. Supuestos vigentes y riesgos

Supuestos `Abierto` en `_persistence/assumptions.md` (columna `Estado`):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep '| Abierto |' | grep -oE 'A-[0-9]+' | sort -u
A-001
A-002
A-003
A-004
A-005
A-006
A-007
A-008
A-009
A-011
A-012
A-013
A-014
A-015
A-018
A-019
A-020
A-021
```

De ellos, el que nace y el que toca esta sesion:

- **`A-021`** (nace hoy) — que el cierre abrira la `T-XXX` del Paso 2d con `Origen: session-closer`
  la primera vez que le toque de verdad, en vez de dejar el aviso solo en el informe. Esta sesion no
  lo ejercita: el Paso 2d de este mismo cierre no encontro ninguna orden que no reprodujera en un
  archivo ajeno (las 7 sin reproducir eran `<hash>` sin anclar, en `decisions.md` y `tasks.md`, que
  el cierre si puede editar). Se confirma o se refuta en el primer cierre cuya tabla del Paso 2d
  tenga un `No` en un archivo que `protocol-close` no edita.
- **`A-018`** (toca hoy, no nace) — que toda diferencia entre las areas agnosticas del proyecto y
  las del esqueleto es una promocion pendiente. La nota de `T-180` la deja intacta: lo que corrige es
  una cifra de su nota anterior (`T-176`), no el supuesto mismo. Sigue abierto; se confirma o refuta
  con la primera promocion real.

Riesgo senalado en este mismo informe y sin codigo propio: ninguno detectado en esta sesion mas alla
de los ya cubiertos por `A-021` arriba.

## 5. Siguiente tarea propuesta

La primera promocion real con `protocol-promote`, sobre las 6 lineas que el Paso 2f de este cierre
deja pendientes (`protocol-close`, `protocol-harvest`, `protocol-promote` nueva, las dos plantillas
de `000_preproject/` tocadas hoy y `CLAUDE.md`). Importancia `Alta`, urgencia `Bloqueante` (es parte
de `T-156`). Solo puede pedirla el usuario, con el repositorio limpio y subido — es tambien la
primera prueba real del supuesto `A-019`.

## 6. Que pedimos auditar

- **Que las dos notas de `T-180` reproduzcan de verdad**, no solo que esten puestas: son las que
  corrigen un defecto de reproducibilidad, y una correccion que no reproduce repetiria exactamente
  lo que `F-107` denuncio.
- **Que la atribucion de `T-181` sea correcta esta vez.** La propia auditoria (`R-038`) encontro el
  error de atribucion en la seccion 7 de `S-038`; conviene que la correccion se verifique linea por
  linea contra la orden anclada, no solo que el bloque este presente.
- **Que `D-157` no sea, en el fondo, un rechazo de comodidad.** Es la lectura mas debil de este
  cierre: se dice que la regla «ya existe» en vez de escribir una mas explicita, y esa lectura
  depende de que el Paso 2d se interprete correctamente por quien lo ejecute. Si vuelve a pasar,
  la alternativa (b) descartada — un control mecanico — merece reconsiderarse.
- **Que el nuevo valor `session-closer` de `Origen` no abra una puerta mas ancha de lo que `D-158`
  pretende.** Esta sesion no lo ejercito (no hubo ningun caso real que abrir), asi que la primera
  vez que el Paso 2d encuentre un `No` en un archivo ajeno es el primer caso real y conviene
  revisarlo con cuidado.

## 7. Evidencia del Paso 2d

La orden en su forma anclada al commit de esta sesion es
`git diff -U0 <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-039.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l`
(se rellena en la NOTA DE CIERRE, Paso 7c). Su equivalencia sobre el area de staging, corrida antes
del commit — con el propio informe excluido, porque una vez commiteado el informe cita estas mismas
ordenes y las cuenta dos veces —, devuelve **23 lineas** (2 duplicadas: la misma orden citada en dos
sitios distintos — `_persistence/decisions.md` y su copia en el bloque «Procedencia por archivo» de
`_audit/S-038.md`):

```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-039.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
23
```

**La lista completa, numerada, con la reejecucion de cada una:**

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' \| awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' \| grep -vE 'git (show\|grep\|log\|diff) [0-9a-f]{7,40}' \| cut -d' ' -f1 \| uniq -c` | Si | `3 findings.md, 2 assumptions.md, 1 decisions.md, 12 tasks.md` |
| 2 | `...\| cut -d' ' -f1 \| cat -n \| sed -n '4p;5p;10p'` | Si | mismas 3 filas publicadas |
| 3 | `git grep -c '0  .claude/skills/protocol-close/SKILL.md' d35bc91 -- _persistence/assumptions.md _persistence/tasks.md` | Si | `assumptions.md:2`, `tasks.md:3` |
| 4 | igual que #1 (duplicada) | Si | ver #1 |
| 5 | `git -C "<esqueleto>" log -1 --format='%h %ad' --date=short` | Si | `1748f0a 2026-09-11` |
| 6 | `git -C "<esqueleto>" show 1748f0a:project.md \| grep -c '^\| Esqueleto de arranque'` | Si | `0` |
| 7 | `git -C "<esqueleto>" ls-tree --name-only 1748f0a` | Si | mismos 11 elementos |
| 8 | igual que #1 pero con `wc -l` en vez de `uniq -c` | Si | `18` |
| 9 | `git show <hash>:.claude/skills/protocol-close/SKILL.md \| grep -c 'Origen: session-closer'` | No anclable todavia | `<hash>` literal — criterio de cierre de `D-158`, lo ancla el Paso 7c-bis |
| 10 | `git show <hash>:_persistence/tasks.md \| grep -c '^\| \`session-closer\` \|'` | No anclable todavia | idem, `D-158` |
| 11 | `git show <hash>:_templates/000_preproject/015_tasks.md \| grep -c '^\| \`session-closer\` \|'` | No anclable todavia | idem, `D-158` |
| 12 | `git show <hash>:.claude/skills/protocol-close/SKILL.md \| grep -c '^Ordenes sin reproducir...'` | No anclable todavia | idem, `D-158` |
| 13 | `grep -c 'Origen: session-closer' .claude/skills/protocol-close/SKILL.md` | Si | `2` |
| 14 | `grep -c '^Ordenes sin reproducir en archivos ajenos (2d) — ' .claude/skills/protocol-close/SKILL.md` | Si | `1` |
| 15 | `grep -c '^\| \`session-closer\` \| un control del cierre' _persistence/tasks.md _templates/000_preproject/015_tasks.md` | Si | `1` en cada archivo |
| 16 | `grep -c '^\| Origen \| ...session-closer\` \|' _persistence/tasks.md _templates/000_preproject/015_tasks.md` | Si | `1` en cada archivo |
| 17 | `grep -nE "[Rr]aidom\|[Rr]aindom\|..." .claude/skills/protocol-close/SKILL.md _templates/000_preproject/015_tasks.md; echo exit=$?` | Si | `exit=1` |
| 18 | `git diff -U0 -- .claude/skills/protocol-close/SKILL.md _templates/000_preproject/015_tasks.md \| grep '^+[^+]' \| grep -noE ... \| grep -vE ':PI-...'; echo exit=$?` | Si | `exit=1` (pregunta por el arbol de trabajo/index, no por un commit) |
| 19 | `git show <hash>:_persistence/assumptions.md \| grep -c 'T-180\`, hallazgo \`F-107'` | No anclable todavia | `<hash>` literal — criterio de cierre de `T-180` |
| 20 | `git show <hash>:_persistence/tasks.md \| grep -c '📌 ...T-1[8]0...F-107'` | No anclable todavia | idem, `T-180` |
| 21 | bucle `for spec in ...` que extrae y reejecuta las ordenes de las notas de `T-180` | Si | `REPRODUCE (3 ordenes)` y `REPRODUCE (1 ordenes)` |
| 22 | `git show <hash>:_audit/S-038.md \| grep -c 'T-181\`, hallazgo \`F-108'` | No anclable todavia | `<hash>` literal — criterio de cierre de `T-181` |
| 23 | bucle `for spec in ...` que extrae y reejecuta la orden de la nota de `T-181` | Si | `REPRODUCE (2 ordenes)` |

**Repetidas (`sort \| uniq -d`):**

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sed 's/^\+\$ //' | sort | uniq -d
git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | uniq -c
```

Son las filas #1 y #4.

**Las 7 que llevan `<hash>` literal (#9-12, #19-20, #22) no son un defecto: son los criterios de
cierre de `D-158`, `T-180` y `T-181`, escritos hoy y sin el hash porque el commit todavia no existe.**
Cuatro viven en `_persistence/decisions.md` y tres en `_persistence/tasks.md` — los dos unicos
archivos que el Paso 7c-bis esta autorizado a anclar. Ninguna orden queda pendiente sobre un archivo
que `protocol-close` no pueda editar, asi que **el Paso 2d no abre ninguna `T-XXX` con
`Origen: session-closer` en esta sesion**: no hay caso que ejercite a `A-021` todavia.

**Procedencia por archivo, derivada del diff (no tecleada), con el informe excluido por el mismo
motivo que arriba:**

```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-039.md" | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | sort | uniq -c
      2 b/_audit/S-038.md
      2 b/_audit/findings.md
      8 b/_persistence/decisions.md
     11 b/_persistence/tasks.md
```

Suma 2+2+8+11 = 23, coincide con el recuento de arriba.

> ## NOTA DE CIERRE
>
> Se completa en el Paso 7c/7c-bis, despues del commit: el hash, el BARRIDO DE ANCLAJE, el CONTROL
> DE PROSA BORRADA, la SEGUNDA PASADA anclada del Paso 2e y el CONTROL DE CIFRA ADYACENTE.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Las cuatro cifras son **heredadas de `HEAD`**, ninguna nueva de esta sesion:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"
done
.claude/skills/protocol-close/SKILL.md head=0 staged=0
_audit/S-038.md head=0 staged=0
_audit/findings.md head=1 staged=1
_persistence/assumptions.md head=4 staged=4
_persistence/decisions.md head=7 staged=7
_persistence/lessons.md head=0 staged=0
_persistence/tasks.md head=5 staged=5
_templates/000_preproject/015_tasks.md head=0 staged=0
```

**Los dos contrastes de la tabla:**

```
$ git diff --cached --name-only --diff-filter=d | wc -l
8

$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
```

8 filas en la tabla (coincide con las 8 archivos tocados hasta ese punto), suma de la columna
`staged` = 1+4+7+5 = 17 (coincide con el total). Cuadra.

⚠️ Esta seccion mide el area de staging previa al commit sustantivo: `progress.md`, este informe y
la fila de `_audit/index.md` quedan fuera por construccion. La SEGUNDA PASADA anclada del Paso 2e
(en la NOTA DE CIERRE de la seccion 7) cubre esa brecha.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-harvest/SKILL.md and .claude/skills/protocol-harvest/SKILL.md differ
Only in .claude/skills: protocol-promote
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_templates/000_preproject/005_project.md and _templates/000_preproject/005_project.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_templates/000_preproject/015_tasks.md and _templates/000_preproject/015_tasks.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/CLAUDE.md and CLAUDE.md differ
```

6 lineas (recuento derivado: `wc -l` sobre la salida de arriba = 6). Todas son promociones
pendientes, ninguna es `Only in` el arbol del esqueleto (ninguna contradice el sentido unico del
andamiaje). El paso informa, no frena: no hay hallazgo aqui. `_templates/000_preproject/015_tasks.md`
es nuevo en esta lista frente a `S-038` (el nuevo valor `session-closer` de `Origen`, escrito hoy en
la plantilla y todavia no promovido).
