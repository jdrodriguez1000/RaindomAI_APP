# Auditoria R-014 — sobre S-014

| Campo | Valor |
|---|---|
| Auditoria | R-014 |
| Informe auditado | `_audit/S-014.md` |
| Commit auditado | `ca56b93` |
| Fecha | 2026-09-03 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

**Ancla del commit** — el informe esta commiteado:

~~~
$ git log -1 --format=%h -- _audit/S-014.md
ca56b93
~~~

**Arbol limpio al arrancar la auditoria:**

~~~
$ git status --porcelain
$ git status -sb
## main...origin/main
~~~

### Control a — indice contra detalle en `_persistence/`

Sin salida en los siete archivos: indice y detalle coinciden.

~~~
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show ca56b93:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show ca56b93:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
~~~

### Control b — carpetas del arbol contra las declaradas

Dos diferencias, **las dos con razon escrita en `project.md`** («Carpetas propias»): `temporal/`
esta excluida en `.gitignore` y nunca aparecera en el arbol versionado; `010_prototype/` esta
declarada por adelantado (`D-061`) y su etapa no ha arrancado. No son hallazgo.

~~~
$ diff <(git ls-tree -d --name-only ca56b93 | sed 's|$|/|' | sort) \
       <(git show ca56b93:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
~~~

### Control c — fuga de datos propios en lo agnostico

Cero lineas.

~~~
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" ca56b93 -- .claude CLAUDE.md _phases _methodology
exit=1
~~~

### Control d — el informe contra el diff

La lista de la seccion 1 coincide con el commit: los ocho archivos que declara, mas
`_audit/S-014.md` y `_audit/index.md`, que el propio informe anuncia como anadidos despues.
Los dos `numstat` que cita (`15 0` en la skill, `35 0` en `_audit/S-013.md`) reproducen.

~~~
$ git show --numstat --format="" ca56b93
15	0	.claude/skills/protocol-close/SKILL.md
35	0	_audit/S-013.md
224	0	_audit/S-014.md
22	4	_audit/findings.md
1	0	_audit/index.md
145	0	_persistence/decisions.md
33	0	_persistence/lessons.md
63	38	_persistence/progress.md
131	0	_persistence/tasks.md
261	0	_workflow/010_prototype.md
~~~

La seccion 2 reproduce literal (columna de enlaces recortada aqui por ancho):

~~~
$ git show ca56b93:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep "No implementada"
| [T-001](...) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002](...) | Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003](...) | Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
| [T-037](...) | Escribir el inventario de acciones irreversibles del proyecto (`LG-38`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-038](...) | Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close` | No implementada | Media | No bloqueante | `000_preproject` |
| [T-039](...) | Normalizar a `No implementada` el estado `Pendiente` de `T-037` y `T-038` (`F-029`) | Implementada | Media | No bloqueante | `000_preproject` |
~~~

La seccion 4 reproduce: los cinco supuestos siguen `Abierto`.

~~~
$ git show ca56b93:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -c "| Abierto |"
5
~~~

`D-065`, `D-066`, `L-020`, `T-049`, `T-050` y `T-051` existen con fila en el indice y entrada en el
detalle (Control a), y las tres tareas llevan bloque de verificacion con orden literal y salida
cruda (`PI-5`).

### Control e — reejecucion de las once ordenes de la seccion 7

Primero, la propia lista del Paso 2d sobre el commit. Excluyendo `_audit/S-014.md` —que es donde el
informe se cita a si mismo, y que el cierre declara haber tomado antes de anadirlo— la salida es
**identica** a la publicada: 19 apariciones, 11 ordenes distintas, las mismas lineas y en el mismo
orden.

~~~
$ git diff ca56b93^ ca56b93 -U0 -- _persistence _audit ":(exclude)_audit/S-014.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
19

$ git diff ca56b93^ ca56b93 -U0 -- _persistence _audit ":(exclude)_audit/S-014.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
11
~~~

Reejecucion de las once, ancladas a `ca56b93`:

~~~
$ git show ca56b93:_persistence/tasks.md | sed -n '/^### T-046/,/^---/p' | grep -cE '^\$ '
2

$ git show ca56b93:_audit/S-013.md | grep -c "Nota del 2026-09-03 (\`T-049\`, hallazgo \`F-034\`)"
1

$ git show ca56b93:.claude/skills/protocol-close/SKILL.md | grep -n "^## 7. Evidencia del Paso 2d"
694:## 7. Evidencia del Paso 2d

$ git show ca56b93:.claude/skills/protocol-close/SKILL.md | grep -n "seccion 7 del informe"
280:🚨 **Y esa lista tiene un sitio fijo: la seccion 7 del informe de `_audit/S-XXX.md`, y no la

$ git show ca56b93:_workflow/010_prototype.md | grep -c "^| \*\*[1-9] · "
9

$ git show ca56b93:_phases/010_prototype.md | grep -n "_workflow/010_prototype"
144:**`_workflow/010_prototype.md`**, que se lee ahora y no despues. Ese reparto se adopta con su `D-XXX` en el
314:🚨 **Las plantillas y el reparto de `_workflow/010_prototype.md` son condicion de entrada, no

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" ca56b93 -- .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
exit=1

$ git show ca56b93:_workflow/010_prototype.md | grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" ; echo "exit=$?"
exit=1

$ git show ca56b93:_workflow/005_discovery.md | grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" ; echo "exit=$?"
188:manda leer es material muerto que ningun control detecta — el cuarto enganche de `L-014`.
exit=0
~~~

Los dos `git diff --numstat` restantes se comprobaron contra el `numstat` del commit (Control d):
`15 0` y `35 0`.

**Las once salidas reproducen exactamente lo publicado.** Lo que no se sostiene es la
**procedencia** que la seccion 7 atribuye a tres de ellas — `F-035`.

### Control f — lo que pidio la seccion 6 del informe

**f.1 — La nota de `T-049` en `_audit/S-013.md` es solo insercion y no toca el parrafo original.**
Confirmado: `35 0`, un unico bloque, cero lineas borradas.

~~~
$ git show ca56b93 -- _audit/S-013.md | grep -cE '^-[^-]'
0

$ git show ca56b93 -- _audit/S-013.md | grep -E '^@@'
@@ -135,6 +135,41 @@
~~~

**f.2 — La lista publicada en la nota (rango `265bfeb..8eb8666`) es reproducible en otro entorno.**
Reejecutada aqui: mismas trece lineas, mismo orden, y los dos recuentos `13` y `10`. El riesgo que
el informe señalaba —que el recuento no sea estable entre entornos— no se materializa esta vez.

~~~
$ git diff 265bfeb 8eb8666 -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+$ git diff --cached --stat --name-only
+$ grep -n "la lista COMPLETA" .claude/skills/protocol-close/SKILL.md
+$ grep -n "^## Paso 2d" .claude/skills/protocol-close/SKILL.md
+$ ls _templates/010_prototype/
+$ grep -rniE "(^|[^0-9])_prototype/|_memory/|015_gate1|terminal (ejecutora|auditora)|sponsor|020_baseline|minimo 3|recomendado 5|SUP-|RES-" _templates/010_prototype/ ; echo "exit=$?"
+$ grep -rlc "Guia de llenado" _templates/010_prototype/
+$ grep -c "sin decidir" _templates/010_prototype/*.md
+$ grep -n "la lista COMPLETA" .claude/skills/protocol-close/SKILL.md
+$ ls _templates/010_prototype/
+$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
+$ grep -rnE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" _templates/010_prototype/ ; echo "exit=$?"
+$ grep -rniE "(^|[^0-9])_prototype/|_memory/|015_gate1|terminal (ejecutora|auditora)|sponsor|020_baseline|minimo 3|recomendado 5|SUP-|RES-" _templates/010_prototype/ ; echo "exit=$?"
+$ LC_ALL=C.UTF-8 grep -rnP "[\x{00e1}\x{00e9}\x{00ed}\x{00f3}\x{00fa}\x{00c1}\x{00c9}\x{00cd}\x{00d3}\x{00da}]" _templates/010_prototype/ ; echo "exit=$?"

$ ... | wc -l
13

$ ... | sort -u | wc -l
10
~~~

**f.3 — Las cinco asignaciones de `D-066` contra `_phases/010_prototype.md` §4.** Consistentes. El
archivo de etapa tiene exactamente nueve pasos y `_workflow/010_prototype.md` tiene nueve filas con
los mismos titulos.

~~~
$ git show ca56b93:_phases/010_prototype.md | sed -n '/^## 4\./,/^## 5\./p' | grep -nE '^### Paso'
15:### Paso 1 — Elegir el camino feliz que se valida
23:### Paso 2 — Escribir la tarea ANTES de construir el prototipo
38:### Paso 3 — Fijar cuantos usuarios y de que perfiles
58:### Paso 4 — Construir el prototipo
67:### Paso 5 — Correr la sesion
88:### Paso 6 — No tocar el prototipo entre sesiones
99:### Paso 7 — Registrar cada sesion, mientras ocurre
118:### Paso 8 — Clasificar las observaciones
131:### Paso 9 — Validacion de negocio, en sesion aparte
~~~

Contrastes puntuales sobre la contradiccion que el informe temia, todos limpios:

- El Paso 4 (la IA construye, con revision humana entera) **no** choca con lo que la etapa prohibe:
  §2 prohibe conservar y promover el prototipo, no generarlo. `_workflow/010_prototype.md` §4 separa
  explicitamente «el prototipo es el **producto** de la etapa, no un participante».
- El Paso 8 (clasificar sin pesar) coincide con la etapa: nueve categorias, y «solo las tres
  primeras pesan en el Gate».

~~~
$ git show ca56b93:_phases/010_prototype.md | sed -n '/^### Paso 8/,/^### Paso 9/p' | grep -n "tres primeras"
9:🔑 **Solo las tres primeras pesan en el Gate.** Las demas son material para las etapas siguientes, y
~~~

- Las sesiones simuladas estan prohibidas en los dos sitios: `_workflow/010_prototype.md` §3 (primera
  fila) y `D-066` (alternativa descartada 4).
- La condicion de entrada esta enganchada por las dos puntas: `_phases/010_prototype.md:314` dice que
  sin el reparto la etapa no se abre, y el reparto lo cita en su cabecera.
- La condicion de salida: `_phases/010_prototype.md` §6 tiene siete casillas, y las siete estan
  mapeadas en `_workflow/010_prototype.md` §5.

~~~
$ git show ca56b93:_phases/010_prototype.md | sed -n '/^## 6\./,/^### /p' | grep -c '^- \[ \]'
7
~~~

- Las secciones citadas de `_workflow/team.md` (§1, §5.1, §8, §9, §12) y de `_workflow/ai_levels.md`
  (§2, §6, §8) existen y tratan de lo que se les atribuye.

~~~
$ git show ca56b93:_workflow/ai_levels.md | grep -nE '^## '
9:## Alcance de este archivo
38:## 1. La unidad de diseño es el trabajo
58:## 2. Los siete niveles
176:## 3. Comportamiento probabilistico: por que hace falta el harness
213:## 4. Lo que se puede variar
226:## 5. El harness
291:## 6. La rubrica de seleccion
330:## 7. Como se declara el nivel en un proyecto
351:## 8. Cuando se elige el nivel, dentro del metodo
372:## 9. Lo que compone un sistema de trabajo con IA
403:## 10. Errores frecuentes
~~~

### Control g — `F-034` contra el diff

Las dos mitades de la correccion estan en el commit. Ademas, la estructura del informe se declara en
un solo sitio, asi que la seccion 7 nueva no deja ninguna copia desincronizada:

~~~
$ git grep -c "6. Que pedimos auditar" ca56b93 -- .claude
ca56b93:.claude/skills/protocol-close/SKILL.md:1
~~~

## 2. Hallazgos

### F-035 — La seccion 7 de `S-014` atribuye tres de sus once ordenes a bloques donde no estan

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-014.md` §7 |

- **Que se observo:** la lista y los recuentos de la seccion 7 reproducen exactos (Control e). Lo
  que no se sostiene es la anotacion de procedencia. Las tres primeras lineas de la lista salen de
  `_audit/findings.md` (bloque de `F-034`), y la seccion las atribuye a otros sitios:

  | Orden | Lo que dice `S-014` §7 | Donde esta de verdad |
  |---|---|---|
  | `sed -n '/^### T-046/,/^---/p' … \| grep -cE '^\$ '` | «reproduce (`_persistence/tasks.md`, T-050)» | `_audit/findings.md`, bloque `F-034` |
  | `grep -c "Nota del 2026-09-03 …" _audit/S-013.md` | «reproduce (`_audit/S-013.md`, T-049)» | `_audit/findings.md`, bloque `F-034` |
  | `grep -n "^## 7. Evidencia del Paso 2d" …` | «citada en T-050 y en T-051» | `_audit/findings.md` (`F-034`), `_persistence/decisions.md` (`D-065`) y `_persistence/tasks.md` (`T-050`) — **no** `T-051` |

  Ademas, el parrafo de cierre de la seccion —«Las once ordenes reproducen exactamente lo publicado
  en los bloques de verificacion de `D-065`, `D-066`, `T-049`, `T-050` y `T-051`»— y la explicacion
  de por que se repiten lineas —«el mismo bloque de verificacion aparece citado tanto en
  `_persistence/decisions.md` como en `_persistence/tasks.md`»— omiten `_audit/findings.md`, que
  aporta tres de las diecinueve apariciones.

  Procedencia real de cada linea, derivada del propio diff:

~~~
$ git diff ca56b93^ ca56b93 -U0 -- _persistence _audit ":(exclude)_audit/S-014.md" | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
b/_audit/findings.md :: +$ sed -n '/^### T-046/,/^---/p' _persistence/tasks.md | grep -cE '^\$ '
b/_audit/findings.md :: +$ grep -c "Nota del 2026-09-03 (\`T-049\`, hallazgo \`F-034\`)" _audit/S-013.md
b/_audit/findings.md :: +$ grep -n "^## 7. Evidencia del Paso 2d" .claude/skills/protocol-close/SKILL.md
b/_persistence/decisions.md :: +$ grep -n "seccion 7 del informe" .claude/skills/protocol-close/SKILL.md
b/_persistence/decisions.md :: +$ grep -n "^## 7. Evidencia del Paso 2d" .claude/skills/protocol-close/SKILL.md
b/_persistence/decisions.md :: +$ git diff --numstat -- .claude/skills/protocol-close/SKILL.md
b/_persistence/decisions.md :: +$ grep -c "^| \*\*[1-9] · " _workflow/010_prototype.md
b/_persistence/decisions.md :: +$ grep -n "_workflow/010_prototype" _phases/010_prototype.md
b/_persistence/decisions.md :: +$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
b/_persistence/decisions.md :: +$ grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" _workflow/010_prototype.md ; echo "exit=$?"
b/_persistence/decisions.md :: +$ grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" _workflow/005_discovery.md ; echo "exit=$?"
b/_persistence/tasks.md :: +$ git diff --numstat -- _audit/S-013.md
b/_persistence/tasks.md :: +$ grep -n "seccion 7 del informe" .claude/skills/protocol-close/SKILL.md
b/_persistence/tasks.md :: +$ grep -n "^## 7. Evidencia del Paso 2d" .claude/skills/protocol-close/SKILL.md
b/_persistence/tasks.md :: +$ git diff --numstat -- .claude/skills/protocol-close/SKILL.md
b/_persistence/tasks.md :: +$ grep -c "^| \*\*[1-9] · " _workflow/010_prototype.md
b/_persistence/tasks.md :: +$ grep -n "_workflow/010_prototype" _phases/010_prototype.md
b/_persistence/tasks.md :: +$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
b/_persistence/tasks.md :: +$ grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" _workflow/010_prototype.md ; echo "exit=$?"
~~~

  Y el bloque de verificacion de `T-051` entero, para que se vea que la tercera orden no esta ahi:

~~~
$ git show ca56b93:_persistence/tasks.md | sed -n '/^### T-051/,/^---$/p' | grep -E '^\$ '
$ grep -c "^| \*\*[1-9] · " _workflow/010_prototype.md
$ grep -n "_workflow/010_prototype" _phases/010_prototype.md
$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
$ grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" _workflow/010_prototype.md ; echo "exit=$?"
~~~

- **Por que importa:** la seccion 7 nace en este mismo commit para cerrar `F-034`, cuyo defecto era
  exactamente «el informe remite a un sitio donde la evidencia no esta». La lista ya no falta —eso
  quedo resuelto—, pero el puntero de tres de sus lineas vuelve a apuntar a donde no estan, y una de
  las atribuciones (`T-051`) es falsa sin lectura benevola posible. Quien siga la anotacion para
  contrastar una orden contra su bloque no la encuentra, que es justo el coste que la seccion existe
  para evitar. Y el parrafo de cierre deja fuera un archivo fuente entero (`_audit/findings.md`), asi
  que la explicacion de las repeticiones tampoco cuadra con lo que produce el comando.
- **Que lo corregiria:** una nota fechada al lado de la seccion 7 de `_audit/S-014.md` (`D-019`, sin
  reescribir), con la tabla de procedencia real anclada al commit. Para el patron de fondo: que la
  seccion 7 de `protocol-close` pida **derivar** la procedencia del propio diff —anadiendo
  `awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}'` a la orden que ya se corre— en vez de
  escribirla a mano, que es donde entra el error. ⚠️ Es una recomendacion, no una orden.

### F-036 — Una no conformidad declarada en `_workflow/005_discovery.md` se aparca sin `DT-XXX`

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_workflow/005_discovery.md:188`; `D-066`; `T-051`; `_persistence/techdebt.md` |

- **Que se observo:** `D-066` y `T-051` declaran por escrito que `_workflow/005_discovery.md`
  incumple la regla de codigos genericos que su propia cabecera enuncia —cita `L-014`, un codigo
  instanciado de `_persistence/lessons.md`—, publican el comando que lo prueba, y deciden no tocarlo
  invocando `PI-3`. No se abre `T-XXX` ni entrada en `techdebt.md`: el indice sigue con dos filas, y
  ninguna es esta.

~~~
$ git show ca56b93:_workflow/005_discovery.md | grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+"
188:manda leer es material muerto que ningun control detecta — el cuarto enganche de `L-014`.

$ git show ca56b93:_workflow/005_discovery.md | sed -n '8,12p'
🔑 **Este archivo es agnostico y sirve para cualquier proyecto.** Las actividades que reparte son
los siete pasos del procedimiento de `_phases/005_discovery.md` §4, que son los mismos en cualquier
proyecto que use este metodo. No lleva dentro ni un nombre, ni una ruta, ni un dato de ningun
producto; los codigos van en su forma generica —`N-XXX`, `I-XXX`, `D-XXX`, `A-XXX`, `C-XXX`—.

$ git show ca56b93:_persistence/techdebt.md | sed -n '/^## Indice/,/^---/p' | grep '^| \[DT'
| [DT-001](#dt-001---debtecmd-incumple-la-regla-de-nombres-en-ingles) | `debtec.md` incumple la regla de nombres en ingles | Implementada | Confirmada | Baja | No bloqueante |
| [DT-002](#dt-002---_workflow-nace-sin-ningun-enganche-de-uso-l-014) | `_workflow/` nace sin ningun enganche de uso (`L-014`) | No implementada | Propuesta (pendiente del usuario) | Media | No bloqueante |
~~~

  `DT-002` trata de otra cosa —que `_workflow/` nacio sin enganche de uso—, aunque cite la misma
  leccion.

- **Por que importa:** `CLAUDE.md` es explicito para el caso simetrico —lo heredado que incumple «se
  deja y **se registra como deuda tecnica** con su motivo»—, y `_workflow/` esta en la lista de
  carpetas que tienen que poder copiarse tal cual a otro proyecto. Aparcado solo dentro del cuerpo de
  una decision, esto no aparece en ningun indice de trabajo pendiente: nada lo va a traer de vuelta,
  y el barrido que lo detecta no es un control del cierre sino una orden escrita a mano en esta
  tarea concreta. El dia que `_workflow/` se copie a otro proyecto, viajara con una cita a una
  leccion que ese proyecto no tiene.
- **Que lo corregiria:** un `DT-XXX` en `techdebt.md` que nombre la cita, su archivo y por que se
  aplaza — o, si se prefiere resolverlo ya, una `T-XXX` que reescriba la frase en forma generica. La
  eleccion entre las dos es de `manager`. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-034` | **`Implementado`** — verificado sobre `ca56b93` | Las dos mitades estan en el diff y las dos son correctas. La concreta: la nota fechada en `_audit/S-013.md` es solo insercion (`35 0`, un solo bloque `@@ -135,6 +135,41 @@`, cero lineas borradas) y publica la lista entera; reejecutada en este entorno sobre `265bfeb..8eb8666` devuelve las mismas trece lineas y los recuentos `13`/`10`. La de fondo: `protocol-close` gana el parrafo del Paso 2d que nombra el destino (linea 280) y la seccion 7 en la estructura del informe (linea 694), y `S-014` la usa de verdad — su seccion 7 existe y su lista reproduce linea a linea. Que la **procedencia** anotada en esa seccion tenga tres errores es defecto nuevo (`F-035`), no la misma falta: lo que faltaba era la lista, y la lista ya esta |

`F-001` a `F-033` siguen `Implementado`; ninguno se reabre.

## 4. Lo que NO se pudo comprobar

Ningun control quedo sin correr. Los tres mecanicos del Paso 4, los del Control d, los once de la
reejecucion y los tres puntos de la seccion 6 del informe se ejecutaron sobre `ca56b93`.

Una precision de alcance, no un fallo: el Control c de este protocolo barre
`.claude CLAUDE.md _phases _methodology`, y deja fuera `_templates` y `_workflow`. Aqui quedaron
cubiertos solo porque la propia sesion publico la orden ampliada (Control e, `exit=1`). Esa asimetria
ya esta registrada como `T-038`, `No implementada`.

## 5. Recomendaciones sin hallazgo

- **La lectura de la rubrica en `_workflow/010_prototype.md` §6 se apoya en una frase que la regla no
  dice.** Afirma que impacto y autonomia «son los dos ejes que protege la regla 2 de §6»; la regla 2
  de `_workflow/ai_levels.md` §6 nombra solo «Impacto de un error» en 3, y remite a
  `_workflow/team.md` §5.1 para la autonomia. La conclusion (nivel 2) puede sostenerse igual, pero la
  frase atribuye a la regla un alcance que no tiene. Precisarla cuesta una linea y evita que una
  discrepancia declarada se apoye en una cita floja. No es defecto del commit: es texto de metodo que
  aun no rige sobre ningun trabajo, porque `D-066` no adopta la etapa.
- **`T-050` deja escrito por que `protocol-audit` no se toca** —que el auditor reejecute por su
  cuenta en vez de leer lo que el auditado pego—, y la razon es buena. Conviene que no se pierda:
  hoy vive solo dentro del bloque de esa tarea, y es justo la clase de razon que va a necesitar la
  sesion que retome `T-038`.
- **`010_prototype` ya tiene sus tres piezas** —archivo de etapa, plantillas y reparto— y sigue sin
  fila en la tabla «Etapas» de `project.md` (`D-060` vigente). No es un defecto: es una decision
  pendiente. Se anota porque a partir de ahora esa fila es lo unico que separa a la etapa de poder
  abrirse.
