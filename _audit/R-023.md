# Auditoria R-023 — sobre S-023

| Campo | Valor |
|---|---|
| Auditoria | R-023 |
| Informe auditado | `_audit/S-023.md` |
| Commit auditado | `b83ce5e` |
| Fecha | 2026-09-06 |
| Veredicto | Con hallazgos (4) |

📌 **Anclaje del commit.** `git log -1 --format=%h -- _audit/S-023.md` devuelve `a48411a`, que es el
**commit de anclaje** (2 archivos). El hash literal de la cabecera del informe es `b83ce5e`, con 16
archivos, y coincide con la enumeracion de su seccion 1. Se audita `b83ce5e`, y ademas —porque la
seccion 6 lo pide expresamente— el contenido de `a48411a`.

```
$ git log -1 --format=%h -- _audit/S-023.md
a48411a

$ git show a48411a:_audit/S-023.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `b83ce5e` |

$ git show --stat --name-only --format= a48411a | grep -c .
2

$ git show --stat --name-only --format= b83ce5e | grep -c .
16
```

---

## 1. Que se comprobo

### a) Indice ↔ detalle en `_persistence/` — coherente

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show b83ce5e:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show b83ce5e:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Siete archivos, ninguna diferencia.

### b) Carpetas del arbol contra las declaradas — dos diferencias, las dos con motivo escrito

```
$ diff <(git ls-tree -d --name-only b83ce5e | sed 's|$|/|' | sort) \
       <(git show b83ce5e:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos razones estan escritas en `project.md` («Carpetas propias»): `temporal/` esta excluida por
`.gitignore`, y `010_prototype/` esta declarada por adelantado (`D-061`). No es hallazgo.

### c) Fuga de datos propios en lo agnostico — cero lineas

Barrido sobre las seis carpetas que `CLAUDE.md` declara copiables:

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" b83ce5e -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

### d) Codigos instanciados (Paso 1c, nacido en `S-022`) — cero lineas

```
$ git grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' b83ce5e -- _phases _workflow; echo "exit=$?"
exit=1
```

### e) Correccion de `F-059` — presente y correcta en `b83ce5e`

Las 16 ordenes de los criterios de cierre de `D-086` a `D-091` estan republicadas ancladas, y
ninguna se quedo sin ancla:

```
$ git show b83ce5e:_persistence/decisions.md | awk '/^### D-086/,/^### D-092/' | grep -cE '^> \$ '
16

$ git show b83ce5e:_persistence/decisions.md | awk '/^### D-086/,/^### D-092/' | grep -E '^> \$ ' | grep -cv '97bb948'
0

$ git show b83ce5e:_persistence/decisions.md | grep -cE 'Nota del 2026-09-06 \(`F-059`, sesion S-023\)'
6

$ git show b83ce5e:.claude/skills/protocol-close/SKILL.md | grep -n '^### 7c-bis'
1176:### 7c-bis — Los criterios de cierre de las decisiones de esta sesion
```

### f) Correccion de `F-060` — presente, y aplicada a esta misma sesion

```
$ git log -1 --format='%h %ad %cd' --date=short b83ce5e
b83ce5e 2026-09-06 2026-09-06

$ git show b83ce5e:_audit/S-023.md | sed -n '/^| Fecha |/p'
| Fecha | 2026-09-06 |

$ git show b83ce5e:_audit/index.md | grep 'S-023'
| `S-023.md` | S-023 | 2026-09-06 | Pendiente | Pendiente | Pendiente | - |

$ git show b83ce5e:.claude/skills/protocol-close/SKILL.md | grep -n '^### 7d'
1220:### 7d — La fecha escrita contra la del commit (obligatorio)
```

La fecha escrita coincide con la del commit. Es la primera fila del tablero desde `S-020` de la que
se puede decir eso.

### g) Correccion de `F-061` — presente

`_audit/index.md` («Convenciones») ya no prescribe la orden derivada como fuente de la columna:

```
$ git show b83ce5e:_audit/index.md | grep -n 'No se deriva con'
84:🚨 **No se deriva con `git log -1 --format=%h -- _audit/S-XXX.md`.** Cuando el cierre ancla el
```

### h) Las 12 ordenes que el Paso 7c-bis anclo en `a48411a` — las 12 reproducen

Reejecutadas hoy, una por una, devuelven exactamente lo publicado en `_persistence/decisions.md`:
`1176:### 7c-bis…`, `1`, `1`, `1`, `6`, `1220:### 7d…`, `1`, `1`, `1`, los tres archivos listados,
`3`, `exit=1`, `…:15`/`…:11`. Ninguna discrepancia. Dos de ellas, como muestra:

```
$ git show b83ce5e:_persistence/decisions.md | grep -cE '^> .+ \*\*Nota del 2026-09-06 \(`F-059`, sesion S-023\)'
6

$ git grep -c '^## ' b83ce5e -- .claude/skills/protocol-gate2/SKILL.md _templates/035_gate2/005_verdict.md
b83ce5e:.claude/skills/protocol-gate2/SKILL.md:15
b83ce5e:_templates/035_gate2/005_verdict.md:11
```

### i) Seccion 7 del informe (Paso 2d) — los dos recuentos reproducen

```
$ git diff -U0 b83ce5e^ b83ce5e -- _persistence _audit ":(exclude)_audit/S-023.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
28

$ git diff -U0 b83ce5e^ b83ce5e -- _persistence _audit ":(exclude)_audit/S-023.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
19
```

### j) Seccion 8 del informe (Paso 2e) — las tres cifras reproducen

```
$ for f in _audit/findings.md _persistence/decisions.md _persistence/tasks.md; do echo "$f: $(git show b83ce5e:$f | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
_audit/findings.md: 1
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

### k) Seccion 2 del informe — las cinco tareas `No implementada` son las que dice

```
$ git show b83ce5e:_persistence/tasks.md | grep -E '^\| \[T-[0-9]+\]' | grep 'No implementada' | grep -oE 'T-[0-9]+' | tr '\n' ' '
T-001 T-002 T-003 T-037 T-038
```

La salvedad de `T-039` que el informe declara es correcta: aparece por el texto de su titulo, y su
estado real es `Implementada`.

### l) Seccion 1 del informe contra el diff — la enumeracion esta completa

```
$ git diff b83ce5e^ b83ce5e -- _persistence | grep -E '^\+### [A-Z]+-[0-9]+'
+### A-010 - El anclaje del Paso 7c-bis se queda en mecanico, y no se desliza a escribir el porque
+### C-008 - El repositorio no tiene un final de linea unico, y no se normaliza
+### D-092 - El anclaje de un criterio de cierre lo hace el cierre, despues del commit
+### D-093 - La fecha de una sesion es la del commit que la cierra
+### D-094 - Nace el Gate 2, y se comprueba por el historial, no por marcas en los artefactos
+### L-031 - Un guion que abre el archivo para escribir antes de tener el contenido lo destruye si falla
+### L-032 - El repositorio mezcla finales de linea, y una sustitucion literal falla sin decir por que
+### T-093 - Republicar ancladas las 16 ordenes de los criterios de cierre de `D-086` a `D-091` (`F-059`)
+### T-094 - Dar al cierre el Paso 7c-bis de anclaje de criterios de cierre (`F-059`)
+### T-095 - Fijar que la fecha de una sesion es la del commit, y anotar el desfase (`F-060`)
+### T-096 - Corregir las dos convenciones que prescriben la orden que `D-090` rechaza (`F-061`)
```

Once entradas nuevas, todas nombradas en la seccion 1 del informe; los 16 archivos del `--stat`
estan todos enumerados. No hay nada relevante en el diff que el informe calle.

### m) Las tres piezas del Gate 2 — existen, son agnosticas y `project.md` las declara

```
$ git ls-tree --name-only b83ce5e .claude/agents/gate2_auditor.md .claude/skills/protocol-gate2/SKILL.md _templates/035_gate2/005_verdict.md
.claude/agents/gate2_auditor.md
.claude/skills/protocol-gate2/SKILL.md
_templates/035_gate2/005_verdict.md

$ git show b83ce5e:project.md | grep -c 'gate2_auditor\|protocol-gate2\|035_gate2'
3
```

Los tres placeholders que la skill usa (`<GRTH>`, `<BASE>`, `<WSLT>`) nombran carpetas que
`project.md` **no** declara, y la skill lo trata correctamente en su Paso 0: «Si alguna de las tres
no esta declarada en `project.md`, dilo y detente». No es hallazgo.

### n) Arbol de trabajo al empezar la auditoria — limpio

```
$ git status --porcelain
$ git status -sb
## main...origin/main
```

---

## 2. Hallazgos

### F-062 — La primera ejecucion del Paso 7c-bis borro prosa en `D-092`, que el propio paso prohibe literalmente
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/decisions.md`, entrada `D-092`; commit de anclaje `a48411a` |

- **Que se observo:** el Paso 7c-bis, nacido en `b83ce5e`, escribe en su tabla «⛔ No puedes:
  **escribir, alterar o borrar una sola palabra de prosa**», y `CLAUDE.md` lo repite en el mismo
  commit. Su primera ejecucion real, en `a48411a`, borro tres lineas de prosa de `D-092` y las
  sustituyo por otras tres.

~~~
$ git diff a48411a^ a48411a -- _persistence/decisions.md | grep -E '^[+-][^+-]' | grep -vE '^[+-]\$ '
-CLAUDE.md:1
-.claude/agents/session-closer.md:1
+1
+1
-⚠️ **Las cuatro ordenes se corren sobre el arbol de trabajo, y el Paso 7c-bis que esta misma
-decision crea las anclara al commit** — es el primer caso al que se aplica la regla. Los numeros de
-linea de la primera son los que mas se mueven, y por eso el anclaje importa.
+📌 **Ancladas por el Paso 7c-bis al commit `b83ce5e`** (la primera ejecucion real de este paso,
+que la propia decision crea). Las cuatro reproducen exactamente lo publicado arriba sobre el arbol
+de trabajo.
+📌 **Ancladas por el Paso 7c-bis al commit `b83ce5e`.** Las cuatro reproducen exactamente lo
+publicado arriba sobre el arbol de trabajo.
-.claude/skills/protocol-gate2/SKILL.md:15
-_templates/035_gate2/005_verdict.md:11
+b83ce5e:.claude/skills/protocol-gate2/SKILL.md:15
+b83ce5e:_templates/035_gate2/005_verdict.md:11
+📌 **Ancladas por el Paso 7c-bis al commit `b83ce5e`.** La primera cambia de forma, no de pregunta
+—`ls` interroga al disco, que no tiene version; `git ls-tree` hace la misma pregunta sobre el
+commit—, igual que ya hizo `D-086` con su propio `ls`. Las cuatro reproducen exactamente lo
+publicado arriba.
~~~

  Las tres unicas lineas con `-` que no son salida de una orden son las de `D-092`: prosa escrita por
  `manager`, borrada por el cierre. La comparacion con `D-093` lo hace evidente — ahi el paso **si**
  respeto la regla: anadio su nota y dejo intacto el `⚠️` anterior, que hoy convive con ella.

~~~
$ git show a48411a:_persistence/decisions.md | sed -n '/^### D-093/,/^### D-094/p' | grep -nE '^(📌|⚠️)'
54:📌 **Ancladas por el Paso 7c-bis al commit `b83ce5e`.** Las cuatro reproducen exactamente lo
57:⚠️ **Mismas cuatro sobre el arbol de trabajo, y las anclara el Paso 7c-bis.**
~~~

  Es decir: el mismo paso se aplico de dos formas incompatibles en el mismo commit.

- **Por que importa:** `A-010` declara que la excepcion es **mecanica** —que no toca el porque— y su
  disparador de validacion es justamente esta primera ejecucion. La ejecucion lo desmiente: el cierre
  reescribio texto explicativo. La grieta que `CLAUDE.md` acota con «no autoriza nada mas» quedo
  abierta en su estreno, y lo que se pierde es la garantia entera de la excepcion. Gravedad `Media`
  y no `Alta` porque el texto sustituido no altera ninguna afirmacion verificable ni ningun dato del
  registro: lo que rompe es la regla, no el hecho.
- **Que lo corregiria:** decidir cual de las dos formas es la correcta y dejarla escrita en el Paso
  7c-bis sin ambiguedad. Si la buena es la de `D-093` (anadir sin borrar), el texto borrado de
  `D-092` se repone por nota fechada, sin reescribir. Si la buena es sustituir, el Paso 7c-bis tiene
  que decirlo y `CLAUDE.md` dejar de prohibirlo — hoy los dos dicen lo contrario de lo que se hizo.
  ⚠️ Es una recomendacion, no una orden.

---

### F-063 — Las dos ordenes del bloque «Contexto» de `D-092` usan `HEAD` y ya no reproducen
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/decisions.md`, entrada `D-092`, bloque «Contexto» |

- **Que se observo:** `D-092` publica su verificacion previa —la que `CLAUDE.md` exige para toda
  decision con `Origen: report_auditor`— con dos ordenes ancladas a `HEAD`. `HEAD` era `20ef118`
  cuando se escribieron y devolvia `18` y `2`. Hoy `HEAD` es `a48411a` y la primera devuelve `33`.

~~~
$ git show a48411a:_persistence/decisions.md | sed -n '5306,5320p'

```
$ git show HEAD:_persistence/decisions.md \
    | awk '/^### D-086/,0' \
    | awk '/Criterio de cierre/{f=1} /^---$/{f=0} f' | grep -cE '^\$ '
18

$ git show HEAD:_persistence/decisions.md \
    | awk '/^### D-086/,0' \
    | awk '/Criterio de cierre/{f=1} /^---$/{f=0} f' \
    | grep -E '^\$ ' | grep -cE 'git show [0-9a-f]{7,40}:'
2
```
~~~

Reejecucion hoy, literal:

~~~
$ git rev-parse --short HEAD
a48411a

$ git show HEAD:_persistence/decisions.md | awk '/^### D-086/,0' | awk '/Criterio de cierre/{f=1} /^---$/{f=0} f' | grep -cE '^\$ '
33

$ git show HEAD:_persistence/decisions.md | awk '/^### D-086/,0' | awk '/Criterio de cierre/{f=1} /^---$/{f=0} f' | grep -E '^\$ ' | grep -cE 'git show [0-9a-f]{7,40}:'
12

$ git log -1 --format='%h %s' b83ce5e^
20ef118 auditoria R-022 sobre S-022 (97bb948)
~~~

- **Por que importa:** es exactamente el defecto que `F-059` acaba de cobrar —una orden que reproduce
  el dia que se escribe y deja de reproducir despues—, esta vez dentro de la decision que lo corrige.
  `D-088` solo exige el ancla en el bloque «Criterio de cierre», y el Paso 7c-bis solo ancla ese
  bloque; el bloque «Contexto», que es donde vive la evidencia de que un hallazgo se verifico antes
  de aceptarlo, se quedo fuera de las dos reglas. Quien reejecute esas dos ordenes manana leera que
  `F-059` describia 33 ordenes, no 18, y que 12 estaban ancladas, no 2. Gravedad `Media`: la
  conclusion sigue siendo cierta; lo que se pierde es la reproducibilidad de la prueba, que es lo
  unico que hace auditable una aceptacion.
- **Que lo corregiria:** ampliar la regla del ancla a **toda** orden que se escriba en
  `_persistence/`, no solo a las del bloque «Criterio de cierre». Una verificacion previa se hace
  sobre un commit que **ya existe** —`HEAD` en ese momento—, asi que aqui no hay huevo-y-gallina: el
  hash literal se puede escribir en el acto, y es el unico caso que el Paso 7c-bis no necesitaria
  arreglar despues. Las dos ordenes ya escritas no se reescriben (`D-019`): saldrian por nota fechada
  con su forma anclada. ⚠️ Es una recomendacion, no una orden.

---

### F-064 — El Paso 7c-bis cambio la forma de una orden que su propia regla declara «no anclable», y dejo un recuento desfasado
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/decisions.md`, entradas `D-092` y `D-094`; commit `a48411a` |

- **Que se observo:** el Paso 7c-bis advierte que hay ordenes que no se pueden anclar, y nombra el
  ejemplo: «Hay ordenes que preguntan por el arbol de trabajo o por el sistema y no por el commit —
  **un `ls`**, un `git status`. Forzarlas a una forma anclada que no significa lo mismo seria peor
  que dejarlas: se anotan en el reporte como "no anclable, y por que"». En `D-094` se hizo justo eso:
  el `ls -1` se sustituyo por `git ls-tree`. Y en `D-092` una orden sobre dos archivos se partio en
  dos ordenes distintas.

~~~
$ git diff a48411a^ a48411a -- _persistence/decisions.md | grep -E '^[+-]\$ (ls|git ls-tree|grep -c)'
-$ grep -c 'Criterios de cierre anclados' .claude/skills/protocol-close/SKILL.md
-$ grep -c 'Paso 7c-bis' CLAUDE.md .claude/agents/session-closer.md
-$ grep -cE '^> .+ \*\*Nota del 2026-09-06 \(`F-059`, sesion S-023\)' _persistence/decisions.md
-$ grep -c 'Fecha de la sesion contra el commit' .claude/skills/protocol-close/SKILL.md
-$ grep -c 'su fecha es la del reloj' CLAUDE.md
-$ grep -c 'Nota del 2026-09-06 (`F-060`, sesion S-023)' _audit/index.md
-$ ls -1 .claude/agents/gate2_auditor.md .claude/skills/protocol-gate2/SKILL.md _templates/035_gate2/005_verdict.md
+$ git ls-tree --name-only b83ce5e .claude/agents/gate2_auditor.md .claude/skills/protocol-gate2/SKILL.md _templates/035_gate2/005_verdict.md
-$ grep -c 'gate2_auditor\|protocol-gate2\|035_gate2' project.md
-$ grep -c '^## ' .claude/skills/protocol-gate2/SKILL.md _templates/035_gate2/005_verdict.md
~~~

  Consecuencia de la particion: el bloque de `D-092` paso de cuatro ordenes a cinco, y la nota que el
  propio paso anadio debajo sigue diciendo «Las cuatro reproducen».

~~~
$ for h in b83ce5e a48411a; do echo -n "$h: "; git show $h:_persistence/decisions.md | awk '/^### D-092/,/^### D-093/' | awk '/^- \*\*Criterio de cierre:/{f=1} f' | grep -cE '^\$ '; done
b83ce5e: 4
a48411a: 5

$ git show a48411a:_persistence/decisions.md | sed -n '/^### D-092/,/^### D-093/p' | grep -n 'Las cuatro reproducen'
74:que la propia decision crea). Las cuatro reproducen exactamente lo publicado arriba sobre el arbol
~~~

- **Por que importa:** el caso del `ls` esta bien resuelto en el fondo —la nota explica que cambia la
  forma y no la pregunta, y cita el precedente de `D-086`—, pero contradice el texto literal del paso
  que se acababa de escribir; y «las cuatro» donde hay cinco es una afirmacion falsa, pequena pero
  verificable, introducida por el propio automatismo. Gravedad `Baja`: no cambia ningun resultado ni
  ninguna conclusion, y las cinco ordenes reproducen (§1h).
- **Que lo corregiria:** en el Paso 7c-bis, o bien autorizar explicitamente la reescritura de forma
  cuando la pregunta se conserva —y entonces retirar el `ls` de la lista de no anclables—, o bien
  prohibirla; y anadir que, si el numero de ordenes del bloque cambia, la nota lo diga en vez de
  repetir el recuento viejo. ⚠️ Es una recomendacion, no una orden.

---

### F-065 — La seccion 0 del informe llama `HEAD` a `97bb948`, que no lo era
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-023.md`, seccion 0, linea 19 |

- **Que se observo:** el informe afirma que los tres hallazgos «se verificaron contra `HEAD`
  (`97bb948`)». `HEAD` al empezar la sesion era `20ef118` —el commit de la auditoria `R-022`—, y
  `97bb948` era el commit **auditado** por ella, uno anterior.

~~~
$ git show a48411a:_audit/S-023.md | grep -n 'contra .HEAD'
19:Los tres se verificaron contra `HEAD` (`97bb948`) antes de tocar nada — la evidencia de esa
360:Separacion nueva/heredada, contra `HEAD` antes de esta sesion:

$ git log -1 --format='%h %s' b83ce5e^
20ef118 auditoria R-022 sobre S-022 (97bb948)

$ git show --stat --name-only --format= 20ef118
_audit/R-022.md
_audit/findings.md
_audit/index.md
~~~

- **Por que importa:** `CLAUDE.md` manda verificar cada hallazgo **contra `HEAD`** antes de tratarlo,
  y el informe certifica ese cumplimiento nombrando un commit que no era `HEAD`. Aqui el resultado no
  cambia —`20ef118` solo toca `_audit/`, asi que `_persistence/decisions.md` es identico en los
  dos—, pero confundir «commit auditado» con «`HEAD`» es la misma familia de error que `F-053` y
  `F-061` llevan dos auditorias persiguiendo, y esta vez aparece dentro de la frase que da la regla
  por cumplida. Gravedad `Baja`: ninguna cifra publicada resulta incorrecta por esto.
- **Que lo corregiria:** nombrar los dos commits por lo que son —«verificado contra `HEAD`
  (`20ef118`), sobre el estado que `97bb948` dejo»—, como costumbre hacia adelante. La linea ya
  escrita no se reescribe. ⚠️ Es una recomendacion, no una orden.

---

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-059` | **Implementado** (`b83ce5e`) | Las dos partes estan en el diff y verificadas: las 16 ordenes de `D-086` a `D-091` republicadas ancladas a `97bb948` (`16` totales, `0` sin ancla), y el Paso 7c-bis existe en `protocol-close` con su reflejo en `CLAUDE.md` y en el agente. Ver §1e |
| `F-060` | **Implementado** (`b83ce5e`) | El Paso 7d existe, `CLAUDE.md` fija que la fecha es la del reloj, el tablero lleva la nota fechada con el barrido de las ocho filas desfasadas, y la fecha de esta sesion coincide con la del commit. Ver §1f |
| `F-061` | **Implementado** (`b83ce5e`) | «Convenciones» de `_audit/index.md` ya no prescribe la orden derivada como fuente de la columna y remite a `D-090`. Ver §1g |

Ningun otro `F-NNN` estaba sin cerrar al empezar esta auditoria.

⚠️ **Los tres se cierran por el diff, no por el estado de sus tareas.** Que `T-093` a `T-096`
figuren `Implementada` no cierra nada; lo que las cierra son las salidas de §1e, §1f y §1g.

📌 **`F-062`, `F-063` y `F-064` no reabren `F-059`.** Aquel hallazgo era que las ordenes no estaban
ancladas, y ahora lo estan. Los tres nuevos son sobre **como** se ejecuto el mecanismo que lo
corrige, y sobre un bloque —«Contexto»— que `F-059` no cubria.

---

## 4. Lo que NO se pudo comprobar

Ninguno de los controles fallo. Los cuatro —indices, carpetas, fuga de datos y codigos
instanciados— corrieron y devolvieron salida.

⚠️ **Una cosa se verifico por argumento y no por ejecucion, y hay que decirlo:** las tres piezas del
Gate 2 (`gate2_auditor`, `protocol-gate2`, `_templates/035_gate2/005_verdict.md`) **no se han corrido
nunca** y hoy no se pueden correr: dependen de `<GRTH>`, `<BASE>` y `<WSLT>`, carpetas de etapas que
el proyecto no ha adoptado. Lo comprobado aqui es que existen, que son agnosticas, que `project.md`
las declara y que la skill se detiene sola si esos valores faltan. **Su comportamiento real sigue
sin evidencia**, igual que el del Gate 1 desde `S-015`. No es hallazgo —nadie afirma lo contrario—,
pero tampoco es una pieza probada.

---

## 5. Recomendaciones sin hallazgo

- **La contradiccion que quedo en `D-093` es correcta, y conviene decirlo para que nadie la
  «arregle».** El `⚠️ Mismas cuatro sobre el arbol de trabajo, y las anclara el Paso 7c-bis` convive
  hoy con la nota que dice que ya estan ancladas. Se lee raro, pero es exactamente lo que produce
  cumplir la regla de no tocar prosa: el texto en futuro es el que se escribio ese dia. Si se decide
  algo sobre `F-062`, la solucion **no** es borrar esa linea.
- **La tercera pregunta de la seccion 6 tiene respuesta, y es que no.** Nada mas dependia de la
  cronologia del tablero. La unica lectura de antiguedad escrita cuenta **filas**, no fechas, y
  ningun control del repositorio deriva nada de la columna `Fecha`; el Paso 7d nacido en esta sesion
  es el primero que la mira.

```
$ git grep -n 'mas de una sesion de antiguedad' b83ce5e -- .claude _audit
b83ce5e:_audit/R-002.md:374:  siguiente falle en alto al encontrar una fila `Pendiente` con mas de una sesion de antiguedad).
b83ce5e:_audit/index.md:94:🚨 **Una fila con `Auditoria: Pendiente` y mas de una sesion de antiguedad es una auditoria que no
```

- **La segunda pregunta de la seccion 6 —si la reincidencia de `D-088` merece ya su propia deuda— es
  de `manager`, y esta auditoria no la convierte en hallazgo.** Lo que si es cierto es lo que el
  informe teme: tres sesiones seguidas (`S-021`, `S-022`, `S-023`) la mencionan y ninguna la registra,
  y el dia que deje de mencionarse desaparecera sin dejar rastro. `F-063` toca el mismo nervio desde
  otro angulo —el ancla que falta fuera del bloque de cierre— y podria absorberla si se decide
  ampliar la regla.
- **`A-010` ya tiene con que resolverse, y deberia moverse en vez de quedarse abierto.** Su
  disparador era la primera ejecucion real del Paso 7c-bis; esa ejecucion es `a48411a`, y `F-062`
  dice como salio.
