# Auditoria R-025 — sobre S-025

| Campo | Valor |
|---|---|
| Auditoria | R-025 |
| Informe auditado | `_audit/S-025.md` |
| Commit auditado | `f1f2291` |
| Fecha | 2026-09-07 |
| Veredicto | Con hallazgos (4) |

## 1. Que se comprobo

### 1.1 Anclaje del informe

La fila mas antigua `Pendiente` del tablero es `S-025`. El informe vive en dos commits; manda el
hash literal de su cabecera, no el que deriva `git log`:

```
$ git log --format='%h %ad %s' --date=short -- _audit/S-025.md
f4658f5 2026-09-07 S-025: ancla el informe y los criterios de cierre al hash f1f2291
f1f2291 2026-09-07 S-025: acepta F-066 a F-069 (T-101 a T-104); nacen las plantillas de _templates/040_evol/ (D-105)

$ git show f4658f5:_audit/S-025.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `f1f2291` |

$ git show --stat --name-only --format= f1f2291 | grep -c .
13

$ git show --stat --name-only --format= f4658f5 | grep -c .
3
```

El commit sustantivo es `f1f2291`, con 13 archivos, que es la lista que enumera la seccion 1 del
informe. El commit de anclaje (`f4658f5`) toca tres: el informe, `decisions.md` y `tasks.md` — los
tres que el Paso 7c/7c-bis autoriza desde `D-102`.

Fecha del commit contra la del informe (Paso 7d, `D-093`): las dos son `2026-09-07`.

```
$ git log -1 --format=%ad --date=short f1f2291
2026-09-07
```

### 1.2 Indices de `_persistence/` (indice contra detalle)

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show f1f2291:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show f1f2291:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete coherentes, sin diferencias.

### 1.3 Carpetas del arbol contra las declaradas

```
$ diff <(git ls-tree -d --name-only f1f2291 | sed 's|$|/|' | sort) <(git show f1f2291:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos diferencias son las que `project.md` declara con su razon escrita (`temporal/` excluida por
`.gitignore`; `010_prototype/` declarada por adelantado, `D-061`). No son hallazgo.

### 1.4 Fuga de datos propios en lo agnostico

Patron ampliado sobre las seis rutas copiables (se usa el patron de `protocol-close`, porque
`T-038` sigue `No implementada`):

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" f1f2291 -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida)
```

Cero lineas.

### 1.5 Caracteres de control (Paso 2e del informe)

```
$ for f in $(git diff --name-only --diff-filter=d a1f5fa8 f1f2291); do n=$(git show f1f2291:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); h=$(git show a1f5fa8:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); echo "$f base=$h commit=$n"; done
.claude/skills/protocol-close/SKILL.md base=0 commit=0
_audit/R-024.md base=0 commit=0
_audit/S-024.md base=0 commit=0
_audit/S-025.md base=0 commit=0
_audit/findings.md base=1 commit=1
_audit/index.md base=0 commit=0
_persistence/assumptions.md base=0 commit=0
_persistence/decisions.md base=7 commit=7
_persistence/lessons.md base=0 commit=0
_persistence/progress.md base=0 commit=0
_persistence/tasks.md base=5 commit=5
_phases/040_evol.md base=0 commit=0
_templates/040_evol/005_iteration_NNN.md base=0 commit=0
_templates/040_evol/010_slice_NNN.md base=0 commit=0
```

La seccion 8 del informe reproduce exactamente: 1 / 7 / 5, heredadas, ninguna nueva.

### 1.6 La frontera del Paso 7c-bis (lo que la seccion 6 pidio mirar)

El commit de anclaje no salio de `decisions.md` y `tasks.md`, y no borro ni una palabra de prosa. El
control que `A-010` escribio para refutarse sale vacio esta vez:

```
$ git diff f4658f5^ f4658f5 -- _persistence/decisions.md | grep -E '^-[^-]' | grep -vE '^-\$ ' | grep -vE '^-[0-9]+:'
(sin salida)

$ git diff f4658f5^ f4658f5 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^\+[^+]' | grep -vE '^\+\$ '
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las cuatro reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las cuatro reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las tres reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las tres reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las seis reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las cuatro reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las cuatro reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las tres reproducen exactamente lo publicado arriba.
+📌 **Ancladas por el Paso 7c-bis al commit `f1f2291`.** Las tres reproducen exactamente lo publicado arriba.
```

Lo unico anadido fuera de los bloques de codigo son las nueve notas de anclaje que el propio paso
tiene autorizadas. **La asimetria «deteccion universal, escritura acotada» se respeto exactamente.**

### 1.7 Las 34 ordenes ancladas reproducen

Reejecutadas todas, una por una, contra el commit al que se anclaron:

```
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show f4658f5:$f | grep -E '^\$ .*f1f2291' | sort -u | while IFS= read -r line; do cmd="${line#\$ }"; echo "--- $cmd"; eval "$cmd" 2>&1 | head -5; done; done
```

Las 34 (20 en `decisions.md`, 14 en `tasks.md`) devuelven exactamente la salida publicada: los
`grep -c` sobre `SKILL.md` dan `1`, el de `S-024.md` da `1`, el de `decisions.md` da `0`, el de
`tasks.md` con `a1f5fa8` da `12`, el `ls-tree` lista las dos plantillas, los dos `grep -cE` de
codigos en las plantillas dan `0`, y el de los cuatro campos de la plantilla de iteracion da `4`.
**Ninguna discrepancia.**

```
$ expr $(git show f4658f5:_persistence/decisions.md | grep -cE '^\$ .*f1f2291') + $(git show f4658f5:_persistence/tasks.md | grep -cE '^\$ .*f1f2291')
34
```

### 1.8 Las afirmaciones de las secciones 0, 1, 2 y 3

```
$ for f in decisions tasks assumptions lessons; do echo "-- $f"; diff <(git show a1f5fa8:_persistence/$f.md | grep -oE '^### [A-Z]+-[0-9]+' | sort -u) <(git show f1f2291:_persistence/$f.md | grep -oE '^### [A-Z]+-[0-9]+' | sort -u); done
-- decisions
100a101,105
> ### D-101
> ### D-102
> ### D-103
> ### D-104
> ### D-105
-- tasks
100a101,105
> ### T-101
> ### T-102
> ### T-103
> ### T-104
> ### T-105
-- assumptions
10a11
> ### A-011
-- lessons
34a35
> ### L-035

$ git show f1f2291:_persistence/progress.md | grep -cE '^### S-025'
1

$ git show f1f2291:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -cE '\| No implementada \|'
6

$ git ls-tree --name-only f1f2291 _templates/040_evol/
_templates/040_evol/005_iteration_NNN.md
_templates/040_evol/010_slice_NNN.md
```

Todo lo que las secciones 1, 2 y 3 declaran esta en el diff, y no hay en el diff nada relevante que
el informe no mencione. Las tres verificaciones staged de la seccion 0 reproducen contra `f1f2291`
(`1`, `0`, `12`).

### 1.9 La edicion de `_phases/040_evol.md` §5 (lo que la seccion 6 pidio mirar)

El informe pide revisar si tratar un archivo de etapa como «procedimiento vivo» y no como «registro»
es consistente con `D-019`. Lo es: `D-019` protege lo ya auditado del **registro** (`_persistence/`,
`_audit/`), y `_phases/` es procedimiento, exactamente igual que `protocol-close`, que esta misma
sesion edito en su sitio y sin nota fechada. **No es hallazgo.**

Tambien pidio mirar que se escriban plantillas para una etapa no adoptada: es el patron ya
establecido — `_templates/` tiene subcarpeta para ocho etapas y gates, y solo dos etapas estan
declaradas en `project.md`. **No es hallazgo.**

## 2. Hallazgos

### F-070 — El barrido universal de la nota de cierre se etiqueta «sobre `f1f2291`» y su salida no reproduce contra `f1f2291`
| Campo | Valor |
|---|---|
| Gravedad | Alta |
| Donde | `_audit/S-025.md` §7, nota de cierre; `.claude/skills/protocol-close/SKILL.md`, Paso 7c |

- **Que se observo:** el informe encabeza el barrido con «El barrido universal que exige `D-101`,
  corrido sobre `HEAD` (`f1f2291`)» y publica tres lineas: `assumptions.md: 2`, `decisions.md: 2`,
  `tasks.md: 14`. La orden publicada es hibrida — toma la **lista de archivos** de `git ls-tree HEAD`
  pero el **contenido** de `cat "$f"`, es decir del arbol de trabajo. Corrida contra el contenido de
  `f1f2291`, que es el hash que la etiqueta cita, devuelve otra cosa:

```
$ for f in $(git ls-tree -r --name-only f1f2291 _persistence _audit | grep -v '_audit/S-'); do n=$(git show f1f2291:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/assumptions.md: 2
_persistence/decisions.md: 22
_persistence/tasks.md: 28

$ for f in $(git ls-tree -r --name-only HEAD _persistence _audit | grep -v '_audit/S-'); do n=$(cat "$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/assumptions.md: 2
_persistence/decisions.md: 2
_persistence/tasks.md: 14
```

  La salida publicada (2 / 2 / 14) corresponde al estado **posterior al anclaje** —el arbol de
  trabajo, que despues fue `f4658f5`—, no a `f1f2291`. Sobre `f1f2291` las cifras son 2 / 22 / 28.

- **Por que importa:** es el control que `D-101` puso como nucleo de la nota de cierre, y todo el
  valor de esa nota esta en poder reejecutarla. Quien la reejecute contra el hash que la propia nota
  cita obtiene 22 y 28 donde el informe publica 2 y 14, y no puede saber si se equivoco el informe o
  si cambio el repositorio — que es literalmente el argumento con el que se abrieron `F-064` y
  `F-068`. Ademas, las tres explicaciones que siguen a la salida («son los dos comandos de `A-011`»,
  «las dos ordenes de la seccion Contexto de `D-102`», «los catorce bloques congelados de `T-097` a
  `T-100`») describen 2 / 2 / 14, no 2 / 22 / 28: si el barrido se hubiera corrido sobre el commit
  citado, ninguna de las tres seria cierta.
- **Que lo corregiria:** que la orden se publique en la forma anclada —`git show <hash>:"$f"`, no
  `cat "$f"`— y que la nota diga a que estado corresponde su salida. El `SKILL.md` ya la escribe
  anclada en su recuadro; lo que fallo es que el cierre publico otra. ⚠️ Es una recomendacion, no
  una orden.

---

### F-071 — La nota de cierre incumple, en su primera ejecucion, la regla que esa misma sesion escribio: el barrido no salio vacio y no se detuvo
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-025.md` §7; `.claude/skills/protocol-close/SKILL.md`, Paso 7c, recuadro «Que dice la nota de la seccion 7, exactamente» |

- **Que se observo:** el recuadro escrito en esta misma sesion (`T-101` / `D-101`) dice:

```
$ git show f1f2291:.claude/skills/protocol-close/SKILL.md | grep -A2 'La orden va con su salida, y esa salida tiene que estar VACIA'
⛔ **La orden va con su salida, y esa salida tiene que estar VACIA.** Una linea significa que queda
una orden sin anclar, y entonces la nota no puede decir que no queda ninguna: **se detiene y se
reporta**, como cualquier otra discrepancia de este paso.
```

  El barrido devolvio tres lineas. El informe no se detuvo: escribio «**No sale vacio, y se explica
  cada linea en vez de forzarla a cero**», justifico las tres en prosa, y a continuacion su frase de
  cierre afirma que «no queda ninguna sin una forma anclada» — que es exactamente lo que el recuadro
  dice que en ese caso no se puede afirmar.
- **Por que importa:** una regla cuya primera ejecucion la incumple, con la excepcion redactada en
  el propio informe, deja de ser una regla. Y la excepcion se introdujo **sin `D-XXX`**: las cinco
  decisiones de la sesion (`D-101` a `D-105`) no la contienen. El registro queda con una regla en el
  protocolo y una practica contraria en el informe, sin nada que diga cual manda — la misma forma de
  defecto que abrieron `F-052` y `F-053`.
- **Que lo corregiria:** o bien acotar el recuadro con una `D-XXX` que distinga «orden pendiente de
  anclar» de «coincidencia del patron» y de «bloque congelado por `D-019`» —que es la distincion que
  el informe hace de hecho—, o bien afinar el patron del barrido para que no las devuelva. Lo que no
  puede quedarse es la regla diciendo «vacia o te detienes» mientras la practica dice otra cosa.
  ⚠️ Es una recomendacion, no una orden.

---

### F-072 — El recuento accesorio del Paso 2d vuelve a ser falso: publica 27 ordenes distintas y son 22
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-025.md` §7 |

- **Que se observo:** el informe publica la orden accesoria con su salida, como `D-103` acaba de
  exigir — pero la salida no reproduce. Reejecutada en la forma anclada que el propio informe declara
  equivalente:

```
$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
35

$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
22
```

  La cifra principal (**35**) reproduce. La accesoria publicada es **27**; el valor real es **22**.
  Se comprueba tambien sobre la propia lista numerada del informe: las filas 3-15 y 22-34 son las
  mismas trece, luego 35 - 13 = 22.

  En el mismo parrafo, el informe describe esas repeticiones como «filas 3-15 y 22-34 son **las
  mismas ocho**», y el `uniq -d` que publica a continuacion lista trece lineas:

```
$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sed 's/^+//' | sort | uniq -d | wc -l
13
```

- **Por que importa:** es la reincidencia de `F-068` en la misma sesion que lo corrige, y por la
  misma via: la cifra accesoria se escribio sin correr la orden que ahora la acompaña. `D-103` cerro
  la puerta de «no se publica la orden» y el defecto entro por la de al lado — que es el mecanismo
  que `L-035`, nacida en esta sesion, describe.
- **Que lo corregiria:** una nota fechada en `_audit/S-025.md` que republique el `sort -u` con su
  salida real (22) y corrija «las mismas ocho» por trece. No se reescribe la linea original
  (`D-019`). ⚠️ Es una recomendacion, no una orden.

---

### F-073 — La frase de cierre contradice a la nota que la precede y publica dos cifras que no salen de ninguna orden
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-025.md` §7, «NOTA DE CIERRE» y «Frase de cierre» |

- **Que se observo:** tres afirmaciones incompatibles entre si o con el repositorio, todas en la
  misma seccion:

  1. La nota dice «**Las tres que NO se anclaron**» (filas 1, 2 y 35); la frase de cierre, once
     lineas mas abajo, dice «34 se anclaron directamente a `f1f2291` en esta pasada, y **la unica que
     no** (`T-105`)». Las filas 1 y 2 pasan de no-ancladas a inexistentes.
  2. La frase de cierre abre con «no queda ninguna sin una forma anclada disponible en algun sitio
     del registro», y la nota anterior dice de las filas 1 y 2 que «quedan con `<hash>` / `<commit>`
     literal».
  3. «Las **16** lineas que devuelve el barrido universal de arriba» — el barrido devuelve tres
     lineas cuyas cifras suman **18**, y la enumeracion que sigue en la misma frase las cuenta: «dos
     evidencias de `A-011`, dos coincidencias de un patron, y **catorce** bloques congelados» =
     2 + 2 + 14 = 18.

  La aritmetica del «34 de las 35» tampoco cierra: de las 35 ordenes listadas, solo 33 viven en
  `decisions.md` o `tasks.md`, y una de esas 33 es la de `T-105`, que no se anclo:

```
$ git diff -U0 f1f2291^ f1f2291 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
33

$ git diff -U0 f1f2291^ f1f2291 -- _persistence/assumptions.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
2
```

  Es decir: de las 35 listadas se anclaron **32**, no 34. Las 34 anclas reales incluyen dos ordenes
  (`git show <hash>:_persistence/tasks.md | grep -cE '^\$ git show a1f5fa8:'`, en `D-102` y `T-102`)
  que el propio filtro del Paso 2d excluye de la lista por contener un hash.

- **Por que importa:** la nota de cierre es el mecanismo entero con el que `D-101` cierra el
  pendiente de la seccion 7. Si su parrafo final contradice al anterior y sus cifras no salen de
  ninguna orden, la nota deja de ser un puntero comprobable y pasa a ser una afirmacion — que es
  justo lo que `F-066` se abrio para arreglar.
- **Que lo corregiria:** una nota fechada que fije las tres cifras contra su orden: cuantas de las 35
  se anclaron (32), cuantas anclas se escribieron en total (34, con las dos que la lista no recoge y
  por que), y cuantas lineas devuelve el barrido (18, no 16). ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-066` | **Implementado** en `f1f2291` | El recuadro «Que dice la nota de la seccion 7, exactamente» esta en el `SKILL.md` (`grep -c` devuelve `1`) y la seccion 7 de `S-025.md` lleva su `**NOTA DE CIERRE**` (`grep -c '^\*\*NOTA DE CIERRE\*\*'` devuelve `1`). El hecho que `F-066` describia —la nota ausente— ya no se da. Sus defectos de contenido van como `F-070`, `F-071` y `F-073`, que son hechos nuevos y no la misma falta |
| `F-067` | **Implementado** en `f1f2291` | `git show f1f2291:.claude/skills/protocol-close/SKILL.md \| grep -c 'Son DOS archivos, no uno'` devuelve `1`; `git show f1f2291:_persistence/tasks.md \| grep -cE '^\$ git show a1f5fa8:'` devuelve `12`. Las doce ordenes tienen su forma anclada republicada, y el Paso 7c-bis alcanza ya a `tasks.md` — verificado en la practica: `f4658f5` anclo 14 ordenes en ese archivo |
| `F-068` | **Implementado** en `f1f2291` | Las dos correcciones que pedia estan: la nota fechada en `S-024.md` (`grep -c 'el recuento accesorio de arriba es falso: son 31, no'` devuelve `1`) y la regla en el `SKILL.md` (`grep -c 'Y si se da esa segunda cifra, va con SU orden y su salida cruda'` devuelve `1`). Que la cifra accesoria vuelva a fallar en `S-025` es un hecho nuevo, sobre otro commit y con otra causa —salida no reproducible, no cifra sin orden—: va como `F-072` |
| `F-069` | **Implementado** en `f1f2291` | `git show f1f2291:_persistence/decisions.md \| grep -A1 '^publicado arriba\.$' \| grep -c '^---$'` devuelve `0`, y la regla esta en el `SKILL.md` (`grep -c 'esta linea en blanco es obligatoria'` devuelve `1`). El recuento global de `---` pegados a texto en `decisions.md` baja de 3 a 1; los dos corregidos son los del Paso 7c-bis, que es lo que el hallazgo describia. El restante y el de `tasks.md` son heredados y ajenos a `F-069` |

```
$ for h in a1f5fa8 f1f2291; do echo "== $h"; for f in decisions tasks; do echo -n "$f: "; git show $h:_persistence/$f.md | awk 'prev!="" && $0=="---" {c++} {prev=$0} END{print c+0}'; done; done
== a1f5fa8
decisions: 3
tasks: 1
== f1f2291
decisions: 1
tasks: 1
```

## 4. Lo que NO se pudo comprobar

Ninguno de los controles quedo sin correr.

⚠️ Una salvedad de alcance, no un control fallido: el barrido de fuga de datos propios se corrio con
el patron de `protocol-close` (que incluye `_templates` y `_workflow`), porque `T-038` —igualar el
barrido de `protocol-audit` con el del cierre— sigue `No implementada`. Con el patron literal de este
protocolo, `_templates` y `_workflow` habrian quedado sin barrer.

## 5. Recomendaciones sin hallazgo

- **La lista numerada del Paso 2d se ha vuelto el sitio donde nacen los errores de recuento.** Tres
  de los cuatro hallazgos de esta auditoria, y dos de la anterior (`F-064`, `F-068`), son cifras de
  esa seccion. La lista literal de 35 lineas reproduce sin problema; lo que falla siempre es la prosa
  que la resume. Vale la pena considerar que la seccion publique **solo ordenes y salidas**, sin
  parrafos que recuenten a mano lo que la salida ya dice.
- **La orden del barrido universal existe hoy en dos formas** —la anclada, en el recuadro del
  `SKILL.md`, y la del arbol de trabajo, que es la que el informe publico—. Dejar una sola forma
  escrita, la anclada, quitaria de raiz la posibilidad de `F-070`.
- **Las dos plantillas de `_templates/040_evol/` son solidas** en lo que este protocolo puede
  comprobar: cero codigos instanciados (`grep -cE` de los dieciseis prefijos devuelve `0` en las dos)
  y cero datos propios del proyecto. No hay nada que señalar en ellas.
