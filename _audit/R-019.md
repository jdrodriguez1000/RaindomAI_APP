# Auditoria R-019 — sobre S-019

| Campo | Valor |
|---|---|
| Auditoria | R-019 |
| Informe auditado | `_audit/S-019.md` |
| Commit auditado | `1b30e16` |
| Fecha | 2026-09-03 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

**Anclaje del commit** — el informe esta en un commit, y es sobre ese que se audita:

```
$ git log -1 --format=%h -- _audit/S-019.md
1b30e16
```

**Indice ↔ detalle en `_persistence/`** — los siete archivos, sin diferencias:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 1b30e16:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 1b30e16:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

**Carpetas del arbol contra las declaradas** — las dos unicas diferencias son las dos que
`project.md` explica por escrito (`temporal/`, excluida en `.gitignore`; `010_prototype/`, declarada
por adelantado, `D-061`). `_templates/025_wslt/` es subcarpeta, no de primer nivel, y no entra en
este control:

```
$ diff <(git ls-tree -d --name-only 1b30e16 | sed 's|$|/|' | sort) <(git show 1b30e16:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

**Fuga de datos propios en lo agnostico** — cero lineas:

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" 1b30e16 -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1
```

Ampliado a `_templates` y `_workflow` (no lo exige este protocolo — `T-038` sigue abierta —, se corre
igual): una sola linea, y es falso positivo, la palabra «USUARIOS» en mayusculas dentro de una frase,
no el segmento de ruta.

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com|USUARIO" 1b30e16 -- _templates _workflow
1b30e16:_templates/010_prototype/025_business_validation.md:14:> 🚨 **SESION APARTE, CON EL PATROCINADOR, SIN USUARIOS DELANTE.**
```

**Lista de archivos de la seccion 1 contra el commit** — coincide exactamente, los catorce:

```
$ git show --name-only --format= 1b30e16
.claude/skills/protocol-close/SKILL.md
_audit/S-018.md
_audit/S-019.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_persistence/techdebt.md
_phases/025_wslt.md
_templates/025_wslt/005_skeleton_record.md
_workflow/025_wslt.md
```

**Entradas que nacen** — las que el informe declara, y ninguna mas:

```
$ for f in decisions tasks assumptions lessons techdebt progress; do echo "== $f"; git diff -U0 1b30e16^ 1b30e16 -- _persistence/$f.md | grep -E '^\+### ' ; done
== decisions
+### D-077 - El patron del control de codigos instanciados se deriva de las tablas, no se escribe a mano
+### D-078 - El archivo de etapa del esqueleto se escribe por adelantado, y la etapa NO queda adoptada
+### D-079 - La etapa del esqueleto produce un acta propia, y por eso tiene plantilla
+### D-080 - El despliegue no se delega en la IA, y por eso el eje de impacto puntua 2 y no 3
== tasks
+### T-066 - Publicar el recuento real de la seccion 7 de `S-018` y numerar la lista con la orden (`F-045`)
+### T-067 - Corregir la atribucion `L-020`/`L-019` en `S-018` y derivar del diff las entradas editadas (`F-046`)
+### T-068 - Derivar de las tablas el patron del control de codigos instanciados (`F-047`)
+### T-069 - Escribir el archivo de etapa del esqueleto que camina (`_phases/025_wslt.md`)
+### T-070 - Escribir la plantilla del acta del esqueleto (`_templates/025_wslt/005_skeleton_record.md`)
+### T-071 - Escribir el reparto de la etapa del esqueleto (`_workflow/025_wslt.md`, `D-080`)
== assumptions
+### A-007 - Habra un humano disponible para ejecutar cada despliegue de la etapa del esqueleto
== lessons
+### L-027 - Cuando existe la orden que produce una lista, escribirla a mano es el error, no el atajo
== techdebt
+### DT-004 - Siete lineas nuevas de `S-019` repiten el defecto de `DT-003`, en `decisions.md` y `tasks.md`
== progress
+### S-019 - Se aceptan `F-045` a `F-047` (`T-066` a `T-068`, `D-077`); nace `_phases/025_wslt.md` y su reparto (`D-078` a `D-080`)
```

**Entradas existentes editadas** — verificado de forma independiente, mapeando **cada linea añadida**
(no solo la cabecera del hunk) a la entrada que la contiene en la version nueva del archivo. Es el
punto que la seccion 6 del informe pidio re-verificar:

```
$ git show 1b30e16:_persistence/decisions.md > /tmp/new.md
$ git diff -U0 1b30e16^ 1b30e16 -- _persistence/decisions.md | awk '/^@@/{split($3,a,","); s=substr(a[1],2)+0; n=(a[2]==""?1:a[2]+0); for(i=0;i<n;i++) print s+i}' > /tmp/lines.txt
$ awk 'NR==FNR{L[$1]=1;next} /^### /{h=$0} {if(FNR in L && h!="") print h}' /tmp/lines.txt /tmp/new.md | sort -u
### D-073 - Las plantillas de la baseline se numeran por orden de procedimiento, no por el orden de la tabla de artefactos
### D-076 - El reparto de la baseline puntua la variabilidad de la entrada en 2, y por eso no declara discrepancia
### D-077 - El patron del control de codigos instanciados se deriva de las tablas, no se escribe a mano
### D-078 - El archivo de etapa del esqueleto se escribe por adelantado, y la etapa NO queda adoptada
### D-079 - La etapa del esqueleto produce un acta propia, y por eso tiene plantilla
### D-080 - El despliegue no se delega en la IA, y por eso el eje de impacto puntua 2 y no 3
```

Y el contenido concreto atribuido a `D-076` son **tres lineas**: una vacia, el separador `---` y otra
vacia. Es el borde del append, no una edicion:

```
$ awk 'NR==FNR{L[$1]=1;next} /^### /{h=$0} {if(FNR in L && h ~ /D-076/) printf "%d: [%s]\n", FNR, $0}' /tmp/lines.txt /tmp/new.md
4093: []
4094: [---]
4095: []
```

La misma comprobacion sobre `tasks.md`, `assumptions.md`, `lessons.md` y `techdebt.md` devuelve el
mismo patron para `T-065`, `A-006`, `L-026` y `DT-003` (linea vacia, `---`, linea vacia). **La
afirmacion «append puro, ninguna entrada existente editada» se sostiene**, y `D-073` si esta editada
con su nota fechada de `T-068`, como el informe dice.

**Correccion de `F-045`** — las tres cifras que la nota publica reproducen sobre `9a52cfa`:

```
$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
26
$ (la misma, con | sort -u | wc -l)
23
$ (la misma, con | sort | uniq -d | wc -l)
3
```

**Correccion de `F-046`** — la orden de atribucion que la nota publica reproduce literal:

```
$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence/lessons.md | awk '/^@@/{split($3,a,","); print a[1]+0}' | while read n; do git show 9a52cfa:_persistence/lessons.md | awk -v n="$n" 'NR<=n && /^### [A-Z]+-[0-9]+/{e=$2} END{print n": "e}'; done
36:
709: L-019
862: L-024
899: L-024
902: L-024
912: L-024
```

**Correccion de `F-047`** — el patron derivado reproduce, y ahora si incluye `H`:

```
$ PAT=$( { sed -n '/^## Codigos/,/^---/p' project.md | grep -oE '^\| `[A-Z]+-[A-Za-z0-9]+`'; sed -n '/^## 46\. Identificadores/,/^```/p' _methodology/000_method.md | grep -oE '^\| `[A-Z]+-[A-Za-z0-9]+`'; } | tr -d '|` ' | sed 's/-.*//' | sort -u | awk '{print length, $0}' | sort -rn | cut -d' ' -f2 | paste -sd'|' - ) && echo "PAT=$PAT"
PAT=ADR|VS|TC|SC|FT|DT|T|S|R|N|L|I|H|F|D|C|A
```

**El endurecimiento de `protocol-close`** esta en el diff: Paso 2d con `cat -n` y `sort | uniq -d`,
Paso 6b con las dos ordenes de atribucion.

```
$ git show 1b30e16:.claude/skills/protocol-close/SKILL.md | grep -c 'nunca a ojo'
1
$ git show 1b30e16:.claude/skills/protocol-close/SKILL.md | grep -c 'que entradas NACEN'
1
```

**Los tres archivos nuevos de `025_wslt`** — ni un codigo instanciado ni un dato propio del proyecto
en ninguno de los tres:

```
$ for f in _phases/025_wslt.md _workflow/025_wslt.md _templates/025_wslt/005_skeleton_record.md; do git show 1b30e16:$f | grep -noE "(^|[^A-Za-z])(${PAT})-[0-9]{2,3}\b"; echo "codigos_exit=$?"; git show 1b30e16:$f | grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com"; echo "fuga_exit=$?"; done
codigos_exit=1
fuga_exit=1
codigos_exit=1
fuga_exit=1
codigos_exit=1
fuga_exit=1
```

**Los recuentos publicados en las posiciones 9 a 21, 24, 27 y 34** de la seccion 7, reejecutados
contra el commit (no contra el arbol de trabajo): reproducen todos.

```
$ git show 1b30e16:_phases/025_wslt.md > /tmp/ph.md; git show 1b30e16:_workflow/025_wslt.md > /tmp/wf.md
9: 1    10: 5   11: 1   12: 1   13: 1   14: 1   15: 1   16: 1   17: 6   18: 6
19: 2   20: 1   21: 4   24: 1   27: 1   34: _templates/025_wslt/005_skeleton_record.md
```

**La afirmacion empirica de `DT-004`** —que las tres ordenes de verificacion de `D-077`, tal como
estan escritas, devuelven `0`— es cierta:

```
$ git show 1b30e16:_persistence/decisions.md > /tmp/d.md; for n in 4141 4144 4147; do sed -n "${n}p" /tmp/d.md | sed 's/^\$ //' | bash; done
0
0
0
```

**Tareas abiertas de la seccion 2** — la lista del informe coincide con el commit (`T-001`, `T-002`,
`T-003`, `T-037`, `T-038`).

**Atribucion de `Origen` en `T-069` a `T-071`** — el otro punto que la seccion 6 pidio auditar. No
puedo verificar la conversacion (arranco en frio, y no debo usarla como evidencia); lo que si se
verifica es la coherencia con lo registrado: cada tarea lleva el mismo `Origen` que la decision que
la produce.

```
$ git show 1b30e16:_persistence/tasks.md | awk '/^### T-0(69|70|71)/{h=$2} /^\| Origen/{if(h)print h,$0}'
T-069 | Origen | usuario |
T-070 | Origen | usuario |
T-071 | Origen | manager |
$ git show 1b30e16:_persistence/decisions.md | awk '/^### D-0(78|79|80)/{h=$2} /^\| Origen/{if(h)print h,$0}'
D-078 | Origen | usuario |
D-079 | Origen | usuario |
D-080 | Origen | manager |
```

Coherente. **Sin hallazgo por este punto.**

**Fila de `_audit/index.md` y estados de `findings.md`** — la fila de `S-019` nace con `Pendiente` en
las tres ultimas columnas, y `F-045`/`F-046`/`F-047` pasan a `Aceptado — pendiente` citando
`T-066`/`T-067`/`T-068`, con `Cerrado en` vacio. Correcto: cerrarlos no le tocaba a `manager`.

```
$ git show 1b30e16:_audit/index.md | grep -n 'S-019'
30:| `S-019.md` | S-019 | 2026-09-03 | Pendiente | Pendiente | Pendiente | - |
$ git show 1b30e16:_audit/findings.md | awk '/^### F-04[567]/{h=$2} /^\| (Estado|Registrado en|Cerrado en)/{if(h)print h,$0}'
F-045 | Estado | Aceptado — pendiente |
F-045 | Registrado en | `T-066` |
F-045 | Cerrado en | |
F-046 | Estado | Aceptado — pendiente |
F-046 | Registrado en | `T-067` |
F-046 | Cerrado en | |
F-047 | Estado | Aceptado — pendiente |
F-047 | Registrado en | `T-068` |
F-047 | Cerrado en | |
```

## 2. Hallazgos

### F-048 — La seccion 7 de `S-019` publica 42 de las 48 lineas que devuelve su propia orden, y las seis omitidas quedan sin salida
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-019.md`, seccion 7 |

- **Que se observo:** la seccion 7 anuncia «Lista COMPLETA, numerada por la propia orden (`cat -n`),
  sin deduplicar» y cierra con «**Recuento: 42 lineas devueltas** (31 ordenes distintas; hay 11
  repetidas)». La orden anclada que el propio informe publica devuelve **48 lineas y 37 ordenes
  distintas**:

```
$ git diff -U0 1b30e16^ 1b30e16 -- _persistence _audit ":(exclude)_audit/S-019.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
48
$ (la misma, con | sort -u | wc -l)
37
$ (la misma, con | sort | uniq -d | wc -l)
11
```

  Las seis que faltan son las posiciones 43 a 48, todas dentro de la entrada `DT-004` de
  `techdebt.md` —que ya estaba escrita cuando el informe se redacto, porque la seccion 6 la cita—:

```
$ (la misma, con | cat -n | sed -n '43,48p')
    43	+$ sed -n '4144p' _persistence/decisions.md | sed 's/^\$ //' | cat -A | head -1
    44	+$ sed -n '4141p' _persistence/decisions.md | sed 's/^\$ //' | bash
    45	+$ sed -n '4144p' _persistence/decisions.md | sed 's/^\$ //' | bash
    46	+$ sed -n '4147p' _persistence/decisions.md | sed 's/^\$ //' | bash
    47	+$ grep -rnoE "\b(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]{3}\b" _templates/020_baseline/ | wc -l
    48	+$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | wc -l
```

  La nota del informe justifica solo la posicion 42 (la primera linea de un `python -c` multilinea);
  las 43 a 48 **no son lineas de continuacion**, son ordenes propias que el filtro si captura. Y el
  bloque de reejecucion se declara explicitamente «(1-41; la 42 se trata arriba)», con lo que las
  seis quedan sin salida publicada.
- **Por que importa:** es el mismo defecto que `F-045` —recuento falso en la seccion 7 y ordenes sin
  salida— y aparece **en el commit que corrige `F-045`**, despues de endurecer el Paso 2d para
  numerar con la orden. El remedio adoptado (`cat -n`, `uniq -d`) cubre la numeracion pero no impide
  truncar la lista al pegarla; mientras el recuento se escriba debajo de una lista recortada, `L-027`
  no esta aplicada al informe que la estrena.
- **Que lo corregiria:** pegar la salida completa de la orden y tomar las tres cifras de `wc -l`,
  `sort -u | wc -l` y `sort | uniq -d`, sin escribirlas aparte; y publicar la salida de las seis
  posiciones que quedaron sin ella. ⚠️ Es una recomendacion, no una orden.

### F-049 — `DT-004` declara siete lineas nuevas con `0x08` donde hay diez, y dos de las no contadas alteran la salida cruda publicada en la correccion de `F-045`
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/techdebt.md` (`DT-004`), `_audit/S-018.md` (nota de `T-066`), `_audit/S-019.md` |

- **Que se observo:** `DT-004` afirma «**siete lineas nuevas** de esta misma sesion […] Cinco estan
  en `_persistence/decisions.md` […] y dos en `_persistence/tasks.md`», y la seccion 6 del informe
  declara que no se barrio el repositorio completo, enumerando los archivos no cubiertos —pero esa
  enumeracion omite justamente los dos donde el defecto tambien aparecio. Barrido sobre todo el arbol
  del commit:

```
$ for f in $(git ls-tree -r --name-only 1b30e16 | grep -E '\.md$'); do n=$(git show 1b30e16:"$f" | grep -c $'\x08'); if [ "$n" -gt 0 ]; then echo "$f: $n"; fi; done
_audit/S-018.md: 2
_audit/S-019.md: 1
_audit/findings.md: 1
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

  Contra el commit padre, lo **nuevo de esta sesion** son diez lineas, no siete (la de
  `_audit/findings.md` si es anterior):

```
$ for f in _audit/S-018.md _audit/findings.md _persistence/decisions.md _persistence/tasks.md; do echo "$f parent=$(git show 1b30e16^:$f | grep -c $'\x08') commit=$(git show 1b30e16:$f | grep -c $'\x08')"; done
_audit/S-018.md parent=0 commit=2
_audit/findings.md parent=1 commit=1
_persistence/decisions.md parent=2 commit=7
_persistence/tasks.md parent=3 commit=5
```

  Las dos de `_audit/S-018.md` caen **dentro de la nota fechada que corrige `F-045`**, y no son
  prosa: son la transcripcion de la salida cruda de dos ordenes que esa nota publica. La salida real
  lleva `\b`; la publicada lleva el caracter de control (`^H` es el `0x08` que muestra `cat -A`):

```
$ git show 1b30e16:_audit/S-018.md | grep -n $'\x08' | cat -A | cut -c1-140
254:> +$ grep -rnoE "^H(FT|SC|VS|TC|ADR)-[0-9]{3}^H" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"$
266:>     18^I+$ grep -rnoE "^H(FT|SC|VS|TC|ADR)-[0-9]{3}^H" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"$

$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d | cat -A | cut -c1-140
+$ grep -nE '^\| `(FT|SC)-XXX`' project.md$
+$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l$
+$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"$
```

  La de `_audit/S-019.md` es la linea 189, en prosa: «Restituyendo el `` que el `0x08` reemplazo» —
  el propio informe pierde el `\b` justo en la frase que explica como restituirlo.
- **Por que importa:** la deuda queda registrada con un ambito y una cifra que el commit desmiente,
  asi que quien la pague creyendo `DT-004` dejara tres lineas sin tocar. Y lo mas caro: la nota que
  corrige `F-045` publica como «salida cruda» algo distinto de lo que la orden devuelve, que es
  exactamente la clase de defecto que `F-045` existia para corregir.
- **Que lo corregiria:** ampliar ambito y cifra de `DT-004` con una nota fechada, y añadir al cierre
  un barrido de caracteres de control **sobre todos los archivos que el commit toca**, no sobre dos
  elegidos a mano. ⚠️ Es una recomendacion, no una orden.

### F-050 — El informe y `progress.md` llaman «las tres etapas nuevas de `025_wslt`» a los tres artefactos de una sola etapa
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-019.md` seccion 2; `_persistence/progress.md`, entrada `S-019` |

- **Que se observo:** dos frases del registro cuentan como etapas lo que son artefactos:

```
$ git show 1b30e16:_audit/S-019.md | grep -c "tres etapas nuevas de \`025_wslt\`"
1
$ git show 1b30e16:_persistence/progress.md | grep -c "tres etapas nuevas de \`025_wslt\`"
1
```

  El texto del informe es «Ninguna de las tres etapas nuevas de `025_wslt` queda adoptada — el
  archivo, la plantilla y el reparto existen», y el de `progress.md` «Las tres etapas nuevas de
  `025_wslt` no quedan adoptadas ni su reparto». `025_wslt` es **una** etapa; lo que nace son tres
  archivos (`_phases/025_wslt.md`, `_templates/025_wslt/005_skeleton_record.md`,
  `_workflow/025_wslt.md`). El mensaje del commit y `D-078` si lo dicen bien.
- **Por que importa:** `project.md` lleva la cuenta de las etapas declaradas y `T-002` existe para
  declarar las que faltan; un registro que dice «tres etapas nuevas» donde hay una deja al arranque
  siguiente buscando dos etapas que no existen.
- **Que lo corregiria:** nota fechada en las dos frases aclarando que son tres artefactos de una sola
  etapa. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-045` | **Implementado** (`1b30e16`) | La nota fechada existe en la seccion 7 de `_audit/S-018.md` con las tres cifras corregidas (26 lineas, 23 ordenes distintas, tres repetidas) y con la salida de la posicion 15 que faltaba; las tres reproducen contra `9a52cfa`. Con una salvedad que va aparte como `F-049`: dos lineas de la salida cruda transcrita en esa nota llevan `0x08` donde el original lleva `\b`. El defecto que `F-045` señalaba —cifras falsas y una orden sin salida— si esta corregido |
| `F-046` | **Implementado** (`1b30e16`) | La nota fechada de la seccion 1 de `_audit/S-018.md` corrige `L-020`→`L-019`, y su orden de atribucion, reejecutada, devuelve `709: L-019` y ningun `L-020`. El Paso 6b de `protocol-close` incorpora ademas las dos ordenes que lo derivan |
| `F-047` | **Implementado** (`1b30e16`) | `D-077` nace, la nota fechada esta en el bloque de `D-073`, y el patron derivado reproduce con `H` dentro y con el cuantificador `[0-9]{2,3}`. Las lineas corrompidas de esos bloques son un defecto distinto, registrado en `DT-004` y ampliado en `F-049` |

## 4. Lo que NO se pudo comprobar

Ningun control quedo sin correr. Dos limites declarados, que no son fallos de este commit:

- El barrido de fuga de este protocolo cubre `.claude`, `CLAUDE.md`, `_phases` y `_methodology`; el
  del cierre cubre ademas `_templates` y `_workflow`. La diferencia ya esta registrada como `T-038`,
  abierta. Se corrio igualmente sobre las seis rutas, y la salida esta arriba.
- Si `Origen: usuario` en `T-069`/`T-070` refleja lo que el usuario pidio de viva voz no es
  verificable desde el repositorio; lo que si se verifico es su coherencia con `D-078`/`D-079`.

## 5. Recomendaciones sin hallazgo

- El filtro `^\+\$ ` del Paso 2d no ve las lineas de continuacion de un bloque multilinea, y este
  cierre ha tenido que explicarlo a mano otra vez. Merece la pena decidir si esas ordenes se publican
  de otra forma o si el filtro cambia, en vez de resolverlo con una nota cada sesion.
- El caracter `0x08` va por tres sesiones consecutivas (`DT-003`, `DT-004`, y las tres lineas nuevas
  que documenta `F-049`). Es mejor candidato a control mecanico del cierre —una orden sobre los
  archivos que el commit toca— que a una entrada de deuda tecnica mas.
- `_templates/025_wslt/005_skeleton_record.md` no instancia ningun codigo, ni siquiera el primero.
  No incumple nada, pero conviene decidir si a esa plantilla le corresponde un codigo propio para su
  artefacto o si de verdad no lleva ninguno.
