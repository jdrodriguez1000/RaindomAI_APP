# Auditoria R-042 — sobre S-042

| Campo | Valor |
|---|---|
| Auditoria | R-042 |
| Informe auditado | `_audit/S-042.md` |
| Commit auditado | `9564675` |
| Fecha | 2026-09-14 |
| Veredicto | Con hallazgos (4) |

## 1. Que se comprobo

### a) Sobre que commit se audita

```
$ git log -1 --format=%h -- _audit/S-042.md
133825a

$ git show 133825a:_audit/S-042.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `9564675` |

$ git show --stat --format= 133825a
 _audit/S-042.md           | 111 ++++++++++++++++++++++++++++++++++++++++++----
 _persistence/decisions.md |   8 +++-
 2 files changed, 108 insertions(+), 11 deletions(-)

$ git show --stat --format= 9564675
 _audit/S-041.md           |  24 +++
 _audit/S-042.md           | 387 ++++++++++++++++++++++++++++++++++++++++++++++
 _audit/findings.md        |  18 +--
 _audit/index.md           |   1 +
 _persistence/decisions.md | 261 +++++++++++++++++++++++++++++++
 _persistence/lessons.md   | 118 +++++++-------
 _persistence/progress.md  | 191 +++++++++++++----------
 _persistence/tasks.md     |  91 ++++++++++-
 8 files changed, 940 insertions(+), 151 deletions(-)
```

La cabecera lleva hash literal, distinto del derivado: se audita `9564675`. `133825a` es el commit de
anclaje; toca ademas `decisions.md`, que es lo que el Paso 7c-bis tiene autorizado (el anclaje de los
dos criterios de `D-168`, verificado en 1f). Los ocho archivos de `9564675` coinciden con la lista de
la seccion 1 del informe.

### b) Controles mecanicos (Paso 4) sobre `9564675`

```
$ H=9564675; for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show $H:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show $H:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress

$ diff <(git ls-tree -d --name-only $H | sed 's|$|/|' | sort) <(git show $H:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
9a11
> temporal/

$ git grep -nE "[Rr]aidom|[Rr]aindom|Proyectos_TripleS|github\.com" $H -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1

$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' $H -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1

$ diff <(git show $H:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) <(git show $H:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u); echo "exit=$?"
exit=0

$ git log -1 --format=%cs 9564675; git log -1 --format=%cs 133825a
2026-09-14
2026-09-14
```

Indices coherentes. Las dos diferencias de carpetas (`010_prototype/`, `temporal/`) tienen su razon
escrita en `project.md`, «Carpetas propias»: no son hallazgo. Fuga de datos propios y de codigos
instanciados: cero lineas. Fecha del informe = fecha de los dos commits.

### c) Que las correcciones no reescriben prosa anterior

```
$ for f in _audit/S-041.md _persistence/decisions.md _persistence/tasks.md _audit/findings.md; do echo "== $f"; git diff -U0 d2ee2aa 9564675 -- $f | grep -E '^-[^-]' | cut -c1-120; done
== _audit/S-041.md
== _persistence/decisions.md
== _persistence/tasks.md
-| [T-128](#t-128---hacer-la-cosecha-de-las-lecciones-de-la-etapa-preparatoria-d-123) | Hacer la cosecha de las leccione
-| [T-156](#t-156---construir-el-esqueleto-reutilizable-de-arranque-de-proyecto-d-143) | Construir el esqueleto reutiliz
-| Estado | No implementada |
-| Estado | No implementada |
== _audit/findings.md
-| [F-113](#f-113---progressmd-publica-tres-veces-8-filas-para-la-tabla-del-paso-2e-que-tiene-10) | `progress.md` public
-| [F-114](#f-114---la-seccion-2-de-s-041-publica-como-salida-de-su-grep-cuatro-filas-y-la-orden-devuelve-cinco) | La se
-| [F-115](#f-115---dos-comprobaciones-se-declaran-no-anclables-cuando-si-lo-son-y-una-de-ellas-se-afirma-sin-orden-ni-s
-| Estado | Abierto |
-| Registrado en | |
-| Estado | Abierto |
-| Registrado en | |
-| Estado | Abierto |
-| Registrado en | |
```

Solo salen lineas de estado. `S-041.md` y `decisions.md` solo ganan lineas.

### d) Las notas de `T-187` a `T-189` reproducen

```
$ git show e332771:_persistence/progress.md | grep -noE 'cuadrando \(8( filas, suma 13\)|$)'
95:cuadrando (8 filas, suma 13)
149:cuadrando (8
2079:cuadrando (8 filas, suma 13)

$ git show e332771:_audit/S-041.md | sed -n '/^## 8/,/^## 9/p' | grep -cE '^[._].* head=[0-9]+ staged=[0-9]+$'
10

$ git show e332771:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep "No implementada" | wc -l
5

$ git ls-tree -r --name-only e332771 _outbound
_outbound/.gitkeep

$ E="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; A='.claude CLAUDE.md _phases _methodology _templates _workflow'; diff <(git -C "$E" ls-tree -r c7d0a87 -- $A) <(git ls-tree -r a3bb32e -- $A); echo "fin exit=$?"
13c13
< 100644 blob a4dad55c91f1c1bfad57fdfe4ad5bed8031e3160	.claude/skills/protocol-promote/SKILL.md
---
> 100644 blob fd16fd17177d40c63b8088987247a1f22d7bccf4	.claude/skills/protocol-promote/SKILL.md
fin exit=1

$ diff <(git ls-tree -r a3bb32e -- $A) <(git -C "$E" ls-tree -r afeedf4 -- $A); echo "fin exit=$?"
fin exit=0

$ diff <(git ls-tree -r a3bb32e -- $A) <(git -C "$E" ls-tree -r bf8c56e -- $A); echo "fin exit=$?"
fin exit=0
```

Las cinco ordenes de las notas devuelven lo que publican (la de `T-188` se muestra con `wc -l`; las
cinco filas completas estan en la propia nota y coinciden con las que `R-041` pego en `F-114`).

### e) `D-166` — tercera promocion y cierre del desfase

```
$ diff <(git ls-tree -r d2ee2aa -- $A) <(git -C "$E" ls-tree -r 707d572 -- $A); echo "fin exit=$?"
fin exit=0

$ diff <(git ls-tree -r 9564675 -- $A) <(git -C "$E" ls-tree -r 707d572 -- $A); echo "fin 9564675 exit=$?"
fin 9564675 exit=0

$ git -C "$E" show --numstat --format='%h %s' 707d572
707d572 Promocion del andamiaje desde RaidomAI_App (origen d2ee2aa)

1	1	.claude/skills/protocol-promote/SKILL.md

$ git -C "$E" show 707d572:.claude/skills/protocol-promote/SKILL.md | cmp - <(git show d2ee2aa:.claude/skills/protocol-promote/SKILL.md) && echo identicos
identicos

$ git show 9564675:.claude/skills/protocol-promote/SKILL.md | grep -nE '^[0-9]+\. push'
310:5. push.

$ git -C "$E" status -sb | head -1
## main...origin/main
```

Las seis areas son identicas por hash de blob entre `9564675` y `707d572`. `D-166` se sostiene.

### f) `D-168` — cosecha, y el anclaje de sus criterios

```
$ L="C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons"; git -C "$L" show --numstat --format='%h %s' 5a32165
5a32165 Cosecha de 000_preproject desde RaindomAI: LG-99 a LG-104 y enmienda de LG-32 (version 3)

13	6	global_lessons.md

$ git -C "$L" show 5a32165:global_lessons.md | grep -oE '^\| \*\*LG-(32|99|10[0-4])\*\* \|'
| **LG-32** |
| **LG-99** |
| **LG-100** |
| **LG-101** |
| **LG-102** |
| **LG-103** |
| **LG-104** |

$ git -C "$L" show 5a32165:global_lessons.md | grep -n '^> \*\*Versión'
26:> **Versión: 3 · 2026-09-14** · 104 lecciones · 10 bloques

$ git -C "$L" status -sb | head -1
## main...origin/main

$ git show 9564675:_persistence/lessons.md | grep -E '^\| \[L-' | wc -l
59

$ git show 9564675:_persistence/lessons.md | sed -n '/^## Indice/,/^---/p' | grep -c 'Sin evaluar'
0
```

La correspondencia leccion → destino de la columna `Portabilidad` coincide, leccion a leccion, con la
tabla de `D-168`:

```
$ git show 9564675:_persistence/lessons.md | grep -E '^\| \[L-' | awk -F'|' '{match($2,/L-[0-9]+/); l=substr($2,RSTART,RLENGTH); print l, $(NF-1)}' | sed 's/  */ /g' | awk '{k=$0; sub(/^L-[0-9]+ /,"",k); a[k]=a[k]" "$1} END{for(k in a) print k":"a[k]}' | sort
Promovida a LG-100 : L-008 L-011 L-040 L-042 L-055
Promovida a LG-101 : L-010 L-037 L-048 L-053 L-056
Promovida a LG-102 : L-002 L-030
Promovida a LG-103 : L-024 L-029 L-038 L-057
Promovida a LG-104 : L-035 L-047
Promovida a LG-32 : L-023 L-027 L-028 L-058
Promovida a LG-99 : L-001 L-039 L-051
Solo proyecto : L-005 L-017 L-018 L-032 L-033 L-059
Ya cubierta por LG-01 : L-022
Ya cubierta por LG-03 : L-043
Ya cubierta por LG-04 : L-006 L-012 L-021 L-025 L-052
Ya cubierta por LG-06 : L-036
Ya cubierta por LG-08 : L-050
Ya cubierta por LG-10 : L-016 L-020
Ya cubierta por LG-11 : L-019
Ya cubierta por LG-13 : L-045
Ya cubierta por LG-14 : L-044
Ya cubierta por LG-26 : L-014 L-015 L-026
Ya cubierta por LG-29 : L-007
Ya cubierta por LG-32 : L-004
Ya cubierta por LG-33 : L-009
Ya cubierta por LG-37 : L-034 L-049
Ya cubierta por LG-70 : L-031
Ya cubierta por LG-84 : L-041
Ya cubierta por LG-97 : L-013
Ya cubierta por LG-98 : L-003 L-046 L-054
```

El anclaje del 7c-bis en `133825a` sustituye `<hash>` por `9564675`, pega `0` y `59`, y no borra prosa
:

```
$ git show 133825a -- _persistence/decisions.md | grep -E '^-[^-]'
-$ git show <hash>:_persistence/lessons.md | grep -E '^\| \[L-' | grep '000_preproject' | grep -c 'Sin evaluar'
-$ git show <hash>:_persistence/lessons.md | grep -E '^\| \[L-' | grep -cE '\| (Promovida a|Ya cubierta por) LG-[0-9]+ \|$|\| Solo proyecto \|$'
```

Lo que no se sostiene son los recuentos resumidos: `F-116`.

### g) Seccion 6 del informe — lo que pidio auditar

**`D-167`.** El CONTROL DE CIFRA ADYACENTE, corrido tal cual sobre `progress.md` en `e332771`, no
devuelve ninguna de las tres lineas con «8 filas»:

```
$ git show e332771:_persistence/progress.md > progress.md; awk '
  /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' progress.md | grep -nE '8 filas|^progress.md:(95|149|2079):'; echo "exit=$?"
exit=1

$ git show e332771:_persistence/progress.md | sed -n '/^### S-041/,/^## 6\./p' | grep -c '^```'
0
```

La lectura de `D-167` es correcta, y ademas cubre las lineas 95 y 149, que su propia orden (acotada a
la entrada `S-041`) no miraba. No es rechazo por coste: no le falta `DT-XXX`.

**Discrepancia de `D-168` entre el archivo global y `protocol-harvest`.** La skill dice que, ante
discrepancia, manda el archivo global, y que solo se detiene si es de fondo:

```
$ git show 9564675:.claude/skills/protocol-harvest/SKILL.md | sed -n '111,113p'
🚨 **Si esa seccion dice algo distinto de lo que dice este protocolo, manda ella.** Este archivo
describe el procedimiento; el otro define el criterio. Y si la discrepancia es de fondo, **se detiene
la cosecha y se reporta** — no se elige la version que resulte mas comoda.

$ git show 9564675:.claude/skills/protocol-harvest/SKILL.md | sed -n '40,41p'
⛔ **Y no se poda el archivo global de paso.** Si al escribir ves que ha crecido mas de lo que se lee
de una sentada, se dice en el reporte y se decide aparte. Podar mientras se promueve mezcla dos
```

Resolver a favor del archivo global y no podar es lo que la skill manda, y `D-168` dice que se
declaro en la puerta. No es hallazgo; la alineacion del texto va a la seccion 5.

**Criterios de cierre en prosa de `T-187` a `T-189`.** Van a la seccion 5: los criterios son
verificables corriendo las ordenes de cada nota (1d). Lo que si es defecto es otra cosa del mismo
sitio: `F-117`.

### h) Resto del informe contra el diff

- Seccion 1 (ocho archivos y que cambio en cada uno): respaldada por 1a y 1c.
- Seccion 0: las tres filas «Aceptado — corregido en este commit» tienen su nota en el diff (1c, 1d).
  Ninguna usa `Implementado`.
- `T-128` y `T-156` pasan a `Implementada`: el criterio de `T-128` da `0` anclado (1f); las siete
  subtareas de `T-156` estan `Implementada` y las seis areas no difieren (1e):

```
$ git show 9564675:_persistence/tasks.md | grep -E '^\| \[T-1(5[7-9]|6[0-3])\]' | grep -oE '^\| \[T-[0-9]+|\| (Implementada|No implementada|Suspendida|En curso) \|'
| [T-157
| Implementada |
| [T-158
| Implementada |
| [T-159
| Implementada |
| [T-160
| Implementada |
| [T-161
| Implementada |
| [T-162
| Implementada |
| [T-163
| Implementada |
```

- CONTROL DE CIFRA ADYACENTE de la seccion 7: la salida publicada es exactamente la del informe en
  `9564675` (14 lineas, la 38 con el texto de esa version). Las cifras que el anclaje anade despues
  (lineas 346, 381-382, 393, 421 en `133825a`) coinciden con los bloques que las preceden.
- Seccion 7, bloque del item 2: `F-119`. Nota de cierre de `T-156`: `F-118`.

## 2. Hallazgos

### F-116 — Los recuentos de la cosecha («26 promueven, 23 ya cubiertas») contradicen la tabla de `D-168` y la columna `Portabilidad`

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/decisions.md` (`D-168`, «Contraste»), `_persistence/progress.md` («Avance de la etapa» y «Ultimo realizado»), `_audit/S-042.md` seccion 3 |

- **Que se observo:** el registro publica que de 59 lecciones «26 promueven (seis nuevas y una
  enmienda), 23 ya cubiertas, 6 `Solo proyecto`» (26+23+6 = 55), y `D-168` cuadra la suma con
  «26 + 4 + 23 + 1 + 5 = 59». La propia tabla de `D-168` y la columna escrita en `lessons.md` dan
  **21** a lecciones nuevas, **4** a la enmienda (25 promovidas) y **28** ya cubiertas:

```
$ git show 9564675:_persistence/decisions.md | sed -n '/^### D-168/,/^- \*\*La puerta/p' | grep -E '^\| (`LG-|enmienda|ya cubierta|`Solo)' | awk -F'|' '{n=gsub(/L-[0-9]+/,"&",$3); print n "\t" $2}'
3	 `LG-99` nueva 
5	 `LG-100` nueva 
5	 `LG-101` nueva 
2	 `LG-102` nueva 
4	 `LG-103` nueva 
2	 `LG-104` nueva 
4	 enmienda a `LG-32` 
28	 ya cubierta, filtro 4 
1	 `Solo proyecto`, filtro 1 
5	 `Solo proyecto`, filtro 2 

$ git show 9564675:_persistence/lessons.md | grep -E '^\| \[L-' | grep -oE '\| (Promovida a LG-[0-9]+|Ya cubierta por LG-[0-9]+|Solo proyecto) \|$' | sed -E 's/ LG-[0-9]+//' | sort | uniq -c
     25 | Promovida a |
      6 | Solo proyecto |
     28 | Ya cubierta por |

$ git grep -nE '26 promueven|23 ya cubiertas|26 \+ 4 \+ 23' 9564675 -- _persistence _audit | cut -c1-160
9564675:_audit/S-042.md:119:salen `Solo proyecto` por el filtro 2, y no se podo antes de anadir). Resultado: 26 promueven (seis
9564675:_audit/S-042.md:120:lecciones nuevas y una enmienda), 23 ya cubiertas por lecciones existentes, 6 `Solo proyecto`. La
9564675:_persistence/decisions.md:10887:  Contraste: 26 + 4 + 23 + 1 + 5 = **59**, igual al recuento del barrido. La suma de la tabla la
9564675:_persistence/progress.md:96:| Avance de la etapa | Se evaluan `F-113` a `F-115` (`R-041`): los tres `Aceptado — pendiente` en `findings.md`, verificad
9564675:_persistence/progress.md:136:forma con el protocolo, declarada y resuelta a favor del archivo global); 26 promueven (seis
9564675:_persistence/progress.md:137:lecciones nuevas, `LG-99` a `LG-104`, y una enmienda a `LG-32`), 23 ya cubiertas por lecciones
```

  `D-168` anade: «La suma de la tabla la hace la orden de abajo sobre la columna ya escrita, no la
  mano». La orden de su criterio solo cuenta el total (`59`), y un total correcto no ve dos sumandos
  cambiados en sentidos opuestos (−5 y +5). La clasificacion leccion a leccion es correcta (1f).
- **Por que importa:** el registro afirma un resultado comprobablemente falso en tres archivos, uno de
  ellos `progress.md`, que es lo primero que se lee al abrir sesion. Es la familia de `F-113`, en la
  sesion que corrige `F-113`, y en la decision que enmienda la leccion global sobre lo derivable
  escrito a mano. `Media` y no `Alta`: la clasificacion y lo publicado fuera son correctos; lo falso
  son los recuentos resumidos.
- **Que lo corregiria:** notas fechadas junto a los tres sitios, sin reescribirlos, con los recuentos
  por destino sacados de la columna por orden (como la segunda de arriba). Que el criterio de `D-168`
  cuente por destino y no solo el total. ⚠️ Es una recomendacion, no una orden.

### F-117 — La verificacion contra `HEAD` de `F-113` a `F-115` se afirma sin orden ni salida, y sus tareas no llevan bloque de verificacion

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-042.md` seccion 0; `_persistence/progress.md`; `_persistence/tasks.md` (`T-187` a `T-189`); `_audit/findings.md` (fichas `F-113` a `F-115`) |

- **Que se observo:** el informe y `progress.md` dicen que los tres hallazgos se «verificaron contra
  `HEAD` (`d2ee2aa`) antes de tratarlos», pero no se publica esa orden en ningun sitio. Las tres tareas
  con `Origen: report_auditor` no tienen ningun bloque, y las fichas no tienen el bloque de tratamiento
  que si tienen las del lote anterior:

```
$ for t in 183 184 185 186 187 188 189; do echo "T-$t fences=$(git show 9564675:_persistence/tasks.md | sed -n "/^### T-$t /,/^---/p" | grep -c '^```')"; done
T-183 fences=2
T-184 fences=2
T-185 fences=2
T-186 fences=2
T-187 fences=0
T-188 fences=0
T-189 fences=0

$ for f in 109 110 111 112 113 114 115; do echo "F-$f tratamiento=$(git show 9564675:_audit/findings.md | sed -n "/^### F-$f /,/^---$/p" | grep -c 'Tratamiento del')"; done
F-109 tratamiento=1
F-110 tratamiento=1
F-111 tratamiento=1
F-112 tratamiento=1
F-113 tratamiento=0
F-114 tratamiento=0
F-115 tratamiento=0

$ git show 133825a:_audit/S-042.md | grep -n 'Verificado contra `HEAD`'
13:Verificado contra `HEAD` (`d2ee2aa`, «auditoria R-041 sobre S-041 (e332771)») antes de tratar cada

$ git diff --stat e332771 d2ee2aa
 _audit/R-041.md       | 371 ++++++++++++++++++++++++++++++++++++++++++++++++++
 _audit/S-041.md       | 115 ++++++++++++++--
 _audit/findings.md    | 161 ++++++++++++++++++++--
 _audit/index.md       |   2 +-
 _persistence/tasks.md |  27 +++-
 5 files changed, 650 insertions(+), 26 deletions(-)
```

  Las notas anclan sus ordenes a `e332771`, no a `HEAD`; y `_audit/S-041.md`, donde viven dos de los
  tres defectos, cambio entre `e332771` y `d2ee2aa`.
- **Por que importa:** `CLAUDE.md` pide que la verificacion previa y toda decision con
  `Origen: report_auditor` lleven la orden literal y su salida. «Verificado contra `HEAD`» sin orden es
  un veredicto. `Baja`: los tres defectos siguen siendo ciertos (1d) y las notas si traen ordenes.
- **Que lo corregiria:** notas fechadas en las fichas `F-113` a `F-115` (o en `T-187` a `T-189`) con
  las ordenes corridas sobre `d2ee2aa`. ⚠️ Es una recomendacion, no una orden.

### F-118 — La nota que cierra `T-156` vuelve a declarar «no anclable por naturaleza» una comprobacion que el mismo commit publica anclada, y afirma «cero lineas» sin orden ni salida

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/tasks.md`, `T-156`, nota «Cerrada en `S-042`» |

- **Que se observo:**

```
$ git show 9564675:_persistence/tasks.md | sed -n '/^### T-156 /,/^---/p' | grep -nE 'no anclable|cero lineas'
15:dos vivos —, corrida sobre el arbol de esta sesion: cero lineas.

$ git show 9564675:_persistence/tasks.md | sed -n '6864,6868p'
📌 **Cerrada en `S-042`.** Las siete subtareas (`T-157` a `T-163`) estan `Implementada`; la tercera
promocion (`D-166`, esqueleto en `707d572`) cerro la unica linea de diferencia que dejaba abierto el
Paso 2f. Criterio de cierre verificado con la misma orden del Paso 2f de `protocol-close` — **no
anclable por naturaleza**, compara el arbol de trabajo de este repositorio contra el del esqueleto, los
dos vivos —, corrida sobre el arbol de esta sesion: cero lineas.
```

  El unico bloque de la ficha es anterior a esta sesion (un barrido inicial con diferencias). En el mismo
  commit, `D-166` publica anclada la misma pregunta (`d2ee2aa` ↔ `707d572`, `exit=0`, 1e), y la nota de
  `T-189` corrige justo esta clase de afirmacion.
- **Por que importa:** es lo que `F-115` corrigio, repetido en la tarea `Bloqueante` que la sesion
  cierra. El resultado es cierto (1e), pero quien lea la ficha tiene que rehacer el barrido o saber
  que la evidencia esta en `D-166`. `Baja`: reproducibilidad, no contenido.
- **Que lo corregiria:** nota fechada bajo `T-156` que remita a la orden anclada de `D-166` o la
  copie, dejando dicho que si es anclable. ⚠️ Es una recomendacion, no una orden.

### F-119 — La seccion 7 de `S-042` pega como salida de una orden un texto que la orden no emite, y lo remite a la decision equivocada

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-042.md`, seccion 7, bloque de reejecucion de los items 2, 3, 4 y 16 |

- **Que se observo:** en el bloque de salida, bajo la orden del item 2
  (`diff <(git -C "$E" ls-tree -r c7d0a87 ...) <(git ls-tree -r a3bb32e ...)`), se pega una linea
  entre parentesis en lugar de la salida, y remite a `D-166`. La salida completa esta en la nota de
  `D-163`, y `D-166` no cita `c7d0a87`:

```
$ git show 133825a:_audit/S-042.md | grep -n 'salida completa ya publicada en'
261:(una diferencia de blob en protocol-promote/SKILL.md; salida completa ya publicada en D-166, _persistence/decisions.md)

$ git show 9564675:_persistence/decisions.md | sed -n '/^### D-166/,/^### D-167/p' | grep -c 'c7d0a87'
0

$ git show 9564675:_persistence/decisions.md | sed -n '/^### D-163/,/^### D-164/p' | grep -c 'ls-tree -r c7d0a87'
1
```

  La orden real devuelve cuatro lineas de `diff` y `fin del contraste exit=1` (1d).
- **Por que importa:** un bloque de salida cruda tiene que ser lo que su orden emite. Es la familia de
  `F-108`, `F-110` y `F-114`, en la sesion que corrige `F-114`, y ademas la referencia manda a una
  decision donde no esta. `Baja`: el hecho resumido es cierto y la salida existe en el registro.
- **Que lo corregiria:** nota fechada al final de la seccion 7 de `S-042` con la salida entera y la
  referencia a `D-163`. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-113 | Implementado (`9564675`) | La nota de `T-187` esta al final de la entrada `S-041` de `progress.md`, sin reescribir las menciones, y sus dos ordenes dan `95/149/2079` y `10` (1d). La mitad no adoptada tiene `D-167`, con orden y salida, y su lectura se sostiene (1g) |
| F-114 | Implementado (`9564675`) | La nota de `T-188` al final de la seccion 2 de `S-041` pega las cinco filas, y la orden anclada devuelve cinco (1d). No reescribe el bloque anterior (1c) |
| F-115 | Implementado (`9564675`) | (a) nota de `T-189` en la seccion 7 de `S-041` con `git ls-tree` anclado, que da `_outbound/.gitkeep`; (b) nota bajo `D-163` con los tres contrastes por hash de blob, que reproducen (1d). La repeticion de la clase en `T-156` es un hallazgo nuevo (`F-118`), no una correccion a medias de este |

No habia ningun otro `F-NNN` sin cerrar.

## 4. Lo que NO se pudo comprobar

- **Las ordenes de la seccion 7 del informe que miden el arbol de trabajo en el momento del cierre**
  (items 5, 6, 7, 8 y 15: `git status`, `diff -rq` entre carpetas de disco): se sustituyen por los
  contrastes por hash de blob de 1e, que cubren la misma pregunta sobre commits.
- **Que la puerta de `D-166` y `D-168` la aprobara el usuario:** ocurrio en la conversacion y no deja
  rastro en `git`. Lo verificable —que lo escrito fuera coincide con lo registrado aqui— si se
  comprobo (1e, 1f).
- **El contenido de las lecciones `LG-99` a `LG-104` y la enmienda de `LG-32`** frente a los cuatro
  filtros: es criterio, no evidencia, y esta auditoria no lo juzga. Solo se comprobo que existen y que
  la version declarada coincide.

## 5. Recomendaciones sin hallazgo

- Los criterios de cierre de `T-187` a `T-189` estan en prosa, sin orden literal. Se pueden verificar
  corriendo las ordenes de cada nota, pero un bloque propio los dejaria reproducibles sin buscar.
- La nota de `T-128` dice que su criterio es «el mismo que verifica `D-168`, con la misma orden». No es
  la misma orden: la de `T-128` recorta por `## Indice` y no filtra por etapa. Las dos dan el resultado
  esperado (`0`).
- `protocol-harvest` no menciona «solo cuando se repite» ni «se poda antes de anadir», que si estan en
  el archivo global. Si la discrepancia vuelve a salir en otra cosecha, conviene alinear el texto.
- El CONTROL DE CIFRA ADYACENTE se corre antes del `git add`, sobre la version sustantiva del informe.
  Las cifras que el anclaje anade despues no pasan por ningun control; esta vez eran correctas.
