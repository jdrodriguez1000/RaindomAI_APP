# Auditoria R-006 — sobre S-006

| Campo | Valor |
|---|---|
| Auditoria | R-006 |
| Informe auditado | `_audit/S-006.md` |
| Commit auditado | `d906a5d` |
| Fecha | 2026-09-02 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

**Anclaje del commit** (Paso 1):

```
$ git log -1 --format=%h -- _audit/S-006.md
d906a5d
```

**Indice ↔ detalle de los siete archivos de `_persistence/`** (Paso 4a) — sin diferencias en
ninguno:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show d906a5d:_persistence/$f.md | awk '/^~~~/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show d906a5d:_persistence/$f.md | awk '/^~~~/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

*(el patron real del protocolo usa el delimitador de bloque de codigo; aqui se transcribe como `~~~`
para no cerrar este bloque)*

**Carpetas del arbol contra las declaradas en `project.md`** (Paso 4b) — una sola diferencia,
`temporal/`, con su razon escrita en `project.md` («existe en disco pero nunca aparecera en el arbol
versionado, porque `.gitignore` la excluye»). `_methodology/` ya aparece en las dos listas:

```
$ diff <(git ls-tree -d --name-only d906a5d | sed 's|$|/|' | sort) \
       <(git show d906a5d:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
6a7
> temporal/
```

**Fuga de datos propios en lo agnostico** (Paso 4c), ya con el ambito ampliado a `_methodology`
segun `D-028` — cero lineas:

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" d906a5d -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1
```

Y una segunda pasada por iniciativa propia, con patrones que el control declarado no cubre (ruta de
disco, usuario del sistema, nombre de la carpeta en disco, handle del remoto) — tambien cero:

```
$ git grep -nE "C:\\\\Users|USUARIO|RaidomAI_App|jdrodriguez" d906a5d -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1
```

**Coherencia de `_audit/findings.md` en el commit auditado** — sin diferencias (`exit=0` del `echo`,
sin salida del `diff`).

**El informe contra el diff** (Paso 3). Los 19 archivos que lista `git show --stat d906a5d` estan
todos nombrados en la seccion 1 de `S-006.md`, y ningun archivo nombrado falta del diff. Cada
correccion afirmada se verifico una a una:

```
$ git grep -n "e61454b" d906a5d -- _persistence/assumptions.md _persistence/tasks.md
d906a5d:_persistence/assumptions.md:125:`HEAD` era `e61454b` —el commit anterior al que acabaria conteniendo la nota—, y sobre ese hash el
d906a5d:_persistence/assumptions.md:129:$ git grep -n "| Pendiente |" e61454b -- _audit/index.md ; echo "exit=$?"
d906a5d:_persistence/tasks.md:382:archivos en `HEAD`» del primer punto se escribio sin decir cual era ese `HEAD`. Era `e61454b`, y
d906a5d:_persistence/tasks.md:386:$ git grep -c "debtec" e61454b -- . | wc -l
d906a5d:_persistence/tasks.md:437:  dos notas fechadas que las anclan a `e61454b`, el `HEAD` real del momento, sin reescribir los
d906a5d:_persistence/tasks.md:444:  ancla. Sobre `e61454b` el comando de `A-001` da `exit=1` y el recuento da 12, tal como se

$ git grep -n "todavia no entra en el ambito del Paso 1b" d906a5d -- _persistence/decisions.md
d906a5d:_persistence/decisions.md:882:⚠️ **`_phases/` todavia no entra en el ambito del Paso 1b**, que hoy cubre solo `.claude`,

$ git show d906a5d:_persistence/decisions.md | sed -n '885,888p'
🕒 **Nota anadida el 2026-09-02 (`S-006`), tras el hallazgo `F-013` de `R-005`.** El parrafo de
arriba **se deja tal cual se escribio** (`D-019`), pero **ya no describe el estado del control**:
`D-026`, en este mismo commit, amplio el ambito del Paso 1b a `_phases/`. Lo que quedaba «pendiente
de acordarla con el usuario» se acordo y se aplico antes de cerrar la sesion.

$ git show d906a5d:_persistence/decisions.md | grep -nE '^### D-0(2[7-9]|3[0-1])'
1062:### D-027 - El texto que señala un hallazgo aceptado lo corrige `manager`, aunque el archivo sea de otro
1114:### D-028 - `_methodology/` entra al repositorio como carpeta agnostica, dentro del control de fuga
1162:### D-029 - `000_method.md` es guia de metodo vigente, y no declara las etapas del proyecto
1213:### D-030 - Los codigos de producto del metodo se renombran a `FT-` y `SC-`
1274:### D-031 - Un Gate lo declara el usuario, sobre el veredicto tecnico de `report_auditor`
```

**Lo que la seccion 6 pidio auditar, punto por punto:**

1. **Las correcciones `T-014`..`T-018` estan en el commit** — verificadas arriba y en la seccion 3.
2. **`D-027` se aplico dentro de sus limites.** El commit no toca ningun documento entregado: en
   `_audit/` solo aparecen `findings.md`, `index.md` y el `S-006.md` nuevo; ningun `R-XXX.md` ni
   ningun `S-00X.md` anterior:

```
$ git show --stat --name-only --format= d906a5d -- _audit/
_audit/S-006.md
_audit/findings.md
_audit/index.md
```

   Y dentro de `findings.md`, el diff solo altera filas de estado del indice, la linea de
   «Convenciones» (`T-018`) y bloques «Que se hizo» / notas fechadas nuevas. El texto de los
   hallazgos entregados —«Que se observo», «Por que importa»— queda intacto.

3. **`_methodology/` es agnostica** — los dos barridos de arriba, sobre el commit ya cerrado, dan
   cero. Y `D-030` esta aplicado: no queda ningun codigo de producto que colisione con `F-NNN` o
   `S-XXX` del registro:

```
$ git show d906a5d:_methodology/000_method.md | grep -nE '`(F|S)-[0-9]' ; echo "exit=$?"
exit=0

$ git show d906a5d:_methodology/000_method.md | grep -noE '\b(FT|SC|VS)-[0-9]+' | head -5
744:VS-01
745:VS-02
746:VS-03
747:VS-04
801:FT-001
```

   (el `exit=0` es el del `echo`; `grep` no imprimio ninguna linea.)

4. **El mecanismo nuevo del Paso 6 de `protocol-close` (`T-019`)**: existe en el commit, con sus dos
   condiciones acumulativas. Se aplico como criterio de esta auditoria sobre los bloques nuevos de
   `S-006`, y **ninguno lo incumple**: los de `D-028`..`D-031` barren `.claude CLAUDE.md _phases
   _methodology`, que el cierre no reescribe; los de `findings.md` y los de la nota de `progress.md`
   usan rutas del registro pero **no se declaran reproducibles sobre su propio commit** ni usan
   `HEAD` sin decir cual. Un bloque que cumple una sola condicion no es hallazgo, por el propio
   texto del Paso 6.

```
$ git show d906a5d:_persistence/decisions.md | sed -n '/^### D-027/,$p' | grep -nE '^\$ '
44:$ git grep -n "debtec" a800d6b -- _audit/findings.md
91:$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
139:$ sed -n '/^| Etapas declaradas/,/^| Etapas posteriores/p' project.md
143:$ ls _phases/
189:$ grep -nE '`F-0|`S-0' _methodology/000_method.md ; echo "exit=$?"
200:$ grep -rnE 'F-[0-9]{3}|S-[0-9]{3}' _methodology/sources/
255:$ grep -n "terminal\|Terminal" _methodology/000_method.md ; echo "exit=$?"
```

5. **El riesgo sin codigo de la seccion 4** — confirmado y abierto como `F-015`.

## 2. Hallazgos

### F-015 - `005_discovery` sigue declarada sin su archivo en `_phases/`, y ya nadie lo agenda

| Campo | Valor |
|---|---|
| Auditoria | R-006 |
| Fecha | 2026-09-02 |
| Gravedad | Media |
| Donde | `_phases/`, `project.md` (tabla «Etapas»), `D-023`, `_persistence/tasks.md` |

- **Que se observo:** `D-023` es una decision **vigente** cuyo titulo es literalmente «Cada etapa
  declarada tiene su archivo agnostico en `_phases/`», y su cuerpo dice «un archivo por etapa
  declarada». `project.md` declara dos etapas. `_phases/` contiene una:

```
$ git show d906a5d:_persistence/decisions.md | grep -n "^### D-023"
815:### D-023 - Cada etapa declarada tiene su archivo agnostico en `_phases/`

$ git show d906a5d:_persistence/decisions.md | sed -n '819,821p'
| Fecha | 2026-09-01 |
| Estado | Vigente |
| Origen | usuario |

$ git show d906a5d:project.md | grep -n "Etapas declaradas"
102:| Etapas declaradas | `000_preproject`, `005_discovery` |

$ git ls-tree --name-only d906a5d _phases/
_phases/000_preproject.md

$ git grep -niE "phases" d906a5d -- _persistence/tasks.md ; echo "exit=$?"
exit=1
```

  No hay ninguna `T-XXX` ni ninguna `DT-XXX` que lo agende. El propio `S-006.md` lo reconoce por
  escrito en su seccion 2 —«esta sesion no la convirtio en `T-XXX`»— y en su seccion 4, y aun asi
  cierra sin darle codigo.
- **Por que importa:** es la tercera sesion consecutiva en que el asunto se nombra y no se agenda
  (`D-024` lo anoto como advertencia, `R-005` lo repitio como recomendacion sin hallazgo, `S-006` lo
  vuelve a nombrar). Un incumplimiento de una decision vigente que solo vive en prosa dentro de
  informes de sesion **no aparece en `tasks.md`, que es lo que `session-starter` lee al arrancar**:
  desaparece del radar en cuanto nadie se acuerde de repetirlo. `Media` y no `Alta` porque `T-001`
  —la que de verdad abre `005_discovery`— sigue sin arrancar, asi que hoy no bloquea nada; pasara a
  ser bloqueante el dia que la etapa empiece.
- **Que lo corregiria:** darle codigo. O una `T-XXX` que escriba `_phases/005_discovery.md`, o una
  `DT-XXX` que registre el aplazamiento con su motivo y su disparador. ⚠️ Es una recomendacion, no
  una orden: cual de las dos, lo decide `manager`.

### F-016 - El criterio de cierre de `T-015` no se cumple al ejecutarlo, y la tarea queda `Implementada`

| Campo | Valor |
|---|---|
| Auditoria | R-006 |
| Fecha | 2026-09-02 |
| Gravedad | Baja |
| Donde | `_persistence/tasks.md`, `T-015` y la seccion «Convenciones» del mismo archivo |

- **Que se observo:** `T-015` declara como criterio de cierre «el barrido de la regla no devuelve
  ningun enunciado que siga afirmando que la excepcion es unica». Ejecutado sobre el commit que la
  da por `Implementada`, el barrido devuelve un enunciado:

```
$ git show d906a5d:_persistence/tasks.md | sed -n '/^### T-015/,/^### T-016/p' | grep -n "Criterio de cierre" -A1
23:- **Criterio de cierre:** el barrido de la regla no devuelve ningun enunciado que siga afirmando que
24-  la excepcion es unica, y el archivo de la etapa sigue pasando el control de agnosticidad.

$ git grep -n "unica excepcion\|salvo la .T-XXX" d906a5d -- .claude CLAUDE.md _phases _persistence/tasks.md
d906a5d:.claude/skills/protocol-close/SKILL.md:490:**La unica excepcion, y es mecanica:** si un supuesto `A-XXX` quedo comprobado por la evidencia del
d906a5d:_persistence/tasks.md:63:con `progress.md`. **Tiene una unica excepcion, y esta escrita:** cuando `manager` evalua un hallazgo

$ git show d906a5d:_persistence/tasks.md | sed -n '62,64p'
🚨 **Este archivo no se escribe a mano durante la jornada.** Lo produce el cierre de sesion, junto
con `progress.md`. **Tiene una unica excepcion, y esta escrita:** cuando `manager` evalua un hallazgo
`F-NNN` de una auditoria y lo acepta, escribe **en ese momento** la `T-XXX` con
```

  La coincidencia de `protocol-close/SKILL.md:490` es de otra regla (los supuestos `A-XXX`) y no
  cuenta. La de `_persistence/tasks.md:63` si: es la convencion del propio archivo, y sigue abriendo
  con «Tiene una unica excepcion» aunque cuatro parrafos mas abajo anuncie la segunda.
- **Por que importa:** es el mismo patron que ya se registro como `F-009` —un criterio de cierre
  autodeclarado que no se cumple al correrlo—, y aqui aparece dentro de la tarea que corrige un
  hallazgo sobre exactamente ese descuido. Quien vuelva a correr el barrido que `T-015` propone
  encontrara una coincidencia y no sabra si es un resto o un descuido. `Baja` porque el fondo esta
  bien: los tres sitios que `F-012` señalaba estan corregidos, la convencion de `tasks.md` si
  describe las dos excepciones, y ningun agente queda con la regla vieja.
- **Que lo corregiria:** o reescribir la apertura de la convencion de `tasks.md` para que no diga
  «unica» —por ejemplo, «tiene dos excepciones, y las dos estan escritas»—, o acotar el criterio de
  cierre de `T-015` a los tres sitios que el hallazgo nombraba. ⚠️ Es una recomendacion, no una
  orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-001` a `F-009` | `Implementado` (sin cambio) | cerrados en auditorias anteriores |
| `F-010` | **`Implementado`** | verificado sobre `d906a5d`: la linea de la seccion «Convenciones» ya cita `techdebt.md` |
| `F-011` | **`Implementado`** | verificado sobre `d906a5d`: las dos notas fechadas anclan a `e61454b`, y sobre ese hash los valores se reproducen |
| `F-012` | **`Implementado`** | verificado sobre `d906a5d`: los tres sitios que el hallazgo señalaba estan reescritos |
| `F-013` | **`Implementado`** | verificado sobre `d906a5d`: la nota fechada bajo `D-023` esta, y ademas corrige el ambito anterior |
| `F-014` | **`Implementado`** | verificado sobre `d906a5d`: secciones 1 y 2 de `progress.md` reescritas y bitacora de `S-005` con nota fechada |

**Evidencia de los cierres:**

```
$ git show d906a5d:_audit/findings.md | sed -n '1,70p' | grep -n "debtec" ; echo "exit=$?"
exit=1

$ git grep -c "debtec" e61454b -- . | wc -l
12

$ git grep -n "unica excepcion\|salvo la .T-XXX" d906a5d -- .claude/agents .claude/skills/protocol-close _phases ; echo "exit=$?"
d906a5d:.claude/skills/protocol-close/SKILL.md:490:**La unica excepcion, y es mecanica:** si un supuesto `A-XXX` quedo comprobado por la evidencia del
exit=0

$ git grep -n "evaluo los seis" d906a5d -- _persistence/progress.md
d906a5d:_persistence/progress.md:239:- **Que se hizo:** `manager` evaluo los seis hallazgos abiertos por `R-003` y `R-004` (`F-005`,
d906a5d:_persistence/progress.md:305:  citaba, aparecio un tercer error de conteo («evaluo los seis» cuando fueron cinco) y el mismo
```

La unica coincidencia viva del barrido de `F-012` es de otra regla (supuestos `A-XXX`) — el resto de
`F-016` es la convencion de `tasks.md`, que el hallazgo original no contaba entre sus tres sitios.
La linea 239 de `progress.md` es la bitacora historica de `S-005`, que por `D-019` no se reescribe y
lleva su nota fechada inmediatamente debajo (lineas 260-266); la 305 es la descripcion de la propia
correccion en la bitacora de `S-006`. Las dos apariciones son las correctas.

Los cinco cierres cumplen la condicion del Paso 5: la correccion **esta en el diff del commit
posterior al que abrio el hallazgo**, y hace lo que su `T-XXX` afirma. Ninguno queda a medias.

## 4. Lo que NO se pudo comprobar

Ninguno de los controles quedo sin correr. Los tres del Paso 4 y el de coherencia de `findings.md`
se ejecutaron sobre `d906a5d` y devolvieron salida, incluida la salida vacia.

## 5. Recomendaciones sin hallazgo

- **`L-009` sigue sin mecanismo, y el propio informe lo dice.** `S-006` señala en su seccion 4 que la
  leccion «un hallazgo acota su ejemplo, no el defecto» depende de que quien corrija se acuerde de
  aplicarla, igual que `D-022` dependia antes de `T-019`. `F-016` de esta auditoria es un ejemplo de
  ese mismo patron: la correccion de `F-012` barrio tres sitios y dejo un cuarto enunciado en pie.
  No lo abro como hallazgo aparte porque la leccion se acaba de escribir y no ha tenido ocasion de
  fallar dos veces; si vuelve a pasar, ahi si.
- **La linea 216 de `_phases/000_preproject.md` quedo sin reajustar al ancho del resto del archivo**
  tras insertar `_methodology/` en la enumeracion de carpetas. Es cosmetico y no afecta a ningun
  control.
- **El campo «Commit auditado» de la cabecera de `S-XXX.md` sigue rellenandose con una instruccion**
  —«ver `git log -1 -- _audit/S-XXX.md`»— en vez de un hash, porque el hash no existe cuando se
  escribe. Es honesto y correcto, pero obliga a cada lector a resolverlo a mano; el sitio donde el
  hash si cabe es la fila del tablero, que ya lo lleva.
