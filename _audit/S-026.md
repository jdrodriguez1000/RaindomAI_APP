# Informe de auditoria — S-026

| Campo | Valor |
|---|---|
| Sesion | S-026 |
| Fecha | 2026-09-07 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | `<HASH LITERAL del commit sustantivo de la sesion — se rellena en el Paso 7c>` |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`3bf61d4`, "auditoria R-025 sobre S-025 (f1f2291)"), sobre el estado que
`f1f2291` (el commit sustantivo de `S-025`) dejo.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-070 — El barrido universal de la nota de cierre se etiqueta «sobre `f1f2291`» y su salida no reproduce contra `f1f2291` | Aceptado — pendiente | `T-106` (`D-106`); el recuadro del Paso 7c prohibe ahora `cat` y `HEAD`, y la nota fechada esta en `_audit/S-025.md` §7 con sus dos salidas, verificado abajo |
| F-071 — La nota de cierre incumple, en su primera ejecucion, la regla que esa misma sesion escribio: el barrido no salio vacio y no se detuvo | Aceptado — pendiente | `T-107` (`D-107`); el recuadro describe ahora los dos barridos (CENSO/CONTROL) y la condicion de parada del CONTROL, verificado abajo |
| F-072 — El recuento accesorio del Paso 2d vuelve a ser falso: publica 27 ordenes distintas y son 22 | Aceptado — pendiente | `T-108` (`D-108`); la nota fechada con las cifras reales (22 y 13) esta en `_audit/S-025.md` §7, verificado abajo |
| F-073 — La frase de cierre contradice a la nota que la precede y publica dos cifras que no salen de ninguna orden | Aceptado — pendiente | `T-109` (`D-109`); la nota fechada con las tres cifras (32, 34, 18) esta en `_audit/S-025.md` §7, y el recuadro exige construir la frase con las salidas de los barridos, verificado abajo |

Los cuatro quedan `Aceptado — pendiente` y no `Implementado`, porque `session-closer` no es la
auditoria que puede cerrar un hallazgo (`CLAUDE.md`): lo implementado por `manager` esta en el diff
de este commit, pero falta la pasada de `report_auditor` sobre el commit que lo contiene.

Verificacion de las correcciones citadas arriba, contra el estado staged de esta sesion (equivalente
al commit que se va a crear):

```
$ git show :.claude/skills/protocol-close/SKILL.md | grep -c 'Los dos barridos se publican SIEMPRE en su forma anclada'
1

$ git show :_audit/S-025.md | grep -c 'el barrido de arriba se etiqueta'
1

$ git show :.claude/skills/protocol-close/SKILL.md | grep -c 'Son DOS barridos, y lo que puede detener el paso es el segundo'
1

$ git show :.claude/skills/protocol-close/SKILL.md | grep -c 'La condicion de parada es del CONTROL'
1

$ git show :_audit/S-025.md | grep -c 'el recuento accesorio de arriba es falso: son 22'
1

$ git show :_audit/S-025.md | grep -c 'la frase de cierre de arriba contradice a la nota'
1

$ git show :.claude/skills/protocol-close/SKILL.md | grep -c 'no recontando a'
1
```

## 1. Que se hizo

Lista completa de archivos tocados. El commit de esta sesion todavia no existe; se publica sobre el
area de staging, y el Paso 7c la ancla al hash una vez creado:

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
_audit/S-025.md
_audit/S-026.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_phases/040_evol.md
_workflow/040_evol.md
```

Con codigos y rutas, que cambio y por que:

- **`.claude/skills/protocol-close/SKILL.md`** — el recuadro «Que dice la nota de la seccion 7,
  exactamente» del Paso 7c se reescribe: pasa de exigir un barrido unico que salga VACIO a describir
  **dos** barridos (CENSO/CONTROL, `D-107`); prohibe `cat` y `HEAD` dentro de las ordenes publicadas y
  exige que la nota nombre el commit al que corresponde su salida (`D-106`); y su punto 3 pasa a
  exigir que la frase de cierre se construya con las salidas de los barridos, nunca recontando a mano
  (`D-109`).
- **`_audit/S-025.md`** — entrada existente que el commit edita, no nace. Tres notas fechadas nuevas
  en su seccion 7: la de `F-070` (republica el barrido contra `f1f2291` y `f4658f5`), la de `F-072`
  (corrige 27→22 y «ocho»→trece) y la de `F-073` (fija 32/34/18 en vez de 34/34/16). Ninguna linea
  original se reescribe (`D-019`).
- **`_audit/findings.md`** — entrada existente que el commit edita. `F-070` a `F-073` pasan de
  `Abierto` a `Aceptado — pendiente`, cada uno citando su `T-XXX` (`D-XXX`).
- **`_audit/index.md`** — nace la fila de `S-026` (esta sesion), con `Pendiente` en las tres ultimas
  columnas.
- **`_persistence/assumptions.md`** — `A-012` (nace): la doble lectura de «opera de forma sostenida
  con usuarios reales» en `_workflow/ai_levels.md` §6. `A-011` (nota anadida): su criterio de
  refutacion original acertaba igual si el supuesto era cierto o falso; se sustituye por uno que se
  valida contra los bloques «Criterio de cierre», y se deja constancia de que su primera version del
  barrido (arbol de trabajo) tambien reproducia mal, con las dos ordenes ancladas y sus salidas.
- **`_persistence/decisions.md`** — `D-106`, `D-107`, `D-108`, `D-109`, `D-110` (nacen). Las cuatro
  primeras aceptan `F-070` a `F-073`; `D-110` es la decision del usuario de escribir
  `_workflow/040_evol.md` ahora y no adoptarlo todavia.
- **`_persistence/lessons.md`** — `L-036` (nace): una condicion de parada que no se puede cumplir se
  convierte en una excepcion redactada en prosa cada vez.
- **`_persistence/progress.md`** — entrada existente que el commit edita (Estado general, Ultimo
  realizado, Siguiente paso, indice de sesiones) y `S-026` (nace) en la Bitacora.
- **`_persistence/tasks.md`** — `T-106`, `T-107`, `T-108`, `T-109` (nacen), todas `Implementada`,
  `Origen: report_auditor`. `T-105` (nota anadida): pasa de `No implementada` a `Implementada`, con
  su criterio de cierre acotado a lo que `D-110` cubre (escribir el archivo, no adoptarlo).
- **`_phases/040_evol.md`** — entrada existente (procedimiento, no registro) que el commit edita: la
  tabla de estado de §5 pasa de «plantillas escritas, reparto pendiente» a «los dos artefactos ya
  estan escritos; lo que falta es adoptar el reparto», con el `D-110` que la respalda.
- **`_workflow/040_evol.md`** (nace, archivo nuevo, no versionado hasta este commit) — el reparto de
  trabajo de la etapa de la evolucion: una fila por cada uno de sus ocho pasos, aplicando `team.md` y
  `ai_levels.md`, con lectura de nivel de sistema de IA en 6.

## 2. Que NO se hizo, y por que

Tareas abiertas en `tasks.md` (filtro de la columna Estado, no de texto libre):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | awk -F'|' 'NF>5 && $4 ~ /No implementada/ {print $2}'
 [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto)
 [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject)
 [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003)
 [T-037](#t-037---escribir-el-inventario-de-acciones-irreversibles-del-proyecto-lg-38)
 [T-038](#t-038---igualar-el-barrido-de-fuga-de-protocol-audit-con-el-de-protocol-close)
```

- `T-001` — Definir alcance y objetivo del proyecto. Bloqueada por `A-004` (acceso a patrocinador).
- `T-002` — Declarar las etapas posteriores a `000_preproject`. Depende de `005_discovery`.
- `T-003` — Verificar si el historico de la fuente oficial es obtenible (`A-003`). Parcialmente
  probado en `S-011`, sin cerrar.
- `T-037` — Escribir el inventario de acciones irreversibles del proyecto (`LG-38`). Disponible, sin
  tomar.
- `T-038` — Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close`. Disponible,
  sin tomar; es la razon por la que esta misma auditoria sigue usando el patron ampliado de
  `protocol-close` en vez del propio.

Ademas, `_workflow/040_evol.md` esta **escrito y no adoptado**: la decision de que reparto adopta
`040_evol` queda para cuando la etapa se declare, por `D-110`.

## 3. Decisiones tomadas

- **`D-106`** — El barrido de la nota de cierre se publica siempre anclado, y la nota nombra el
  commit de su salida. Alternativas descartadas: dejarlo solo con la nota fechada (no ataca la
  causa); prohibir el arbol de trabajo en cualquier orden de cualquier archivo (demasiado ancho,
  rompe ordenes legitimas como `git status`); exigir que la nota se escriba tras el commit de
  anclaje (mismo huevo-y-gallina un nivel mas arriba).
- **`D-107`** — El barrido se parte en CENSO (universal, informa) y CONTROL (universal, detiene si
  senala un archivo no autorizado). Alternativas descartadas: afinar el patron para que no detecte
  las dos clases no-pendientes (no es distinguible con una regex sin un marcador nuevo, contra
  `PI-2`); dejar la regla y aceptar que el informe explique en prosa (la practica que produjo el
  hallazgo); quitar la condicion de parada (pierde la unica defensa contra anclar un archivo no
  autorizado); excluir `assumptions.md` del barrido (convierte un archivo del registro en punto
  ciego permanente).
- **`D-108`** — El recuento accesorio de `S-025` se corrige por nota fechada (22 y 13). No lleva
  cambio de protocolo propio: la regla que faltaba se escribe en `D-109`.
- **`D-109`** — La frase de cierre se construye con las salidas de los barridos, no recontando a
  mano. Alternativa descartada y explicita: `R-025` §5 sugirio que la seccion publique solo ordenes y
  salidas, sin prosa — no se adopta, porque la prosa explica el porque de lo que no se pudo anclar, y
  eso no sale de ninguna salida.
- **`D-110`** — Se escribe `_workflow/040_evol.md` ahora; su adopcion queda para cuando la etapa se
  declare. Alternativas descartadas: adoptar el reparto de paso (le quita la decision al usuario en
  el momento en que de verdad importa); copiar el reparto de `030_growth` y ajustarlo (no comparte
  fuente del trabajo, riesgo ni cadencia); leer el nivel de IA como 5 igual que la etapa anterior (no
  nombra lo unico que el 6 anade); esperar a que la etapa se declare para escribirlo (es la condicion
  de entrada que `R-024` §5 senalo para que no se descubriera tarde).

Las cinco estan marcadas «Reversible a criterio», declarado como criterio y no leido de una tabla
(`T-037` sigue abierta, sin inventario de acciones irreversibles).

## 4. Supuestos vigentes y riesgos

- **`A-006`** a **`A-009`** — abiertos, sin cambios esta sesion.
- **`A-010`** — `Refutado` (de `S-024`), sin cambios.
- **`A-011`** — abierto. Su enunciado —los bloques «Criterio de cierre» no nacen fuera de
  `decisions.md` y `tasks.md`— se sostiene, pero su criterio de refutacion original era el
  equivocado: barria toda linea con `<hash>`, no los bloques «Criterio de cierre», y acertaba igual
  si el supuesto era cierto o falso. Se sustituye (`D-107`) por: «el barrido de bloques `Criterio de
  cierre` devuelve una linea de un archivo distinto de `decisions.md`/`tasks.md`». Si este supuesto
  cae, el Paso 7c-bis tendria que ampliarse a un tercer archivo sin que ninguna regla lo autorizara.
- **`A-012`** (nace) — «Opera de forma sostenida con usuarios reales» (`ai_levels.md` §6) se lee
  sobre el sistema de trabajo, no sobre el producto. Sobre esa lectura descansa que `_workflow/
  040_evol.md` declare nivel de sistema de IA 6, la unica del metodo. Si resulta falso —si la linea
  se lee sobre los usuarios del producto— el reparto de `040_evol` habria elegido un nivel
  equivocado, y ese error se descubriria recien al adoptar el reparto, que es cuando empieza a costar
  dinero. Disparador: el `D-XXX` de adopcion de `040_evol`, o antes, cualquier auditoria que lea
  `ai_levels.md` §6 con esta lectura aplicada.

## 5. Siguiente tarea propuesta

Lanzar `report_auditor` sobre el commit de este cierre. Sin tarea de codigo nueva mas urgente que
las cinco ya abiertas (`T-001`, `T-002`, `T-003`, `T-037`, `T-038`); de esas, `T-037` (inventario de
acciones irreversibles, Alta, No bloqueante) y `T-038` (igualar el barrido de fuga de
`protocol-audit`, Media, No bloqueante) son las unicas que no dependen de `A-004` ni de abrir una
etapa nueva, y por eso son la propuesta concreta para la proxima sesion si no hay una decision de
alcance que la anteceda.

## 6. Que pedimos auditar

- **La particion CENSO/CONTROL de `D-107` es nueva y se autoexplica con tres versiones fallidas
  documentadas** (la del patron `<hash>` a secas, la de `<hash>:` sin `git show`, y la final). Vale la
  pena que una mirada independiente corra las dos ordenes del CONTROL sobre el commit de esta sesion
  y confirme que no senala ningun archivo fuera de `decisions.md`/`tasks.md` — es exactamente el caso
  que `F-071` abrio, y esta sesion no lo prueba fuera de la propia sesion que lo escribio.
- **Las tres notas fechadas de `_audit/S-025.md` §7 introducen cifras nuevas (22, 13, 32, 34, 18)
  sin tocar las lineas originales.** Es la primera vez que una misma seccion acumula tres notas
  fechadas seguidas sobre el mismo parrafo; vale la pena revisar si eso sigue siendo legible o si
  convendria consolidar, aunque `D-019` prohiba reescribir.
- **`A-012` es un supuesto sobre la lectura de un archivo agnostico (`ai_levels.md`) aplicado a una
  etapa que no existe todavia.** Es la parte de esta sesion con menos certeza: la lectura elegida es
  defendible pero no la unica, y el propio supuesto lo dice. Si el auditor tiene una lectura distinta
  del texto de `ai_levels.md` §6, es preferible que se discuta ahora que la etapa esta lejos, no
  cuando `040_evol` se adopte.
- **`T-105` se marca `Implementada` con un criterio de cierre acotado por `D-110`, no con el criterio
  original.** Es una forma de cierre parcial: el archivo existe, pero la condicion de entrada de la
  etapa (`_phases/040_evol.md` §5) sigue sin cumplirse porque falta la adopcion. Vale la pena que el
  auditor confirme que esta forma de "cerrar acotando" no abre una puerta para cerrar tareas
  rebajando su criterio sin una decision que lo respalde — aqui la hay (`D-110`), pero es el primer
  caso de este patron en el registro.

## 7. Evidencia del Paso 2d

La orden se corre sobre el area de staging, porque el commit de esta sesion todavia no existe (su
forma anclada al commit, con el propio informe excluido, es
`git diff -U0 <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-026.md"`):

```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-026.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Lista completa, numerada, de las 41 lineas que devolvio (recuento de LINEAS devueltas: **41**):

```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-026.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
 1	$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
 2	$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
 3	$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sed 's/^+//' | sort | uniq -d | wc -l
 4	$ git diff -U0 f1f2291^ f1f2291 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
 5	$ git diff -U0 f1f2291^ f1f2291 -- _persistence/assumptions.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
 6	$ for f in $(git diff --name-only f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md"); do n=$(git diff -U0 f1f2291^ f1f2291 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
 7	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'Los dos barridos se publican SIEMPRE en su forma anclada'
 8	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'la nota nombra el commit al que corresponde su salida'
 9	$ git show <hash>:_audit/S-025.md | grep -c 'el barrido de arriba se etiqueta'
10	$ for f in $(git diff --name-only <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-XXX.md"); do n=$(git diff -U0 <hash>^ <hash> -- "$f" | grep -cE '^\+\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
11	$ for f in $(git diff --name-only f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md"); do n=$(git diff -U0 f1f2291^ f1f2291 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
12	$ for f in $(git diff --name-only <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-XXX.md"); do n=$(git diff -U0 <hash>^ <hash> -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
13	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'Son DOS barridos, y lo que puede detener el paso es el segundo'
14	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'La condicion de parada es del CONTROL'
15	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cE 'Barrido [12].*el (CENSO|CONTROL)'
16	$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
17	$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
18	$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sed 's/^+//' | sort | uniq -d | wc -l
19	$ git show <hash>:_audit/S-025.md | grep -c 'el recuento accesorio de arriba es falso: son 22'
20	$ git show <hash>:_audit/S-025.md | grep -c 'las filas 3-15 y 22-34 son las'
21	$ git diff -U0 f1f2291^ f1f2291 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
22	$ git diff -U0 f1f2291^ f1f2291 -- _persistence/assumptions.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
23	$ git show <hash>:_audit/S-025.md | grep -c 'la frase de cierre de arriba contradice a la nota'
24	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'no recontando a'
25	$ git show <hash>:_workflow/040_evol.md | grep -cE '^\| \*\*[1-8] · '
26	$ git show <hash>:_workflow/040_evol.md | grep -cE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b'
27	$ git show <hash>:_phases/040_evol.md | grep -c '_workflow/040_evol'
28	$ git show <hash>:_phases/040_evol.md | grep -c 'escrito, sin adoptar'
29	$ git show <hash>:_workflow/040_evol.md | grep -cE '^\| \*\*[1-8] · '
30	$ git show <hash>:_workflow/040_evol.md | grep -cE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b'
31	$ git show <hash>:_phases/040_evol.md | grep -c 'escrito, sin adoptar'
32	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'Los dos barridos se publican SIEMPRE en su forma anclada'
33	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'la nota nombra el commit al que corresponde su salida'
34	$ git show <hash>:_audit/S-025.md | grep -c 'el barrido de arriba se etiqueta'
35	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'Son DOS barridos, y lo que puede detener el paso es el segundo'
36	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'La condicion de parada es del CONTROL'
37	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cE 'Barrido [12].*el (CENSO|CONTROL)'
38	$ git show <hash>:_audit/S-025.md | grep -c 'el recuento accesorio de arriba es falso: son 22'
39	$ git show <hash>:_audit/S-025.md | grep -c 'las filas 3-15 y 22-34 son las'
40	$ git show <hash>:_audit/S-025.md | grep -c 'la frase de cierre de arriba contradice a la nota'
41	$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'no recontando a'
```

Recuento de LINEAS devueltas: **41**. Ordenes distintas, aparte (misma tuberia, con `sort -u` antes
de contar):

```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-026.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
22
```

Las repetidas (`decisions.md` y `tasks.md` citan la misma orden de criterio a proposito, cada una en
su propia entrada `D-10x`/`T-10x`):

```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-026.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sed 's/^+//' | sort | uniq -d | wc -l
19
```

**Reejecucion de las 41, una por una, contra su estado real:**

- **Filas 1-6, 11, 16-18, 21-22 (12 lineas)** llevan un hash **real** (`f1f2291`), no `<hash>`
  literal. No las excluye el filtro del Paso 2d porque la forma es `git diff -U0 f1f2291^ f1f2291`
  —el hash no va pegado a `diff`, sino despues de `-U0`—, que es el mismo falso positivo que el
  recuadro del Paso 2d ya documenta para `git grep -nE '<patron>' <hash> -- <ruta>`. Las doce estan
  **ya ancladas**, viven dentro de las secciones «Contexto» de `D-106` a `D-109` (que verifican, con
  su propia orden, las cifras que esos hallazgos corrigen) y reproducen exactamente:

```
$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
35
$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
22
$ git diff -U0 f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sed 's/^+//' | sort | uniq -d | wc -l
13
$ git diff -U0 f1f2291^ f1f2291 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
33
$ git diff -U0 f1f2291^ f1f2291 -- _persistence/assumptions.md | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
2
$ for f in $(git diff --name-only f1f2291^ f1f2291 -- _persistence _audit ":(exclude)_audit/S-025.md"); do n=$(git diff -U0 f1f2291^ f1f2291 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 19
_persistence/tasks.md: 14
```

  Los seis valores (`35`, `22`, `13`, `33`, `2`, `19`/`14`) coinciden exactamente con los que las
  secciones «Contexto» de `D-106` a `D-109` publican como verificacion de los hallazgos `F-070` a
  `F-073`. **Ninguna discrepancia.**

- **Las 29 lineas restantes (7-10, 12-15, 19-20, 23-41)** llevan `<hash>` literal a proposito: son
  los bloques «Criterio de cierre» de `D-106` a `D-110` (`decisions.md`) y de `T-105` a `T-109`
  (`tasks.md`, incluidas las cuatro nuevas de `T-105` que verifican `_workflow/040_evol.md` y
  `_phases/040_evol.md`). No son reproducibles todavia porque el commit que citan **es el que esta
  sesion va a crear**; corridas ahora devuelven error de sintaxis de `git` (`<hash>` no es una
  referencia valida), que es el comportamiento esperado hasta que el Paso 7c-bis las ancle:

```
$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'Los dos barridos se publican SIEMPRE en su forma anclada'
fatal: Invalid object name '<hash>'.
```

**NOTA DE CIERRE (la escribe el Paso 7c, tras el commit de esta sesion):** pendiente. El hash del
commit, cuantas de las 29 ordenes con `<hash>` quedaron ancladas y en que archivos —con el CENSO y
el CONTROL que exige `D-107`, cada uno con su propia orden y salida— y la frase que cierra el
pendiente, construida con esas dos salidas y no recontando a mano (`D-109`), se anaden aqui una vez
exista el commit.

## 8. Evidencia del Paso 2e

Barrido de caracteres de control sobre los archivos que este commit toca:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
_audit/findings.md: 1
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Separando lo nuevo de lo heredado, contra `HEAD` (`3bf61d4`):

```
$ for f in _audit/findings.md _persistence/decisions.md _persistence/tasks.md; do echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
_audit/findings.md head=1 staged=1
_persistence/decisions.md head=7 staged=7
_persistence/tasks.md head=5 staged=5
```

Las tres cifras son **heredadas**, ninguna nueva: `head` y `staged` coinciden exactamente en los tres
archivos. `_workflow/040_evol.md`, `_phases/040_evol.md`, `_persistence/assumptions.md`,
`_persistence/lessons.md`, `_persistence/progress.md` y `.claude/skills/protocol-close/SKILL.md` no
aparecen en el primer barrido: devuelven `0`.
