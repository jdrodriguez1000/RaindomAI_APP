# Informe de auditoria — S-008

| Campo | Valor |
|---|---|
| Sesion | S-008 |
| Fecha | 2026-09-02 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-008.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-017 — `T-020` queda `Implementada` sin ningun bloque de verificacion, y el informe remite a una seccion que tampoco lo tiene | Aceptado — pendiente | `T-023`, `Implementada` en `_persistence/tasks.md` (los bloques se añaden anclados a `122b770`, con nota fechada) |
| F-018 — `T-021` afirma un barrido «sobre todo el repositorio» del que no registra ni el patron ni el ambito ni la salida | Aceptado — pendiente | `T-024`, `Implementada` en `_persistence/tasks.md` |
| F-019 — La lista de archivos de la seccion 1 del informe no cuadra con el comando que dice haberla producido | Aceptado — pendiente | `T-025`, `Implementada` en `_persistence/tasks.md`; `D-040` explica por que la correccion no toca `_audit/S-007.md` |

Los tres quedan `Aceptado — pendiente` y no `Implementado`: la correccion esta hecha en este commit,
pero el estado `Implementado` solo lo escribe la auditoria siguiente al verificarlo sobre `HEAD`.

## 1. Que se hizo

Salida cruda de `git diff --cached --stat --name-only`, tomada sobre el arbol ya preparado para
este commit (el metodo se explica en la seccion 6, ultimo punto, por la circularidad de pedir
`git show` de un commit que todavia no existe):

```
.claude/skills/protocol-close/SKILL.md
CLAUDE.md
_audit/S-008.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_phases/005_discovery.md
_templates/005_discovery/005_needs.md
_templates/005_discovery/010_actors.md
_templates/005_discovery/015_stakeholders.md
_templates/005_discovery/020_hypothesis.md
project.md
```

Dieciseis archivos: los nueve que ya estaban modificados antes de este cierre (`.claude/skills/protocol-close/SKILL.md`, `CLAUDE.md`, `_audit/findings.md`, `_persistence/assumptions.md`, `_persistence/decisions.md`, `_persistence/lessons.md`, `_persistence/tasks.md`, `_phases/005_discovery.md`, `project.md`) mas los siete que anade el propio cierre y el trabajo de esta sesion: `_persistence/progress.md`, `_audit/index.md`, `_audit/S-008.md` (este informe), y las cuatro plantillas nuevas de `_templates/005_discovery/`.

Lo que muestra el diff, con codigos y rutas:

- **`_persistence/decisions.md`** — nacen `D-036` a `D-042`. `D-036` decide que los artefactos
  rellenos del descubrimiento viven en `_discovery/` (carpeta que no se crea todavia, `git` no
  versiona carpetas vacias). `D-037` decide no escribir la quinta plantilla del descubrimiento
  (restricciones y supuestos) porque ya tienen sitio en `_persistence/`, y añade ahi los campos
  `Dueño` y `Riesgo abierto`. `D-038` declara `I-XXX` como codigo de interesado. `D-039` reetiqueta
  `T-022` de `005_discovery` a `000_preproject`. `D-040` decide no reescribir `_audit/S-007.md` para
  corregir `F-019`: la correccion va al mecanismo de `protocol-close`. `D-041` decide adaptar (no
  copiar) las cuatro plantillas de descubrimiento que aporto el usuario. `D-042` fija que las
  entradas de esta sesion se fechan `2026-09-02`, por continuidad con `S-007`/`R-007`.
- **`_persistence/assumptions.md`** — la seccion de convenciones gana el estado `Riesgo abierto` y
  el campo `Dueño` (`D-037`). No se anade ningun `A-XXX` nuevo.
- **`_persistence/lessons.md`** — nacen `L-011` (un mecanismo escrito como aviso se salta; escrito
  como hueco de plantilla, no) y `L-012` (un barrido sobre texto de prosa se corre insensible a
  mayusculas, o no barre).
- **`_persistence/tasks.md`** — `T-022` pasa a `Implementada` y cambia de etapa/titulo/alcance
  (cuatro plantillas, no cinco). Nacen `T-023`, `T-024`, `T-025` (`Origen: report_auditor`, cierran
  `F-017`/`F-018`/`F-019`) y `T-026` (`Origen: manager`, extiende el Paso 1b a `_templates/`),
  las cuatro `Implementada`. `T-020` y `T-021` reciben notas fechadas con los bloques que faltaban.
- **`_audit/findings.md`** — `F-017`, `F-018`, `F-019` pasan de `Abierto` a `Aceptado — pendiente`,
  cada uno con su «Que se hizo» y su `T-XXX`.
- **`_phases/005_discovery.md`** §5 — se reescribe la tabla de artefactos: cuatro llevan plantilla y
  el quinto (restricciones y supuestos) remite directamente a `_persistence/`.
- **`project.md`** — nueva fila `_templates/` en «Carpetas propias»; nueva fila `I-XXX` en
  «Codigos»; la fila de `N-XXX` se actualiza con la ruta ya decidida (`_discovery/005_needs.md`).
- **`CLAUDE.md`** — `_templates/` entra en la lista de lo que tiene que poder copiarse a otro
  proyecto tal cual, con su nota de por que y la diferencia con `_phases/` sobre codigos genericos
  vs. primer codigo de ejemplo.
- **`.claude/skills/protocol-close/SKILL.md`** — el Paso 1b amplia su ambito a `_templates/`; la
  seccion 1 del informe pasa de «lista redactada» a «pega la salida cruda del comando».
- **`_templates/005_discovery/`** (nueva carpeta, no versionada hasta este commit) — nacen
  `005_needs.md`, `010_actors.md`, `015_stakeholders.md`, `020_hypothesis.md`, adaptadas de las
  plantillas que aporto el usuario (`D-041`).
- **`_persistence/progress.md`** — este cierre: `Estado general`, `Ultimo realizado`, `Siguiente
  paso` sobrescritos, y nace la entrada `S-008` en la bitacora.
- **`_audit/index.md`** — nueva fila `S-008.md`, con `Pendiente` en commit, auditoria y veredicto.
- **`_audit/S-008.md`** — este mismo informe.

## 2. Que NO se hizo, y por que

Tareas que siguen `No implementada` en `_persistence/tasks.md`:

```
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject) | Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003) | Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
```

Ninguna de las tres se toco esta sesion: `T-001` y `T-002` pertenecen a `005_discovery`, etapa que
no puede abrirse hasta resolver `A-004` (acceso al patrocinador). `T-003` depende de verificar
`A-003`, y esta sesion se dedico entera a evaluar la auditoria `R-007` y a completar `T-022`, que
ya estaba agendada desde `S-007`.

## 3. Decisiones tomadas

- **`D-036`** — Los artefactos rellenos del descubrimiento viven en `_discovery/` (carpeta propia
  de primer nivel, no creada todavia). Alternativas descartadas: meterlos en `_persistence/` (mezcla
  «como va el trabajo» con «que se entendio del producto»); dejarlo sin decidir y escribir las
  plantillas con la ruta como hueco (deja los bloques de verificacion sin poder ejecutarse).
- **`D-037`** — El descubrimiento no lleva plantilla de restricciones y supuestos: van a
  `_persistence/assumptions.md` y `_persistence/constraints.md`, que ganan los campos `Dueño` y
  `Riesgo abierto`. Alternativas descartadas: escribir la plantilla completa adaptando `SUP-`/`RES-`
  a `A-XXX`/`C-XXX` (dos sitios para lo mismo); escribirla delgada, solo con lo que faltaba (mismo
  defecto en pequeño).
- **`D-038`** — `I-XXX` es el codigo de interesado. Alternativas descartadas: listar interesados sin
  identificador (sin forma estable de citarlos); reutilizar `A-XXX`/`C-XXX` (no son ni supuesto ni
  restriccion).
- **`D-039`** — `T-022` se reetiqueta a `000_preproject`: escribir plantillas es andamiaje, no
  descubrimiento. Alternativa descartada: dejarla en `005_discovery` (mezcla la condicion de salida
  de la etapa con trabajo de metodo, que es lo que `R-007` señalo sin abrirlo como hallazgo).
- **`D-040`** — `_audit/S-007.md` no se reescribe para corregir `F-019`; la correccion va al
  mecanismo (`T-025`). Alternativas descartadas: completar la lista en `S-007.md` (reescribe
  historia auditada y reproduce el mismo defecto: una lista que dice venir de un comando y no viene);
  no hacer nada y anotar solo la leccion (`L-008` dice que una leccion sin mecanismo no evita la
  reincidencia, y este defecto ya iba por la segunda vez).
- **`D-041`** — Las cuatro plantillas se adaptan de las del usuario, no se copian. Alternativas
  descartadas: copiarlas y corregirlas despues (deja vocabulario del esquema revocado dentro de
  archivos que el control de agnosticidad no detecta porque busca datos propios, no incoherencias de
  metodo); escribirlas desde cero (tira el trabajo de metodo que ya aportaba el usuario).
- **`D-042`** — Las entradas de esta sesion se fechan `2026-09-02`, por continuidad con el registro.
  Alternativas descartadas: usar `2026-09-01`, la del reloj (dejaria a `S-008` fechada antes que
  `S-007`); usar `2026-09-03` (un dia sin respaldo en ningun reloj ni commit).

Las siete llevan su clasificacion de reversibilidad declarada como criterio (no como tabla) dentro
de la propia entrada de `decisions.md`, porque no existe todavia un inventario de acciones
irreversibles en `_persistence/`.

## 4. Supuestos vigentes y riesgos

Los cuatro supuestos siguen `Abierto` en `_persistence/assumptions.md`; ninguno se toco esta sesion:

- **`A-001`** — Un auditor lanzado por el auditado conserva independencia suficiente. Sostiene todo
  el esquema de `report_auditor`. Si resultara falso, las auditorias de `_audit/` perderian su valor
  como revision independiente.
- **`A-002`** — El brief recibido es el encargo completo. Sostiene `005_discovery`.
- **`A-003`** — El historico de la fuente oficial es obtenible de forma automatizable. De el depende
  el ciclo entero del producto (`T-003`, `Alta`/`Bloqueante`).
- **`A-004`** — Existe acceso al patrocinador y a personas que puedan hablar del proceso real. Es
  entrada obligatoria declarada por `_phases/005_discovery.md`: sin resolverla, la etapa no puede
  empezar.

`D-037` añadio a `assumptions.md` los campos `Dueño` y `Riesgo abierto`, pero es explicito que **no
es retroactivo**: `A-001` a `A-004` no llevan `Dueño` todavia, y no se les añade uno inventado en
este cierre.

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto a partir de `_brief/client_brief.md`. Importancia
`Alta`, urgencia `Bloqueante`, etapa `005_discovery`. Antes de arrancarla hace falta resolver
`A-004`: confirmar que existe un patrocinador alcanzable y personas que puedan hablar del proceso
real, porque `_phases/005_discovery.md` declara ese acceso como entrada obligatoria.

## 6. Que pedimos auditar

- **`T-026` se escribio a mano fuera de las dos excepciones que la convencion de `tasks.md`
  permite**, y la propia ficha lo declara asi en vez de disimularlo: no nace de un `F-NNN` (`R-007`
  no la abrio) ni de una decision del usuario que no se pudiera deducir del diff (esta si se deduce:
  el diff toca `CLAUDE.md` y `SKILL.md`). Se dejo escrita, con su bloque de verificacion, en vez de
  borrarla para que el cierre la reescribiera identica. Pedimos que se audite si esa forma de
  declarar el incumplimiento es aceptable o si el hallazgo deberia abrirse igual.
- **El bloque de verificacion nuevo de `T-023` y `T-024` no se ejecuto en el momento: se reprodujo**
  contra `122b770`, el commit sobre el que las tareas originales quedaron `Implementada`. La ficha
  lo declara con nota fechada, siguiendo el patron que ya establecio `T-014` para `A-001`. Pedimos
  que se compruebe que esa forma de anclar sigue sin leerse como evidencia contemporanea.
- **`D-040` decide no tocar `_audit/S-007.md`** aunque `F-019` tenga razon sobre su contenido. Es
  coherente con la prohibicion de `CLAUDE.md` de reescribir informes ya auditados, pero es una
  decision que deja un defecto conocido visible para siempre en un archivo entregado. Pedimos que se
  evalue si ese criterio —«el mecanismo se corrige, el informe entregado no»— es el correcto o si
  faltaria, por ejemplo, una nota de fe de erratas en el propio `S-007.md`.
- **La seccion 1 de este mismo informe** es la primera que se escribe bajo la regla nueva de
  `T-025`: pega la salida cruda de `git show --stat --name-only --format= <commit>` en el bloque de
  arriba. Como el comando solo puede correrse **despues** de que el commit exista, y el informe tiene
  que estar dentro de ese mismo commit, el cierre resuelve la circularidad calculando la lista sobre
  el arbol ya preparado para el commit (`git diff --cached --stat --name-only`) antes de commitear, y
  pega esa salida en el marcador de arriba. Pedimos que se audite si ese metodo es equivalente al que
  pide la plantilla o si deja un hueco que `T-025` no prevdefinia.

## Nota del cierre sobre la seccion 1

El marcador `<PEGA_SECCION_1_AQUI>` se sustituye, antes del `git add` final, por la salida cruda de
`git diff --cached --stat --name-only` tomada sobre el arbol ya preparado para este commit — el
metodo que explica el punto anterior de la seccion 6.
