# Auditoria R-030 — sobre S-030

| Campo | Valor |
|---|---|
| Auditoria | R-030 |
| Informe auditado | `_audit/S-030.md` |
| Commit auditado | `9b3f9ee` |
| Fecha | 2026-09-08 |
| Veredicto | Con hallazgos (4) |

## 1. Que se comprobo

### Que commit se audita

`git log -1 --format=%h -- _audit/S-030.md` devuelve `a6f798e`, que es el **commit de anclaje** del
Paso 7c/7c-bis. El hash literal de la cabecera del informe manda, y es otro:

```
$ git show a6f798e:_audit/S-030.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `9b3f9ee` |
$ git show --stat --name-only --format= a6f798e | grep -c .
3
$ git show --stat --name-only --format= 9b3f9ee | grep -c .
17
```

Se audita `9b3f9ee`, cuyo `--stat` (17 archivos) cuadra con la lista de la seccion 1 del informe.
Fecha del commit y fecha del informe coinciden:

```
$ git log -1 --format='%h %ad' --date=short 9b3f9ee
9b3f9ee 2026-09-08
```

### Control a — indice contra detalle en `_persistence/` (limpio)

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 9b3f9ee:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 9b3f9ee:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

### Control b — carpetas del arbol contra las declaradas (dos diferencias, las dos con razon escrita)

```
$ diff <(git ls-tree -d --name-only 9b3f9ee | sed 's|$|/|' | sort) <(git show 9b3f9ee:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos razones estan escritas en `project.md`, seccion «Carpetas propias»: `temporal/` esta en
`.gitignore` y `010_prototype/` esta declarada por adelantado (`D-061`). No es hallazgo.

### Control c — fuga de datos propios (cero lineas, en los cuatro y en los seis)

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" 9b3f9ee -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 9b3f9ee -- .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
exit=1
```

Y ningun codigo instanciado en lo que el commit anade a los seis agnosticos:

```
$ git grep -nE '\b(S|T|D|C|A|L|R|F|DT|N|I|FT|SC|H|LG)-[0-9]{2,3}\b' 9b3f9ee -- .claude/skills/protocol-phase-exit/SKILL.md .claude/agents/phase_exit_auditor.md _templates/phase_exit_record.md ; echo "exit=$?"
exit=1
$ git diff -U0 9b3f9ee^ 9b3f9ee -- .claude _phases _templates CLAUDE.md _methodology _workflow | grep -E '^\+' | grep -oE '\b(S|T|D|C|A|L|R|F|DT|LG)-[0-9]{2,3}\b' | sort -u
```

(sin salida)

Esto responde al cuarto punto de la seccion 6 del informe: sobre el commit ya trackeado, el barrido
sigue devolviendo cero.

### El recuento de hunks de la seccion 1 (reproduce, archivo por archivo)

```
$ for f in $(git diff --name-only 9b3f9ee^ 9b3f9ee); do echo -n "$f: "; git diff -U0 9b3f9ee^ 9b3f9ee -- "$f" | grep -c '^@@'; done
.claude/agents/phase_exit_auditor.md: 1
.claude/skills/protocol-close/SKILL.md: 13
.claude/skills/protocol-phase-exit/SKILL.md: 1
_audit/S-029.md: 2
_audit/S-030.md: 1
_audit/findings.md: 5
_audit/index.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 2
_persistence/lessons.md: 2
_persistence/progress.md: 9
_persistence/tasks.md: 9
_phases/000_preproject.md: 6
_templates/000_preproject/005_project.md: 1
_templates/020_baseline/045_traceability.md: 1
_templates/phase_exit_record.md: 1
project.md: 2
```

Las doce cifras que el informe publica (13, 2, 5, 4, 2, 2, 9, 9, 6, 1, 1, 2) coinciden todas. Esto
responde al primer punto de la seccion 6: la atribucion **por posicion** reproduce lo afirmado.

La atribucion de contenido tambien se contrasto y sale correcta. En el caso mas dudoso
—`_phases/000_preproject.md`, que `F-082` ya habia cazado por numerar mal sus secciones—:

```
$ git show 9b3f9ee:_phases/000_preproject.md | grep -n '^## '
21:## 1. Que autoriza esta etapa
45:## 2. Que prohibe esta etapa
69:## 3. Entradas — que debe existir antes de empezar
81:## 4. Procedimiento
143:## 5. Artefactos que produce
195:## 6. Condicion de salida
267:## 7. Que registra `manager` en `_persistence/`
295:## 8. Lo que esta etapa le entrega a la siguiente
```

La casilla 2 esta en la seccion 6 y el subtitulo «### 2. La forma de trabajo entre los seis agentes»
en la 8, como el informe dice.

### El Paso 2d (reproduce)

```
$ git diff -U0 9b3f9ee^ 9b3f9ee -- _persistence _audit ':(exclude)_audit/S-030.md' | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
28
$ git diff -U0 9b3f9ee^ 9b3f9ee -- _persistence _audit ':(exclude)_audit/S-030.md' | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d | wc -l
7
```

`28` es la cifra publicada, y las 7 lineas de `uniq -d` son las 7 publicadas, literalmente. La cifra
de ordenes distintas ya no se publica, que es lo que `D-124` decidio.

### El CENSO, el CONTROL y los residuos del Paso 7c-bis (reproducen)

```
$ for f in $(git ls-tree -r --name-only 9b3f9ee _persistence _audit | grep -v '_audit/S-030.md'); do n=$(git show 9b3f9ee:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-025.md: 2
_audit/R-026.md: 7
_audit/R-027.md: 1
_audit/R-029.md: 2
_audit/S-017.md: 1
_audit/S-024.md: 1
_audit/S-025.md: 17
_audit/S-026.md: 8
_audit/S-027.md: 17
_audit/S-029.md: 2
_persistence/assumptions.md: 9
_persistence/decisions.md: 27
_persistence/tasks.md: 30
$ for f in $(git diff --name-only 9b3f9ee^ 9b3f9ee -- _persistence _audit ":(exclude)_audit/S-030.md"); do n=$(git diff -U0 9b3f9ee^ 9b3f9ee -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 7
_persistence/tasks.md: 8
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo -n "$f: "; git show a6f798e:"$f" | grep -cE '^\$ .*<hash>'; done
_persistence/decisions.md: 15
_persistence/tasks.md: 21
```

Las tres salidas son identicas a las publicadas, incluidos los residuos `27 - 12 = 15` y
`30 - 9 = 21`.

### Las 21 ordenes ancladas (estan las 21, y las 21 reproducen)

```
$ git show a6f798e:_persistence/decisions.md | awk '/^### D-/{d=$2} /^\$ .*9b3f9ee/{print d}' | sort | uniq -c
      2 D-124
      2 D-125
      1 D-126
      2 D-127
      3 D-128
      2 D-129
$ git show a6f798e:_persistence/tasks.md | awk '/^### T-/{d=$2} /^\$ .*9b3f9ee/{print d}' | sort | uniq -c
      1 T-130
      2 T-131
      1 T-132
      2 T-133
      1 T-134
      2 T-135
```

12 + 9 = 21, como afirma la NOTA DE CIERRE. Reejecutadas todas contra `9b3f9ee`:

```
$ for p in 'la UNICA cifra del paso' 'La cifra de ORDENES DISTINTAS ya no se publica' 'Los dos contrastes de la tabla' 'DOS CONTRASTES de la tabla' 'Su orden y su salida se publican en la NOTA DE CIERRE' 'la salida del CONTROL DE PROSA BORRADA del Paso 7c-bis'; do echo -n "[$p] = "; git show 9b3f9ee:.claude/skills/protocol-close/SKILL.md | grep -c "$p"; done
[la UNICA cifra del paso] = 1
[La cifra de ORDENES DISTINTAS ya no se publica] = 1
[Los dos contrastes de la tabla] = 1
[DOS CONTRASTES de la tabla] = 1
[Su orden y su salida se publican en la NOTA DE CIERRE] = 1
[la salida del CONTROL DE PROSA BORRADA del Paso 7c-bis] = 1
$ git show 9b3f9ee:_audit/S-029.md | grep -c 'Nota del 2026-09-08 (`F-083`)'
1
$ git show 9b3f9ee:_audit/S-029.md | grep -c 'Nota del 2026-09-08 (`F-084`)'
1
$ git ls-tree --name-only 9b3f9ee _templates/ | grep -c 'phase_exit'
1
$ git grep -nE '\b(S|T|D|C|A|L|R|F|DT|N|I|FT|SC|H)-[0-9]{2,3}\b' 9b3f9ee -- _templates/phase_exit_record.md ; echo "exit=$?"
exit=1
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 9b3f9ee -- _templates/phase_exit_record.md ; echo "exit=$?"
exit=1
$ git show 9b3f9ee:.claude/agents/phase_exit_auditor.md | grep -c '^model: sonnet'
1
$ git grep -h '^model:' 9b3f9ee -- .claude/agents/ | sort | uniq -c
      1 model: haiku
      3 model: opus
      2 model: sonnet
$ git grep -hIoE '\b(FT|SC)-[0-9]+\b' 9b3f9ee -- .claude CLAUDE.md _phases _methodology _templates _workflow | sort -u | tr '\n' ' '
FT-001 FT-002 FT-003 FT-004 SC-001 SC-002 SC-003 SC-004
```

Las 21 reproducen lo publicado en su `D-XXX` / `T-XXX`.

### El CONTROL DE PROSA BORRADA del anclaje (limpio, reejecutado sobre los dos commits)

```
$ outside() { awk '/^```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do git show 9b3f9ee:"$f" | outside > /tmp/a; git show a6f798e:"$f" | outside > /tmp/b; echo "== $f =="; diff /tmp/a /tmp/b | grep '^<'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

El anclaje `a6f798e` no borro ni una linea de prosa en los dos archivos que el Paso 7c-bis tiene
autorizados. Las cinco lineas que si desaparecen de `_audit/S-030.md` son los marcadores
`<HASH>` y «el commit todavia no existe» que el Paso 7c sustituye por diseño.

### Las afirmaciones de contenido de las secciones 2 y 3 (correctas)

```
$ git show 9b3f9ee:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -c "No implementada"
11
$ git show 9b3f9ee:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -E '\[T-039\]' | awk -F'|' '{print $4}'
 Implementada
```

Once filas, y `T-039` empareja por el texto de su titulo teniendo estado `Implementada` — el falso
positivo que el informe declara. Las seis decisiones, las tres lecciones y las seis tareas nuevas
existen, y `A-016` quedo `Confirmado`:

```
$ git diff 9b3f9ee^ 9b3f9ee -- _persistence/decisions.md | grep -cE '^\+### D-'
6
$ git diff 9b3f9ee^ 9b3f9ee -- _persistence/lessons.md | grep -cE '^\+### L-'
3
$ git diff 9b3f9ee^ 9b3f9ee -- _persistence/tasks.md | grep -cE '^\+### T-'
6
$ git show 9b3f9ee:_persistence/assumptions.md | grep -A4 '^### A-016' | grep '^| Estado'
| Estado | Confirmado |
```

### El Paso 2e sobre el commit (la conclusion es cierta; el ambito medido, no)

```
$ for f in $(git diff --name-only --diff-filter=d 9b3f9ee^ 9b3f9ee); do echo "$f parent=$(git show 9b3f9ee^:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') commit=$(git show 9b3f9ee:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
.claude/agents/phase_exit_auditor.md parent=0 commit=0
.claude/skills/protocol-close/SKILL.md parent=0 commit=0
.claude/skills/protocol-phase-exit/SKILL.md parent=0 commit=0
_audit/S-029.md parent=0 commit=0
_audit/S-030.md parent=0 commit=0
_audit/findings.md parent=1 commit=1
_audit/index.md parent=0 commit=0
_persistence/assumptions.md parent=4 commit=4
_persistence/decisions.md parent=7 commit=7
_persistence/lessons.md parent=0 commit=0
_persistence/progress.md parent=0 commit=0
_persistence/tasks.md parent=5 commit=5
_phases/000_preproject.md parent=0 commit=0
_templates/000_preproject/005_project.md parent=0 commit=0
_templates/020_baseline/045_traceability.md parent=0 commit=0
_templates/phase_exit_record.md parent=0 commit=0
project.md parent=0 commit=0
$ git diff --name-only --diff-filter=d 9b3f9ee^ 9b3f9ee | wc -l
17
$ for f in $(git diff --name-only --diff-filter=d 9b3f9ee^ 9b3f9ee); do git show 9b3f9ee:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
```

La conclusion del informe —«esta sesion no anadio ninguna linea nueva con caracteres de control»— es
**cierta**: `parent` y `commit` coinciden en los diecisiete. El ambito medido no son diecisiete sino
catorce; ver `F-090`.

## 2. Hallazgos

### F-087 - La seccion 4 del informe publica «trece» supuestos abiertos donde su propia orden devuelve quince, y su propia enumeracion suma quince
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-030.md`, seccion 4 |

- **Que se observo:** el informe escribe la orden y afirma a continuacion que «devuelve **trece**
  filas: `A-001` a `A-009`, `A-011` a `A-015` y `A-017`». Esa enumeracion son 9 + 5 + 1 = **quince**
  codigos, y la orden devuelve quince:

  ```
  $ git show 9b3f9ee:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -cE 'Abierto'
  15
  $ git show 9b3f9ee:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -E 'Abierto' | grep -oE 'A-[0-9]+' | tr '\n' ' '
  A-001 A-002 A-003 A-004 A-005 A-006 A-007 A-008 A-009 A-011 A-012 A-013 A-014 A-015 A-017
  ```

  La lista de codigos es exacta; lo unico falso es la cifra.
- **Por que importa:** es la cuarta vez que un recuento escrito a mano junto a la orden que lo
  desmiente sale falso (`F-068`, `F-072`, `F-083`, `F-084`), y esta vez en un sitio que ninguna de
  las correcciones de esta misma sesion cubre: `D-124` suprimio la cifra del Paso 2d y `D-125` blindo
  la tabla del Paso 2e, pero la seccion 4 del informe sigue publicando un recuento sin contraste.
  Ademas la cifra se autodesmiente contra su propia enumeracion, en la misma frase — el caso mas
  barato de detectar y el que mas erosiona la confianza en el resto del informe.
- **Que lo corregiria:** pegar la salida de la orden en vez de resumirla en prosa; o, consistente con
  `D-124`, suprimir la cifra y dejar solo la lista de codigos. Es una recomendacion, no una orden.

### F-088 - La NOTA DE CIERRE afirma «las 6 restantes» y su desglose enumera cinco
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-030.md`, seccion 7, NOTA DE CIERRE |

- **Que se observo:** el informe escribe «El CONTROL detecta 15 ordenes … las **6** restantes de las
  21 nacidas esta sesion usan `git grep ... <hash> -- ...` o `git ls-tree --name-only <hash> ...`
  (`D-126`: 1; `D-128`: 3 — una `git ls-tree` y dos `git grep`; `T-134`: 1)». `1 + 3 + 1 = 5`. La
  sexta existe y esta en `D-129`:

  ```
  $ git show a6f798e:_persistence/decisions.md | awk '/^### D-/{d=$2} /^\$ .*9b3f9ee/{print d": "$0}' | grep -v 'git show 9b3f9ee:'
  D-126: $ git grep -hIoE '\b(FT|SC)-[0-9]+\b' 9b3f9ee -- .claude CLAUDE.md _phases _methodology _templates _workflow | sort -u | tr '\n' ' '
  D-128: $ git ls-tree --name-only 9b3f9ee _templates/ | grep -c 'phase_exit'
  D-128: $ git grep -nE '\b(S|T|D|C|A|L|R|F|DT|N|I|FT|SC|H)-[0-9]{2,3}\b' 9b3f9ee -- _templates/phase_exit_record.md ; echo "exit=$?"
  D-128: $ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 9b3f9ee -- _templates/phase_exit_record.md ; echo "exit=$?"
  D-129: $ git grep -h '^model:' 9b3f9ee -- .claude/agents/ | sort | uniq -c
  $ git show a6f798e:_persistence/tasks.md | awk '/^### T-/{d=$2} /^\$ .*9b3f9ee/{print d": "$0}' | grep -v 'git show 9b3f9ee:'
  T-134: $ git grep -hIoE '\b(FT|SC)-[0-9]+\b' 9b3f9ee -- .claude CLAUDE.md _phases _methodology _templates _workflow | sort -u | tr '\n' ' '
  ```

  Son seis —`D-126`: 1, `D-128`: 3, `D-129`: 1, `T-134`: 1—, y la que falta en el desglose publicado
  es la de `D-129`. La cifra `6` es correcta; el desglose que la explica esta incompleto.
- **Por que importa:** el desglose existe para que la afirmacion «las 21, sin excepcion, se anclaron»
  sea recorrible sin rehacer el trabajo. Un desglose que no suma su propia cifra obliga a rehacerlo,
  que es justo lo que la NOTA DE CIERRE existe para evitar. La afirmacion de fondo si se sostiene
  —verificado en la seccion 1: las 21 estan ancladas y reproducen—; lo que no se sostiene es su
  enumeracion.
- **Que lo corregiria:** derivar el desglose con una orden (la de arriba sirve) en vez de escribirlo
  a mano. Es una recomendacion, no una orden.

### F-089 - La plantilla `005_project.md` no recibe la actualizacion de la fila `_audit/` que si recibe `project.md`, y el informe la describe como «el mismo cambio»
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_templates/000_preproject/005_project.md:189` frente a `project.md:192` |

- **Que se observo:** `project.md` recibe **dos** cambios —la fila de `phase_exit_auditor` y la fila
  de `_audit/`, que pasa a nombrar las actas de cierre de etapa—; su plantilla recibe **uno**.

  ```
  $ git diff --stat 9b3f9ee^ 9b3f9ee -- _templates/000_preproject/005_project.md
   _templates/000_preproject/005_project.md | 1 +
   1 file changed, 1 insertion(+)
  $ git show 9b3f9ee:_templates/000_preproject/005_project.md | grep -n '^| `_audit/`'
  189:| `_audit/` | **Como se comprueba** el trabajo: el informe de cada sesion, la auditoria de cada una, el tablero y el registro de hallazgos. En las subcarpetas de cada Gate, ademas, sus dictamenes — que **no** son auditorias de sesion y no entran en el tablero ni en `findings.md` |
  $ git show 9b3f9ee:project.md | grep -n '^| `_audit/`'
  192:| `_audit/` | **Como se comprueba** el trabajo: el informe de cada sesion, la auditoria de cada una, el tablero y el registro de hallazgos. En `015_gate1/` y `035_gate2/`, ademas, los dictamenes de cada Gate; y en una subcarpeta con el nombre de cada etapa, las **actas de cierre de esa etapa**. Ni unos ni otras son auditorias de sesion: **no** entran en el tablero ni en `findings.md` |
  ```

  Y la seccion 1 del informe describe el cambio de la plantilla como «se anade la fila de
  `phase_exit_auditor` a la tabla "Reparto de autoridad", **el mismo cambio que recibe
  `project.md`**», cuando `project.md` recibio dos.
- **Por que importa:** la plantilla existe para copiarse a otro proyecto. Un repositorio nuevo que la
  copie se lleva un `project.md` que no dice donde viven las actas de cierre de etapa — y el agente
  `phase_exit_auditor` que este mismo commit crea escribe justamente ahi. La consecuencia concreta:
  alli el control de carpetas del cierre señalaria `_audit/<etapa>/` sin razon escrita, que es
  exactamente la clase de diferencia que `project.md` obliga a justificar. Es ademas el defecto que
  `_templates/` sufre sin que nadie lo note, porque la instancia queda bien y solo falla la copia.
- **Que lo corregiria:** llevar a la fila `_audit/` de la plantilla la misma frase sobre las actas de
  cierre de etapa, en la forma generica que esa fila ya usa para los Gates. Es una recomendacion, no
  una orden.

### F-090 - El Paso 2e declara como ambito «los archivos que el commit toca» y mide el area de staging previa: tres archivos del commit quedan fuera, y los dos contrastes nuevos tampoco los ven
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `.claude/skills/protocol-close/SKILL.md`, Paso 2e (`D-125`); `_audit/S-030.md`, seccion 8 |

- **Que se observo:** la tabla de la seccion 8 publica catorce archivos y el contraste 1 devuelve
  `14` — cuadran entre si, pero el commit lleva **diecisiete**. Los tres que faltan son los que el
  cierre escribe **despues** del Paso 2e:

  ```
  $ diff <(git show a6f798e:_audit/S-030.md | grep -E '^[^ ]+ head=[0-9]+ staged=[0-9]+$' | awk '{print $1}' | sort) <(git diff --name-only --diff-filter=d 9b3f9ee^ 9b3f9ee | sort)
  4a5
  > _audit/S-030.md
  5a7
  > _audit/index.md
  8a11
  > _persistence/progress.md
  ```

  Los tres salen `0` sobre el commit (verificado en la seccion 1 de esta auditoria), asi que la
  conclusion del informe es cierta; lo que no cubre la evidencia publicada es el ambito que el propio
  paso se atribuye, y que enuncia de forma literal: «El ambito es "los archivos que el commit toca",
  y esa palabra es el paso entero».
- **Por que importa:** los dos contrastes que `D-125` acaba de anadir se derivan de
  `git diff --cached`, es decir del mismo staging incompleto, asi que **no pueden detectar esta
  brecha**: la tabla y sus dos contrastes cuadran entre si estando los tres cortos. Y el archivo que
  queda fuera con mas riesgo es `_persistence/progress.md`, que el cierre reescribe entero y que
  contiene bloques con patrones `\b` — el origen exacto del `0x08` que este paso persigue. Un
  caracter de control introducido ahi atravesaria el cierre completo sin que ningun control lo viera.
- **Que lo corregiria:** repetir el barrido y sus dos contrastes **despues** del commit, anclados
  (`git diff --name-only --diff-filter=d <hash>^ <hash>`), y publicar esa segunda salida en la NOTA
  DE CIERRE junto al CENSO y al CONTROL, que es donde ya se publica lo que solo existe despues del
  commit. Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-083 | **Implementado** en `9b3f9ee` | La nota fechada existe en `_audit/S-029.md` con la orden anclada a `c1fb41e` y su salida real (`37` / `27`), sin reescribir la prosa anterior; reejecutado aqui, `... \| sort \| uniq -d \| wc -l` devuelve `10`, los diez pares que la nota afirma. Y el Paso 2d ya no publica la cifra de ordenes distintas: los dos `grep -c` de su criterio de cierre devuelven `1` |
| F-084 | **Implementado** en `9b3f9ee` | Nota fechada en la seccion 8 de `_audit/S-029.md` con el barrido anclado sobre los once archivos, y los dos contrastes obligatorios escritos en el Paso 2e: `grep -c 'Los dos contrastes de la tabla'` → `1` y `grep -c 'DOS CONTRASTES de la tabla'` → `1`. La brecha de ambito que queda **no** es este hallazgo: es `F-090`, con evidencia nueva |
| F-085 | **Implementado** en `9b3f9ee` | `_templates/020_baseline/045_traceability.md` pasa de `SC-007` a `SC-004`, y sobre la union de los seis agnosticos las dos series salen completas desde el primero: `FT-001 FT-002 FT-003 FT-004 SC-001 SC-002 SC-003 SC-004` |
| F-086 | **Implementado** en `9b3f9ee` | El Paso 7c-bis exige publicar la salida del control (`grep -c 'Su orden y su salida se publican en la NOTA DE CIERRE'` → `1`) y la plantilla de la seccion 7 la pide (`grep -c 'la salida del CONTROL DE PROSA BORRADA del Paso 7c-bis'` → `1`). Ademas el propio informe la publica entera, con sus dos lineas `== ... ==` |

Ningun otro `F-NNN` quedaba sin cerrar antes de esta pasada.

## 4. Lo que NO se pudo comprobar

- **Que devolvieron las ordenes en el entorno del cierre**, para los dos recuentos escritos a mano
  (`F-087`, `F-088`). Solo es comprobable que lo publicado no reproduce, que es lo unico que los dos
  hallazgos afirman.
- Ningun control mecanico quedo sin correr: los tres (a, b, c) se ejecutaron enteros sobre el commit.

## 5. Recomendaciones sin hallazgo

- El informe escribe «1 hunch» donde quiere decir «1 hunk» (seccion 1,
  `_templates/000_preproject/005_project.md`). No ensucia ninguna evidencia, pero en un registro que
  se recorre con `grep` conviene que el vocabulario sea uno solo.
- `T-038` —igualar el barrido de fuga de `protocol-audit` con el de `protocol-close`— sigue abierta y
  se nota: esta auditoria tuvo que correr el barrido dos veces, sobre cuatro rutas segun
  `protocol-audit` y sobre seis segun `CLAUDE.md`, para poder afirmar algo sobre los seis. Las dos
  salieron `exit=1`, asi que no hay hallazgo; lo que hay es un protocolo que pide menos de lo que el
  proyecto exige.
- La plantilla del acta (`_templates/phase_exit_record.md`), el agente y la skill
  `protocol-phase-exit` son agnosticos y consistentes con `project.md` en donde vive el acta. Lo que
  no existe todavia es su primer uso real: `A-016` esta `Confirmado` sobre dos comprobaciones
  manuales, no sobre una ejecucion. El propio informe lo propone como siguiente paso, y hasta que
  ocurra el sexto agente sigue siendo andamio sin probar.
