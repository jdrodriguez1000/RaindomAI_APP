# Auditoria R-017 — sobre S-017

| Campo | Valor |
|---|---|
| Auditoria | R-017 |
| Informe auditado | `_audit/S-017.md` |
| Commit auditado | `1988d2f` |
| Fecha | 2026-09-03 |
| Veredicto | Con hallazgos (5) |

## 1. Que se comprobo

**a) Anclaje del informe al commit** — el informe existe en un commit, la auditoria es reproducible.

```
$ git log -1 --format=%h -- _audit/S-017.md
1988d2f

$ git show --stat 1988d2f | tail -20
 .claude/skills/protocol-close/SKILL.md         |  27 ++-
 _audit/S-016.md                                |  25 +++
 _audit/S-017.md                                | 287 +++++++++++++++++++++++++
 _audit/findings.md                             |  11 +-
 _audit/index.md                                |   1 +
 _persistence/decisions.md                      | 102 +++++++++
 _persistence/lessons.md                        |  93 ++++++++
 _persistence/progress.md                       | 144 +++++++------
 _persistence/tasks.md                          |  56 +++++
 _phases/020_baseline.md                        |   2 +-
 _templates/020_baseline/005_scope.md           | 247 +++++++++++++++++++++
 _templates/020_baseline/010_product.md         | 240 +++++++++++++++++++++
 _templates/020_baseline/015_features.md        | 216 +++++++++++++++++++
 _templates/020_baseline/020_scenarios.md       | 225 +++++++++++++++++++
 _templates/020_baseline/025_specification.md   | 263 ++++++++++++++++++++++
 _templates/020_baseline/030_architecture.md    | 280 ++++++++++++++++++++++++
 _templates/020_baseline/035_adr_NNN.md         | 215 ++++++++++++++++++
 _templates/020_baseline/040_three_questions.md | 241 +++++++++++++++++++++
 _templates/020_baseline/045_traceability.md    | 224 +++++++++++++++++++
 19 files changed, 2833 insertions(+), 66 deletions(-)
```

La lista de la seccion 1 del informe declara 17 archivos y dice expresamente que `_audit/S-017.md` y
`_audit/index.md` entran ademas por encima: 17 + 2 = 19. **Cuadra.**

**b) Indice ↔ detalle en `_persistence/` (siete archivos)** — limpio, sin salida en ninguno.

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show 1988d2f:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 1988d2f:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

**c) Carpetas del arbol contra las declaradas en `project.md`** — dos diferencias, **las dos con
motivo escrito** en `project.md` (`temporal/`, excluida por `.gitignore`; `010_prototype/`, declarada
por adelantado, `D-061`). No es hallazgo.

```
$ diff <(git ls-tree -d --name-only 1988d2f | sed 's|$|/|' | sort) \
       <(git show 1988d2f:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

**d) Fuga de datos propios en lo agnostico** — cero lineas, y ampliado a los seis ambitos que
`project.md` declara agnosticos, no solo a los cuatro del protocolo.

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" 1988d2f -- .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
exit=1
```

**e) Reejecucion de las ordenes que el informe publica en su seccion 7** — las 13 reproducen contra
el commit. Las que sostienen `D-073`, `D-074` y `T-059`:

```
$ git ls-tree --name-only 1988d2f _templates/020_baseline/
_templates/020_baseline/005_scope.md
_templates/020_baseline/010_product.md
_templates/020_baseline/015_features.md
_templates/020_baseline/020_scenarios.md
_templates/020_baseline/025_specification.md
_templates/020_baseline/030_architecture.md
_templates/020_baseline/035_adr_NNN.md
_templates/020_baseline/040_three_questions.md
_templates/020_baseline/045_traceability.md

$ git show 1988d2f:_phases/020_baseline.md | sed -n '287p'
Nueve artefactos de registro, mas el esqueleto del repositorio. **Ninguno es codigo de producto.**

$ git show 1988d2f:_phases/020_baseline.md | sed -n '/^## 5. Artefactos que produce/,/^Y \*\*el esqueleto/p' | grep -c "^| \*\*"
9

$ git show 1988d2f:_phases/020_baseline.md | grep -nEi "\bocho\b" ; echo "exit=$?"
exit=1

$ git diff -U0 bd8a9ff^ bd8a9ff -- _persistence _audit ":(exclude)_audit/S-016.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
21

$ git diff -U0 bd8a9ff^ bd8a9ff -- _persistence _audit ":(exclude)_audit/S-016.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq | wc -l
15

$ git show 1988d2f:_audit/S-016.md | grep -c 'Nota del 2026-09-03 (`T-059`, hallazgo `F-039`)'
1

$ git show 1988d2f:.claude/skills/protocol-close/SKILL.md | grep -c "El recuento que se publica es el de lineas que devolvio la orden"
1

$ git show 1988d2f:.claude/skills/protocol-close/SKILL.md | grep -c "sin deduplicar"
1

$ git show 1988d2f:.claude/skills/protocol-close/SKILL.md | grep -c "la orden se publica en la forma que reproduce contra el commit"
1
```

**f) La propia seccion 7 del informe, contra la regla nueva que esa misma sesion escribio** — la
orden anclada devuelve exactamente las 13 lineas publicadas, y el recuento se nombra por separado
(«lineas devueltas» / «ordenes distintas»). **La sesion cumple la regla que acaba de escribir.**

```
$ git diff -U0 1988d2f^ 1988d2f -- _persistence _audit ":(exclude)_audit/S-017.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
13
```

**g) Barrido de caracteres de control sobre TODO el arbol del commit** — el del informe se limitaba a
`decisions.md` y `tasks.md`; su resultado se sostiene, y su ambito resulta suficiente de hecho: no
hay ninguna aparicion fuera de esos dos archivos.

```
$ python - <<'PYEOF'
import subprocess,re
files=subprocess.run(['git','ls-tree','-r','--name-only','1988d2f'],capture_output=True,text=True).stdout.split('\n')
pat=re.compile('[\x00-\x08\x0b\x0c\x0e-\x1f]')
for f in files:
    if not f.strip(): continue
    b=subprocess.run(['git','show','1988d2f:'+f],capture_output=True).stdout
    try: t=b.decode('utf-8')
    except: continue
    v=[(i,len(pat.findall(l))) for i,l in enumerate(t.split('\n'),1) if pat.findall(l)]
    if v: print(f,v,'apariciones=',sum(c for _,c in v))
PYEOF
_persistence/decisions.md [(917, 2), (1304, 2)] apariciones= 4
_persistence/tasks.md [(1212, 2), (1931, 10), (1987, 2)] apariciones= 14
```

18 apariciones en 5 lineas: exactamente lo que declara el informe.

**h) Los tres puntos que el informe pide auditar en su seccion 6:**

**h.1 — La lectura del §4 de `_phases/020_baseline.md` en que se apoya `D-073`.** Contrastada contra
el texto real, no contra el resumen del informe. **Se sostiene**, y la correspondencia Paso ↔
plantilla es completa:

```
$ git show 1988d2f:_phases/020_baseline.md | grep -nE "^### Paso"
144:### Paso 1 — Leer el dictamen y lo que dejo abierto
152:### Paso 2 — Definir el alcance del MVP, empezando por el «no»
184:### Paso 3 — Declarar los codigos de producto antes de escribir el primero
199:### Paso 4 — El documento de producto
208:### Paso 5 — El comportamiento esperado: features y escenarios
222:### Paso 6 — La especificacion
230:### Paso 7 — La arquitectura: solo la Base
242:### Paso 8 — Registrar como decision arquitectonica lo que sea caro de revertir
257:### Paso 9 — ⭐ Declarar las tres preguntas
273:### Paso 10 — Montar la trazabilidad, y comprobarla en los dos sentidos

$ for f in 005_scope 010_product 015_features 020_scenarios 025_specification 030_architecture 035_adr_NNN 040_three_questions 045_traceability; do printf "%s :: " "$f"; git show 1988d2f:_templates/020_baseline/$f.md | grep -m1 -oE "Paso [0-9]+"; done
005_scope :: Paso 2
010_product :: Paso 4
015_features :: Paso 5
020_scenarios :: Paso 5
025_specification :: Paso 6
030_architecture :: Paso 7
035_adr_NNN :: Paso 8
040_three_questions :: Paso 9
045_traceability :: Paso 10
```

**h.2 — La clasificacion «reversible a criterio» de `D-073` y `D-074`.** **No es una frase
reciclada:** cada entrada la justifica con su propio objeto («archivos nuevos en una carpeta
agnostica, sin etapa adoptada, sin artefacto escrito todavia contra ellos y sin nada que dependa de
sus nombres» frente a «es una palabra en un archivo de metodo, sin etapa adoptada y sin ningun
artefacto escrito todavia contra el»). Comparten el nucleo —ningun artefacto depende aun— porque de
hecho es el mismo hecho, verificable: la etapa no esta declarada en `project.md` y su carpeta de
entregables no existe. **Sin hallazgo.** Que `T-037` siga sin escribirse ya es tarea abierta y no se
reabre aqui.

**h.3 — El hueco de registro de las cinco apariciones antiguas de `\x08`.** Es un hallazgo real:
`F-042`, en la seccion 2.

**i) Lo que el cierre escribio en `findings.md`, contra lo que le esta permitido escribir** — limpio.
Los `Implementado` que hay en el archivo los puso `R-016` (`b990e05`); el cierre de `S-017` solo movio
`F-039` de `Abierto` a `Aceptado — pendiente` con su `T-059`, que es exactamente lo que le
corresponde.

```
$ git diff b990e05 1988d2f -- _audit/findings.md | grep -E '^[-+]\| Estado|^[-+]\| Registrado'
-| Estado | Abierto |
-| Registrado en | |
+| Estado | Aceptado — pendiente |
+| Registrado en | `T-059` |
```

**j) La lista de `No implementada` de la seccion 2 del informe** — reproduce: seis filas, las mismas
seis que publica.

```
$ git show 1988d2f:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -cE '\| No implementada \|'
6
```

## 2. Hallazgos

### F-040 — Las plantillas nuevas usan los codigos `FT-` y `SC-`, que `project.md` declara «propuesta, no declarada»

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_templates/020_baseline/015_features.md`, `020_scenarios.md`, `025_specification.md`, `045_traceability.md`; `project.md` §Codigos; `D-073` |

- **Que se observo:** cuatro de las nueve plantillas que nacen en este commit usan `FT-XXX` (feature)
  y `SC-XXX` (scenario) como codigos en sus ejemplos. `project.md` los nombra **solo como propuesta
  de la guia de metodo**, no como codigos declarados, y dice literalmente que un codigo que aparece
  en un archivo antes que en su tabla es un desfase. Ninguna `D-XXX` de este commit los declara.

~~~
$ git grep -nEo '\b(FT|SC|VS|TC|ADR)-[0-9]+' 1988d2f -- _templates/ | sed 's/^1988d2f://' | awk -F: '{print $1" "$3}' | sort | uniq -c
      5 _templates/020_baseline/015_features.md FT-001
      2 _templates/020_baseline/015_features.md FT-002
      1 _templates/020_baseline/015_features.md FT-003
      1 _templates/020_baseline/015_features.md SC-001
      1 _templates/020_baseline/015_features.md SC-002
      1 _templates/020_baseline/020_scenarios.md FT-001
      3 _templates/020_baseline/020_scenarios.md SC-001
      2 _templates/020_baseline/020_scenarios.md SC-002
      1 _templates/020_baseline/020_scenarios.md SC-003
      1 _templates/020_baseline/025_specification.md FT-001
      1 _templates/020_baseline/025_specification.md SC-003
      1 _templates/020_baseline/045_traceability.md FT-001
      1 _templates/020_baseline/045_traceability.md FT-004
      1 _templates/020_baseline/045_traceability.md SC-001
      1 _templates/020_baseline/045_traceability.md SC-007

$ git show 1988d2f:project.md | sed -n '245,249p'
📌 **Hay una propuesta escrita, y esta en `_methodology/000_method.md` (§46):** `N-`
necesidad, `FT-` feature, `SC-` scenario, `VS-` vertical slice, `T-` task, `TC-` caso de prueba,
`ADR-` decision arquitectonica. **Propuesta, no declarada:** de esos, `N-` ya entro por `D-034`
—porque el archivo de etapa de `005_discovery` lo cita y un codigo citado sin declarar es un
desfase—; los demas no entran hasta que haya producto y su `D-XXX`.

$ git grep -nEo '\b(FT|SC|VS|TC|ADR)-[0-9]+' 1988d2f | sed 's/^1988d2f://' | awk -F: '{print $1}' | sort | uniq -c
      5 _audit/R-006.md
     15 _methodology/000_method.md
     11 _methodology/sources/005_vertical.md
     10 _templates/020_baseline/015_features.md
      7 _templates/020_baseline/020_scenarios.md
      2 _templates/020_baseline/025_specification.md
      4 _templates/020_baseline/045_traceability.md
~~~

- **Por que importa:** es exactamente el caso que ya produjo `D-034` (`N-`) y `D-038` (`I-`) —«la
  plantilla lo cita, y un codigo citado antes de declararse es un desfase»—, repetido aqui sin su
  `D-XXX`. Y el desfase **nace en este commit**: fuera de `_methodology/` (que es propuesta) y de
  `_audit/R-006.md` (que discute la propuesta), `FT-`/`SC-` solo aparecen en estas cuatro plantillas.
  `_phases/020_baseline.md`, escrito en la sesion anterior, no los citaba.
- **Que lo corregiria:** o declarar `FT-` y `SC-` en la tabla de codigos de `project.md` con su
  `D-XXX`, siguiendo el precedente de `D-034`/`D-038`; o escribir las plantillas con el hueco
  generico en vez del codigo instanciado. ⚠️ Es una recomendacion, no una orden.

### F-041 — `T-057` afirma que las plantillas no llevan codigos instanciados «mas alla del primero», y el commit lo desmiente

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md`, parrafo «En que punto quedo (S-017)» de `T-057`; bloque de verificacion de `D-073` |

- **Que se observo:** el parrafo nuevo de `T-057` afirma que las nueve plantillas quedan «sin codigos
  instanciados del registro mas alla del primero que la convencion de `_templates/` permite
  (`N-001`)». El bloque de verificacion de `D-073` publica la salida que lo respalda, y esa salida es
  **ciega a `FT-` y `SC-` por construccion**: el patron `(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]{3}` con
  limite de palabra no reconoce `FT-001` —la `F` no va seguida de `-`, y la `T` no tiene limite de
  palabra por delante—. Con el patron ampliado aparecen `FT-004`, `SC-003` y `SC-007`, que no son
  «el primero».

~~~
$ git show 1988d2f:_persistence/tasks.md | grep -n 'mas alla del primero'
2544:  proyecto y sin codigos instanciados del registro mas alla del primero que la convencion de

$ git grep -nEo '\b(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]{3}\b' 1988d2f -- _templates/020_baseline/ | sed 's/^1988d2f://'
_templates/020_baseline/015_features.md:189:N-001
_templates/020_baseline/045_traceability.md:199:N-001

$ git grep -nEo '\b(FT|SC)-[0-9]{3}\b' 1988d2f -- _templates/020_baseline/ | sed 's/^1988d2f://' | grep -vE '(FT|SC)-00[12]'
_templates/020_baseline/020_scenarios.md:213:SC-003
_templates/020_baseline/025_specification.md:170:SC-003
_templates/020_baseline/045_traceability.md:206:FT-004
_templates/020_baseline/045_traceability.md:215:SC-007
~~~

- **Por que importa:** el resultado se publica con su comando —que es lo correcto—, pero **el ambito
  del comando no cubre lo que la frase afirma**, y la frase se lee como un veredicto de conformidad
  de toda la carpeta. Es la familia de `F-018` y de `L-021`: un barrido que devuelve poco porque no
  mira donde esta la cosa. Quien lea `T-057` manana dara la carpeta por limpia sin volver a mirarla,
  que es justo cuando el defecto de `F-040` deja de verse.
- **Que lo corregiria:** una nota fechada al lado del bloque de `D-073` y del parrafo de `T-057` con
  el patron ampliado y su salida real, sin reescribir el bloque original; y ampliar ese control a los
  prefijos de dos letras. ⚠️ Es una recomendacion, no una orden.

### F-042 — Las cinco apariciones de `\x08` quedan sin `T-XXX` ni `DT-XXX`, y la decision se delega en un actor que no decide

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/lessons.md`, `L-024`; `_persistence/tasks.md`; `_persistence/techdebt.md` |

- **Que se observo:** `L-024` deja cinco lineas del registro con el caracter `0x08` dentro de ordenes
  publicadas —ordenes que, por tanto, no se reejecutan tal como estan escritas— y remite la decision
  a «una auditoria, no `manager`». No hay ninguna tarea ni deuda tecnica que lo sostenga: el pendiente
  vive solo como parrafo dentro de una leccion.

~~~
$ git grep -n "L-024" 1988d2f -- _persistence/tasks.md _persistence/techdebt.md ; echo "exit=$?"
exit=1

$ git grep -nE "x08|retroceso|caracter de control" 1988d2f -- _persistence/tasks.md _persistence/techdebt.md ; echo "exit=$?"
exit=1

$ git show 1988d2f:_persistence/lessons.md | grep -n "lo decide una auditoria"
888:  como estan, se declaran aqui, y **lo decide una auditoria**, no `manager`.
~~~

- **Por que importa:** por dos motivos, y el segundo es el de fondo. (1) Un pendiente sin `T-XXX` ni
  `DT-XXX` no lo mira nadie: el arranque lee tareas y deuda, no el cuerpo de las lecciones, y en
  cuanto `L-024` deje de ser la ultima escrita el asunto sale del radar — el propio informe lo
  anticipa en su seccion 4 y lo señala en la 6. (2) `project.md` §Reparto de autoridad dice que el
  auditor «no construye, no corrige, no decide»: delegar en el la decision de tocar o no cinco bloques
  antiguos la deja en un actor que por definicion no puede tomarla. Lo que un auditor puede hacer es
  **recomendar** —y esto lo es—, pero mientras tanto el asunto no tiene dueño.
- **Que lo corregiria:** abrir una `DT-XXX` (encaja: se sabe que esta mal y se decide no tocarlo
  ahora, con su motivo) o una `T-XXX` que la cite, y cambiar en `L-024` «lo decide una auditoria» por
  algo que respete el reparto: se registra como deuda y lo decide `manager` con la recomendacion de
  la auditoria delante. **Recomendacion del auditor sobre el fondo, ya que se le pregunta:** no
  reescribir las cinco lineas antiguas —el argumento de `L-024` es correcto—, sino anotarlas con una
  nota fechada al lado de cada bloque que diga que la orden publicada lleva un caracter corrompido y
  cual es su forma reejecutable. ⚠️ Es una recomendacion, no una orden.

### F-043 — `L-024` dice «seis apariciones» donde son seis lineas y veinte apariciones, en la misma entrada

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/lessons.md`, `L-024`, viñeta «Que ocurrio» |

- **Que se observo:** la viñeta afirma que el barrido encontro «**seis** apariciones en el registro»;
  tres parrafos mas abajo, la misma entrada dice que «el total del barrido fue **veinte apariciones
  en seis lineas**». Seis es el numero de **lineas**, no de apariciones.

~~~
$ git show 1988d2f:_persistence/lessons.md | sed -n '845,870p' | grep -nE "seis|veinte"
6:  obtiene otro resultado. El barrido que lo destapo encontro **seis** apariciones en el registro
23:como `(x10)`; el total del barrido fue **veinte apariciones en seis lineas**. Se dice aqui para que
~~~

- **Por que importa:** es literalmente el defecto que `F-039` corrige en este mismo commit —un
  recuento publicado con el nombre de otra magnitud— reproducido dentro de la leccion escrita para
  evitarlo. Gravedad `Baja` porque la cifra correcta esta en la misma entrada dos parrafos mas abajo
  y no induce a ninguna accion equivocada; pero deja el registro diciendo dos numeros distintos para
  lo mismo, y es el numero que un lector rapido se lleva.
- **Que lo corregiria:** cambiar «seis apariciones» por «seis lineas» en la viñeta «Que ocurrio».
  ⚠️ Es una recomendacion, no una orden.

### F-044 — La seccion 1 del informe omite las dos notas de reincidencia añadidas a `L-004` y `L-019`

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-017.md` §1; `_persistence/lessons.md` |

- **Que se observo:** el informe describe `_persistence/lessons.md` unicamente por «`L-024` (nace)».
  El diff muestra que el commit ademas **edita dos entradas existentes**: `L-004` recibe una nota de
  reincidencia con su bloque de verificacion (la de `D-074`), y `L-019` otra con el suyo (la de
  `F-039`). Ninguna de las dos aparece en la seccion 1, ni `L-004` ni `L-019` se nombran en el
  informe.

~~~
$ git diff b990e05 1988d2f -- _persistence/lessons.md | grep -E '^@@'
@@ -32,6 +32,7 @@
@@ -162,6 +163,20 @@ envejeceria por su cuenta.
@@ -667,6 +682,28 @@ ampliarlo a los criterios de cierre es candidato natural si el patron reaparece.
@@ -794,3 +831,59 @@ ampliarlo a los criterios de cierre es candidato natural si el patron reaparece.

$ git diff b990e05 1988d2f -- _persistence/lessons.md | grep -E '^\+> 📌 \*\*Reincidencia'
+> 📌 **Reincidencia del 2026-09-03 (sesion `S-017`).** El defecto volvio, esta vez en
+> 📌 **Reincidencia del 2026-09-03 (`F-039`, `T-059`).** El defecto volvio, esta vez en

$ git show 1988d2f:_audit/S-017.md | grep -nE 'L-004|L-019|Reincidencia' ; echo "exit=$?"
exit=1
~~~

- **Por que importa:** mismo defecto que `F-028` («la lista de la seccion 1 del informe omite dos
  ediciones de `decisions.md`»). Las notas de reincidencia no son cosmetica: son la evidencia de que
  dos lecciones ya registradas volvieron a fallar, que es el dato mas util de este commit para
  decidir si el mecanismo de lecciones funciona. Quien lea el informe sin el diff no sabra que
  existen.
- **Que lo corregiria:** que la seccion 1 nombre tambien las entradas existentes que el commit edita,
  no solo las que nacen. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-037` | **sigue `Aceptado — pendiente`** | La parte que faltaba —`_persistence/progress.md`, el tercer sitio que el hallazgo nombra— **no llego en este commit**. La afirmacion sigue publicada como hecho, sin correccion y sin nota |
| `F-039` | **`Implementado`**, cerrado en `1988d2f` | Las tres correcciones que el hallazgo pedia estan en el diff, reproducen, y la propia seccion 7 del informe ya aplica la regla nueva |

Los demas `F-001` a `F-038` estan `Implementado` desde auditorias anteriores y no se revisan aqui.

**`F-037` — evidencia de que sigue vivo:**

```
$ git show 1988d2f:_persistence/progress.md | sed -n '691,694p'
  `D-069` adopta `NO AUDITABLE` como tercer resultado, ausente en la guia de metodo: se determina en
  una Comprobacion 0 previa a cualquier criterio, resuelta con fechas del historial de `git` — la
  unica comprobacion del metodo imposible de aprobar a posteriori. Cada criterio se resuelve con tres
  valores, `CUMPLE`/`NO CUMPLE`/`NO COMPROBABLE`, tambien ausente en la guia.
```

⚠️ No se abre hallazgo nuevo por esto: **es `F-037`**, que sigue abierto con su `T-055`. Se deja
constancia de que dos commits despues de aceptarlo la parte que faltaba sigue sin corregir, y sin
que la omision este declarada en ningun sitio.

**`F-039` — evidencia de que se cierra:** la nota fechada esta en `_audit/S-016.md` sin reescribir el
bloque original; el Paso 2d y la plantilla de la seccion 7 de `protocol-close` exigen ahora las tres
cosas que el hallazgo pedia; y la seccion 7 de `S-017` ya publica el recuento de lineas y el de
ordenes distintas por separado, con la orden anclada al commit y el informe excluido, y reproduce
(13 = 13). Salidas crudas en la seccion 1, apartados (e) y (f).

## 4. Lo que NO se pudo comprobar

Ninguno. Los tres controles mecanicos del protocolo corrieron completos, y el de fuga se corrio
ademas ampliado a `_templates/` y `_workflow/`.

## 5. Recomendaciones sin hallazgo

- **El barrido de caracteres de control de `L-024` publica su ambito, que son dos archivos** — eso es
  correcto. Se comprobo aparte sobre **todo el arbol** del commit y no aparece ninguna ocurrencia
  fuera de esos dos archivos, asi que el resultado no cambia. Si ese barrido se vuelve rutinario,
  vale la pena correrlo sobre el arbol entero: cuesta lo mismo y cierra la pregunta en vez de
  dejarla acotada.
- **`T-057` sigue `No implementada` con nueve de sus artefactos ya escritos.** Esta bien justificado
  —falta `_workflow/020_baseline.md`— y el informe lo explica. Pero una tarea que cubre dos
  entregables distintos con un solo estado no deja ver que la mitad esta hecha. Partirla haria
  visible el avance real; es una preferencia, no un defecto.
- **El falso positivo del patron de exclusion del Paso 2d** —hashes que van despues de `-U0` y no
  pegados a `git diff`— esta declarado en el informe y se deja sin ajustar, con criterio explicito.
  Ya hay una tarea emparentada abierta (`T-038`); si algun dia se toca ese patron, este es el caso a
  incluir.
