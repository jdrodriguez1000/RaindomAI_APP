# Informe de auditoria — S-020

| Campo | Valor |
|---|---|
| Sesion | S-020 |
| Fecha | 2026-09-04 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-020.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-048 — La seccion 7 de `S-019` publica 42 de las 48 lineas que devuelve su propia orden, y las seis omitidas quedan sin salida | Aceptado — pendiente | `T-072`. Nota fechada en la seccion 7 de `_audit/S-019.md` con las tres cifras reales (48 lineas, 37 ordenes distintas, 11 repetidas), tomadas de `wc -l` / `sort -u \| wc -l` / `sort \| uniq -d \| wc -l`, y la salida de las seis posiciones (43-48) que el informe original no publico. El informe no se reescribe (`D-019`) |
| F-049 — `DT-004` declara siete lineas nuevas con `0x08` donde hay diez, y dos de las no contadas alteran la salida cruda publicada en la correccion de `F-045` | Aceptado — pendiente | `T-073`, `D-081`. Nota fechada en `DT-004` con el barrido sobre el commit (10 lineas en 4 archivos) y contra el padre (para separar lo nuevo de lo heredado). La escribe `manager`, no el cierre: `D-081` abre esa excepcion puntual, acotada a un dato factual, sin tocar `Estado`, `Confirmacion` ni el titulo |
| F-050 — El informe y `progress.md` llaman «las tres etapas nuevas de `025_wslt`» a los tres artefactos de una sola etapa | Aceptado — pendiente | `T-075`. Nota fechada en `_audit/S-019.md` y en la entrada `S-019` de `_persistence/progress.md`, con la orden que deriva los tres archivos del diff. Ni el mensaje del commit ni `D-078` se reescriben, porque ya lo decian bien |

## 1. Que se hizo

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
_audit/S-019.md
_audit/findings.md
_audit/S-020.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_persistence/techdebt.md
_phases/030_growth.md
```

(la lista definitiva, generada con `git show --stat --name-only --format= <commit>` sobre el commit
ya cerrado, se deja pegada en la nota de cierre de esta seccion; el listado de arriba es el mismo
conjunto de archivos, tomado del area de staging porque el commit aun no existia al escribir esta
seccion.)

Que cambia en cada uno:

- **`.claude/skills/protocol-close/SKILL.md`** — nace el **Paso 2e** (barrido de caracteres de
  control sobre los archivos que el commit toca, derivados de `git diff --cached --name-only`) y su
  seccion **8** en la estructura del informe, para publicar ese resultado (`T-074`).
- **`_audit/S-019.md`** — dos notas fechadas nuevas: la de la seccion 7 (`F-048`, `T-072`) y la de
  la seccion 2 (`F-050`, `T-075`). El resto del informe no se toca (`D-019`).
- **`_audit/findings.md`**: `F-048`, `F-049` y `F-050` (nace) — nace: no, estos tres hallazgos ya
  existian desde `R-019`; lo que cambia es su fila en el indice, de `Abierto` a
  `Aceptado — pendiente (T-072)`, `Aceptado — pendiente (T-073 y T-074)` y
  `Aceptado — pendiente (T-075)` respectivamente. Ver seccion 6 sobre un desfase que dejo esta
  edicion.
- **`_audit/S-020.md`** (nace) — este mismo informe.
- **`_audit/index.md`** — nace la fila de `S-020.md`.
- **`_persistence/decisions.md`** — `D-081` (nace: autoriza la excepcion puntual de `manager` sobre
  `DT-004`) y `D-082` (nace: registra `_phases/030_growth.md` como archivo de etapa escrito por
  adelantado, etapa no adoptada).
- **`_persistence/lessons.md`** — `L-028` (nace): el conjunto sobre el que corre un recuento se
  deriva del diff, igual que la lista misma (`L-027`, del que esta no es variante: aquella cubria la
  lista, esta cubre el ambito).
- **`_persistence/progress.md`** — dos notas fechadas en la entrada `S-019` (`F-050`, `T-075`, ya
  contadas en `_audit/S-019.md` arriba) **y** la entrada nueva `S-020` (nace), mas su fila en el
  indice de sesiones y la actualizacion de las secciones 1, 2 y 3.
- **`_persistence/tasks.md`** — `T-072`, `T-073`, `T-074`, `T-075` y `T-076` (nacen las cinco), todas
  `Implementada`.
- **`_persistence/techdebt.md`** — nota fechada en `DT-004` (`F-049`, `T-073`, `D-081`), ampliando su
  ambito de siete lineas en dos archivos a diez en cuatro. `Estado`, `Confirmacion` y el titulo de la
  entrada no cambian.
- **`_phases/030_growth.md`** (nace) — el sexto archivo de `_phases/`, la etapa que hace crecer el
  producto colgando slices de un esqueleto que ya camina. No adopta la etapa (`D-082`).

## 2. Que NO se hizo, y por que

Tareas `No implementada` segun el indice de `_persistence/tasks.md` (`sed -n '/^## Indice/,/^---/p'
_persistence/tasks.md | grep "No implementada"`, filtrando por la columna de estado real y no por
texto de titulo):

- `T-001` — Definir alcance y objetivo del proyecto. Bloqueante, etapa `005_discovery`; depende de
  `A-004` (acceso al patrocinador).
- `T-002` — Declarar las etapas posteriores a `000_preproject`. No bloqueante, etapa
  `005_discovery`.
- `T-003` — Verificar si el historico de la fuente oficial es obtenible (`A-003`). Bloqueante,
  parcialmente probado en `S-011`.
- `T-037` — Escribir el inventario de acciones irreversibles del proyecto (`LG-38`). No bloqueante.
- `T-038` — Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close`. No
  bloqueante.

Ninguna de las tareas de esta sesion (`T-072` a `T-076`) quedo a medias: las cinco estan
`Implementada`.

`_phases/030_growth.md` deja pendientes, por declaracion propia (su §5), dos artefactos que su etapa
necesitara: `_templates/030_growth/` y `_workflow/030_growth.md`. Ninguno de los dos existe todavia.

## 3. Decisiones tomadas

- **`D-081`** — Una nota fechada que corrige un dato factual de `techdebt.md`, respaldada por un
  `F-NNN`, la escribe `manager` y no el cierre — acotado a no tocar `Estado`, `Confirmacion` ni el
  titulo de la entrada. **Alternativas descartadas:** dejar la nota al `session-closer` (no puede
  derivarla del diff: el dato nace de un hallazgo y de un barrido corrido a proposito, y el hallazgo
  quedaria evaluado sin registro hasta la noche); corregir la cifra dentro del texto de `DT-004` en
  vez de anotarla (lo prohibe `D-019`); abrir una `DT-005` nueva en vez de ampliar `DT-004`
  (duplicaria la deuda).
- **`D-082`** — Nace `_phases/030_growth.md`, con las mismas ocho secciones que sus hermanos, sin
  adoptar la etapa. Se conserva la estructura y las dos prohibiciones con recuadro del borrador del
  usuario; se descarta su vocabulario de rutas y codigos. **La desviacion deliberada respecto al
  borrador:** el archivo no instancia ningun codigo de producto, ni siquiera generico, porque la
  guia de metodo asigna a la tarea de producto el mismo prefijo `T-` que `project.md` ya tiene tomado
  por la tarea de la jornada, y esta etapa es la que hace estallar esa colision (su Paso 3 descompone
  slices en tareas). **Alternativas descartadas:** escribir que la tarea de una slice **es** la
  `T-XXX` del registro de jornada (mete una decision de proyecto en un archivo agnostico); estrenar
  un codigo generico nuevo (declarar codigo desde un archivo que no declara codigos, y antes de que
  exista producto); copiar el borrador tal cual y limpiar despues (es lo que el Paso 1b existe para
  atrapar).

## 4. Supuestos vigentes y riesgos

Sin cambios esta sesion. Siguen abiertos: `A-003` (historico de la fuente oficial obtenible),
`A-004` (acceso al patrocinador), `A-005` (parte de `ai_levels.md` sin experiencia propia), `A-006`
(codigos `FT-`/`SC-` declarados), `A-007` (humano disponible para el despliegue del esqueleto). Si
`A-003` resulta falso, el ciclo entero del producto depende de reconsiderar la fuente de datos. Si
`A-004` resulta falso, `005_discovery` no puede abrir. Ninguno se apoya en el trabajo de esta sesion.

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto a partir de `_brief/client_brief.md`, etapa
`005_discovery`. Importancia Alta, Urgencia Bloqueante. Antes de abrirla, resolver `A-004` (acceso al
patrocinador), porque `_phases/005_discovery.md` declara esa entrada como obligatoria.

## 6. Que pedimos auditar

- La fila de `_audit/findings.md` para `F-048`, `F-049` y `F-050` se actualizo en el **indice** a
  `Aceptado — pendiente (T-XXX)`, pero **la tabla de campos de cada entrada de detalle sigue
  diciendo `Estado | Abierto`** y `Registrado en |` vacio. Este informe no lo corrige —`findings.md`
  es del auditor, y `protocol-close` prohibe tocarlo—, pero lo señala porque es un desfase entre el
  indice y el detalle del mismo archivo, la misma clase de cosa que el Paso 2b de este protocolo
  vigila en `_persistence/`.
- La nota de `F-049` en `DT-004` la escribio `manager` bajo la excepcion de `D-081`, no el cierre.
  Es la primera vez que se usa esa excepcion; conviene que la auditoria compruebe que su alcance
  —solo un dato factual, sin tocar `Estado`/`Confirmacion`/titulo— se respeto de verdad y no se usa
  como puerta para escribir mas de lo que `D-081` autoriza.
- `_phases/030_growth.md` es el archivo mas largo de `_phases/` y el primero que declara
  explicitamente que NO instancia ningun codigo de producto por una colision de prefijos con
  `project.md`. Es una decision de diseño con consecuencias para cuando se adopte `020_baseline`
  (que es quien tendria que resolver la colision); vale la pena que la auditoria contraste si el
  razonamiento de `D-082` sobre esa colision es correcto o si hay una alternativa mas simple que no
  se considero.
- La seccion 1 de este informe no pudo pegar `git show --stat` sobre el commit real, porque se
  escribio antes del commit (el propio informe y `_audit/index.md` aun no estaban en el arbol). Se
  pego el equivalente sobre el area de staging; la nota de cierre de esta seccion trae la version
  anclada al hash definitivo. Si las dos listas no coincidieran, manda la anclada.

## 7. Evidencia del Paso 2d

Orden (sobre el area de staging, porque el commit no existe todavia al escribir esta seccion):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
     1	+$ sed -n '/^### DT-004/,/^| Fecha/p' _persistence/techdebt.md | grep -E '^\| (Estado|Confirmacion)'
     2	+$ grep -c '^### DT-004 - Siete lineas nuevas de `S-019` repiten el defecto de `DT-003`, en `decisions.md` y `tasks.md`' _persistence/techdebt.md
     3	+$ grep -c "^## " _phases/030_growth.md
     4	+$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|TripleS|github.com|USUARIO" _phases/030_growth.md ; echo "exit=$?"
     5	+$ PAT=$( { sed -n '/^## Codigos/,/^---/p' project.md | grep -oE '^\| `[A-Z]+-[A-Za-z0-9]+`'; sed -n '/^## 46\. Identificadores/,/^```/p' _methodology/000_method.md | grep -oE '^\| `[A-Z]+-[A-Za-z0-9]+`'; } | tr -d '|` ' | sed 's/-.*//' | sort -u | awk '{print length, $0}' | sort -rn | cut -d' ' -f2 | paste -sd'|' - ) && grep -noE "(^|[^A-Za-z])(${PAT})-[0-9]{2,3}" _phases/030_growth.md ; echo "exit=$?"
     6	+$ grep -c "^### Paso " _phases/030_growth.md
     7	+$ sed -n '/^## 6. Condicion de salida/,/^Si alguna falla/p' _phases/030_growth.md | grep -c "^- \[ \]"
     8	+$ sed -n '/^## 5. Artefactos que produce/,/^⚠️ \*\*Cual es la carpeta/p' _phases/030_growth.md | grep -c "^| \*\*"
     9	+$ sed -n '/^## 8\./,$p' _phases/030_growth.md | grep -c "^### "
    10	+$ ls -1 _phases/
    11	+$ grep -n "Etapas declaradas" project.md
    12	+$ grep -c '^## Paso 2e' .claude/skills/protocol-close/SKILL.md
    13	+$ grep -c '^## 8. Evidencia del Paso 2e' .claude/skills/protocol-close/SKILL.md
    14	+$ git diff --name-only --diff-filter=A 1b30e16^ 1b30e16 | grep 025_wslt
    15	+$ grep -c 'Nota del 2026-09-04 (`T-072`, hallazgo `F-048`)' _audit/S-019.md
    16	+$ git diff -U0 1b30e16^ 1b30e16 -- _persistence _audit ":(exclude)_audit/S-019.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
    17	+$ grep -c 'Nota del 2026-09-04 (`T-073`, hallazgo `F-049`, decision `D-081`)' _persistence/techdebt.md
    18	+$ grep -c '^## Paso 2e' .claude/skills/protocol-close/SKILL.md
    19	+$ grep -c '^## 8. Evidencia del Paso 2e' .claude/skills/protocol-close/SKILL.md
    20	+$ grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]' .claude/skills/protocol-close/SKILL.md
    21	+$ grep -c 'Nota del 2026-09-04 (`T-075`, hallazgo `F-050`)' _audit/S-019.md _persistence/progress.md
    22	+$ git diff --name-only --diff-filter=A 1b30e16^ 1b30e16 | grep 025_wslt
    23	+$ grep -c "^## " _phases/030_growth.md
    24	+$ grep -c "^### Paso " _phases/030_growth.md
    25	+$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|TripleS|github.com|USUARIO" _phases/030_growth.md ; echo "exit=$?"
    26	+$ grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]' _phases/030_growth.md
    27	+$ ls -1 _phases/ | wc -l
```

Recuento de **lineas devueltas**: 27. Recuento de **ordenes distintas** (aparte, con ese nombre):
21. Repetidas (`sort | uniq -d`):

```
+$ git diff --name-only --diff-filter=A 1b30e16^ 1b30e16 | grep 025_wslt
+$ grep -c "^## " _phases/030_growth.md
+$ grep -c "^### Paso " _phases/030_growth.md
+$ grep -c '^## 8. Evidencia del Paso 2e' .claude/skills/protocol-close/SKILL.md
+$ grep -c '^## Paso 2e' .claude/skills/protocol-close/SKILL.md
+$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|TripleS|github.com|USUARIO" _phases/030_growth.md ; echo "exit=$?"
```

Esta orden **no es reproducible por naturaleza**: describe el area de staging (`git diff --cached`),
que deja de existir en cuanto se hace el commit. Su equivalencia anclada, una vez exista el hash de
este cierre, es:

```
git diff -U0 <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-020.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Reejecucion de las 27, una por una, contra el arbol de trabajo actual (equivalente valido mientras
el commit no exista: los mismos archivos, en el mismo contenido que va a quedar commiteado):

```
 1: | Estado | No implementada |
    | Confirmacion | Propuesta (pendiente del usuario) |
 2: 1
 3: 8
 4: exit=1
 5: exit=1
 6: 8
 7: 8
 8: 7
 9: 5
10: 000_preproject.md / 005_discovery.md / 010_prototype.md / 020_baseline.md / 025_wslt.md / 030_growth.md
11: 105:| Etapas declaradas | `000_preproject`, `005_discovery` |
12: 1
13: 1
14: _phases/025_wslt.md / _templates/025_wslt/005_skeleton_record.md / _workflow/025_wslt.md
15: 1
16: 48
17: 1
18: 1
19: 1
20: 0
21: _audit/S-019.md:1 / _persistence/progress.md:1
22: _phases/025_wslt.md / _templates/025_wslt/005_skeleton_record.md / _workflow/025_wslt.md
23: 8
24: 8
25: exit=1
26: 0
27: 6
```

Las 27 reproducen contra lo que sus entradas afirman (`T-072` a `T-076`, `D-081`, `D-082`).

> 📌 **Nota de cierre (post-commit).** Una vez exista el hash de este commit, se pega aqui la salida
> anclada equivalente:
> ```
> git diff -U0 <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-020.md"
> ```
> Si el resultado difiere del bloque de arriba en algo que no sea el ancla del comando, se corrige
> con una nota fechada, nunca reescribiendo el bloque original (`D-019`).

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/S-019.md: 1
_audit/findings.md: 1
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Las cuatro cifras son **heredadas**, no nuevas de esta sesion — comprobado contra `HEAD`:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"
done
.claude/skills/protocol-close/SKILL.md head=0 staged=0
_audit/S-019.md head=1 staged=1
_audit/findings.md head=1 staged=1
_persistence/decisions.md head=7 staged=7
_persistence/lessons.md head=0 staged=0
_persistence/progress.md head=0 staged=0
_persistence/tasks.md head=5 staged=5
_persistence/techdebt.md head=0 staged=0
_phases/030_growth.md head=0 staged=0
```

`head` y `staged` coinciden en los cuatro archivos con cifra distinta de cero: son exactamente los
diez casos que `DT-004` (ampliada por `F-049`/`T-073`) ya documenta como deuda tecnica sin pagar.
Ningun archivo de los que esta sesion escribio de cero (`.claude/skills/protocol-close/SKILL.md`,
`_persistence/lessons.md`, `_persistence/techdebt.md`, `_phases/030_growth.md`) ni de los que edito
(`_persistence/progress.md`, `_persistence/tasks.md` no suma nuevas sobre su base heredada) introduce
un caracter de control nuevo.
