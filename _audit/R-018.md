# Auditoria R-018 — sobre S-018

| Campo | Valor |
|---|---|
| Auditoria | R-018 |
| Informe auditado | `_audit/S-018.md` |
| Commit auditado | `9a52cfa` |
| Fecha | 2026-09-03 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

### a) Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-018.md
9a52cfa

$ git status --porcelain
(sin salida)

$ git status -sb
## main...origin/main
```

El informe esta dentro de un commit y el arbol venia limpio.

### b) Indice ↔ detalle en `_persistence/` — coherente

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show 9a52cfa:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 9a52cfa:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Ninguna diferencia en los siete archivos.

### c) Carpetas del arbol contra las declaradas — las dos diferencias tienen razon escrita

```
$ diff <(git ls-tree -d --name-only 9a52cfa | sed 's|$|/|' | sort) \
       <(git show 9a52cfa:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
exit=1
```

Las dos estan justificadas en `project.md`, seccion «Carpetas propias»: `temporal/` excluida por
`.gitignore`, `010_prototype/` declarada por adelantado por `D-061`. Sin hallazgo.

### d) Fuga de datos propios en lo agnostico — cero lineas

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" 9a52cfa -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" 9a52cfa -- _templates _workflow ; echo "exit=$?"
exit=1
```

Se amplio el barrido a `_templates/` y `_workflow/`, que este commit toca —`_workflow/020_baseline.md`
nace—. Cero lineas en los seis ambitos.

### e) El informe contra el diff

```
$ git show --stat 9a52cfa
 .claude/skills/protocol-close/SKILL.md |  24 +++
 _audit/S-018.md                        | 351 +++++
 _audit/findings.md                     |  77 ++++--
 _audit/index.md                        |   1 +
 _persistence/assumptions.md            |  35 ++++
 _persistence/decisions.md              | 151 ++++++
 _persistence/lessons.md                | 127 +++++-
 _persistence/progress.md               | 172 +++++----
 _persistence/tasks.md                  | 293 ++++++++-
 _persistence/techdebt.md               |  49 +++
 _phases/020_baseline.md                |  10 +-
 _workflow/020_baseline.md              | 310 +++++
 project.md                             |  24 ++-
 13 files changed, 1536 insertions(+), 88 deletions(-)
```

Los trece archivos estan nombrados en la seccion 1 del informe: los once del `git diff --stat` que
publica, mas `_workflow/020_baseline.md` como no versionado en ese momento y `_audit/S-018.md`
declarado aparte. El recuento de archivos cuadra.

Contraste de cada correccion afirmada, sobre el commit:

```
$ git show 9a52cfa:project.md | grep -nE '^\| `(FT|SC)-XXX`'
232:| `FT-XXX` | el artefacto de features de `020_baseline` (ruta por declarar: la etapa no esta adoptada) | feature |
233:| `SC-XXX` | el artefacto de escenarios de `020_baseline` (ruta por declarar: la etapa no esta adoptada) | scenario |

$ git show 9a52cfa:_persistence/decisions.md | grep -nE '^### D-07[56]'
3940:### D-075 - Se declaran `FT-` y `SC-` en la tabla «Codigos», porque las plantillas ya los citan
4003:### D-076 - El reparto de la baseline puntua la variabilidad de la entrada en 2, y por eso no declara discrepancia

$ git show 9a52cfa:_persistence/decisions.md | grep -c 'Nota del 2026-09-03 (`T-062`, hallazgo `F-041`)'
1
$ git show 9a52cfa:_persistence/tasks.md | grep -c 'Nota del 2026-09-03 (`T-062`, hallazgo `F-041`)'
2

$ git show 9a52cfa:_persistence/techdebt.md | grep -nE '^### DT-003'
182:### DT-003 - Cinco lineas del registro publican ordenes con un caracter de control `\x08` y no se reejecutan (`L-024`)

$ git show 9a52cfa:_persistence/lessons.md | grep -nE 'seis lineas|veinte apariciones'
862:  obtiene otro resultado. El barrido que lo destapo encontro **seis lineas** afectadas en el registro
863:  —una de esta sesion, ya reparada, y **cinco anteriores**, que no se tocan—. Son veinte apariciones
864:  del caracter repartidas en esas seis lineas; el recuento por lineas y el recuento por apariciones
881:como `(x10)`; el total del barrido fue **veinte apariciones en seis lineas**. Se dice aqui para que

$ git show 9a52cfa:.claude/skills/protocol-close/SKILL.md | grep -c 'no es todo lo que cambia'
1
```

Todas las correcciones que el informe afirma haber hecho estan en el diff.

Seccion 2 del informe, «Que NO se hizo», reproducida sobre el commit:

```
$ git show 9a52cfa:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | awk -F'|' '{gsub(/^ +| +$/,"",$4); if($4=="No implementada") print $2}'
 [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto)
 [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject)
 [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003)
 [T-037](#t-037---escribir-el-inventario-de-acciones-irreversibles-del-proyecto-lg-38)
 [T-038](#t-038---igualar-el-barrido-de-fuga-de-protocol-audit-con-el-de-protocol-close)
```

Las cinco coinciden con lo publicado. Sin hallazgo.

### f) Reejecucion de las salidas de la seccion 7

Se reejecutaron las ordenes cuya salida el informe publica. Todas reproducen:

```
$ grep -rnoE "\b(VS|TC|ADR)-[0-9]{3}\b" _templates/ _phases/ _workflow/ _persistence/ _audit/ ; echo "exit=$?"
exit=1

$ grep -l "discrepancia con la tabla de lectura" _workflow/005_discovery.md _workflow/010_prototype.md _workflow/020_baseline.md
_workflow/005_discovery.md
_workflow/010_prototype.md

$ grep -cE "^\| \*\*([1-9]|10) · " _workflow/020_baseline.md
10

$ grep -c "^### Paso " _phases/020_baseline.md
10

$ python -c "import io,re; pat=re.compile(u'[\x00-\x08\x0b\x0c\x0e-\x1f]'); [print(f,i,repr(m.group())) for f in ['_persistence/decisions.md','_persistence/tasks.md'] for i,l in enumerate(io.open(f,encoding='utf-8'),1) for m in pat.finditer(l)]" | awk '{print $1, $2}' | uniq -c
      2 _persistence/decisions.md 919
      2 _persistence/decisions.md 1306
      2 _persistence/tasks.md 1218
     10 _persistence/tasks.md 1937
      2 _persistence/tasks.md 1993
```

Y la orden que el informe no llega a publicar —ver `F-045`— tambien reproduce:

```
$ grep -c 'Nota del 2026-09-03 (`T-060`, hallazgo `F-037`)' _persistence/progress.md
1
```

Lo que no reproduce es el recuento de ese bloque, ni su afirmacion de que no hay ordenes repetidas:
es `F-045`.

### g) Lo que la seccion 6 del informe pidio auditar

- **Cobertura del patron ampliado (`L-025`/`T-062`).** Comprobado: falta el prefijo `H-`. Es `F-047`.
- **Las tres notas fechadas acumuladas sobre `T-057`.** Las tres existen y ninguna contradice a otra
  ni al cuerpo original. No hay defecto. Que falte una regla escrita sobre cuando una entrada deja de
  ser legible por acumulacion es una mejora, no un defecto: va a la seccion 5.
- **`A-006` como supuesto sobre una decision de la misma sesion.** No se sostiene la sospecha de
  blanqueo. `A-006` declara su refutacion en tres formas concretas, su disparador —`T-002` o el
  Paso 3 de la baseline, lo que llegue primero— y dice expresamente que la retirada se registra con
  su `D-XXX` citando `D-075`. `D-075`, por su parte, publica tres alternativas descartadas y no se
  apoya en el supuesto para sostenerse: el supuesto acota su alcance, no lo justifica. Sin hallazgo.

## 2. Hallazgos

### F-045 - La seccion 7 declara 28 ordenes donde son 26, afirma que no hay repetidas cuando hay tres, y deja una sin salida publicada
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-018.md`, seccion 7 |

- **Que se observo:** el informe dice «**Recuento: 28 lineas devueltas** (28 ordenes distintas
  citadas; no hay repeticion de una misma orden en dos archivos esta sesion, asi que las dos cifras
  coinciden)». Su propia orden, anclada al commit, devuelve 26 lineas y 23 ordenes distintas, con
  tres repetidas.

~~~
$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
26

$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
23

$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
+$ grep -nE '^\| `(FT|SC)-XXX`' project.md
+$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
+$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
~~~

  La lista que el propio informe imprime debajo tiene 26 lineas, no 28: el recuento no cuadra ni con
  el comando ni con lo que el informe publica. Y la numeracion se corrio en una posicion — el informe
  marca «(=15, =16)» sobre la primera orden, cuando la posicion 15 de su lista es
  `grep -c 'Nota del 2026-09-03 (T-060, hallazgo F-037)' _persistence/progress.md`, que no es
  duplicada de nada. Consecuencia: esa orden queda sin salida publicada —la lista de reejecucion
  salta de `--14--` a `--19--`— mientras el informe afirma que «ninguna de las 28 lineas fallo al
  reejecutarse».

~~~
$ git show 9a52cfa:_audit/S-018.md | grep -nE '^--(1[3-9])--'
299:--13--
302:--14--
305:--19--
~~~

- **Por que importa:** el Paso 2d existe para que el recuento sea contrastable, y es exactamente la
  pieza que `F-039` (`R-016`) y `T-059` (`S-017`) acaban de reparar. La cifra publicada es el unico
  numero que un lector puede comparar sin rehacer el barrido; si es falsa, el control no controla
  nada. Ademas, una orden marcada como duplicada de otra que no lo es desaparece de la verificacion
  sin que se note: nadie la reejecuta, y el informe declara que todas reprodujeron.
- **Que lo corregiria:** publicar el recuento que devuelve la orden anclada al commit —lineas y
  ordenes distintas por separado, con las repetidas nombradas— y dar salida propia a la orden de la
  posicion 15. ⚠️ Es una recomendacion, no una orden.

### F-046 - La seccion 1 atribuye a `L-020` la nota de reincidencia que el commit pone en `L-019`
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-018.md`, seccion 1, viñeta de `_persistence/lessons.md` |

- **Que se observo:** el informe dice «nota de reincidencia añadida a `L-020` (la entrada de
  `D-069`/reincidencia numero dos, `F-041`)». El commit no toca `L-020`: la nota cae dentro de
  `L-019`.

~~~
$ git show 9a52cfa:_persistence/lessons.md | grep -nE '^### L-0(19|20)'
657:### L-019 - Un control documentado sobre una parte de su propia salida no es el control
721:### L-020 - Una regla que dice QUE registrar pero no DONDE no se incumple: se evapora

$ git diff -U0 9a52cfa^ 9a52cfa -- _persistence/lessons.md | grep -E '^@@'
@@ -35,0 +36,2 @@
@@ -706,0 +709,10 @@ ampliarlo a los criterios de cierre es candidato natural si el patron reaparece.
@@ -850,2 +862,4 @@ ampliarlo a los criterios de cierre es candidato natural si el patron reaparece.
@@ -885 +899 @@ anteriores. Antes de reparar la de hoy eran veinte en seis.
@@ -888 +902,9 @@ anteriores. Antes de reparar la de hoy eran veinte en seis.
@@ -889,0 +912,97 @@ anteriores. Antes de reparar la de hoy eran veinte en seis.

$ git show 9a52cfa:_persistence/lessons.md | sed -n '709,712p'
> 🔁 **Reincidencia numero dos, del 2026-09-03 (`T-062`, hallazgo `F-041`).** Tercera vez que esta
> leccion se cumple, y la primera con una causa **mecanica** en vez de editorial. En `F-035` el
> control cubria parte de su salida por como se redacto; en `F-039`, por deduplicarla. Aqui el
> bloque publica su salida **entera y sin tocar** —no hay recorte, no hay deduplicacion— y aun asi
~~~

  El hunk `+709,10` cae entre `L-019` (linea 657) y `L-020` (linea 721), y el texto de la nota cita
  `F-035` y `F-039`, que son los dos casos previos de `L-019`. El parentesis «la entrada de `D-069`»
  tampoco corresponde a ninguna de las dos entradas.

- **Por que importa:** es el mismo defecto que `F-028` y `F-044`, y aparece en el commit que endurece
  la plantilla justo para eso — el ejemplo que ese endurecimiento escribe dentro de `protocol-close`
  es literalmente «`lessons.md`: `L-024` (nace), `L-004` y `L-019` (nota de reincidencia)». Una
  atribucion equivocada es peor que la omision que corrige: quien busque la reincidencia abrira
  `L-020`, no encontrara nada, y concluira que el informe exagera cuando lo que hay es un codigo mal
  escrito.
- **Que lo corregiria:** derivar la lista de entradas editadas del diff, como la propia regla nueva
  del skill indica (`git diff <commit>^ <commit> -- <archivo>`), en vez de escribirla a mano.
  ⚠️ Es una recomendacion, no una orden.

### F-047 - El patron ampliado que `T-062` adopta «de aqui en adelante» sigue ciego al prefijo `H-`
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/decisions.md` (nota fechada de `D-073`), `_persistence/tasks.md` (`T-057`, `T-062`), `_persistence/lessons.md` (`L-025`) |

- **Que se observo:** la nota fechada de `D-073` cierra con «De aqui en adelante este control se corre
  con el patron ampliado». Entre las dos listas publicadas —`\b(N|T|D|A|C|I|F|L|S|R|DT)-` y
  `\b(FT|SC|VS|TC|ADR)-`— queda fuera un prefijo que `project.md` si declara: `H-`, el hito.

~~~
$ git show 9a52cfa:project.md | sed -n '/^## Codigos/,/^---/p' | grep -oE '^\| `[A-Z]+-[A-Za-z]+`' | tr -d '|` ' | sed 's/-.*//' | sort -u | tr '\n' ' '
A C D DT F FT H I L N R S SC T
~~~

  Union de las dos listas: `N T D A C I F L S R DT FT SC VS TC ADR`. Diferencia contra lo declarado:
  `H`. Ademas el cuantificador `[0-9]{3}` no casaria con la forma que `project.md` declara para ese
  codigo —`H-nn`, dos digitos—, asi que el punto ciego es doble.
- **Por que importa:** `F-041` se abrio precisamente porque el ambito del patron era mas estrecho que
  la frase que respaldaba. El patron sucesor se declara vigente «de aqui en adelante» sin que su
  cobertura se haya contrastado contra la tabla «Codigos», que es el unico sitio donde esa lista se
  puede cerrar. El dia que una plantilla escriba `H-02`, el control devolvera cero y la frase volvera
  a decir mas de lo que prueba — esta vez sin nota al lado.
- **Que lo corregiria:** derivar la alternancia de la tabla «Codigos» de `project.md` en vez de
  escribirla a mano, o dejar escrito en la nota que `H-` queda deliberadamente fuera y por que.
  ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-037` | **Implementado** (`9a52cfa`) | El tercer sitio —la bitacora de `S-015` en `_persistence/progress.md`— recibe la nota fechada que faltaba, cita `F-037` y `D-071`, y su bloque reproduce. Los otros dos sitios ya se habian corregido en `bd8a9ff` |
| `F-040` | **Implementado** (`9a52cfa`) | `FT-XXX` y `SC-XXX` estan declarados en la tabla «Codigos» de `project.md` (lineas 232-233), con su `D-075` y el aviso que separa declarar de adoptar |
| `F-041` | **Implementado** (`9a52cfa`) | Las dos notas fechadas existen (`decisions.md`:1, `tasks.md`:2), publican el patron ampliado y su salida real, y ninguno de los dos bloques originales se reescribio. El punto ciego residual del patron sucesor se abre aparte como `F-047`; no reabre este |
| `F-042` | **Implementado** (`9a52cfa`) | `DT-003` existe con su fila en el indice y estado `No implementada` / `Propuesta (pendiente del usuario)`; la frase que delegaba la decision en `report_auditor` ya no esta (`grep -c 'lo decide una auditoria'` devuelve 0) y `L-024` cita `DT-003` |
| `F-043` | **Implementado** (`9a52cfa`) | `L-024` distingue ahora «seis lineas» de «veinte apariciones» en las dos frases que tenian el defecto; `grep -cE '\*\*seis\*\* apariciones\|\*\*cinco apariciones'` devuelve 0 |
| `F-044` | **Implementado** (`9a52cfa`) | La plantilla del Paso 6b de `protocol-close` incorpora la seccion «lo que **nace** no es todo lo que cambia» y dos huecos nuevos en la estructura del informe. ⚠️ El nuevo `F-046` es un fallo de aplicacion de esa regla en esta misma sesion, no un defecto de la regla |

Las salidas crudas de estas seis verificaciones estan en la seccion 1e.

## 4. Lo que NO se pudo comprobar

Ninguno de los controles fallo. Los tres mecanicos y las reejecuciones de la seccion 7 corrieron
completos sobre el commit `9a52cfa`.

## 5. Recomendaciones sin hallazgo

- **Cuando una entrada acumula demasiadas notas fechadas.** La seccion 6 del informe lo plantea y
  tiene razon en que no hay regla escrita: `T-057` lleva ya tres parrafos de nota sobre su cuerpo.
  Ninguna contradice a otra ni al cuerpo, asi que hoy no hay defecto; pero conviene un umbral escrito
  —o un criterio de sucesion por `T-XXX` nueva— antes de que el problema aparezca en una entrada que
  si haya que leer de un vistazo.
- **Correr el Paso 2d ya anclado al commit.** El informe declara honestamente que reejecuto contra el
  arbol de trabajo y publica la equivalencia anclada. Hacerlo directamente sobre el hash, una vez
  existe, evitaria justo la clase de desajuste que produjo `F-045`.
