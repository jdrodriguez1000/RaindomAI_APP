# Informe de auditoria — S-005

| Campo | Valor |
|---|---|
| Sesion | S-005 |
| Fecha | 2026-09-01 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | ver `git log -1 -- _audit/S-005.md` (se rellena tras el commit de este cierre) |

## 0. Respuesta a la auditoria anterior

`R-004` (sobre `S-004`, commit `c70b757`) abrio tres hallazgos nuevos: `F-008`, `F-009`, `F-010`.
Ademas quedaban abiertos dos hallazgos de `R-003` (sobre `S-003`, commit `ea0b850`) que `S-004` no
evaluo: `F-005` y `F-006`. Esta sesion evaluo los seis.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-005 — El bloque de verificacion de la observacion nueva de `A-001` no se reproduce sobre su propio commit | Aceptado — pendiente | `T-009`, `Implementada` en este commit (nota fechada bajo `A-001` en `_persistence/assumptions.md`) |
| F-006 — La nota de ambito de `D-016` registra recuentos que no cuadran con el commit que la contiene | Aceptado — pendiente | `T-010`, `Implementada` en este commit (segunda nota fechada bajo `D-016` en `_persistence/decisions.md`) |
| F-007 — `D-020` declara una excepcion que la convencion de `tasks.md` no recoge | Implementado | ya cerrado en `S-004` (`T-008`); no vuelve a tocarse esta sesion |
| F-008 — El bloque de verificacion de `D-021` afirma reproducirse sobre su commit y no se reproduce | Aceptado — pendiente | `T-011`, `Implementada` en este commit (nota fechada bajo `D-021`) |
| F-009 — `DT-001` queda `Implementada` con un criterio de cierre que no se cumple | Aceptado — pendiente | `T-012`, `Implementada` en este commit (nota anadida a `DT-001` en `_persistence/techdebt.md`) |
| F-010 — Una convencion vigente de `_audit/findings.md` cita un archivo que ya no existe | Aceptado — pendiente | `T-013`, `Implementada` en este commit (nota fechada bajo `D-021`; la correccion de la linea 56 de `_audit/findings.md` queda expresamente fuera, porque ese archivo lo escribe `report_auditor`, no `manager`) |

Nota de lectura para las cinco filas `Aceptado — pendiente`: **la correccion ya esta escrita en
este commit**, pero el veredicto que las cierra —`Implementado`— solo lo puede dar esta auditoria
en su siguiente pasada, verificando sobre `HEAD` que la nota realmente esta ahi y dice lo que la
`T-XXX` afirma. `manager` no puede cerrar sus propios hallazgos.

## 1. Que se hizo

Archivos tocados en este commit (via `git show --stat --name-only --format= <commit>` sobre el
staged diff):

- `.claude/skills/protocol-audit/SKILL.md` — amplia el ambito del control (c) a `_phases/`
- `.claude/skills/protocol-close/SKILL.md` — amplia el ambito del Paso 1b a `_phases/`
- `CLAUDE.md` — nombra `_phases/` entre lo agnostico que tiene que poder copiarse tal cual
- `_audit/findings.md` — las seis filas de la tabla (`F-005`, `F-006`, `F-008`, `F-009`, `F-010`) pasan de `Abierto` a `Aceptado — pendiente`, citando su `T-XXX`; cada entrada de detalle lleva el bloque «Que se hizo» con comando y salida cruda
- `_persistence/assumptions.md` — nota fechada bajo `A-001` (`T-009`)
- `_persistence/decisions.md` — dos notas fechadas nuevas (bajo `D-016` y bajo `D-021`, `T-010`/`T-011`/`T-013`), mas las entradas nuevas `D-022`, `D-023`, `D-024`, `D-025`, `D-026`
- `_persistence/lessons.md` — entrada nueva `L-008`
- `_persistence/tasks.md` — campo `Etapa` nuevo, obligatorio, en ficha e indice; tareas nuevas `T-009` a `T-013` (`Origen: report_auditor`); `T-001` y `T-002` cambian de etapa a `005_discovery` por nota fechada, sin reescribir su contenido original
- `_persistence/techdebt.md` — nota anadida a `DT-001` (`T-012`)
- `_phases/000_preproject.md` — archivo nuevo, agnostico, describe la etapa en curso
- `project.md` — declara `005_discovery` en la tabla «Etapas», declara `_phases/` en «Carpetas propias»

Y del propio cierre de sesion (no de contenido de la jornada): `_persistence/progress.md` (entrada
`S-005`, y las secciones 1, 2 y 3 sobrescritas), `_audit/S-005.md` (este archivo) y su fila en
`_audit/index.md`.

**Resumen por bloque:**

1. **Los seis hallazgos de auditoria.** Todos comparten el mismo defecto de fondo: un recuento
   sobre un ambito que incluye archivos que la propia sesion todavia iba a escribir (`_persistence/`,
   `_audit/S-XXX.md`), presentado como si valiera sobre el commit que lo contendria. Se corrigieron
   **por nota fechada** (`D-019`: nunca se reescribe el bloque original), y de ahi salio la regla
   general `D-022` y la leccion `L-008`.
2. **`_phases/`.** El usuario aporto un archivo de otro metodo (`temporal/005_discovery.md`) y pidio
   escribir, con esa guia como forma y no como contenido, el archivo de la etapa en curso. Nace
   `_phases/000_preproject.md` (`D-023`), que fija que `000_preproject` **no define alcance ni
   objetivo del proyecto**.
3. **Consecuencia del punto 2.** `T-001` y `T-002` (definir alcance, declarar etapas posteriores)
   habian nacido dentro de `000_preproject` por no haber otra etapa declarada. `D-023` las deja sin
   etapa donde encajar. El usuario decidio: declarar `005_discovery` (`D-024`); anadir el campo
   `Etapa` a `tasks.md`, obligatorio y en el indice, y mover las dos tareas (`D-025`); y ampliar el
   ambito del Paso 1b del control de fuga a `_phases/` (`D-026`), en los dos skills que lo ejecutan.

## 2. Que NO se hizo, y por que

Del indice de `tasks.md`, filtrando `No implementada`:

- `T-001` — Definir alcance y objetivo del proyecto. Sigue sin tocar; ahora en etapa `005_discovery`.
- `T-002` — Declarar las etapas posteriores a `000_preproject`. Sigue sin tocar; ahora en etapa
  `005_discovery`. Su criterio de cierre escrito («la tabla Etapas lista mas de una») ya se cumple
  literalmente por efecto de `D-024`, y aun asi sigue abierta: lo que cierra la tarea es la
  **secuencia completa** decidida, no el nombre de la inmediata. Queda anotado en la propia tarea
  para que nadie la de por hecha leyendo solo la tabla.
- `T-003` — Verificar si el historico de la fuente oficial es obtenible (`A-003`). Sin tocar,
  etapa `000_preproject`. Bloqueante segun `tasks.md`.

Un asunto que queda a medias, escrito en el propio `D-022` y sin codigo que lo agende: **llevar la
regla que enuncia** —un recuento de ambito global se fecha, no se declara reproducible— **a
`CLAUDE.md` o a `protocol-close`**, que es donde `L-007` diria que tiene que estar para que la
aplique quien no leyo la entrada. `D-022` lo deja escrito como pendiente de decidir con el usuario,
pero no tiene `T-XXX` ni `DT-XXX` propio. Ver «Sin resolver» al final de este reporte.

## 3. Decisiones tomadas

- **`D-022` — Un recuento de ambito global se fecha, no se declara reproducible sobre su commit.**
  Regla general extraida de `F-005`, `F-006` y `F-008`. Alternativas descartadas: prohibir los
  recuentos globales en bloques de verificacion (pierde la unica evidencia de ambito completo);
  tomar siempre el recuento contra el commit anterior (se reproduce, pero mide un estado que no es
  el que la entrada describe); dejarlo solo en `L-006` (ya se probo: no basta).
- **`D-023` — Cada etapa declarada tiene su archivo agnostico en `_phases/`.** Origen: usuario, a
  partir de una guia externa aportada como forma, no como contenido. Alternativas descartadas: meter
  la descripcion de la etapa en `project.md`; escribirlo con datos de este proyecto y generalizar
  despues; copiar la guia externa tal cual y adaptarla.
- **`D-024` — La etapa siguiente a `000_preproject` se llama `005_discovery`.** Origen: usuario.
  Alternativas descartadas: declarar de golpe toda la secuencia del brief (§22); dejar las tareas de
  alcance en `000_preproject` como excepcion (contradice `D-023` el mismo dia); usar un identificador
  provisional.
  Nota: 🚨 declarar `005_discovery` **no cierra** `T-002` — su criterio literal ya se cumple sin que
  la tarea este hecha, y eso queda anotado en la propia tarea para evitar la lectura equivocada.
- **`D-025` — `tasks.md` gana el campo `Etapa`, y las dos tareas de alcance pasan a `005_discovery`.**
  Origen: usuario. Generaliza ademas la excepcion de escritura de `manager` sobre `tasks.md` (antes
  solo cubria la `T-XXX` de un hallazgo aceptado; ahora cubre cualquier cambio que nazca de una
  decision registrada que el `session-closer` no pueda deducir del diff). Alternativas descartadas:
  no anadir campo y expresarlo solo en el cuerpo de la tarea; usar el estado `Suspendida` (miente);
  dejar el campo solo en la ficha sin columna de indice.
- **`D-026` — El control de fuga de datos del Paso 1b cubre tambien `_phases/`.** Origen: manager,
  aplicando `D-023` mecanicamente. Alternativas descartadas: confiar en la disciplina; un control
  aparte solo para `_phases/`; ampliar el ambito al arbol entero.

## 4. Supuestos vigentes y riesgos

- **`A-001`** — Un auditor lanzado por el auditado conserva independencia suficiente. Sigue
  `Abierto`. La señal 2 (cero sesiones cerradas sin auditar) se rehizo esta sesion (`T-009`) para
  que sea disparable: una sesion cuenta como sin auditar cuando su fila sigue `Pendiente` **al
  abrirse la sesion siguiente**, no en el instante del cierre — con la version anterior el comando
  no podia devolver `exit=1` en ningun commit de cierre. Van tres auditorias registradas (`R-001`,
  `R-002`, `R-003`) mas `R-004`, con lo que la revision explicita fijada en el disparador del
  supuesto ya toca; queda como asunto propio, no se despacho esta sesion.
- **`A-003`** — Si el historico de la fuente oficial es obtenible. Sigue sin verificar (`T-003`,
  bloqueante). De el depende el ciclo entero del producto.
- **Riesgo sin codigo:** `D-022` deja escrito, en su propio texto, que llevar la regla a
  `CLAUDE.md` o a `protocol-close` queda pendiente de decidir con el usuario — y no tiene `T-XXX`
  ni `DT-XXX` que lo agende. Ver «Sin resolver».

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto a partir de `_brief/client_brief.md`. Importancia
`Alta`, urgencia `Bloqueante`, etapa `005_discovery`. Es la primera accion que de verdad abre esa
etapa; declararla no la empieza.

## 6. Que pedimos auditar

- **Que las cinco notas fechadas de esta sesion (`T-009` a `T-011` y `T-013`, mas la de `T-012`)
  realmente esten en `HEAD` con el comando y la salida cruda que sus tareas afirman.** Son
  correcciones sobre correcciones — el patron mismo que fallo tres veces (`L-008`) — asi que el
  riesgo de que una quinta repeticion del defecto se cuele aqui es real y no teorico.
- **Que el recuento nuevo de `D-022`, tomado "al momento de escribir la entrada", no se haya
  presentado por descuido como reproducible sobre este commit.** Es la regla que la propia entrada
  establece contra si misma; un fallo aqui seria especialmente grave porque invalidaria la decision
  que la enuncia.
- **Que `_phases/000_preproject.md` sea de verdad agnostico** — sin nombre del proyecto, sin ruta
  de disco, sin codigo `T-XXX`/`D-XXX` instanciado. El bloque de verificacion de `D-023` lo
  comprueba con `grep`, pero es la primera vez que este proyecto declara un archivo con esa
  exigencia y conviene una segunda mirada independiente.
- **Que la nueva excepcion generalizada de `D-025`** —`manager` escribe en `tasks.md` cuando el
  cambio nace de una decision registrada que el `session-closer` no puede deducir del diff— **no se
  use como puerta**: en esta sesion se aplico a exactamente dos filas (el traslado de etapa de
  `T-001` y `T-002`), cada una con su cita (`D-023`, `D-024`, `D-025`). Vale la pena que la auditoria
  fije un criterio de cuanto uso de esa excepcion en una sesion empieza a ser sospechoso.
- **El riesgo sin codigo senalado en la seccion 4** — que `D-022` quede como intencion sin `T-XXX`
  que la agende, y se pierda en cuanto pase esta sesion.
