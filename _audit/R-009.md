# Auditoria R-009 — sobre S-009

| Campo | Valor |
|---|---|
| Auditoria | R-009 |
| Informe auditado | `_audit/S-009.md` |
| Commit auditado | `fc91957` |
| Fecha | 2026-09-02 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

### 1.1 Ancla del informe

~~~
$ git log -1 --format=%h -- _audit/S-009.md
fc91957
~~~

### 1.2 Indice contra detalle en `_persistence/` (los siete archivos)

~~~
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show fc91957:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show fc91957:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
~~~

Sin diferencias en ninguno de los siete. **Correcto.**

### 1.3 Carpetas del arbol contra las declaradas en `project.md`

~~~
$ diff <(git ls-tree -d --name-only fc91957 | sed 's|$|/|' | sort) \
       <(git show fc91957:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
8a9
> temporal/
~~~

Una sola diferencia, `temporal/`, con su razon escrita en `project.md` (excluida por `.gitignore`).
**No es hallazgo.** La segunda diferencia que el informe anticipaba en su seccion 6 —`_workflow/`
todavia sin trackear— **ha desaparecido**, como el propio informe predijo: la carpeta esta en el
arbol del commit y declarada.

### 1.4 Fuga de datos propios en lo agnostico

Corrido sobre el ambito del protocolo (cuatro rutas) y sobre el ambito ampliado que este mismo
commit declara (seis rutas, con `_templates/` y `_workflow/`):

~~~
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" fc91957 -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" fc91957 -- .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
exit=1

$ git grep -niE "triple ?s|raindom|raidom|jdrodriguez|_brief" fc91957 -- _workflow ; echo "exit=$?"
exit=1
~~~

**Cero lineas en los tres.** La carpeta nueva `_workflow/` entra limpia.

### 1.5 La seccion 0 del informe, fila a fila

**`F-020` — declarado `Implementado`. Se sostiene.**

~~~
$ git show fc91957:_audit/findings.md | sed -n '/^### F-017/,/^### F-018/p' | grep -c "Que se hizo"
1
~~~

La vineta residual esta borrada; queda solo la fechada.

**`F-021` — declarado `Implementado`. NO se sostiene.** Ver `F-024`.

**`F-022` — declarado `Implementado`. Se sostiene.** Las tres notas existen y las tres ordenes
nuevas se reproducen tal cual, ahora si:

~~~
$ git show fc91957:_persistence/decisions.md | grep -c 'Nota del 2026-09-02 (`T-029`, hallazgo `F-022`)'
3

$ git show f096fff:project.md | grep -n '^| `_discovery/`' ; echo "exit=$?"
exit=1

$ git grep -nE "\bI-[0-9]{3}\b" 122b770 -- _persistence/ _audit/ project.md ; echo "exit=$?"
exit=1

$ git status --porcelain -- _audit/S-007.md | wc -l
0
~~~

Y no se reescribio ninguna salida antigua: las dos unicas lineas que `decisions.md` pierde en este
commit son la fila de indice y el campo `Estado` de `D-036`, que revoca `D-045`.

~~~
$ git show fc91957 -- _persistence/decisions.md | grep "^-[^-]"
-| [D-036](#d-036---los-artefactos-rellenos-del-descubrimiento-viven-en-_discovery) | Los artefactos rellenos del descubrimiento viven en `_discovery/` | 2026-09-02 | Vigente |
-| Estado | Vigente |
~~~

**`F-023` — declarado `Implementado`. Se sostiene.**

~~~
$ git show fc91957:_persistence/decisions.md | grep -n "^### D-044"
1918:### D-044 - La ficha `T-026` escrita a mano se asume como caso puntual, no como tercera excepcion

$ git show fc91957:_persistence/tasks.md | sed -n '/^### T-026/,/^### T-027/p' | grep -c "D-044"
2

$ git show fc91957:_persistence/tasks.md | sed -n '/^## Convenciones/,/^## Tareas/p' | grep -c "Son dos excepciones, no una puerta"
1
~~~

`D-044` existe, `T-026` la cita dos veces, y la convencion de `tasks.md` no gano ninguna tercera
excepcion, que es exactamente lo que la decision dice haber hecho.

### 1.6 La lista de archivos de la seccion 1 contra el commit

~~~
$ git show --stat --format= fc91957
 .claude/skills/protocol-close/SKILL.md       |   8 +-
 CLAUDE.md                                    |  17 +-
 _audit/S-009.md                              | 190 ++++++++++++
 _audit/findings.md                           |  57 ++--
 _audit/index.md                              |   1 +
 _persistence/assumptions.md                  |  41 +++
 _persistence/decisions.md                    | 394 ++++++++++++++++++++++++-
 _persistence/lessons.md                      |  81 ++++++
 _persistence/progress.md                     |  95 ++++--
 _persistence/tasks.md                        | 254 ++++++++++++++++
 _persistence/techdebt.md                     |  34 +++
 _templates/005_discovery/005_needs.md        |   8 +-
 _templates/005_discovery/010_actors.md       |   8 +-
 _templates/005_discovery/015_stakeholders.md |   8 +-
 _templates/005_discovery/020_hypothesis.md   |  10 +-
 _workflow/ai_levels.md                       | 417 +++++++++++++++++++++++++++
 _workflow/team.md                            | 349 ++++++++++++++++++++++
 project.md                                   |   5 +-
 18 files changed, 1905 insertions(+), 72 deletions(-)
~~~

Los quince archivos de la lista del informe, mas `_audit/S-009.md` y los dos de `_workflow/`, dan
los dieciocho del commit. **La lista cuadra**, y el diff no trae ningun archivo que el informe no
mencione. No se repite el defecto de `F-019`.

### 1.7 `T-031` — la mudanza a `005_discovery/`

~~~
$ git grep -n -- "_discovery/" fc91957 -- _templates project.md | grep -v "005_discovery/" ; echo "exit=$?"
exit=1

$ for f in _templates/005_discovery/005_needs.md _templates/005_discovery/010_actors.md _templates/005_discovery/015_stakeholders.md _templates/005_discovery/020_hypothesis.md project.md; do echo -n "$f:"; git show fc91957:$f | grep -c "005_discovery/"; done
_templates/005_discovery/005_needs.md:4
_templates/005_discovery/010_actors.md:4
_templates/005_discovery/015_stakeholders.md:4
_templates/005_discovery/020_hypothesis.md:5
project.md:2

$ git ls-tree -d --name-only fc91957 | grep "005_discovery" ; echo "exit=$?"
exit=1
~~~

La ruta nueva esta en los cinco archivos, no queda ninguna ruta vieja en el ambito de la tarea, y no
se creo ninguna carpeta — que es exactamente lo que `T-031` declara. **Correcto.** Las menciones de
`_discovery/` que quedan en el repositorio estan todas en texto historico (`D-036`, `S-008`,
`R-008`, bitacora de `progress.md`), que `D-019` prohibe reescribir.

### 1.8 Los enganches de `_workflow/`

~~~
$ git grep -n "_workflow" fc91957 -- _phases _methodology .claude
fc91957:.claude/skills/protocol-close/SKILL.md:105:git grep -nE "<nombre del proyecto>|<carpeta raiz de las rutas absolutas>|<host del remoto>" -- .claude CLAUDE.md _phases _methodology _templates _workflow
fc91957:.claude/skills/protocol-close/SKILL.md:116:`CLAUDE.md`, `_phases/`, `_methodology/`, `_templates/` y `_workflow/`, y a nada mas, porque son los
~~~

Responde a la segunda pregunta de la seccion 6 del informe: **`DT-002` se sostiene, no sobra.**
Ningun archivo de `_phases/` ni de `_methodology/` menciona `_workflow/`; el unico enganche fuera de
`project.md` y `CLAUDE.md` es el del Paso 1b, que es de **control** y no de **uso**.

### 1.9 Los bloques de verificacion de las decisiones nuevas

Es la primera pregunta de la seccion 6 del informe. Se confirma, y va como `F-025`.

## 2. Hallazgos

### F-024 — `F-021` se declara `Implementado` y la correccion no esta en el diff

| Campo | Valor |
|---|---|
| Gravedad | Alta |
| Donde | `_audit/S-009.md` §0, `_persistence/tasks.md` (`T-028`), `_persistence/progress.md` §1 y §2, `_audit/findings.md` (`F-021`) |

- **Que se observo:** la cadena que `F-021` señalaba **no existe en ninguna forma** en el commit:

~~~
$ git show fc91957:_persistence/progress.md | grep -o 'R-007` (sobre `[0-9a-f]*`)' ; echo "exit=$?"
exit=1

$ git show f096fff:_persistence/progress.md | grep -o 'R-007` (sobre `[0-9a-f]*`)'
R-007` (sobre `ae06147`)
~~~

  El diff de `progress.md` no contiene ninguna correccion de ese texto: la celda «Avance de la
  etapa» se **sobrescribio entera** con el contenido de `S-009`, como hace cada cierre, y con ella
  desaparecio el hash mal atribuido. No hay ninguna linea del diff que toque `ae06147`:

~~~
$ git show fc91957 -- _persistence/progress.md | grep -c "^[+-].*ae06147"
0
~~~

  El bloque de verificacion de `T-028` registra una salida que no se reproduce sobre el commit que
  la contiene —«R-007` (sobre `122b770`)», salida del arbol intermedio; sobre `fc91957` la misma
  orden da `exit=1`—, y su criterio de cierre («la celda atribuye a `R-007` el commit `122b770`, y
  la nota fechada explica que se corrigio») **no se cumple en el commit**. Ademas, tres registros
  afirman una correccion documentada que el diff desmiente: la celda de `progress.md` dice «`T-028`
  corrige **en esta misma celda** el commit atribuido a `R-007` … dejando nota fechada»; la seccion
  2 dice «con nota fechada»; y la entrada de `F-021` en `findings.md` dice lo mismo.
- **Por que importa:** el registro afirma una nota fechada que no existe. La nota fechada es el
  mecanismo con el que este proyecto distingue «se corrigio» de «se reescribio la historia»
  (`D-019`, `CLAUDE.md`); quien la busque en `progress.md` no la encuentra. `T-028` queda
  `Implementada` con un criterio de cierre que su propio commit incumple —el patron de `F-016`— y su
  bloque de verificacion no es reproducible. Y el protocolo es explicito: un `Implementado` que el
  diff no muestra deja el hallazgo original abierto.
- **Que lo corregiria:** dejar constancia de que `F-021` se resolvio **por desaparicion** del texto
  al sobrescribirse la celda, no por correccion; o, si se quiere la correccion de verdad,
  escribirla donde el texto sobrevive —la bitacora de `S-008`, que sigue en el archivo—. En
  cualquiera de los dos casos, ajustar la redaccion de la celda de `progress.md`, de la seccion 2 y
  de la entrada de `F-021`, que hoy afirman una nota fechada inexistente, y revisar el estado y el
  bloque de verificacion de `T-028`. ⚠️ Es una recomendacion, no una orden.

### F-025 — Los bloques de verificacion de `D-043` y `D-044` usan `HEAD` sin anclar y no se reproducen

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/decisions.md`, `D-043` y `D-044` |

- **Que se observo:** el informe lo somete a auditoria en su seccion 6, y se confirma.

~~~
$ git show fc91957:_persistence/decisions.md | grep -c "^| Fecha | 2026-09-02 |"
23
~~~

  `D-043` registra `16` para esa misma orden, escrita como `git show HEAD:_persistence/decisions.md
  | grep -c ...`. Y su segunda orden, `git log -1 --format="%h %ad" --date=short`, registra
  `7025a05`, que es el commit **anterior**:

~~~
$ git log -1 --format="%h %ad" --date=short fc91957
fc91957 2026-09-01
~~~

  En `D-044`, las dos ordenes del bloque sobre el commit que las contiene:

~~~
$ git show fc91957:_persistence/decisions.md | grep -c "T-026"
8

$ git show fc91957:_persistence/tasks.md | grep -c "NO encaja en ninguna de las dos excepciones"
1
~~~

  La primera registra `exit=1` —«ninguna decision menciona `T-026`»— y sobre el commit devuelve ocho
  coincidencias; la segunda si se reproduce.
- **Por que importa:** es el cuarto commit consecutivo con el mismo defecto (`F-005`, `F-008`,
  `F-011`, `F-022`), y esta vez ocurre **en el mismo commit** que estrena `L-013`, la leccion que lo
  nombra: «un bloque de verificacion sin ancla caduca». Una decision cuya verificacion no se
  reproduce sobre su propio commit no es auditable: obliga a rehacer el barrido entero para saber si
  el fondo era cierto. Aqui el fondo lo es en los dos casos —lo he comprobado—, pero eso solo se
  sabe despues de rehacerlo, y entonces la evidencia que vale es la del auditor y no la del
  registro.
- **Que lo corregiria:** el mismo patron que `T-029` acaba de aplicar a `D-036`, `D-038` y `D-040`:
  nota fechada al lado, sin reescribir el texto original, con la orden anclada a `fc91957` —o a
  `7025a05` para lo que se afirmaba del estado previo— y su salida cruda. Y, mirando hacia adelante,
  que `L-013` deje de ser solo una leccion escrita: como leccion ya fallo en el commit que la
  estreno. ⚠️ Es una recomendacion, no una orden.

### F-026 — `DT-002` cita `L-013` donde corresponde `L-014`

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/techdebt.md`, `DT-002` |

- **Que se observo:**

~~~
$ git show fc91957:_persistence/techdebt.md | grep -n "L-013\|L-014"
14:| [DT-002](#dt-002---_workflow-nace-sin-ningun-enganche-de-uso-l-014) | `_workflow/` nace sin ningun enganche de uso (`L-014`) | No implementada | Propuesta (pendiente del usuario) | Media | No bloqueante |
135:### DT-002 - `_workflow/` nace sin ningun enganche de uso (`L-014`)
149:  registrado `L-013` de `lessons.md`.
162:📌 **Propuesta del cierre `S-009`**, a partir de `L-014`. `manager` no la escribe como deuda

$ git show fc91957:_persistence/lessons.md | grep -n "^### L-013\|^### L-014"
390:### L-013 - Un bloque de verificacion sin ancla caduca; el codigo de salida no prueba una ausencia
431:### L-014 - Una carpeta agnostica nueva necesita cuatro enganches, y el cuarto es el que se olvida
~~~

  El titulo, la fila del indice y el cierre de la entrada citan `L-014`; el cuerpo, en la linea 149,
  cita `L-013`, que trata de otra cosa por completo.
- **Por que importa:** es una remision cruzada dentro del mismo registro. Quien siga la cita del
  cuerpo llega a la leccion de los bloques de verificacion y no a la de los cuatro enganches, que es
  la que sostiene la deuda. Gravedad baja porque las otras tres menciones son correctas y el error
  se ve al abrir la leccion.
- **Que lo corregiria:** sustituir `L-013` por `L-014` en esa linea. ⚠️ Es una recomendacion, no una
  orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-020` | **Implementado** (`fc91957`) | la entrada de `F-017` tiene exactamente una vineta «Que se hizo», la fechada; verificado en §1.5 |
| `F-021` | **sigue `Aceptado — pendiente`** | la correccion no aparece en el diff: la cadena señalada desaparecio por sobrescritura de la celda, y la nota fechada que `T-028` declara no existe. Ver `F-024` |
| `F-022` | **Implementado** (`fc91957`) | las tres notas fechadas existen y sus tres ordenes se reproducen tal cual sobre el commit; ninguna salida original fue reescrita. Verificado en §1.5 |
| `F-023` | **Implementado** (`fc91957`) | `D-044` existe, `T-026` la cita, y la convencion de `tasks.md` no gano una tercera excepcion. Verificado en §1.5 |

## 4. Lo que NO se pudo comprobar

Ninguno de los controles quedo sin correr. Los tres controles mecanicos y las cuatro filas de la
seccion 0 se ejecutaron sobre el commit `fc91957`, con su salida en la seccion 1.

Una precision, que no es un `SIN COMPROBAR` pero conviene decir: el contenido de `_workflow/team.md`
y `_workflow/ai_levels.md` se audito **solo** por agnosticidad y por coherencia con lo que el
informe declara. Su correccion de fondo no es auditable contra el repositorio —no hay nada con lo
que contrastarla—, y el propio cierre ya la registro como supuesto abierto en `A-005`, con su
disparador de revision. Ese es el tratamiento correcto y no hay hallazgo que abrir.

## 5. Recomendaciones sin hallazgo

- **La seccion 1 del informe pide una salida que todavia no se puede generar.** La plantilla de
  `protocol-close` —endurecida por `T-025` a raiz de `F-019`— manda pegar `git show --stat
  --name-only --format= <commit>`, pero el informe se escribe **antes** del commit que lo contiene.
  `S-009` lo resolvio bien: declaro la sustitucion, dio las dos ordenes que si podia correr con su
  salida cruda, y la lista resultante cuadra con el commit (§1.6). Aun asi, cada cierre va a
  tropezar con lo mismo; merece resolverse en el mecanismo y no en cada informe.
- **Los bloques de verificacion no deberian pegar numeros de linea.** `T-030` registra
  `1914:### D-044` y sobre el commit sale `1918`. No afecta al fondo y no lo abro como hallazgo,
  pero es ruido evitable: un `grep -c`, o un `grep -n` cuyo numero no se comente en la prosa,
  envejece mejor.
- **`_workflow/` sigue sin lector.** `DT-002` esta bien planteada (§1.8). Mientras el cuarto
  enganche no exista, los dos archivos nuevos son 766 lineas que ningun protocolo manda abrir; es la
  clase de material que envejece sin que ningun control lo note.
