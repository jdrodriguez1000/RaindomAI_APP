# Informe de auditoria — S-022

| Campo | Valor |
|---|---|
| Sesion | S-022 |
| Fecha | 2026-09-06 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | PENDIENTE-DE-ANCLAJE (se rellena en el Paso 7c con el hash literal del commit sustantivo de esta sesion) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-055 — `_phases/030_growth.md` instancia `T-083` y `D-085`, y es la unica ocurrencia de toda `_phases/` | Aceptado — pendiente | `T-084` (correccion) y `T-085` (control nuevo), ambas `Implementada` en `_persistence/tasks.md`; su cierre efectivo lo confirma la proxima auditoria sobre este commit |
| F-056 — La salida cruda que publica `_phases/030_growth.md` no es la que devuelve la orden que la encabeza | Aceptado — pendiente | `T-086`, `Implementada` |
| F-057 — Los bloques «Criterio de cierre» de `D-083` y `D-084` publican los comandos sin su salida | Aceptado — pendiente | `T-087` (correccion de las dos entradas) y `T-089` (forma fijada en las convenciones de `decisions.md`), ambas `Implementada` |
| F-058 — `_audit/S-021.md` conserva dos lineas de instruccion de la plantilla | Aceptado — pendiente | `T-088`, `Implementada` |

Los cuatro se verificaron contra `HEAD` (`e1d1b54`) antes de tocar nada — la evidencia de esa
verificacion esta en `_persistence/decisions.md`, entradas `D-086` a `D-089`, cada una con su bloque
de comando y salida cruda.

🚨 **Ninguno de los cuatro pasa aqui a `Implementado`.** `manager` corrigio los cuatro en esta misma
sesion, pero el estado `Implementado` exige una auditoria posterior que verifique la correccion
sobre el commit resultante — esa auditoria es `report_auditor`, no esta sesion de cierre.

Ademas, esta sesion atendio **dos recomendaciones sin hallazgo** que dejo `R-021`:
- La fila de `S-020` en `_audit/index.md` publicaba el commit de anclaje (`3ff670e`) en vez del
  commit de la sesion (`f09d1f7`) — eso ya estaba registrado como `F-053`, y no se abre hallazgo
  nuevo. `D-090` corrige la regla en `protocol-audit`: la fila publica desde ahora el hash literal
  de la cabecera del informe. Las filas anteriores no se reescriben.
- Nace `_workflow/030_growth.md` a peticion del usuario (`D-091`, `T-091`), sin relacion con ningun
  hallazgo de `R-021`.

## 1. Que se hizo

Lista completa de archivos que el commit de esta sesion toca, salida cruda del area de staging
(el commit todavia no existe mientras se escribe esta seccion; la nota de cierre con la version
anclada la pega el Paso 7c):

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-audit/SKILL.md
.claude/skills/protocol-close/SKILL.md
_audit/S-021.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_phases/030_growth.md
_workflow/030_growth.md
```

Esa lista **no incluye todavia** `_audit/S-022.md` (este informe), porque se escribe antes de
anadirlo al `git add` final; entra en el mismo commit por el Paso 6b.

Detalle, con codigos y rutas:

- **`.claude/skills/protocol-audit/SKILL.md`** — gana la regla de `D-090`: la fila de `_audit/index.md`
  publica el hash literal de la cabecera del informe auditado, no el que deriva
  `git log -1 -- _audit/S-XXX.md` (que en un cierre con anclaje apunta al commit de anclaje).
- **`.claude/skills/protocol-close/SKILL.md`** — nacen dos controles: el **Paso 1c** (cero codigos
  instanciados en `_phases/` y `_workflow/`, `D-087`) y, en el Paso 6b, el control de huecos de
  plantilla en el informe (`grep -nE '^<'`, `D-089`). Los dos con su linea en el reporte de pantalla
  del Paso 8.
- **`_audit/S-021.md`** — no se borra nada; recibe la nota fechada de `D-089` (`F-058`) documentando
  las dos lineas `^<` que conserva y por que se dejan.
- **`_audit/findings.md`** — `F-055` a `F-058` (nace su fila, escrita en `S-021`/`R-021`) pasan de
  `Abierto` a `Aceptado — pendiente (T-XXX)`, con las tareas que las recogen. Ninguna entrada nace
  en este commit; se editan las cuatro filas del indice y las cuatro fichas de detalle.
- **`_audit/index.md`** — nace la fila de `S-022` (`Pendiente` en las tres ultimas columnas).
- **`_persistence/assumptions.md`** — nace `A-009` (que `_phases/` y `_workflow/` puedan seguir en
  cero codigos instanciados sin que aparezca una necesidad legitima de citar un numero ahi).
- **`_persistence/decisions.md`** — nacen `D-086` a `D-091`, seis decisiones. Ademas se anaden notas
  fechadas a `D-083` y `D-084` (evidencia de `F-057`, corregida con la salida real de sus criterios de
  cierre) y se amplian las convenciones del archivo con la forma obligatoria del bloque «Criterio de
  cierre» (`D-088`/`T-089`).
- **`_persistence/lessons.md`** — nace `L-030`. Ademas se edita la fila de `L-029` en el indice
  (hallazgo propio, `T-092`): tenia cinco campos en vez de siete, faltaban `Etapa` y `Portabilidad`.
- **`_persistence/progress.md`** — se sobrescribe `## 1. Estado general`, `## 2. Ultimo realizado` y
  `## 3. Siguiente paso`; nace `S-022` en el indice de sesiones y en la bitacora.
- **`_persistence/tasks.md`** — nacen `T-084` a `T-092`, nueve tareas, todas `Implementada` salvo
  ninguna a medias. Ademas se anade una nota fechada al bloque de verificacion de `T-083` (parte de
  `F-055`/`F-056`, documentando que dos de sus cuatro ordenes dejaron de reproducir sobre el arbol de
  trabajo por el propio trabajo de esta sesion).
- **`_phases/030_growth.md`** — la cabecera de la nota del §5 se reescribe quitando `T-083` y `D-085`
  (`D-086`, `F-055`); nace una segunda nota fechada que corrige la salida cruda no reproducible
  (`F-056`) y confirma que las dos condiciones de entrada del §5 ya estan cumplidas.
- **`_workflow/030_growth.md`** (nuevo, no rastreado hasta este commit) — el quinto y ultimo archivo
  de reparto por etapa, ocho filas (una por paso del procedimiento de `_phases/030_growth.md` §4),
  a peticion del usuario (`D-091`, `T-091`).

## 2. Que NO se hizo, y por que

Tareas `No implementada` en el indice de `_persistence/tasks.md` (filtro `grep "No implementada"`
sobre la tabla del indice; una fila mas —`T-039`— aparece por coincidencia de texto en su titulo
("Normalizar a `No implementada`...") y no por su estado real, que es `Implementada`; se excluye
abajo y se deja dicho aqui, no en la lista):

```
| [T-001] Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002] Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003] Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
| [T-037] Escribir el inventario de acciones irreversibles del proyecto (`LG-38`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-038] Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close` | No implementada | Media | No bloqueante | `000_preproject` |
```

Ninguna de las cinco se toco esta sesion. `T-001`, `T-002` y `T-003` dependen de que el usuario
resuelva `A-004` (acceso al patrocinador) para poder abrir `005_discovery`. `T-037` y `T-038` estan
disponibles y no dependen de nada, pero no eran el objeto de esta sesion: el usuario pidio evaluar
`R-021` y escribir `_workflow/030_growth.md`.

Tampoco se adopto ninguna etapa nueva en `project.md`: `010_prototype`, `020_baseline`, `025_wslt` y
`030_growth` siguen sin declarar, aunque `030_growth` ya tiene sus cinco entregables completos
(archivo de etapa, tres plantillas, reparto).

## 3. Decisiones tomadas

- **`D-086` — La cabecera de una nota se reescribe; su bloque de verificacion, no.** `F-055` y
  `F-056` caen sobre la misma nota fechada, pero sobre partes distintas: la cabecera cita codigos
  instanciados (dato que no es evidencia, se puede corregir); el bloque de abajo es evidencia (una
  salida cruda), y `D-019` prohibe reescribirla. Alternativas descartadas: reescribir tambien el
  bloque (viola `D-019`); dejar la cabecera y solo anotar (no quita la fuga que el control nuevo
  volveria a encontrar cada sesion); borrar la nota entera (pierde el porque del §5 a medias).
- **`D-087` — Nace el Paso 1c de `protocol-close`: cero codigos instanciados en `_phases/` y
  `_workflow/`.** El Paso 1b no ve esta clase de fuga (busca nombre, ruta, host — no codigos con
  numero). Alternativas descartadas: ampliar el patron del Paso 1b (mezclaria ambitos distintos,
  obligando a excluir cuatro carpetas dentro de una orden hoy de una linea); dejarlo como norma sin
  control (ya fallo una vez, es `L-008` literal); ponerlo en la auditoria en vez del cierre (en el
  cierre todavia se puede corregir antes del commit).
- **`D-088` — El criterio de cierre lleva orden anclada y salida, siempre.** `F-057` mostro que el
  formato del bloque «Criterio de cierre» no estaba fijado: la misma sesion publico un criterio con
  salida (`D-085`) y dos sin ella (`D-083`, `D-084`). Alternativas descartadas: reescribir los
  bloques originales anadiendoles la salida (parecerian ejecutados entonces, viola `D-019`); anotarlo
  solo en `findings.md` (el defecto seguiria vivo en el archivo que la gente lee); fijar la forma sin
  corregir las dos entradas (deja la regla nueva sin su primer caso aplicado).
- **`D-089` — Un hueco de plantilla en un informe auditado se anota, y el control va antes del
  commit.** `F-058`: dos lineas de `_audit/S-021.md` eran instruccion de llenado, no contenido.
  Alternativas descartadas: borrarlas (reescribe un artefacto ya auditado); rehacer la plantilla para
  que los huecos no vayan entre `<` y `>` (esa forma es justo lo que hace el control posible);
  anotarlo sin control (el defecto volveria a entrar sin que nadie lo notara).
- **`D-090` — El tablero de auditoria publica el hash literal del informe.** Recomendacion sin
  hallazgo de `R-021`. La fila de una sesion apuntaba al commit que `git log -1 -- _audit/S-XXX.md`
  deriva, que en un cierre con anclaje es el commit de anclaje, no el de la sesion. Alternativas
  descartadas: anadir una segunda columna con los dos hashes (viola `PI-2`); reescribir la fila de
  `S-020` (reescribiria una historia que ya paso asi); dejarlo como esta porque la fila es
  tecnicamente correcta sobre lo que aquella auditoria miro (pero eso es justo lo que el lector del
  tablero no sabe).
- **`D-091` — El reparto del crecimiento: un bucle que llega a usuarios reales.** A peticion del
  usuario, nace `_workflow/030_growth.md`. Dos diferencias de fondo con los cuatro repartos
  anteriores: es la primera etapa que se repite (el reparto se ejecuta decenas de veces por slice), y
  es la primera cuyo trabajo llega a usuarios reales con datos reales, lo que sube el eje «impacto de
  un error» a 3 y con el la lectura de nivel de IA a 5 (`_workflow/ai_levels.md` §6). Alternativas
  descartadas: mantener el eje en 2 como en el esqueleto (la condicion que sostenia ese 2 se rompe por
  definicion en esta etapa); puntuar tambien «autonomia» en 3 (la IA sigue trabajando dentro de una
  descomposicion que firma un humano); partir el archivo en dos (viola `PI-2` y la regla de una fila
  por paso del procedimiento).

Todas marcadas **reversible a criterio** en su propia entrada (ediciones de texto en archivos
versionados, sin efecto fuera del repositorio) — criterio declarado, no leido de una tabla, porque
`_persistence/` sigue sin el inventario de acciones irreversibles (`T-037`).

## 4. Supuestos vigentes y riesgos

- **`A-009`** (nace esta sesion): `D-087` exige **cero** codigos instanciados en `_phases/` y
  `_workflow/`, y ese cero es una apuesta a futuro — que ninguna necesidad legitima de trazabilidad
  vaya a pedir un numero ahi. Si el Paso 1c devuelve alguna vez una linea que, evaluada con
  honestidad, "esta bien puesta", el supuesto queda refutado en el acto: es un control que se
  autodiagnostica. Disparador: cada ejecucion del Paso 1c, sin fecha fija.
- **`A-006`, `A-007`, `A-008`** siguen abiertos sin cambios esta sesion (codigos `FT-`/`SC-`
  declarados por adelantado; disponibilidad de un humano para el despliegue del esqueleto; huecos de
  codigo en las plantillas de `_templates/030_growth/` rellenables cuando la etapa se abra).
- **Riesgo nuevo, sin codigo propio, senalado en la seccion 6:** el bloque «Criterio de cierre» de
  `D-088` es autorreferencial (ver Paso 2d, abajo). No se le abrio `A-XXX` ni `DT-XXX` esta sesion:
  se deja senalado para que `manager` decida.

## 5. Siguiente tarea propuesta

**`T-001` — Definir alcance y objetivo del proyecto** (Alta, Bloqueante, etapa `005_discovery`) sigue
siendo la tarea de fondo, pero esta bloqueada por `A-004` (acceso al patrocinador). Como accion
inmediata y no bloqueada: revisar la autorreferencia del criterio de cierre de `D-088` (seccion 6) y
decidir si se ancla al commit `76a2cb6` (el commit en que la decision se cerro) o se reformula el
enunciado de busqueda para que no se cite a si mismo.

## 6. Que pedimos auditar

- **La autorreferencia del criterio de cierre de `D-088`.** Su segunda orden —
  `grep -n 'que hasta ahora no tenia forma fijada' _persistence/decisions.md`— publica una sola
  coincidencia (linea 115 en el momento de escribirse), pero al re-ejecutarla en esta misma sesion
  devuelve tres: la ocurrencia original (desplazada a la linea 121) mas dos mas, porque el propio
  bloque de evidencia de `D-088` cita literalmente ese mismo texto de busqueda como parte de su
  salida pegada. Es un defecto de diseño del criterio, no un error de transcripcion: cualquier
  criterio que busque un texto que el propio bloque tambien cita se autorreferencia. No se corrigio
  en esta sesion porque `decisions.md` no es un archivo que este cierre pueda editar de fondo (solo
  puede revisar y senalar). Verificacion:

```
$ grep -n "que hasta ahora no tenia forma fijada" _persistence/decisions.md
121:🚨 **Y eso incluye el bloque «Criterio de cierre», que hasta ahora no tenia forma fijada.** El
5007:$ grep -n 'que hasta ahora no tenia forma fijada' _persistence/decisions.md
5008:115:🚨 **Y eso incluye el bloque «Criterio de cierre», que hasta ahora no tenia forma fijada.** El
```

- **El Paso 1c es nuevo y solo tiene un ciclo de uso.** `A-009` lo dice explicito: el cero que exige
  es una apuesta, comprobada una sola vez (la nota del §5 de `_phases/030_growth.md`, donde bastaba
  con la fecha). Conviene que la auditoria mire si el ambito de dos carpetas sigue siendo el correcto
  cuando aparezcan mas archivos de etapa y de reparto.
- **La decision de no abrir `A-XXX`/`DT-XXX` para el hallazgo del punto 1** es discutible: podria
  argumentarse que un criterio de cierre que no reproduce merece su propio hallazgo de deuda tecnica
  en vez de quedar solo en la seccion 6 de este informe. Se dejo asi porque el defecto se detecto y
  documento en el mismo cierre que lo introdujo, y el canal natural para eso es este informe —pero es
  exactamente el riesgo que `CLAUDE.md` señala de «un riesgo nombrado solo en el informe y en ningun
  otro sitio»: si esta sesion se lee y no se actua, el riesgo desaparece del radar en cuanto llegue el
  siguiente informe.

## 7. Evidencia del Paso 2d

Orden ejecutada sobre el area de staging (el commit de esta sesion no existe todavia; la version
anclada al commit se pega en la nota de cierre de esta seccion, en el Paso 7c):

```
$ git diff -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
```

Recuento de **lineas** devueltas: **38**. Recuento de **ordenes distintas** (aparte, con ese nombre,
`sort -u | wc -l`): **23**. Las que se repiten (`sort | uniq -c | sort -rn`, mostrando solo las que
aparecen mas de una vez):

```
3  ls _workflow/030_growth.md 2>&1
2  grep -noE '\b(T|D|F|L|A|C|DT|S|N|I|R|H)-[0-9]{2,3}\b' _workflow/030_growth.md; echo "exit=$?"
2  grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' _phases/030_growth.md; echo "exit=$?"
2  grep -n 'que hasta ahora no tenia forma fijada' _persistence/decisions.md
2  grep -n 'el literal de la cabecera del informe' .claude/skills/protocol-audit/SKILL.md
2  grep -n '^### 1c' .claude/skills/protocol-close/SKILL.md
2  grep -n 'Ninguna linea de la plantilla sobrevive' .claude/skills/protocol-close/SKILL.md
2  grep -n "_workflow/030_growth" _phases/030_growth.md | head -2
2  grep -cE "^\| \*\*[1-8] · " _workflow/030_growth.md
2  grep -c 'Nota del 2026-09-06' _phases/030_growth.md
2  grep -c 'Nota del 2026-09-06' _audit/S-021.md
2  grep -c 'Codigos instanciados en' .claude/skills/protocol-close/SKILL.md
2  git grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' -- _phases _workflow; echo "exit=$?"
2  awk '/^### D-083/,/^### D-085/' _persistence/decisions.md | grep -c 'Nota del 2026-09-06'
```

(13 ordenes aparecen dos veces y una —`ls _workflow/030_growth.md 2>&1`— aparece tres: una vez en
la nota de `_phases/030_growth.md`, `D-086`, y otra vez cada una en su copia dentro de `tasks.md` y
en la propia `D-086`. Las 9 restantes hasta 23 aparecen una sola vez.)

Lista completa, numerada por la propia orden (38 lineas, sin deduplicar):

```
 1	git rev-parse --short HEAD
 2	git grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' HEAD -- _phases
 3	grep -n "cannot access" _phases/030_growth.md
 4	ls _workflow/030_growth.md 2>&1
 5	git grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' e1d1b54 -- _phases
 6	grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' _phases/030_growth.md; echo "exit=$?"
 7	grep -c 'Nota del 2026-09-06' _phases/030_growth.md
 8	ls _workflow/030_growth.md 2>&1
 9	for d in .claude _methodology _phases _templates _workflow CLAUDE.md; do echo "== $d"; git grep -cE '\b(T|D|F|L|A|C|DT|S|N|I|R|H)-[0-9]{2,3}\b' HEAD -- "$d" | head -3; done
10	grep -n '^### 1c' .claude/skills/protocol-close/SKILL.md
11	grep -c 'Codigos instanciados en' .claude/skills/protocol-close/SKILL.md
12	git grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' -- _phases _workflow; echo "exit=$?"
13	awk '/^### D-083/,/^### D-085/' _persistence/decisions.md | grep -c 'Nota del 2026-09-06'
14	grep -n 'que hasta ahora no tenia forma fijada' _persistence/decisions.md
15	grep -nE '^<' _audit/S-021.md
16	grep -c 'Nota del 2026-09-06' _audit/S-021.md
17	grep -n 'Ninguna linea de la plantilla sobrevive' .claude/skills/protocol-close/SKILL.md
18	grep -c 'Huecos de plantilla en el informe' .claude/skills/protocol-close/SKILL.md
19	grep -n 'el literal de la cabecera del informe' .claude/skills/protocol-audit/SKILL.md
20	grep -cE "^\| \*\*[1-8] · " _workflow/030_growth.md
21	grep -n "_workflow/030_growth" _phases/030_growth.md | head -2
22	grep -nE "RaidomAI|RaindomAI|TripleS|Proyectos_TripleS|github.com|USUARIO" _workflow/030_growth.md; echo "exit=$?"
23	grep -noE '\b(T|D|F|L|A|C|DT|S|N|I|R|H)-[0-9]{2,3}\b' _workflow/030_growth.md; echo "exit=$?"
24	grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' _phases/030_growth.md; echo "exit=$?"
25	git grep -noE '\b(T|D|F|L|A|C|DT|S)-[0-9]{2,3}\b' -- _phases _workflow; echo "exit=$?"
26	grep -n '^### 1c' .claude/skills/protocol-close/SKILL.md
27	grep -c 'Codigos instanciados en' .claude/skills/protocol-close/SKILL.md
28	grep -c 'Nota del 2026-09-06' _phases/030_growth.md
29	ls _workflow/030_growth.md 2>&1
30	awk '/^### D-083/,/^### D-085/' _persistence/decisions.md | grep -c 'Nota del 2026-09-06'
31	grep -c 'Nota del 2026-09-06' _audit/S-021.md
32	grep -n 'Ninguna linea de la plantilla sobrevive' .claude/skills/protocol-close/SKILL.md
33	grep -n 'que hasta ahora no tenia forma fijada' _persistence/decisions.md
34	grep -n 'el literal de la cabecera del informe' .claude/skills/protocol-audit/SKILL.md
35	grep -cE "^\| \*\*[1-8] · " _workflow/030_growth.md
36	grep -n "_workflow/030_growth" _phases/030_growth.md | head -2
37	grep -noE '\b(T|D|F|L|A|C|DT|S|N|I|R|H)-[0-9]{2,3}\b' _workflow/030_growth.md; echo "exit=$?"
38	awk 'NR>=12 && /^\| \[L-/' _persistence/lessons.md | awk -F'|' '{if(NF!=7) print "FILA MAL: "$2}'; echo "exit=$?"
```

Reejecucion de cada una (posicion → resultado), sobre el area de staging, hoy:

```
 1 → e1d1b54
 2 → HEAD:_phases/030_growth.md:402:T-083 / HEAD:_phases/030_growth.md:402:D-085
 3 → 413:> ls: cannot access '"_workflow/030_growth.md"': No such file or directory
 4 → _workflow/030_growth.md
 5 → e1d1b54:_phases/030_growth.md:402:T-083 / e1d1b54:_phases/030_growth.md:402:D-085
 6 → exit=1
 7 → 1
 8 → _workflow/030_growth.md
 9 → (nueve bloques == carpeta; ver salida completa mas abajo — reproduce igual que lo publicado en D-087)
10 → 136:### 1c. El control de codigos instanciados
11 → 1
12 → exit=1
13 → 2
14 → 121:🚨 ... + 5007/5008 (AUTORREFERENCIA — difiere de lo publicado, ver seccion 6)
15 → 41:<PEGA AQUI... / 131:<Las tareas abiertas...  (numeros de linea distintos a los publicados por
     el commit de anclaje del Paso 7c de S-021; el hecho —dos lineas `^<`— es el mismo)
16 → 1
17 → 1022:### 🚨 Ninguna linea de la plantilla sobrevive en el informe
18 → 1
19 → 275:🚨 **El `<hash>` de esa fila es el mismo que auditaste...
20 → 8
21 → 201:... / 387:...
22 → exit=1
23 → exit=1
24 → exit=1
25 → exit=1
26 → 136:### 1c. El control de codigos instanciados
27 → 1
28 → 1
29 → _workflow/030_growth.md
30 → 2
31 → 1
32 → 1022:### 🚨 Ninguna linea de la plantilla sobrevive en el informe
33 → 121:🚨 ... + 5007/5008 (misma autorreferencia que la posicion 14)
34 → 275:🚨 **El `<hash>` de esa fila es el mismo que auditaste...
35 → 8
36 → 201:... / 387:...
37 → exit=1
38 → exit=0
```

Salida completa de la orden 9 (`for d in ...`):

```
== .claude
HEAD:.claude/agents/session-starter.md:1
HEAD:.claude/skills/protocol-close/SKILL.md:23
HEAD:.claude/skills/protocol-start/SKILL.md:2
== _methodology
HEAD:_methodology/000_method.md:11
HEAD:_methodology/sources/005_vertical.md:16
== _phases
HEAD:_phases/030_growth.md:1
== _templates
HEAD:_templates/005_discovery/005_needs.md:5
HEAD:_templates/005_discovery/010_actors.md:2
HEAD:_templates/005_discovery/015_stakeholders.md:10
== _workflow
== CLAUDE.md
HEAD:CLAUDE.md:1
```

**37 de las 38 posiciones reproducen exactamente lo publicado en su entrada de origen.** La unica
que no —posiciones 14 y 33, la misma orden repetida, del criterio de cierre de `D-088`— esta
documentada en la seccion 6 de este informe: es autorreferencial por diseño, no un error de
transcripcion, y no se corrigio porque este cierre no reescribe `decisions.md` de fondo.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/findings.md: 1
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Separacion nueva/heredada, contra `HEAD` antes de esta sesion:

```
$ for f in _audit/findings.md _persistence/decisions.md _persistence/tasks.md; do
  echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(cat "$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"
done
_audit/findings.md head=1 staged=1
_persistence/decisions.md head=7 staged=7
_persistence/tasks.md head=5 staged=5
```

Las tres cifras son **identicas a `HEAD`**: ninguna linea de caracter de control es nueva de esta
sesion. Las tres son heredadas (`DT-003`, `DT-004`, `DT-005`, ya registradas). `_workflow/030_growth.md`
(archivo nuevo, no rastreado hasta este commit) tambien se barrio y salio en cero:

```
$ cat _workflow/030_growth.md | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```
