# Informe de auditoria — S-041

| Campo | Valor |
|---|---|
| Sesion | S-041 |
| Fecha | 2026-09-14 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | `e332771` |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`a3bb32e`, «auditoria R-040 sobre S-040 (4962db2)») antes de tratar cada
hallazgo, como manda `CLAUDE.md`. `R-039` dejo `F-109`; `R-040` dejo `F-110` a `F-112`.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-109 — la seccion 1 de `S-039` atribuye a `T-180` las tres reincidencias de la nota de `L-056`, y la tercera es de `T-179` | Aceptado — corregido en este commit | `T-183`: nota fechada al final de la seccion 1 de `_audit/S-039.md`, sin reescribir la vineta |
| F-110 — la seccion 7 de `S-040` publica como salida cruda dos bloques que su orden no produce | Aceptado — corregido en este commit | `T-184`: nota fechada al final de la seccion 7 de `_audit/S-040.md`, con la procedencia reanclada (16 lineas) y el control de cifra adyacente corrido sobre las dos versiones commiteadas del informe |
| F-111 — dos resultados nuevos del registro se afirman sin su orden ni su salida | Aceptado — corregido en este commit | `T-185`: nota fechada bajo `T-162` (`_persistence/tasks.md`) y bajo `D-159` (`_persistence/decisions.md`), con las ordenes ancladas y su salida |
| F-112 — el Paso 6 de `protocol-promote` queda con dos pasos numerados «4.» | Aceptado — corregido en este commit, y promovido antes de corregirse | `T-186`: el push pasa a «5.» en `.claude/skills/protocol-promote/SKILL.md`. ⚠️ El esqueleto ya llevaba la version con el defecto (commit `afeedf4`, `D-163`, por decision del usuario); la correccion sube en la promocion siguiente |

## 1. Que se hizo

Anclada al commit sustantivo de esta sesion:

```
$ git show --stat --name-only --format= e332771
.claude/skills/protocol-promote/SKILL.md
_audit/S-039.md
_audit/S-040.md
_audit/S-041.md
_audit/findings.md
_audit/index.md
_outbound/.gitkeep
_outbound/skeleton_readme.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

Con codigos y rutas, lo que muestra el diff (procedencia derivada, no de memoria — ver seccion 7):

- **`_audit/findings.md`** — `F-109` a `F-112` (fichas ya existentes; ninguna nace aqui): fila e
  indice pasan de `Abierto` a `Aceptado — pendiente`, `Registrado en` se rellena con su `T-XXX`, y
  cada ficha lleva su bloque «Tratamiento del 2026-09-14 (`S-041`) — ACEPTADO», verificado contra
  `HEAD`.
- **`_audit/S-039.md`** — no nace nada nuevo: `T-183` anade una nota fechada al final de la seccion
  1, sin tocar la vineta que corrige.
- **`_audit/S-040.md`** — no nace nada nuevo: `T-184` anade una nota fechada al final de la seccion
  7, sin tocar los bloques que corrige.
- **`.claude/skills/protocol-promote/SKILL.md`** — `T-186` cambia un solo caracter: el push del
  Paso 6 pasa de «4.» a «5.».
- **`_persistence/decisions.md`** — `D-163` (nace), `D-164` (nace), `D-165` (nace); y una nota
  fechada (`T-185`, `F-111`) que edita la entrada existente `D-159` (atribuida por posicion a `D-160`
  en la salida cruda de la seccion 7, por el borde del script que ya se documento en `S-038`/`S-040`),
  con la medicion del final de linea que la vineta de `D-159` afirmaba sin ella.
- **`_persistence/tasks.md`** — `T-183`, `T-184`, `T-185`, `T-186` (nacen); y una edicion de la
  entrada existente `T-162` (estado `No implementada` → `Implementada`, y una nota fechada de
  `T-185`/`F-111` con los dos barridos rehechos del borrador de la guia).
- **`_persistence/lessons.md`** — no nace nada: una nota de reincidencia bajo `L-056` (`D-164`), la
  tercera forma del mismo defecto (salida tecleada) en esta misma sesion.
- **`_outbound/skeleton_readme.md`** — se borra (`D-164`, aplicando `D-154`: publicada la pieza, el
  borrador desaparece).
- **`_outbound/.gitkeep`** — nace (`D-165`): la carpeta declarada en `project.md` queda sin archivos
  al borrarse el borrador, y git no versiona carpetas vacias.
- **`_persistence/progress.md`** — lo escribe este cierre: `Estado general`, «Ultimo realizado»,
  «Siguiente paso» y la entrada `S-041` de la bitacora, mas su fila en el indice de sesiones.
- **`_audit/index.md`** — la fila de `S-041`, con `Pendiente` en las tres columnas que solo puede
  rellenar la auditoria.
- **`_audit/S-041.md`** (este informe) — lo anade el propio cierre.

## 2. Que NO se hizo, y por que

Tareas `No implementada` en el indice de `_persistence/tasks.md` (filtrado por la columna `Estado`,
no de memoria):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-128](#t-128---hacer-la-cosecha-de-las-lecciones-de-la-etapa-preparatoria-d-123) | Hacer la cosecha de las lecciones de la etapa preparatoria (`D-123`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-144](#t-144---dar-dueño-y-sitio-a-evaluacion-y-observabilidad-lg-54-d-134) | Dar dueño y sitio a evaluacion y observabilidad (`LG-54`, `D-134`) | No implementada | Media | No bloqueante | `005_discovery` |
| [T-156](#t-156---construir-el-esqueleto-reutilizable-de-arranque-de-proyecto-d-143) | Construir el esqueleto reutilizable de arranque de proyecto (`D-143`) | No implementada | Alta | Bloqueante | `000_preproject` |
```

(La fila de `T-039` que el `grep` tambien devuelve es un falso positivo del patron: su columna real
dice `Implementada`, y la cadena `No implementada` aparece en su titulo, no en su estado; no forma
parte de esta lista.)

`T-003` sigue `Suspendida` (no aparece en el filtro anterior porque el filtro busca
`No implementada`, no `Suspendida`).

`T-156` es la tarea paraguas del esqueleto: con `T-162` y `T-163` ya `Implementada`, lo unico que le
falta a su criterio de cierre («las seis areas agnosticas coinciden sin una linea de diferencia») es
promover `protocol-promote/SKILL.md` con la correccion de `F-112` dentro — el Paso 2f de este mismo
cierre lo devuelve como la unica linea pendiente (seccion 9). `T-128` (la cosecha real de
`000_preproject`) sigue sin bloqueo pero sin trabajarse: la etapa preparatoria no ha llegado a su
cierre. `T-001` y `T-144` siguen aplazadas por `D-143` hasta que `005_discovery` este activa.

## 3. Decisiones tomadas

### D-163 — Segunda promocion al esqueleto: sube `protocol-promote` con el defecto de `F-112`, desde `a3bb32e` a `afeedf4`

Al ir a promover `protocol-promote/SKILL.md` (ya corregido por `T-186`), dos reglas chocaban:
promover un archivo que la propia sesion acaba de corregir subiria una version sin cierre ni
auditoria (la skill lo prohibe explicitamente), y `D-160` fija la promocion **antes** de trabajar los
hallazgos. El usuario decide: se promueve **ya** la version commiteada y auditada (`R-040`), con el
«4.» duplicado de `F-112` dentro; la correccion del archivo, ya escrita aqui, sube en el lote
siguiente. Commit del esqueleto `afeedf4`, identico byte a byte al origen `a3bb32e`, subido.

**Alternativas descartadas:** (a) corregir `F-112` y promover en otra sesion — dejaria al esqueleto
una sesion mas sin el Paso 1b (67 lineas) por un defecto de numeracion; (b) corregir y promover hoy —
rompe la regla de que lo promovido ya paso por cierre y auditoria.

### D-164 — La guia de arranque se publica como `README.md` en la raiz del esqueleto (`bf8c56e`)

El borrador vivia en `_outbound/skeleton_readme.md`, puesto al dia y auditado en `S-040`/`R-040`. Se
publica **tal cual** en la raiz del esqueleto, que no tenia ningun `README`, e identico byte a byte al
origen. Por `D-154` el borrador se borra en la misma pasada.

**Alternativas descartadas:** (a) otro nombre (`START.md`, `GUIDE.md`) — `README.md` es lo que un
clone muestra primero; (b) conservar la copia de `_outbound/` — dos originales, lo que `D-154`
prohibe.

### D-165 — `_outbound/` lleva un `.gitkeep` para existir en el arbol aunque este vacia

Al aplicar `D-164` se borro el unico archivo de `_outbound/`, y git no versiona carpetas vacias: la
carpeta desaparecia del arbol, contradiciendo a `project.md` («existe en el arbol, asi que el control
no la senala») y al criterio de cierre de `D-154`. Se anade un `.gitkeep` que no se borra al publicar
una pieza — la regla «se vacia» de `D-154` se refiere a las piezas, no a este archivo.

**Alternativas descartadas:** reescribir `project.md` y `D-154` para que la carpeta solo exista
mientras tenga algo pendiente — cambia dos reglas escritas y deja al control de carpetas del cierre
senalando una carpeta declarada la mayor parte del tiempo.

## 4. Supuestos vigentes y riesgos

Codigos derivados de la columna `Estado` del indice, no de memoria:

```
$ grep -oE '^\| \[A-[0-9]+\].*Abierto \|$' _persistence/assumptions.md | grep -oE 'A-[0-9]+' | sort -u
A-001
A-002
A-003
A-004
A-005
A-006
A-007
A-008
A-009
A-011
A-012
A-013
A-014
A-015
A-018
A-019
A-020
A-021
```

Ninguno nace ni cambia de estado esta sesion. El mas relevante para lo hecho hoy es `A-019` («la
skill de promocion funciona de punta a punta»): esta sesion la ejecuto por segunda vez de punta a
punta (`D-163`, `D-164`) sin encontrar un caso que la refute, pero sigue `Abierto` — no lo confirma
`manager`, lo confirma una auditoria sobre el registro de vuelta. Si resultara falso, el esqueleto
volveria a quedarse atras sin que el mecanismo de promocion lo avisara.

## 5. Siguiente tarea propuesta

Promover `protocol-promote/SKILL.md` con la correccion de `F-112` dentro, cerrando el desfase que
deja el Paso 2f de este cierre y, con el, el criterio de `T-156` (`Alta`, `Bloqueante`). La ventana la
fija `D-160`: al abrir la sesion siguiente, antes de trabajar hallazgos nuevos.

## 6. Que pedimos auditar

- **Que las cuatro notas fechadas de `F-109` a `F-112` no reescriben ningun bloque anterior.** Es la
  misma familia de defecto que produjo `F-108` y `F-110` en las dos sesiones previas — una tercera
  reincidencia seria la senal mas fuerte de que el mecanismo (nota fechada, nunca edicion) no basta
  por si solo.
- **Que el commit del esqueleto `afeedf4` es de verdad identico byte a byte a `a3bb32e`** para
  `protocol-promote/SKILL.md`, y que lleva el «4.» duplicado de `F-112` tal como `D-163` declara a
  sabiendas — no una version ya corregida ni una distinta.
- **Que `bf8c56e` (`README.md` del esqueleto) es identico byte a byte** al borrador
  `_outbound/skeleton_readme.md` en `a3bb32e`, y que ese borrador ya no existe en el commit de esta
  sesion.
- **El punto mas debil de esta sesion:** las cinco ordenes de los criterios de cierre de `T-183` a
  `T-186` se escribieron con `<hash>` literal, a la espera del Paso 7c-bis. Si ese paso fallara o se
  saltara, esas cinco quedarian sin ancla real y sin que el Paso 2d de un cierre futuro las detectara
  como nuevas (ya estarian en `HEAD`, heredadas).

## 7. Evidencia del Paso 2d

Orden anclada al area de staging (el commit de esta sesion no existe todavia mientras se escribe esta
seccion; el Paso 7c-bis en la NOTA DE CIERRE ancla las cinco que le corresponden):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
     1	$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | uniq -c
     2	$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
     3	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show 1748f0a:.claude/skills/protocol-harvest/SKILL.md | tr -dc '\r' | wc -c
     4	$ git grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|Company_" a3bb32e -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
     5	$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' a3bb32e -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
     6	$ git grep -nE 'github\.com|gitlab|bitbucket|gmail|hotmail|outlook\.|@[A-Za-z0-9-]+\.(com|org|net|io)|[A-Za-z]:[\/][Uu]sers|/home/[a-z]|USUARIO|jdrodriguez|[Tt]riple[Ss]|[Rr]aidom|SDAI' a3bb32e -- .claude/skills/protocol-promote/SKILL.md; echo "exit=$?"
     7	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show c7d0a87:.claude/skills/protocol-promote/SKILL.md | tr -dc '\r' | wc -c
     8	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show --numstat --format='%h %s' afeedf4
     9	$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for p in 005_project.md:project.md 010_progress.md:_persistence/progress.md 015_tasks.md:_persistence/tasks.md 020_decisions.md:_persistence/decisions.md 025_constraints.md:_persistence/constraints.md 030_assumptions.md:_persistence/assumptions.md 035_lessons.md:_persistence/lessons.md 040_techdebt.md:_persistence/techdebt.md 045_audit_index.md:_audit/index.md 050_audit_findings.md:_audit/findings.md; do t=${p%%:*}; c=${p#*:}; printf '%3d  %s\n' "$(diff --strip-trailing-cr <(git -C "$ESQ" show afeedf4:_templates/000_preproject/$t) <(git -C "$ESQ" show afeedf4:$c) | grep -c '^[<>]')" "$c"; done
    10	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" ls-tree -r --long c7d0a87 -- _brief
    11	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show c7d0a87:.gitignore | grep -n '^temporal/$'
    12	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show c7d0a87:project.md | grep -c '^| Esqueleto de arranque'
    13	$ git grep -nE 'github\.com|gitlab|bitbucket|gmail|hotmail|outlook\.|@[A-Za-z0-9-]+\.(com|org|net|io)|[A-Za-z]:[\/][Uu]sers|/home/[a-z]|USUARIO|jdrodriguez|[Tt]riple[Ss]|[Rr]aidom|[Rr]aindom|SDAI|Company_' a3bb32e -- _outbound/skeleton_readme.md; echo "exit=$?"
    14	$ git grep -nE '\b(S|T|D|C|A|L|R|F|DT|N|I|FT|SC|H|LG)-[0-9]{2,3}\b' a3bb32e -- _outbound/skeleton_readme.md; echo "exit=$?"
    15	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show --numstat --format='%h %s' bf8c56e
    16	$ git ls-files _outbound
    17	$ git grep -nE 'github\.com|gitlab|bitbucket|gmail|hotmail|outlook\.|@[A-Za-z0-9-]+\.(com|org|net|io)|[A-Za-z]:[\/][Uu]sers|/home/[a-z]|USUARIO|jdrodriguez|[Tt]riple[Ss]|[Rr]aidom|[Rr]aindom|SDAI|Company_' 4962db2 -- _outbound/skeleton_readme.md; echo "exit=$?"
    18	$ git grep -nE '\b(S|T|D|C|A|L|R|F|DT|N|I|FT|SC|H|LG)-[0-9]{2,3}\b' 4962db2 -- _outbound/skeleton_readme.md; echo "exit=$?"
    19	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show bf8c56e:README.md | grep -n '^## '
    20	$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show bf8c56e:README.md | grep -cE '^\| (10|[1-9]) \| `'
    21	$ git show <hash>:_audit/S-039.md | grep -c 'T-183`, hallazgo `F-109'
    22	$ git show <hash>:_audit/S-040.md | grep -c 'T-184`, hallazgo `F-110'
    23	$ git show <hash>:_persistence/decisions.md | grep -c 'T-185`, hallazgo `F-111'
    24	$ git show <hash>:_persistence/tasks.md | grep -c '📌 \*\*Nota del 2026-09-14 (`T-1[8]5`, hallazgo `F-111'
    25	$ git show <hash>:.claude/skills/protocol-promote/SKILL.md | grep -nE '^[0-9]+\. ' | sed -n '1,5p'

$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
25
```

Reejecucion de cada una, contra el estado que va a entrar en el commit (`git show :<archivo>`, el
area de staging) o contra los commits externos ya existentes que citan:

| # | Reproduce | Salida | Nota |
|---|---|---|---|
| 1 | Si | `16 b/_persistence/decisions.md` | falso positivo conocido: `git diff -U0 <hash1> <hash2>` no lleva un solo hash tras el verbo |
| 2 | Si | `16` | mismo falso positivo que la 1 |
| 3 | Si | `0` | falso positivo conocido: forma `-C "<ruta>" show <hash>:` |
| 4 | Si | `exit=1` | falso positivo: `git grep -nE "<patron>" <hash> --` lleva el hash detras del patron |
| 5 | Si | `exit=1` | mismo falso positivo que la 4 |
| 6 | Si | `exit=1` | mismo falso positivo que la 4 |
| 7 | Si | `0` | mismo falso positivo que la 3 |
| 8 | Si | `afeedf4 Promocion del andamiaje desde RaidomAI_App (origen a3bb32e)` + `67	2	.claude/skills/protocol-promote/SKILL.md` | mismo falso positivo que la 3 |
| 9 | Si | diez filas en `0` (`project.md`, `_persistence/progress.md`, `_persistence/tasks.md`, `_persistence/decisions.md`, `_persistence/constraints.md`, `_persistence/assumptions.md`, `_persistence/lessons.md`, `_persistence/techdebt.md`, `_audit/index.md`, `_audit/findings.md`) | mismo falso positivo que la 3 |
| 10 | Si | `100644 blob e69de29bb2d1d6434b8b29ae775ad8c2e48c5391       0	_brief/client_brief.md` | mismo falso positivo que la 3 |
| 11 | Si | `5:temporal/` | mismo falso positivo que la 3 |
| 12 | Si | `3` | mismo falso positivo que la 3 |
| 13 | Si | `exit=1` | mismo falso positivo que la 4 |
| 14 | Si | `exit=1` | mismo falso positivo que la 4 |
| 15 | Si | `bf8c56e Publica la guia de arranque como README.md (origen a3bb32e)` + `187	0	README.md` | mismo falso positivo que la 3 |
| 16 | Si | `_outbound/.gitkeep` | orden sobre el arbol de trabajo, no anclable a un commit por naturaleza (lista lo que existe hoy) |
| 17 | Si | `exit=1` | mismo falso positivo que la 4 |
| 18 | Si | `exit=1` | mismo falso positivo que la 4 |
| 19 | Si | `8:## Que es esto, y que no` / `24:## 1. Arrancar un proyecto nuevo` / `52:## 2. Lo que un clone NO trae` / `66:## 3. Rellenar: los diez archivos de instancia` / `130:## 4. Lo que no se toca` / `154:## 5. El primer ciclo de trabajo` / `178:## 6. Las dos cosas que hay que entender antes de empezar` (siete lineas) | mismo falso positivo que la 3 |
| 20 | Si | `10` | mismo falso positivo que la 3 |
| 21 | No anclable hoy | — | `<hash>` literal a proposito: el commit de esta sesion no existe. Ancla el Paso 7c-bis, en `_persistence/tasks.md` |
| 22 | No anclable hoy | — | igual que la 21, en `_persistence/tasks.md` |
| 23 | No anclable hoy | — | igual que la 21, en `_persistence/tasks.md` |
| 24 | No anclable hoy | — | igual que la 21, en `_persistence/tasks.md` |
| 25 | No anclable hoy | — | igual que la 21, en `_persistence/tasks.md` |

`uniq -d` sobre la misma lista (ninguna se repite):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
```

(sin salida)

Las 20 primeras son el mismo falso positivo conocido, documentado por primera vez en `S-038`/`S-040`:
el patron estrecho del control (`git (show|grep|log|diff) [0-9a-f]{7,40}` inmediatamente despues del
verbo) no reconoce `-C "<ruta>"`, dos hashes tras `diff -U0`, ni un hash detras del patron en
`git grep`. Las cinco ultimas son las que este cierre deja para el Paso 7c-bis, unicas en
`_persistence/tasks.md` (los criterios de cierre de `T-183` a `T-186`): `decisions.md` no lleva
ninguna orden nueva con `<hash>` literal esta sesion, porque `D-163` a `D-165` citan hashes ya reales
en sus criterios de cierre.

> ## NOTA DE CIERRE
>
> **Commit sustantivo: `e332771`** (`git log -1 --format=%h` tras el commit del Paso 7).
>
> **BARRIDO DE ANCLAJE — salida:**
>
> El CENSO (universal, todo el registro, heredado incluido) y el CONTROL (acotado a lo que este
> commit anadio, forma literal `git show <hash>:` al principio de la orden):
>
> ```
> $ for f in $(git ls-tree -r --name-only e332771 _persistence _audit | grep -v '_audit/S-041.md'); do n=$(git show e332771:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
> _audit/R-025.md: 2
> _audit/R-026.md: 7
> _audit/R-027.md: 1
> _audit/R-029.md: 2
> _audit/R-030.md: 3
> _audit/R-031.md: 2
> _audit/R-032.md: 2
> _audit/R-033.md: 3
> _audit/R-034.md: 2
> _audit/R-035.md: 1
> _audit/R-037.md: 2
> _audit/R-038.md: 1
> _audit/R-039.md: 2
> _audit/R-040.md: 1
> _audit/S-017.md: 1
> _audit/S-024.md: 1
> _audit/S-025.md: 17
> _audit/S-026.md: 8
> _audit/S-027.md: 17
> _audit/S-029.md: 2
> _audit/S-030.md: 2
> _persistence/assumptions.md: 9
> _persistence/decisions.md: 16
> _persistence/tasks.md: 30
>
> $ for f in $(git diff --name-only e332771^ e332771 -- _persistence _audit ":(exclude)_audit/S-041.md"); do n=$(git diff -U0 e332771^ e332771 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
> _persistence/tasks.md: 5
> ```
>
> El CONTROL solo senala `_persistence/tasks.md`, un archivo autorizado (junto con
> `_persistence/decisions.md`, que aqui no llevaba ninguna orden nueva). Las 5 son las 5 que la
> seccion 7 de arriba lista como no anclables (lineas 21 a 25), y son exactamente las que el Paso
> 7c-bis acaba de anclar en `tasks.md` (`T-183` a `T-186`), con su salida pegada bajo cada bloque.
> **Ya no queda ninguna orden nueva de esta sesion sin forma anclada.** Todo el resto del CENSO (139
> lineas repartidas en 23 archivos) es herencia de sesiones anteriores, ya auditadas, y no lo toca
> este paso.
>
> **CONTROL DE PROSA BORRADA — salida:**
>
> ```
> $ outside() { awk '/^```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | outside > /tmp/prosa_antes; outside < "$f" > /tmp/prosa_despues; echo "== $f =="; diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'; done
> == _persistence/decisions.md ==
> == _persistence/tasks.md ==
> ```
>
> Solo las dos lineas `== … ==`: ninguna linea de prosa desaparecio en ninguno de los dos archivos.
>
> **SEGUNDA PASADA anclada del Paso 2e — salida:**
>
> ```
> $ for f in $(git diff --name-only --diff-filter=d e332771^ e332771); do n=$(git show e332771:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
> _audit/findings.md: 1
> _persistence/decisions.md: 7
> _persistence/tasks.md: 5
>
> $ git diff --name-only --diff-filter=d e332771^ e332771 | wc -l
> 11
>
> $ for f in $(git diff --name-only --diff-filter=d e332771^ e332771); do git show e332771:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
> 13
> ```
>
> El commit lleva 11 archivos (excluido el borrado `_outbound/skeleton_readme.md`), uno mas que la
> tabla de la seccion 8 de abajo (10 filas, escrita antes del commit sobre el area de staging): el que
> queda fuera es este mismo informe, `_audit/S-041.md`, que no existia todavia cuando se escribio esa
> tabla. Aporta `0` ocurrencias, asi que la suma no cambia: 13 en los dos casos. La brecha que esta
> pasada existe para ver queda cerrada sin hallazgo nuevo.
>
> **CONTROL DE CIFRA ADYACENTE — salida:**
>
> ```
> $ awk '
>   /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
>   infence { next }
>   armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
> ' _audit/S-041.md
> _audit/S-041.md:43: Con codigos y rutas, lo que muestra el diff (procedencia derivada, no de memoria — ver seccion 7):
> _audit/S-041.md:87: (La fila de `T-039` que el `grep` tambien devuelve es un falso positivo del patron: su columna real
> _audit/S-041.md:163: Ninguno nace ni cambia de estado esta sesion. El mas relevante para lo hecho hoy es `A-019` («la
> _audit/S-041.md:307: Las 13 ocurrencias son **heredadas de `HEAD`** en los tres archivos (`head` = `staged` en los tres);
> _audit/S-041.md:320: La tabla tiene 10 filas (coincide con el primer contraste) y la suma de su columna `staged`
> _audit/S-041.md:321: (0+0+0+1+0+0+7+0+0+5) da 13 (coincide con el segundo contraste). Cuadra.
> _audit/S-041.md:333: Una linea. **No es un fallo del cierre: es una promocion pendiente**, y es exactamente la que `D-163`
> _audit/S-041.md:334: dejo a sabiendas para el lote siguiente (`F-112` corregido aqui, `afeedf4` en el esqueleto todavia
> ```
>
> Lectura, linea por linea: 43 no afirma una cifra, remite a la seccion 7. 87 explica un falso
> positivo del `grep` de la seccion 2, sin cifra propia que contradecir. 163 no cita numero. 307 dice
> «13 ocurrencias» y «heredadas… cero nuevas»: coincide con el bloque de arriba (1+7+5=13, y
> `head`=`staged` en las tres filas no vacias). 320-321 leen la tabla de 10 filas y su suma 13:
> coinciden con los dos contrastes pegados justo encima. 333-334 dicen «una linea», que es la salida
> del bloque de la seccion 9. Ninguna cifra contradice a la salida que la precede.

## 8. Evidencia del Paso 2e

Primera pasada, sobre el area de staging previa al commit:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
_audit/findings.md: 1
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Contraste contra `HEAD`, para separar lo heredado de lo nuevo:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
.claude/skills/protocol-promote/SKILL.md head=0 staged=0
_audit/S-039.md head=0 staged=0
_audit/S-040.md head=0 staged=0
_audit/findings.md head=1 staged=1
_audit/index.md head=0 staged=0
_outbound/.gitkeep head=0 staged=0
_persistence/decisions.md head=7 staged=7
_persistence/lessons.md head=0 staged=0
_persistence/progress.md head=0 staged=0
_persistence/tasks.md head=5 staged=5
```

Las 13 ocurrencias son **heredadas de `HEAD`** en los tres archivos (`head` = `staged` en los tres);
**cero nuevas**.

Los dos contrastes de la tabla:

```
$ git diff --cached --name-only --diff-filter=d | wc -l
10

$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
13
```

La tabla tiene 10 filas (coincide con el primer contraste) y la suma de su columna `staged`
(0+0+0+1+0+0+7+0+0+5) da 13 (coincide con el segundo contraste). Cuadra.

⚠️ Esta seccion mide el area de staging previa al commit. La SEGUNDA PASADA anclada al commit real va
en la NOTA DE CIERRE de la seccion 7, junto al CENSO y al CONTROL del Paso 7c-bis.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-promote/SKILL.md and .claude/skills/protocol-promote/SKILL.md differ
```

Una linea. **No es un fallo del cierre: es una promocion pendiente**, y es exactamente la que `D-163`
dejo a sabiendas para el lote siguiente (`F-112` corregido aqui, `afeedf4` en el esqueleto todavia
con el defecto). Ninguna linea es `Only in` el arbol del esqueleto.

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md | wc -l
1
```
