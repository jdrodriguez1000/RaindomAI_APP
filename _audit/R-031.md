# Auditoria R-031 — sobre S-031

| Campo | Valor |
|---|---|
| Auditoria | R-031 |
| Informe auditado | `_audit/S-031.md` |
| Commit auditado | `30ce070` |
| Fecha | 2026-09-10 |
| Veredicto | Con hallazgos (2) |

🔑 **El commit auditado es `30ce070`, el sustantivo, no `eca7304`.** `git log -1 -- _audit/S-031.md`
devuelve `eca7304` (commit de anclaje, Pasos 7c/7c-bis, 3 archivos); la cabecera del informe escribe
el literal `30ce070`, y su `--stat` tiene 15 archivos, que es lo que la seccion 1 enumera.

```
$ git log --format='%h %ad %s' --date=short -- _audit/S-031.md
eca7304 2026-09-10 S-031: ancla el informe y los criterios de cierre al hash 30ce070
30ce070 2026-09-10 S-031: se aceptan F-087 a F-090 (T-136 a T-141); nace la skill protocol-harvest y se cierra el enganche del acta (T-126, T-127, T-129)

$ git show eca7304:_audit/S-031.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `30ce070` |

$ git show --stat --name-only --format= eca7304 | grep -c .
3
$ git show --stat --name-only --format= 30ce070 | grep -c .
15
```

## 1. Que se comprobo

### a) Indice contra detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show 30ce070:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show 30ce070:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete, sin diferencias.

### b) Carpetas del arbol contra las declaradas — coinciden, con las dos diferencias que tienen razon escrita

```
$ diff <(git ls-tree -d --name-only 30ce070 | sed 's|$|/|' | sort) \
       <(git show 30ce070:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos estan justificadas en `project.md` (`temporal/` excluida por `.gitignore`; `010_prototype/`
declarada por adelantado, `D-061`). No es hallazgo.

### c) Fuga de datos propios — cero lineas

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 30ce070 -- .claude CLAUDE.md _phases _methodology _templates _workflow
<sin salida>
```

Barrido adicional de codigos instanciados sobre los mismos seis:

```
$ git grep -nE '\b(T|D|C|A|L|S|R|N|I)-[0-9]{3}\b|\bF-[0-9]{3}\b|\bDT-[0-9]{3}\b' 30ce070 -- .claude CLAUDE.md _phases _methodology _templates _workflow
```

Todas las coincidencias son **series de ejemplo** (`N-001`/`N-002`/`N-003` en `CLAUDE.md` y en las
plantillas de `005_discovery`; `T-001`…`T-008` y `N-001 → FT-001 → SC-001 → …` en `_methodology/`),
ninguna con contenido del registro detras. El diff de `_phases/` de esta sesion escribe `D-XXX`
generico. Sin hallazgo.

### d) CENSO y CONTROL de la NOTA DE CIERRE — reproducen exacto

```
$ for f in $(git diff --name-only 30ce070^ 30ce070 -- _persistence _audit ":(exclude)_audit/S-031.md"); do
  n=$(git diff -U0 30ce070^ 30ce070 -- "$f" | grep -cE '^\+\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
_persistence/decisions.md: 6
_persistence/tasks.md: 13
```

El CENSO reejecutado devuelve las mismas quince filas publicadas (`R-025` 2, `R-026` 7, `R-027` 1,
`R-029` 2, `R-030` 3, `S-017` 1, `S-024` 1, `S-025` 17, `S-026` 8, `S-027` 17, `S-029` 2, `S-030` 2,
`assumptions.md` 9, `decisions.md` 21, `tasks.md` 34).

### e) El anclaje del Paso 7c-bis — 19 ordenes, ni una palabra de prosa tocada

```
$ git diff --numstat 30ce070 eca7304
152	7	_audit/S-031.md
6	6	_persistence/decisions.md
13	13	_persistence/tasks.md

$ git diff -U0 30ce070 eca7304 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^[+-][^+-]' | grep -vE '30ce070|<hash>'
<sin salida>
```

`6 + 13 = 19`, y todas las lineas cambiadas son la sustitucion `<hash>` por `30ce070`. Reejecutadas
las 14 ordenes unicas resultantes, **las 14 devuelven `1`**, que es lo publicado:

```
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show eca7304:"$f"; done \
  | grep -E '^\$ .*30ce070' | sed 's/^\$ //' | sort -u \
  | while IFS= read -r c; do printf '%s => ' "$c"; eval "$c"; done
git show 30ce070:.claude/skills/protocol-close/SKILL.md | grep -c 'DERIVA con una orden y se pega su salida, nunca se teclea' => 1
git show 30ce070:.claude/skills/protocol-close/SKILL.md | grep -c 'SEGUNDA PASADA, anclada, despues del commit' => 1
git show 30ce070:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion mide el AREA DE STAGING previa al commit' => 1
git show 30ce070:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion no escribe a mano' => 1
git show 30ce070:.claude/skills/protocol-harvest/SKILL.md | grep -c 'LA PUERTA' => 1
git show 30ce070:.claude/skills/protocol-harvest/SKILL.md | grep -c 'Uso exclusivo de manager' => 1
git show 30ce070:CLAUDE.md | grep -c 'protocol-harvest' => 1
git show 30ce070:_audit/S-030.md | grep -c 'Nota del 2026-09-10 (`F-087`)' => 1
git show 30ce070:_audit/S-030.md | grep -c 'Nota del 2026-09-10 (`F-088`)' => 1
git show 30ce070:_persistence/decisions.md | grep -c '^- 🚨 \*\*Bloques NO MIRADOS' => 1
git show 30ce070:_persistence/decisions.md | grep -cE '^### D-[0-9]+ - La cosecha la ejecuta `manager` bajo un protocolo propio' => 1
git show 30ce070:_persistence/tasks.md | grep -cE '^### T-[0-9]+ - Escribir la skill `protocol-harvest`' => 1
git show 30ce070:_phases/005_discovery.md | grep -c 'acta de cierre' => 1
git show 30ce070:_templates/000_preproject/005_project.md | grep -c 'actas de cierre de esa etapa' => 1
```

### f) SEGUNDA PASADA anclada del Paso 2e (`D-132`/`T-141`) — reproduce

```
$ for f in $(git diff --name-only --diff-filter=d 30ce070^ 30ce070); do
  n=$(git show 30ce070:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5

$ git diff --name-only --diff-filter=d 30ce070^ 30ce070 | wc -l
15
$ for f in $(git diff --name-only --diff-filter=d 30ce070^ 30ce070); do git show 30ce070:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
$ git show 30ce070:_audit/S-031.md | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

`1+4+7+5 = 17` y `15` archivos: los dos contrastes cuadran, y el archivo que la primera pasada no
podia ver (`_audit/S-031.md`) sale `0`. La correccion de `F-090` funciona sobre su propio commit.

### g) Seccion 1 del informe — recuento de hunks por archivo, verificado uno a uno

```
$ for f in <los 13 archivos con hunks declarados>; do echo "$f: $(git diff -U0 30ce070^ 30ce070 -- "$f" | grep -c '^@@')"; done
.claude/skills/protocol-close/SKILL.md: 4
CLAUDE.md: 1
_audit/S-030.md: 2
_audit/findings.md: 5
_audit/index.md: 1
_persistence/assumptions.md: 3
_persistence/decisions.md: 2
_persistence/lessons.md: 3
_persistence/progress.md: 10
_persistence/tasks.md: 13
_phases/000_preproject.md: 1
_phases/005_discovery.md: 2
_templates/000_preproject/005_project.md: 1
```

Los trece coinciden con lo declarado. `.claude/skills/protocol-harvest/SKILL.md` nace entero y tiene
nueve pasos (`Paso 0` a `Paso 8`) con `LA PUERTA` en el `Paso 5`, como afirma la seccion 1.

### h) Primera viñeta de la seccion 6 — no queda un cuarto caso de atribucion posicional

Barrido independiente: se extrae el cuerpo de cada entrada `### CODIGO` en `30ce070^` y en `30ce070`
y se comparan uno a uno con `cmp`.

```
$ for f in tasks decisions lessons assumptions progress; do
    ... extraer cuerpo por codigo en before/ y after/, luego cmp -s por codigo ...
  done
== tasks: entradas preexistentes con cuerpo modificado ==
  T-126
  T-127
  T-129
  T-135
== decisions: entradas preexistentes con cuerpo modificado ==
  D-129
== lessons: entradas preexistentes con cuerpo modificado ==
  L-010
  L-044
== assumptions: entradas preexistentes con cuerpo modificado ==
  A-017
== progress: entradas preexistentes con cuerpo modificado ==
  S-030
```

`T-135`, `D-129` y `L-044` son los tres bordes que el informe declara corregidos a mano: eran la
**ultima** entrada de su archivo, asi que la insercion posterior cae dentro de su rango. `S-030` es
el mismo artefacto: lo unico que cambia dentro de su rango es que la seccion `## 6. Mapa de
persistencia` pasa a quedar detras de `S-031`.

```
$ diff progress-before/S-030.txt progress-after/S-030.txt
36,46d35
< ## 6. Mapa de persistencia
< ... (la tabla entera, ninguna edicion del cuerpo de S-030)
```

Las ediciones reales son `T-126`, `T-127`, `T-129`, `L-010` y `A-017` — **las cinco declaradas**. No
hay un cuarto caso sin corregir.

### i) Cuarta viñeta de la seccion 6 — el segundo barrido independiente que pedian, y confirma

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    diff <(git show 30ce070^:_persistence/$f.md | grep -oE '^### [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 30ce070:_persistence/$f.md  | grep -oE '^### [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) | grep '^>'
  done
-- tasks:      T-136 T-137 T-138 T-139 T-140 T-141 T-142 T-143 T-144
-- decisions:  D-130 D-131 D-132 D-133 D-134
-- lessons:    L-045 L-046
-- progress:   S-031
-- constraints / assumptions / techdebt: (ninguna)
```

Por diferencia de conjuntos —camino distinto al `grep -E '^\+### '` que uso el cierre— nacen
exactamente `T-136` a `T-144` y `D-130` a `D-134`. El supuesto se sostiene.

### j) Tercera viñeta de la seccion 6 — la clasificacion de la linea 21 es correcta

`grep -n 'assumptions' .claude/skills/protocol-start/SKILL.md` es la evidencia que `T-143` cito al
abrirse, tomada sobre el arbol de trabajo, y no describe el commit de la sesion: no tiene `<hash>`
que anclar. Reejecutada reproduce lo publicado en `tasks.md` (lineas 3, 155, 224 y 287).

### k) Seccion 2 del informe — la lista de `No implementada` reproduce

```
$ git show 30ce070:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -c "No implementada"
10
$ git show 30ce070:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep "No implementada" | grep -oE '^\| \[T-[0-9]+' | grep -oE 'T-[0-9]+'
T-001 T-002 T-003 T-037 T-038 T-039 T-112 T-128 T-143 T-144
```

Diez filas, y el falso positivo `T-039` esta declarado y es real: su titulo contiene la frase, su
columna `Estado` es `Implementada`.

### l) Estados de las tareas que la seccion 0 invoca

```
$ git show 30ce070:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -E '\[T-(126|127|129|13[6-9]|14[0-2])\]'
T-126 Implementada · T-127 Implementada · T-129 Implementada · T-136 Implementada · T-137 Implementada
T-138 Implementada · T-139 Implementada · T-140 Implementada · T-141 Implementada · T-142 Implementada
```

### m) Fecha del informe contra la del commit (Paso 7d) — cuadra

```
$ git log -1 --format='%h %ad' --date=short 30ce070
30ce070 2026-09-10
```

El informe declara `Fecha | 2026-09-10`, y la fila `S-031` del tablero tambien.

### n) `findings.md` en `30ce070` — `manager` no escribio `Implementado`

```
$ git show 30ce070:_audit/findings.md | grep -E '^\| \[?F-(08[7-9]|090)'
F-087 | R-030 | Media | Aceptado — pendiente
F-088 | R-030 | Baja  | Aceptado — pendiente
F-089 | R-030 | Media | Aceptado — pendiente
F-090 | R-030 | Media | Aceptado — pendiente
```

Las cuatro filas y sus cuatro entradas de detalle dicen `Aceptado — pendiente`, con su `T-XXX`
citada. La palabra `Implementado` de la seccion 0 del informe es el veredicto del cierre sobre su
propio trabajo, no un estado escrito en `findings.md`. Correcto.

## 2. Hallazgos

### F-091 — La seccion 7 y la NOTA DE CIERRE de `S-031` publican dos recuentos tecleados que su propia lista de 21 lineas desmiente

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-031.md`, seccion 7 (linea 280) y NOTA DE CIERRE (linea 305) |

- **Que se observo:** la lista del Paso 2d tiene **21** lineas, de las que **19** llevan `<hash>`.

```
$ git show eca7304:_audit/S-031.md | sed -n '/^## 7. Evidencia del Paso 2d/,/^> ## NOTA DE CIERRE/p' | grep -cE '^ +[0-9]+\s+\+\$ '
21
$ git show eca7304:_audit/S-031.md | sed -n '/^## 7. Evidencia del Paso 2d/,/^> ## NOTA DE CIERRE/p' | grep -E '^ +[0-9]+\s+\+\$ ' | grep -c '<hash>'
19
```

  Contra eso, el informe escribe dos cifras que no salen de ninguna orden:

```
$ git show eca7304:_audit/S-031.md | grep -n 'las 20 lineas 1 a 20'
280:Reejecucion de cada una — las 20 lineas 1 a 20 llevan `<hash>` literal **a proposito**: el commit de

$ git show eca7304:_audit/S-031.md | grep -n 'restantes de las 21'
305:> prosa. Las 14 restantes de las 21 lineas del Paso 2d (seccion 7, arriba) no llevaban `<hash>`
```

  La primera es falsa por una: la linea 15 del bloque esta dentro del rango 1-20 y **no** lleva
  `<hash>`, sino el hash historico `9b3f9ee`.

```
$ git show eca7304:_audit/S-031.md | sed -n '/^## 7. Evidencia del Paso 2d/,/^> ## NOTA/p' | grep -E '^ +15\s'
    15	+$ git diff --name-only --diff-filter=d 9b3f9ee^ 9b3f9ee | wc -l
```

  La segunda es falsa por doce: `21 - 19 = 2`, no `14`. Y se contradice con la frase que la sigue en
  la misma oracion —«las otras 20 ya estan contadas en las 19 del CONTROL mas la unica linea no
  anclable»—, que si describe el reparto correcto (`19 + 1 + 1 = 21`).
- **Por que importa:** la afirmacion de fondo se sostiene —las 19 estan ancladas y las 14 unicas
  reproducen `1`, verificado en la seccion 1e de esta auditoria—, pero es **reincidencia del mismo
  defecto que esta sesion acaba de aceptar dos veces**: `F-087` (cifra tecleada al lado de la orden
  que la desmiente) y `F-088` (desglose de la NOTA DE CIERRE que no cuadra con su cifra). Ocurre
  ademas en el commit que estrena `D-131`, la decision que prohibe exactamente esto: un desglose que
  explica una cifra en la NOTA DE CIERRE se deriva con una orden, o no se publica. Una regla que se
  incumple en el commit que la escribe no se puede dar por vigente sin comprobarlo.
- **Que lo corregiria:** derivar las dos cifras con una orden sobre el bloque ya publicado —el mismo
  `grep -c` que esta ficha usa arriba— y pegar su salida, en vez de teclearlas. ⚠️ Es una
  recomendacion, no una orden.

### F-092 — La seccion 4 enumera once codigos de supuesto y afirma que «completan los catorce»

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-031.md`, seccion 4 |

- **Que se observo:** la seccion 4 publica correctamente la salida cruda de su orden (`14`) y a
  continuacion escribe: «`A-006` a `A-009`, `A-011` a `A-015` (nueve codigos) mas `A-004` y `A-005`
  (citados en «Bloqueos activos» de `progress.md`) completan los catorce». Son `4 + 5 + 2 = 11`.
  Faltan `A-001`, `A-002` y `A-003`.

```
$ git show 30ce070:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -cE 'Abierto'
14
$ git show 30ce070:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -E 'Abierto' | grep -oE 'A-[0-9]+' | sort -u | tr '\n' ' '
A-001 A-002 A-003 A-004 A-005 A-006 A-007 A-008 A-009 A-011 A-012 A-013 A-014 A-015
```

- **Por que importa:** el parrafo dice literalmente que no transcribe la lista «para no volver a
  cometer el error que `F-087` acaba de corregir», y a renglon seguido transcribe una lista parcial
  presentandola como completa. La omision no es inocua: `A-003` es justamente el supuesto que la
  propia seccion 4 destaca dos lineas mas abajo como el que dejaria «el ciclo entero del producto en
  duda», y aparece nombrado alli pero fuera del recuento. La regla nueva de la plantilla —«la lista
  de codigos si se escribe; la cifra sale de la orden o no sale»— se cumplio a medias: la cifra es
  buena, la lista es incompleta, y ademas se declara completa.
- **Que lo corregiria:** derivar tambien la lista de codigos con la misma orden
  (`... | grep -oE 'A-[0-9]+' | sort -u`) y pegarla, o suprimir el verbo «completan». ⚠️ Es una
  recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-087` | **Implementado** en `30ce070` | La nota fechada existe en `_audit/S-030.md` (`git show 30ce070:_audit/S-030.md \| grep -c 'Nota del 2026-09-10 (F-087)'` devuelve `1`) y su orden reproduce (`git show 9b3f9ee:_persistence/assumptions.md \| sed -n '/^## Indice/,/^---/p' \| grep -cE 'Abierto'` devuelve `15`). La plantilla de la seccion 4 exige salida cruda (`grep -c 'esta seccion no escribe a mano'` devuelve `1`). La prosa anterior no se reescribio |
| `F-088` | **Implementado** en `30ce070` | Nota fechada presente (`grep -c 'Nota del 2026-09-10 (F-088)'` devuelve `1`); su desglose derivado, reejecutado, devuelve `D-126 1 / D-128 3 / D-129 1 / T-134 1` y los totales `12` y `9`, exactamente lo publicado. `D-131` esta en la skill (`grep -c 'DERIVA con una orden y se pega su salida, nunca se teclea'` devuelve `1`) |
| `F-089` | **Implementado** en `30ce070` | `git diff 30ce070^ 30ce070 -- _templates/000_preproject/005_project.md` muestra la fila `_audit/` reescrita con «las **actas de cierre de esa etapa**», en forma generica, igualando a `project.md` |
| `F-090` | **Implementado** en `30ce070` | `D-132` esta en la skill (`grep -c 'SEGUNDA PASADA, anclada, despues del commit'` devuelve `1`, y `grep -c 'esta seccion mide el AREA DE STAGING previa al commit'` devuelve `1`), y la segunda pasada corrio **sobre este mismo commit**: 15 archivos, suma 17, y `_audit/S-031.md` en `0` (seccion 1f de esta auditoria) |

⚠️ **`F-091` no reabre `F-087` ni `F-088`.** Las correcciones que esos dos hallazgos pedian estan en
el diff y funcionan; lo que `F-091` observa es un caso **nuevo**, en el informe nuevo, con evidencia
nueva.

## 4. Lo que NO se pudo comprobar

Ninguno. Los tres controles mecanicos corrieron enteros, y todas las ordenes que el informe publica
se reejecutaron.

## 5. Recomendaciones sin hallazgo

- **La segunda viñeta de la seccion 6 pregunta si es deliberado que `T-143` y `T-144` no lleven
  bloque de verificacion anclado en su «Criterio de cierre».** No es un defecto contra ninguna regla
  escrita: las dos estan `No implementada`, `PI-5` exige el bloque para dar una tarea por Terminada, y
  el patron no es uniforme en el registro. Lo que si conviene es **decidir cual de las dos formas es
  la buena y escribirlo**, porque hoy conviven sin criterio y el Paso 7c-bis solo puede anclar las que
  ya traen orden.

```
$ for t in T-001 T-003 T-037 T-038 T-112 T-128 T-143 T-144; do ... bloques de codigo tras «Criterio de cierre» ... done
T-001 No implementada  bloques_tras_criterio=0
T-003 No implementada  bloques_tras_criterio=0
T-037 No implementada  bloques_tras_criterio=2
T-038 No implementada  bloques_tras_criterio=2
T-112 No implementada  bloques_tras_criterio=0
T-128 No implementada  bloques_tras_criterio=2
T-143 No implementada  bloques_tras_criterio=0
T-144 No implementada  bloques_tras_criterio=0
```

- **`T-038` («Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close`») sigue
  `No implementada`.** Esta auditoria tuvo que componer el patron de fuga a mano desde `project.md`,
  porque la skill lo deja entre angulos. No es defecto de esta sesion, pero cada pasada de auditoria
  paga el mismo minuto y el patron compuesto puede no ser el mismo que el del cierre — que es
  exactamente el riesgo que `T-038` describe.
