# Auditoria R-035 — sobre S-035

| Campo | Valor |
|---|---|
| Auditoria | R-035 |
| Informe auditado | `_audit/S-035.md` |
| Commit auditado | `cce48e0` |
| Fecha | 2026-09-11 |
| Veredicto | Con hallazgos (2) |

⚠️ **El commit auditado no es el que devuelve `git log -1 -- _audit/S-035.md`.** Esa orden devuelve
`441d260`, que es el **commit de anclaje** del Paso 7c y toca tres archivos. Manda el hash literal de
la cabecera del informe:

```
$ git log -1 --format=%h -- _audit/S-035.md
441d260
$ git show 441d260:_audit/S-035.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `cce48e0` |
$ git show --stat --name-only --format= 441d260 | grep -c .
3
$ git show --stat --name-only --format= cce48e0 | grep -c .
9
```

La seccion 1 del informe enumera nueve archivos, que es lo que trae `cce48e0`. Se audita ese.

---

## 1. Que se comprobo

### a) Indice ↔ detalle de `_persistence/`, sobre el commit auditado

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show cce48e0:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show cce48e0:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

**Coherentes los siete.**

### b) Carpetas del arbol contra las declaradas

```
$ diff <(git ls-tree -d --name-only cce48e0 | sed 's|$|/|' | sort) \
       <(git show cce48e0:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

**Las dos diferencias tienen su motivo escrito en `project.md`** (`temporal/` excluida por
`.gitignore`; `010_prototype/` declarada por adelantado, `D-061`). No es hallazgo.

### c) Fuga de datos propios en las seis areas agnosticas

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" cce48e0 -- .claude CLAUDE.md _phases _methodology _templates _workflow
$ echo $?
1
```

**Cero lineas.**

### d) Fuga por codigos instanciados en `_phases/` y `_workflow/`

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' cce48e0 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
$ echo $?
1
```

**Cero lineas.**

### e) El informe contra el diff

**La lista de archivos y el recuento de hunks de la seccion 1 reproducen, uno por uno:**

```
$ git show --stat cce48e0 --format='%H%n%ad' --date=short
cce48e07e0b619948d76d9292fb72dcdb486e9cf
2026-09-11
 .claude/skills/protocol-close/SKILL.md |  33 +++
 _audit/S-034.md                        |  67 +++++
 _audit/S-035.md                        | 505 +++++++++++++++++++++++++++++++++
 _audit/findings.md                     |  12 +-
 _audit/index.md                        |   1 +
 _persistence/decisions.md              | 143 +++++++++-
 _persistence/lessons.md                |  27 ++
 _persistence/progress.md               | 211 +++++++++-----
 _persistence/tasks.md                  | 269 +++++++++++++++++-
 9 files changed, 1182 insertions(+), 86 deletions(-)

$ for f in $(git diff --name-only cce48e0^ cce48e0); do echo "$f: $(git diff cce48e0^ cce48e0 -- $f | grep -c '^@@')"; done
.claude/skills/protocol-close/SKILL.md: 1
_audit/S-034.md: 1
_audit/S-035.md: 1
_audit/findings.md: 3
_audit/index.md: 1
_persistence/decisions.md: 5
_persistence/lessons.md: 2
_persistence/progress.md: 4
_persistence/tasks.md: 5
```

La fecha del commit (`2026-09-11`) es la que el informe escribe en su cabecera: el Paso 7d cuadra.

**Lo que la seccion 1 dice que nace, nace:**

```
$ git diff cce48e0^ cce48e0 -- _persistence/decisions.md _persistence/tasks.md _persistence/lessons.md _audit/findings.md | grep -E '^\+### '
+### D-149 - La orden de la seccion 1 se comprueba por cadena literal, antes del commit de anclaje
+### D-150 - Un proyecto nuevo arranca por clone, anota el hash y parte con historial limpio
+### L-055 - Un aviso movido a la plantilla sigue copiandose a mano
+### T-164 - Fijar por nota fechada la orden de la seccion 1 de `S-034` (`F-098`)
+### T-165 - Fijar por nota fechada la descripcion de la vineta de `findings.md` en `S-034` (`F-099`)
+### T-166 - Escribir el Paso 7c-quater: la orden de la seccion 1 por cadena literal (`D-149`, `F-098`)
+### T-167 - Anadir a la plantilla de `project.md` las dos filas del esqueleto de origen (`D-150`)
+### T-168 - Corregir las tres citas cruzadas de `D-146`, `D-147` y `D-148`
```

**`findings.md` cambia de estado y no gana ninguna entrada ni ninguna nota, como afirma el informe:**

```
$ git diff cce48e0^ cce48e0 -- _audit/findings.md | grep -E '^[-+]\| Estado \|'
-| Estado | Abierto |
+| Estado | Aceptado — pendiente |
-| Estado | Abierto |
+| Estado | Aceptado — pendiente |
$ git diff cce48e0^ cce48e0 -- _audit/findings.md | grep -cE '^\+.*Nota del'
0
$ git diff cce48e0^ cce48e0 -- _audit/findings.md | grep -cE '^\+### '
0
```

**Todo lo que el commit borra de `decisions.md` y `tasks.md` esta explicado en el informe:**

```
$ git diff cce48e0^ cce48e0 -- _persistence/decisions.md _persistence/tasks.md | grep -E '^-' | grep -vE '^---'
-  ejecutor. Lo implementa `T-160`.
-  del caso invertido. Lo implementa `T-162`.
-  va al informe. Lo implementa `T-161`.
-| [T-157](#t-157---crear-el-repositorio-del-esqueleto-y-commitear-su-estado-actual-d-145) | Crear el repositorio del esqueleto y commitear su estado actual (`D-145`) | No implementada | Alta | Bloqueante | `000_preproject` |
-| Estado | No implementada |
-  que rellenar.
```

Las tres primeras son las citas cruzadas de `T-168`; la cuarta y la quinta, el paso de `T-157` a
`Implementada`; la sexta es el criterio de cierre de `T-162`, que se **amplia** (`D-150`), no se
borra. **Ninguna prosa desaparece**, y `_audit/S-034.md` es todo adiciones:

```
$ git diff cce48e0^ cce48e0 -- _audit/S-034.md | grep -c '^-'
1
```

(esa unica linea es la cabecera `--- a/_audit/S-034.md` del propio diff).

### f) Los recuentos y listas que el informe publica, reejecutados

```
$ git show cce48e0:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | awk -F'|' '$4 ~ /No implementada/' | grep -oE '^\| \[T-[0-9]+\]' | grep -oE 'T-[0-9]+' | tr '\n' ' '
T-001 T-128 T-144 T-156 T-158 T-159 T-160 T-161 T-162 T-163 T-167
$ git show cce48e0:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | awk -F'|' '$4 ~ /No implementada/' | wc -l
11
$ git show cce48e0:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | awk -F'|' '$4 ~ /Suspendida/' | grep -oE 'T-[0-9]+'
T-003
$ git show cce48e0:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | awk -F'|' '$5 ~ /Abierto/' | grep -oE 'A-[0-9]+' | sort -u | tr '\n' ' '
A-001 A-002 A-003 A-004 A-005 A-006 A-007 A-008 A-009 A-011 A-012 A-013 A-014 A-015 A-018
$ git show cce48e0:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | awk -F'|' '$5 ~ /Abierto/' | wc -l
15
```

**Los cinco reproducen exactamente lo publicado en las secciones 2 y 4.**

Y las dos lineas que la seccion 1 cita de `_audit/S-034.md`:

```
$ git show cce48e0:_audit/S-034.md | grep -n '^## ' | head -3
11:## 0. Respuesta a la auditoria anterior
21:## 1. Que se hizo
155:## 2. Que NO se hizo, y por que
```

### g) El CONTROL de anclaje del Paso 7c-bis, reejecutado

```
$ for f in $(git diff --name-only cce48e0^ cce48e0 -- _persistence _audit ":(exclude)_audit/S-035.md"); do
    n=$(git diff -U0 cce48e0^ cce48e0 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"
  done
_persistence/decisions.md: 2
_persistence/tasks.md: 7
```

Nueve ordenes por anclar, y el commit de anclaje las ancla las nueve, sin dejar residuo de las que
esta sesion anadio:

```
$ git diff 441d260^ 441d260 -- _persistence/decisions.md _persistence/tasks.md | grep -cE '^\+\$ git show cce48e0:'
9
$ git diff -U0 441d260^ 441d260 | grep -cE '^\+\$ git show <hash>:'
0
```

**Y el anclaje no borra prosa:**

```
$ outside() { awk '/^```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f"; diff <(git show cce48e0:"$f" | outside) <(git show 441d260:"$f" | outside) | grep '^<'; done
== _persistence/decisions.md
== _persistence/tasks.md
```

### h) La orden que el Paso 2d corrigio (seccion 6, punto 5), reejecutada

```
$ git show 2cef150:_persistence/decisions.md | grep -n 'Lo implementa `T-1[56][0-9]`'
9428:  ejecutor. Lo implementa `T-160`.
9460:  del caso invertido. Lo implementa `T-162`.
9511:  va al informe. Lo implementa `T-161`.
$ git show cce48e0:_persistence/decisions.md | grep -n 'Lo implementa `T-1[56][0-9]`'
9447:  ejecutor. Lo implementa `T-161`.
9483:  del caso invertido. Lo implementa `T-159`.
9538:  va al informe. Lo implementa `T-160`.
9587:  tres filas y su `SIN COMPROBAR`. Lo implementa `T-166`.
```

**Las dos reproducen literalmente lo que `T-168` publica**, antes y despues. La correccion que el
cierre declara en su seccion 6 punto 5 **no toco nada mas**: `T-168` nace entera en este commit, y las
unicas lineas borradas de `decisions.md` son las tres citas cruzadas (ver 1e).

### i) El mecanismo que `D-149` encarga, sobre el commit

```
$ git show cce48e0:.claude/skills/protocol-close/SKILL.md | grep -c '^### 7c-quater'
1
$ git show cce48e0:.claude/skills/protocol-close/SKILL.md | grep -c "grep -qF 'git show --stat --name-only --format='"
1
$ git show cce48e0:.claude/skills/protocol-close/SKILL.md | grep -c 'FALTA en la seccion 1'
1
```

El paso lleva su tabla de tres filas, incluida la de `SIN COMPROBAR`, y declara lo que **no** cubre.

### j) La evidencia externa de `T-157`, la unica que no vive en este repositorio

El informe pide expresamente que se juzgue (seccion 6, punto 2). De las ordenes de su bloque, las que
interrogan al servicio externo **si son reejecutables desde aqui, y reproducen**:

```
$ gh repo view jdrodriguez1000/SDAI_TripleS --json name,visibility,defaultBranchRef,url
{"defaultBranchRef":{"name":"main"},"name":"SDAI_TripleS","url":"https://github.com/jdrodriguez1000/SDAI_TripleS","visibility":"PRIVATE"}
$ gh api repos/jdrodriguez1000/SDAI_TripleS/commits/main --jq '{sha:.sha,msg:.commit.message}'
{"msg":"Estado de partida del esqueleto, tal como estaba antes de sincronizar\n\nPrimer commit del esqueleto de arranque de proyectos (D-145). ...","sha":"fa7da56084d378c975bcfb52ed73129d3240acaf"}
```

El `sha` corto es `fa7da56`, el mismo que el bloque publica. **El criterio de cierre de `T-157`
—«al menos un commit y un remoto configurado, y `git status -sb` no dice `ahead`»— queda verificado
desde este repositorio**, y `Implementada` se sostiene. Que `git init`, `git add` y `gh repo create`
no se puedan reejecutar no es un defecto: son ordenes que crean algo, y lo que se audita es que ese
algo exista. No es hallazgo.

### k) Los otros puntos de la seccion 6 del informe

- **Punto 1** — los dos hallazgos marcados `Aceptado — pendiente` aunque la correccion este en el
  commit: **la etiqueta es la correcta**. `CLAUDE.md` reserva `Implementado` a la auditoria, y
  `findings.md` en `cce48e0` dice lo mismo que la seccion 0 del informe. No esconde trabajo: las dos
  correcciones estan en el diff y se cierran en esta auditoria (seccion 3).
- **Punto 3** — el segundo barrido sin su patron: **es hallazgo**, `F-101`.
- **Punto 4** — las ordenes con `...` y con `<carpeta desechable>`: **no es hallazgo**, va a la
  seccion 5.
- **Punto 5** — verificado en 1h.
- **Punto 6** — `D-149` decide no poner control a `F-099`: la decision esta registrada, con sus
  cuatro alternativas descartadas y su criterio explicito, y `L-055` lo razona. **No es hallazgo**;
  discrepar de ella seria opinion, no evidencia.
- **Punto 7** — el riesgo sin codigo: va a la seccion 5.

---

## 2. Hallazgos

### F-100 — La bitacora de `S-035` afirma que `project.md` gana dos filas nuevas, y el commit no toca `project.md`
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/progress.md`, entrada `S-035` de la bitacora |

- **Que se observo:**

```
$ git show cce48e0:_persistence/progress.md | grep -n 'gana dos filas nuevas'
1648:  irreversible. `project.md` gana dos filas nuevas (esqueleto de origen y version de partida), y nace
$ git show --stat --name-only --format= cce48e0 | grep -c 'project.md'
0
$ git diff --stat cce48e0^ cce48e0 -- project.md
$ git show cce48e0:project.md | grep -ciE 'esqueleto de origen|version de partida'
0
$ git grep -ciE 'esqueleto de origen|version de partida' cce48e0 -- _templates/000_preproject
$ git show cce48e0:_persistence/tasks.md | sed -n '/^### T-167 /,/^| Estado/p' | tail -1
| Estado | No implementada |
```

  `project.md` no esta entre los nueve archivos del commit, no cambio, y no contiene esas filas — ni
  el, ni su plantilla. Lo que `D-150` prescribe es que el `project.md` **de un proyecto nuevo** las
  lleve, y `T-167` —`No implementada`— es la tarea que las llevara a la plantilla.

- **Por que importa:** la bitacora es la parte **permanente** de `progress.md`; `Estado general` y
  `Ultimo realizado` se reescriben en el cierre siguiente. Los otros dos sitios del mismo archivo lo
  dicen bien —«nace `T-167` para llevar esas dos filas a la plantilla de `project.md`»—, asi que el
  registro se contradice consigo mismo y el unico texto que sobrevive a la proxima sesion es el que
  afirma un cambio que no ocurrio. Media y no Alta porque el trabajo real esta bien registrado en
  `D-150` y en `T-167`: lo que falla es la cronica, no la decision.
- **Que lo corregiria:** una nota fechada en la entrada `S-035` de la bitacora con la formulacion
  correcta, sin reescribir la prosa ya commiteada. ⚠️ Es una recomendacion, no una orden.

### F-101 — El bloque de verificacion de `T-157` publica el resultado de un segundo barrido sin su patron ni su salida
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md`, `T-157` |

- **Que se observo:**

```
$ git show cce48e0:_persistence/tasks.md | sed -n '/^### T-157 /,/^### T-158 /p' | grep -n '^\$ \|Un segundo patron'
26:$ cd C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS && git init -b main
29:$ git add -A && git status --short | wc -l
32:$ git log --oneline -1
35:$ gh repo create SDAI_TripleS --private --source=. --remote=origin --push
40:$ git status -sb
43:$ gh repo view jdrodriguez1000/SDAI_TripleS --json name,visibility,defaultBranchRef,url
53:$ grep -rniE "raidom|raindom|Proyectos_TripleS|github\.com|vercel|jdrodriguez" C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS --include=*.md
57:- ⚠️ **Un segundo patron, por codigos instanciados, si devolvio lineas — y no son fuga.** Son las
63:$ sed -n '458,465p' .../SDAI_TripleS/_methodology/sources/005_vertical.md
```

  Despues del push solo hay dos ordenes: la **53**, que es el barrido de datos propios con su patron
  completo, y la **63**, que es un `sed` de contexto de una coincidencia. **Ninguna orden publicada
  corresponde al «segundo patron»** que la linea 57 declara haber corrido, ni se publica su ambito ni
  su salida.

- **Por que importa:** `CLAUDE.md` lo prohibe literalmente —«si el registro afirma un resultado […]
  ese resultado va con **el patron y el ambito** con que se obtuvo, aunque nadie lo haya pedido»— y da
  la razon: sin la orden hay que rehacer el barrido entero para contrastarlo. Y aqui el resultado
  afirmado decide algo: que la fuga de codigos instanciados del esqueleto recien publicado son series
  de ejemplo legitimas. El patron es justo lo que determina el resultado —el control equivalente de
  este repositorio acota a dos carpetas y excluye `PI-`—, y de lo que se corrio alli no se sabe ni el
  ambito ni la exclusion. Media y no Alta porque la conclusion es plausible y el defecto es de
  evidencia, no de fondo. El propio cierre lo señalo en su seccion 6 y no lo arreglo, que es lo
  correcto: la entrada la escribio `manager`.
- **Que lo corregiria:** una nota fechada en `T-157` con el patron, el ambito y la salida cruda del
  segundo barrido; o, si ya no se puede reconstruir lo que se corrio entonces, decirlo asi y publicar
  el barrido de hoy con su fecha. Sin reescribir la prosa ya commiteada. ⚠️ Es una recomendacion, no
  una orden.

---

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-098` | **Implementado** (`cce48e0`) | la nota fechada esta en `_audit/S-034.md` y el mecanismo en `protocol-close` |
| `F-099` | **Implementado** (`cce48e0`) | la nota fechada esta en `_audit/S-034.md`, con sus cuatro ordenes |

**`F-098` — la correccion, verificada sobre el diff:**

```
$ git diff cce48e0^ cce48e0 -- _audit/S-034.md | grep -cE '^\+.*Nota del 2026-09-11 \(`F-098`, `T-164`\)'
1
$ git show cce48e0:_audit/S-034.md | grep -c 'git show --stat --name-only --format= 2cef150'
1
$ git show cce48e0:.claude/skills/protocol-close/SKILL.md | grep -c '^### 7c-quater'
1
```

La nota publica la orden prescrita con su salida **y** lo que devuelve de mas la orden defectuosa, sin
reescribir la prosa original —`_audit/S-034.md` es todo adiciones (ver 1e)—. Y el hallazgo pedia
«valorar si el Paso 7c debe fijar esa orden por cadena literal»: el Paso 7c-quater lo hace, con su
tabla de tres filas y diciendo explicitamente lo que no cubre. **Corregido de fondo y con mecanismo.**

**`F-099` — la correccion, verificada sobre el diff:**

```
$ git diff cce48e0^ cce48e0 -- _audit/S-034.md | grep -cE '^\+.*Nota del 2026-09-11 \(`F-099`, `T-165`\)'
1
$ git show cce48e0:_audit/S-034.md | grep -c '\*\*Ninguna nota anadida\.\*\*'
1
```

La nota fija la descripcion correcta —tres filas de `Abierto` a `Aceptado — pendiente`, tres
`Registrado en`, ninguna nota— en bloque citado, y la sostiene con cuatro ordenes. Es exactamente lo
que el hallazgo pedia. Que `D-149` decida **no** escribirle mecanismo no lo deja a medias: el defecto
concreto esta corregido, y la decision de no mecanizar esta registrada y razonada.

---

## 4. Lo que NO se pudo comprobar

Ninguno. Los cuatro controles mecanicos corrieron sobre el commit auditado, y las dos ordenes del
bloque de `T-157` que interrogan a un servicio externo se pudieron reejecutar desde aqui (seccion 1j).

⚠️ **Queda una cosa fuera de alcance, y no es un control fallido sino un limite:** el contenido del
repositorio `SDAI_TripleS` no es este repositorio, asi que lo que `T-157` afirma **de dentro** de aquel
arbol —los 80 archivos, el desfase de siete archivos, `protocol-harvest` ausente— se juzga por lo que
el registro publica, no por evidencia propia. `F-101` es precisamente el caso en que eso no basta.

---

## 5. Recomendaciones sin hallazgo

- **Las ordenes que no se pueden pegar y correr** (seccion 6, punto 4 del informe): el `sed` de
  `T-157` lleva la ruta elidida con `...`, y las tres del clone de `T-163` llevan `<carpeta
  desechable>` como marcador. No se abren como hallazgo porque en los dos casos la salida publicada
  es real y el marcador esta declarado como tal, pero **una convencion escrita para los marcadores de
  ruta** —o simplemente no elidir— cerraria la duda de golpe. La elision del `sed` es la mas facil de
  arreglar: la ruta completa ya esta unas lineas mas arriba, en el mismo bloque.
- **El riesgo sin codigo que el informe nombra en su seccion 4** (las citas cruzadas que ningun
  control detecta, encontradas por leer de corrido). El informe hace bien en decir que un informe es
  canal y no registro. **Merece un `A-XXX` o un `DT-XXX`**: `T-168` corrigio tres en un solo bloque,
  lo que sugiere que la renumeracion que las produjo pudo dejar mas, y hoy nada las buscaria. La
  decision es de `manager`.
- **Una imprecision en `Ultimo realizado` de `progress.md`**, que no se abre como hallazgo porque esa
  seccion se reescribe en el cierre siguiente y la bitacora —la permanente— lo dice bien: afirma que
  «el barrido de fuga de datos **y de codigos instanciados** sobre el arbol entero del esqueleto sale
  limpio», cuando el segundo devolvio lineas segun `T-157`. Es la misma zona que `F-101`.

```
$ git show cce48e0:_persistence/progress.md | grep -n 'de codigos instanciados sobre el arbol entero'
121:mecanismo hacia falta. El barrido de fuga de datos y de codigos instanciados sobre el arbol entero del
```
