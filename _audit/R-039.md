# Auditoria R-039 — sobre S-039

| Campo | Valor |
|---|---|
| Auditoria | R-039 |
| Informe auditado | `_audit/S-039.md` |
| Commit auditado | `62ff0d2` |
| Fecha | 2026-09-14 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### a) Sobre que commit se audita

```
$ git log -1 --format=%h -- _audit/S-039.md
e04681b

$ git show e04681b:_audit/S-039.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `62ff0d2` |

$ git show --stat --name-only --format= e04681b
_audit/S-039.md
_persistence/decisions.md
_persistence/tasks.md

$ git show --stat --name-only --format= 62ff0d2 | grep -c .
11
```

Se audita **`62ff0d2`**. `e04681b` es el commit de anclaje: toca el informe y, por el Paso 7c-bis,
`decisions.md` y `tasks.md` (de ahi los tres archivos). Los 11 archivos de `62ff0d2` coinciden con la
lista de la seccion 1 del informe en su version anclada. El informe se leyo en la version de
`e04681b`; en `62ff0d2` la cabecera decia `PENDIENTE — se rellena en el Paso 7c…` y la NOTA DE CIERRE
estaba sin completar, que es lo esperado.

### b) Indice ↔ detalle en `_persistence/` — coherentes

```
$ H=62ff0d2; for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show $H:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show $H:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

### c) Carpetas declaradas — dos diferencias, las dos con razon escrita

```
$ diff <(git ls-tree -d --name-only 62ff0d2 | sed 's|$|/|' | sort) \
     <(git show 62ff0d2:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
9a11
> temporal/
```

`010_prototype/` (declarada por adelantado, `D-061`) y `temporal/` (excluida en `.gitignore`) estan
justificadas en `project.md`, seccion «Carpetas propias». No es hallazgo.

### d) Fuga de datos propios en las seis areas agnosticas — cero lineas

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 62ff0d2 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

### e) Codigos instanciados en `_phases/` y `_workflow/` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 62ff0d2 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

### f) Fila ↔ ficha e indice ↔ detalle en `_audit/findings.md` (sobre el commit auditado) — coinciden

```
$ H=62ff0d2; diff <(git show $H:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) <(git show $H:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u); echo "exit=$?"
exit=0

$ git show 62ff0d2:_audit/findings.md | awk '/^```/{c=!c; next} c{next} /^\| \[F-[0-9]+\]/{split($0,a,"|"); id=a[2]; sub(/.*\[/,"",id); sub(/\].*/,"",id); gsub(/ /,"",a[6]); fila[id]=a[6]} /^### F-[0-9]+/{cur=$2} /^\| Estado \|/{if(cur!=""){s=$0; sub(/^\| Estado \| /,"",s); sub(/ \|$/,"",s); gsub(/ /,"",s); ficha[cur]=s; cur=""}} END{for(k in fila) if(fila[k]!=ficha[k]) print k, fila[k], ficha[k]}'
```

(sin salida: ninguna fila difiere de su ficha)

### g) Seccion 6 del informe, punto 1 y 2 — las notas de `T-180` y `T-181` reproducen

Cada nota se extrajo **de la version del commit** por su rango, se reejecutaron sus ordenes y se
comparo con lo publicado. Mismo metodo aplicado a los dos tratamientos de `findings.md` y a los
bloques nuevos de `decisions.md` (`D-157`, nota bajo `D-153`).

```
$ chk() { f="$1"; a="$2"; b="$3"; echo "===== $f :: $a"; git show 62ff0d2:"$f" | sed -n "/$a/,/$b/{p;/$b/q}" > "$SCR/blk.md"; awk '/^```$/{inb=!inb; next} inb' "$SCR/blk.md" > "$SCR/pub.txt"; grep '^\$ ' "$SCR/pub.txt" | sed 's/^\$ //' > "$SCR/cmds.sh"; : > "$SCR/re.txt"; while IFS= read -r l; do { printf '$ %s\n' "$l"; bash -c "$l"; echo; } >> "$SCR/re.txt"; done < "$SCR/cmds.sh"; diff <(sed '/^$/d' "$SCR/pub.txt") <(sed '/^$/d' "$SCR/re.txt") && echo "REPRODUCE ($(wc -l < "$SCR/cmds.sh") ordenes)"; }
$ chk _persistence/assumptions.md 'T-180`, hallazgo `F-107' '^### A-019 '
$ chk _persistence/tasks.md 'Nota del 2026-09-14 (`T-180`, hallazgo `F-107' '^### T-177 '
$ chk _audit/S-038.md 'T-181`, hallazgo `F-108' '^> ## NOTA DE CIERRE'
$ chk _audit/findings.md 'Tratamiento del 2026-09-14 (`S-039`) — ACEPTADO\.' '^### F-108 '
$ chk _audit/findings.md 'ACEPTADO en la correccion' 'no se adopta como regla'
$ chk _persistence/decisions.md '^### D-157 ' '^### D-158 '
$ chk _persistence/decisions.md 'la promocion sola no cumple' '^### D-154 '
===== _persistence/assumptions.md :: T-180`, hallazgo `F-107
REPRODUCE (3 ordenes)
===== _persistence/tasks.md :: Nota del 2026-09-14 (`T-180`, hallazgo `F-107
REPRODUCE (1 ordenes)
===== _audit/S-038.md :: T-181`, hallazgo `F-108
REPRODUCE (2 ordenes)
===== _audit/findings.md :: Tratamiento del 2026-09-14 (`S-039`) — ACEPTADO\.
REPRODUCE (2 ordenes)
===== _audit/findings.md :: ACEPTADO en la correccion
REPRODUCE (2 ordenes)
===== _persistence/decisions.md :: ^### D-157 
REPRODUCE (5 ordenes)
===== _persistence/decisions.md :: la promocion sola no cumple
REPRODUCE (4 ordenes)
```

(`$SCR` es el directorio temporal de esta sesion de auditoria, fuera del repositorio.)

La afirmacion de la nota de `T-180` bajo `A-018` —que `D-156` ya evaluaba la seccion 5 de `R-036` en
`e822ae3`— se sostiene:

```
$ git show e822ae3:_persistence/decisions.md | grep -n "la regla «anclada o viva» no se adopta"
167:| [D-156](#d-156---la-seccion-5-de-r-036-la-regla-anclada-o-viva-no-se-adopta-y-el-7c-quater-se-endurece) | La seccion 5 de `R-036`: la regla «anclada o viva» no se adopta, y el 7c-quater se endurece | 2026-09-14 | Vigente | report_auditor |
9969:### D-156 - La seccion 5 de `R-036`: la regla «anclada o viva» no se adopta, y el 7c-quater se endurece
```

La atribucion de la nota de `T-181` (filas 4 y 5 en `assumptions.md`, fila 10 en `tasks.md`) coincide
con la orden anclada, que la propia nota publica y que reproduce (arriba).

### h) Seccion 6, punto 3 — `D-157` se apoya en texto que existe

Las dos citas de `protocol-close` que `D-157` usa para decir que la regla ya existe estan en la version
de `d35bc91`, y forman parte del bloque que reproduce en (g):

```
$ git show d35bc91:.claude/skills/protocol-close/SKILL.md | grep -nE "escribe a mano mirando los bloques|se pega con su salida cruda"
391:escribe a mano mirando los bloques.** A mano es exactamente donde se equivoca: la orden se busca
393:la produce, y que se pega con su salida cruda:

$ git show d35bc91:.claude/skills/protocol-close/SKILL.md | grep -nF "<PEGA AQUI, sin editar, la salida cruda de:>"
1180:<PEGA AQUI, sin editar, la salida cruda de:>
```

El rechazo es «el hallazgo propone algo que ya existe», no «tiene razon pero no ahora», asi que no
exige `DT-XXX`. No es hallazgo.

### i) Seccion 6, punto 4 — `D-158`: la regla y el valor estan donde dice, y no mas ancho

```
$ git show 62ff0d2:.claude/skills/protocol-close/SKILL.md | grep -c 'Origen: session-closer'
2
$ git show 62ff0d2:_persistence/tasks.md | grep -c '^| `session-closer` |'
1
$ git show 62ff0d2:_templates/000_preproject/015_tasks.md | grep -c '^| `session-closer` |'
1
$ git show 62ff0d2:.claude/skills/protocol-close/SKILL.md | grep -c '^Ordenes sin reproducir en archivos ajenos (2d) — '
1
$ git show 62ff0d2:_persistence/assumptions.md | grep -c 'T-180`, hallazgo `F-107'
1
$ git show 62ff0d2:_persistence/tasks.md | grep -c '📌 \*\*Nota del 2026-09-14 (`T-1[8]0`, hallazgo `F-107'
1
$ git show 62ff0d2:_audit/S-038.md | grep -c 'T-181`, hallazgo `F-108'
1
```

Las siete coinciden con lo que el 7c-bis publico en `e04681b`. El texto anadido a la skill limita el
valor a «cada orden que no reproduce ahi» en un archivo que el cierre no edita (diff de `62ff0d2`,
hunks del Paso 2d y del Paso 4). No se observo ensanchamiento.

### j) Seccion 7 del informe (Paso 2d) — recuento, lista, repetidas y procedencia reproducen

```
$ B='git diff -U0 62ff0d2^ 62ff0d2 -- _persistence _audit ":(exclude)_audit/S-039.md"'
$ eval "$B" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -c1-170 | cat -n
     1	+$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff
     2	+$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff
     3	+$ git grep -c '0  .claude/skills/protocol-close/SKILL.md' d35bc91 -- _persistence/assumptions.md _persistence/tasks.md
     4	+$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff
     5	+$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" log -1 --format='%h %ad' --date=short
     6	+$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" show 1748f0a:project.md | grep -c '^| Esqueleto de arranque'
     7	+$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS" ls-tree --name-only 1748f0a
     8	+$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff
     9	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'Origen: session-closer'
    10	+$ git show <hash>:_persistence/tasks.md | grep -c '^| `session-closer` |'
    11	+$ git show <hash>:_templates/000_preproject/015_tasks.md | grep -c '^| `session-closer` |'
    12	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c '^Ordenes sin reproducir en archivos ajenos (2d) — '
    13	+$ grep -c 'Origen: session-closer' .claude/skills/protocol-close/SKILL.md
    14	+$ grep -c '^Ordenes sin reproducir en archivos ajenos (2d) — ' .claude/skills/protocol-close/SKILL.md
    15	+$ grep -c '^| `session-closer` | un control del cierre' _persistence/tasks.md _templates/000_preproject/015_tasks.md
    16	+$ grep -c '^| Origen | `usuario` / `manager` / `report_auditor` / `session-closer` |' _persistence/tasks.md _templates/000_preproject/015_tasks.md
    17	+$ grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|Company_" .claude/skills/protocol-close/SKILL.md _templates/000_preproject/015_tasks.md; 
    18	+$ git diff -U0 -- .claude/skills/protocol-close/SKILL.md _templates/000_preproject/015_tasks.md 2>/dev/null | grep '^+[^+]' | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -v
    19	+$ git show <hash>:_persistence/assumptions.md | grep -c 'T-180`, hallazgo `F-107'
    20	+$ git show <hash>:_persistence/tasks.md | grep -c '📌 \*\*Nota del 2026-09-14 (`T-1[8]0`, hallazgo `F-107'
    21	+$ for spec in "_persistence/assumptions.md|T-180\`, hallazgo \`F-107|^### A-019 " "_persistence/tasks.md|T-180\`, hallazgo \`F-107|^### T-177 "; do IFS='|' read -r f a b
    22	+$ git show <hash>:_audit/S-038.md | grep -c 'T-181`, hallazgo `F-108'
    23	+$ for spec in "_audit/S-038.md|T-181\`, hallazgo \`F-108|^> ## NOTA DE CIERRE"; do IFS='|' read -r f a b <<<"$spec"; echo "===== $f"; sed -n "/$a/,/$b/{p;/$b/q}" "$f" > 

$ eval "$B" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
23

$ eval "$B" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
+$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | uniq -c

$ eval "$B" | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | sort | uniq -c
      2 b/_audit/S-038.md
      2 b/_audit/findings.md
      8 b/_persistence/decisions.md
     11 b/_persistence/tasks.md
```

Recuento (23), numeracion de la tabla, repetidas (#1/#4) y procedencia coinciden con la seccion 7.
Las filas con `<hash>` (#9-12 en `decisions.md`, #19, #20, #22 en `tasks.md`) son las siete que el
7c-bis anclo.

### k) NOTA DE CIERRE — CENSO, CONTROL y prosa del anclaje

```
$ for f in $(git ls-tree -r --name-only 62ff0d2 _persistence _audit | grep -v '_audit/S-039.md'); do n=$(git show 62ff0d2:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done | tr '\n' ' '
_audit/R-025.md: 2 _audit/R-026.md: 7 _audit/R-027.md: 1 _audit/R-029.md: 2 _audit/R-030.md: 3 _audit/R-031.md: 2 _audit/R-032.md: 2 _audit/R-033.md: 3 _audit/R-034.md: 2 _audit/R-035.md: 1 _audit/R-037.md: 2 _audit/R-038.md: 1 _audit/S-017.md: 1 _audit/S-024.md: 1 _audit/S-025.md: 17 _audit/S-026.md: 8 _audit/S-027.md: 17 _audit/S-029.md: 2 _audit/S-030.md: 2 _persistence/assumptions.md: 9 _persistence/decisions.md: 20 _persistence/tasks.md: 28 

$ for f in $(git diff --name-only 62ff0d2^ 62ff0d2 -- _persistence _audit ":(exclude)_audit/S-039.md"); do n=$(git diff -U0 62ff0d2^ 62ff0d2 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 4
_persistence/tasks.md: 3

$ outside() { awk '/^```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f"; diff <(git show 62ff0d2:"$f" | outside) <(git show e04681b:"$f" | outside) | grep '^[<>]'; done
== _persistence/decisions.md
> 
> 📌 **Ancladas por el Paso 7c-bis al commit `62ff0d2`.** Las cuatro reproducen lo publicado arriba.
> 
> ---
== _persistence/tasks.md
> 📌 **Anclada por el Paso 7c-bis al commit `62ff0d2`.** La orden reproduce lo publicado arriba.
> 
> 📌 **Ancladas por el Paso 7c-bis al commit `62ff0d2`.** Las dos reproducen lo publicado arriba.
> 
```

CENSO y CONTROL coinciden con los publicados. Fuera de los bloques, el anclaje solo **anade** (ninguna
linea `<`): las lineas de anclaje que el 7c-bis autoriza, con su recuento correcto (4, 2, 1).

### l) Seccion 1 del informe — hunks

```
$ for f in _persistence/decisions.md _persistence/tasks.md _persistence/assumptions.md _persistence/lessons.md _audit/findings.md _audit/S-038.md; do echo "$f $(git diff -U0 62ff0d2^ 62ff0d2 -- $f | grep -c '^@@')"; done
_persistence/decisions.md 3
_persistence/tasks.md 6
_persistence/assumptions.md 3
_persistence/lessons.md 1
_audit/findings.md 5
_audit/S-038.md 1
```

Coinciden con la seccion 1. La descripcion del contenido de `lessons.md`, no: ver `F-109`.

## 2. Hallazgos

### F-109 — La seccion 1 del informe atribuye a `T-180` las tres reincidencias de la nota de `L-056`, y la tercera es de `T-179`

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-039.md`, seccion 1, linea de `_persistence/lessons.md`; `_persistence/lessons.md`, `L-056` |

- **Que se observo:** el informe describe la nota anadida a `L-056` como «tres formas del mismo defecto
  en `T-180`». La nota dice que en `T-180` paso **dos** veces y que la tercera fue en **`T-179`**.

```
$ git show e04681b:_audit/S-039.md | grep -n "tres formas"
43:- **`_persistence/lessons.md`** — 1 hunk. No nace ninguna entrada; se edita la existente `L-056` con una nota de reincidencia (tres formas del mismo defecto en `T-180`).

$ git show 62ff0d2:_persistence/lessons.md | awk 'NR<=2084 && /^### L-/{h=NR": "$0} END{print h}'
2058: ### L-056 - Una orden que busca una cadena en el archivo donde vive se cuenta a si misma

$ git show 62ff0d2:_persistence/lessons.md | sed -n '2084,2086p;2095,2097p'
> 🕒 **Nota de reincidencia del 2026-09-14 (`T-180`, `F-107`).** Volvio a pasar dos veces en la misma
> tarea, y las dos se cazaron antes de publicar. **La primera, con otra forma:** la verificacion
> extraia las ordenes de una nota por un rango `sed -n '/inicio/,/fin/p'`, y la ficha de la tarea,
>
> ⚠️ **Y una tercera vez en la misma jornada (`T-179`)**, ahora con la salida **tecleada** junto a la
> orden: un `grep -n … tasks.md` que encontraba su propia linea y un `cut -c` que corta por bytes y
```

- **Por que importa:** la seccion 1 es la lista contra la que se audita el diff, y su lector la usa
  para saber donde nacio cada reincidencia. Atribuir a `T-180` la de `T-179` borra que la verificacion
  de `T-179` —la tarea que cambia `protocol-close`— se tecleo con el mismo defecto que `L-056`
  describe, que es justo el dato que sirve para decidir si la leccion necesita algo mas que notas. `Baja`: no cambia ningun estado ni ninguna cifra,
  y el archivo del porque (`L-056`) esta bien.
- **Que lo corregiria:** una nota fechada en la seccion 1 de `S-039` con la atribucion correcta (dos en
  `T-180`, una en `T-179`), sin reescribir la linea. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| F-107 | Aceptado — pendiente | **Implementado** | En `62ff0d2` estan la nota fechada bajo `A-018` (cifra `2` en el commit y `0` en el padre, y la aclaracion de que `D-156` ya evaluaba la seccion 5 de `R-036`) y la nota bajo `T-176`; las cuatro ordenes van ancladas por los dos lados y reproducen (1g). Es lo que recomendaba el hallazgo. |
| F-108 | Aceptado — pendiente | **Implementado** | En `62ff0d2` esta la nota fechada en la seccion 7 de `S-038` con la orden anclada y la atribucion de las filas 4, 5 y 10; sus dos ordenes reproducen (1g). La mitad hacia adelante queda rechazada por `D-157`, registrada y apoyada en texto que existe (1h); rechazo por «ya existe», sin `DT-XXX` exigible. |

No quedan otros `F-NNN` sin cerrar en el indice de `findings.md`.

## 4. Lo que NO se pudo comprobar

Ninguno de los controles fallo. Una precision de ambito: las ordenes que leen el repositorio del
esqueleto (`git -C … 1748f0a`) se reejecutaron contra ese repositorio en esta maquina y reprodujeron;
quien no tenga ese repositorio no puede repetir esa parte.

## 5. Recomendaciones sin hallazgo

- **El pendiente del usuario de la nota bajo `D-153` no tiene `T-XXX`.** Vive en esa nota y en dos
  parrafos de `progress.md`, que el arranque lee, asi que no se pierde; pero el «Siguiente paso» de
  `progress.md` sigue diciendo que tras la promocion, `T-162` y `T-163`, «`T-156` quedaria lista para
  cerrarse», y la nota dice que `D-153` no puede cerrarse por su criterio aunque la promocion se haga.
  Es el mismo patron que `D-158` atribuye a un aviso sin dueno. Valorar abrirle tarea.
- **`D-158` no dice si un informe `S-XXX` de una sesion anterior es «archivo ajeno» para el Paso 2d.**
  La skill da como editables «el informe» (el de la sesion) y como ajenos los del porque y los del
  auditor; esta misma sesion tiene dos ordenes (#1, #2) viviendo en `_audit/S-038.md`. Reprodujeron,
  asi que no hubo caso, pero el primero que no reproduzca ahi no tiene casilla clara.
- **`T-179` queda `Implementada` con su criterio original («una prueba») sustituido en la misma sesion
  por `D-158`.** La sustitucion esta escrita y lleva `A-021`, por eso no es hallazgo; conviene que el
  primer caso real que dispare `A-021` se lea tambien como el cierre pendiente del criterio de `T-179`.
