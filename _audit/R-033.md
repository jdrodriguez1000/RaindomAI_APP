# Auditoria R-033 — sobre S-033

| Campo | Valor |
|---|---|
| Auditoria | R-033 |
| Informe auditado | `_audit/S-033.md` |
| Commit auditado | `91a59c8` |
| Fecha | 2026-09-10 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

**Anclaje del commit.** `git log -1 --format=%h -- _audit/S-033.md` devuelve `f3ae6b6`, que es el
**commit de anclaje** del Paso 7c (3 archivos: el informe, `decisions.md` y `tasks.md` — los dos
unicos que el Paso 7c-bis puede escribir). Manda el hash literal de la cabecera:

```
$ git show f3ae6b6:_audit/S-033.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `91a59c8` |

$ git show --stat --name-only --format= 91a59c8 | grep -c .
14
```

Catorce archivos, la misma lista que enumera la seccion 1 del informe. **Se audita `91a59c8`.**

**a) Indice ↔ detalle en `_persistence/` — coherentes, los siete archivos, sin una sola diferencia:**

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show 91a59c8:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show 91a59c8:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

**b) Carpetas del arbol contra las declaradas — las dos diferencias son las dos que `project.md`
explica por escrito** (`010_prototype/` declarada por adelantado, `D-061`; `temporal/` excluida por
`.gitignore`):

```
$ diff <(git ls-tree -d --name-only 91a59c8 | sed 's|$|/|' | sort) \
       <(git show 91a59c8:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

**c) Fuga de datos propios en las seis areas agnosticas — cero lineas:**

```
$ git grep -nE "RaindomAI|Company_TripleS|github.com" 91a59c8 -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida)
```

**d) Codigos instanciados en `_phases/` y `_workflow/` — cero lineas:**

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 91a59c8 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
(sin salida)
```

**Seccion 1 del informe contra el diff — cuadra, incluido el recuento de hunks.** El informe publica
17 hunks para `tasks.md` y 7 para `progress.md`, donde `git diff` por defecto (`-U3`) da 14 y 3. No
es un desfase: la cuenta es con `-U0`, y con ese contexto coincide en los siete archivos.

```
$ for f in _persistence/decisions.md _persistence/tasks.md _persistence/constraints.md _persistence/lessons.md _persistence/progress.md _audit/S-032.md _audit/findings.md; do echo "$f U3=$(git diff 91a59c8^ 91a59c8 -- $f | grep -c '^@@') U0=$(git diff -U0 91a59c8^ 91a59c8 -- $f | grep -c '^@@')"; done
_persistence/decisions.md U3=3 U0=3
_persistence/tasks.md U3=14 U0=17
_persistence/constraints.md U3=2 U0=2
_persistence/lessons.md U3=3 U0=3
_persistence/progress.md U3=3 U0=7
_audit/S-032.md U3=2 U0=2
_audit/findings.md U3=3 U0=3
```

**Entradas nuevas — exactamente las que el informe declara** (`D-137` a `D-143`, `T-148` a `T-151`,
`C-009`, `L-049` a `L-051`), ni una mas:

```
$ diff <(git show 91a59c8^:_persistence/decisions.md | grep -oE '^### D-[0-9]+') <(git show 91a59c8:_persistence/decisions.md | grep -oE '^### D-[0-9]+')
136a137,143
> ### D-137
> ### D-138
> ### D-139
> ### D-140
> ### D-141
> ### D-142
> ### D-143
$ diff <(git show 91a59c8^:_persistence/tasks.md | grep -oE '^### T-[0-9]+') <(git show 91a59c8:_persistence/tasks.md | grep -oE '^### T-[0-9]+')
147a148,151
> ### T-148
> ### T-149
> ### T-150
> ### T-151
$ diff <(git show 91a59c8^:_persistence/constraints.md | grep -oE '^### C-[0-9]+') <(git show 91a59c8:_persistence/constraints.md | grep -oE '^### C-[0-9]+')
8a9
> ### C-009
$ diff <(git show 91a59c8^:_persistence/lessons.md | grep -oE '^### L-[0-9]+') <(git show 91a59c8:_persistence/lessons.md | grep -oE '^### L-[0-9]+')
48a49,51
> ### L-049
> ### L-050
> ### L-051
```

**Lo que la seccion 6 pidio auditar, punto por punto:**

**1. Los criterios de cierre de `T-002`, `T-037`, `T-038` y `T-112` reproducen sobre el commit**
—este era el punto que el informe declaro como su mas debil, y se sostiene entero. Reejecutados
anclando cada orden a `91a59c8`, dan lo mismo que publican:

```
$ git show 91a59c8:project.md | grep -c '^| Secuencia adoptada |'
1
$ git show 91a59c8:project.md | grep -c 'no registradas'
0
$ git show 91a59c8:project.md | sed -n '/^| Etapas declaradas/p' | grep -oE '`[0-9]{3}_[a-z]+`' | wc -l
7
$ git show 91a59c8:_persistence/constraints.md | grep -c '^#### Irreversible — permiso antes'
1
$ git show 91a59c8:_persistence/constraints.md | grep -c '^#### Reversible — se hace y se revisa despues'
1
$ git show 91a59c8:CLAUDE.md | grep -c 'mientras no exista en `_persistence/` un'
0
$ git show 91a59c8:.claude/skills/protocol-close/SKILL.md > /tmp/c.md; git show 91a59c8:.claude/skills/protocol-audit/SKILL.md > /tmp/a.md
$ grep -hoE '^git grep -nE .* -- .*$' /tmp/c.md /tmp/a.md | sed 's/.*-- //' | sort -u
.claude CLAUDE.md _phases _methodology _templates _workflow
$ grep -hoE '^git grep -noE .*$' /tmp/c.md /tmp/a.md | sed 's/<hash> //' | sort -u
git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' -- _phases _workflow | grep -vE ':PI-[0-9]+$'
$ for f in protocol-close protocol-audit; do git show 91a59c8:.claude/skills/$f/SKILL.md | grep -cF "git grep -noE '\b[A-Z]{1,2}-[0-9]+\b'"; done
1
1
$ for f in protocol-close protocol-audit; do git show 91a59c8:.claude/skills/$f/SKILL.md | grep -cF '(T|D|F|L|A|C|DT|S)-'; done
0
0
```

**2. El CONTROL DE CIFRA ADYACENTE esta escrito de verdad, con su condicion de parada:**

```
$ git show 91a59c8:.claude/skills/protocol-close/SKILL.md | grep -n 'CONTROL DE CIFRA ADYACENTE'
926:cruda**. Ese recorte es el CONTROL DE CIFRA ADYACENTE del final de este paso: obligatorio, y no
1290:### 🚨 CONTROL DE CIFRA ADYACENTE (obligatorio, antes del `git add`)
$ git show 91a59c8:.claude/skills/protocol-close/SKILL.md | grep -n 'La respuesta correcta NO es cero'
1308:⛔ **La respuesta correcta NO es cero.** Un informe sano devuelve del orden de diez lineas, y casi
```

⚠️ **Escrito si; corrido y publicado sobre este propio informe, no** — ver `F-097`.

**3. Ninguna orden con `<hash>` sobrevive sin ancla en las entradas nuevas.** Sobre el arbol del
commit de anclaje:

```
$ git show f3ae6b6:_persistence/decisions.md | awk '/^### D-137/{f=1} /^### D-144/{f=0} f' | grep -nE '^\$ .*<hash>'
299:$ grep -hoE '^git grep -noE .*$' /tmp/c.md /tmp/a.md | sed 's/<hash> //' | sort -u | wc -l
$ git show f3ae6b6:_persistence/tasks.md | awk '/^### T-148/{f=1} f' | grep -nE '^\$ .*<hash>'
(sin salida)
```

La unica linea es la que el informe ya identifica: el criterio de `D-140`/`T-038`, que busca la
cadena `<hash> ` como **dato** dentro de las skills. No es una orden pendiente de anclar.

**4. El CENSO, el CONTROL y el CONTROL DE PROSA BORRADA de la NOTA DE CIERRE reproducen exactos.**
Reejecutados los tres, salida identica a la publicada (CENSO: los mismos 17 archivos con las mismas
cifras; CONTROL: `_persistence/decisions.md: 14` y `_persistence/tasks.md: 8`, ningun otro archivo).
Y el anclaje `f3ae6b6` no borro prosa:

```
$ outside() { awk '/^```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show 91a59c8:"$f" | outside > /tmp/pa; git show f3ae6b6:"$f" | outside > /tmp/pd
  echo "== $f =="; diff /tmp/pa /tmp/pd | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

**5. El Paso 2d reproduce: 43 lineas, la misma cifra que el informe publica.**

```
$ git diff -U0 91a59c8^ 91a59c8 -- _persistence _audit ":(exclude)_audit/S-033.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
43
```

**6. La seccion 0 no usa `Implementado`**, como `D-138` exige a partir de este commit: sus dos filas
dicen `Aceptado — corregido en este commit`, y las dos correcciones estan en el diff (las dos notas
fechadas de `_audit/S-032.md`, sin tocar la prosa original).

**7. Fecha del tablero contra fecha del commit (Paso 7d) — coinciden:** `91a59c8` es de `2026-09-10`
y la fila de `S-033` en `_audit/index.md` dice `2026-09-10`.

**8. La afirmacion «cada etapa tiene ya su archivo en `_phases/`» se sostiene**, y `_workflow/`
cubre las seis que le tocan (todas salvo `000_preproject`):

```
$ git ls-tree --name-only 91a59c8 _phases/
_phases/000_preproject.md
_phases/005_discovery.md
_phases/010_prototype.md
_phases/020_baseline.md
_phases/025_wslt.md
_phases/030_growth.md
_phases/040_evol.md
$ git ls-tree --name-only 91a59c8 _workflow/
_workflow/005_discovery.md
_workflow/010_prototype.md
_workflow/020_baseline.md
_workflow/025_wslt.md
_workflow/030_growth.md
_workflow/040_evol.md
_workflow/ai_levels.md
_workflow/team.md
```

## 2. Hallazgos

### F-095 — `project.md` afirma tres veces que `020_baseline` no esta adoptada, en el mismo commit que la declara
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `project.md`, tabla «Codigos» (filas `FT-XXX` y `SC-XXX`) y la nota que las explica; `D-142` |

- **Que se observo:** `D-142` declara las siete etapas en la tabla «Etapas» del mismo archivo, y
  unas ciento cincuenta lineas mas abajo el archivo sigue diciendo lo contrario:

```
$ git show 91a59c8:project.md | grep -n 'la etapa no esta adoptada'
250:| `FT-XXX` | el artefacto de features de `020_baseline` (ruta por declarar: la etapa no esta adoptada) | feature |
251:| `SC-XXX` | el artefacto de escenarios de `020_baseline` (ruta por declarar: la etapa no esta adoptada) | scenario |

$ git show 91a59c8:project.md | sed -n '267,269p'
`N-XXX` (`D-034`) e `I-XXX` (`D-038`). **Declararlos no adopta `020_baseline`**: la etapa sigue sin
declarar en la tabla «Etapas», y por eso la columna «Archivo» de esas dos filas dice «ruta por
declarar» en vez de inventarse una.

$ git show 91a59c8:project.md | sed -n '/^| Etapas declaradas/p'
| Etapas declaradas | `000_preproject`, `005_discovery`, `010_prototype`, `020_baseline`, `025_wslt`, `030_growth`, `040_evol` |
```

- **Por que importa:** `project.md` es el unico archivo del proyecto que guarda datos propios, y es
  donde se resuelve «que etapas existen» para los protocolos, los agentes y las auditorias. Un
  archivo que se contradice consigo mismo obliga a decidir cual de sus dos mitades miente, y esa
  respuesta no esta escrita en ningun sitio. Ademas la razon que esas notas dan para dejar la columna
  «Archivo» sin ruta —«la etapa no esta adoptada»— dejo de ser cierta en este commit: la ruta de los
  artefactos de `FT-XXX` y `SC-XXX` pasa a ser un hueco real, no una consecuencia de que la etapa no
  existiera.
- **Que lo corregiria:** actualizar las dos filas de la tabla «Codigos» y la nota que las explica
  para que digan lo que ahora es cierto —la etapa esta declarada; la ruta del artefacto sigue sin
  fijarse— y, si esa ruta se decide, escribirla con su `D-XXX`. ⚠️ Es una recomendacion, no una
  orden.

### F-096 — El bloque de verificacion de la seccion 2 de `S-033` publica dos salidas que no reproducen
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-033.md`, seccion 2 |

- **Que se observo:** dos salidas crudas del mismo bloque no devuelven lo que el informe publica.
  La primera esta podada: el comando devuelve **cuatro** filas y el informe publica **tres** (falta
  la de `T-039`, que el `grep` recoge porque «No implementada» aparece en su titulo, aunque su estado
  es `Implementada`):

```
$ git show 91a59c8:_persistence/tasks.md > /tmp/t.md
$ sed -n '/^## Indice/,/^---/p' /tmp/t.md | grep "No implementada"
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-039](#t-039---normalizar-a-no-implementada-el-estado-pendiente-de-t-037-y-t-038-f-029) | Normalizar a `No implementada` el estado `Pendiente` de `T-037` y `T-038` (`F-029`) | Implementada | Media | No bloqueante | `000_preproject` |
| [T-128](#t-128---hacer-la-cosecha-de-las-lecciones-de-la-etapa-preparatoria-d-123) | Hacer la cosecha de las lecciones de la etapa preparatoria (`D-123`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-144](#t-144---dar-dueño-y-sitio-a-evaluacion-y-observabilidad-lg-54-d-134) | Dar dueño y sitio a evaluacion y observabilidad (`LG-54`, `D-134`) | No implementada | Media | No bloqueante | `005_discovery` |
```

  La segunda es una cifra que se queda una corta. El informe publica `151`; la orden da `152`:

```
$ grep -n "^### T-" /tmp/t.md | wc -l
152
$ for h in 91a59c8^ 91a59c8 f3ae6b6; do echo "$h: $(git show $h:_persistence/tasks.md | grep -c '^### T-')"; done
91a59c8^: 148
91a59c8: 152
f3ae6b6: 152
```

  El commit anterior tiene 148 entradas y este anade cuatro, asi que `151` no corresponde a ningun
  estado del archivo, ni antes ni despues del anclaje.
- **Por que importa:** las dos conclusiones de fondo del bloque son correctas —las tareas realmente
  abiertas son `T-001`, `T-128` y `T-144`, y no existe ninguna `T-XXX` para el trabajo que `D-143`
  prioriza—, y eso es justo lo que hace el defecto dificil de ver. Quien reejecute la primera orden
  para contrastar vera una fila de mas y tendra que averiguar por su cuenta si el informe oculto una
  tarea o si el filtro es demasiado ancho; una salida cruda recortada a mano deja de ser evidencia y
  pasa a ser una afirmacion con formato de evidencia, que es peor que no publicarla. Es ademas el
  mismo defecto de fondo que `F-093`, reapareciendo en el commit que estrena el control escrito para
  impedirlo.
- **Que lo corregiria:** una nota fechada en `_audit/S-033.md` con las dos salidas derivadas, sin
  reescribir la prosa; y, hacia adelante, que un filtro por estado acote la **columna** en vez del
  texto libre de la fila (`grep -E '\| No implementada \|'`), que es lo que el enunciado del bloque
  dice estar haciendo. ⚠️ Es una recomendacion, no una orden.

### F-097 — La NOTA DE CIERRE no publica la salida del CONTROL DE CIFRA ADYACENTE, que el Paso 6b declara obligatoria
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-033.md`, seccion 7 (NOTA DE CIERRE); `.claude/skills/protocol-close/SKILL.md`, Paso 6b |

- **Que se observo:** el Paso 6b, en la version que este mismo commit deja escrita, exige correr el
  control antes del `git add` y **publicar su salida siempre**:

```
$ git show 91a59c8:.claude/skills/protocol-close/SKILL.md | sed -n '1290p;1308p;1319,1320p'
### 🚨 CONTROL DE CIFRA ADYACENTE (obligatorio, antes del `git add`)
⛔ **La respuesta correcta NO es cero.** Un informe sano devuelve del orden de diez lineas, y casi
⚠️ **La salida se publica en la NOTA DE CIERRE, tambien cuando no obliga a corregir nada.** Un
control cuyo resultado no se publica no se distingue de un control que no se corrio.
```

  En el informe, «CIFRA ADYACENTE» aparece seis veces y ninguna es su salida: dos son prosa de las
  secciones 0 y 1, una es la peticion de la seccion 6, y tres son la cadena dentro de las ordenes de
  los criterios de cierre de `D-137`/`T-149`.

```
$ git show f3ae6b6:_audit/S-033.md | grep -n -i 'cifra adyacente'
25:  Nace el **CONTROL DE CIFRA ADYACENTE** obligatorio en el Paso 6b de `protocol-close` (`D-137`,
109:  (`D-139`); nace el CONTROL DE CIFRA ADYACENTE del Paso 6b (`D-137`); la seccion 0 sustituye
256:- Que el CONTROL DE CIFRA ADYACENTE (`D-137`) este realmente escrito en `protocol-close` con su
282: 1	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c '^### 🚨 CONTROL DE CIFRA ADYACENTE'
319:38	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c '^### 🚨 CONTROL DE CIFRA ADYACENTE'
332:+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c '^### 🚨 CONTROL DE CIFRA ADYACENTE'
```

  Corrido ahora sobre el informe devuelve trece lineas — «del orden de diez», exactamente lo que su
  enunciado predice; el control es ejecutable y no fallaba:

```
$ git show f3ae6b6:_audit/S-033.md > /tmp/s033.md
$ awk '
  /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' /tmp/s033.md | wc -l
13
```

- **Por que importa:** el control nace en este commit precisamente porque «una regla de redaccion
  sola ya fallo tres veces seguidas». Estrenarlo sin publicar su salida repite el patron que `D-137`
  vino a romper: desde fuera no se distingue un control que paso de uno que nadie corrio, que es
  literalmente lo que su propio enunciado advierte. Y no es teorico — la cifra de `F-096` es una
  cifra pegada a una salida cruda que no reproduce.
- **Que lo corregiria:** una nota fechada en `_audit/S-033.md` con la salida del control corrida
  sobre el informe; y valorar si el Paso 6b necesita ademas una condicion comprobable desde fuera
  —por ejemplo, que el Paso 7b no de por cerrado un informe cuya NOTA DE CIERRE no contenga esa
  salida—, porque hoy el control depende de que el cierre se acuerde de correrlo. ⚠️ Es una
  recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-093` | **Implementado** (`91a59c8`) | La nota fechada esta en el diff de `_audit/S-032.md` y su salida reproduce: la suma da `17`. Ademas nace el control mecanico (`D-137`, `T-149`), escrito y con su condicion de parada. La correccion del hallazgo esta hecha; que el control no se publicara en este cierre es un defecto **nuevo** (`F-097`), no la reapertura de este |
| `F-094` | **Implementado** (`91a59c8`) | La nota fechada esta en el diff, sus dos ordenes reproducen, y la seccion 0 de `S-033` ya no usa `Implementado`: dice `Aceptado — corregido en este commit`. `protocol-close` retira la palabra de su plantilla y `protocol-audit` gana la fila que marca su uso como hallazgo |

Reproduccion de las dos:

```
$ for f in $(git diff --name-only --diff-filter=d a6279c7^ a6279c7); do git show a6279c7:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'; done | awk '{s+=$1} END {print s}'
17
$ git show a6279c7:_audit/findings.md | grep -E '^\| \[F-09[12]\]' | awk -F'|' '{gsub(/^ +| +$/,"",$6); print $6}'
Aceptado — pendiente
Aceptado — pendiente
$ git show 91a59c8:_audit/S-032.md | grep -c 'Nota del 2026-09-10 (`F-093`)'
1
$ git show 91a59c8:_audit/S-032.md | grep -c 'Nota del 2026-09-10 (`F-094`)'
1
$ git show 91a59c8:.claude/skills/protocol-audit/SKILL.md | grep -n 'la seccion 0 usa `Implementado` en su columna'
135:| la seccion 0 usa `Implementado` en su columna `Veredicto` | 🚨 hallazgo: ese estado es tuyo, `manager` no puede cerrar sus hallazgos |
```

No queda ningun otro `F-NNN` sin cerrar antes de esta pasada.

## 4. Lo que NO se pudo comprobar

Ninguno de los controles quedo sin correr. Dos matices, declarados:

- Las lineas 30 a 33 de la lista del Paso 2d (la inyeccion de fuga de `D-139`/`L-050`) **no se
  reejecutan** aqui, por la misma razon que dio el cierre: escriben en `_phases/010_prototype.md`.
  Se comprobo en su lugar lo que esa secuencia afirma — el control (d) devuelve cero lineas sobre
  `91a59c8`, con el archivo limpio.
- El barrido anclado a `cbb92a9` que `T-037` publica —afirmando que el inventario no existia
  entonces— no se rehace por diseno: su ancla lo mantiene reproducible, y repetirlo hoy daria otra
  cifra por el motivo correcto.

## 5. Recomendaciones sin hallazgo

- **El vacio que la seccion 2 senala sigue sin codigo.** El informe lo dice bien y no lo disimula:
  `D-143` declara como prioridad inmediata «extraer del andamiaje un esqueleto reutilizable» y no
  existe ninguna `T-XXX` que lo concrete. No es un defecto del cierre —esta declarado—, pero es lo
  primero que se pierde si nadie lo recoge en la sesion siguiente.
- **`A-014` toca de lleno ese trabajo y sigue `Abierto`.** Si el esqueleto empieza a construirse
  antes de validarlo, conviene que su disparador quede fijado en la misma pasada en que nazca la
  tarea.
