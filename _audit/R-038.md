# Auditoria R-038 — sobre S-038

| Campo | Valor |
|---|---|
| Auditoria | R-038 |
| Informe auditado | `_audit/S-038.md` |
| Commit auditado | `e822ae3` |
| Fecha | 2026-09-14 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

### a) Sobre que commit se audita

El informe esta en el commit de anclaje `85cc2ed` (un solo archivo), y su cabecera escribe el hash
literal del commit sustantivo:

```
$ git log -1 --format=%h -- _audit/S-038.md
85cc2ed

$ git show 85cc2ed:_audit/S-038.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `e822ae3` |

$ git show --stat --format='%h %ad %s' --date=short 85cc2ed
85cc2ed 2026-09-14 S-038: ancla el informe al hash e822ae3
 _audit/S-038.md | 126 +++++++++++++++++++++++++++++++++++++++++++++++++-------
 1 file changed, 112 insertions(+), 14 deletions(-)

$ git show --stat --name-only --format= e822ae3 | grep -c .
10
```

Se audita **`e822ae3`**. Los 10 archivos coinciden con la lista anclada de la seccion 1 del informe
(version de `85cc2ed`). El informe se leyo en la version de `e822ae3` y se contrasto el diff del
anclaje: solo toca la cabecera, la seccion 1 y la NOTA DE CIERRE.

### b) Indice ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show e822ae3:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show e822ae3:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

### c) Carpetas del arbol contra las declaradas — dos diferencias, las dos con razon escrita

```
$ diff <(git ls-tree -d --name-only e822ae3 | sed 's|$|/|' | sort) \
       <(git show e822ae3:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
9a11
> temporal/
```

`010_prototype/` (declarada por adelantado, `D-061`) y `temporal/` (excluida por `.gitignore`) tienen
su razon escrita en `project.md`, seccion «Carpetas propias». No es hallazgo.

### d) Fuga de datos propios en las seis areas agnosticas — cero lineas

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github.com" e822ae3 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

### e) Codigos instanciados en `_phases/` y `_workflow/` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' e822ae3 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

### f) Fila ↔ ficha en `_audit/findings.md` y fecha de la sesion

```
$ diff <(git show e822ae3:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) <(git show e822ae3:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u); echo "exit=$?"
exit=0

$ git log -1 --format=%ad --date=short e822ae3
2026-09-14
```

La fecha coincide con la de `S-038` en `progress.md`, en el tablero y en el informe.

### g) Seccion 0 — las tres correcciones estan en el diff

`F-104` (`T-175`): la nota fechada bajo `D-153` lleva la ruta literal, y la orden devuelve el `0`
publicado (estado vivo del esqueleto, `1748f0a`):

```
$ git show e822ae3:_persistence/decisions.md | awk '/^### D-/{h=$2} /Nota del 2026-09-14/{print h": "NR}'
D-153: 9851
D-156: 10052

$ git show e822ae3:_persistence/decisions.md | grep -n 'SDAI_TripleS/project.md"$'
9858:$ grep -c '^| Esqueleto de arranque' "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/project.md"

$ grep -c '^| Esqueleto de arranque' "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/project.md"
0
```

`F-106` (`D-155`, `T-177`): la regla esta en `CLAUDE.md` y el barrido en el Paso 2b. El barrido,
extraido de la skill **en la version de `e822ae3`**, sale vacio sobre el `findings.md` de ese commit y
senala los dos hallazgos sobre `3b20720`:

```
$ git show e822ae3:CLAUDE.md | grep -n 'Y su ficha, en la misma pasada'
166:   🚨 **Y su ficha, en la misma pasada: `Estado` y `Registrado en`.** El hallazgo vive en dos sitios

$ git show e822ae3:.claude/skills/protocol-close/SKILL.md | sed -n '/^### Y la fila contra la ficha/,/^Sin salida = /p' | sed -n '/^```bash$/,/^```$/p' | sed '1d;$d' > "$S/fila.sh"
$ git show e822ae3:_audit/findings.md > "$S/c/_audit/findings.md"; (cd "$S/c" && bash ../fila.sh); echo "e822ae3 exit=$?"
e822ae3 exit=0

$ git show 3b20720:_audit/findings.md > "$S/n/_audit/findings.md"; (cd "$S/n" && bash ../fila.sh); echo "3b20720 exit=$?"
102,103c102,103
< F-102 Aceptado — pendiente
< F-103 Aceptado — pendiente
---
> F-102 Abierto
> F-103 Abierto
3b20720 exit=1
```

(`$S` es el directorio de trabajo temporal de esta auditoria, fuera del repositorio.)

**El punto 3 de la seccion 6 del informe —casos intermedios— se probo aqui.** Sobre una copia del
`findings.md` de `e822ae3` se cambio solo la ficha de `F-050` (a `No se implementa`) y solo la fila de
`F-090` (a `Abierto`); el barrido senala las dos:

```
$ awk '/^### F-050 /{a=1} a && /^\| Estado \|/{sub(/\| Estado \| .*\|$/,"| Estado | No se implementa |"); a=0} {print}' "$S/c/_audit/findings.md" | sed -E '/^\| \[F-090\]/s/\| [^|]+ \|$/| Abierto |/' > "$S/c/_audit/f2"; mv "$S/c/_audit/f2" "$S/c/_audit/findings.md"; (cd "$S/c" && bash ../fila.sh); echo "intermedio exit=$?"
50c50
< F-050 Implementado
---
> F-050 No se implementa
90c90
< F-090 Abierto
---
> F-090 Implementado
intermedio exit=1
```

`F-105` (`T-176`): la segunda nota bajo `A-018` esta; su septima cifra (`3`) cuadra con el commit.
Pero su tercera cifra no — vease `F-107`.

### h) `D-156` / `T-178` — el patron nuevo del 7c-quater, reejecutado sobre el commit

```
$ P=$(git show e822ae3:.claude/skills/protocol-close/SKILL.md | sed -n '/^### 7c-quater/,/^### 7d/p' | grep "^grep -qE" | sed -E "s/^grep -qE '(.*)' _audit.*/\1/"); echo "$P"; for s in S-033:91a59c8 S-034:2cef150 S-035:cce48e0 S-036:6fb6d39 S-037:3b20720; do r=${s%%:*}; h=${s##*:}; git show e822ae3:_audit/$r.md | grep -qE "${P/<hash>/$h}" && echo "$r ($h): pasa" || echo "$r ($h): FALTA"; done; git show e822ae3:_audit/S-036.md | sed '/^> ## NOTA DE CIERRE/,$d' | grep -qE "${P/<hash>/6fb6d39}" && echo "nuevo sin nota: pasa" || echo "nuevo sin nota: FALTA"
^(> )?\$ git show --stat --name-only --format= <hash>$
S-033 (91a59c8): pasa
S-034 (2cef150): pasa
S-035 (cce48e0): pasa
S-036 (6fb6d39): pasa
S-037 (3b20720): pasa
nuevo sin nota: FALTA

$ git show 85cc2ed:_audit/S-038.md | grep -nE '^(> )?\$ git show --stat --name-only --format= e822ae3$'; echo "exit=$?"
31:$ git show --stat --name-only --format= e822ae3
exit=0
```

### i) Seccion 1 del informe — ubicacion de las notas nuevas

```
$ git show e822ae3:_persistence/lessons.md | awk '/^### L-/{h=$2} /Nota de reincidencia del 2026-09-14/{print h": "NR}'
L-047: 1803
L-053: 1993

$ git show e822ae3:_persistence/assumptions.md | awk '/^### A-/{h=$2} /Nota del 2026-09-14/{print h": "NR}'
A-018: 1167

$ git diff e822ae3^ e822ae3 -- _persistence/tasks.md | grep -E '^@@'
@@ -183,6 +183,11 @@
@@ -8111,3 +8116,252 @@ exit=1
```

Coincide con lo que la seccion 1 enumera por archivo (`L-047`, `L-053`, `A-018`, `D-153`, `D-155`,
`D-156`, `T-175` a `T-179`, fichas de `F-104` a `F-106`, `CLAUDE.md` y `protocol-close`).

### j) Paso 2e — caracteres de control en los tres archivos que la tabla del informe no cubre

```
$ for f in _audit/S-038.md _audit/index.md _persistence/progress.md; do echo "$f $(git show e822ae3:$f | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
_audit/S-038.md 0
_audit/index.md 0
_persistence/progress.md 0
```

### k) Paso 2d — equivalente anclado de la lista de ordenes

```
$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
18
```

Cuadra con el `18` publicado. La procedencia por archivo no cuadra con la publicada — vease `F-108`.

## 2. Hallazgos

### F-107 — La nota que corrige `F-105` publica, en `assumptions.md`, `findings.md` y `tasks.md`, una cifra que ya no reproducia en su propio commit, y `T-176` queda `Implementada`

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/assumptions.md` (`A-018`, nota `T-176`), `_audit/findings.md` (ficha de `F-105`), `_persistence/tasks.md` (`T-176`) |

- **Que se observo:** las tres copias del bucle de desfase con el esqueleto publican `0` para
  `.claude/skills/protocol-close/SKILL.md`. En `e822ae3` esa cifra es `2`, porque `T-178` edito ese
  archivo en la misma sesion (dos lineas del 7c-quater) despues de correr la orden. El esqueleto sigue
  en el mismo commit, asi que el desfase sale entero de este lado — exactamente el mecanismo de `F-105`:

```
$ git show e822ae3:_persistence/assumptions.md | grep -n '0  .claude/skills/protocol-close/SKILL.md'
1131:  0  .claude/skills/protocol-close/SKILL.md
1178:  0  .claude/skills/protocol-close/SKILL.md

$ git show e822ae3:_audit/findings.md | grep -n '0  .claude/skills/protocol-close/SKILL.md'
4749:  0  .claude/skills/protocol-close/SKILL.md
4864:  0  .claude/skills/protocol-close/SKILL.md
4902:  0  .claude/skills/protocol-close/SKILL.md

$ git show e822ae3:_persistence/tasks.md | grep -n '0  .claude/skills/protocol-close/SKILL.md'
8036:  0  .claude/skills/protocol-close/SKILL.md
8062:  0  .claude/skills/protocol-close/SKILL.md
8198:  0  .claude/skills/protocol-close/SKILL.md

$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" log -1 --format='%h %ad' --date=short; for f in CLAUDE.md .claude/skills/protocol-audit/SKILL.md .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _phases/000_preproject.md _phases/005_discovery.md _templates/000_preproject/005_project.md; do printf '%3d  %s\n' "$(git show e822ae3:"$f" | diff --strip-trailing-cr "$ESQ/$f" - | grep -c '^<')" "$f"; done
1748f0a 2026-09-11
  4  CLAUDE.md
  0  .claude/skills/protocol-audit/SKILL.md
  2  .claude/skills/protocol-close/SKILL.md
  0  .claude/skills/protocol-start/SKILL.md
  0  _phases/000_preproject.md
  0  _phases/005_discovery.md
  3  _templates/000_preproject/005_project.md

$ for f in .claude/skills/protocol-close/SKILL.md; do printf '%3d  %s\n' "$(git show e822ae3^:"$f" | diff --strip-trailing-cr "$ESQ/$f" - | grep -c '^<')" "$f"; done
  0  .claude/skills/protocol-close/SKILL.md
```

  Las lineas 1178 (`assumptions.md`), 4902 (`findings.md`) y 8198 (`tasks.md`) son las de esta sesion;
  las demas son las de sesiones anteriores, ya tratadas. `T-176` fija como criterio de cierre que «la
  orden de la nota [...] devuelve la salida que publica», y en su commit no la devuelve; aun asi figura
  `Implementada`. Su verificacion llega a anticipar el riesgo («`T-177` toca [...] `protocol-close` en
  esta misma sesion [...] Se dice aqui para no repetir el defecto», `tasks.md` linea 8210), pero no
  cubre la edicion posterior de `T-178`.

  Ademas, la misma nota bajo `A-018` afirma que la recomendacion de la seccion 5 de `R-036` «**sigue
  sin evaluar**», y el mismo commit la evalua y la rechaza en `D-156`:

```
$ git show e822ae3:_persistence/assumptions.md | grep -n 'sigue sin evaluar'
1197:anclada, o declara en su linea que mide un estado vivo—, que **sigue sin evaluar**.

$ git show e822ae3:_persistence/decisions.md | grep -n '^### D-156'
9969:### D-156 - La seccion 5 de `R-036`: la regla «anclada o viva» no se adopta, y el 7c-quater se endurece
```

  El informe lo declara (secciones 4, 6 y 7), y lo deja sin corregir y sin `T-XXX`: la seccion 6 lo
  remite a que `manager` «decida si merece una tercera nota fechada o si es deuda tecnica».
- **Por que importa:** es la cuarta vez que el registro publica una cifra que su propio commit no
  sostiene por el mismo mecanismo, y esta vez **dentro de la correccion del hallazgo que describia ese
  mecanismo**. Quien lea `A-018` hoy lee un `0` falso y lee que una medida de raiz sigue pendiente de
  evaluar cuando ya se rechazo. Y `T-176` queda `Implementada` con su criterio de cierre incumplido. No
  es `Alta` porque la cifra no sostiene ninguna conclusion (el `2` es promocion pendiente, igual que el
  `3`) y porque el informe lo declara abiertamente; es `Media` porque el registro se contradice en tres
  archivos y el pendiente vuelve a quedar solo en el informe de sesion. A la pregunta de la seccion 6
  del informe: el argumento de `D-156` se sostiene —la regla «anclada o viva» tampoco lo habria evitado,
  las notas ya declaraban medir un estado vivo—, y este caso **confirma su diagnostico**: el cierre lo
  detecto y el pendiente quedo sin tarea, que es lo que `T-179` (aplazada) atacaria.
- **Que lo corregiria:** una nota fechada bajo `A-018` —y la constancia correspondiente junto a
  `T-176`— con la cifra que da el commit y la aclaracion de que `D-156` ya evaluo la recomendacion,
  sin reescribir nada; o bien registrar el pendiente como `T-XXX`/`DT-XXX`, de modo que no quede solo
  en `S-038`. ⚠️ Es una recomendacion, no una orden.

### F-108 — El bloque «Procedencia por archivo» de la seccion 7 de `S-038` contiene una linea que su orden no produce, y la tabla de reejecucion atribuye dos ordenes al archivo equivocado

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-038.md`, seccion 7 (Evidencia del Paso 2d) |

- **Que se observo:** el bloque publicado como salida de la orden de procedencia incluye la linea
  `b/_audit/findings.md :: same file also carries the git -C log line (see above)`, que no es salida de
  ninguna orden: es un comentario escrito dentro de un bloque de salida cruda. El equivalente anclado de
  la misma orden devuelve 18 lineas, sin ella, y con las filas 4 y 5 en `assumptions.md`:

```
$ git show e822ae3:_audit/S-038.md | grep -n 'same file also carries'
252:b/_audit/findings.md :: same file also carries the git -C log line (see above)

$ git diff -U0 e822ae3^ e822ae3 -- _persistence _audit ':(exclude)_audit/S-038.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -c1-70 | cat -n
     1	b/_audit/findings.md :: +$ grep -c '^| Esqueleto de arranque' "C:/User
     2	b/_audit/findings.md :: +$ ESQ="C:/Users/USUARIO/Documents/Company_Tri
     3	b/_audit/findings.md :: +$ git -C "C:/Users/USUARIO/Documents/Company_
     4	b/_persistence/assumptions.md :: +$ ESQ="C:/Users/USUARIO/Documents/Co
     5	b/_persistence/assumptions.md :: +$ git -C "C:/Users/USUARIO/Documents
     6	b/_persistence/decisions.md :: +$ grep -c '^| Esqueleto de arranque' "
     7	b/_persistence/tasks.md :: +$ grep -c 'T-175`, hallazgo `F-104' _persi
     8	b/_persistence/tasks.md :: +$ grep "SDAI_TripleS/project.md\"$" _persi
     9	b/_persistence/tasks.md :: +$ grep -c 'T-176`, hallazgo `F-105' _persi
    10	b/_persistence/tasks.md :: +$ sed -n '/T-176`, hallazgo `F-105/,/^---$
    11	b/_persistence/tasks.md :: +$ grep -c 'Y su ficha, en la misma pasada'
    12	b/_persistence/tasks.md :: +$ grep -nE '^### Y la fila contra la ficha
    13	b/_persistence/tasks.md :: +$ sed -n '/^### Y la fila contra la ficha/
    14	b/_persistence/tasks.md :: +$ grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple
    15	b/_persistence/tasks.md :: +$ grep -noE '[A-Z]{1,2}-[0-9]+' .claude/sk
    16	b/_persistence/tasks.md :: +$ grep -nE '^### Y la fila contra la ficha
    17	b/_persistence/tasks.md :: +$ grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple
    18	b/_persistence/tasks.md :: +$ grep -noE '[A-Z]{1,2}-[0-9]+' .claude/sk
```

  La tabla de reejecucion dice «fila 4 — mismo bucle que la fila 2, segunda copia (en
  `_audit/findings.md`)» y trata la fila 5 como segunda copia en el mismo archivo; las dos estan en
  `_persistence/assumptions.md`. Y el parrafo que la sigue dice que las filas 2, 4 y 10 viven en
  «`_audit/findings.md`, `_persistence/assumptions.md`», cuando la 10 esta en `_persistence/tasks.md`.
- **Por que importa:** la seccion 7 existe para que la procedencia sea derivada y reproducible, y
  publica como salida cruda un texto que la orden no emite. Quien la contraste encuentra una linea de
  mas y dos atribuciones que no cuadran, y tiene que rehacer el barrido para saber cual es la real — el
  mismo tipo de defecto que `F-102` (atribuir salidas a ordenes equivocadas), y peor en un punto: aqui
  la linea no es un error de correspondencia, es texto anadido dentro de un bloque de salida. `Media`,
  no `Alta`: el recuento (`18`), las repetidas y las conclusiones sobre que reproduce y que no son
  correctas.
- **Que lo corregiria:** una nota fechada en la seccion 7 de `S-038` con la salida real de la orden de
  procedencia (anclada) y la atribucion correcta de las filas 4, 5 y 10, sin reescribir el bloque; y,
  hacia adelante, que ningun comentario vaya dentro de un bloque de salida. ⚠️ Es una recomendacion, no
  una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-104` | **Implementado** (commit `e822ae3`) | La nota fechada bajo `D-153` (linea 9851) publica la orden con la ruta literal; corrida, devuelve el `0` publicado. El bloque original no se reescribio — seccion 1g |
| `F-105` | **Implementado** (commit `e822ae3`) | La segunda nota bajo `A-018` deja constancia del desfase de la septima cifra, con la salida de hoy (`3`), que cuadra con el commit, sin reescribir la nota de `T-173` — seccion 1g. ⚠️ Que la propia nota publique otra cifra desfasada dentro de su commit es **evidencia nueva** y va como `F-107`, no como reapertura |
| `F-106` | **Implementado** (commit `e822ae3`) | `CLAUDE.md` nombra fila y ficha (linea 166); el Paso 2b de `protocol-close` lleva el barrido y su linea de reporte; extraido del commit, sale vacio sobre `e822ae3`, falla sobre `3b20720` y detecta los casos intermedios — seccion 1g |

## 4. Lo que NO se pudo comprobar

- **Las ordenes del Paso 2d y 2e que interrogan el area de staging** (`git diff --cached ...`) no se
  pueden reejecutar: ese staging ya no existe. Se contrastaron por su equivalente anclado
  (`git diff e822ae3^ e822ae3`), secciones 1j y 1k.
- **El bucle de desfase con el esqueleto, la orden de `D-153` y el Paso 2f** miden un repositorio
  externo (`SDAI_TripleS`, hoy en `1748f0a`) que ningun commit de este fija. Se reejecutaron contra su
  estado de hoy, y se dice asi.
- **Las verificaciones de `T-175` a `T-178` que escriben en `$TEMP/sc/`** no se reejecutaron tal cual;
  se reprodujo su contenido con ordenes equivalentes ancladas a `e822ae3` (secciones 1g y 1h).

## 5. Recomendaciones sin hallazgo

- La seccion 2 y la 5 del informe dicen que a `T-179` «le falta decidir su `Origen`», pero `T-179`
  lleva `Origen: manager`; lo que su ficha deja pendiente es el `Origen` de las tareas **que abriria el
  cierre**. Conviene que el proximo informe no confunda las dos cosas.
- La NOTA DE CIERRE usa `/tmp/prosa_antes` en su CONTROL DE PROSA BORRADA; en este entorno el resto de
  ordenes usan `$TEMP`. No cambia el resultado, pero una orden que se pega en otra maquina reproduce
  mejor con la misma convencion de temporales.
