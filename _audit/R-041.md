# Auditoria R-041 — sobre S-041

| Campo | Valor |
|---|---|
| Auditoria | R-041 |
| Informe auditado | `_audit/S-041.md` |
| Commit auditado | `e332771` |
| Fecha | 2026-09-14 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

### a) Sobre que commit se audita

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep Pendiente
| `S-041.md` | S-041 | 2026-09-14 | Pendiente | Pendiente | Pendiente | - |

$ git log -1 --format=%h -- _audit/S-041.md
13ce2cb

$ git show 13ce2cb:_audit/S-041.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `e332771` |

$ git show --stat --format= 13ce2cb
 _audit/S-041.md       | 115 ++++++++++++++++++++++++++++++++++++++++++++++++++++----
 _persistence/tasks.md |  27 +++++++++---
 2 files changed, 129 insertions(+), 13 deletions(-)

$ git show --numstat --format= e332771
1	1	.claude/skills/protocol-promote/SKILL.md
15	0	_audit/S-039.md
40	0	_audit/S-040.md
341	0	_audit/S-041.md
79	12	_audit/findings.md
1	0	_audit/index.md
0	0	_outbound/.gitkeep
0	187	_outbound/skeleton_readme.md
194	0	_persistence/decisions.md
6	0	_persistence/lessons.md
108	74	_persistence/progress.md
150	2	_persistence/tasks.md
```

`13ce2cb` es el commit de anclaje (informe + `tasks.md`, que el Paso 7c/7c-bis de `protocol-close`
autoriza). Se audita el hash literal de la cabecera, `e332771`, cuyos 12 archivos coinciden con la
lista anclada de la seccion 1 del informe. El informe se leyo en su version de `13ce2cb`.

### b) Controles mecanicos sobre `e332771`

```
$ h=e332771; for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show $h:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show $h:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress

$ diff <(git ls-tree -d --name-only $h | sed 's|$|/|' | sort) <(git show $h:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
9a11
> temporal/

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" $h -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1

$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' $h -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1

$ diff <(git show $h:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) <(git show $h:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u); echo "exit=$?"
exit=0
```

- Indices de `_persistence/` y de `findings.md`: coherentes.
- Carpetas: las dos diferencias (`010_prototype/`, `temporal/`) tienen su razon escrita en
  `project.md`, «Carpetas propias». `_outbound/` esta en el arbol (por `.gitkeep`, `D-165`). No es
  hallazgo.
- Fuga de datos propios y codigos instanciados: cero lineas.

### c) Pasos 2d, 7c-ter, 7c-quater, 7c-bis y 7d del cierre, reproducidos

```
$ git diff -U0 a3bb32e e332771 -- _persistence _audit ':(exclude)_audit/S-041.md' | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
25

$ for m in "**BARRIDO DE ANCLAJE — salida:**" "**CONTROL DE PROSA BORRADA — salida:**" "**SEGUNDA PASADA anclada del Paso 2e — salida:**" "**CONTROL DE CIFRA ADYACENTE — salida:**"; do git show 13ce2cb:_audit/S-041.md | grep -qF "$m" || echo "FALTA $m"; done; git show 13ce2cb:_audit/S-041.md | grep -qE '^(> )?\$ git show --stat --name-only --format= e332771$' || echo "FALTA orden seccion 1"; echo "fin 7c"
fin 7c

$ for f in $(git diff --name-only 13ce2cb^ 13ce2cb -- _persistence _audit ":(exclude)_audit/S-041.md"); do n=$(git diff -U0 13ce2cb^ 13ce2cb -- "$f" | grep -cE '^\+\$ git show <hash>:'); echo "$f: $n"; done
_persistence/tasks.md: 0

$ git log -1 --format=%ad --date=short e332771; git show 13ce2cb:_audit/S-041.md | grep -m1 '^| Fecha |'
2026-09-14
| Fecha | 2026-09-14 |
```

El recuento 25 del Paso 2d, los cuatro rotulos, la orden de la seccion 1 y la fecha reproducen. El
anclaje de `13ce2cb` sustituye las 5 ordenes con `<hash>` de `T-183` a `T-186` y deja cero nuevas.

### d) Seccion 6 del informe, punto 1 — ninguna nota reescribe un bloque anterior

```
$ git show e332771 -- _audit/S-039.md _audit/S-040.md _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md _audit/findings.md | grep -E '^-[^-]'
-| [F-109](#f-109---la-seccion-1-del-informe-atribuye-a-t-180-las-tres-reincidencias-de-la-nota-de-l-056-y-la-tercera-es-de-t-179) | La seccion 1 del informe atribuye a `T-180` las tres reincidencias de la nota de `L-056`, y la tercera es de `T-179` | R-039 | Baja | Abierto |
-| [F-110](#f-110---la-seccion-7-de-s-040-publica-como-salida-cruda-dos-bloques-que-su-orden-no-produce) | La seccion 7 de `S-040` publica como salida cruda dos bloques que su orden no produce | R-040 | Media | Abierto |
-| [F-111](#f-111---dos-resultados-nuevos-del-registro-se-afirman-sin-su-orden-ni-su-salida) | Dos resultados nuevos del registro se afirman sin su orden ni su salida | R-040 | Baja | Abierto |
-| [F-112](#f-112---el-paso-6-de-protocol-promote-queda-con-dos-pasos-numerados-4) | El Paso 6 de `protocol-promote` queda con dos pasos numerados «4.» | R-040 | Baja | Abierto |
-| Estado | Abierto |
-| Registrado en | |
-| Estado | Abierto |
-| Registrado en | |
-| Estado | Abierto |
-| Registrado en | |
-| Estado | Abierto |
-| Registrado en | |
-| [T-162](#t-162---escribir-la-guia-de-arranque-del-esqueleto) | Escribir la guia de arranque del esqueleto | No implementada | Media | No bloqueante | `000_preproject` |
-| Estado | No implementada |
```

Las unicas lineas retiradas son campos de estado. Ningun bloque ni linea de prosa de `S-039`,
`S-040`, `decisions.md`, `lessons.md` o `tasks.md` se reescribe. Correcto.

### e) Las ordenes de las cuatro correcciones, reejecutadas

```
$ git show 62ff0d2:_persistence/lessons.md | grep -n 'Nota de reincidencia del 2026-09-14 (`T-180`\|tercera vez en la misma jornada (`T-179`)'
2084:> 🕒 **Nota de reincidencia del 2026-09-14 (`T-180`, `F-107`).** Volvio a pasar dos veces en la misma
2096:> ⚠️ **Y una tercera vez en la misma jornada (`T-179`)**, ahora con la salida **tecleada** junto a la

$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | uniq -c
     16 b/_persistence/decisions.md

$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | grep -c 'git show db0e613:.gitignore'
0

$ for h in 4962db2 97ccbe1; do git show $h:_audit/S-040.md | awk '/^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next } infence { next } armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print NR; if (since>3) armed=0 }' | tr '\n' ' '; echo; done
33 40 41 104 105 256 257 310 311 349 350 396 397 412 413 
48 114 115 266 267 320 321 364 375 397 418 419 446 447 493 494 509 510 

$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-harvest/SKILL.md .claude/skills/protocol-promote/SKILL.md _templates/000_preproject/005_project.md _templates/000_preproject/015_tasks.md CLAUDE.md; do printf '%s  origen CR=%s LF=%s  esqueleto CR=%s LF=%s\n' "$f" "$(git show db0e613:$f | tr -dc '\r' | wc -c)" "$(git show db0e613:$f | tr -dc '\n' | wc -c)" "$(git -C "$ESQ" show 1748f0a:$f 2>/dev/null | tr -dc '\r' | wc -c)" "$(git -C "$ESQ" show 1748f0a:$f 2>/dev/null | tr -dc '\n' | wc -c)"; done
.claude/skills/protocol-close/SKILL.md  origen CR=0 LF=2087  esqueleto CR=0 LF=1929
.claude/skills/protocol-harvest/SKILL.md  origen CR=0 LF=312  esqueleto CR=0 LF=312
.claude/skills/protocol-promote/SKILL.md  origen CR=0 LF=349  esqueleto CR=0 LF=0
_templates/000_preproject/005_project.md  origen CR=0 LF=289  esqueleto CR=0 LF=271
_templates/000_preproject/015_tasks.md  origen CR=0 LF=110  esqueleto CR=0 LF=109
CLAUDE.md  origen CR=0 LF=520  esqueleto CR=0 LF=516

$ git show e332771:.claude/skills/protocol-promote/SKILL.md | grep -nE '^[0-9]+\. ' | sed -n '1,5p'
304:1. se copia cada archivo aprobado, **respetando el final de linea del destino** (Paso 4);
305:2. las carpetas nuevas se crean con su contenido **completo**, no a medias;
306:3. cada copia de la raiz aprobada se regenera **desde la plantilla ya copiada al esqueleto**, en el
308:4. commit en el esqueleto, con un mensaje que diga **de que proyecto viene** la promocion, **que
310:5. push.
```

Todo lo que publican las notas de `T-183`, `T-184` y `T-185` reproduce, y la de `T-186` esta en el
diff. Los barridos rehechos de la nota bajo `T-162` (`4962db2`, `exit=1` los dos) coinciden con los
que `R-040` corrio con otro patron.

### f) Seccion 6, puntos 2 y 3 — lo afirmado del esqueleto (solo lectura, `git -C`)

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" log --oneline -6
bf8c56e Publica la guia de arranque como README.md (origen a3bb32e)
afeedf4 Promocion del andamiaje desde RaidomAI_App (origen a3bb32e)
c7d0a87 Regenera las copias de la raiz desde sus plantillas (origen 841dc53)
841dc53 Promocion del andamiaje desde RaidomAI_App (origen db0e613)
1748f0a Sincroniza las seis areas agnosticas con el proyecto de origen
fa7da56 Estado de partida del esqueleto, tal como estaba antes de sincronizar

$ git -C "$ESQ" show --numstat --format='%h %s' afeedf4
afeedf4 Promocion del andamiaje desde RaidomAI_App (origen a3bb32e)

67	2	.claude/skills/protocol-promote/SKILL.md

$ git -C "$ESQ" show afeedf4:.claude/skills/protocol-promote/SKILL.md | cmp - <(git show a3bb32e:.claude/skills/protocol-promote/SKILL.md) && echo identicos
identicos

$ git -C "$ESQ" show afeedf4:.claude/skills/protocol-promote/SKILL.md | grep -nE '^[0-9]+\. ' | sed -n '4,5p'
308:4. commit en el esqueleto, con un mensaje que diga **de que proyecto viene** la promocion, **que
310:4. push.

$ git -C "$ESQ" show --numstat --format='%h %s' bf8c56e
bf8c56e Publica la guia de arranque como README.md (origen a3bb32e)

187	0	README.md

$ git -C "$ESQ" show bf8c56e:README.md | cmp - <(git show a3bb32e:_outbound/skeleton_readme.md) && echo identicos
identicos

$ git -C "$ESQ" show bf8c56e:README.md | grep -n '^## '
8:## Que es esto, y que no
24:## 1. Arrancar un proyecto nuevo
52:## 2. Lo que un clone NO trae
66:## 3. Rellenar: los diez archivos de instancia
130:## 4. Lo que no se toca
154:## 5. El primer ciclo de trabajo
178:## 6. Las dos cosas que hay que entender antes de empezar

$ git -C "$ESQ" show bf8c56e:README.md | grep -cE '^\| (10|[1-9]) \| `'
10

$ git ls-tree -r --name-only e332771 _outbound
_outbound/.gitkeep
```

`afeedf4` es identico byte a byte a `a3bb32e` para `protocol-promote/SKILL.md` y lleva el «4.»
duplicado, tal como declara `D-163`. `bf8c56e` es identico al borrador de `a3bb32e`, y el borrador ya
no esta en `e332771` (187 lineas borradas, arriba). Correcto.

### g) Paso 2f del cierre, reejecutado sobre los arboles vivos

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-promote/SKILL.md and .claude/skills/protocol-promote/SKILL.md differ
```

Una linea, la que el informe declara. Correcto.

## 2. Hallazgos

### F-113 — `progress.md` publica tres veces «8 filas» para la tabla del Paso 2e, que tiene 10

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/progress.md` en `e332771`, lineas 95 («Avance de la etapa»), 149 y 2079 (entrada `S-041`) |

- **Que se observo:** `progress.md` dice «con los dos contrastes cuadrando (8 filas, suma 13)». La
  tabla de la seccion 8 del informe tiene 10 filas, el informe lo dice, y el contraste anclado da 10.

```
$ git show e332771:_persistence/progress.md | grep -noE 'cuadrando \(8( filas, suma 13\)|$)'
95:cuadrando (8 filas, suma 13)
149:cuadrando (8
2079:cuadrando (8 filas, suma 13)

$ git show e332771:_audit/S-041.md | sed -n '/^## 8/,/^## 9/p' | grep -cE '^[._].* head=[0-9]+ staged=[0-9]+$'
10

$ git diff --name-only --diff-filter=d a3bb32e e332771 -- ':(exclude)_audit/S-041.md' | wc -l
10

$ git show 13ce2cb:_audit/S-041.md | grep -n 'La tabla tiene 10 filas'
367:> _audit/S-041.md:320: La tabla tiene 10 filas (coincide con el primer contraste) y la suma de su columna `staged`
420:La tabla tiene 10 filas (coincide con el primer contraste) y la suma de su columna `staged`
```

  (La linea 149 sigue en la 150 con «filas, suma 13)».) El 8 coincide con 10 menos `_audit/index.md`
  y `_persistence/progress.md`, los dos que el propio cierre anade: es una cifra tomada antes de que
  el area de staging estuviera completa, y no se rehizo.
- **Por que importa:** `progress.md` es lo primero que se lee al abrir sesion, y afirma un resultado
  de control que su propio informe contradice. Es la forma «cifra que nace desfasada» que el Paso 6b
  y el CONTROL DE CIFRA ADYACENTE persiguen, y pasa porque ese control solo se corre sobre el informe,
  no sobre `progress.md`. `Media`: el registro afirma algo comprobablemente falso; `Alta` no, porque la
  conclusion («cuadra», 13 ocurrencias heredadas) es cierta.
- **Que lo corregiria:** nota fechada en `progress.md` junto a las tres menciones con el recuento
  anclado (10), sin reescribirlas; y valorar si el control de cifra adyacente del cierre debe cubrir
  tambien las cifras de control que `progress.md` repite. ⚠️ Es una recomendacion, no una orden.

### F-114 — La seccion 2 de `S-041` publica como salida de su `grep` cuatro filas, y la orden devuelve cinco

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-041.md`, seccion 2, bloque `sed -n '/^## Indice/,/^---/p' _persistence/tasks.md \| grep "No implementada"` |

- **Que se observo:** el bloque pega la orden y cuatro filas (`T-001`, `T-128`, `T-144`, `T-156`). La
  orden devuelve ademas la de `T-039`, que el parrafo siguiente reconoce como falso positivo y dice
  «no forma parte de esta lista»: la linea se quito del bloque de salida, no de la lista.

```
$ git show 13ce2cb:_audit/S-041.md | sed -n '/^## 2\./,/^## 3\./p' | grep -c '^| \[T-'
4

$ git show e332771:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep "No implementada" | grep -oE '^\| \[T-[0-9]+\]|\| (No implementada|Implementada) \|' | paste - -
| [T-001]	| No implementada |
| [T-039]	| Implementada |
| [T-128]	| No implementada |
| [T-144]	| No implementada |
| [T-156]	| No implementada |
```

- **Por que importa:** es la familia de `F-108` y `F-110` (un bloque de salida cruda que no es lo que
  su orden emite), en la sesion que corrige `F-110`. `protocol-close` lo dice sin matiz: «una salida
  recortada y presentada como cruda es peor que no publicarla». `Baja` y no `Media`: a diferencia de
  aquellos, el recorte esta declarado en la linea siguiente y la lista resultante es correcta.
- **Que lo corregiria:** nota fechada en la seccion 2 de `S-041` con la salida entera (cinco filas),
  dejando el filtrado del falso positivo en la prosa, no en el bloque; o un filtro por columna en la
  propia orden. ⚠️ Es una recomendacion, no una orden.

### F-115 — Dos comprobaciones se declaran «no anclables» cuando si lo son, y una de ellas se afirma sin orden ni salida

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-041.md` seccion 7, fila 16 (`git ls-files _outbound`, de `D-165`); `_persistence/decisions.md`, `D-163`, vineta «Barrido de despues (Paso 7.2)» |

- **Que se observo:** (a) la fila 16 de la tabla de reejecucion clasifica `git ls-files _outbound`
  como «orden sobre el arbol de trabajo, no anclable a un commit por naturaleza». `protocol-close`
  dice lo contrario para archivos versionados, y la forma anclada existe. (b) `D-163` afirma que el
  barrido del Paso 7.2 «salio vacio» y lo justifica con «Mide arboles vivos y no se puede anclar», sin
  publicar orden ni salida. `protocol-promote` pide ese barrido «con su salida», y `D-159`, una sesion
  antes, lo publico en forma anclada (`fin del contraste`). La forma anclada, corrida ahora, confirma
  que el resultado es cierto.

```
$ git show 13ce2cb:_audit/S-041.md | grep -n '^| 16 |'
249:| 16 | Si | `_outbound/.gitkeep` | orden sobre el arbol de trabajo, no anclable a un commit por naturaleza (lista lo que existe hoy) |

$ git show e332771:.claude/skills/protocol-close/SKILL.md | grep -n 'sobre archivos versionados NO es un ejemplo'
1804:⛔ **Un `ls` sobre archivos versionados NO es un ejemplo de orden no anclable**, y decia lo

$ git ls-tree -r --name-only e332771 _outbound
_outbound/.gitkeep

$ git show e332771:_persistence/decisions.md | grep -n 'Mide arboles vivos y no se puede anclar'
10587:  y `bf8c56e`, salio vacio: **el desfase cerro**. Mide arboles vivos y no se puede anclar; lo anclado

$ git show e332771:.claude/skills/protocol-promote/SKILL.md | grep -n 'El barrido del Paso 1, corrido otra vez despues'
345:2. **El barrido del Paso 1, corrido otra vez despues**, con su salida: es lo que demuestra que lo

$ git show 4962db2:_persistence/decisions.md | sed -n '/^### D-159/,/^### D-160/p' | grep -c 'fin del contraste'
2

$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in $(git ls-tree -r --name-only a3bb32e -- .claude CLAUDE.md _phases _methodology _templates _workflow); do git show a3bb32e:"$f" | diff -q --strip-trailing-cr <(git -C "$ESQ" show afeedf4:"$f" 2>/dev/null) - >/dev/null || echo "difiere  $f"; done; for f in $(git -C "$ESQ" ls-tree -r --name-only afeedf4 -- .claude CLAUDE.md _phases _methodology _templates _workflow); do git cat-file -e a3bb32e:"$f" 2>/dev/null || echo "solo en el esqueleto  $f"; done; echo "fin del contraste"
fin del contraste
```

  Lo mismo vale, en menor medida, para «Desfase medido (Paso 1): un solo candidato… nada solo en el
  esqueleto» de `D-163`, que tampoco lleva orden ni salida.
- **Por que importa:** es la clase de `F-111` en la sesion que la corrige: un resultado del registro
  sin orden, y ademas con una razon escrita («no se puede anclar») que el repositorio desmiente. Si
  «no anclable» se acepta sin contraste, sirve para no publicar evidencia de cualquier cosa. `Baja`:
  los dos resultados son ciertos; el defecto es de reproducibilidad.
- **Que lo corregiria:** nota fechada bajo `D-163` con el contraste anclado `a3bb32e`↔`afeedf4` y su
  salida; y nota en la seccion 7 de `S-041` o bajo `D-165` con la forma anclada `git ls-tree`. ⚠️ Es
  una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-109 | Implementado (`e332771`) | La nota fechada de `T-183` esta al final de la seccion 1 de `S-039`, sin reescribir la vineta, y su orden anclada a `62ff0d2` devuelve lo que publica (1e) |
| F-110 | Implementado (`e332771`) | La nota de `T-184` en la seccion 7 de `S-040` reproduce: 16 lineas, 0 de `.gitignore`, y el control de cifra adyacente sobre `4962db2` y `97ccbe1` da las listas publicadas (1e). No reescribe bloques (1d) |
| F-111 | Implementado (`e332771`) | Nota bajo `T-162` con los dos barridos anclados a `4962db2` (`exit=1`), y nota bajo `D-159` con la medicion `tr -dc` sobre `db0e613`/`1748f0a`, que reproduce linea a linea (1e) |
| F-112 | Implementado (`e332771`) | El push del Paso 6 es «5.» en `e332771` (1e). El esqueleto conserva el «4.» en `afeedf4` a sabiendas, registrado en `D-163`; el hallazgo era sobre la skill de este repositorio, y la subida pendiente la mide el Paso 2f (1g) |

Ningun otro `F-NNN` estaba sin cerrar.

## 4. Lo que NO se pudo comprobar

- **El orden temporal que `D-163` declara** (promover primero, con el defecto dentro, y corregir
  `F-112` despues) y que el barrido de despues del Paso 7.2 saliera vacio **en el momento**: ocurrio en
  arboles vivos que ya no existen. Solo es comprobable su forma anclada, que se corrio en `F-115`.
- **Que el usuario aprobara las puertas** de `D-163` y `D-164`: no deja rastro en el repositorio.

Todos los controles mecanicos se pudieron correr.

## 5. Recomendaciones sin hallazgo

- Los criterios de cierre de `T-183` a `T-186` dicen «su orden… devuelve lo que publica», pero la
  orden anclada solo cuenta la cabecera de la nota (`grep -c 'T-18X`, hallazgo…'`). La segunda mitad
  se puede verificar corriendo las ordenes de cada nota (esta auditoria lo hizo), pero el bloque del
  criterio no la prueba. Conviene que el criterio diga solo lo que su orden comprueba.
- `D-165` enumera sus alternativas descartadas empezando por «(b)», sin «(a)».
- El CONTROL DE CIFRA ADYACENTE solo mira el informe; las cifras de control que `progress.md` repite
  (origen de `F-113`) quedan fuera de todo control mecanico.
