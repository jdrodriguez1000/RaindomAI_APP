# Auditoria R-005 — sobre S-005

| Campo | Valor |
|---|---|
| Auditoria | R-005 |
| Informe auditado | `_audit/S-005.md` |
| Commit auditado | `510d580` |
| Fecha | 2026-09-01 |
| Veredicto | Con hallazgos (4) |

## 1. Que se comprobo

**a) Anclaje del informe a su commit.**

```
$ git log -1 --format=%h -- _audit/S-005.md
510d580
```

El informe viaja dentro del commit que describe. Correcto.

**b) Indice ↔ detalle en los siete archivos de `_persistence/`, sobre el commit auditado.**

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show 510d580:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 510d580:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Cero diferencias en los siete. Incluye las cinco tareas nuevas (`T-009`..`T-013`) y las cinco
decisiones nuevas (`D-022`..`D-026`).

**c) Carpetas del arbol contra las declaradas en `project.md`.**

```
$ diff <(git ls-tree -d --name-only 510d580 | sed 's|$|/|' | sort) \
       <(git show 510d580:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
5a6
> temporal/
```

Unica diferencia: `temporal/`, fila declarada sin carpeta en el arbol. **Tiene su razon escrita** en
`project.md` (excluida por `.gitignore`, area de trabajo del usuario). No es hallazgo.
`_phases/` aparece ya en las dos listas: la carpeta nueva se declaro en la misma pasada.

**d) Fuga de datos propios en lo agnostico, con el ambito nuevo que fija `D-026`.**

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" 510d580 -- .claude CLAUDE.md _phases ; echo "rc=$?"
rc=1
```

Cero lineas. Ampliado a las dos grafias del nombre, el resultado es el mismo:

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 510d580 -- .claude CLAUDE.md _phases ; echo "exit=$?"
exit=1
```

**e) `_phases/000_preproject.md` es agnostico** —lo pidio expresamente la seccion 6 del informe—.
Reproducido sobre el commit, no sobre el arbol de trabajo:

```
$ git grep -nE "[A-Z]{1,2}-[0-9]+" 510d580 -- _phases/000_preproject.md ; echo "exit=$?"
exit=1
```

Ningun codigo instanciado. Lectura completa del archivo (246 lineas): ni un nombre de proyecto, ni
una ruta de disco, ni un host. Correcto.

**f) Los bloques de verificacion de las cinco decisiones nuevas, reproducidos sobre `510d580`.**

```
$ git grep -n "debtec" 510d580 -- .claude CLAUDE.md project.md ; echo "exit=$?"    # D-022, acotado
exit=1

$ git grep -c "debtec" 510d580 -- . | wc -l                                        # D-022, global
13

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 510d580 -- _phases ; echo "exit=$?"   # D-023
exit=1

$ git show 510d580:project.md | sed -n '/^| Etapas declaradas/,/^| Etapas posteriores/p'               # D-024
| Etapas declaradas | `000_preproject`, `005_discovery` |
| Etapas posteriores a `005_discovery` | **no registradas** |

$ git show 510d580:_persistence/tasks.md | grep -oE "^\| Etapa \| .[0-9_a-z]+" | sort | uniq -c        # D-025
     11 | Etapa | `000_preproject
      2 | Etapa | `005_discovery
      1 | Etapa | una

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 510d580 -- .claude CLAUDE.md _phases ; echo "exit=$?"   # D-026
exit=1
```

🔑 **El recuento global de `D-022` da 13 sobre el commit y la entrada registro 12** — y **eso es
correcto**, porque la propia entrada lo declara como estado «al momento de escribir esta entrada» y
avisa de que «sera otro numero en el commit». Es exactamente lo que `D-022` obliga a hacer, aplicado
contra si misma. El acotado (`exit=1`) si se reproduce, como la entrada afirma. **Este punto, que la
seccion 6 pidio auditar, sale limpio.**

**g) Las notas fechadas ancladas a un commit, reproducidas literalmente.**

```
$ git grep -cE '`auditor`|\*\*auditor\*\*|Origen: auditor|agente auditor' ea0b850 -- . ; echo "exit=$?"   # nota 2 de D-016
ea0b850:_audit/R-002.md:28
ea0b850:_audit/S-001.md:1
ea0b850:_audit/S-002.md:10
ea0b850:_audit/S-003.md:3
ea0b850:_audit/findings.md:6
ea0b850:_persistence/assumptions.md:1
ea0b850:_persistence/decisions.md:14
ea0b850:_persistence/lessons.md:3
ea0b850:_persistence/progress.md:8
ea0b850:_persistence/tasks.md:4
exit=0

$ git grep -n "debtec" c70b757 -- _persistence/decisions.md | awk -F: '$3>=573' | wc -l                  # nota 1 de D-021
9

$ git grep -n "debtec" 510d580 -- _audit/findings.md | head -1                                           # nota 2 de D-021
510d580:_audit/findings.md:56:rechazo por coste sin entrada en `debtec.md` es, por si solo, un hallazgo nuevo — y no requiere

$ git grep -n "debtec" 510d580 -- .claude CLAUDE.md project.md ; echo "exit=$?"                          # nota de DT-001
exit=1
```

Las cuatro coinciden **caracter por caracter** con lo registrado. Las que van ancladas a un hash
(`ea0b850`, `c70b757`) y las que van a `HEAD` con ambito acotado se reproducen sobre `510d580`.

**h) El informe contra el diff.** Los catorce archivos que la seccion 1 del informe enumera son
exactamente los catorce que muestra `git show --stat 510d580`, con el contenido que describe:
ampliacion del ambito en los dos skills, `CLAUDE.md` nombrando `_phases/`, cinco notas fechadas, cinco
`D-XXX` nuevas, `L-008`, campo `Etapa` en `tasks.md`, `_phases/000_preproject.md` nuevo y `project.md`
declarando `005_discovery` y `_phases/`. **Ninguna afirmacion de la seccion 1 quedo sin respaldo, y no
se encontro en el diff ningun cambio relevante que el informe no mencione.**

**i) La excepcion de `D-025` no se uso como puerta** —lo pidio la seccion 6—. Sobre `tasks.md`, las
unicas dos filas movidas de etapa son `T-001` y `T-002`, cada una con su nota fechada citando
`D-023`, `D-024` y `D-025`:

```
$ git grep -n "005_discovery" 510d580 -- _persistence/tasks.md | head -3
510d580:_persistence/tasks.md:12:| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | ... | `005_discovery` |
510d580:_persistence/tasks.md:13:| [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject) | ... | `005_discovery` |
510d580:_persistence/tasks.md:108:| Etapa | `005_discovery` |
```

Dos de trece. El uso declarado coincide con el uso real. **Criterio que propongo, ya que se pidio
uno:** la excepcion no se mide por cantidad sino por cita — **toda fila editada a mano debe llevar un
`D-XXX` o `F-NNN` en su propia nota fechada**. Una sola edicion sin cita es el umbral; cincuenta con
cita, no. Contar filas invitaria a repartir el mismo cambio en dos sesiones para pasar por debajo.

## 2. Hallazgos

### F-011 — La nota nueva de `A-001` vuelve a registrar un recuento sobre `HEAD` que no se reproduce sobre su commit
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/assumptions.md`, nota del 2026-09-01 bajo `A-001` (`T-009`) |

- **Que se observo:** la nota que corrige `F-005` cierra el enunciado de la señal 2 rehecha con este
  bloque, presentado como la prueba de que «hoy la señal sigue sin cumplirse»:

```
$ git grep -n "| Pendiente |" HEAD -- _audit/index.md ; echo "exit=$?"
exit=1
```

  Sobre el commit que contiene esa nota, el mismo comando devuelve lo contrario:

```
$ git grep -n "| Pendiente |" 510d580 -- _audit/index.md ; echo "exit=$?"
510d580:_audit/index.md:16:| `S-005.md` | S-005 | 2026-09-01 | Pendiente | Pendiente | Pendiente | - |
exit=0
```

  Segunda instancia del mismo defecto, en `_persistence/tasks.md`, ficha de `T-012`: «criterio
  absoluto que la entrada no cumple —el comando devuelve **12** archivos en `HEAD`—», sin fecha ni
  ancla. Sobre el commit son 13:

```
$ git grep -c "debtec" 510d580 -- . | wc -l
13
```

- **Por que importa:** es el patron de `F-005`, `F-006` y `F-008` reapareciendo **dentro de la
  correccion de `F-005`**, y contra la regla que esta misma sesion escribio (`D-022`, regla 1: un
  bloque cuyo ambito incluye lo que la sesion todavia va a escribir se enuncia con su fecha, no con
  su commit). `_audit/index.md` es justo uno de esos archivos. El daño concreto: quien reproduzca el
  bloque de `A-001` sobre el commit obtiene `exit=0` y concluye que la señal 2 **si** se disparo —lo
  contrario de lo que la nota afirma—, y esa señal es una de las dos que pueden refutar `A-001`.
  Gravedad `Media` y no `Alta` porque el fondo de la correccion es correcto: la señal quedo bien
  redefinida («al abrirse la sesion siguiente») y el bloque original quedo intacto como manda
  `D-019`; lo que falla es, otra vez, la declaracion de ambito temporal.
- **Que lo corregiria:** una nota fechada mas bajo `A-001` que ancle el comando a un hash o que lo
  enuncie con su fecha —«al momento de escribir esta nota, `HEAD` era `e61454b`»—, y lo mismo en la
  ficha de `T-012` para la cifra 12. ⚠️ Es una recomendacion, no una orden. **Y el asunto de fondo no
  es la nota:** `D-022` sigue sin mecanismo que la aplique (ver seccion 5), que es literalmente lo
  que `L-008` describe.

### F-012 — La segunda excepcion de `D-025` no llego a los tres sitios que siguen diciendo «es la unica excepcion»
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `.claude/skills/protocol-close/SKILL.md:40`, `.claude/agents/session-closer.md:68`, `_phases/000_preproject.md:182` |

- **Que se observo:** `D-025` generaliza la excepcion de escritura de `manager` sobre `tasks.md` a un
  segundo caso, y la escribe en la convencion de `tasks.md`. Los tres sitios que enuncian la regla
  para quien la ejecuta siguen afirmando que la excepcion es unica:

```
$ git grep -n "unica excepcion\|salvo la .T-XXX" 510d580 -- .claude CLAUDE.md _phases _persistence/tasks.md
510d580:.claude/agents/session-closer.md:68:auditable. **No la dupliques ni la reescribas:** comprueba que esta y sigue. Es la unica excepcion a
510d580:.claude/skills/protocol-close/SKILL.md:40:auditable. **No la dupliques ni la reescribas:** comprueba que esta y sigue. Es la unica excepcion a
510d580:.claude/skills/protocol-close/SKILL.md:447:**La unica excepcion, y es mecanica:** si un supuesto `A-XXX` quedo comprobado por la evidencia del
510d580:_persistence/tasks.md:57:con `progress.md`. **Tiene una unica excepcion, y esta escrita:** cuando `manager` evalua un hallazgo
510d580:_phases/000_preproject.md:182:| `tasks.md` | las tareas y su estado | **`session-closer`**, salvo la `T-XXX` que nace de un hallazgo aceptado |
```

  La linea 447 de `protocol-close` es de otro asunto —supuestos— y no cuenta. La linea 57 de
  `tasks.md` tambien conserva «una unica excepcion», pero ahi el texto que sigue la corrige de forma
  explicita («Y tiene una segunda excepcion… Son dos excepciones, no una puerta»), asi que se lee
  entera y no engaña. Los otros tres no tienen nada que los corrija.
- **Por que importa:** es exactamente `F-007` otra vez, y `F-007` se cerro en `S-004` arreglando
  **esos mismos tres sitios**. `L-007` —«una excepcion se escribe donde esta la regla, no donde se
  decidio»— lleva escrita desde `S-004`. El `session-closer` arranca en frio y lee su skill, no
  `decisions.md`: con el texto actual, si encuentra en `tasks.md` una fila editada a mano que no nace
  de un hallazgo, la leera como una infraccion. Y `_phases/000_preproject.md:182` nace hoy con la
  version vieja de la regla dentro.
- **Que lo corregiria:** llevar el segundo caso de `D-025` a los tres enunciados, en una sola pasada.
  ⚠️ Es una recomendacion, no una orden.

### F-013 — `D-023` conserva vigente una advertencia que `D-026` desmiente en el mismo commit
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/decisions.md`, entrada `D-023` (`Estado: Vigente`) |

- **Que se observo:** `D-023` cierra con esta advertencia, sin ninguna nota que la module:

```
$ git show 510d580:_persistence/decisions.md | grep -n "todavia no entra en el ambito del Paso 1b" -A 3
877:⚠️ **`_phases/` todavia no entra en el ambito del Paso 1b**, que hoy cubre solo `.claude`,
878-`CLAUDE.md` y `project.md`. Mientras no entre, la agnosticidad de estos archivos **depende de que
879-alguien se acuerde**, que es justo lo que `L-008` describe. Ampliar el ambito del control es una
880-modificacion del protocolo y queda pendiente de acordarla con el usuario.
```

  `D-026`, en el mismo commit, ya lo amplio:

```
$ git show 510d580:.claude/skills/protocol-close/SKILL.md | grep -n "CLAUDE.md _phases"
96:git grep -nE "<nombre del proyecto>|<carpeta raiz de las rutas absolutas>|<host del remoto>" -- .claude CLAUDE.md _phases
```

- **Por que importa:** una entrada `Vigente` afirma como pendiente algo que ya esta hecho, y ademas
  describe mal el ambito del control («cubre solo `.claude`, `CLAUDE.md` y `project.md`»). Quien lea
  `D-023` sin llegar a `D-026` concluye que la agnosticidad de `_phases/` no tiene control que la
  compruebe. `Baja` porque la informacion correcta esta a tres entradas de distancia y nada operativo
  depende de esta linea.
- **Que lo corregiria:** una nota fechada bajo `D-023` remitiendo a `D-026`, sin reescribir el texto
  original (`D-019`). Es el mismo mecanismo que la sesion uso cinco veces hoy. ⚠️ Es una
  recomendacion, no una orden.

### F-014 — El «Avance de la etapa» de `progress.md` atribuye mal la procedencia de los hallazgos y cuenta `F-006` dos veces
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/progress.md`, seccion 1, fila «Avance de la etapa» |

- **Que se observo:**

```
$ git show 510d580:_persistence/progress.md | grep -n "dejo cinco hallazgos abiertos" | cut -c1-220
59:| Avance de la etapa | El commit `e61454b` (auditoria `R-004` sobre `S-004`) dejo cinco hallazgos abiertos (`F-005`, `F-006`, `F-008`, `F-009`, `F-010`), sumados al `F-006` ya abierto por `R-003`.
```

  `R-004` abrio tres (`F-008`, `F-009`, `F-010`); `F-005` y `F-006` los abrio `R-003`. La frase
  atribuye los cinco a `R-004` y despues suma `F-006` **otra vez**, que ya esta en su propia lista. El
  tablero lo desmiente, y la seccion 0 del propio informe lo cuenta bien:

```
$ git show 510d580:_audit/index.md | sed -n '14,15p'
| `S-003.md` | S-003 | 2026-09-01 | `ea0b850` | `R-003.md` | Con hallazgos (3) | F-005, F-006, F-007 |
| `S-004.md` | S-004 | 2026-09-01 | `c70b757` | `R-004.md` | Con hallazgos (3) | F-008, F-009, F-010 |
```

- **Por que importa:** `progress.md` es lo primero que lee `session-starter` en cada arranque, y de
  los tres textos que cuentan lo mismo hoy (informe, bitacora y avance) es el unico que se lee
  siempre. Un recuento que se contradice consigo mismo dentro de la misma frase invita a desconfiar
  del resto. `Baja` porque no hay consecuencia operativa: los seis hallazgos quedaron evaluados y
  registrados correctamente, cada uno con su `T-XXX`.
- **Que lo corregiria:** reescribir esa frase. `progress.md` seccion 1 se sobrescribe en cada cierre,
  no es historico y no le aplica `D-019`. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| `F-005` | `Aceptado — pendiente` (`T-009`) | **`Implementado`** (`510d580`) | La nota fechada existe bajo `A-001`, deja el bloque original intacto, declara su ambito real y rehace la señal 2 con su momento de comprobacion. Los dos puntos del criterio de cierre de `T-009` estan en el diff. ⚠️ El defecto **nuevo** que la propia nota introduce va como `F-011`; no reabre este |
| `F-006` | `Aceptado — pendiente` (`T-010`) | **`Implementado`** (`510d580`) | Segunda nota fechada bajo `D-016` con el recuento sobre `ea0b850`; reproducido y coincide linea por linea. Los bloques anteriores quedan intactos |
| `F-008` | `Aceptado — pendiente` (`T-011`) | **`Implementado`** (`510d580`) | Nota fechada bajo `D-021` con el recuento sobre `c70b757`, la separacion de las 9 coincidencias internas (reproducida: da 9) y la frase corregida. Bloque original intacto |
| `F-009` | `Aceptado — pendiente` (`T-012`) | **`Implementado`** (`510d580`) | La nota de `DT-001` enuncia el criterio de cierre realmente aplicado, con comando y salida cruda que **si** se reproducen sobre el commit. «Como se paga» intacto |
| `F-010` | `Aceptado — pendiente` (`T-013`) | **sigue `Aceptado — pendiente`** | La parte de `manager` esta hecha (nota fechada en `D-021` acotando `_audit/` a historico salvo las convenciones vivas). **Pero el defecto sigue vivo:** `_audit/findings.md:56` continua citando `debtec.md` sobre `510d580`. Un hallazgo corregido a medias sigue abierto, y ademas quien debe corregir esa linea no esta zanjado — ver seccion 5 |

`F-001` a `F-004` y `F-007` siguen `Implementado`; no se tocaron y no se vuelven a revisar.

## 4. Lo que NO se pudo comprobar

Ninguno. Los cuatro controles mecanicos corrieron enteros sobre `510d580`, y los cinco bloques de
verificacion de las decisiones nuevas mas las cuatro notas fechadas se reprodujeron uno por uno.

## 5. Recomendaciones sin hallazgo

- 🚨 **`F-010` esta en un punto muerto de autoria, y hay que zanjarlo.** `T-013` dice que la linea 56
  de `_audit/findings.md` «la corrige `report_auditor` en una pasada posterior». Esta auditoria **no
  la ha corregido, y a proposito**: el protocolo me prohibe corregir cualquier cosa —«ni una linea,
  ni aunque sea obvio»— y me autoriza a escribir en `findings.md` solo hallazgos, estados y filas. Si
  edito la seccion de convenciones estoy arreglando sobre el commit auditado, que es justo lo que la
  separacion de roles existe para impedir. **Decision para `manager` o para el usuario:** o se amplia
  explicitamente el mandato del auditor sobre las convenciones de `findings.md` —con su `D-XXX`—, o
  se declara que esa seccion la escribe `manager` pese a vivir en `_audit/`. Mientras no se decida,
  `F-010` no lo puede cerrar nadie.
- **El riesgo sin codigo de `D-022` sigue sin codigo, y el informe lo admite.** La regla existe en
  `decisions.md` y no en `CLAUDE.md` ni en `protocol-close`. `F-011` es la prueba empirica de que eso
  no basta: la regla se incumplio en el mismo commit que la creo. `L-008` ya describe el mecanismo.
  Convertirlo en `T-XXX` cuesta una linea y es lo unico que impide la sexta repeticion.
- **`005_discovery` esta declarada y todavia no tiene archivo en `_phases/`**, contra la regla que
  `D-023` acaba de fijar. La propia `D-023` lo deja anotado como trabajo de la etapa. No lo abro como
  hallazgo porque la razon esta escrita, pero conviene que tenga codigo antes de que la etapa empiece.
- **Sobre la calidad general de este commit:** las cinco correcciones por nota fechada son
  reproducibles caracter por caracter, y `D-022` cumple su propia regla contra si misma. Es el mejor
  registro de evidencia que ha pasado por esta auditoria. Los cuatro hallazgos de arriba son de
  redaccion y de propagacion; ninguno es de metodo.
