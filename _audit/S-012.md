# Informe de auditoria — S-012

| Campo | Valor |
|---|---|
| Sesion | S-012 |
| Fecha | 2026-09-02 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-012.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-029 — `T-037` y `T-038` llevan el estado `Pendiente`, que la convencion de `tasks.md` no declara | Aceptado — pendiente | `T-039`, `Implementada` en esta sesion (normaliza a `No implementada`), con `D-057`. El estado del hallazgo lo cierra la proxima auditoria verificando sobre este commit |
| F-030 — `T-038` se escribio a mano sin citar el `D-XXX` o `F-NNN` que la habilita | Aceptado — pendiente | `T-040`, `Implementada` en esta sesion, con `D-058` como respaldo citado en `T-038` |
| F-031 — El recuento «quince lecciones `Sin evaluar`» no se reproduce sobre su propio commit, y esta en cuatro sitios | Aceptado — pendiente | `T-041` (nota fechada en los cuatro sitios) y `T-042` (Paso 2d de `protocol-close`, mecanismo que evita el octavo caso), ambas `Implementada` en esta sesion |

## 1. Que se hizo

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
CLAUDE.md
_audit/S-011.md
_audit/S-012.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_phases/010_prototype.md
project.md
```

Nota: la salida de arriba se tomo con `git diff --cached` porque en el momento de generarla el
commit de este cierre todavia no existia; una vez commiteado, `git show --stat --name-only HEAD`
sobre ese hash tiene que devolver la misma lista.

Detalle, con codigos y rutas:

- **`.claude/skills/protocol-close/SKILL.md`** — nace el **Paso 2d**: un control que localiza los
  bloques de verificacion que la jornada publica sin ancla a un commit (`git diff --cached -U0 --
  _persistence _audit | grep '^\+\$ ' | grep -v 'git (show|grep|log|diff) <hash>'`) y obliga a
  reejecutarlos antes de cerrar. Es `T-042`, atendiendo la segunda mitad de `F-031`.
- **`CLAUDE.md`** — `PI-5` gana una excepcion explicita: una etapa cuyo producto es deliberadamente
  descartable puede declarar, en su propio archivo de etapa, un instrumento distinto de un test en
  verde. Es `D-059`, aplicado dentro de `T-044` para no dejar la excepcion sin puntero (`L-007`).
- **`_audit/S-011.md`** — nota fechada (`T-041`, `F-031`): «quince lecciones» son diecisiete, con el
  recuento anclado a `2a2d3b6` y a `f1f3fea`.
- **`_audit/findings.md`** — `F-029`, `F-030` y `F-031` pasan de `Abierto` a `Aceptado — pendiente`,
  citando `T-039`..`T-042`, con el bloque de verificacion de cada evaluacion.
- **`_audit/index.md`** — fila nueva `S-012.md` / `S-012` / `2026-09-02`, con `Commit auditado`,
  `Auditoria`, `Veredicto` y `Hallazgos` en `Pendiente` / `-`, tal como exige el cierre.
- **`_persistence/decisions.md`** — nacen `D-057` a `D-062`: `D-057` (no crear un quinto estado de
  tarea), `D-058` (respaldo de `T-038`), `D-059` (excepcion a `PI-5`), `D-060` (el archivo de etapa
  se adelanta sin declarar la etapa), `D-061` (convencion de nombre para carpetas de entregables),
  `D-062` (el numero de usuarios del prototipo lo fija el proyecto). Ademas, nota fechada sobre el
  bloque de `D-056` corrigiendo «quince» a «diecisiete» (`T-041`).
- **`_persistence/lessons.md`** — nace `L-018` (un archivo traido de otro proyecto destapa
  contradicciones que alli no existian); nota sobre `L-007` explicando que se cumplio a si misma
  cuando `D-059` la incumplio y se corrigio en la misma sesion.
- **`_persistence/progress.md`** — seccion 1 (Estado general), 2 (Ultimo realizado) y 3 (Siguiente
  paso) sobrescritas; nace la entrada de bitacora `S-012` y su fila en el indice de sesiones.
- **`_persistence/tasks.md`** — `T-037` y `T-038` pasan de `Pendiente` (invalido) a
  `No implementada`; nacen `T-039` a `T-044`, las seis `Implementada`.
- **`_phases/010_prototype.md`** — archivo nuevo, sin trackear hasta este commit. Adaptacion de un
  archivo de etapa de prototipo de otro proyecto (`T-043`, `T-044`): agnostico, sin datos propios ni
  codigos instanciados, ocho secciones, siete casillas de condicion de salida, con la excepcion a
  `PI-5` declarada dentro (`D-059`) y sin declarar la etapa en `project.md` (`D-060`).
- **`project.md`** — nueva fila «Entregables de `010_prototype`» en Rutas, nueva fila
  `010_prototype/` en «Carpetas propias» con su razon de diferencia frente al arbol versionado
  (`D-061`).

## 2. Que NO se hizo, y por que

Tareas abiertas del indice de `_persistence/tasks.md` (filtro `No implementada`):

```
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject) | Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003) | Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
| [T-037](#t-037---escribir-el-inventario-de-acciones-irreversibles-del-proyecto-lg-38) | Escribir el inventario de acciones irreversibles del proyecto (`LG-38`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-038](#t-038---igualar-el-barrido-de-fuga-de-protocol-audit-con-el-de-protocol-close) | Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close` | No implementada | Media | No bloqueante | `000_preproject` |
```

- `T-001` y `T-002` dependen de abrir `005_discovery`, que a su vez depende de `A-004` (acceso a un
  patrocinador alcanzable).
- `T-003` (verificar `A-003`, el historico de la fuente oficial) quedo con una comprobacion parcial
  en `S-011` (respuesta `200` sin cabeceras de navegador, datos en HTML crudo) y no avanzo esta
  sesion.
- `T-037` (inventario de acciones irreversibles) esta disponible desde `S-011` y no se atendio esta
  sesion: el trabajo de hoy fue evaluar la auditoria `R-011` y adaptar el archivo de etapa del
  prototipo.
- `T-038` (igualar el barrido de fuga de `protocol-audit` con el de `protocol-close`) sigue esperando
  la decision del usuario, explicita en `D-058`: es la vara con la que se audita a `manager`.
- `_phases/010_prototype.md` referencia `_templates/010_prototype/` y `_workflow/010_prototype.md`,
  que hoy no existen. `D-060` lo declara como condicion de entrada a la etapa, no como trabajo
  pendiente de esta sesion.

## 3. Decisiones tomadas

- **`D-057`** — No nace un quinto estado de tarea: `Pendiente` se normaliza a `No implementada`.
  Alternativas descartadas: declarar `Pendiente` con su propio `D-XXX` (duplica un estado
  existente); dejarlo sin normalizar (perpetua el defecto que `F-029` señalo).
- **`D-058`** — `T-038` se regulariza declarando que nacio fuera de las dos excepciones de la
  convencion de `tasks.md` (no de aceptar un hallazgo, no de una decision ya registrada que el
  cierre no pudiera deducir del diff). Alternativas descartadas: borrar `T-038`; citarle `D-054`
  como si hubiera nacido de la consulta a las lecciones globales (seria falso); declarar una tercera
  excepcion (convertiria las dos excepciones en una puerta).
- **`D-059`** — El prototipo descartable es la unica excepcion a `PI-5`, y se declara dentro del
  archivo de etapa, no en `CLAUDE.md`. Cambia el instrumento de verificacion (evidencia registrada
  de sesiones en vez de test en verde), nunca la exigencia. Alternativas descartadas: exigir tests
  de humo minimos; escribir el archivo sin mencionar `PI-5`; reescribir `PI-5` para un tercer caso.
- **`D-060`** — El archivo de etapa del prototipo se escribe por adelantado, sin declarar la etapa en
  `project.md`: esa decision es de `005_discovery`. Alternativas descartadas: declarar la etapa hoy;
  escribir el archivo sin ningun registro; dejarlo en `temporal/` hasta que el descubrimiento lo
  pida.
- **`D-061`** — Una carpeta de entregables se llama igual que su etapa, y se declara por adelantado
  cuando su etapa esta escrita y su contenido enumerado. Alternativas descartadas: `015_prototype`
  (desfase de nombre); una carpeta contenedora con subcarpetas por etapa; declararla solo al nacer
  el primer artefacto.
- **`D-062`** — El numero minimo de usuarios del prototipo lo fija el proyecto, porque la guia de
  metodo no lo fija (verificado: `_methodology/000_method.md` no trae ninguna cifra). Alternativas
  descartadas: copiar el «minimo 3, recomendado 5» del archivo fuente (lo asciende a regla sin
  respaldo); fijar aqui un minimo (no hay con que decidirlo todavia); dejar la redaccion ambigua.

## 4. Supuestos vigentes y riesgos

- `A-001` (un auditor lanzado por el auditado conserva independencia suficiente) — `Abierto`. Sigue
  sin verificar; esta sesion vuelve a apoyarse en el mismo esquema.
- `A-002` (el brief recibido es el encargo completo) — `Abierto`.
- `A-003` (el historico de la fuente oficial es obtenible de forma automatizable) — `Abierto`. Se
  apoya la etapa `000_preproject` y de el depende el ciclo entero del producto; no avanzo esta
  sesion.
- `A-004` (existe acceso al patrocinador) — `Abierto`. Bloquea `T-001`, `T-002` y la apertura de
  `005_discovery`. Tambien bloquea que `_phases/010_prototype.md` pueda adoptarse como etapa
  (`D-060`).
- `A-005` (la parte de `ai_levels.md` escrita sin experiencia propia es correcta) — `Abierto`.
- Riesgo nuevo, sin `A-XXX` propio: `_phases/010_prototype.md` referencia `_templates/010_prototype/`
  y `_workflow/010_prototype.md`, que no existen. Si la etapa se adopta sin escribir esos dos
  artefactos, el archivo referencia contenido inexistente. `D-060` lo declara como condicion de
  entrada, no como supuesto — se señala aqui porque no tiene codigo propio que lo agende.

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto (Alta, Bloqueante, etapa `005_discovery`), previa
resolucion de `A-004`. Alternativa disponible sin depender de `A-004`: `T-037` (inventario de
acciones irreversibles, Alta, No bloqueante) o continuar `T-003`/`A-003` con lo que quedo sin probar.

## 6. Que pedimos auditar

- **La clasificacion "reversible a criterio" en `D-057` a `D-062`.** Ninguna de las seis decisiones
  de esta sesion se declara irreversible, y todas se justifican con el mismo argumento —«es texto en
  un archivo versionado»—. Conviene que el auditor confirme si ese criterio se sostiene para
  `D-059` en particular: abre una excepcion a un principio de ingenieria (`PI-5`) que gobierna todas
  las etapas futuras, y aunque hoy solo afecta a una etapa no adoptada, el precedente que sienta
  podria pesar mas de lo que «texto versionado» sugiere.
- **Que `D-060` y `D-061` convivan sin contradecirse.** `D-060` dice que declarar el archivo de etapa
  no declara la etapa; `D-061`, del mismo dia, si añade filas a `project.md`. La nota fechada dentro
  de `D-060` explica por que no es una contradiccion, pero es exactamente el tipo de matiz que
  conviene que una lectura externa confirme o refute.
- **El Paso 2d de `protocol-close`, recien nacido (`T-042`).** Es un mecanismo nuevo que se aplico a
  si mismo por primera vez en esta misma sesion (ver `T-042`, nota sobre el falso positivo con ancla
  detras del patron). No ha pasado por una segunda sesion que lo ponga a prueba con datos distintos;
  si el patron `^\+\$ ` deja pasar algo que no deberia, esta es la sesion mas probable para que
  ocurra sin que nadie lo note todavia.
