# Auditoria R-007 — sobre S-007

| Campo | Valor |
|---|---|
| Auditoria | R-007 |
| Informe auditado | `_audit/S-007.md` |
| Commit auditado | `122b770` |
| Fecha | 2026-09-02 |
| Veredicto | Con hallazgos (3) |

📌 **La fecha se escribe `2026-09-02` por continuidad con el registro** (`D-032`, que fecha asi las
entradas de `S-007`, y `R-006`, que ya llevaba esa fecha), aunque el reloj del entorno de esta
auditoria marque `2026-09-01`. Se declara aqui para que la diferencia no se lea como un descuido.

📌 **En todos los bloques de este archivo, el delimitador de bloque de codigo se transcribe como
`~~~`** cuando forma parte de un comando o de su salida — escribirlo literal cerraria el bloque que
lo contiene. Es la misma convencion que uso `R-006`.

## 1. Que se comprobo

**Anclaje del commit** (Paso 1) — la unica fila `Pendiente` del tablero era `S-007`:

```
$ git log -1 --format=%h -- _audit/S-007.md
122b770
```

**Indice ↔ detalle de los siete archivos de `_persistence/`** (Paso 4a) — sin diferencias en
ninguno:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show 122b770:_persistence/$f.md | awk '/^~~~/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 122b770:_persistence/$f.md | awk '/^~~~/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
    echo "   rc=$?"
  done
== tasks
   rc=0
== decisions
   rc=0
== constraints
   rc=0
== assumptions
   rc=0
== lessons
   rc=0
== techdebt
   rc=0
== progress
   rc=0
```

**Carpetas del arbol contra las declaradas en `project.md`** (Paso 4b) — una sola diferencia,
`temporal/`, con su razon escrita en `project.md`. `_templates/005_discovery/` **no aparece en
ninguna de las dos listas**, que es lo correcto: `D-035` y `T-022` dejan escrito que la carpeta y su
fila entran juntas y todavia no:

```
$ diff <(git ls-tree -d --name-only 122b770 | sed 's|$|/|' | sort) \
       <(git show 122b770:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
6a7
> temporal/
```

**Fuga de datos propios en lo agnostico** (Paso 4c) — cero lineas, con el archivo de etapa nuevo ya
dentro del ambito:

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 122b770 -- .claude CLAUDE.md _phases _methodology ; echo "rc=$?"
rc=1
```

**Codigos instanciados en `_phases/`, por iniciativa propia** — `CLAUDE.md` exige que los archivos
de etapa escriban los codigos genericos (`T-XXX`, `D-XXX`, `F-NNN`) y no instanciados. El archivo
nuevo cumple, igual que el que ya estaba:

```
$ git show 122b770:_phases/005_discovery.md | grep -nE "\b(T|D|C|A|L|S|R|N|DT|F)-[0-9]{3}\b" ; echo "rc=$?"
rc=1

$ git show 122b770:_phases/000_preproject.md | grep -nE "\b(T|D|C|A|L|S|R|N|DT|F)-[0-9]{3}\b" ; echo "rc=$?"
rc=1
```

**Criterio de cierre de `T-020`, reproducido sobre el commit** — se cumple:

```
$ git ls-tree --name-only 122b770 _phases/
_phases/000_preproject.md
_phases/005_discovery.md

$ git show 122b770:project.md | grep -n "Etapas declaradas"
79:| Etapas declaradas | `000_preproject`, `005_discovery` |
```

**Estructura del archivo de etapa nuevo contra la del que ya existia** — ocho secciones, mismos
titulos, como afirma `T-020`:

```
$ git show 122b770:_phases/005_discovery.md | grep -nE "^## "
28:## 1. Que autoriza esta etapa
50:## 2. Que prohibe esta etapa
75:## 3. Entradas — que debe existir antes de empezar
113:## 4. Procedimiento
226:## 5. Artefactos que produce
251:## 6. Condicion de salida
276:## 7. Que registra `manager` en `_persistence/`
296:## 8. Lo que esta etapa le entrega a la siguiente

$ git show 122b770:_phases/000_preproject.md | grep -nE "^## "
21:## 1. Que autoriza esta etapa
37:## 2. Que prohibe esta etapa
59:## 3. Entradas — que debe existir antes de empezar
71:## 4. Procedimiento
133:## 5. Artefactos que produce
153:## 6. Condicion de salida
179:## 7. Que registra `manager` en `_persistence/`
207:## 8. Lo que esta etapa le entrega a la siguiente
```

**Criterio de cierre de `T-021`, reproducido sobre el commit** — se cumple en sus dos mitades:

```
$ git grep -nE "unica excepcion|salvo la .T-XXX" 122b770 -- .claude CLAUDE.md _phases ; echo "rc=$?"
122b770:.claude/skills/protocol-close/SKILL.md:490:**La unica excepcion, y es mecanica:** si un supuesto `A-XXX` quedo comprobado por la evidencia del
rc=0

$ git show 122b770:_persistence/tasks.md | sed -n '/^## Convenciones/,/^## Tareas/p' | grep -n "unica excepcion\|una sola excepcion\|la excepcion es unica\|solo una excepcion" ; echo "rc=$?"
rc=1
```

**Entradas nuevas que el informe declara, contra el arbol del commit** — las seis existen:

```
$ git show 122b770:_persistence/decisions.md | grep -nE "^### D-03[2-5]"
1326:### D-032 - Las entradas de esta sesion se fechan 2026-09-02, por continuidad con el registro
1356:### D-033 - El archivo de etapa de descubrimiento se adapta de la guia del usuario, no se copia
1403:### D-034 - `N-XXX` es el codigo de necesidad; los supuestos y restricciones siguen siendo `A-XXX` y `C-XXX`
1446:### D-035 - Las plantillas de los artefactos de descubrimiento viven en `_templates/005_discovery/`

$ git show 122b770:_persistence/assumptions.md | grep -nE "^### A-004"
202:### A-004 - Existe acceso al patrocinador y a personas que puedan hablar del proceso real

$ git show 122b770:_persistence/lessons.md | grep -nE "^### L-010"
285:### L-010 - Un criterio de cierre cuyo ambito incluye el registro no puede cumplirse nunca
```

**Lo que el informe dice que NO se hizo** — las cuatro tareas siguen `No implementada`, tal cual
(las anclas de la primera columna se abrevian a `(...)` para que la tabla quepa; el resto es
literal):

```
$ git show 122b770:_persistence/tasks.md | grep -E "^\| \[T-0(01|02|03|22)\]"
| [T-001](...) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002](...) | Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003](...) | Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
| [T-022](...) | Escribir las cinco plantillas de `_templates/005_discovery/` | No implementada | Media | No bloqueante | `005_discovery` |
```

**Coherencia de `findings.md` despues de tocarlo** (Paso 7d) — sin salida:

```
$ diff <(awk '/^~~~/{c=!c; next} !c' _audit/findings.md | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) \
       <(awk '/^~~~/{c=!c; next} !c' _audit/findings.md | grep -oE '^#{3} F-[0-9]+'   | grep -oE 'F-[0-9]+' | sort -u)
$ echo "rc=$?"
rc=0
```

## 2. Hallazgos

### F-017 — `T-020` queda `Implementada` sin ningun bloque de verificacion, y el informe remite a una seccion que tampoco lo tiene

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md`, ficha `T-020`; `_audit/S-007.md`, secciones 1 y 6 |

- **Que se observo:** `T-020` produce documentacion (`_phases/005_discovery.md`), asi que su
  Definicion de Terminado es «existe su bloque de verificacion: la orden ejecutada literal y su
  salida cruda» (`CLAUDE.md`, PI-5). La ficha no contiene ni un bloque de codigo, mientras que la de
  `T-021` —cerrada en la misma pasada— contiene dos (cuatro delimitadores):

```
$ git show 122b770:_persistence/tasks.md | sed -n '/^### T-020/,/^### T-021/p' | grep -c '^~~~'
0

$ git show 122b770:_persistence/tasks.md | sed -n '/^### T-021/,/^### T-022/p' | grep -c '^~~~'
4
```

  El informe declara que el criterio se comprobo y remite a su seccion 6, que tampoco lleva ningun
  comando:

```
$ git show 122b770:_audit/S-007.md | grep -n "ver seccion 6"
27:  arbol de trabajo (ver seccion 6).

$ git show 122b770:_audit/S-007.md | sed -n '/^## 6/,$p' | grep -c '^~~~'
0
```

  Lo mismo en `progress.md`, que afirma el resultado sin la orden que lo produjo:

```
$ git show 122b770:_persistence/progress.md | sed -n '61p' | grep -o "Los criterios de cierre de ambas tareas se comprobaron contra el arbol de trabajo antes de cerrarlas[^;]*;"
Los criterios de cierre de ambas tareas se comprobaron contra el arbol de trabajo antes de cerrarlas: `_phases/` ya contiene un archivo por cada etapa declarada y el control de fuga de datos propios del Paso 1b sigue en cero;
```

- **Por que importa:** el criterio **si se cumple** —esta auditoria lo reprodujo en la seccion 1—,
  asi que el registro no miente; lo que falta es la evidencia que lo hace reproducible. Es
  exactamente el patron que `F-009` y `F-016` ya abrieron dos veces: una tarea `Implementada` cuyo
  cierre descansa en un veredicto («se comprobo») en vez de en «corri esto, salio esto». `Media` y no
  `Baja` porque `T-020` es la tarea que cierra un hallazgo de auditoria y porque es la tercera
  aparicion del mismo patron, no la primera.
- **Que lo corregiria:** anadir a la ficha de `T-020` un bloque con las dos ordenes que su propio
  criterio nombra —el listado de `_phases/` y el `git grep` del Paso 1b— con su salida cruda y
  ancladas al commit sobre el que se cumplieron. ⚠️ Es una recomendacion, no una orden.

### F-018 — `T-021` afirma un barrido «sobre todo el repositorio» del que no registra ni el patron ni el ambito ni la salida

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/tasks.md`, ficha `T-021`, bloque «Que se hizo ademas, por `L-009`» |

- **Que se observo:** la ficha afirma un resultado sobre un ambito que sus dos bloques de
  verificacion no cubren — los dos corren acotados (`.claude CLAUDE.md _phases`, y la seccion
  «Convenciones» del propio archivo), no sobre el repositorio:

```
$ git show 122b770:_persistence/tasks.md | sed -n '/^### T-021/,/^### T-022/p' | grep -n "todo el repositorio" -B2
20-  barre solo el ejemplo citado deja vivo el defecto. Se barrieron tambien las variantes que el patron
21-  del hallazgo no cubria —«una sola excepcion», «la excepcion es unica», «solo una excepcion»— sobre
22:  todo el repositorio. No aparecio ninguna mas viva de esta regla.
```

  Reproducido por esta auditoria, el barrido global devuelve 45 lineas, y hay una que **ninguno de
  los patrones escritos en la ficha alcanza**, porque va en mayuscula:

```
$ git grep -niE "una sola excepcion|la excepcion es unica|solo una excepcion|unica excepcion" 122b770 -- . | wc -l
45

$ git grep -nE "Unica excepcion" 122b770 -- .claude
122b770:.claude/agents/session-closer.md:90:  - *Unica excepcion, y es mecanica:* ascender un supuesto `A-XXX` ya comprobado por el diff — y
```

  Esa linea **no es un resto de la regla vieja** —habla de los supuestos `A-XXX`, igual que la de
  `protocol-close`—, asi que la conclusion de la ficha es correcta en el fondo. Lo que no existe es
  la forma de llegar a ella sin rehacer el barrido entero.
- **Por que importa:** `CLAUDE.md` obliga a que un resultado afirmado por iniciativa propia vaya
  «con el patron y el ambito con que se obtuvo», precisamente para que quien audite no tenga que
  repetirlo. Aqui hubo que repetirlo, y al repetirlo aparecio una coincidencia que el patron escrito
  en la ficha no habria encontrado. `Baja` porque el fondo esta bien: ninguna variante viva de la
  regla vieja quedo en pie.
- **Que lo corregiria:** anadir a la ficha el barrido de variantes tal como se corrio —patron,
  ambito y salida cruda—; o, si no llego a correrse con ese alcance, acotar la frase a lo que los
  dos bloques existentes si demuestran. ⚠️ Es una recomendacion, no una orden.

### F-019 — La lista de archivos de la seccion 1 del informe no cuadra con el comando que dice haberla producido

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-007.md`, seccion 1 |

- **Que se observo:** la seccion 1 presenta su lista como salida de `git show --stat --name-only
  --format= HEAD` y la describe como «estos siete mas el nuevo». El comando devuelve **diez**
  archivos; la lista enumera **ocho**, y faltan `_audit/index.md` —la fila nueva de `S-007`, que es
  lo que hace posible esta auditoria— y `_audit/S-007.md`, el propio informe:

```
$ git show --stat --name-only --format= 122b770
_audit/S-007.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_phases/005_discovery.md
project.md

$ git show 122b770:_audit/S-007.md | sed -n '/^## 1\./,/^## 2\./p' | grep -c '^- `'
8
```

  `S-006` si los declaraba, en un parrafo final expreso, asi que es una regresion respecto al
  informe anterior:

```
$ git show d906a5d:_audit/S-006.md | grep -n "su fila en"
50:`S-006`) y su fila en `_audit/index.md`.
```

- **Por que importa:** el informe es la unica pieza que le dice a la auditoria que estado esta
  juzgando; una lista que se presenta como salida de un comando y no coincide con el debilita
  justo lo que la hace util. `Baja` porque los dos archivos omitidos son mecanica del propio cierre
  y ninguno esconde trabajo no declarado.
- **Que lo corregiria:** que la seccion 1 liste los archivos del propio cierre —el informe y su fila
  en el tablero— como hacia `S-006`, o que el recuento entre parentesis diga cuantos son de verdad.
  ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-015` | **Implementado** (`122b770`) | La correccion esta en el diff de este commit y hace lo que `T-020` afirma: `_phases/005_discovery.md` existe, con las mismas ocho secciones que `_phases/000_preproject.md`, sin codigos instanciados y sin fuga de datos propios; `_phases/` ya contiene un archivo por cada etapa de la fila «Etapas declaradas» de `project.md`. Comandos y salidas en la seccion 1 |
| `F-016` | **Implementado** (`122b770`) | La convencion de `tasks.md` abre ahora con «Tiene dos excepciones, y las dos estan escritas», y los dos parrafos siguientes dejaron de presentarlas como una y un anadido. El barrido sobre la seccion «Convenciones» no devuelve ninguna variante viva, y la unica coincidencia en `.claude CLAUDE.md _phases` es de otra regla —la de los supuestos `A-XXX`—. `T-015` conserva su texto con nota fechada, como exige `D-019` |

⚠️ **Los dos se cierran pese a haberse aceptado y corregido en la misma sesion**, y eso es lo que la
seccion 6 del informe pedia revisar: la secuencia es coherente con «Revision independiente» porque
el estado en `findings.md` se quedo en `Aceptado — pendiente` y ha sido **esta** auditoria, sobre el
commit ya cerrado, la que ha mirado el diff. Lo que la sesion cerro por su cuenta fueron sus tareas
—`T-020` y `T-021`—, que son suyas. La debilidad real de esa secuencia no es el orden, es la
evidencia: `F-017` senala que una de las dos tareas se cerro sin ella.

Ningun otro `F-NNN` queda abierto: `F-001` a `F-014` ya estaban `Implementado` antes de esta pasada.

## 4. Lo que NO se pudo comprobar

- **La fidelidad de `D-033` a las fuentes que aporto el usuario.** El informe lo declara en su
  seccion 6 y tiene razon: esos archivos viven en `temporal/`, fuera del repositorio y del alcance
  de este protocolo, que ademas tiene prohibido leerla. Lo que si se pudo comprobar es el resultado
  contra lo que la decision dice haber descartado —el esquema de dos terminales revocado por
  `D-012`, `_memory/`, y los prefijos `SUP-`/`RES-`—, y no queda rastro de ninguno:

```
$ git grep -nEi "dos terminales|_memory/|SUP-[0-9]|RES-[0-9]" 122b770 -- _phases/005_discovery.md ; echo "rc=$?"
rc=1
```

- Ninguno de los tres controles del Paso 4, ni el de coherencia de `findings.md`, quedo sin correr.

## 5. Recomendaciones sin hallazgo

- **`A-004` nace despues de la regla que la origina, y el informe pregunta si eso deja hueco. No lo
  deja:** el archivo de etapa y el supuesto entran en el mismo commit, asi que no existe ningun
  estado del repositorio en el que la entrada obligatoria exista sin su supuesto registrado. Lo que
  si conviene vigilar es su disparador —`T-001`—, porque `A-004` es hoy la unica condicion escrita
  capaz de impedir que la etapa siguiente arranque.
- **`_phases/005_discovery.md` remite a plantillas que no existen** (`_templates/005_discovery/`,
  linea 238). Esta cubierto por `D-035` y `T-022`, y por eso no es hallazgo; pero mientras `T-022`
  no se haga, la regla de «adherencia a plantillas» de `CLAUDE.md` no tiene nada que comprobar en
  esa etapa. Merece resolverse antes de abrirla, no durante.
- **`T-022` esta etiquetada `005_discovery` y es trabajo de andamiaje**, no de descubrimiento:
  escribir plantillas no responde ninguna pregunta sobre la necesidad del cliente. Si la etapa se
  abre con esa tarea dentro, su condicion de salida se mezcla con trabajo de metodo. No lo abro como
  hallazgo porque hoy ningun archivo obliga a lo contrario.
