# Auditoria R-013 — sobre S-013

| Campo | Valor |
|---|---|
| Auditoria | R-013 |
| Informe auditado | `_audit/S-013.md` |
| Commit auditado | `8eb8666` |
| Fecha | 2026-09-02 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

**Ancla del commit** — el informe esta commiteado:

```
$ git log -1 --format=%h -- _audit/S-013.md
8eb8666
```

**Control a — indice ↔ detalle en `_persistence/`** (sobre `8eb8666`, los siete archivos): sin
diferencias.

~~~
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 8eb8666:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 8eb8666:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
~~~

**Control b — carpetas del arbol contra las declaradas en `project.md`:** dos diferencias, **las dos
con motivo escrito** en `project.md` («Carpetas propias»). No son hallazgo.

```
$ diff <(git ls-tree -d --name-only 8eb8666 | sed 's|$|/|' | sort) <(git show 8eb8666:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

`temporal/` esta excluida por `.gitignore` y `010_prototype/` esta declarada por adelantado
(`D-061`); las dos razones estan escritas en `project.md`.

**Control c — fuga de datos propios en lo agnostico:** cero lineas, tanto en el ambito de este
protocolo como en el ambito ampliado de seis rutas que usa el Paso 1b del cierre.

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github.com" 8eb8666 -- .claude CLAUDE.md _phases _methodology ; echo "exit=$?"
exit=1

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" 8eb8666 -- .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
exit=1
```

**Control d — la lista de archivos de la seccion 1 del informe contra el commit:** coincide con lo
que el informe declara —los once archivos publicados **mas** `_audit/S-013.md` y `_audit/index.md`,
que el propio informe advierte que se staged despues—.

```
$ git show --stat 8eb8666 | tail -14
 .claude/skills/protocol-close/SKILL.md             |  12 +
 _audit/S-013.md                                    | 144 +++++++++++
 _audit/findings.md                                 |  26 +-
 _audit/index.md                                    |   1 +
 _persistence/decisions.md                          | 149 +++++++++++
 _persistence/lessons.md                            |  35 ++-
 _persistence/progress.md                           | 137 ++++++----
 _persistence/tasks.md                              | 206 ++++++++++++++-
 _templates/010_prototype/005_happy_path.md         | 193 ++++++++++++++
 _templates/010_prototype/010_participants.md       | 222 ++++++++++++++++
 _templates/010_prototype/015_session_NNN.md        | 278 +++++++++++++++++++++
 _templates/010_prototype/020_observations.md       | 254 +++++++++++++++++++
 .../010_prototype/025_business_validation.md       | 210 ++++++++++++++++
 13 files changed, 1804 insertions(+), 63 deletions(-)
```

**Control e — reejecucion de TODOS los bloques de verificacion sin ancla que anade el commit.** Se
corrio la primera orden del Paso 2d sobre el rango `265bfeb..8eb8666` y se reejecuto **cada linea**.
Se publica la lista entera con su recuento, como exige `D-063`: trece apariciones, diez ordenes
distintas.

~~~
$ git diff 265bfeb 8eb8666 -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+$ git diff --cached --stat --name-only
+$ grep -n "la lista COMPLETA" .claude/skills/protocol-close/SKILL.md
+$ grep -n "^## Paso 2d" .claude/skills/protocol-close/SKILL.md
+$ ls _templates/010_prototype/
+$ grep -rniE "(^|[^0-9])_prototype/|_memory/|015_gate1|terminal (ejecutora|auditora)|sponsor|020_baseline|minimo 3|recomendado 5|SUP-|RES-" _templates/010_prototype/ ; echo "exit=$?"
+$ grep -rlc "Guia de llenado" _templates/010_prototype/
+$ grep -c "sin decidir" _templates/010_prototype/*.md
+$ grep -n "la lista COMPLETA" .claude/skills/protocol-close/SKILL.md
+$ ls _templates/010_prototype/
+$ grep -rnE "RaindomAI|RaidomAI|Proyectos_TripleS|github.com" .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
+$ grep -rnE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" _templates/010_prototype/ ; echo "exit=$?"
+$ grep -rniE "(^|[^0-9])_prototype/|_memory/|015_gate1|terminal (ejecutora|auditora)|sponsor|020_baseline|minimo 3|recomendado 5|SUP-|RES-" _templates/010_prototype/ ; echo "exit=$?"
+$ LC_ALL=C.UTF-8 grep -rnP "[\x{00e1}\x{00e9}\x{00ed}\x{00f3}\x{00fa}\x{00c1}\x{00c9}\x{00cd}\x{00d3}\x{00da}]" _templates/010_prototype/ ; echo "exit=$?"

$ git diff 265bfeb 8eb8666 -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
13
~~~

Reejecucion anclada a `8eb8666` — **las nueve reproducibles reproducen lo publicado**:

~~~
$ git show 8eb8666:.claude/skills/protocol-close/SKILL.md | grep -n "la lista COMPLETA"
273:🚨 **Y la evidencia de este paso publica la lista COMPLETA de su primera orden, nunca una

$ git show 8eb8666:.claude/skills/protocol-close/SKILL.md | grep -n "^## Paso 2d"
242:## Paso 2d — Ningun bloque de verificacion sin ancla (antes del `git add`)

$ git ls-tree --name-only 8eb8666 _templates/010_prototype/
_templates/010_prototype/005_happy_path.md
_templates/010_prototype/010_participants.md
_templates/010_prototype/015_session_NNN.md
_templates/010_prototype/020_observations.md
_templates/010_prototype/025_business_validation.md

$ git grep -niE "(^|[^0-9])_prototype/|_memory/|015_gate1|terminal (ejecutora|auditora)|sponsor|020_baseline|minimo 3|recomendado 5|SUP-|RES-" 8eb8666 -- _templates/010_prototype/ ; echo "exit=$?"
exit=1

$ git grep -l "Guia de llenado" 8eb8666 -- _templates/010_prototype/
8eb8666:_templates/010_prototype/005_happy_path.md
8eb8666:_templates/010_prototype/010_participants.md
8eb8666:_templates/010_prototype/015_session_NNN.md
8eb8666:_templates/010_prototype/020_observations.md
8eb8666:_templates/010_prototype/025_business_validation.md

$ for f in 005_happy_path 010_participants 015_session_NNN 020_observations 025_business_validation; do echo -n "$f: "; git show 8eb8666:_templates/010_prototype/$f.md | grep -c "sin decidir"; done
005_happy_path: 1
010_participants: 1
015_session_NNN: 1
020_observations: 3
025_business_validation: 1

$ git grep -nE "(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]+" 8eb8666 -- _templates/010_prototype/ ; echo "exit=$?"
8eb8666:_templates/010_prototype/005_happy_path.md:107:🔑 **Esa es tambien la forma en que esta etapa cumple `PI-5`.** El archivo de etapa declara la unica
exit=0

$ git grep -nP "[\x{00e1}\x{00e9}\x{00ed}\x{00f3}\x{00fa}\x{00c1}\x{00c9}\x{00cd}\x{00d3}\x{00da}]" 8eb8666 -- _templates/010_prototype/ ; echo "exit=$?"
exit=1
~~~

La decima —`git diff --cached --stat --name-only`— no es reproducible por naturaleza: describe el
area de staging de aquel momento. El informe lo advierte y da su equivalencia sobre el commit, que
es la del Control d, y coincide.

**Control f — los bloques anclados que el commit publica tambien reproducen** (`T-045`, `T-046`,
`T-047`, `D-063`, `D-064`):

```
$ for f in _persistence/decisions.md _persistence/progress.md _audit/S-011.md; do echo -n "$f: "; git show 7f55389:$f | grep -c 'Nota del 2026-09-02 (`T-041`, hallazgo `F-031`)'; done
_persistence/decisions.md: 1
_persistence/progress.md: 1
_audit/S-011.md: 1

$ git show 265bfeb:_persistence/progress.md | grep -c 'Nota del 2026-09-02 (`T-041`, hallazgo `F-031`)'
1

$ git show 7f55389 -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
28

$ git show 265bfeb:project.md | grep -c "010_prototype"
3
```

📌 El `28` que publica `T-046` **si se reproduce en este entorno**, y `R-012` habia publicado `26`
para la misma orden sobre el mismo commit. La observacion de `S-013` —que el recuento no es estable
entre entornos— queda confirmada desde el lado de la auditoria.

**Control g — la seccion 2 del informe («Que NO se hizo») contra el indice de `tasks.md`:** las cinco
tareas que lista son exactamente las que llevan `No implementada` en la columna Estado.

```
$ git show 8eb8666:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -c "| No implementada |"
5
```

**Control h — la seccion 4 del informe contra `assumptions.md`:** los cinco supuestos citados existen
y estan `Abierto`.

```
$ git show 8eb8666:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -cE '^\| \[A-0[0-9]+\].*\| Abierto \|'
5
```

**Control i — las cinco plantillas nuevas contra lo que declara la etapa:** una por artefacto de
registro de `_phases/010_prototype.md` §5, y las rutas de `005_discovery` que citan existen todas
como plantilla.

```
$ git grep -hoE "005_discovery/[0-9A-Za-z_]+\.md" 8eb8666 -- _templates/010_prototype/ | sort | uniq -c
      1 005_discovery/005_needs.md
      1 005_discovery/010_actors.md
      1 005_discovery/015_stakeholders.md
      2 005_discovery/020_hypothesis.md

$ git ls-tree --name-only 8eb8666 _templates/005_discovery/
_templates/005_discovery/005_needs.md
_templates/005_discovery/010_actors.md
_templates/005_discovery/015_stakeholders.md
_templates/005_discovery/020_hypothesis.md
```

## 2. Hallazgos

### F-034 — El informe remite a una lista completa del Paso 2d que no existe en el commit

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-013.md` §6, primer punto; `_persistence/tasks.md`, verificacion de `T-046` |

- **Que se observo:** la seccion 6 del informe afirma que el Paso 2d se aplico a si mismo y remite a
  un sitio concreto para la evidencia. En ese sitio **no hay ninguna lista**: la verificacion de
  `T-046` publica dos ordenes, y ninguna de las dos es la primera orden del Paso 2d aplicada a este
  cierre.

~~~
$ git show 8eb8666:_audit/S-013.md | sed -n '133,137p'
- **Que la lista completa que exige `D-063` de verdad se haya publicado en esta misma sesion.** El
  Paso 2d se aplico a si mismo dentro del cierre de `S-013` (ver Paso 2d de este cierre, en la
  seccion de verificacion de `T-046`): las nueve ordenes sin ancla se reejecutaron y sus salidas
  coinciden con lo publicado. Es la primera vez que el parrafo nuevo se pone a prueba con datos
  reales; conviene que una lectura externa confirme que ninguna linea quedo fuera.

$ git show 8eb8666:_persistence/tasks.md | sed -n '/^### T-046/,/^---/p' | grep -E '^\$ '
$ grep -n "la lista COMPLETA" .claude/skills/protocol-close/SKILL.md
$ git show 7f55389 -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l

$ git grep -c "nueve ordenes" 8eb8666 -- _persistence _audit
8eb8666:_audit/S-013.md:1
~~~

  La segunda de esas dos ordenes recuenta las ordenes sin ancla de **`7f55389`** —el commit anterior,
  el del caso `F-032`— y publica solo la cifra `28`. La lista de «las nueve ordenes» de este cierre
  no aparece en ningun archivo del commit: la unica mencion de la frase esta en el propio informe.

- **Por que importa:** `D-063` y `L-019`, nacidos en esta misma sesion, dicen literalmente que un
  control documentado sobre una parte de su propia salida no es el control, y que la evidencia es la
  lista entera con su recuento. El informe afirma haber cumplido esa regla y remite a una evidencia
  que no esta publicada — que es la forma «se comprobo» que `CLAUDE.md` prohibe expresamente. Es la
  novena aparicion de la misma familia (`F-005`, `F-008`, `F-011`, `F-022`, `F-025`, `F-027`,
  `F-031`, `F-032`), esta vez no por un numero que no cuadra sino por una evidencia declarada y
  ausente. La cifra tampoco cuadra: `nueve`, frente a las diez ordenes distintas (trece apariciones)
  que devuelve la orden en este entorno, y el informe no dice sobre que rango ni con que criterio
  —unicas o apariciones— se conto.

- **Que lo corregiria:** nota fechada al lado —sin reescribir (`D-019`)— que publique la lista
  completa que el Paso 2d devolvio en el cierre de `S-013`, anclada a `8eb8666`, con el rango usado
  y el resultado de reejecutar cada linea; y si esa salida solo vivio en pantalla y ya no se puede
  reconstruir, que la nota lo diga asi en vez de remitir a `T-046`. Para el patron de fondo: `D-063`
  dice **que** hay que publicar, pero no **donde** — la evidencia del Paso 2d no tiene hoy ningun
  sitio asignado en el repositorio, y por eso desaparece con la sesion. ⚠️ Es una recomendacion, no
  una orden.

📌 **El fondo si se sostiene, y se deja escrito para que el hallazgo no se lea como algo peor de lo
que es:** esta auditoria reejecuto las diez ordenes sin ancla del commit (Control e) y **todas
reproducen lo publicado**. Lo que falla es la evidencia del control, no el control.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-032` | **Implementado** (`8eb8666`) | Las dos mitades estan en el diff y verificadas. La nota fechada de `T-045` esta al lado del bloque de `T-041` —sin reescribirlo (`D-019`)—, publica el recuento real (`1` y `3`, no `2` y `4`) anclado a `7f55389` y a `265bfeb`, y reproduce: la orden sobre `7f55389:_persistence/progress.md` devuelve `1`. La segunda mitad —el Paso 2d exige la lista completa— esta en `.claude/skills/protocol-close/SKILL.md:273` sobre este commit, con `D-063` y `L-019` detras |
| `F-033` | **Implementado** (`8eb8666`) | La nota fechada de `T-047` esta al lado de la nota de cierre de `D-060`, acota la frase a lo que la orden prueba y publica las tres menciones ancladas a `265bfeb`, que reproducen: `git show 265bfeb:project.md \| grep -c "010_prototype"` devuelve `3`, y la fila «Etapas declaradas» sigue con dos. La decision no se reescribio |

## 4. Lo que NO se pudo comprobar

- **La primera orden del Paso 2d tal como la corrio el cierre** (`git diff --cached ...`) no es
  reproducible: describe el area de staging de aquel momento, que ya no existe. Se sustituyo por el
  rango equivalente `265bfeb..8eb8666`, que es lo que el commit contiene. Esa misma imposibilidad es
  parte de lo que motiva `F-034`.
- **El recuento `nueve` del informe** no se pudo reproducir con ningun criterio evidente: en este
  entorno la orden devuelve `13` apariciones y `10` ordenes distintas sobre el rango del commit. No
  se abre hallazgo aparte por la cifra; queda dentro de `F-034`, que es el mismo defecto.
- Ningun control mecanico quedo sin correr.

## 5. Recomendaciones sin hallazgo

- **`_workflow/010_prototype.md` sigue sin ficha en `tasks.md`, y es condicion de entrada.**
  `_phases/010_prototype.md` §5 dice que sin el la etapa no puede abrirse; el commit lo reconoce como
  faltante en cinco sitios (`_audit/S-013.md` §2/§4/§5, `D-064`, `T-048`) y ninguno es una tarea.
  `R-012` ya lo recomendo para las plantillas y `S-013` escribio esas; queda la otra mitad. Una
  alternativa mencionada en un informe no la ve `session-starter`, que lee `tasks.md`.

  ~~~
  $ git grep -n "_workflow/010_prototype" 8eb8666 -- _persistence/tasks.md
  8eb8666:_persistence/tasks.md:1954:| `_phases/010_prototype.md` | paralelismo con la etapa anterior: `_templates/010_prototype/` y `_workflow/010_prototype.md` escritos con nombre, no en vago |
  8eb8666:_persistence/tasks.md:1982:$ grep -c "_templates/010_prototype/\|_workflow/010_prototype.md" _phases/010_prototype.md
  8eb8666:_persistence/tasks.md:1986:📌 **Son `3` y no `2` porque `_workflow/010_prototype.md` se nombra dos veces** —en §4, donde se
  8eb8666:_persistence/tasks.md:2134:  `_workflow/010_prototype.md`, que no se escribio hoy.
  ~~~

  Las cuatro lineas pertenecen a `T-044` y a `T-048`; ninguna es una ficha propia.

- **La clasificacion «reversible a criterio» de `D-063` y `D-064` se sostiene**, y el informe pedia
  confirmarlo. `D-064` crea cinco archivos nuevos en una carpeta ya declarada, y no borra ni publica
  nada; `D-063` anade un parrafo a una skill sin reescribir ningun registro anterior —el diff son
  `12` lineas, todas insertadas—. Que `D-063` afecte a cada cierre futuro la hace importante, no
  irreversible: retirar el parrafo devuelve el estado anterior.

  ~~~
  $ git show 8eb8666 --numstat -- .claude/skills/protocol-close/SKILL.md | tail -1
  12	0	.claude/skills/protocol-close/SKILL.md
  ~~~

- **La diferencia de recuento entre entornos (`26` / `28`) no merece investigarse por si sola.** Es
  la tercera pregunta de la seccion 6 del informe. Esta auditoria reproduce `28`, y la respuesta util
  ya esta tomada: `D-063` obliga a comparar listas y no cifras, y una lista se compara linea a linea
  aunque el total difiera. Investigar la causa —version de `grep`, expansion del patron— es trabajo
  sin producto mientras el control no dependa del numero.

- **La nota de `T-045` nombra `_persistence/progress.md` §2 entre los sitios que «dicen los cuatro
  sitios», y en este commit esa seccion ya no lo dice**: el cierre la sobrescribio en la misma
  jornada. No es un defecto —la nota acota de mas, no de menos—, pero quien la lea dentro de un mes
  buscara en `progress.md` una frase que no encontrara.
