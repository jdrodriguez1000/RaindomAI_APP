# Auditoria R-015 — sobre S-015

| Campo | Valor |
|---|---|
| Auditoria | R-015 |
| Informe auditado | `_audit/S-015.md` |
| Commit auditado | `ea48ae8` |
| Fecha | 2026-09-03 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

### a) Anclaje del commit

~~~
$ git log -1 --format=%h -- _audit/S-015.md
ea48ae8

$ git log -1 --format='%h %ad' --date=short ea48ae8
ea48ae8 2026-09-02
~~~

El informe esta dentro del commit que describe. La fila del tablero declara `2026-09-02`, que
coincide con la fecha del commit.

### b) Indice contra detalle en `_persistence/` — coherentes

~~~
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show ea48ae8:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show ea48ae8:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
~~~

Los siete archivos, sin una sola diferencia.

### c) Carpetas del arbol contra las declaradas — las dos diferencias tienen razon escrita

~~~
$ diff <(git ls-tree -d --name-only ea48ae8 | sed 's|$|/|' | sort) \
       <(git show ea48ae8:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
~~~

`010_prototype/` (declarada por adelantado, `D-061`) y `temporal/` (excluida en `.gitignore`) tienen
su motivo escrito en `project.md`. No hay diferencia nueva.

### d) Fuga de datos propios — cero lineas

Ambito ampliado a `_templates` y `_workflow`, mas alla del que el protocolo de auditoria escribe
(`T-038` sigue abierta sobre esa desigualdad):

~~~
$ git grep -nE "RaindomAI|RaidomAI_App|Proyectos_TripleS|github\.com" ea48ae8 -- .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"
exit=1
~~~

Los tres archivos nuevos —`gate1_auditor.md`, `protocol-gate1/SKILL.md` y
`_templates/015_gate1/005_verdict.md`— no filtran nada. La plantilla lleva su hueco
(`<NOMBRE DEL PROYECTO>`, linea 1), no un valor.

### e) El informe contra el diff — la lista de la seccion 1 cuadra exacta

~~~
$ git show --stat ea48ae8
 .claude/agents/gate1_auditor.md        | 116 +++++++++
 .claude/skills/protocol-gate1/SKILL.md | 446 +++++++++++++++++++++++++++++++++
 _audit/S-015.md                        | 282 +++++++++++++++++++++
 _audit/index.md                        |   1 +
 _persistence/decisions.md              | 258 +++++++++++++++++++
 _persistence/lessons.md                |  36 +++
 _persistence/progress.md               | 136 +++++++---
 _persistence/tasks.md                  |  53 ++++
 _phases/010_prototype.md               |  25 ++
 _templates/015_gate1/005_verdict.md    | 259 +++++++++++++++++++
 project.md                             |  36 ++-
 11 files changed, 1606 insertions(+), 42 deletions(-)
~~~

Once archivos, los mismos once que la seccion 1 enumera, sin uno de mas ni de menos. `D-067` a
`D-070`, `L-021`, `T-052` y la entrada `S-015` de `progress.md` estan todas en el diff.
`_phases/010_prototype.md` es insercion pura (`25 ++`, cero borrados), como afirma el informe.

### f) La evidencia del Paso 2d (seccion 7) — se reproduce entera

~~~
$ git show ea48ae8 -U0 -- _persistence _audit ":(exclude)_audit/S-015.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+$ ls -1 .claude/agents/ .claude/skills/
+$ ls -1 _phases/
+$ ls -1 _templates/015_gate1/
+$ grep -rnE "RaindomAI|RaidomAI_App|C:\\Users\\USUARIO|github\.com" .claude CLAUDE.md _phases _methodology _templates _workflow
+$ grep -n "corresponde al patrocinador" .claude/skills/protocol-gate1/SKILL.md _templates/015_gate1/005_verdict.md
+$ grep -nE "NO APROBADO|APROBADO" _templates/015_gate1/005_verdict.md
+$ sed -n '/^## 31. Resultados posibles/,/^↳/p' _methodology/000_method.md
+$ grep -c "NO AUDITABLE" .claude/skills/protocol-gate1/SKILL.md _templates/015_gate1/005_verdict.md
+$ grep -n "no puede repetirse dos veces" .claude/skills/protocol-gate1/SKILL.md
+$ grep -n "ya salio en un dictamen anterior" _templates/015_gate1/005_verdict.md
+$ ls -1 .claude/agents/ .claude/skills/
+$ grep -rnE "RaindomAI|RaidomAI_App|C:\\Users\\USUARIO|github\.com" .claude CLAUDE.md _phases _methodology _templates _workflow ; echo "exit=$?"

(el mismo pipe, contado)  ->  total: 12   ·   distintas: 11
~~~

Misma lista, mismo orden y mismos recuentos que publica la seccion 7 (doce apariciones, once
ordenes distintas). **Es la lista completa, no una muestra.**

### g) Las atribuciones de la seccion 7 — todas correctas

Es lo que `F-035` senalo sobre `S-014`. Aqui no se repite:

~~~
$ git show ea48ae8:_persistence/decisions.md | awk '/^### D-067/,0' | grep -nE '^\$ |^### D-0'
1:### D-067 - El Gate 1 no es una etapa: se monta como agente y skill
53:$ ls -1 .claude/agents/ .claude/skills/
66:$ ls -1 _phases/
71:$ ls -1 _templates/015_gate1/
78:$ grep -rnE "RaindomAI|RaidomAI_App|C:\\Users\\USUARIO|github\.com" .claude CLAUDE.md _phases _methodology _templates _workflow
88:### D-068 - El Gate 1 son dos firmas: `gate1_auditor` dictamina y el patrocinador decide
134:$ grep -n "corresponde al patrocinador" .claude/skills/protocol-gate1/SKILL.md _templates/015_gate1/005_verdict.md
140:$ grep -nE "NO APROBADO|APROBADO" _templates/015_gate1/005_verdict.md
148:### D-069 - `NO AUDITABLE` es un tercer resultado del Gate 1, que la guia de metodo no tiene
195:$ sed -n '/^## 31. Resultados posibles/,/^↳/p' _methodology/000_method.md
207:$ grep -c "NO AUDITABLE" .claude/skills/protocol-gate1/SKILL.md _templates/015_gate1/005_verdict.md
214:### D-070 - Los dictamenes del Gate 1 son correlativos, y ninguno se sobrescribe
246:$ grep -n "no puede repetirse dos veces" .claude/skills/protocol-gate1/SKILL.md
249:$ grep -n "ya salio en un dictamen anterior" _templates/015_gate1/005_verdict.md
~~~

Cada orden que la seccion 7 atribuye a `D-067`/`D-068`/`D-069`/`D-070` esta en el bloque que dice:
las cuatro del `ls` y el barrido de fuga caen bajo `D-067` (lineas 53–78), las dos del patrocinador
bajo `D-068` (134–140), las dos de `NO AUDITABLE` bajo `D-069` (195–207) y las dos de correlatividad
bajo `D-070` (246–249). Ninguna atribucion apunta a un bloque donde no este.

### h) Las once ordenes de la seccion 7, reejecutadas sobre el commit

~~~
$ git ls-tree --name-only ea48ae8 .claude/agents/ .claude/skills/
.claude/agents/gate1_auditor.md
.claude/agents/report_auditor.md
.claude/agents/session-closer.md
.claude/agents/session-starter.md
.claude/skills/protocol-audit
.claude/skills/protocol-close
.claude/skills/protocol-gate1
.claude/skills/protocol-start

$ git ls-tree --name-only ea48ae8 _phases/
_phases/000_preproject.md
_phases/005_discovery.md
_phases/010_prototype.md

$ git ls-tree --name-only ea48ae8 _templates/015_gate1/
_templates/015_gate1/005_verdict.md

$ git grep -n "corresponde al patrocinador" ea48ae8 -- .claude/skills/protocol-gate1/SKILL.md _templates/015_gate1/005_verdict.md
ea48ae8:.claude/skills/protocol-gate1/SKILL.md:232
ea48ae8:.claude/skills/protocol-gate1/SKILL.md:410:| 6 | — | corresponde al patrocinador |
ea48ae8:.claude/skills/protocol-gate1/SKILL.md:437
ea48ae8:_templates/015_gate1/005_verdict.md:136:| 6 | Hay confianza suficiente para la inversion del MVP | **—** | **corresponde al patrocinador** |

$ git grep -nE "NO APROBADO|APROBADO" ea48ae8 -- _templates/015_gate1/005_verdict.md
ea48ae8:_templates/015_gate1/005_verdict.md:29

$ git grep -c "NO AUDITABLE" ea48ae8 -- .claude/skills/protocol-gate1/SKILL.md _templates/015_gate1/005_verdict.md
ea48ae8:.claude/skills/protocol-gate1/SKILL.md:10
ea48ae8:_templates/015_gate1/005_verdict.md:8

$ git grep -n "no puede repetirse dos veces" ea48ae8 -- .claude/skills/protocol-gate1/SKILL.md
ea48ae8:.claude/skills/protocol-gate1/SKILL.md:348

$ git grep -n "ya salio en un dictamen anterior" ea48ae8 -- _templates/015_gate1/005_verdict.md
ea48ae8:_templates/015_gate1/005_verdict.md:213
~~~

Los numeros de linea, los recuentos (`10` y `8`) y los listados coinciden con lo publicado. La
lectura de `_methodology/000_method.md` §31 tambien reproduce literal (dos resultados: Aprobado /
No aprobado), que es justo lo que `D-069` dice ampliar.

### i) Consistencia de `D-067` a `D-070` con `_methodology/000_method.md` §28–§32 — sostenida

Punto pedido por la seccion 6 del informe.

~~~
$ git show ea48ae8:_methodology/000_method.md | sed -n '/^## 29\./,/^## 30\./p'
## 29. Criterios de aprobación
1. El problema identificado es relevante.
2. La solución propuesta es comprensible.
3. El Actor Generador puede ejecutar el flujo principal **de forma autónoma**.
4. La solución satisface razonablemente la necesidad.
5. No existen problemas funcionales fundamentales que impidan continuar.
6. Existe confianza suficiente para realizar la inversión del MVP.
7. **La empresa considera válido el proceso de negocio representado.** *(dimensión D)*
~~~

Siete criterios en la guia; la tabla del Paso 5 del skill los reproduce los siete y marca el 6 como
no auditable por el agente. §32 exige exactamente las dos firmas que `D-068` implanta —«quien
audita» / «quien patrocina»— y admite que la revision independiente sea un agente que arranca en
frio. `D-069` declara de forma explicita que anade un resultado que §31 no tiene, en vez de
presentarlo como si ya estuviera. Las cuatro decisiones encajan entre si y con la guia: no encontre
contradiccion.

### j) La referencia a `_phases/010_prototype.md` §8 existe y dice lo que el skill supone

~~~
$ git show ea48ae8:_phases/010_prototype.md | sed -n '/^## 8\./,/^## 9\./p'
## 8. Lo que esta etapa le entrega al Gate
1. **La hipotesis con su condicion de falsacion** — del descubrimiento, sin tocar.
2. **La tarea entregada al usuario** — para comprobar que no describe la solucion.
3. **El perfil representativo y el numero fijado**, definidos **antes** de las sesiones.
4. **Un archivo por sesion**, con su estado y sus bloqueos.
5. **Las observaciones clasificadas.**
6. **La validacion de negocio.**
~~~

Los seis artefactos que el Paso 1b del skill lista corresponden con los que §8 declara, y con las
plantillas que existen en `_templates/010_prototype/` y `_templates/005_discovery/`.

### k) Hallazgos anteriores — ningun archivo de `_persistence/` los evalua

~~~
$ git grep -nE "F-03[56]" ea48ae8 -- _persistence project.md
ea48ae8:_persistence/progress.md:69  (... Quedan sin evaluar dos hallazgos de `R-014` ... ambos `Abierto` ...)
ea48ae8:_persistence/progress.md:70  (... `F-035` y `F-036` de `R-014` siguen sin evaluar por `manager`)
ea48ae8:_persistence/progress.md:112 (... `R-014` sobre `S-014` abrio `F-035` (Media) y `F-036` ...)
ea48ae8:_persistence/progress.md:120 (`manager` debe evaluar `F-035` y `F-036` de `R-014` ...)
ea48ae8:_persistence/progress.md:705 (... `R-014` sobre `S-014` abrio `F-035` (Media) y `F-036` ...)
~~~

Las cinco menciones son de registro —«siguen sin evaluar»—, no de evaluacion: no hay `T-XXX` ni
`D-XXX` que los trate, y `techdebt.md` no cambio en este commit. La seccion 0 del informe lo declara
asi, y es exacto.

## 2. Hallazgos

### F-037 — La Comprobacion 0 del Gate 1 compara fechas de autor, que si se pueden falsificar, y tres archivos afirman lo contrario

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `.claude/skills/protocol-gate1/SKILL.md` (Paso 2), `_persistence/decisions.md` (`D-069`), `_persistence/progress.md` |

- **Que se observo:** el Paso 2 resuelve la auditabilidad comparando la **fecha de autor** que
  imprime `%ad`, y el propio archivo afirma que eso no se puede burlar:

~~~
$ git show ea48ae8:.claude/skills/protocol-gate1/SKILL.md | grep -nE '%ad|imposible de aprobar a posteriori|no se pueden convencer'
124:git log --diff-filter=A --format="%ad %h %s" --date=short -- <DISC>/020_hypothesis.md
125:git log --diff-filter=A --format="%ad %h %s" --date=short -- <PROTO>/005_happy_path.md
126:git log --diff-filter=A --format="%ad %h %s" --date=short -- <PROTO>/010_participants.md
127:git log --diff-filter=A --format="%ad %h %s" --date=short -- <PROTO>/015_session_001.md
128:git log --diff-filter=A --format="%ad %h %s" --date=short -- <PROTO>/<subcarpeta del prototipo>
133:git log --format="%ad %h" --date=short -- <PROTO>/<subcarpeta del prototipo>
136:  git log --diff-filter=A --format="commit: %ad" --date=short -- "$f"
166:🔑 **Esta comprobacion es la unica del metodo imposible de aprobar a posteriori.** Las fechas del
167:historial no se pueden convencer. Todo lo demas se puede redactar mejor; el orden en que se
~~~

  `%ad` es un campo que fija quien hace el commit, y se sobrescribe con una variable de entorno.
  Prueba reproducible, en un repositorio desechable **fuera** de este proyecto:

~~~
$ git init -q . && echo sesion > 015_session_001.md && git add . \
  && GIT_AUTHOR_DATE="2026-01-10T10:00:00" GIT_COMMITTER_DATE="2026-01-10T10:00:00" git commit -q -m "sesion 1"
$ echo hipotesis > 020_hypothesis.md && git add . \
  && GIT_AUTHOR_DATE="2026-01-01T10:00:00" GIT_COMMITTER_DATE="2026-01-01T10:00:00" git commit -q -m "hipotesis (escrita DESPUES, fechada ANTES)"

$ git log --diff-filter=A --format="%ad %h %s" --date=short -- 020_hypothesis.md
2026-01-01 19cbaec hipotesis (escrita DESPUES, fechada ANTES)

$ git log --diff-filter=A --format="%ad %h %s" --date=short -- 015_session_001.md
2026-01-10 db6c138 sesion 1

$ git log --oneline
19cbaec hipotesis (escrita DESPUES, fechada ANTES)
db6c138 sesion 1
~~~

  La hipotesis se escribio **despues** de la sesion —es el commit hijo, el mas reciente del grafo, y
  el `git log --oneline` lo delata— y aun asi la lectura que el Paso 2 manda hacer la muestra nueve
  dias **antes**. La Comprobacion 0, tal como esta escrita, daria `PASA`.

- **Por que importa:** `D-069` justifica todo el tercer resultado sobre esa propiedad —«la unica
  comprobacion del metodo imposible de aprobar a posteriori»—, y `progress.md` la repite como hecho.
  Hoy es una afirmacion del registro que la evidencia contradice. El dia que el Gate se corra de
  verdad, esa comprobacion es la unica barrera de auditabilidad antes de la inversion mas cara del
  proyecto, y se atraviesa con dos variables de entorno. El orden real del grafo —que es lo que si
  resiste— sale en la misma pantalla, y el protocolo no manda mirarlo.
- **Que lo corregiria:** resolver la Comprobacion 0 por **orden topologico**, no por fecha —por
  ejemplo `git merge-base --is-ancestor <commit de la hipotesis> <commit de la sesion 1>`, o el orden
  en que `git log --format='%H'` devuelve los commits—, dejando `%ad` como dato informativo; y
  ajustar la frase de `D-069`, la del skill y la de `progress.md` a lo que el mecanismo garantice de
  verdad. ⚠️ Es una recomendacion, no una orden.

### F-038 — La Comprobacion 0 depende de `<subcarpeta del prototipo>`, que `project.md` no declara, y para ese caso no hay salida escrita

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `.claude/skills/protocol-gate1/SKILL.md` (Paso 0 y Paso 2), `project.md` |

- **Que se observo:** el skill usa `<subcarpeta del prototipo>` como valor a resolver en
  `project.md`, y `project.md` no lo nombra:

~~~
$ git show ea48ae8:.claude/skills/protocol-gate1/SKILL.md | grep -n "subcarpeta del prototipo"
128:git log --diff-filter=A --format="%ad %h %s" --date=short -- <PROTO>/<subcarpeta del prototipo>
133:git log --format="%ad %h" --date=short -- <PROTO>/<subcarpeta del prototipo>

$ git grep -n "subcarpeta" ea48ae8 -- project.md
ea48ae8:project.md:37:| Entregables de `010_prototype` | `010_prototype/` (el codigo descartable, en una subcarpeta suya) |
ea48ae8:project.md:182:| `010_prototype/` | ... y el codigo descartable del prototipo en una subcarpeta suya. ...
~~~

  Las dos filas dicen que hay una subcarpeta; ninguna dice **como se llama**. El Paso 0 del skill
  ordena resolver en `project.md` todo lo que va entre angulos y da una salida —`NO COMPROBABLE`—
  que la Comprobacion 0 no admite: sus resultados son `PASA` o `NO AUDITABLE`, y `NO COMPROBABLE` es
  un valor de criterio, no de la Comprobacion 0.

- **Por que importa:** dos de las siete lecturas de la Comprobacion 0 —«la tarea existia antes de
  construir el prototipo» y «el prototipo no cambio entre sesiones»— dependen de ese valor. Sin el
  declarado, el agente llega al Gate teniendo que elegir entre inventarse un nombre o dejar dos
  lecturas en un estado que su tabla no contempla, y ninguna de las dos salidas esta escrita.
- **Que lo corregiria:** declarar el nombre de esa subcarpeta en `project.md` al abrir
  `010_prototype/`; o, si se prefiere dejarla libre, que el Paso 2 diga como se localiza —por
  ejemplo «la unica subcarpeta de `<PROTO>/`»— y que resultado emite si no existe. ⚠️ Es una
  recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-035` | **Abierto** (sin cambio) | `manager` no lo evaluo: no hay `T-XXX` ni `D-XXX` que lo cite (control 1k). El informe lo declara asi en su seccion 0, lo que es honesto pero no lo cierra. Van dos auditorias con el abierto |
| `F-036` | **Abierto** (sin cambio) | Igual: ninguna `T-XXX` ni `D-XXX` lo trata en este commit, y `techdebt.md` no cambio en `ea48ae8` |

📌 **El defecto que `F-035` describia no se ha repetido en `S-015`:** las once atribuciones de la
seccion 7 de este informe son correctas (control 1g). Eso no cierra `F-035` —su objeto es la seccion
7 de `S-014`, que sigue como estaba—, pero conviene que quede dicho.

## 4. Lo que NO se pudo comprobar

Ninguno de los controles fallo. Los tres mecanicos se corrieron enteros sobre `ea48ae8`.

⚠️ **Un limite del alcance, no un control fallido:** el Gate 1 no se ha ejecutado nunca. Todo lo que
esta auditoria dice sobre `protocol-gate1` se comprobo leyendo el texto y contrastando sus
referencias, no viendolo correr. `F-037` es la excepcion: ahi si se reprodujo el mecanismo en un
repositorio desechable.

## 5. Recomendaciones sin hallazgo

- **`_audit/015_gate1/` esta declarada en `project.md` y no existe en el arbol**, igual que
  `010_prototype/`. El control de carpetas no la ve —solo mira el primer nivel— y no tiene la nota
  de «declarada por adelantado» que si tiene `010_prototype/` (`D-061`). Una linea que lo diga evita
  que el dia que alguien la busque lo lea como un olvido.
- **El criterio 3 del skill dice «El Generador» donde `_methodology/000_method.md` §29 dice «El Actor
  Generador».** No cambia el sentido, y el skill ya manda leer la guia como fuente; pero la cita
  literal cuesta lo mismo que la abreviada.
- **La seccion 7 afirma los recuentos «12 y 11» citando una fila de `wc -l`/`sort -u | wc -l` que no
  publica.** Los doce renglones estan a la vista y se cuentan, asi que no es un veredicto sin
  evidencia; aun asi, si el recuento se cita como comprobado, la orden que lo produjo entra en el
  mismo bloque y se acaba la duda.
