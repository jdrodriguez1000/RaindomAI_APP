# Auditoria R-040 — sobre S-040

| Campo | Valor |
|---|---|
| Auditoria | R-040 |
| Informe auditado | `_audit/S-040.md` |
| Commit auditado | `4962db2` |
| Fecha | 2026-09-14 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

### a) Sobre que commit se audita

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep Pendiente
| `S-040.md` | S-040 | 2026-09-14 | Pendiente | Pendiente | Pendiente | - |

$ git log -1 --format=%h -- _audit/S-040.md
97ccbe1

$ git show 97ccbe1:_audit/S-040.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `4962db2` |

$ git show --stat 97ccbe1 | tail -2
 _audit/S-040.md | 113 ++++++++++++++++++++++++++++++++++++++++++++++++++++----
 1 file changed, 105 insertions(+), 8 deletions(-)

$ git show --stat --name-only --format= 4962db2 | grep -c .
8
```

`97ccbe1` es el commit de anclaje (un solo archivo). Se audita el hash literal de la cabecera,
`4962db2`, cuyos 8 archivos coinciden con la lista anclada de la seccion 1 del informe. El informe se
leyo en su version de `97ccbe1`.

### b) Controles mecanicos sobre `4962db2`

```
$ h=4962db2; for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show $h:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show $h:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress

$ diff <(git ls-tree -d --name-only $h | sed 's|$|/|' | sort) <(git show $h:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort); echo "exit=$?"
1a2
> 010_prototype/
9a11
> temporal/
exit=1

$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" $h -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1

$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' $h -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

- Indices: coherentes en los siete archivos.
- Carpetas: las dos diferencias (`010_prototype/`, `temporal/`) tienen su razon escrita en
  `project.md`, seccion «Carpetas propias». No es hallazgo.
- Fuga de datos propios y codigos instanciados: cero lineas.

Codigos instanciados en lo que el commit anade a `.claude/`:

```
$ git show 4962db2 -- .claude | grep -E '^\+' | grep -noE '\b[A-Z]{1,3}-[0-9]+\b'; echo "exit=$?"
exit=1
```

### c) Paso 2d y 2e del cierre, reproducidos anclados

```
$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
16

$ for f in _persistence/assumptions.md _persistence/decisions.md _persistence/tasks.md; do printf '%s %s\n' $f "$(git show db0e613:$f | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
_persistence/assumptions.md 4
_persistence/decisions.md 7
_persistence/tasks.md 5

$ for f in $(git diff --name-only 4962db2^ 4962db2 -- _persistence _audit ":(exclude)_audit/S-040.md"); do n=$(git diff -U0 4962db2^ 4962db2 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done; echo "exit-bucle"
exit-bucle
```

El recuento 16, las 16 ocurrencias heredadas de `\x08` (4+7+5, iguales en `db0e613`) y el CONTROL
vacio del 7c-bis reproducen. El recuento de la «Procedencia» no: ver `F-110`.

### d) Seccion 6 del informe, punto 1 — la nota bajo `D-153`

```
$ git show 4962db2:_persistence/decisions.md | awk '/la mitad del criterio que dependia/{print h; exit} /^### D-[0-9]+/{h=NR": "$0}'
9817: ### D-153 - La guia de arranque se publica DESPUES de promover la plantilla, no antes
```

La nota pertenece a `D-153`, y su «Criterio de cierre» (linea 38 de la entrada: «el `project.md` del
esqueleto lleva sus tres filas, y solo despues aparece la guia en su raiz») es el que la nota dice
medio cumplido. Correcto.

### e) Afirmaciones sobre el repositorio del esqueleto (lectura, sin escribir alli)

Lo que `D-159`, `D-161` y `D-162` afirman del otro repositorio se contrasto de solo lectura con
`git -C`:

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" log --oneline -4; git -C "$ESQ" status -sb | head -3
c7d0a87 Regenera las copias de la raiz desde sus plantillas (origen 841dc53)
841dc53 Promocion del andamiaje desde RaidomAI_App (origen db0e613)
1748f0a Sincroniza las seis areas agnosticas con el proyecto de origen
fa7da56 Estado de partida del esqueleto, tal como estaba antes de sincronizar
## main...origin/main

$ for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-harvest/SKILL.md .claude/skills/protocol-promote/SKILL.md _templates/000_preproject/005_project.md _templates/000_preproject/015_tasks.md CLAUDE.md; do [ "$(git rev-parse db0e613:$f)" = "$(git -C "$ESQ" rev-parse 841dc53:$f)" ] && echo "mismo blob $f" || echo "BLOB DISTINTO $f"; done
mismo blob .claude/skills/protocol-close/SKILL.md
mismo blob .claude/skills/protocol-harvest/SKILL.md
mismo blob .claude/skills/protocol-promote/SKILL.md
mismo blob _templates/000_preproject/005_project.md
mismo blob _templates/000_preproject/015_tasks.md
mismo blob CLAUDE.md

$ for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-harvest/SKILL.md .claude/skills/protocol-promote/SKILL.md _templates/000_preproject/005_project.md _templates/000_preproject/015_tasks.md CLAUDE.md; do printf '%s origen_CR=%s 1748f0a_CR=%s 841dc53_CR=%s\n' "$f" "$(git show db0e613:$f | tr -dc '\r' | wc -c)" "$(git -C "$ESQ" show 1748f0a:$f 2>/dev/null | tr -dc '\r' | wc -c)" "$(git -C "$ESQ" show 841dc53:$f | tr -dc '\r' | wc -c)"; done
.claude/skills/protocol-close/SKILL.md origen_CR=0 1748f0a_CR=0 841dc53_CR=0
.claude/skills/protocol-harvest/SKILL.md origen_CR=0 1748f0a_CR=0 841dc53_CR=0
.claude/skills/protocol-promote/SKILL.md origen_CR=0 1748f0a_CR=0 841dc53_CR=0
_templates/000_preproject/005_project.md origen_CR=0 1748f0a_CR=0 841dc53_CR=0
_templates/000_preproject/015_tasks.md origen_CR=0 1748f0a_CR=0 841dc53_CR=0
CLAUDE.md origen_CR=0 1748f0a_CR=0 841dc53_CR=0

$ git -C "$ESQ" diff -U0 1748f0a 841dc53 | grep -cE '^-[^-]|^-$'
13

$ git -C "$ESQ" diff -U0 1748f0a 841dc53 | grep -E '^(\+\+\+ |-[^-])' | cut -c1-150
+++ b/.claude/skills/protocol-close/SKILL.md
-(`Bloqueante` / `No bloqueante`), mas **Origen** (`usuario` / `manager` / `report_auditor`).
-grep -qF 'git show --stat --name-only --format=' _audit/S-XXX.md ||
-7c, no esto.
+++ b/.claude/skills/protocol-harvest/SKILL.md
-Este protocolo lo ejecuta **unicamente `manager`**, y es el unico del andamiaje que **escribe fuera de
-este repositorio**. Convierte las lecciones de la etapa que cierra en entradas del archivo de
+++ b/.claude/skills/protocol-promote/SKILL.md
+++ b/CLAUDE.md
-🚨 **La cosecha la ejecutas tu, con la skill `protocol-harvest`, y es el unico protocolo del
-andamiaje que escribe FUERA de este repositorio.** Por eso lleva puerta: clasificar las lecciones y
-redactar lo que se propone son tuyos, pero **no se toca el otro repositorio hasta que el usuario
-aprueba** las entradas, una por una. Aprobado eso, escribir, commitear y subir van seguidos.
+++ b/_templates/000_preproject/005_project.md
-existen porque nombran **una ubicacion en esta maquina** —la de este repositorio y la del
-repositorio de lecciones—, no porque sirvan para navegar dentro de ellos. **Para citar un archivo
-del proyecto se usa la relativa.**
+++ b/_templates/000_preproject/015_tasks.md
-| Origen | `usuario` / `manager` / `report_auditor` |

$ git -C "$ESQ" show --name-only --format= c7d0a87
_persistence/tasks.md
project.md

$ git -C "$ESQ" diff -U0 841dc53 c7d0a87 | grep -E '^(\+\+\+ |-[^-])' | cut -c1-140
+++ b/_persistence/tasks.md
-| Origen | `usuario` / `manager` / `report_auditor` |
+++ b/project.md
-existen porque nombran **una ubicacion en esta maquina** —la de este repositorio y la del
-repositorio de lecciones—, no porque sirvan para navegar dentro de ellos. **Para citar un archivo
-del proyecto se usa la relativa.**
-| `_audit/` | **Como se comprueba** el trabajo: el informe de cada sesion, la auditoria de cada una, el tablero y el registro de hallazgos.
```

Todo lo que `D-159`, `D-161` y `D-162` afirman del esqueleto se sostiene: los seis blobs son
identicos al origen, todo LF, 13 lineas borradas y todas versiones anteriores, `c7d0a87` toca solo las
dos copias, y el esqueleto no va `ahead`.

### f) Seccion 6 del informe, punto 3 — el lote pendiente

```
$ for f in $(git ls-tree -r --name-only 4962db2 -- .claude CLAUDE.md _phases _methodology _templates _workflow); do git show 4962db2:"$f" | diff -q --strip-trailing-cr <(git -C "$ESQ" show c7d0a87:"$f" 2>/dev/null) - >/dev/null || echo "difiere  $f"; done; for f in $(git -C "$ESQ" ls-tree -r --name-only c7d0a87 -- .claude CLAUDE.md _phases _methodology _templates _workflow); do git cat-file -e 4962db2:"$f" 2>/dev/null || echo "solo en el esqueleto  $f"; done; echo fin
difiere  .claude/skills/protocol-promote/SKILL.md
fin
```

Del lote pendiente solo falta `protocol-promote/SKILL.md`, como dice el informe. No falta nada mas.

### g) La prueba del Paso 1b (`D-162`), con la skill leida del commit y no del arbol

```
$ git show 4962db2:.claude/skills/protocol-promote/SKILL.md | sed -n '/^## Paso 1b /,/^---$/p' | sed -n '/^```bash$/,/^```$/p' | sed '1d;$d' > "$SP/p1b.sh"; for h in 841dc53 c7d0a87; do rm -rf "$SP/esq_$h"; mkdir -p "$SP/esq_$h"; git -C "$ESQ" archive $h | tar -x -C "$SP/esq_$h"; echo "== $h"; ESQ="$SP/esq_$h"; source "$SP/p1b.sh"; done
== 841dc53
 26  project.md
  0  _persistence/progress.md
  3  _persistence/tasks.md
  0  _persistence/decisions.md
  0  _persistence/constraints.md
  0  _persistence/assumptions.md
  0  _persistence/lessons.md
  0  _persistence/techdebt.md
  0  _audit/index.md
  0  _audit/findings.md
== c7d0a87
  0  project.md
  0  _persistence/progress.md
  0  _persistence/tasks.md
  0  _persistence/decisions.md
  0  _persistence/constraints.md
  0  _persistence/assumptions.md
  0  _persistence/lessons.md
  0  _persistence/techdebt.md
  0  _audit/index.md
  0  _audit/findings.md
```

(`$SP` es el directorio temporal de esta auditoria, fuera del repositorio.) Reproduce lo publicado en
`D-162`. `T-182` y `T-163` quedan `Implementada` con su criterio cumplido en el diff: la skill lleva
Paso 1b, item de puerta, paso de escritura, comprobacion en el registro de vuelta y linea de reporte;
`D-161` existe para `T-163`.

## 2. Hallazgos

### F-110 — La seccion 7 de `S-040` publica como salida cruda dos bloques que su orden no produce

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-040.md`, seccion 7: bloque «Procedencia por archivo» (lineas 297-318 en `97ccbe1`) con su nota (320-324), y bloque «CONTROL DE CIFRA ADYACENTE — salida» (426-451) |

- **Que se observo (a) — procedencia.** El bloque pega 17 lineas; la 17.ª de su «salida» (linea 309)
  es `b/_persistence/decisions.md :: $ git show db0e613:.gitignore | grep -n '^temporal/$'`. Esa linea
  no puede salir de la orden pegada, porque la propia orden termina en
  `grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'`, que la excluye. La nota que sigue explica la
  diferencia 17/16 diciendo que esa linea «esta anclada … y por eso no aparece en el recuento», pero la
  orden de la procedencia lleva el mismo filtro que el recuento.

```
$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
16

$ git diff -U0 db0e613 4962db2 -- _persistence _audit ':(exclude)_audit/S-040.md' | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | grep -c 'git show db0e613:.gitignore'
0

$ echo '$ git show db0e613:.gitignore | grep -n' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'; echo "exit=$?"
exit=1
```

- **Que se observo (b) — cifra adyacente.** La salida pegada cita las lineas 33, 34, 97, 98, 249,
  250, 303, 304, 363, 364, 379 y 380 del informe. Corrida la misma orden sobre las dos versiones
  commiteadas del informe, no sale esa lista en ninguna:

```
$ git show 4962db2:_audit/S-040.md > "$TEMP/s40_4962db2.md"; awk '/^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next } infence { next } armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print "S-040.md:"NR": "substr($0,1,70); if (since>3) armed=0 }' "$TEMP/s40_4962db2.md"
S-040.md:33: Su equivalencia anclada, la que el Paso 7c pega en la NOTA DE CIERRE d
S-040.md:40: Y ademas, en este mismo commit, el propio cierre anade `_audit/S-040.m
S-040.md:41: `_audit/index.md` (Paso 6b), y actualiza `_persistence/progress.md` (P
S-040.md:104: De esas, la real es de `T-001` (alcance sin definir, aplazada por `D-1
S-040.md:105: real, se hace al cerrar la etapa), `T-144` (aplazada, `005_discovery` 
S-040.md:256: Reejecucion de las 16, en el orden de arriba — todas reproducen exac
S-040.md:257: `decisions.md` (ver seccion 1, y el detalle completo dentro de `D-159`
S-040.md:310: 📌 **Nota sobre esta procedencia:** el barrido de arriba (sin acotar
S-040.md:311: una mas que las 16 del recuento oficial del paso: la linea extra —`$
S-040.md:349: Lectura linea por linea de la condicion de parada: las 33-34 y 97-98 n
S-040.md:350: (citan codigos `T-XXX`, no recuentos); las 249-250 citan «16», consi
S-040.md:396: 5 filas de la tabla = 5 archivos del contraste 1; suma de la columna `
S-040.md:397: contraste 2. Cuadran.
S-040.md:412: Recuento: 1 linea. No es un fallo del cierre: es el propio cambio de e
S-040.md:413: `protocol-promote`), que sube al esqueleto en el proximo lote de promo

$ git show 97ccbe1:_audit/S-040.md > "$TEMP/s40_97ccbe1.md"; awk '<el mismo programa awk de arriba>' "$TEMP/s40_97ccbe1.md"
S-040.md:48: (`_audit/S-040.md`, `_audit/index.md`, `_persistence/progress.md`), ex
S-040.md:114: De esas, la real es de `T-001` (alcance sin definir, aplazada por `D-1
S-040.md:115: real, se hace al cerrar la etapa), `T-144` (aplazada, `005_discovery` 
S-040.md:266: Reejecucion de las 16, en el orden de arriba — todas reproducen exac
S-040.md:267: `decisions.md` (ver seccion 1, y el detalle completo dentro de `D-159`
S-040.md:320: 📌 **Nota sobre esta procedencia:** el barrido de arriba (sin acotar
S-040.md:321: una mas que las 16 del recuento oficial del paso: la linea extra —`$
S-040.md:364: principio de la orden; tiene que dar la lista de trabajo del Paso 7c-b
S-040.md:375: rellenar.** `D-159` a `D-162` y sus criterios de cierre (`T-182`, la n
S-040.md:397: Ninguna linea `< …`: no se borro prosa. Consistente con que el Paso 
S-040.md:418: El commit real lleva **8** archivos (no los 5 que cubria la tabla de l
S-040.md:419: area de staging antes de que el propio cierre añadiera `_audit/S-040.
S-040.md:446: Lectura linea por linea de la condicion de parada: las 33-34 y 97-98 n
S-040.md:447: (citan codigos `T-XXX`, no recuentos); las 249-250 citan «16», consi
S-040.md:493: 5 filas de la tabla = 5 archivos del contraste 1; suma de la columna `
S-040.md:494: contraste 2. Cuadran.
S-040.md:509: Recuento: 1 linea. No es un fallo del cierre: es el propio cambio de e
S-040.md:510: `protocol-promote`), que sube al esqueleto en el proximo lote de promo
```

  La salida publicada corresponde a un borrador que despues cambio (en `4962db2` las mismas lineas
  estan 7 lineas mas abajo en la primera mitad y 33 en la segunda). La «lectura linea por linea» que
  la acompana revisa ese borrador: la linea 33 de `4962db2` («Su equivalencia anclada…») no la miro, y
  las 418-419 de `97ccbe1` —las unicas del informe final con cifras nuevas, «8 archivos (no los 5…)»—
  tampoco.

- **Por que importa:** es la familia exacta de defecto que ya cerro `F-108` —una linea en un bloque
  de salida cruda que su orden no emite— y la que describe `L-056`, reaparecida en el commit
  inmediatamente posterior a su cierre; ademas va acompanada de una explicacion que el propio bloque
  contradice. El CONTROL DE CIFRA ADYACENTE existe para que el informe final no lleve cifras sin mirar,
  y publicado sobre un borrador deja de poder distinguirse de uno que no se corrio sobre lo commiteado.
  `Media` y no `Alta`: ninguna de las cifras que el informe da como resultado (16, 8, 16 `\x08`) es
  falsa; lo que falla es la evidencia que dice respaldarlas.
- **Que lo corregiria:** una nota fechada en la seccion 7 de `S-040`, sin reescribir los bloques, que
  pegue la procedencia reproducida (16 lineas) retirando la explicacion de la 17.ª, y la salida del
  CONTROL DE CIFRA ADYACENTE corrida sobre `git show 4962db2:_audit/S-040.md` con su lectura. Como
  mejora hacia adelante, valorar que el 7c-ter del cierre corra ese control sobre la version anclada y
  no sobre el borrador. ⚠️ Es una recomendacion, no una orden.

### F-111 — Dos resultados nuevos del registro se afirman sin su orden ni su salida

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/tasks.md`, nota del 2026-09-14 bajo `T-162`; `_persistence/decisions.md`, `D-159`, vineta «Final de linea (Paso 4)» |

- **Que se observo:** la nota bajo `T-162` afirma «Barridos de datos propios y de codigos instanciados
  sobre el borrador: cero lineas» sin ningun bloque de codigo en la nota. `D-159` afirma «todas las
  parejas LF en los dos lados; copia byte a byte desde el commit de origen» sin la medicion `tr -dc`
  que el Paso 4 de `protocol-promote` define.

```
$ git show 4962db2:_persistence/tasks.md | awk '/el borrador de `_outbound\/` se pone al dia/{print h; exit} /^### T-[0-9]+/{h=NR": "$0}'
7510: ### T-162 - Escribir la guia de arranque del esqueleto

$ git show 4962db2:_persistence/tasks.md | sed -n '/el borrador de `_outbound\/` se pone al dia/,/^### T-163/p' | grep -c '^```'
0

$ git show 4962db2:_persistence/decisions.md | grep -n "Final de linea (Paso 4)"
10271:- **Final de linea (Paso 4):** todas las parejas LF en los dos lados; copia byte a byte desde el
```

  Los dos resultados **son ciertos**: el primero lo reproduce esta auditoria (abajo), y el segundo la
  medicion de CR y la comparacion de blobs de la seccion 1e.

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" 4962db2 -- _outbound/skeleton_readme.md; echo "exit=$?"
exit=1
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 4962db2 -- _outbound/skeleton_readme.md | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

- **Por que importa:** `CLAUDE.md` exige que un resultado afirmado —«cero coincidencias»— vaya con
  patron y ambito, y la regla rige para entradas nuevas como estas. Sin la orden, quien quiera
  contrastarlo tiene que adivinar que patron se uso; en el caso de `T-162`, ademas, el borrador va a
  publicarse en otro repositorio, que es donde una fuga ya no se corrige. `Baja`: los dos resultados
  reproducen, el defecto es de reproducibilidad, no de contenido.
- **Que lo corregiria:** notas fechadas bajo `T-162` y `D-159` con las ordenes y su salida, ancladas a
  `4962db2`. ⚠️ Es una recomendacion, no una orden.

### F-112 — El Paso 6 de `protocol-promote` queda con dos pasos numerados «4.»

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `.claude/skills/protocol-promote/SKILL.md`, Paso 6 (lineas 304-310 en `4962db2`); `T-182` / `D-162` |

- **Que se observo:** al insertar el paso 3 nuevo («cada copia de la raiz aprobada se regenera…») se
  renumero el commit a «4.» pero no el push, que sigue en «4.».

```
$ git show 4962db2:.claude/skills/protocol-promote/SKILL.md | grep -nE '^[0-9]+\. ' | sed -n '1,5p'
304:1. se copia cada archivo aprobado, **respetando el final de linea del destino** (Paso 4);
305:2. las carpetas nuevas se crean con su contenido **completo**, no a medias;
306:3. cada copia de la raiz aprobada se regenera **desde la plantilla ya copiada al esqueleto**, en el
308:4. commit en el esqueleto, con un mensaje que diga **de que proyecto viene** la promocion, **que
310:4. push.
```

- **Por que importa:** es el procedimiento que un agente o `manager` sigue paso a paso y cita por
  numero; dos «4.» hacen ambigua la referencia. Ademas este archivo sube al esqueleto en el proximo
  lote y se heredaria. `Baja`: el orden del texto sigue siendo el correcto.
- **Que lo corregiria:** renumerar el push a «5.» antes de promover el lote. ⚠️ Es una recomendacion,
  no una orden.

## 3. Hallazgos anteriores

```
$ sed -n '/^## Indice/,/^---/p' _audit/findings.md | grep -vE "\| Implementado \|"
## Indice

| Codigo | Hallazgo | Auditoria | Gravedad | Estado |
|---|---|---|---|---|
| [F-109](#f-109---la-seccion-1-del-informe-atribuye-a-t-180-las-tres-reincidencias-de-la-nota-de-l-056-y-la-tercera-es-de-t-179) | La seccion 1 del informe atribuye a `T-180` las tres reincidencias de la nota de `L-056`, y la tercera es de `T-179` | R-039 | Baja | Abierto |
```

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-109 | **Sigue `Abierto`** | `manager` no lo evaluo: la seccion 0 de `S-040` lo declara «Sin evaluar esta sesion», no hay `T-XXX` ni `D-XXX` que lo cite, y `4962db2` no toca `_audit/findings.md` ni `_audit/S-039.md` (ver `git show --stat 4962db2`) |

## 4. Lo que NO se pudo comprobar

- **La coincidencia entre lo presentado en la puerta y lo subido en `841dc53`** (`D-159`, `A-019`).
  Lo presentado en la puerta vivio en la conversacion y no esta en ningun archivo; solo se pudo
  comprobar que lo subido es identico al origen `db0e613` (seccion 1e). `D-162` declara ella misma que
  la puerta de `c7d0a87` presento cifras equivocadas (21/5 frente a 22/4).
- **El remoto del esqueleto:** `status -sb` sin `ahead` se leyo contra la referencia local
  `origin/main`; no se consulto el remoto.

## 5. Recomendaciones sin hallazgo

- **La cifra «13 lineas (3, 2, 0, 3, 1, 4)» de `D-159`** (seccion 6 del informe, punto 2) no lleva
  orden propia, pero se deriva de la columna de borrados del `numstat` de `841dc53` pegado en la misma
  entrada (3+2+0+4+3+1, en otro orden) y esta auditoria la reproduce (seccion 1e). Una nota con la orden
  `git -C … diff -U0 1748f0a 841dc53 | grep '^-'` la haria autosuficiente.
- **`D-160` y la seccion 0 de `S-040` no dicen lo mismo.** `D-160` fija que «los hallazgos se trabajan
  despues en la misma sesion»; la seccion 0 invoca `D-160` para justificar no evaluar `F-109`. Lo que
  paso es que la ventana se acabo, no que `D-160` lo mandara. Sobre el punto 4 de la seccion 6 del
  informe: el hueco que se ve es este, que la decision no dice que ocurre cuando la promocion ocupa la
  sesion entera.
- **Separador duplicado en `decisions.md`:** `4962db2` anade un segundo `---` justo detras del que
  cerraba `D-158`, antes de `D-159`. Cosmetico.
- **El Paso 1b de `protocol-promote` solo trae la orden sobre el arbol del esqueleto.** La medicion
  «despues del commit» que pide el Paso 7.3 (con `git -C "$ESQ" show <hash>:`) queda en prosa, y
  `D-162` tuvo que escribir su propia variante. Traer esa segunda orden al Paso 1b evitaria que cada
  promocion la reinvente.
