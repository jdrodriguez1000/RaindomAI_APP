# Auditoria R-027 — sobre S-027

| Campo | Valor |
|---|---|
| Auditoria | R-027 |
| Informe auditado | `_audit/S-027.md` |
| Commit auditado | `79e88a2` |
| Fecha | 2026-09-07 |
| Veredicto | Con hallazgos (4) |

> El informe se ancla en un segundo commit (`43151e1`, Paso 7c/7c-bis). `git log -1 --format=%h --
> _audit/S-027.md` devuelve `43151e1`; la cabecera del informe escribe `79e88a2`, que es el commit
> sustantivo y el que se audita. El `--stat` lo confirma: `43151e1` toca 3 archivos (informe,
> `decisions.md`, `tasks.md` — lo que el 7c-bis autoriza), `79e88a2` toca 15, que son los que la
> seccion 1 del informe enumera.

```
$ git log -1 --format=%h -- _audit/S-027.md
43151e1
$ git show 43151e1:_audit/S-027.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `79e88a2` |
$ git show --stat --name-only --format= 43151e1 | grep -c .
3
$ git show --stat --name-only --format= 79e88a2 | grep -c .
15
```

## 1. Que se comprobo

### a) Indice ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 79e88a2:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 79e88a2:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

### b) Carpetas del arbol contra las declaradas — dos diferencias, las dos con razon escrita

```
$ diff <(git ls-tree -d --name-only 79e88a2 | sed 's|$|/|' | sort) <(git show 79e88a2:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos estan justificadas en `project.md` («Carpetas propias»): `010_prototype/` declarada por
adelantado (`D-061`) y `temporal/` excluida por `.gitignore`. No hay hallazgo.

### c) Fuga de datos propios — sin lineas reales

Patron propio de este protocolo (nombre, carpeta raiz, host):

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" 79e88a2 -- .claude CLAUDE.md _phases _methodology
$ echo "exit=$?"
exit=1
```

Patron ampliado de `protocol-close`, sobre las seis areas y `.gitignore` (`T-038` sigue abierta):

```
$ git grep -nIE "RaindomAI|RaidomAI|Proyectos_TripleS|Company_TripleS|TripleS|github\.com|jdrodriguez1000|USUARIO|C:\\Users" 79e88a2 -- .claude CLAUDE.md _phases _methodology _templates _workflow .gitignore
79e88a2:_templates/010_prototype/025_business_validation.md:14:> 🚨 **SESION APARTE, CON EL PATROCINADOR, SIN USUARIOS DELANTE.**
```

Unica linea, falso positivo conocido (`USUARIO` dentro de «USUARIOS»), ya publicado por el propio
informe y por `D-113`. No hay hallazgo.

### d) Codigos instanciados en `.claude/` — el objetivo declarado del commit se cumple

```
$ git grep -nIoE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2 -- .claude
$ echo "exit=$?"
exit=1
$ git grep -nIoE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2 -- CLAUDE.md
79e88a2:CLAUDE.md:251:N-001
79e88a2:CLAUDE.md:251:I-001
79e88a2:CLAUDE.md:252:FT-001
79e88a2:CLAUDE.md:287:N-001
79e88a2:CLAUDE.md:287:I-001
```

Los cinco restantes son los primeros de serie que la convencion nueva declara permitidos. El mensaje
de commit («`CLAUDE.md` y `.claude/` quedan agnosticos») se sostiene.

### e) Seccion 0 del informe — las siete comprobaciones reproducen sobre el commit

```
$ git show 79e88a2:_audit/S-026.md | grep -c 'Ancladas: 28, no 29'
1
$ git show 79e88a2:_audit/S-026.md | grep -c 'se anclaron 27, no 29'
1
$ git show 79e88a2:.claude/skills/protocol-close/SKILL.md | grep -c 'ese total es la suma de los numeros por archivo que el'
1
$ git show 79e88a2:.claude/skills/protocol-close/SKILL.md | grep -c 'nunca se dobla dentro del total'
1
$ git show 79e88a2:_persistence/tasks.md | grep -cE '^- \*\*Criterio de cierre:\*\* el archivo existe, tiene una fila por paso del procedimiento, y su adopcion$'
1
$ git show 79e88a2:_persistence/tasks.md | grep -cE '^- \*\*Criterio de cierre:\*\* el archivo existe, tiene una fila por paso del procedimiento, es agnostico$'
0
$ git show 79e88a2:_persistence/tasks.md | grep -cE '^- 📌 \*\*Nota del 2026-09-07 \(`F-075`, `D-111`\)'
1
```

Y las cifras que `D-112` fija (14/14) reproducen contra el commit de anclaje de `S-026`:

```
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo -n "$f: "; git diff -U0 89f9f73^ 89f9f73 -- "$f" | grep -cE '^-\$ .*<hash>'; done
_persistence/decisions.md: 14
_persistence/tasks.md: 14
```

### f) Recuentos del Paso 2d (seccion 7) — correctos

```
$ git diff -U0 79e88a2^ 79e88a2 -- _persistence _audit ":(exclude)_audit/S-027.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
46
$ git diff -U0 79e88a2^ 79e88a2 -- _persistence _audit ":(exclude)_audit/S-027.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
34
```

El desglose por categorias del informe (4 con hash real + 4 con `HEAD` + 8 sin hash + 30 con
`<hash>`) tambien cuadra:

```
$ git diff -U0 79e88a2^ 79e88a2 -- _persistence _audit ":(exclude)_audit/S-027.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | grep -c 'HEAD'
4
$ git diff -U0 79e88a2^ 79e88a2 -- _persistence _audit ":(exclude)_audit/S-027.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | grep -c '89f9f73'
4
```

### g) Anclaje del Paso 7c-bis — 24 ordenes, y el ambito respetado

```
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo -n "$f: "; git diff -U0 43151e1^ 43151e1 -- "$f" | grep -cE '^\+\$ .*79e88a2'; done
_persistence/decisions.md: 16
_persistence/tasks.md: 8
```

Las 24 ordenes que la nota de cierre declara ancladas existen (16 + 8). El commit de anclaje solo
toca los dos archivos que el Paso 7c-bis autoriza, y solo sustituye ordenes y anade sus salidas: no
borra prosa. Ver `F-078` para la forma en que la nota deriva ese total.

### h) `_audit/S-026.md` — solo adiciones, sin reescritura

```
$ git diff --numstat 79e88a2^ 79e88a2 -- _audit/S-026.md
35	0	_audit/S-026.md
```

Cumple `D-019`: la nota fechada se anade, la linea original no se toca.

### i) Fecha del informe contra la del commit (Paso 7d)

```
$ git log -1 --format='%h %ad' --date=short 79e88a2
79e88a2 2026-09-07
```

Coincide con la cabecera del informe (`2026-09-07`).

### j) Caracteres de control (Paso 2e) — la salida publicada reproduce

```
$ for f in _audit/findings.md _persistence/assumptions.md _persistence/decisions.md _persistence/tasks.md; do echo -n "$f: "; git show 79e88a2:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'; done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Las cuatro nuevas de `assumptions.md` estan registradas en `DT-006` (`Propuesta (pendiente del
usuario)`), no ocultas.

### k) La pregunta de la seccion 6 sobre `A-013` — comprobada, y el supuesto sale reforzado

De los 50 codigos distintos que el commit quita de `.claude/` y `CLAUDE.md`, **49 existen como
entrada real del registro**; el unico que no es `H-01`, que era un ejemplo generico de hito en
`session-starter.md` (sustituido por `H-nn`), no una cita:

```
$ git diff 79e88a2^ 79e88a2 -- .claude CLAUDE.md | grep -E '^-' | grep -ohE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' | sort -u > /tmp/removed.txt
$ grep -rhoE '^### [A-Z]+-[0-9]+' _persistence/ _audit/findings.md | grep -oE '[A-Z]+-[0-9]+' | sort -u > /tmp/reg.txt
$ git show 79e88a2:_persistence/progress.md | grep -oE '^### S-[0-9]+' | grep -oE 'S-[0-9]+' >> /tmp/reg.txt
$ ls _audit/R-*.md | grep -oE 'R-[0-9]+' >> /tmp/reg.txt
$ sort -u /tmp/reg.txt -o /tmp/reg.txt; comm -23 /tmp/removed.txt /tmp/reg.txt
H-01
```

Esto comprueba **existencia de la entrada**, no que cada entrada nombre la regla concreta: `A-013`
sigue siendo un supuesto, pero su parte comprobable no falla en ningun caso.

## 2. Hallazgos

### F-076 — Las cifras del volumen de la limpieza de `.claude/` no salen de ninguna orden y contradicen la salida que `D-113` publica en el mismo commit

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md` (`T-115`: titulo, cuerpo y nota) y `_audit/S-027.md` §1 |

- **Que se observo:** `T-115` se titula «Aplicar la misma ruta a `.claude/`: **54 citas en tres
  archivos**», su cuerpo dice «`protocol-close/SKILL.md` (**51 lineas, 50 codigos distintos**)», su
  nota dice «Salieron las 54 citas: **51 de `protocol-close`**, 2 de `protocol-start` y 1 de
  `session-closer`», y la seccion 1 del informe dice «`protocol-close/SKILL.md` — **51 lineas** […]
  **50 citas** de codigos instanciados salen del texto». Los numeros reales, medidos con el mismo
  patron que `D-113` publica dos entradas mas arriba en el mismo commit:

```
$ git grep -cIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2^ -- .claude
79e88a2^:.claude/agents/session-closer.md:1
79e88a2^:.claude/agents/session-starter.md:1
79e88a2^:.claude/skills/protocol-close/SKILL.md:48
79e88a2^:.claude/skills/protocol-start/SKILL.md:2
$ git show 79e88a2^:.claude/skills/protocol-close/SKILL.md | grep -ohE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' | sort -u | wc -l
48
$ git diff --numstat 79e88a2^ 79e88a2 -- .claude/skills/protocol-close/SKILL.md
73	54	.claude/skills/protocol-close/SKILL.md
```

  Es decir: **48** lineas y **48** codigos distintos en `protocol-close` (no 51/50); **51** lineas en
  los tres archivos (48+2+1), no 54; y el cambio del archivo son **73 lineas anadidas y 54
  eliminadas**, ninguna de las cuales es 51. Ninguna medida —lineas, codigos distintos, ocurrencias
  (67) o lineas del diff— produce 50, 51 ni 54.
- **Por que importa:** el propio commit contiene la salida correcta. `D-113` publica
  `protocol-close/SKILL.md:48` y su prosa dice «`.claude/` (52 lineas en cuatro archivos)»; `T-115` y
  la seccion 1 del informe publican otras cifras para el mismo hecho, sin ninguna orden detras. Es la
  familia de `F-072`, `F-073` y `F-074` —recuento en prosa que contradice a la salida del mismo
  commit—, esta vez en `tasks.md`, que es donde se lee el alcance de la tarea.
- **Que lo corregiria:** una nota fechada en `T-115` que fije las cifras contra la salida de `D-113`
  (48 lineas en `protocol-close`, 51 en los tres archivos, 52 contando los cuatro de `.claude/`), sin
  reescribir el titulo ni el cuerpo (`D-019`); y, si se estima, extender la regla del recuadro del
  Paso 7c —«el total sale de la salida, no de la prosa»— a las cifras con que una tarea describe su
  propio alcance. ⚠️ Es una recomendacion, no una orden.

### F-077 — `A-013` y `D-114` publican `113` en una orden que devuelve `121` sobre su propio commit

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/assumptions.md` (`A-013`) y `_persistence/decisions.md` (`D-114`) |

- **Que se observo:** las dos entradas publican, como evidencia de lo que se comprobo antes de
  elegir, el bloque `$ grep -c 'CLAUDE\.md' _persistence/decisions.md` → `113`. Contra el commit que
  las contiene:

```
$ git show 79e88a2:_persistence/decisions.md | grep -c 'CLAUDE\.md'
121
$ git show 79e88a2:_persistence/assumptions.md | sed -n '779,780p'
$ grep -c 'CLAUDE\.md' _persistence/decisions.md
113
```

- **Por que importa:** es un recuento de ambito global tomado a mitad de la jornada sobre un archivo
  que la propia jornada siguio creciendo — el patron exacto que `D-022` y `D-108` existen para
  evitar. La cifra es el unico respaldo del muestreo agregado con el que `A-013` justifica no haber
  comprobado caso por caso; publicada asi, quien la reejecute obtiene otra cosa y no sabe si el
  supuesto se midio mal o si el archivo cambio. El informe lo declara en sus secciones 4 y 6 y lo
  escala, lo que evita que sea un error oculto — pero el registro sigue conteniendo una cifra que no
  reproduce contra su commit.
- **Que lo corregiria:** una nota fechada en `A-013` y en `D-114` que ancle el recuento («`121`
  sobre `79e88a2`») o que declare explicitamente que el numero es «al momento de escribir la
  entrada» y por que ahi no se puede anclar, sin reescribir la linea original (`D-019`).
  ⚠️ Es una recomendacion, no una orden.

### F-078 — La NOTA DE CIERRE suma 3 en prosa dentro del total, que es lo que el recuadro escrito por esta misma sesion prohibe

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-027.md` §7 (NOTA DE CIERRE) contra `.claude/skills/protocol-close/SKILL.md` (Paso 7c) |

- **Que se observo:** el recuadro que `D-112` escribe en este mismo commit dice que si la nota
  publica un total, «ese total es la suma de los numeros por archivo que el CONTROL devolvio, y nada
  mas», y que lo que el CONTROL no cuenta «se enumera aparte […] nunca se dobla dentro del total». La
  NOTA DE CIERRE de `S-027` §7 publica un CONTROL que devuelve `13` y `8` (21) y a continuacion
  escribe «**24 ordenes ancladas**», sumando en prosa las 3 que el CONTROL no cuenta.

```
$ git show 79e88a2:.claude/skills/protocol-close/SKILL.md | sed -n '1242,1246p'
🚨 **Y si la nota publica un TOTAL, ese total es la suma de los numeros por archivo que el
CONTROL devolvio, y nada mas.** Ni una linea se anade ni se resta en prosa. Toda orden que el Paso
7c-bis ancle y que el CONTROL **no** haya contado —porque la escribio una sesion anterior, o porque
su forma no empieza por `git show <hash>:`— se **enumera aparte, una por una, con su archivo y su
orden literal**; nunca se dobla dentro del total.
$ git show 43151e1:_audit/S-027.md | grep -n 'ordenes ancladas'
442:show <hash>:`) pero que el mismo bloque «Criterio de cierre» autoriza: **24 ordenes ancladas**, con
```

- **Por que importa:** el total publicado (`24`) resulta **cierto** —el barrido de §1g devuelve 16 y
  8 sobre el commit de anclaje—, asi que aqui no hay cifra falsa; lo que falla es la forma que la
  regla acaba de fijar para que deje de haberlas. Es el patron de `F-071`: la primera ejecucion de
  una regla nueva la incumple, y sin cifra falsa que lo delate el incumplimiento pasa desapercibido y
  sienta precedente.
- **Que lo corregiria:** o publicar el total con una orden que lo devuelva entero (`git diff -U0
  43151e1^ 43151e1 -- <archivo> | grep -cE '^\+\$ .*79e88a2'` da 16 y 8 sin sumar nada en prosa), o
  dejar el total en 21 y enumerar las 3 aparte, como el recuadro pide. ⚠️ Es una recomendacion, no
  una orden.

### F-079 — La convencion nueva de `CLAUDE.md` afirma de los seis lo que `_methodology/` no cumple, y su excepcion no cubre el caso

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `CLAUDE.md`, «Registro del proyecto», parrafo nuevo de `D-114` |

- **Que se observo:** el parrafo nuevo dice «**ninguno de los seis cita una entrada del registro**»,
  y su excepcion cubre «el **primer numero de una serie** en una plantilla o en un ejemplo trabajado
  —`N-001`, `I-001`, `FT-001`—». `_methodology/` (uno de los seis) contiene, en el mismo commit,
  codigos que **si** existen como entradas del registro y que **no** son primeros de serie:

```
$ git grep -hoIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2 -- _methodology | sort | uniq -c | sort -rn
      6 T-001
      4 TC-001
      4 N-001
      2 T-008
      2 T-007
      2 T-006
      2 T-005
      2 T-004
      2 T-003
      2 T-002
      2 SC-001
      2 S-001
      2 FT-001
      2 F-001
```

- **Por que importa:** `T-001` a `T-008`, `F-001` y `S-001` existen como entradas reales en
  `tasks.md`, `findings.md` y `progress.md`. La regla queda enunciada como absoluta y el propio
  commit la contradice en catorce ocurrencias; el barrido que `T-112` va a escribir no puede ser
  binario contra un texto asi, que es justo lo que `D-114` descarto como alternativa («deja el
  barrido incapaz de ser binario»). El informe declara la exclusion en su seccion 6 y la deja para
  `T-112`, pero lo que queda escrito en `CLAUDE.md` —el archivo que fija la regla— no la recoge.
- **Que lo corregiria:** ampliar la excepcion del parrafo para que cubra el **ejemplo trabajado
  consecutivo** de la guia de metodo (no solo el primer numero de la serie) y diga como se distingue
  de una cita; o acotar la afirmacion a las cinco areas que hoy cumplen y nombrar `_methodology/`
  como pendiente de `T-112`. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-074` | **Implementado** (`79e88a2`) | La nota fechada esta en `_audit/S-026.md` §7 con las cifras 28/27/2 y su desglose («la `ls-tree` es la primera de esas 14»), verificada contra `89f9f73` (`14`/`14`), y el recuadro del Paso 7c de `protocol-close` prohibe la aritmetica de prosa. Comandos y salidas en §1e. Se cierra pese a `F-078`, que es un incumplimiento posterior de la regla nueva, no un defecto de la correccion |
| `F-075` | **Implementado** (`79e88a2`) | `T-105` vuelve a llevar la linea literal de `S-025` (`grep -c` = `1`), ya no lleva la acotada de `S-026` (`0`), y debajo esta la nota fechada de restauracion (`1`). El diff sobre `_audit/S-026.md` es de solo adiciones (`35 0`), asi que nada se reescribio |

Ningun otro hallazgo quedaba sin cerrar: antes de esta pasada el indice de `_audit/findings.md`
tenia 73 `Implementado` y solo estos dos como `Aceptado — pendiente`.

## 4. Lo que NO se pudo comprobar

- **`A-013` regla por regla.** Se comprobo lo comprobable —que los 50 codigos retirados existen como
  entrada del registro, §1k—, pero no que cada `D-XXX` **nombre la regla concreta** que se limpio:
  eso exige leer las 50 entradas y juzgar contenido, no un barrido. El supuesto sigue vigente y su
  disparador (antes de aplicar `D-114` a un tercer archivo) es el correcto.
- **La tercera via que la seccion 6 propone para el Paso 2e** —archivos que ni `session-closer` ni el
  7c-bis pueden tocar— es una decision de diseno, no un hecho contrastable contra el commit: se
  responde en la seccion 5 como recomendacion, no como hallazgo.

## 5. Recomendaciones sin hallazgo

- **`DT-005` y `DT-006` repiten el mismo patron** —un defecto detectado por el Paso 2e en un archivo
  que el cierre no puede escribir— y los dos acaban en `techdebt.md` sin momento de pago. La
  respuesta util a la pregunta de la seccion 6 quiza no sea una via nueva sino un disparador: que esa
  deuda se pague en la primera sesion en que `manager` edite ese archivo por otro motivo. Hoy nadie
  tiene ese momento asignado.
- **El barrido de fuga de este protocolo sigue siendo mas estrecho que el de `protocol-close`**
  (`T-038`, abierta desde hace varias sesiones). Esta auditoria ha corrido los dos, como las
  anteriores; mientras la tarea no se tome, cada pasada depende de que el auditor se acuerde.
- **`T-115` se titula «tres archivos» y el commit toca cuatro de `.claude/`** (entra
  `session-starter.md` por el `H-nn`). La nota lo explica como genericizacion adicional, asi que no
  es una afirmacion falsa; aun asi, el titulo de la tarea y su alcance real no coinciden, y el titulo
  es lo que se lee desde el indice.
