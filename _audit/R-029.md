# Auditoria R-029 — sobre S-029

| Campo | Valor |
|---|---|
| Auditoria | R-029 |
| Informe auditado | `_audit/S-029.md` |
| Commit auditado | `c1fb41e` |
| Fecha | 2026-09-08 |
| Veredicto | Con hallazgos (4) |

📌 **Sobre que commit se audita.** `git log -1 --format=%h -- _audit/S-029.md` devuelve `c5f6365`,
que es el **commit de anclaje** (Pasos 7c/7c-bis, 3 archivos). La cabecera del informe escribe el
hash literal `c1fb41e`, y es ese el que se audita: su `--stat` da 11 archivos y coincide con la
lista que la seccion 1 enumera.

```
$ git log -1 --format=%h -- _audit/S-029.md
c5f6365
$ git show c5f6365:_audit/S-029.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `c1fb41e` |
$ git show --stat --name-only --format= c1fb41e | grep -c .
11
$ git show --stat --name-only --format= c5f6365 | grep -c .
3
```

---

## 1. Que se comprobo

### 1.1 Indices contra detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show c1fb41e:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show c1fb41e:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete archivos, sin diferencia.

### 1.2 Carpetas del arbol contra las declaradas — las dos diferencias conocidas, con razon escrita

```
$ diff <(git ls-tree -d --name-only c1fb41e | sed 's|$|/|' | sort) \
       <(git show c1fb41e:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos tienen motivo escrito en `project.md`, seccion «Carpetas propias»: `temporal/` excluida por
`.gitignore`, `010_prototype/` declarada por adelantado por `D-061`. No es hallazgo.

### 1.3 Fuga de datos propios — cero lineas, en el ambito del protocolo y en el ampliado

```
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" c1fb41e -- .claude CLAUDE.md _phases _methodology
(sin salida, rc=1)
$ git grep -nE "RaindomAI|RaidomAI|Proyectos_TripleS|github\.com" c1fb41e -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida, rc=1)
```

El segundo amplia el ambito a los **seis** de los que `CLAUDE.md` afirma la propiedad (`T-038` sigue
abierta para igualar los dos ambitos). Tambien limpio.

### 1.4 Codigos instanciados en los seis agnosticos — hay uno suelto (ver `F-085`)

```
$ git grep -hIoE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' c1fb41e -- .claude CLAUDE.md _phases _methodology _templates _workflow | sort | uniq -c
      2 F-001
     10 FT-001
      2 FT-002
      1 FT-003
      1 FT-004
      2 H-1
      6 I-001
      3 I-002
      3 I-003
     13 N-001
      6 N-002
      2 N-003
      2 S-001
      7 SC-001
      3 SC-002
      2 SC-003
      1 SC-007
      6 T-001
      2 T-002
      2 T-003
      2 T-004
      2 T-005
      2 T-006
      2 T-007
      2 T-008
      4 TC-001
```

### 1.5 Recuentos por archivo de la seccion 1 — todos reproducen

```
$ for f in $(git diff --name-only c1fb41e^ c1fb41e); do echo "$f: $(git diff c1fb41e^ c1fb41e -- "$f" | grep -c '^@@')"; done
.claude/skills/protocol-close/SKILL.md: 2
CLAUDE.md: 1
_audit/S-027.md: 1
_audit/S-029.md: 1
_audit/findings.md: 8
_audit/index.md: 1
_persistence/assumptions.md: 3
_persistence/decisions.md: 4
_persistence/lessons.md: 4
_persistence/progress.md: 3
_persistence/tasks.md: 2
```

Coincide con lo que el informe publica archivo por archivo (2, 1, 1, 8, 4, 4, 2, 3). Es la primera
seccion 1 escrita bajo `D-120`, y la cumple.

Las entradas nacidas tambien cuadran con lo declarado:

```
$ for f in tasks decisions assumptions lessons; do echo "-- $f"; git diff -U0 c1fb41e^ c1fb41e -- _persistence/$f.md | grep -cE '^\+### '; done
-- tasks
14
-- decisions
6
-- assumptions
2
-- lessons
2
```

### 1.6 Las correcciones por nota fechada (`T-116` a `T-121`) — todas reproducen

```
$ git diff -U0 79e88a2 43151e1 -- _persistence | grep -cE '^\+\$ .*79e88a2'
24
$ for f in $(git diff --name-only 79e88a2 43151e1 -- _persistence); do n=$(git diff -U0 79e88a2 43151e1 -- "$f" | grep -cE '^\+\$ .*79e88a2'); echo "$f: $n"; done
_persistence/decisions.md: 16
_persistence/tasks.md: 8
$ git grep -c 'CLAUDE\.md' 79e88a2 -- _persistence/decisions.md
79e88a2:_persistence/decisions.md:121
$ git grep -n '_workflow' acb3359 -- _phases/ | wc -l
20
$ git grep -c '_workflow' acb3359 -- _phases/
acb3359:_phases/005_discovery.md:1
acb3359:_phases/010_prototype.md:2
acb3359:_phases/020_baseline.md:2
acb3359:_phases/025_wslt.md:2
acb3359:_phases/030_growth.md:10
acb3359:_phases/040_evol.md:3
$ git grep -cIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2^ -- .claude
79e88a2^:.claude/agents/session-closer.md:1
79e88a2^:.claude/agents/session-starter.md:1
79e88a2^:.claude/skills/protocol-close/SKILL.md:48
79e88a2^:.claude/skills/protocol-start/SKILL.md:2
$ git grep -cIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2^ -- .claude | awk -F: '{s+=$NF} END {print s}'
52
$ git grep -ohIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2^ -- .claude/skills/protocol-close/SKILL.md | sort -u | wc -l
48
```

`24`, `16/8`, `121`, `20`, `1+2+2+2+10+3=20`, `52` y `48`: los numeros publicados salen de sus
ordenes. Y la evidencia de `D-118` devuelve las catorce lineas que declara:

```
$ git grep -hIoE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' d23e5a6 -- _methodology | sort | uniq -c
      2 F-001
      2 FT-001
      4 N-001
      2 S-001
      2 SC-001
      6 T-001
      2 T-002
      2 T-003
      2 T-004
      2 T-005
      2 T-006
      2 T-007
      2 T-008
      4 TC-001
```

### 1.7 Las doce ordenes ancladas por el Paso 7c-bis — las doce reproducen

```
$ git show c1fb41e:CLAUDE.md | grep -c 'serie consecutiva de un ejemplo trabajado'
1
$ git grep -hIoE '\bT-[0-9]{3}\b' c1fb41e -- _methodology | sort -u | tr '\n' ' '
T-001 T-002 T-003 T-004 T-005 T-006 T-007 T-008
$ git show c1fb41e:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
1
$ git show c1fb41e:.claude/skills/protocol-close/SKILL.md | grep -c 'recuento sin la orden que lo devuelve'
1
$ git show c1fb41e:_persistence/tasks.md | grep -cE '^### T-[0-9]+ - Decidir quien escribe y commitea en el repositorio de lecciones globales'
1
$ git show c1fb41e:_persistence/assumptions.md | grep -c 'Nota del 2026-09-08 (`F-077`)'
1
$ git show c1fb41e:_persistence/decisions.md | grep -c 'Nota del 2026-09-08 (`F-077`)'
1
$ git show c1fb41e:_audit/S-027.md | grep -c 'Nota del 2026-09-08 (`F-078`)'
1
$ git show c1fb41e:_persistence/decisions.md | grep -c 'Nota del 2026-09-08 (`F-080`)'
1
$ git show c1fb41e:_persistence/decisions.md | grep -c 'la tercera, cero lineas'
1
```

### 1.8 El anclaje NO borro prosa esta vez — `F-081` de verdad corregido

Las trece lineas que el commit de anclaje elimina estan **todas dentro de bloques de codigo** (son
las formas con `<hash>` sustituidas por su version anclada). Cero lineas de prosa perdidas:

```
$ git diff -U0 c1fb41e c5f6365 -- _persistence | grep -cE '^-[^-]'
13
$ git diff -U0 c1fb41e c5f6365 -- _persistence | grep -E '^-[^-]'
-$ git show <hash>:CLAUDE.md | grep -c 'serie consecutiva de un ejemplo trabajado'
-$ git grep -hIoE '\bT-[0-9]{3}\b' <hash> -- _methodology | sort -u | tr '\n' ' '
-$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
-$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'recuento sin la orden que lo devuelve'
-$ git show <hash>:_persistence/tasks.md | grep -cE '^### T-[0-9]+ - Decidir quien escribe y commitea en el repositorio de lecciones globales'
-$ git show <hash>:_persistence/assumptions.md | grep -c 'Nota del 2026-09-08 (`F-077`)'
-$ git show <hash>:_persistence/decisions.md | grep -c 'Nota del 2026-09-08 (`F-077`)'
-$ git show <hash>:_audit/S-027.md | grep -c 'Nota del 2026-09-08 (`F-078`)'
-$ git show <hash>:CLAUDE.md | grep -c 'serie consecutiva de un ejemplo trabajado'
-$ git show <hash>:_persistence/decisions.md | grep -c 'Nota del 2026-09-08 (`F-080`)'
-$ git show <hash>:_persistence/decisions.md | grep -c 'la tercera, cero lineas'
-$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
-$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'recuento sin la orden que lo devuelve'
```

### 1.9 CENSO y CONTROL de la NOTA DE CIERRE — reproducen exactos

```
$ for f in $(git ls-tree -r --name-only c1fb41e _persistence _audit | grep -v '_audit/S-029.md'); do n=$(git show c1fb41e:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-025.md: 2
_audit/R-026.md: 7
_audit/R-027.md: 1
_audit/S-017.md: 1
_audit/S-024.md: 1
_audit/S-025.md: 17
_audit/S-026.md: 8
_audit/S-027.md: 17
_persistence/assumptions.md: 9
_persistence/decisions.md: 20
_persistence/tasks.md: 29
$ for f in $(git diff --name-only c1fb41e^ c1fb41e -- _persistence _audit ":(exclude)_audit/S-029.md"); do n=$(git diff -U0 c1fb41e^ c1fb41e -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 5
_persistence/tasks.md: 12
```

Las once filas del CENSO y las dos del CONTROL coinciden con lo publicado. El desglose «12 de 17
anclados, 5 + 7 pendientes de un commit futuro» tambien cuadra:

```
$ git diff -U0 c1fb41e c5f6365 -- _persistence | grep -cE '^\+\$ git show c1fb41e:'
12
```

### 1.10 Fecha del informe contra la del commit (Paso 7d) — coinciden

```
$ git log -1 --format=%ad --date=short c1fb41e
2026-09-08
```

El informe y la fila del tablero dicen `2026-09-08`.

### 1.11 Estados de `findings.md` — los siete pasaron a `Aceptado — pendiente`, ninguno a `Implementado`

```
$ for n in 076 077 078 079 080 081 082; do echo "-- F-$n"; git show c1fb41e:_audit/findings.md | sed -n "/^### F-$n/,/^---/p" | grep -E '^\| (Estado|Registrado en)'; done
-- F-076
| Estado | Aceptado — pendiente |
| Registrado en | `T-116` |
-- F-077
| Estado | Aceptado — pendiente |
| Registrado en | `T-117` |
-- F-078
| Estado | Aceptado — pendiente |
| Registrado en | `T-118` |
-- F-079
| Estado | Aceptado — pendiente |
| Registrado en | `T-119` |
-- F-080
| Estado | Aceptado — pendiente |
| Registrado en | `T-120` |
-- F-081
| Estado | Aceptado — pendiente |
| Registrado en | `T-121` |
-- F-082
| Estado | Aceptado — pendiente |
| Registrado en | `T-122` |
```

El cierre respeto la frontera: no se atribuyo ningun `Implementado`.

### 1.12 Recuento de ordenes distintas del Paso 2d — la lista reproduce, el recuento no (ver `F-083`)

### 1.13 Barrido de caracteres de control del Paso 2e — no reproduce en dos filas (ver `F-084`)

---

## 2. Hallazgos

### F-083 - El recuento de ordenes distintas del Paso 2d publica `35` donde la orden devuelve `27`, y su bloque `uniq -d` publica 3 lineas donde devuelve 10

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-029.md` seccion 7 |

- **Que se observo:** el informe escribe «Recuento de LINEAS devueltas: **37**. Recuento de ordenes
  distintas (`sort -u | wc -l`): **35** (las lineas 3/4 y 17/20 y 18/21 se repiten: `sort -u` sobre
  la misma lista da 35, verificado)», y publica un bloque `uniq -d` con **tres** lineas. La lista de
  37 reproduce exacta; los dos recuentos derivados, no.

  ```
  $ git diff -U0 c1fb41e^ c1fb41e -- _persistence _audit ':(exclude)_audit/S-029.md' | grep -E '^\+\$ ' | sed 's/^+//' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
  37
  $ git diff -U0 c1fb41e^ c1fb41e -- _persistence _audit ':(exclude)_audit/S-029.md' | grep -E '^\+\$ ' | sed 's/^+//' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
  27
  $ git diff -U0 c1fb41e^ c1fb41e -- _persistence _audit ':(exclude)_audit/S-029.md' | grep -E '^\+\$ ' | sed 's/^+//' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
  $ git grep -c 'CLAUDE\.md' 79e88a2 -- _persistence/decisions.md
  $ git grep -cIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2^ -- .claude | awk -F: '{s+=$NF} END {print s}'
  $ git grep -ohIE '\b(T|D|F|L|A|C|DT|S|R|N|I|H|FT|SC|LG|TC)-[0-9]+\b' 79e88a2^ -- .claude/skills/protocol-close/SKILL.md | sort -u | wc -l
  $ git ls-tree --name-only <hash> .claude/agents/ | grep -c 'phase_exit_auditor'
  $ git ls-tree --name-only <hash> _templates/ | grep -c 'phase_exit'
  $ git ls-tree -r --name-only <hash> .claude/skills/protocol-phase-exit/ | grep -c 'SKILL.md'
  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'recuento sin la orden que lo devuelve'
  $ git show <hash>:CLAUDE.md | grep -c 'serie consecutiva de un ejemplo trabajado'
  $ git show <hash>:_phases/005_discovery.md | grep -c 'acta de cierre'
  ```

  Son **diez** pares repetidos, no tres: a los tres que el informe nombra (lineas 3/4, 18/20, 19/21)
  se suman siete que llevan `<hash>` y que el informe deja fuera (8/25, 10/28, 11/29, 12/30, 13/35,
  14/32, 15/33). `37 − 10 = 27`.
- **Por que importa:** es la tercera vez que este mismo recuento sale falso. `F-068` («afirma que las
  41 ordenes no se repiten; se repiten diez») y `F-072` («publica 27 ordenes distintas y son 22»)
  estan los dos cerrados como `Implementado`, y la conducta no ha cambiado. Ademas el informe declara
  el numero «verificado» y publica una salida de `uniq -d` truncada: eso ya no es un numero sin orden
  detras, es una salida **presentada como cruda** que no es la que la orden devuelve, que es lo que
  `CLAUDE.md` describe como «hay evidencia falsa».
- **Que lo corregiria:** que las salidas de `sort -u | wc -l` y de `uniq -d` se peguen tal cual salen
  en vez de resumirse, o que el Paso 2d deje de pedir el recuento accesorio de distintas si nadie lo
  usa. El informe commiteado no se reescribe: la correccion es una nota fechada con la orden y su
  salida entera, mas una regla hacia adelante.

### F-084 - La tabla del Paso 2e publica `0` caracteres de control para `_audit/findings.md` y `_persistence/tasks.md`, donde el barrido devuelve `1` y `5`

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-029.md` seccion 8 |

- **Que se observo:** la seccion 8 publica un primer barrido con solo dos filas
  (`_persistence/assumptions.md: 4`, `_persistence/decisions.md: 7`) y una tabla de separacion
  nuevo/heredado que escribe `_audit/findings.md head=0 staged=0` y
  `_persistence/tasks.md head=0 staged=0`. Corrido sobre el mismo estado:

  ```
  $ for f in $(git diff --name-only c1fb41e^ c1fb41e); do echo "$f head=$(git show c1fb41e^:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') commit=$(git show c1fb41e:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
  .claude/skills/protocol-close/SKILL.md head=0 commit=0
  CLAUDE.md head=0 commit=0
  _audit/S-027.md head=0 commit=0
  _audit/S-029.md head=0 commit=0
  _audit/findings.md head=1 commit=1
  _audit/index.md head=0 commit=0
  _persistence/assumptions.md head=4 commit=4
  _persistence/decisions.md head=7 commit=7
  _persistence/lessons.md head=0 commit=0
  _persistence/progress.md head=0 commit=0
  _persistence/tasks.md head=5 commit=5
  ```

  Las seis lineas que la tabla del informe no ve (`^H` es `0x08` mostrado con `cat -v`):

  ```
  $ git show c1fb41e:_audit/findings.md | grep -n $'[\x01-\x08\x0b\x0c\x0e-\x1f]' | cat -v | cut -c1-120
  2749:+$ grep -rnoE "^H(FT|SC|VS|TC|ADR)-[0-9]{3}^H" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
  $ git show c1fb41e:_persistence/tasks.md | grep -n $'[\x01-\x08\x0b\x0c\x0e-\x1f]' | cat -v | cut -c1-120
  1297:$ git grep -noE "^HI-[0-9]{3}^H" 7025a05 -- _persistence/ _audit/ project.md | wc -l
  2016:$ grep -nE '^H(N|T|D|A|C)-[0-9]{3}^H|^HF-[0-9]{3}^H|^HL-[0-9]+^H|^HS-[0-9]+^H|^HR-[0-9]+^H' _phases/010_prototype.md ; echo "exit=$?"
  2072:$ grep -nEi '^Hhoy^H|todavia no existen|no ha empezado|aun no' _phases/010_prototype.md ; echo "exit=$?"
  3119:  a mas corto, con `(^|[^A-Za-z])` en vez de `^H` y `[0-9]{2,3}` en vez de `[0-9]{3}`. Nota fechada
  3137:$ PAT=$( { sed -n '/^## Codigos/,/^---/p' project.md | grep -oE '^\| `[A-Z]+-[A-Za-z0-9]+`'; …
  ```
- **Por que importa:** la conclusion de fondo del informe —«esta sesion no anadio ninguna linea nueva
  con caracteres de control»— **es cierta**: `head` y `commit` coinciden en los once archivos. Pero la
  evidencia con la que la sostiene es falsa en dos filas de once. El Paso 2e existe justamente para
  detectar el `\b` de una regex convertido en `0x08`, y un barrido que devuelve `0` donde hay seis
  ocurrencias deja de servir para eso: la proxima vez que aparezca una **nueva** en `tasks.md` o en
  `findings.md`, este control no la vera. Choca ademas con lo que el propio registro dice: `DT-003`
  a `DT-006` tienen registrado el problema, y este informe afirma que en dos de esos archivos no
  existe.
- **Que lo corregiria:** determinar por que la orden devolvio `0` en esos dos archivos —clase de
  caracteres, `grep` del entorno o ambito— y fijar en `protocol-close` una forma del barrido que
  reproduzca. El numero publicado tiene que salir de la orden que se pega, no de otra.

### F-085 - La regla que este commit escribe en `CLAUDE.md` («la serie empieza por el primero y no se salta ninguno») la incumple el propio commit, en `_templates/`

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `CLAUDE.md` (parrafo nuevo de `D-118`) y `_templates/020_baseline/045_traceability.md:200` |

- **Que se observo:** `c1fb41e` anade a `CLAUDE.md`: «⚠️ **Y la serie tiene que empezar por el primero
  y no saltarse ninguno.** Un ejemplo que arranca por la mitad —o que elige numeros sueltos— ya no se
  distingue de una cita». El mismo commit contiene un numero suelto en el ambito del que la regla
  habla:

  ```
  $ git grep -hIoE '\b(FT|SC)-[0-9]+\b' c1fb41e -- .claude CLAUDE.md _phases _methodology _templates _workflow | sort | uniq -c
       10 FT-001
        2 FT-002
        1 FT-003
        1 FT-004
        7 SC-001
        3 SC-002
        2 SC-003
        1 SC-007
  $ git grep -nE '\bSC-007\b' c1fb41e -- _templates
  c1fb41e:_templates/020_baseline/045_traceability.md:200:| `SC-007` | `FT-004` Exportar historico mensual | — | **NO — feature huerfana** |
  ```

  La serie `SC-` va `001, 002, 003, 007`: hay un salto de tres, y `SC-004` a `SC-006` no aparecen en
  ningun archivo de los seis.
- **Por que importa:** la evidencia con la que `D-118` justifica la ampliacion se corrio **solo sobre
  `_methodology`** (`git grep … d23e5a6 -- _methodology`), mientras que la afirmacion que respalda es
  sobre los **seis**. Ampliar el ambito de esa misma orden a los seis muestra el caso que la regla
  nueva no cubre. Es la forma exacta de `F-079` —una regla enunciada como absoluta que el propio
  commit contradice—, ahora en `_templates/` en vez de en `_methodology/`, con evidencia distinta. El
  barrido binario que `T-112` tiene que escribir para el Paso 1b seguira sin poder ser binario.
- **Que lo corregiria:** o completar la serie del ejemplo de trazabilidad para que `SC-007` deje de
  ser un numero suelto, o —si el salto es deliberado, porque el ejemplo ilustra una feature huerfana
  y el hueco es parte de lo que ensena— ampliar la excepcion de `CLAUDE.md` para que cubra el numero
  suelto cuando el ejemplo lo necesita para significar algo, con su criterio escrito. Cual de las dos,
  lo decide `manager`.

### F-086 - El CONTROL DE PROSA BORRADA nace obligatorio y no deja ninguna salida en el repositorio: su primera ejecucion no es auditable

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `.claude/skills/protocol-close/SKILL.md`, Paso 7c-bis (`D-119`) |

- **Que se observo:** el control se declara «obligatorio, antes de commitear el anclaje» y su tabla de
  resultados manda el fallo «a **Sin resolver**», que es una seccion del **reporte en pantalla** (Paso
  8), no de ningun archivo. No hay rastro de su ejecucion ni en el commit de anclaje ni en el informe:

  ```
  $ git grep -n 'PROSA BORRADA' c5f6365 -- _audit _persistence
  c5f6365:_audit/S-029.md:24:  … `protocol-close` gana el CONTROL DE PROSA BORRADA en el Paso 7c-bis |
  c5f6365:_audit/S-029.md:61:  «CONTROL DE PROSA BORRADA (obligatorio, antes de commitear el anclaje)» al Paso 7c-bis, con su
  c5f6365:_audit/S-029.md:160:cambia la conducta porque depende de que quien ejecuta lea. Nace el CONTROL DE PROSA BORRADA:
  c5f6365:_audit/S-029.md:239:- **Que el CONTROL DE PROSA BORRADA (`D-119`) este descrito de forma que de verdad se pueda ejecutar
  c5f6365:_audit/S-029.md:272:10  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
  c5f6365:_audit/S-029.md:290:28  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
  c5f6365:_persistence/decisions.md:7696:$ git show c1fb41e:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
  c5f6365:_persistence/progress.md:83:  … gana un control mecanico —CONTROL DE PROSA BORRADA— …
  c5f6365:_persistence/progress.md:108:  gana un **control mecanico** —CONTROL DE PROSA BORRADA— que compara la prosa fuera de los bloques
  c5f6365:_persistence/progress.md:168:`T-120` reproducen sus ordenes, que el CONTROL DE PROSA BORRADA quedo descrito en `protocol-close` y
  c5f6365:_persistence/progress.md:1310:  CONTROL DE PROSA BORRADA: compara la prosa fuera de bloques de codigo antes y despues del anclaje y
  c5f6365:_persistence/tasks.md:5235:$ git show c1fb41e:.claude/skills/protocol-close/SKILL.md | grep -c 'CONTROL DE PROSA BORRADA'
  ```

  Las once menciones son **descriptivas** —que el control existe, que se escribio, que `grep -c`
  devuelve `1`—; ninguna publica la salida del control corrido.
- **Por que importa:** los otros dos controles del mismo tramo —CENSO y CONTROL de la NOTA DE
  CIERRE— si publican su orden y su salida en la seccion 7 del informe, y por eso esta auditoria ha
  podido reejecutarlos y confirmarlos. Este no. Se ha comprobado a mano que esta vez **no se borro
  prosa** (§1.8), pero eso lo comprobo la auditoria, no el control: el control nacio para dejar de
  depender de que alguien lea, y sin salida registrada vuelve a depender de que alguien lo corra.
  Choca ademas con `PI-5` de `CLAUDE.md`, que para un producto de documentacion exige «la orden
  ejecutada literal y su salida cruda».
- **Que lo corregiria:** que el Paso 7c-bis publique la salida del control dentro de la NOTA DE CIERRE
  del informe, junto al CENSO y al CONTROL, aunque salga limpia. Gravedad `Baja` porque el control
  esta bien escrito y esta vez no habia nada que detectar.

---

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-076` | **Implementado** (`c1fb41e`) | La nota fechada esta bajo `T-115` en `_persistence/tasks.md:5024` y sus tres ordenes reproducen: cuatro archivos, suma `52`, `48` codigos distintos en `protocol-close`. `T-115` no se reescribio |
| `F-077` | **Implementado** (`c1fb41e`) | Notas del 2026-09-08 en `A-013` y `D-114` (`grep -c` → `1` en las dos) y `git grep -c 'CLAUDE\.md' 79e88a2 -- _persistence/decisions.md` → `121`, el numero que publican |
| `F-078` | **Implementado** (`c1fb41e`) | Nota en la seccion 7 de `_audit/S-027.md` con el total salido de una sola orden: `24`, y el desglose `16` + `8`. Los tres reproducen |
| `F-079` | **Implementado** (`c1fb41e`) | La excepcion de `CLAUDE.md` pasa a cubrir la serie consecutiva del ejemplo trabajado y anade el criterio de distincion, que es lo que el hallazgo pedia; su alcance —`_methodology/`: `T-001` a `T-008`, `F-001`, `S-001`— queda cubierto. **El caso nuevo de `_templates/` va como `F-085`, no como reapertura** |
| `F-080` | **Implementado** (`c1fb41e`) | Nota del 2026-09-08 en `D-117` (`grep -c` → `1`) con `git grep -n '_workflow' acb3359 -- _phases/ \| wc -l` → `20` y el desglose 1+2+2+2+10+3 |
| `F-081` | **Implementado** (`c1fb41e` y `c5f6365`) | Dos mitades verificadas: la prosa perdida esta restaurada (`grep -c 'la tercera, cero lineas'` → `1`) y el CONTROL nuevo esta escrito (`grep -c 'CONTROL DE PROSA BORRADA'` → `1`). Y sobre todo, el anclaje de esta sesion **no borro ni una linea de prosa**: las trece eliminadas estan todas dentro de bloques de codigo (§1.8) |
| `F-082` | **Implementado** (`c1fb41e`) | `protocol-close` gana la subseccion «Un recuento sin la orden que lo devuelve no entra en la seccion 1» (`grep -c` → `1`), y la seccion 1 de `S-029` ya la cumple: los ocho recuentos por archivo llevan su orden y todos reproducen (§1.5) |

Ningun otro `F-NNN` quedaba abierto: de los 82 previos, 75 estaban `Implementado` y estos siete
`Aceptado — pendiente`.

---

## 4. Lo que NO se pudo comprobar

Ninguno de los controles quedo `SIN COMPROBAR`. Los tres mecanicos —indices, carpetas y fuga—
corrieron sobre `c1fb41e` y devolvieron salida.

⚠️ Una salvedad de alcance, que no es un control fallido: el barrido del Paso 2e (`F-084`) devuelve
en esta maquina numeros distintos de los publicados. No se ha podido determinar si la diferencia
viene del `grep` del entorno del cierre o de la orden escrita; lo que si es comprobable, y es lo que
el hallazgo afirma, es que **la salida publicada no reproduce**.

---

## 5. Recomendaciones sin hallazgo

- **El ambito del barrido de fuga de `protocol-audit` sigue siendo cuatro y el de `CLAUDE.md` es
  seis.** `T-038` ya esta abierta para igualarlos. Esta auditoria corrio los dos y los dos salen
  limpios, pero mientras la skill no se toque, cada pasada depende de que el auditor amplie el ambito
  por su cuenta.
- **El criterio de distincion de `D-118` funciona como criterio de lectura, pero no da un patron
  ejecutable, y `T-112` necesita uno.** Una forma barata es derivar el barrido de la propia
  consecutividad: una serie completa desde el `001` se acepta, cualquier hueco se imprime. Es mecanico
  y capta justo el caso de `F-085`.
- **La seccion 7 del informe mezcla tres cosas** —la lista del Paso 2d, la reejecucion una a una, y la
  NOTA DE CIERRE con CENSO y CONTROL— y es la mas larga con diferencia. Los dos hallazgos numericos de
  esta pasada salen los dos de ahi. No es un defecto, pero es donde el informe se hace mas dificil de
  contrastar.
- **El diseno del acta de cierre de etapa (`D-121` a `D-123`) no muestra huecos ademas del declarado
  (`T-127`)**, dentro de lo que un commit puede mostrar: el acta todavia no tiene plantilla, agente ni
  skill, asi que lo unico auditable hoy son las tres decisiones y sus siete tareas, y las siete estan
  abiertas con criterio de cierre escrito. La pregunta de la seccion 6 sobre la frontera entre
  «montado» y «certificando» en la casilla 2 de `_phases/000_preproject.md` no se puede responder
  contra este commit: `T-125` es justamente la que la toca.
