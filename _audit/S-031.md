# Informe de auditoria — S-031

| Campo | Valor |
|---|---|
| Sesion | S-031 |
| Fecha | 2026-09-10 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | <SE RELLENA EN EL PASO 7c> |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`b32193d`, `auditoria R-030 sobre S-030 (9b3f9ee)`), sobre el estado que
`9b3f9ee` (`S-030`) dejo.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-087 — la seccion 4 de `S-030.md` afirma «trece» supuestos abiertos donde la orden devuelve quince | Implementado | `T-136` (nota fechada en `_audit/S-030.md`) y `T-137` (`D-130`, plantilla de la seccion 4 exige la salida cruda), en este commit |
| F-088 — la NOTA DE CIERRE de `S-030.md` afirma «las 6 restantes» con un desglose que suma cinco | Implementado | `T-138` (nota fechada en `_audit/S-030.md`) y `T-139` (`D-131`, la NOTA DE CIERRE deriva sus desgloses con una orden), en este commit |
| F-089 — la plantilla `005_project.md` no recibe la actualizacion de la fila `_audit/` que si recibe `project.md` | Implementado | `T-140`, en este commit |
| F-090 — el Paso 2e mide el area de staging previa al commit, no «los archivos que el commit toca» | Implementado | `T-141` (`D-132`, segunda pasada anclada del Paso 2e), en este commit |

## 1. Que se hizo

La lista anclada (`git show --stat --name-only --format= <hash>`) se pega en la NOTA DE CIERRE del
Paso 7c. Mientras el commit no existe, sale del area de staging:

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
.claude/skills/protocol-harvest/SKILL.md
CLAUDE.md
_audit/S-030.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_phases/000_preproject.md
_phases/005_discovery.md
_templates/000_preproject/005_project.md
```

Esta lista tiene 14 archivos y **no incluye este propio informe** (`_audit/S-031.md`), que entra
al `git add` despues de escribirse: la lista anclada de la NOTA DE CIERRE tendra 15.

Que cambio en cada uno, con codigos y con las entradas de registro que **nacen** y las que **se
editan** (derivado con `git diff --cached -U0 -- <archivo>` y contando sus hunks; los que caen sobre
la posicion de una entrada existente **por insercion al final de otra**, no por edicion de su
cuerpo, se anotan aparte):

- **`.claude/skills/protocol-close/SKILL.md`** (4 hunks `-U0`) — nace la SEGUNDA PASADA anclada del
  Paso 2e (`D-132`); la seccion 4 del informe exige salida cruda en vez de recuento tecleado
  (`D-130`); la NOTA DE CIERRE exige desgloses derivados (`D-131`); la plantilla de la seccion 8
  remite a la segunda pasada.
- **`.claude/skills/protocol-harvest/SKILL.md`** — nace entero: la skill de la cosecha de lecciones,
  de uso exclusivo de `manager`, con nueve pasos y la puerta como Paso 5 (`D-133`, `T-142`).
- **`CLAUDE.md`** (1 hunk) — nace la seccion que nombra la skill `protocol-harvest` y su puerta, en
  la seccion de lecciones globales.
- **`_audit/S-030.md`** (2 hunks) — dos notas fechadas del 2026-09-10 (`F-087`, `F-088`), sin
  reescribir su prosa anterior.
- **`_audit/findings.md`** (5 hunks: 1 en el indice, 4 en el detalle) — `F-087`, `F-088`, `F-089`,
  `F-090` pasan de `Abierto` a `Aceptado — pendiente`, citando `T-136`/`T-137`, `T-138`/`T-139`,
  `T-140`, `T-141`.
- **`_audit/index.md`** (1 hunk) — nace la fila `S-031` (`Pendiente` en las tres ultimas columnas;
  la escribe este cierre, no la auditoria).
- **`_persistence/assumptions.md`** (3 hunks: 1 en el indice, 2 en el detalle) — `A-017` pasa de
  `Abierto` a `Confirmado`, con la nota de confirmacion del 2026-09-10 (`T-129`). No nace ninguna
  entrada.
- **`_persistence/decisions.md`** (2 hunks: 1 en el indice, el resto es el cuerpo nuevo al final)
  — nacen `D-130`, `D-131`, `D-132`, `D-133`, `D-134`. No edita ninguna entrada existente (el hunk
  que el barrido posicional atribuye a `D-129` es la insercion al final de esa entrada, no una
  edicion de su cuerpo).
- **`_persistence/lessons.md`** (3 hunks: 1 en el indice, 1 nota de reincidencia sobre `L-010`, el
  resto es el cuerpo nuevo al final) — nacen `L-045` y `L-046`; se anade una **nota de reincidencia
  del 2026-09-10** a `L-010` (el mismo defecto —un criterio de cierre que se incluye a si mismo—
  volvio a ocurrir, esta vez al escribir el criterio de cierre de `T-129`/`D-134`, y se detecto antes
  de publicarlo). El hunk que el barrido posicional atribuye a `L-044` es insercion al final, no
  edicion de su cuerpo.
- **`_persistence/progress.md`** (10 hunks) — se sobrescriben «Estado general», «Ultimo realizado» y
  «Siguiente paso»; nace la fila de indice y la entrada de bitacora `S-031`.
- **`_persistence/tasks.md`** (13 hunks: 3 en el indice/apertura, 9 en el cuerpo nuevo al final, mas
  ediciones reales sobre `T-126`, `T-127` y `T-129`) — nacen `T-136` a `T-144`; se editan `T-126`,
  `T-127` y `T-129` (estado `No implementada` → `Implementada`, mas la seccion «Como quedo (S-031)»
  y su bloque de verificacion anclado a `<hash>`). El hunk que el barrido posicional atribuye a
  `T-135` es insercion al final, no edicion de su cuerpo.
- **`_phases/000_preproject.md`** (1 hunk) — la casilla 10 de la condicion de salida nombra quien
  ejecuta la cosecha (`manager`, con `protocol-harvest`) y exige que corra antes de la firma del
  patrocinador.
- **`_phases/005_discovery.md`** (2 hunks) — nace la tercera entrada obligatoria: el acta de cierre
  de la etapa anterior, levantada y firmada (`T-126`).
- **`_templates/000_preproject/005_project.md`** (1 hunk) — la fila `_audit/` nombra tambien las
  actas de cierre de etapa, igualando a `project.md` (`F-089`, `T-140`).

## 2. Que NO se hizo, y por que

Derivado de `sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"` (salida
completa, sin filtrar):

```
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject) | Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003) | Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
| [T-037](#t-037---escribir-el-inventario-de-acciones-irreversibles-del-proyecto-lg-38) | Escribir el inventario de acciones irreversibles del proyecto (`LG-38`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-038](#t-038---igualar-el-barrido-de-fuga-de-protocol-audit-con-el-de-protocol-close) | Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close` | No implementada | Media | No bloqueante | `000_preproject` |
| [T-039](#t-039---normalizar-a-no-implementada-el-estado-pendiente-de-t-037-y-t-038-f-029) | Normalizar a `No implementada` el estado `Pendiente` de `T-037` y `T-038` (`F-029`) | Implementada | Media | No bloqueante | `000_preproject` |
| [T-112](#t-112---anadir-al-paso-1b-el-barrido-de-codigos-instanciados-d-113) | Anadir al Paso 1b el barrido de codigos instanciados (`D-113`) | No implementada | Media | No bloqueante | `000_preproject` |
| [T-128](#t-128---hacer-la-cosecha-de-las-lecciones-de-la-etapa-preparatoria-d-123) | Hacer la cosecha de las lecciones de la etapa preparatoria (`D-123`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-143](#t-143---hacer-que-el-arranque-mire-tambien-los-supuestos-abiertos-lg-52-d-134) | Hacer que el arranque mire tambien los supuestos abiertos (`LG-52`, `D-134`) | No implementada | Alta | No bloqueante | `000_preproject` |
| [T-144](#t-144---dar-dueño-y-sitio-a-evaluacion-y-observabilidad-lg-54-d-134) | Dar dueño y sitio a evaluacion y observabilidad (`LG-54`, `D-134`) | No implementada | Media | No bloqueante | `005_discovery` |
```

⚠️ **`T-039` es un falso positivo del patron `grep`:** su titulo contiene literalmente la frase
«Normalizar a `No implementada`», pero su columna `Estado` es `Implementada` (comprobado con
`grep -n '\[T-039\]' _persistence/tasks.md`). Las diez restantes si estan `No implementada` de
verdad.

De ellas, las que esta sesion toco de alguna forma: `T-128` sigue abierta, pero **ya no depende de
una decision pendiente** — la skill que la ejecutara (`protocol-harvest`) existe desde esta sesion
(`T-142`, `D-133`); corre solo cuando `000_preproject` se vaya a cerrar de verdad, nunca antes de la
firma del patrocinador. `T-143` y `T-144` nacieron esta sesion, a partir de la consulta de arranque
a las lecciones globales (`D-134`), y siguen sin trabajar. El resto (`T-001`, `T-002`, `T-003`,
`T-037`, `T-038`, `T-112`) no se tocaron esta sesion.

## 3. Decisiones tomadas

- **`D-130`** — la seccion 4 del informe de cierre no teclea recuentos: si publica la orden que
  enumera los supuestos abiertos, debajo va su salida cruda. **Alternativas descartadas:** suprimir
  la cifra entera (aqui la consume la seccion 6, y sin cifra la enumeracion no se puede contrastar
  consigo misma); un control mecanico que recalcule (coste desproporcionado para una orden que ya
  esta escrita).
- **`D-131`** — un desglose que explica una cifra en la NOTA DE CIERRE se deriva con una orden sobre
  el commit de **anclaje** (no el sustantivo), o no se publica. **Alternativas descartadas:**
  corregir solo el parentesis de la sesion anterior (arregla el caso, deja viva la forma que lo
  produjo); prohibir los desgloses (se pierde lo unico que hace recorrible la afirmacion de fondo).
- **`D-132`** — el Paso 2e gana una segunda pasada obligatoria, anclada al commit, porque sus dos
  contrastes de `D-125` comparten el mismo ambito de staging que la tabla que validan y no pueden ver
  lo que ese ambito deja fuera. **Alternativas descartadas:** reordenar el cierre para escribir
  `progress.md`/informe/tablero antes del Paso 2e (el informe cita un hash que aun no existe — el
  mismo huevo-y-gallina que resuelve el Paso 7c-bis); acotar solo la prosa a «area de staging» (deja
  el archivo con mas riesgo sin barrer nunca); dejarlo a la auditoria (coste entero para el auditor).
- **`D-133`** — la cosecha la ejecuta `manager`, con la skill propia `protocol-harvest`, y con puerta
  antes de escribir en el repositorio de lecciones globales. **Alternativas descartadas:** un agente
  `lessons_harvester` dedicado (cambio de alcance fuera de este repositorio, y arranca en frio contra
  el filtro 2 de portabilidad); el usuario a mano (protege el original al maximo, pero deja el paso
  sin evidencia automatica); un paso dentro de `protocol-phase-exit` (rompe separacion de roles: el
  mismo agente que verifica la casilla 10 no puede rellenarla); sin puerta, todo automatico
  (contradice «con una persona decidiendo» del propio archivo global).
- **`D-134`** — la consulta de arranque a las lecciones globales queda registrada: dos bloques (D, E)
  recorridos por indice, 17 lecciones, ocho bloques declarados `NO MIRADOS`. **Alternativas
  descartadas:** recorrer los diez bloques ya que se abria el archivo (exactamente lo que la regla
  del archivo global prohibe); aplicar las once lecciones aplazadas igualmente (adelantaria
  decisiones de arquitectura/producto que esta etapa tiene prohibido tomar).

Las cinco declaran su reversibilidad **a criterio** (no leida de una tabla: `T-037` — el inventario
de acciones irreversibles — sigue `No implementada`).

## 4. Supuestos vigentes y riesgos

`A-017` (los cuatro filtros de promocion estan escritos en el archivo global y son aplicables tal
cual) pasa de `Abierto` a `Confirmado` esta sesion, con la acotacion de que «aplicables sin
interpretacion» solo lo comprobara la primera cosecha real (`T-128`).

El resto de supuestos abiertos no se tocaron esta sesion. Derivado de
`sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep -E 'Abierto'`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep -cE 'Abierto'
14
```

`A-006` a `A-009`, `A-011` a `A-015` (nueve codigos) mas `A-004` y `A-005` (citados en «Bloqueos
activos» de `progress.md`) completan los catorce; el detalle linea a linea no se transcribe aqui
para no volver a cometer el error que `F-087` acaba de corregir (una lista tecleada al lado de una
cifra). Sobre ellos: si `A-004` (acceso al patrocinador) resulta falso, `005_discovery` no puede
empezar; si `A-003` (historico de la fuente oficial obtenible) resulta falso, el ciclo entero del
producto queda en duda.

## 5. Siguiente tarea propuesta

`T-143` — hacer que el arranque mire tambien los supuestos abiertos (`LG-52`, `D-134`), Importancia
Alta, No bloqueante. Conviene resolverla **antes** de la primera cosecha real (`T-128`), para que el
propio arranque de la sesion que cosecha no repita el sesgo que esta sesion detecto: quince (ahora
catorce, con `A-017` confirmado) supuestos `Abierto` que el arranque no reporta porque solo lee
`tasks.md`.

## 6. Que pedimos auditar

- **La atribucion de hunks por posicion en la seccion 1.** Se derivo con una orden (`awk` sobre las
  cabeceras `@@`), pero esa orden tiene el borde conocido que la propia skill documenta: un hunk que
  **anade** al final de una entrada sale rotulado con la entrada anterior. Se corrigio a mano en tres
  casos (`D-129`, `L-044`, `T-135`) comparando contra el diff real; pedimos que se verifique que no
  quedo un cuarto caso sin corregir.
- **Los criterios de cierre de `T-143` y `T-144` no llevan bloque de verificacion anclado**, a
  diferencia de todas las demas tareas de esta sesion. Es fiel a como quedaron escritas por `manager`
  en `tasks.md` (no las reescribimos: no son nuestras), pero deja dos tareas sin nada que el
  Paso 7c-bis pueda anclar. Pedimos que se confirme si eso es deliberado o un olvido.
- **La linea 21 del Paso 2d** (`grep -n 'assumptions' .claude/skills/protocol-start/SKILL.md`, dentro
  de `T-143`) no es una orden con `<hash>` pendiente de anclar: es evidencia de arranque tomada sobre
  el arbol de trabajo, como un `git status`. La tratamos como no anclable en la seccion 7; pedimos que
  se confirme que la clasificacion es correcta.
- **El supuesto mas debil de los que citamos:** que las nueve entradas nuevas de `tasks.md` (`T-136`
  a `T-144`) y las cinco de `decisions.md` (`D-130` a `D-134`) sean las **unicas** que nacieron esta
  sesion. Se derivo con `grep -E '^\+### '`, pero no se corrio un segundo barrido independiente que
  lo confirme por otro camino.

## 7. Evidencia del Paso 2d

```
$ git diff --cached -U0 -- _persistence _audit \
  | grep -E '^\+\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
     1	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion no escribe a mano'
     2	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'DERIVA con una orden y se pega su salida, nunca se teclea'
     3	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'SEGUNDA PASADA, anclada, despues del commit'
     4	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion mide el AREA DE STAGING previa al commit'
     5	+$ git show <hash>:_persistence/tasks.md | grep -cE '^### T-[0-9]+ - Escribir la skill `protocol-harvest`'
     6	+$ git show <hash>:_persistence/decisions.md | grep -c '^- 🚨 \*\*Bloques NO MIRADOS'
     7	+$ git show <hash>:_phases/005_discovery.md | grep -c 'acta de cierre'
     8	+$ git show <hash>:_persistence/decisions.md | grep -cE '^### D-[0-9]+ - La cosecha la ejecuta `manager` bajo un protocolo propio'
     9	+$ git show <hash>:_persistence/decisions.md | grep -c '^- 🚨 \*\*Bloques NO MIRADOS'
    10	+$ git show <hash>:_audit/S-030.md | grep -c 'Nota del 2026-09-10 (`F-087`)'
    11	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion no escribe a mano'
    12	+$ git show <hash>:_audit/S-030.md | grep -c 'Nota del 2026-09-10 (`F-088`)'
    13	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'DERIVA con una orden y se pega su salida, nunca se teclea'
    14	+$ git show <hash>:_templates/000_preproject/005_project.md | grep -c 'actas de cierre de esa etapa'
    15	+$ git diff --name-only --diff-filter=d 9b3f9ee^ 9b3f9ee | wc -l
    16	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'SEGUNDA PASADA, anclada, despues del commit'
    17	+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion mide el AREA DE STAGING previa al commit'
    18	+$ git show <hash>:.claude/skills/protocol-harvest/SKILL.md | grep -c 'Uso exclusivo de manager'
    19	+$ git show <hash>:.claude/skills/protocol-harvest/SKILL.md | grep -c 'LA PUERTA'
    20	+$ git show <hash>:CLAUDE.md | grep -c 'protocol-harvest'
    21	+$ grep -n 'assumptions' .claude/skills/protocol-start/SKILL.md
```

El recuento de LINEAS devueltas por esta orden (el unico que publica este paso):

```
$ git diff --cached -U0 -- _persistence _audit \
  | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
21
```

Duplicados (`uniq -d`, salida entera):

```
$ git diff --cached -U0 -- _persistence _audit \
  | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'DERIVA con una orden y se pega su salida, nunca se teclea'
+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'SEGUNDA PASADA, anclada, despues del commit'
+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion mide el AREA DE STAGING previa al commit'
+$ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c 'esta seccion no escribe a mano'
+$ git show <hash>:_persistence/decisions.md | grep -c '^- 🚨 \*\*Bloques NO MIRADOS'
```

Reejecucion de cada una — las 20 lineas 1 a 20 llevan `<hash>` literal **a proposito**: el commit de
esta sesion todavia no existe cuando se escribieron, y todas se declaran ancladas por el Paso 7c-bis
del cierre (cada una lo dice en su propio bloque, en `decisions.md` y `tasks.md`). No son
reproducibles hoy por diseño; su forma anclada y su salida esperada se publican en la NOTA DE CIERRE
de abajo, despues del commit.

La linea 21 **no es una orden con `<hash>` pendiente**: es la evidencia que `T-143` cito al abrirse,
tomada sobre el arbol de trabajo (como un `git status`), y no describe el commit de esta sesion.
Reejecutada ahora, sobre el arbol actual:

```
$ grep -n 'assumptions' .claude/skills/protocol-start/SKILL.md
3:description: ... a demanda decisions.md, constraints.md, assumptions.md, lessons.md y techdebt.md. ...
155:for f in tasks decisions constraints assumptions lessons techdebt progress; do
224:| `_persistence/assumptions.md` | haya tareas apoyadas en supuestos sin confirmar, o supuestos que puedan haber caducado |
287:| `assumptions.md` | `Confirmado` · `Refutado` |
```

Reproduce lo publicado en `tasks.md`.

> NOTA DE CIERRE — la escribe el Paso 7c despues del commit: el hash, cuantas ordenes iban con
> `<hash>` y en que archivos quedaron ancladas por el 7c-bis, con el CENSO y el CONTROL y su salida,
> y la frase de cierre. Tambien la SEGUNDA PASADA anclada del Paso 2e (sus tres ordenes y sus tres
> salidas) y la salida del CONTROL DE PROSA BORRADA del Paso 7c-bis. **Pendiente hasta el Paso 7c.**

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5
```

Las cuatro son **HEREDADAS**, no nuevas de esta sesion (comprobado contra `HEAD`, que para estos
cuatro archivos coincide exactamente con lo staged):

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"
done
.claude/skills/protocol-close/SKILL.md head=0 staged=0
.claude/skills/protocol-harvest/SKILL.md head=0 staged=0
CLAUDE.md head=0 staged=0
_audit/S-030.md head=0 staged=0
_audit/findings.md head=1 staged=1
_audit/index.md head=0 staged=0
_persistence/assumptions.md head=4 staged=4
_persistence/decisions.md head=7 staged=7
_persistence/lessons.md head=0 staged=0
_persistence/progress.md head=0 staged=0
_persistence/tasks.md head=5 staged=5
_phases/000_preproject.md head=0 staged=0
_phases/005_discovery.md head=0 staged=0
_templates/000_preproject/005_project.md head=0 staged=0
```

Estas cuatro cifras heredadas son las mismas que sostienen `DT-003` a `DT-006` (`No implementada`,
`Propuesta (pendiente del usuario)`); no se omiten aqui: heredada no es inexistente.

Los dos contrastes de la tabla:

```
$ git diff --cached --name-only --diff-filter=d | wc -l
14
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done \
  | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
```

La tabla tiene 14 filas (coincide con el contraste 1) y sus cifras suman `1+4+7+5=17` (coincide con
el contraste 2). Cuadra.

⚠️ **Esta seccion mide el AREA DE STAGING previa al commit, no el commit.** Este propio informe
(`_audit/S-031.md`) todavia no estaba en el `git add` cuando se corrio este barrido — entra despues,
junto con la fila `_audit/index.md` ya incluida. Quien cubre esa brecha es la SEGUNDA PASADA anclada
del Paso 2e, que va en la NOTA DE CIERRE de la seccion 7, escrita por el Paso 7c despues del commit.
