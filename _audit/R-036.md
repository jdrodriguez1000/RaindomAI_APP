# Auditoria R-036 — sobre S-036

| Campo | Valor |
|---|---|
| Auditoria | R-036 |
| Informe auditado | `_audit/S-036.md` |
| Commit auditado | `6fb6d39` |
| Fecha | 2026-09-11 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

### a) Que commit se audita, y por que ese

La fila mas antigua con `Auditoria: Pendiente` del tablero es `S-036`. El hash derivado no es el que
se audita: el cierre dejo **dos** commits, y manda el literal de la cabecera del informe.

```
$ git log -1 --format=%h -- _audit/S-036.md
0268901

$ git show 0268901:_audit/S-036.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `6fb6d39` |

$ git show --stat --name-only --format= 0268901
_audit/S-036.md

$ git show --stat --name-only --format= 6fb6d39 | grep -c .
15
```

El commit de anclaje toca un solo archivo; el de la sesion lleva quince, que es la lista que la NOTA
DE CIERRE del informe publica. **Se audita `6fb6d39`.**

### b) Indices contra detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show 6fb6d39:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 6fb6d39:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Sin diferencias en los siete archivos.

### c) Carpetas del arbol contra las declaradas — las dos diferencias conocidas, con motivo escrito

```
$ diff <(git ls-tree -d --name-only 6fb6d39 | sed 's|$|/|' | sort) \
       <(git show 6fb6d39:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
8a10
> temporal/
```

Las dos tienen su razon escrita en `project.md`, seccion «Carpetas propias», en sus dos vinetas de
excepcion: `temporal/` excluida por `.gitignore`, `010_prototype/` declarada por adelantado por
`D-061`. **No hay hallazgo.**

### d) Fuga de datos propios en las seis areas agnosticas — cero lineas

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github\.com" 6fb6d39 -- .claude CLAUDE.md _phases _methodology _templates _workflow
$ echo $?
1
```

Barrido propio, mas ancho que el del protocolo, porque esta es la sesion en que nace una skill que
habla de un repositorio externo (`protocol-promote`):

```
$ git grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|Company_" 6fb6d39 -- .claude CLAUDE.md _phases _methodology _templates _workflow
$ echo $?
1
```

La skill nueva lee la ruta del esqueleto de `project.md` entre angulos, como afirma su propio texto.

### e) Fuga por codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 6fb6d39 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
$ echo $?
1
```

### f) Fecha del informe contra la del commit — coinciden

```
$ git log -1 --format=%ad --date=short 6fb6d39
2026-09-11

$ git show 6fb6d39:_audit/S-036.md | grep -m1 '^| Fecha |'
| Fecha | 2026-09-11 |
```

### g) El informe contra el diff — la seccion 1, vineta por vineta

Las quince rutas de la NOTA DE CIERRE coinciden con el `--stat` del commit (control (a)). Lo que cada
vineta afirma:

```
$ git show 6fb6d39:.claude/skills/protocol-close/SKILL.md | grep -nE "^## Paso 2f |^## 9\. Evidencia del Paso 2f$|^Desfase con el esqueleto \(2f\)"
616:## Paso 2f — El desfase con el esqueleto de arranque (antes del `git add`)
1224:## 9. Evidencia del Paso 2f
1970:Desfase con el esqueleto (2f) — <al dia, sin salida | <N> archivos por promover: <la lista entera> | 🚨 SIN COMPROBAR — <el motivo>>

$ git show 6fb6d39:.claude/skills/protocol-harvest/SKILL.md | grep -n "uno de los dos"
8:Este protocolo lo ejecuta **unicamente `manager`**, y es uno de los dos del andamiaje que **escriben

$ git show 6fb6d39:CLAUDE.md | grep -n "uno de los dos"
367:🚨 **La cosecha la ejecutas tu, con la skill `protocol-harvest`, y es uno de los dos protocolos del

$ git show 6fb6d39:.claude/skills/protocol-promote/SKILL.md | grep -nE "^## Paso [0-9]+ —"
58:## Paso 0 — Los datos propios, y el disparador
95:## Paso 1 — El desfase, medido, no recordado
128:## Paso 2 — Leer las dos direcciones antes de sobreescribir nada
160:## Paso 3 — Los controles de agnosticismo, ANTES de la puerta
189:## Paso 4 — El final de linea, medido por pareja
217:## Paso 5 — 🚨 LA PUERTA
241:## Paso 6 — Escribir fuera, commitear y subir
275:## Paso 7 — El registro de vuelta, en este repositorio
297:## Paso 8 — Reporte en pantalla

$ git show 6fb6d39:.claude/skills/protocol-promote/SKILL.md | grep -c 'unicamente `manager`'
1
```

La puerta (Paso 5) va antes de escribir fuera (Paso 6), como el informe afirma.

```
$ for x in "assumptions.md:### A-019" "constraints.md:### C-010" "decisions.md:### D-151" "lessons.md:### L-056" "lessons.md:### L-057" "techdebt.md:### DT-007"; do f=${x%%:*}; p=${x##*:}; printf "%s %s -> " "$f" "$p"; git show 6fb6d39:_persistence/$f | grep -c "^$p "; done
assumptions.md ### A-019 -> 1
constraints.md ### C-010 -> 1
decisions.md ### D-151 -> 1
lessons.md ### L-056 -> 1
lessons.md ### L-057 -> 1
techdebt.md ### DT-007 -> 1
```

Estados de tareas en el indice de `tasks.md`, tal como el informe los describe:

```
$ git show 6fb6d39:_persistence/tasks.md | sed -n '/^## Indice/,/^---$/p' | grep -E '\| \[T-(15[6-9]|16[0-9]|17[01])\]' | awk -F'|' '{gsub(/ /,"",$4); print $2" -> "$4}' | sed 's/\[//;s/\](.*)//'
T-156 -> Noimplementada
T-157 -> Implementada
T-158 -> Implementada
T-159 -> Implementada
T-160 -> Implementada
T-161 -> Implementada
T-162 -> Noimplementada
T-163 -> Noimplementada
T-164 -> Implementada
T-165 -> Implementada
T-166 -> Implementada
T-167 -> Noimplementada
T-168 -> Implementada
T-169 -> Implementada
T-170 -> Implementada
T-171 -> Implementada
```

Y las dos filas nuevas de `project.md`, que el informe atribuye a `T-159`:

```
$ git show 6fb6d39 -- project.md | grep -E '^\+\| Esqueleto de arranque'
+| Esqueleto de arranque — repositorio | `C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS` |
+| Esqueleto de arranque — remoto | `https://github.com/jdrodriguez1000/SDAI_TripleS.git` (privado) |

$ git show 6fb6d39 -- project.md | grep -cE '^\+.*Las tres rutas absolutas'
1
```

**Ninguna afirmacion de la seccion 1 quedo sin respaldo en el diff, y el diff no muestra nada
relevante que la seccion 1 no enumere.**

### h) La afirmacion «las seis areas agnosticas estan limpias» — tiene su orden, en `L-057`

`D-151` y `DT-007` afirman ese resultado sin publicar orden propia. No es un veredicto suelto: el
barrido si esta publicado en la misma sesion, dentro de `L-057`, y reproduce hoy.

```
$ git show 6fb6d39:_persistence/lessons.md | grep -n "archivos, 0 ocurrencias"
2108:71 archivos, 0 ocurrencias

$ python -c "
import subprocess
BS=chr(8)
fs=subprocess.run(['git','ls-files','.claude','CLAUDE.md','_phases','_methodology','_templates','_workflow'],capture_output=True,text=True).stdout.split()
t=sum(open(f,encoding='utf-8',newline='').read().count(BS) for f in fs)
print(len(fs),'archivos,',t,'ocurrencias')"
71 archivos, 0 ocurrencias
```

**No hay hallazgo.**

### i) La seccion 6 del informe, punto por punto

| Lo que el informe pide auditar | Resultado |
|---|---|
| un bloque de `assumptions.md` ya no reproduce y el cierre no lo pudo corregir | **confirmado** — `F-103` |
| cinco bloques de `tasks.md` dejaron de reproducir dentro de la propia jornada, corregidos con nota fechada | las correcciones estan en el arbol; **pero la tabla que lo certifica esta mal numerada** — `F-102` |
| `DT-007` puede solapar con `DT-003` a `DT-006` | la entrada lo declara y lo deja a confirmacion del usuario; **no es un defecto** — ver seccion 5 |
| `T-171` pudo dejar una quinta cita cruzada | barrido propio: no aparece ninguna mas en `D-145` a `D-151` |

```
$ git show 6fb6d39:_persistence/decisions.md | awk '/^### D-(14[5-9]|15[01]) /{d=$2} d && /Lo implementa `T-1[0-9][0-9]`/{print d": "$0}'
D-146:   ejecutor. Lo implementa `T-161`.
D-147:   del caso invertido. Lo implementa `T-159`.
D-148:   va al informe. Lo implementa `T-160`.
```

Las tres remisiones apuntan a la tarea correcta, y la cita del **cuerpo** de `D-147` —«la escritura es
`T-162`»— lleva su nota fechada de `T-171` debajo. **No hay quinta cita cruzada en ese rango.**

## 2. Hallazgos

### F-102 — La tabla de reejecucion del Paso 2d de `S-036` atribuye salidas a las ordenes equivocadas

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-036.md`, seccion 7, tabla «Reejecucion, linea por linea» |

- **Que se observo:** la tabla dice reejecutar las 54 ordenes de la lista numerada con `cat -n` «por
  numero», y en un bloque de al menos doce filas la nota pegada corresponde a **otra** orden de la
  lista, no a la que la fila numera. Extracto de la lista numerada que el propio informe publica:

```
$ git show 6fb6d39:_audit/S-036.md | sed -n '/^     1\t/,/^    54\t/p' | sed -n '32,35p;41,44p;47,48p'
    32	+$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
    33	+$ test -d "$ESQ" && echo SI || echo NO
    34	+$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
    35	+$ grep -nE "^## Paso 2f |^## 9\. Evidencia|^Desfase con el esqueleto" .claude/skills/protocol-close/SKILL.md
    41	+$ if [ ! -d "$ESQ" ]; then echo "SIN COMPROBAR - ..."; else for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md; fi
    42	+$ grep -c 'unicamente `manager`' .claude/skills/protocol-promote/SKILL.md
    43	+$ grep -c 'No lo delegas en ningun agente' .claude/skills/protocol-promote/SKILL.md
    44	+$ grep -nE '^## Paso [0-9]+ —' .claude/skills/protocol-promote/SKILL.md
    47	+$ grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|vercel|supabase|Company_" .claude/skills/protocol-promote/SKILL.md
    48	+$ git grep -n "unico protocolo del\|unico del andamiaje que" -- CLAUDE.md .claude/
```

  Y estas son las filas que la tabla les pone enfrente:

```
$ git show 6fb6d39:_audit/S-036.md | grep -E '^\| (32|33|34|35|40|41|43|44|47|48) \|'
| 32 | Si | `SI` |
| 33 | 🚨 NO | Publicaba «sin salida»; hoy difiere (`T-161` volvio a tocar `CLAUDE.md`). **Corregido con nota fechada**, bajo el bloque de `T-159` (misma causa que la 19) |
| 34 | Si | lineas 616/1224/1970 |
| 35 | Si | mismos dos `No such file or directory`, `exit=2` |
| 40 | Si | `1` |
| 41 | Si | mismas 9 lineas `## Paso N —` |
| 43 | Si | vacio |
| 44 | Si | vacio |
| 47 | Si | `0` |
| 48 | Si | `1` |
```

  Contrastado contra lo que cada orden devuelve de verdad sobre el commit auditado:

```
$ P=.claude/skills/protocol-promote/SKILL.md

$ git show 6fb6d39:$P | grep -c 'No lo delegas en ningun agente'          # orden 43
1

$ git show 6fb6d39:$P | grep -cE '^## Paso [0-9]+ —'                      # orden 44
9

$ git show 6fb6d39:$P | grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|Company_"; echo "exit=$?"     # orden 47
exit=1

$ git grep -n "unico protocolo del\|unico del andamiaje que" 6fb6d39 -- CLAUDE.md .claude/; echo "exit=$?"                          # orden 48
exit=1

$ git show 6fb6d39:.claude/skills/protocol-close/SKILL.md | grep -nE "^## Paso 2f |^## 9\. Evidencia|^Desfase con el esqueleto" | cut -d: -f1 | tr '\n' '/'     # orden 35
616/1224/1970/
```

  La correspondencia real es otra: `SI` es la salida de la **33**, no de la 32 (una asignacion);
  «hoy difiere por `CLAUDE.md`» describe la **34**, no la 33 (un `test -d`); `616/1224/1970` es la
  **35**, no la 34; «mismas 9 lineas `## Paso N —`» es la **44**, no la 41 (que es el barrido de
  desfase entero); `vacio` no es la salida de la 43 (`1`) ni de la 44 (nueve lineas); `0` no es la de
  la 47 (sin salida); `1` no es la de la 48 (sin salida — y el registro publica «(sin salida)» para
  esa orden en el bloque de `T-161`). Las filas 36, 37, 38 y 40 arrastran el mismo desplazamiento.

- **Por que importa:** esa tabla **es** la evidencia del Paso 2d, el control que certifica que toda
  orden publicada en el commit sigue reproduciendo. Con el mapa roto, un lector no puede saber que
  orden se reejecuto y cual no, y una fila marcada `Si` no prueba nada sobre la orden que numera. El
  fondo si se sostiene —las seis ordenes que no reproducian estan identificadas por su contenido y
  corregidas con nota fechada en `tasks.md`, y las comprobe una por una—, y por eso es Media y no
  Alta: lo roto es la trazabilidad de la tabla, no la conclusion.
- **Que lo corregiria:** una nota fechada en la seccion 7 de `_audit/S-036.md` que diga que la columna
  `#` esta desplazada a partir de la fila 32, y que publique la correspondencia correcta —o, si
  reconstruirla entera no sale a cuenta, que acote que filas son fiables y cuales no—, sin reescribir
  la tabla ya commiteada. Hacia adelante, que el Paso 2d pegue en cada fila **la orden literal** y no
  solo su numero: un numero se desplaza en silencio, una cadena no.
  Es una recomendacion, no una orden.

---

### F-103 — El bloque de `A-018` en `assumptions.md` publica una orden que ya no reproduce, y queda sin corregir y sin tarea

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/assumptions.md`, nota del 2026-09-11 bajo `A-018` |

- **Que se observo:** el bloque publica `5 3 7 4 1 0 1` como desfase con el esqueleto para siete
  archivos. Corrido hoy devuelve otra cosa, porque `T-158` sincronizo el esqueleto mas tarde en la
  misma jornada:

```
$ git show 6fb6d39:_persistence/assumptions.md | sed -n '1108,1115p'
' "$(diff --strip-trailing-cr "$ESQ/$f" "$f" | grep -c '^<')" "$f"; done
  5  CLAUDE.md
  3  .claude/skills/protocol-audit/SKILL.md
  7  .claude/skills/protocol-close/SKILL.md
  4  .claude/skills/protocol-start/SKILL.md
  1  _phases/000_preproject.md
  0  _phases/005_discovery.md
  1  _templates/000_preproject/005_project.md

$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in CLAUDE.md .claude/skills/protocol-audit/SKILL.md .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _phases/000_preproject.md _phases/005_discovery.md _templates/000_preproject/005_project.md; do printf '%3d  %s\n' "$(diff --strip-trailing-cr "$ESQ/$f" "$f" | grep -c '^<')" "$f"; done
  4  CLAUDE.md
  0  .claude/skills/protocol-audit/SKILL.md
  0  .claude/skills/protocol-close/SKILL.md
  0  .claude/skills/protocol-start/SKILL.md
  0  _phases/000_preproject.md
  0  _phases/005_discovery.md
  0  _templates/000_preproject/005_project.md
```

  El informe lo declara en su seccion 6 y dice por que no lo toco: `assumptions.md` no es del cierre.
  Pero en el commit no hay ninguna `T-XXX` que lo recoja:

```
$ git show 6fb6d39:_persistence/tasks.md | sed -n '/^## Indice/,/^---$/p' | grep -ci "assumptions"
0
```

- **Por que importa:** ese bloque sostiene el **primer ejercicio del disparador** de `A-018`, que es
  el argumento entero de por que el supuesto sigue `Abierto` y no confirmado — y hoy sus siete cifras
  son falsas contra el arbol. Los cinco casos gemelos de `tasks.md` si llevan su nota fechada, asi que
  el registro queda desigual: el mismo defecto corregido en un archivo y no en el otro, y lo unico que
  lo recuerda es un parrafo de un informe de sesion. Sin `T-XXX`, el unico mecanismo que lo devuelve a
  la superficie es que alguien vuelva a leer `S-036.md`. Es Media y no Alta porque la conclusion del
  bloque —ninguna de las 21 lineas exigia conciliacion— no depende de las cifras que se desfasaron, y
  el supuesto sigue correctamente `Abierto`.
- **Que lo corregiria:** una nota fechada de `manager` bajo ese bloque de `A-018`, con la misma forma
  que las cinco de `tasks.md`: que la orden se corrio antes de `T-158`, que el esqueleto paso de
  `fa7da56` a `1748f0a` dentro de la jornada, y la salida de hoy — sin reescribir el bloque. Y su
  `T-XXX`, para que el pendiente viva en el registro y no en un informe.
  Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-100` | **Implementado** (`6fb6d39`) | la nota fechada existe en la entrada `S-035` de la bitacora, declara falsa la frase original sin reescribirla, y sus dos ordenes reproducen |
| `F-101` | **Implementado** (`6fb6d39`) | la nota fechada en `T-157` declara que la orden de entonces no se puede reconstruir, y republica el barrido de hoy con su ambito, su patron y su salida cruda |

```
$ git show 6fb6d39:_persistence/progress.md | grep -c 'Nota del 2026-09-11 (`T-169`, hallazgo `F-100`)'
1

$ git show --name-only --format= cce48e0 | grep -xc "project.md"
0

$ git show ce0ac4e:project.md | grep -ciE "esqueleto|version de partida"
0

$ git show 6fb6d39:_persistence/tasks.md | grep -c 'Nota del 2026-09-11 (`T-170`, hallazgo `F-101`)'
1
```

Las dos ordenes que la nota de `F-100` publica devuelven `0` y `0`, igual que el archivo afirma. La
nota de `F-101` declara ambito y patron en prosa y pega tres barridos con su salida cruda, que es
exactamente lo que el hallazgo pedia.

Una advertencia que **no** reabre `F-101`: el bloque republicado incluye una orden
(`cd <esqueleto> && git log --oneline -1`, que publica `fa7da56`) que tampoco reproduce hoy, por el
mismo mecanismo intrasesion. Esta corregida con su propia nota fechada inmediatamente debajo, dentro
del mismo commit, asi que no queda pendiente.

## 4. Lo que NO se pudo comprobar

Nada quedo `SIN COMPROBAR`. Los cinco controles mecanicos, el contraste del informe contra el diff y
los dos hallazgos anteriores se corrieron enteros sobre `6fb6d39`.

Una salvedad de alcance, que no es un control fallido: las ordenes de `F-103` y las de la seccion 9
del informe leen el repositorio **externo** del esqueleto en su estado de hoy, no anclado a ningun
commit de este repositorio. Es inevitable —el objeto medido vive fuera— y es el mismo limite que el
propio informe declara.

## 5. Recomendaciones sin hallazgo

- **La orden que se desfasa dentro de la propia jornada va ya por su sexta aparicion, y el informe lo
  pregunta explicitamente.** Las seis de esta sesion tienen la misma firma: una orden **sin ancla**
  sobre un objeto que siguio cambiando despues de pegar su salida. El Paso 2d las caza a posteriori,
  una por una y a mano — y esta vez la tabla que lo documenta salio mal numerada (`F-102`), que es el
  coste de hacerlo a mano sobre 54 filas. Lo que lo quitaria de raiz no es otro control: es una regla
  de redaccion —toda orden que se pegue en el registro va anclada a un commit, o declara en su propia
  linea que mide un estado vivo—. Evaluarlo es de `manager`; aqui solo se deja observado que el
  mecanismo actual crece en coste con cada sesion.
- **`DT-007` y el solape con `DT-003` a `DT-006`.** No es un defecto: la entrada declara que no se
  hizo la reconciliacion linea a linea y deja la decision al usuario, que es lo que corresponde a una
  deuda `Propuesta`. Vale la pena que, cuando se confirme, la cifra que se pague salga de un barrido
  unico y no de la suma de cinco entradas que pueden contar lo mismo dos veces.
- **El titulo de la entrada `S-036` de la bitacora omite `L-056`**, que el cuerpo si registra y que el
  mensaje del commit si nombra. Es un resumen, no una afirmacion falsa, y lo commiteado no se toca; se
  deja dicho porque el titulo es lo que se lee desde el indice.
- **El Paso 7c-quater paso de forma vacia en esta sesion, y conviene saberlo antes de confiar en el.**
  Busca la cadena `git show --stat --name-only --format=` en **todo** el informe, y la encontro en la
  NOTA DE CIERRE; la seccion 1 publica otra orden (`git diff --cached --stat --name-only`), legitima
  porque el commit aun no existia. El control no distingue los dos sitios, asi que hoy responde «la
  cadena esta en el archivo», no «la seccion 1 publica la orden prescrita». Aqui no hubo defecto: la
  nota de cierre si la publica, anclada. Se deja observado para que no se cuente como una garantia que
  el control no da.
