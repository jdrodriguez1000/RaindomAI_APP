# Auditoria R-037 — sobre S-037

| Campo | Valor |
|---|---|
| Auditoria | R-037 |
| Informe auditado | `_audit/S-037.md` |
| Commit auditado | `3b20720` |
| Fecha | 2026-09-11 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

### a) Sobre que commit se audita

El informe esta en el commit de anclaje `f0661e8` (un solo archivo), y su cabecera escribe el hash
literal del commit sustantivo:

```
$ git log -1 --format=%h -- _audit/S-037.md
f0661e8

$ git show f0661e8:_audit/S-037.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `3b20720` |

$ git show --stat --name-only --format= f0661e8
_audit/S-037.md

$ git show --stat --name-only --format= 3b20720 | grep -c .
13
```

Se audita **`3b20720`**, que es el que lleva el trabajo. Los 13 archivos coinciden con la lista que
enumera la seccion 1 del informe.

### b) Indice ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show 3b20720:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show 3b20720:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Sin diferencias en los siete archivos.

### c) Carpetas del arbol contra las declaradas — las dos diferencias documentadas, ninguna nueva

```
$ diff <(git ls-tree -d --name-only 3b20720 | sed 's|$|/|' | sort) \
       <(git show 3b20720:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 010_prototype/
9a11
> temporal/
```

Las dos tienen su razon escrita en `project.md` (`temporal/` excluida por `.gitignore`;
`010_prototype/` declarada por adelantado, `D-061`). **`_outbound/`, que nace en este commit, no
aparece en el diff**: esta en el arbol y esta declarada, tal como `D-154` exigia en su criterio de
cierre.

### d) Fuga de datos propios en las seis areas agnosticas — cero lineas

```
$ git grep -nE "RaindomAI|Proyectos_TripleS|github.com" 3b20720 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

### e) Fuga por codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 3b20720 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

### f) Fuga sobre `_outbound/skeleton_readme.md` (archivo nuevo, fuera de las seis areas)

`A-020` afirma que la guia no lleva datos propios ni codigos instanciados. Se comprueba, aunque
ningun control del cierre lo cubre:

```
$ git show 3b20720:_outbound/skeleton_readme.md | grep -nE "RaindomAI|Raidom|Proyectos_TripleS|jdrodriguez|Users/USUARIO"; echo "exit=$?"
exit=1

$ git show 3b20720:_outbound/skeleton_readme.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

Las dos afirmaciones verificadas de `A-020` se sostienen.

### g) La seccion 1 contra el diff — hunks archivo por archivo

```
$ for f in $(git diff --name-only 3b20720^ 3b20720); do echo "$f: $(git diff -U0 3b20720^ 3b20720 -- "$f" | grep -c '^@@')"; done
.claude/skills/protocol-close/SKILL.md: 1
_audit/S-036.md: 1
_audit/S-037.md: 1
_audit/findings.md: 3
_audit/index.md: 1
_outbound/skeleton_readme.md: 1
_persistence/assumptions.md: 3
_persistence/decisions.md: 2
_persistence/lessons.md: 2
_persistence/progress.md: 10
_persistence/tasks.md: 5
_templates/000_preproject/005_project.md: 3
project.md: 2
```

Los recuentos que la seccion 1 publica —`decisions.md` 2, `assumptions.md` 3, `tasks.md` 5,
`lessons.md` 2, `findings.md` 3, `S-036.md` 1, la skill 1, la plantilla 3, `project.md` 2— coinciden
uno a uno. Los diez hunks de `progress.md` caen todos dentro de las secciones 1, 2, 3, la fila de
indice y la bitacora, como dice el informe (verificado: la linea 198 pertenece a la seccion 3, que
termina en la 206 con `## 4. Hitos`).

### h) La correccion de `F-102` (`T-172`) — la nota esta y sus cuatro ordenes reproducen

```
$ git show 3b20720:_audit/S-036.md | grep -c 'T-172`, hallazgo `F-102'
1

$ git show 6fb6d39:.claude/skills/protocol-promote/SKILL.md | grep -c 'No lo delegas en ningun agente'
1

$ git show 6fb6d39:.claude/skills/protocol-promote/SKILL.md | grep -cE '^## Paso [0-9]+ —'
9

$ git show 6fb6d39:.claude/skills/protocol-promote/SKILL.md | grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|Company_"; echo "exit=$?"
exit=1

$ git grep -n "unico protocolo del" 6fb6d39 -- CLAUDE.md .claude/; echo "exit=$?"
exit=1
```

Y las ordenes que la nota numera **son** las 43, 44, 47 y 48 de la lista de `S-036`:

```
$ git show 6fb6d39:_audit/S-036.md | grep -nE '^ *4[3-8][[:space:]]'
268:    43	+$ grep -c 'No lo delegas en ningun agente' .claude/skills/protocol-promote/SKILL.md
269:    44	+$ grep -nE '^## Paso [0-9]+ —' .claude/skills/protocol-promote/SKILL.md
270:    45	+$ git grep -nE "RaindomAI|Raidom|Proyectos_TripleS|github[.]com" -- .claude CLAUDE.md _phases _methodology _templates _workflow
271:    46	+$ grep -noE '[A-Z]{1,2}-[0-9]+' .claude/skills/protocol-promote/SKILL.md
272:    47	+$ grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|vercel|supabase|Company_" .claude/skills/protocol-promote/SKILL.md
273:    48	+$ git grep -n "unico protocolo del|unico del andamiaje que" -- CLAUDE.md .claude/
```

Las filas 43, 44, 47 y 48 de la tabla publican «vacio», «vacio», `0` y `1`, que no es lo que esas
ordenes devuelven — el desplazamiento que la nota describe existe. Y las cuatro filas fusionadas que
la nota invoca para explicar los huecos (`2, 42`, `5, 31`, `7, 54`, `9, 49`) estan en la tabla:

```
$ git show 6fb6d39:_audit/S-036.md | grep -nE '^\| [0-9]+, '
304:| 2, 42 | Si | `1` |
307:| 5, 31 | Si | `1` |
309:| 7, 54 | Si | `26` |
311:| 9, 49 | Si | `0` |
```

⚠️ Dos de las cuatro ordenes se reejecutaron **acortadas** respecto a la lista original (la 47 sin
`vercel|supabase`, la 48 sin su segunda alternativa). Se comprobo que la version completa da lo
mismo, asi que no altera la conclusion:

```
$ git show 6fb6d39:.claude/skills/protocol-promote/SKILL.md | grep -nE "[Rr]aidom|[Rr]aindom|[Tt]riple[_ ]?S|SDAI|jdrodriguez|Users/USUARIO|vercel|supabase|Company_"; echo "exit47=$?"
exit47=1

$ git grep -n "unico protocolo del|unico del andamiaje que" 6fb6d39 -- CLAUDE.md .claude/; echo "exit48=$?"
exit48=1
```

### i) La correccion de `F-103` (`T-173`) — la nota esta, y el numero de linea corregido es el bueno

```
$ git show 3b20720:_persistence/assumptions.md | grep -c 'T-173`, hallazgo `F-103'
1

$ git show 3b20720:_persistence/assumptions.md | sed -n '1126,1129p' | cat -n
     1
     2	(cerca de codigo)
     3	$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in CLAUDE.md ... ; do printf '%3d  %s\n' "$(diff --strip-trailing-cr "$ESQ/$f" "$f" | grep -c '^<')" "$f"; done
     4	  4  CLAUDE.md
```

La linea 1127 es la cerca y la 1128 la orden, tal como la nota de cierre de `T-173` afirma — la
seccion 6 del informe pedia comprobarlo. El bloque original de `T-173` no se perdio: el diff solo
**anade** la nota bajo el.

### j) La mitad hacia adelante de `F-102` (`D-152`, `T-174`) — esta en la skill

```
$ git show 3b20720:.claude/skills/protocol-close/SKILL.md | grep -c 'lleva LA ORDEN LITERAL'
1

$ git show 3b20720:.claude/skills/protocol-close/SKILL.md | grep -c '^| # | Orden | Reproduce | Nota |'
1
```

### k) `T-167` — la plantilla lleva las tres filas y no filtra datos propios

```
$ git show 3b20720:_templates/000_preproject/005_project.md | grep -n '^| Esqueleto de arranque'
44:| Esqueleto de arranque — repositorio | `<ruta absoluta del esqueleto en esta maquina; un guion si no esta clonado aqui>` |
45:| Esqueleto de arranque — remoto | `<url del remoto del esqueleto>` |
46:| Esqueleto de arranque — version de partida | `<hash completo del commit del que salio este proyecto>` |
```

Los tres huecos estan en blanco. La discrepancia «dos filas» del titulo de `T-167` frente a las tres
implementadas **esta declarada** en el propio bloque de verificacion de la tarea y en la seccion 6
del informe, asi que no se abre hallazgo por ella.

### l) El Paso 2d — la lista de 16 ordenes cuadra con el commit

```
$ git diff -U0 3b20720^ 3b20720 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
29

$ git diff -U0 3b20720^ 3b20720 -- _audit/S-037.md | grep -cE '^\+\$ '
13
```

29 − 13 = **16**, exactamente las que el informe reejecuta: la diferencia son las ordenes que el
propio informe aporta, que no estaban en staging cuando el Paso 2d se corrio.

### m) Los barridos de la NOTA DE CIERRE — reproducen exactos

```
$ for f in $(git ls-tree -r --name-only 3b20720 _persistence _audit | grep -v '_audit/S-037.md'); do n=$(git show 3b20720:"$f" | grep -cE '^\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-025.md: 2
_audit/R-026.md: 7
_audit/R-027.md: 1
_audit/R-029.md: 2
_audit/R-030.md: 3
_audit/R-031.md: 2
_audit/R-032.md: 2
_audit/R-033.md: 3
_audit/R-034.md: 2
_audit/R-035.md: 1
_audit/S-017.md: 1
_audit/S-024.md: 1
_audit/S-025.md: 17
_audit/S-026.md: 8
_audit/S-027.md: 17
_audit/S-029.md: 2
_audit/S-030.md: 2
_persistence/assumptions.md: 9
_persistence/decisions.md: 16
_persistence/tasks.md: 23

$ for f in $(git diff --name-only 3b20720^ 3b20720 -- _persistence _audit ":(exclude)_audit/S-037.md"); do n=$(git diff -U0 3b20720^ 3b20720 -- "$f" | grep -cE '^\+\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
(sin salida)
```

Censo identico al publicado; el control sale vacio, como el informe dice.

### n) La segunda pasada del Paso 2e — los tres numeros cuadran

```
$ for f in $(git diff --name-only --diff-filter=d 3b20720^ 3b20720); do n=$(git show 3b20720:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
_audit/findings.md: 1
_persistence/assumptions.md: 4
_persistence/decisions.md: 7
_persistence/tasks.md: 5

$ git diff --name-only --diff-filter=d 3b20720^ 3b20720 | wc -l
13

$ for f in $(git diff --name-only --diff-filter=d 3b20720^ 3b20720); do git show 3b20720:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
17
```

`1+4+7+5 = 17` y 13 archivos: identico a lo publicado.

### o) El Paso 2f — las 5 lineas de desfase con el esqueleto reproducen hoy

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files .../SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files .../SDAI_TripleS/.claude/skills/protocol-harvest/SKILL.md and .claude/skills/protocol-harvest/SKILL.md differ
Only in .claude/skills: protocol-promote
Files .../SDAI_TripleS/_templates/000_preproject/005_project.md and _templates/000_preproject/005_project.md differ
Files .../SDAI_TripleS/CLAUDE.md and CLAUDE.md differ
```

⚠️ Mide un estado vivo (otro repositorio), no anclable. Las cinco son promociones pendientes, como
el informe dice.

### p) La seccion 0 del informe

Los dos hallazgos que declara «corregidos en este commit» estan en el diff (`T-172` en
`_audit/S-036.md`, `T-173` en `_persistence/assumptions.md`), y la columna `Veredicto` **no** usa
`Implementado` en ningun caso: dice `Aceptado — corregido en este commit`, y `findings.md` publica
`Aceptado — pendiente`. Correcto.

### q) Fecha del commit contra la del informe y la del tablero

```
$ git log -1 --format=%ad --date=short 3b20720
2026-09-11
```

Coincide con la cabecera del informe y con la fila del tablero.

### r) Fila ↔ ficha en `_audit/findings.md` — **no coinciden** (hallazgo `F-106`)

```
$ git show 3b20720:_audit/findings.md | grep -E '^\| \[F-10[23]\]' | sed 's/.*| R-036/R-036/'
R-036 | Media | Aceptado — pendiente |
R-036 | Media | Aceptado — pendiente |

$ git show 3b20720:_audit/findings.md | sed -n '/^### F-102 /,/^- \*\*Que se observo/p' | sed -n '7,9p'
| Estado | Abierto |
| Registrado en | |
| Cerrado en | |
```

(Detalle en la seccion 2.)

## 2. Hallazgos

### F-104 - La orden que sostiene `D-153` usa un marcador en vez de la ruta, y no es ejecutable
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/decisions.md`, `D-153` (commit `3b20720`, linea 9822) |

- **Que se observo:** `D-153` publica la medicion que justifica la decision con un marcador donde
  deberia ir la ruta:

```
$ git show 3b20720:_persistence/decisions.md | grep -n "Esqueleto de arranque' <ruta del esqueleto>"
9822:$ grep -c '^| Esqueleto de arranque' <ruta del esqueleto>/project.md
```

  Ejecutada literalmente, no produce el `0` publicado: falla.

```
$ bash -c "grep -c '^| Esqueleto de arranque' <ruta del esqueleto>/project.md"; echo "exit=$?"
bash: line 1: ruta: No such file or directory
exit=1
```

  Con la ruta real, el dato de fondo **si** es el publicado:

```
$ grep -c '^| Esqueleto de arranque' "C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/project.md"
0
```

- **Por que importa:** `CLAUDE.md` exige que un resultado afirmado vaya con «el patron y el ambito
  con que se obtuvo», y la razon es que se pueda reproducir sin rehacerlo. Aqui hay que adivinar el
  ambito: `decisions.md` no es un archivo agnostico, asi que nada obligaba a esconder la ruta —las
  otras ordenes de la misma sesion la escriben literal (`ESQ="C:/Users/.../SDAI_TripleS"`)—, y el
  marcador no distingue entre «se omitio a proposito» y «se pego mal».
- 🔑 **Por que es Baja:** el dato es correcto, la ruta esta declarada en `project.md` y la conclusion
  de `D-153` no depende de ella.
- **Por que es hallazgo y no solo una nota del informe:** el cierre lo detecto y lo dejo escrito en
  su seccion 6, pero `decisions.md` no es del cierre y no quedo ninguna `T-XXX`. Es el mismo patron
  que `F-103` levanto una sesion antes: un pendiente que solo vive en un informe de sesion no vuelve
  a la superficie solo.
- **Que lo corregiria:** una nota fechada bajo `D-153` con la orden escrita con la ruta literal y su
  salida, sin reescribir el bloque original. ⚠️ Es una recomendacion, no una orden.

### F-105 - La nota que corrige `F-103` vuelve a publicar en `assumptions.md` una cifra que ya no reproducia en ese mismo commit
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_persistence/assumptions.md`, nota del 2026-09-11 bajo `A-018` (`T-173`) |

- **Que se observo:** la nota que corrige `F-103` publica `4 0 0 0 0 0 0`. Reejecutada, la septima
  cifra es `3`:

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in CLAUDE.md .claude/skills/protocol-audit/SKILL.md .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _phases/000_preproject.md _phases/005_discovery.md _templates/000_preproject/005_project.md; do printf '%3d  %s\n' "$(diff --strip-trailing-cr "$ESQ/$f" "$f" | grep -c '^<')" "$f"; done
  4  CLAUDE.md
  0  .claude/skills/protocol-audit/SKILL.md
  0  .claude/skills/protocol-close/SKILL.md
  0  .claude/skills/protocol-start/SKILL.md
  0  _phases/000_preproject.md
  0  _phases/005_discovery.md
  3  _templates/000_preproject/005_project.md
```

  Y no es un cambio posterior al commit: `T-167` edito ese mismo archivo **dentro de la sesion**,
  despues de que la nota se escribiera — el propio commit ya la contradecia.

```
$ git diff --name-only 3b20720^ 3b20720 -- _templates/000_preproject/005_project.md
_templates/000_preproject/005_project.md
```

- **Por que importa:** `A-018` es el supuesto que sostiene que toda diferencia con el esqueleto es
  una promocion pendiente, y esa nota es su unico ejercicio publicado. Quien lea `assumptions.md` ve
  seis ceros y una diferencia; el estado del propio commit son cinco ceros y dos diferencias. El
  pendiente quedo en la seccion 6 del informe y en una nota de `tasks.md` bajo `T-173`, **no en el
  archivo donde se lee la cifra**.
- 🔑 **Por que es Baja y no Media:** a diferencia de `F-103`, esta nota **si declara en su propia
  linea** que mide un estado vivo y que «la cifra de hoy vale para hoy», asi que no afirma
  permanencia. Lo que falta es la constancia del desfase donde vive la cifra.
- **Que lo corregiria:** una segunda nota fechada bajo el mismo bloque de `A-018` con la salida del
  cierre, sin reescribir nada — o, mejor, evaluar la recomendacion de la seccion 5 de `R-036` (que
  toda orden vaya anclada o declare en su linea que mide un estado vivo), que sigue sin evaluar y es
  la raiz comun de este caso y del anterior. ⚠️ Es una recomendacion, no una orden.

### F-106 - El tratamiento de `F-102` y `F-103` cambia la fila del indice de `findings.md` pero deja la ficha diciendo `Abierto`
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/findings.md`, fichas `F-102` y `F-103` (commit `3b20720`) |

- **Que se observo:** el indice pasa los dos hallazgos a `Aceptado — pendiente`, y la ficha de
  detalle de cada uno sigue con `Estado | Abierto` y `Registrado en` vacio:

```
$ git show 3b20720:_audit/findings.md | grep -E '^\| \[F-10[23]\]' | sed 's/.*| R-036/R-036/'
R-036 | Media | Aceptado — pendiente |
R-036 | Media | Aceptado — pendiente |

$ git show 3b20720:_audit/findings.md | sed -n '/^### F-102 /,/^- \*\*Que se observo/p' | sed -n '7,9p'
| Estado | Abierto |
| Registrado en | |
| Cerrado en | |

$ git show 3b20720:_audit/findings.md | sed -n '/^### F-103 /,/^- \*\*Que se observo/p' | sed -n '7,9p'
| Estado | Abierto |
| Registrado en | |
| Cerrado en | |
```

  Y **es una regresion**, no la costumbre de la casa: las dos sesiones anteriores si rellenaron los
  dos campos en la ficha.

```
$ git show 6fb6d39:_audit/findings.md | sed -n '/^### F-100 /,/^- \*\*Que se observo/p' | sed -n '7,8p'
| Estado | Aceptado — pendiente |
| Registrado en | `T-169` |

$ git show cce48e0:_audit/findings.md | sed -n '/^### F-098 /,/^- \*\*Que se observo/p' | sed -n '7,8p'
| Estado | Aceptado — pendiente |
| Registrado en | `T-164` |
```

- **Por que importa:** el mismo archivo afirma dos estados distintos del mismo hallazgo, y el campo
  que enlaza el hallazgo con su tarea queda vacio — el puntero a `T-172`/`T-173` solo sobrevive
  dentro de la prosa del bloque `🕐 Tratamiento`. `CLAUDE.md` pide actualizar la fila, y el protocolo
  de auditoria exige que fila y entrada digan lo mismo; el control mecanico de indices de
  `_persistence/` no cubre `findings.md`, asi que nada lo habria cazado. Es **Media**: el registro se
  contradice consigo mismo, aunque el tratamiento de fondo si esta escrito y es correcto.
- **Que lo corregiria:** hacia atras, nada que reescribir con cuidado especial — esta auditoria ya
  deja los dos campos puestos al cerrarlos. Hacia adelante, que el tratamiento de un hallazgo toque
  **los dos sitios** (fila y ficha: `Estado` y `Registrado en`), y que el cierre lo compruebe con el
  mismo tipo de barrido indice↔detalle que ya corre sobre `_persistence/`. ⚠️ Es una recomendacion,
  no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| `F-102` | **Implementado** (commit `3b20720`) | La nota fechada esta en la seccion 7 de `_audit/S-036.md`, publica la correspondencia entera fila por fila, y sus cuatro ordenes ancladas reproducen (`1`, `9`, vacio, vacio) — verificado en 1h. La tabla original no se reescribio. La mitad hacia adelante esta en la skill (`D-152`, `T-174`), verificada en 1j |
| `F-103` | **Implementado** (commit `3b20720`) | La nota fechada esta bajo el bloque de `A-018`, con la salida de hoy y la declaracion de estado vivo, sin reescribir el bloque original, y con su `T-173` en el registro — verificado en 1i. ⚠️ Que esa nota quedara desfasada dentro del mismo commit es **evidencia nueva** y va como `F-105`, no como reapertura |

## 4. Lo que NO se pudo comprobar

- **Las ordenes del Paso 2d que interrogan el area de staging** (`git diff --cached ...`) no se
  pueden reejecutar: ese staging ya no existe. Se contrastaron por su equivalente anclado
  (`git diff 3b20720^ 3b20720`), y los tres numeros publicados —16 ordenes, 13 archivos, 17
  caracteres de control— cuadran (secciones 1l y 1n).
- **El Paso 2f y el bloque de `A-018`** miden un repositorio externo (`SDAI_TripleS`) y no son
  anclables a ningun commit de este. Se reejecutaron contra su estado de hoy, y se dice asi.
- **`A-020` no se puede probar desde aqui**, como el propio informe declara: este repositorio no
  salio del esqueleto. Solo se verifico lo verificable (seccion 1f).

## 5. Recomendaciones sin hallazgo

- **La recomendacion de la seccion 5 de `R-036` sigue sin evaluar**, y esta sesion produjo dos casos
  mas de lo mismo (`F-104`, `F-105`). Merece decision propia —`T-XXX` o `D-XXX`— antes de que el
  patron vuelva a repetirse.
- **El commit anade una linea en blanco suelta** antes de `## 4. Hitos` en `_persistence/progress.md`
  (hunk `@@ -197,0 +205 @@`). Es ruido, no defecto; se menciona por si conviene no arrastrarlo.
- **Las reejecuciones acortadas** de las ordenes 47 y 48 en la nota de `T-172` (seccion 1h) dieron el
  mismo resultado que las completas, asi que no cambian nada. Aun asi, la forma que `D-152` acaba de
  fijar —orden literal, o trozo discriminante— vale tambien para una orden que se reejecuta: pegarla
  tal como estaba habria evitado tener que comprobar las dos versiones.
