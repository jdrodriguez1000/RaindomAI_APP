# Informe de auditoria — S-009

| Campo | Valor |
|---|---|
| Sesion | S-009 |
| Fecha | 2026-09-02 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-009.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-020 — La entrada de `F-017` en `findings.md` conserva la linea que dice que sigue pendiente de evaluar | Implementado | `T-027`, en este commit: la vineta residual «Que se hizo: pendiente de la evaluacion de `manager`» de la entrada `F-017` se borro; queda una sola vineta «Que se hizo», la fechada |
| F-021 — `progress.md` atribuye a `R-007` un commit auditado que no es el suyo | Implementado | `T-028`, en este commit: la celda «Avance de la etapa» de `S-008` en `progress.md` dice ahora «`R-007` (sobre `122b770`)», con nota fechada explicando la correccion; el segundo `ae06147` de la misma celda (el `HEAD` contra el que se verificaron los hallazgos) no se toco, porque era correcto |
| F-022 — Tres bloques de verificacion de `decisions.md` registran una salida que no se reproduce | Implementado | `T-029`, en este commit: `D-036`, `D-038` y `D-040` reciben cada uno una nota fechada, con la orden que si se reproduce y su salida cruda; ningun texto original se reescribio |
| F-023 — `T-026` se escribio a mano sin el `D-XXX` ni el `F-NNN` que la convencion exige, y la desviacion no llego a `decisions.md` | Implementado | `T-030` y `D-044`, en este commit: nace `D-044`, que asume la desviacion de `T-026` como caso puntual (no como tercera excepcion) y la ficha de `T-026` la cita |

## 1. Que se hizo

Estado previo al commit (no existe todavia el commit de este cierre; la lista equivalente a
`git show --stat --name-only --format= <commit>` se genera aqui con `git status`/`git diff` sobre
el arbol de trabajo, porque el commit que la contendra es este mismo):

```
$ git diff --stat --name-only
.claude/skills/protocol-close/SKILL.md
CLAUDE.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_persistence/techdebt.md
_templates/005_discovery/005_needs.md
_templates/005_discovery/010_actors.md
_templates/005_discovery/015_stakeholders.md
_templates/005_discovery/020_hypothesis.md
project.md

$ git status --porcelain | grep '^??'
?? _workflow/
```

A esa lista el commit de este cierre le suma ademas el propio informe (`_audit/S-009.md`, este
archivo) y su fila en `_audit/index.md` (ya incluida arriba, porque se escribio antes del commit).
El auditor puede confirmar la lista final, ya anclada al hash, con
`git show --stat --name-only --format= <commit>` sobre el commit que contiene este archivo.

**Que cambio, con codigos y rutas:**

- **`_audit/findings.md`** — `F-020` a `F-023` pasan de `Abierto` a `Aceptado — pendiente`, cada uno
  con su `T-XXX` citado (`T-027` a `T-030`).
- **`_persistence/decisions.md`** — nacen `D-043` a `D-049`. `D-045` revoca `D-036` (los artefactos
  del descubrimiento pasan de `_discovery/` a `005_discovery/`, por orden del usuario). `D-044`
  registra como caso puntual la desviacion de `T-026`. `D-046` a `D-049` documentan el nacimiento de
  `_workflow/`. `D-043` fija la fecha de las entradas de esta sesion.
- **`_persistence/tasks.md`** — nacen `T-027` a `T-031`, las cinco `Implementada`, todas con
  `Origen: report_auditor` salvo `T-031` (`Origen: usuario`, por `D-045`).
- **`_persistence/assumptions.md`** — nace `A-005`: la parte de `_workflow/ai_levels.md` sin
  experiencia propia detras (harness, observabilidad, evaluaciones, rubricas, metricas) es un
  supuesto abierto, con su disparador de refutacion.
- **`_persistence/lessons.md`** — nacen `L-013` (un bloque de verificacion sin ancla caduca) y
  `L-014` (una carpeta agnostica nueva necesita cuatro enganches; el cuarto —de uso, no de
  control— quedo sin resolver para `_workflow/`).
- **`_persistence/techdebt.md`** — nace `DT-002` (propuesta del cierre, ver seccion 6 y el reporte
  en pantalla): `_workflow/` sin ningun enganche de uso, a partir de `L-014`.
- **`_templates/005_discovery/*.md`** (cuatro plantillas) — la ruta `_discovery/...` se sustituye por
  `005_discovery/...` en la cabecera `Artefacto` y en los bloques de comprobacion (`T-031`, `D-045`).
- **`project.md`** — las dos filas de la tabla «Codigos» (`N-XXX`, `I-XXX`) apuntan ahora a
  `005_discovery/...`; la cita de `D-036` se sustituye por `D-045`.
- **`CLAUDE.md`** y **`.claude/skills/protocol-close/SKILL.md`** — se anade `_workflow/` a la lista
  de carpetas agnosticas que tienen que poder copiarse tal cual, y al ambito del Paso 1b (de cinco a
  seis rutas) (`D-046`, `D-047`).
- **`_workflow/`** (nueva, sin trackear hasta este commit) — `team.md` y `ai_levels.md`, adaptados
  de un documento aportado por el usuario (`D-046`, `D-048`, `D-049`).
- **`_persistence/progress.md`** — se actualiza «Estado general» (avance de la etapa),
  «Ultimo realizado», «Siguiente paso», y nace la entrada `S-009` en la bitacora con su fila en el
  indice de sesiones.
- **`_audit/index.md`** — nueva fila para `S-009.md`, con `Pendiente` en las tres columnas que le
  tocan al auditor.

## 2. Que NO se hizo, y por que

Tareas que siguen `No implementada` en el indice de `_persistence/tasks.md` (filtro literal, no de
memoria):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
| [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto) | Definir alcance y objetivo del proyecto | No implementada | Alta | Bloqueante | `005_discovery` |
| [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject) | Declarar las etapas posteriores a `000_preproject` | No implementada | Media | No bloqueante | `005_discovery` |
| [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003) | Verificar si el historico de la fuente oficial es obtenible (`A-003`) | No implementada | Alta | Bloqueante | `000_preproject` |
```

Ninguna de las tres se toco esta sesion; siguen bloqueadas por `A-003` (T-003) y por el acceso al
patrocinador (`A-004`, T-001/T-002 dependen de la etapa `005_discovery`, que no puede empezar sin
ese acceso).

Ademas, `DT-002` (propuesta) queda **sin pagar**: `_workflow/` nacio sin ningun enganche de uso
—nada en `_phases/` ni en ningun protocolo manda leer `team.md` o `ai_levels.md`—, y decidir donde
se engancha es una decision del usuario que esta sesion no toma.

## 3. Decisiones tomadas

- **`D-043`** — las entradas de `S-009` se fechan `2026-09-02`, por continuidad con el registro
  (mismo caso que `D-042` para `S-008`, con su propia entrada porque `D-042` nombra explicitamente
  `S-008`). Alternativas descartadas: usar `2026-09-01` (la del reloj, deja `S-009` antes que
  `S-007`/`S-008`); `2026-09-03` (sin respaldo); fechar en silencio.
- **`D-044`** — la ficha `T-026`, escrita a mano fuera de las dos excepciones de la convencion de
  `tasks.md`, se asume como **caso puntual**, no como tercera excepcion. Alternativas descartadas:
  abrir una tercera excepcion en la convencion (no acota nada); dejarlo solo en la prosa de la ficha
  (es exactamente el hueco que `F-023` señalo); borrar la ficha y dejar que la reescriba el cierre
  (borra el rastro del incumplimiento).
- **`D-045`** — los artefactos rellenos del descubrimiento viven en `005_discovery/`, no en
  `_discovery/`; revoca `D-036`. Decision del **usuario**. Alternativas descartadas: mantener
  `_discovery/` (no dice de que etapa viene); `discovery/` sin prefijo numerico (pierde el orden
  entre etapas).
- **`D-046`** — nace `_workflow/`, carpeta agnostica de primer nivel con `team.md` y `ai_levels.md`.
  Alternativas descartadas: un unico archivo con todo (mezcla dos naturalezas); dentro de
  `_methodology/` (la haria pasar por fuente canonica no editable); repartir el contenido dentro de
  cada archivo de etapa (se repite y se desincroniza).
- **`D-047`** — `_workflow/` aplica a todas las etapas declaradas salvo `000_preproject`. Sin
  alternativas registradas aparte de la justificacion (la etapa preparatoria construye el sistema de
  trabajo que hace posible el reparto, no reparte trabajo sobre un producto).
- **`D-048`** — la secuencia de fases propia del documento fuente del usuario
  (`DISCOVER → AUDIT → MODEL → ...`) no entra en el repositorio. Alternativas descartadas: incluirla
  como referencia informativa (se acaba leyendo como declaracion); mapear las dos secuencias en una
  tabla de equivalencias (inventa correspondencias que nadie ha decidido).
- **`D-049`** — frontera de contenido entre `team.md` (nombra los niveles) y `ai_levels.md`
  (los desarrolla, con la rubrica de seleccion añadida en §6, que no estaba en la fuente).
  Alternativas descartadas: un solo archivo (mezcla naturalezas y crece demasiado); que `team.md` no
  mencionara los niveles (deja su razonamiento a medias).

Todas marcadas **reversible a criterio**, declarado como criterio en la propia entrada — no existe
todavia en `_persistence/` un inventario de acciones irreversibles registrado.

## 4. Supuestos vigentes y riesgos

- **`A-004`** (abierto desde `S-007`) — sigue sin resolver: existe acceso al patrocinador y a
  personas que puedan hablar del proceso real. De el depende que `005_discovery` pueda empezar
  (`T-001`, `T-002`).
- **`A-003`** (abierto desde el primer cierre) — si el historico de la fuente oficial es obtenible.
  Del el depende el ciclo entero del producto (`T-003`).
- **`A-005`** (nuevo, esta sesion) — la parte de `_workflow/ai_levels.md` escrita sin experiencia
  propia (harness, observabilidad, evaluaciones, rubricas, metricas — secciones 3 a 6) es correcta.
  Se apoya en ella cualquier decision futura que use ese archivo para elegir un nivel de sistema de
  IA. Si resulta falsa: la rubrica de §6 no discrimina entre niveles al aplicarla de verdad, o las
  metricas de §5.4 no son las que hacen falta cuando se instrumente el primer harness. Disparador de
  revision: la primera vez que se declare un nivel de sistema de IA con su `D-XXX` (`ai_levels.md`
  §8, en la linea base del producto).
- **Riesgo sin codigo propio, señalado en `L-014`** — `_workflow/` nacio con sus tres enganches de
  control (declarada, agnostica, dentro del ambito del Paso 1b) pero sin ningun enganche de **uso**:
  nada manda leerla en el momento en que aplica. Este cierre lo convirtio en `DT-002` (propuesta,
  pendiente del usuario) para que no quede solo en la leccion.

## 5. Siguiente tarea propuesta

`T-001` — Definir alcance y objetivo del proyecto a partir de `_brief/client_brief.md`, etapa
`005_discovery`. Importancia `Alta`, urgencia `Bloqueante`. Antes de arrancarla hay que resolver
`A-004`: confirmar que existe un patrocinador alcanzable y personas que puedan hablar del proceso
real, porque `_phases/005_discovery.md` dice explicitamente que sin ese acceso la etapa no puede
empezar.

## 6. Que pedimos auditar

- **El ancla temporal de dos bloques de verificacion de esta sesion.** `D-043` y `D-044` usan
  `git show HEAD:_persistence/decisions.md ...` (y `...tasks.md`) sin fijar el hash explicitamente
  dentro de la propia orden — el hash solo aparece en un comando anterior del mismo bloque
  (`git log -1` en `D-043`) o en la frase «antes de esta entrada» (`D-044`). Verificado: el conteo
  que `D-043` registra como `16` (`git show HEAD:_persistence/decisions.md | grep -c "^| Fecha |
  2026-09-02 |"`) da `23` al correrlo sobre el arbol de trabajo actual, que es exactamente lo que
  contendra el proximo `HEAD` en cuanto este commit exista. Es el patron que `L-013` nombra: un
  bloque que cuenta algo que la propia sesion sigue añadiendo no da el mismo numero corrido sobre el
  commit de cierre que corrido hoy. No lo reescribimos —no es nuestro, y `CLAUDE.md` prohibe tocar
  una entrada de `decisions.md` que no sea propia del cierre—; lo dejamos señalado para que el
  auditor lo confirme y, si corresponde, `manager` lo ancle a `7025a05` explicitamente en una nota
  fechada.
- **`DT-002`, la deuda que este cierre propuso a partir de `L-014`.** Es una lectura razonable del
  diff, pero la evaluamos con el mismo criterio con el que se proponen todas: sin confirmacion del
  usuario. Si el auditor considera que el enganche de uso ya existe en algun archivo de `_phases/`
  que no revisamos con suficiente detalle, la propuesta sobra.
- **La doble diferencia del Paso 2c.** El control de carpetas (`git ls-tree -d --name-only HEAD` vs.
  la tabla de `project.md`) devuelve dos filas declaradas sin carpeta en el arbol: `temporal/`
  (razon ya escrita en `project.md`, esperada en cada cierre) y `_workflow/` (todavia sin trackear
  en el momento de correr el control, porque el commit de este cierre es el que la va a añadir). La
  segunda deberia desaparecer del control en cuanto el auditor lo corra sobre el commit ya hecho;
  si no desaparece, es un hallazgo real.
