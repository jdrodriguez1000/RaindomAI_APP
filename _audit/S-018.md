# Informe de auditoria — S-018

| Campo | Valor |
|---|---|
| Sesion | S-018 |
| Fecha | 2026-09-03 |
| Etapa | `000_preproject` |
| Rama | `main` |
| Commit auditado | el commit que contiene este archivo (`git log -1 -- _audit/S-018.md`) |

## 0. Respuesta a la auditoria anterior

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-040 — Las plantillas nuevas usan los codigos `FT-` y `SC-`, que `project.md` declara «propuesta, no declarada» | Aceptado — pendiente | `T-061`, `D-075` |
| F-041 — `T-057` afirma que las plantillas no llevan codigos instanciados «mas alla del primero», y el commit lo desmiente | Aceptado — pendiente | `T-062` |
| F-042 — Las cinco apariciones de `\x08` quedan sin `T-XXX` ni `DT-XXX`, y la decision se delega en un actor que no decide | Aceptado — pendiente | `T-063`, `DT-003` |
| F-043 — `L-024` dice «seis apariciones» donde son seis lineas y veinte apariciones, en la misma entrada | Aceptado — pendiente | `T-064` |
| F-044 — La seccion 1 del informe omite las dos notas de reincidencia añadidas a `L-004` y `L-019` | Aceptado — pendiente | `T-065` |

Los cinco hallazgos de `R-017` se evaluaron y se corrigio lo que cada uno señalaba en esta misma
sesion (ver seccion 1). Ninguno se marca `Implementado` porque esa verificacion **la hace la
auditoria siguiente sobre este commit**, no `manager`.

## 1. Que se hizo

Salida cruda de `git diff --stat` contra el estado anterior al cierre (equivalente, una vez hecho el
commit, a `git show --stat --name-only --format= <commit>`; se excluye este mismo informe porque
todavia no forma parte del arbol al correr el comando):

```
$ git diff --stat HEAD -- . ':(exclude)_audit/S-018.md'
 .claude/skills/protocol-close/SKILL.md |  24 +++
 _audit/findings.md                     |  77 +++++++--
 _audit/index.md                        |   1 +
 _persistence/assumptions.md            |  35 ++++
 _persistence/decisions.md              | 151 +++++++++++++++++
 _persistence/lessons.md                | 127 +++++++++++++-
 _persistence/progress.md               | 172 +++++++++++++------
 _persistence/tasks.md                  | 293 ++++++++++++++++++++++++++++++++-
 _persistence/techdebt.md               |  49 ++++++
 _phases/020_baseline.md                |  10 +-
 project.md                             |  24 ++-
 11 files changed, 875 insertions(+), 88 deletions(-)

$ git status --porcelain | grep '^??'
?? _workflow/020_baseline.md
```

El commit final añade ademas este mismo archivo (`_audit/S-018.md`, nuevo) y la fila de `S-018` en
`_audit/index.md` (ya incluida en el diff de arriba).

Detalle por archivo, con codigos:

- **`project.md`**: `FT-XXX` y `SC-XXX` (nacen, filas nuevas en la tabla «Codigos»); se reescribe el
  parrafo que decia «de los codigos de producto solo estan declarados `N-XXX` e `I-XXX`»; nace el
  aviso que cita `D-075` y `F-040`. Resuelve `F-040`.
- **`_persistence/decisions.md`**: `D-075` (nace — declara `FT-`/`SC-`), `D-076` (nace — puntuacion
  de variabilidad de la entrada de `020_baseline`); nota fechada añadida al bloque de verificacion
  de `D-073` (`T-062`, `F-041`).
- **`_persistence/tasks.md`**: `T-057` (edita — pasa de `No implementada` a `Implementada`, con nota
  fechada añadida a su cuerpo por `F-041`, y dos parrafos nuevos: correccion colateral de la cita
  generica en `_phases/020_baseline.md`, y remision a `D-076`); `T-060`, `T-061`, `T-062`, `T-063`,
  `T-064`, `T-065` (nacen, las seis correcciones de `R-017` mas la de `F-037` sobre `progress.md`).
- **`_persistence/lessons.md`**: `L-025` (nace — patron con limite de palabra ciego a prefijos mas
  largos), `L-026` (nace — un enganche de uso escrito en generico no engancha); nota de reincidencia
  añadida a `L-020` (la entrada de `D-069`/reincidencia numero dos, `F-041`); correccion de recuento
  dentro de `L-024` (viñeta «Que ocurrio» y viñeta «Lo que queda pendiente», `F-043`) mas nota fechada
  añadida a `L-024` (`F-042`, cita `DT-003`).

  📌 **Nota del 2026-09-03 (`T-067`, hallazgo `F-046`).** El informe se deja tal cual se escribio
  (`D-019`), y ese `L-020` esta mal: la nota de reincidencia cae en **`L-019`**, no en `L-020`, y el
  parentesis «la entrada de `D-069`» no corresponde a ninguna de las dos. La atribucion se deriva
  del diff, mapeando cada hunk a la entrada que lo contiene:

  ```
  $ git diff -U0 9a52cfa^ 9a52cfa -- _persistence/lessons.md | awk '/^@@/{split($3,a,","); print a[1]+0}' | while read n; do git show 9a52cfa:_persistence/lessons.md | awk -v n="$n" 'NR<=n && /^### [A-Z]+-[0-9]+/{e=$2} END{print n": "e}'; done
  36:
  709: L-019
  862: L-024
  899: L-024
  902: L-024
  912: L-024

  $ git diff -U0 9a52cfa^ 9a52cfa -- _persistence/lessons.md | grep -E '^\+### '
  +### L-025 - Un patron con limite de palabra es ciego a los prefijos mas largos que empiezan igual
  +### L-026 - Un enganche de uso escrito en generico no engancha nada
  ```

  La primera orden nombra la entrada que contiene cada punto tocado —el `36:` vacio es el indice, y
  el `912:` es el final de `L-024`, donde el diff engancha las dos entradas que nacen—; la segunda
  nombra las que nacen. Entre las dos, ningun `L-020`.
- **`_persistence/assumptions.md`**: `A-006` (nace — los codigos `FT-`/`SC-` declarados son
  regularizacion, no eleccion de diseño confirmada).
- **`_persistence/techdebt.md`**: `DT-003` (nace — las cinco lineas antiguas con `\x08`, `Propuesta
  (pendiente del usuario)`).
- **`_persistence/progress.md`**: nota fechada añadida al parrafo de la bitacora `S-015` (`T-060`,
  `F-037`); se sobrescriben `## 1. Estado general`, `## 2. Ultimo realizado`, `## 3. Siguiente paso`;
  nace `S-018` en `## 5. Bitacora` y su fila en el indice de sesiones.
- **`_audit/findings.md`**: `F-040` a `F-044` pasan de `Estado: Abierto` a `Aceptado — pendiente`,
  con su `Registrado en` (`T-061` a `T-065`) y su viñeta «Que se hizo»; `F-037` (fila de `_audit`,
  cita en su bloque historico) recibe una viñeta nueva «Que se hizo con lo que `R-016` dejo abierto».
- **`_phases/020_baseline.md`**: dos citas genericas al reparto («el archivo de esta etapa en
  `_workflow/`») se reescriben para nombrar `_workflow/020_baseline.md` (§4 y §5).
- **`_workflow/020_baseline.md`** (nuevo, no editado): el reparto Humano/Software/IA de los diez
  pasos de la etapa `020_baseline`.
- **`.claude/skills/protocol-close/SKILL.md`**: se añade la seccion «En un archivo de registro, lo
  que nace no es todo lo que cambia» y los dos huecos nuevos en la plantilla del informe (Paso 6b,
  seccion 1), resolviendo `F-044`.

## 2. Que NO se hizo, y por que

Salida cruda del filtro sobre el indice de `tasks.md`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | awk -F'|' '{gsub(/^ +| +$/,"",$4); if($4=="No implementada") print}'
  [T-001](#t-001---definir-alcance-y-objetivo-del-proyecto)   Definir alcance y objetivo del proyecto  No implementada  Alta   Bloqueante   `005_discovery`
  [T-002](#t-002---declarar-las-etapas-posteriores-a-000_preproject)   Declarar las etapas posteriores a `000_preproject`  No implementada  Media   No bloqueante   `005_discovery`
  [T-003](#t-003---verificar-si-el-historico-de-la-fuente-oficial-es-obtenible-a-003)   Verificar si el historico de la fuente oficial es obtenible (`A-003`)  No implementada  Alta   Bloqueante   `000_preproject`
  [T-037](#t-037---escribir-el-inventario-de-acciones-irreversibles-del-proyecto-lg-38)   Escribir el inventario de acciones irreversibles del proyecto (`LG-38`)  No implementada  Alta   No bloqueante   `000_preproject`
  [T-038](#t-038---igualar-el-barrido-de-fuga-de-protocol-audit-con-el-de-protocol-close)   Igualar el barrido de fuga de `protocol-audit` con el de `protocol-close`  No implementada  Media   No bloqueante   `000_preproject`
```

Nada de lo que esta sesion se propuso quedo a medias: los cinco hallazgos de `R-017` se cerraron con
su tarea, y `_workflow/020_baseline.md` quedo escrito completo. Lo que sigue abierto es el trabajo
de fondo de siempre — `T-001`/`T-002`/`T-003` (etapa `005_discovery`, sin arrancar) y `T-037`/`T-038`
(deuda de metodo, disponible pero no priorizada) — mas lo que esta sesion abrio nuevo:

- **`DT-003`** (`Propuesta — pendiente del usuario`): las cinco lineas antiguas del registro con el
  caracter `0x08` siguen sin anotar. La correccion —una nota fechada por linea— la decide el usuario
  porque implica tocar entradas ya auditadas.
- **El reparto de `_workflow/020_baseline.md` no queda adoptado por existir**: su adopcion formal
  con `D-XXX` es trabajo del dia en que la etapa se abra (`_workflow/team.md` §8), igual que paso con
  `_workflow/010_prototype.md`.
- **`A-006`** queda abierto: que `FT-XXX`/`SC-XXX` sean los codigos definitivos de la baseline no
  esta confirmado, solo regularizado contra el desfase que ya existia en las plantillas.

## 3. Decisiones tomadas

- **`D-075` — Se declaran `FT-` y `SC-` en la tabla «Codigos», porque las plantillas ya los citan.**
  Alternativas descartadas: (1) escribir las plantillas con el hueco generico en vez del codigo
  instanciado — se descarta porque vuelve ilegible la plantilla de trazabilidad y `CLAUDE.md` permite
  expresamente que una plantilla use codigos instanciados en sus ejemplos; (2) declarar los cinco
  prefijos del metodo de una vez (`FT-`, `SC-`, `VS-`, `TC-`, `ADR-`) — se descarta porque declararia
  cuatro codigos que nadie ha escrito y le quitaria al Paso 3 de la etapa su contraste contra el
  registro; (3) añadir una excepcion a `project.md` que exente los codigos dentro de `_templates/` —
  se descarta porque una lista de excepciones envejece sin que nadie la revise.
- **`D-076` — El reparto de la baseline puntua la variabilidad de la entrada en 2, y por eso no
  declara discrepancia.** Alternativas descartadas: (1) puntuar 3 por coherencia con los dos
  archivos hermanos — se descarta porque convertiria la rubrica en formalidad heredada; (2) puntuar
  3 solo por los pasos de conversacion (Paso 2 y Paso 7) — se descarta porque la rubrica puntua la
  etapa, no el paso; (3) no puntuar y remitir al archivo anterior — prohibido expresamente por
  `_workflow/ai_levels.md` §7.

Las dos se clasifican **reversible a criterio**, declarado como tal en la propia entrada: son filas
de un archivo de registro sin ningun artefacto de producto escrito contra ellas todavia, y mientras
no exista el inventario de `T-037` la clasificacion no se lee de ninguna tabla.

## 4. Supuestos vigentes y riesgos

- **`A-006`** (nuevo esta sesion): los codigos `FT-XXX`/`SC-XXX` que `D-075` declaro se apoyan en que
  este proyecto va a adoptar la etapa `020_baseline` con el juego de codigos de la guia de metodo. Si
  resulta falso — otras etapas posteriores con otro vocabulario, u otros nombres al adoptar
  `020_baseline` — la fila de `project.md` sobra o cambia, y la retirada se registra con su `D-XXX`
  citando `D-075`. Disparador: la decision que declare las etapas posteriores a `005_discovery`
  (`T-002`), o el Paso 3 de la baseline el dia que se abra — lo que llegue primero.
- **`A-004`** (existente, no tocado esta sesion): sigue condicionando que `005_discovery` pueda
  empezar.
- **`A-003`** (existente, no tocado esta sesion): sigue condicionando el ciclo entero del producto.

## 5. Siguiente tarea propuesta

**`manager` lanza `report_auditor`** sobre el commit de este cierre (paso obligatorio, no una tarea
de `tasks.md`). Como primera tarea de producto: **`T-001`** — definir alcance y objetivo del
proyecto a partir de `_brief/client_brief.md` (Importancia `Alta`, Urgencia `Bloqueante`, etapa
`005_discovery`) —, condicionada a resolver antes `A-004` (acceso al patrocinador).

## 6. Que pedimos auditar

- **La cobertura del patron ampliado de `L-025`/`T-062`.** Se amplio `\b(N|T|D|A|C|I|F|L|S|R|DT)-…`
  a `\b(FT|SC|VS|TC|ADR)-…` para las dos notas fechadas, pero **no se reescribio el patron original
  de `D-073`**, que sigue publicado tal cual con su punto ciego documentado al lado. Si en una
  proxima plantilla aparece un prefijo de dos letras nuevo que ninguna de las dos listas cubre, el
  mismo punto ciego se repite y esta vez sin nota — conviene que la auditoria confirme si las dos
  listas (`\b(N|T|D|A|C|I|F|L|S|R|DT)` y `\b(FT|SC|VS|TC|ADR)`) cubren hoy **todos** los prefijos que
  `project.md` declara, o si falta alguno.
- **La decision de no reescribir `T-057` es la mas discutible de la sesion.** `T-057` acumula ya
  tres párrafos de nota fechada sobre su cuerpo original (`F-041`, la correccion colateral de la cita
  generica, y la remision a `D-076`), y en algun punto una entrada que se explica mas por sus notas
  que por su cuerpo deja de ser legible de un vistazo — que es justo lo que el indice de `tasks.md`
  existe para evitar. No hay una regla escrita que diga cuando una entrada tiene demasiadas notas
  fechadas encima y deberia reescribirse por una version nueva (`T-XXX` sucesora) en vez de seguir
  acumulando.
- **`A-006` es un supuesto sobre una decision que la misma sesion tomo.** Es una forma valida de
  supuesto —«esto se supone, no se afirma»—, pero conviene que la auditoria confirme que no se esta
  usando para blanquear una decision insuficientemente fundamentada: si `D-075` fuera en realidad mas
  debil de lo que su texto sugiere, envolverla en un supuesto no la hace mas solida, solo la hace mas
  dificil de discutir porque ya no se lee como decision abierta a objecion sino como decision tomada
  con una reserva aparte.

## 7. Evidencia del Paso 2d

Orden ejecutada (sobre el area de staging, antes de que el informe existiera; su equivalencia anclada
al commit, una vez creado el hash, es `git diff -U0 <hash>^ <hash> -- _persistence _audit
":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'`):

```
$ git diff -U0 -- _persistence _audit | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+$ grep -nE '^\| `(FT|SC)-XXX`' project.md
+$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
+$ grep -rnoE "\b(VS|TC|ADR)-[0-9]{3}\b" _templates/ _phases/ _workflow/ _persistence/ _audit/ ; echo "exit=$?"
+$ grep -c "Variabilidad de la entrada" _workflow/005_discovery.md _workflow/010_prototype.md _workflow/020_baseline.md
+$ grep -l "discrepancia con la tabla de lectura" _workflow/005_discovery.md _workflow/010_prototype.md _workflow/020_baseline.md
+$ grep -c "no hay discrepancia que declarar" _workflow/020_baseline.md
+$ grep -rnoE "\b(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]{3}\b" _templates/020_baseline/
+$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
+$ ls -1 _workflow/
+$ grep -cE "^\| \*\*([1-9]|10) · " _workflow/020_baseline.md
+$ grep -c "^### Paso " _phases/020_baseline.md
+$ grep -n "_workflow/020_baseline" _phases/020_baseline.md
+$ grep -rnE "RaidomAI|Proyectos_TripleS|TripleS|github.com|USUARIO" _workflow/020_baseline.md ; echo "exit=$?"
+$ grep -rnoE "\b(N|T|D|A|C|I|F|L|S|R|DT|FT|SC|VS|TC|ADR)-[0-9]{3}\b" _workflow/020_baseline.md ; echo "exit=$?"
+$ grep -c 'Nota del 2026-09-03 (`T-060`, hallazgo `F-037`)' _persistence/progress.md
+$ grep -nE '^\| `(FT|SC)-XXX`' project.md
+$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
+$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
+$ grep -c 'Nota del 2026-09-03 (`T-062`, hallazgo `F-041`)' _persistence/decisions.md _persistence/tasks.md
+$ grep -c 'lo decide una auditoria' _persistence/lessons.md
+$ grep -c 'DT-003' _persistence/lessons.md _persistence/techdebt.md
+$ grep -nE 'seis lineas|cinco lineas|veinte apariciones|dieciocho apariciones' _persistence/lessons.md
+$ grep -cE '\*\*seis\*\* apariciones|\*\*cinco apariciones' _persistence/lessons.md
+$ grep -c 'entradas YA EXISTENTES que el commit edita' .claude/skills/protocol-close/SKILL.md
+$ grep -c 'no es todo lo que cambia' .claude/skills/protocol-close/SKILL.md
+$ python -c "import io,re; pat=re.compile(u'[\x00-\x08\x0b\x0c\x0e-\x1f]'); [print(f,i,repr(m.group())) for f in ['_persistence/decisions.md','_persistence/tasks.md'] for i,l in enumerate(io.open(f,encoding='utf-8'),1) for m in pat.finditer(l)]" | awk '{print $1, $2}' | uniq -c
```

**Recuento: 28 lineas devueltas** (28 ordenes distintas citadas; no hay repeticion de una misma
orden en dos archivos esta sesion, asi que las dos cifras coinciden).

> 📌 **Nota del 2026-09-03 (`T-066`, hallazgo `F-045`).** El informe se deja tal cual se escribio
> (`D-019`), y las dos cifras de arriba son falsas. La orden anclada al commit que este informe
> describe devuelve **26 lineas** y **23 ordenes distintas**, con **tres repetidas**:
>
> ```
> $ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
> 26
>
> $ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort -u | wc -l
> 23
>
> $ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
> +$ grep -nE '^\| `(FT|SC)-XXX`' project.md
> +$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
> +$ grep -rnoE "(FT|SC|VS|TC|ADR)-[0-9]{3}" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
> ```
>
> Y la numeracion de la lista de reejecucion se corrio **una posicion**: las repetidas son la 16,
> la 17 y la 18 —duplicados de la 1, la 2 y la 8—, no la 15, la 16, la 17 y la 18. La posicion 15
> es una orden propia que quedo **sin salida publicada**; su salida, sobre el mismo commit, es esta:
>
> ```
> $ git diff -U0 9a52cfa^ 9a52cfa -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n | sed -n '15,18p'
>     15	+$ grep -c 'Nota del 2026-09-03 (`T-060`, hallazgo `F-037`)' _persistence/progress.md
>     16	+$ grep -nE '^\| `(FT|SC)-XXX`' project.md
>     17	+$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
>     18	+$ grep -rnoE "(FT|SC|VS|TC|ADR)-[0-9]{3}" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
>
> $ git show 9a52cfa:_persistence/progress.md > /tmp/p.md; grep -c 'Nota del 2026-09-03 (`T-060`, hallazgo `F-037`)' /tmp/p.md
> 1
> ```
>
> Reproduce. Ninguna de las 26 fallo — pero eso no lo probaba el informe, que solo publico 25.

Reejecucion de cada una, contra el estado del arbol de trabajo que este cierre va a commitear (todas
reproducen; no hubo que corregir ninguna):

```
--1-- (=15, =16)
232:| `FT-XXX` | el artefacto de features de `020_baseline` (ruta por declarar: la etapa no esta adoptada) | feature |
233:| `SC-XXX` | el artefacto de escenarios de `020_baseline` (ruta por declarar: la etapa no esta adoptada) | scenario |

--2-- (=17)
0

--3--
exit=1

--4--
_workflow/005_discovery.md:1
_workflow/010_prototype.md:1
_workflow/020_baseline.md:1

--5--
_workflow/005_discovery.md
_workflow/010_prototype.md

--6--
1

--7--
_templates/020_baseline/015_features.md:189:N-001
_templates/020_baseline/045_traceability.md:199:N-001

--8-- (=18)
_templates/020_baseline/015_features.md:72:FT-003
_templates/020_baseline/020_scenarios.md:72:SC-003
_templates/020_baseline/025_specification.md:236:SC-003
_templates/020_baseline/045_traceability.md:200:SC-007
_templates/020_baseline/045_traceability.md:200:FT-004

--9--
005_discovery.md
010_prototype.md
020_baseline.md
ai_levels.md
team.md

--10--
10

--11--
10

--12--
138:**`_workflow/020_baseline.md`**, que se lee ahora y no despues. Ese reparto se adopta con su `D-XXX`
316:🚨 **Las plantillas y el reparto de `_workflow/020_baseline.md` son condicion de entrada, no trabajo

--13--
exit=1

--14--
exit=1

--19--
_persistence/decisions.md:1
_persistence/tasks.md:2

--20--
0

--21--
_persistence/lessons.md:1
_persistence/techdebt.md:2

--22--
862:  obtiene otro resultado. El barrido que lo destapo encontro **seis lineas** afectadas en el registro
863:  —una de esta sesion, ya reparada, y **cinco anteriores**, que no se tocan—. Son veinte apariciones
864:  del caracter repartidas en esas seis lineas; el recuento por lineas y el recuento por apariciones
881:como `(x10)`; el total del barrido fue **veinte apariciones en seis lineas**. Se dice aqui para que
885:`_persistence/decisions.md:3869`: devuelve **dieciocho apariciones en cinco lineas**, las cinco
899:- **Lo que queda pendiente, y por que no se hizo hoy:** las **cinco lineas anteriores** no se

--23--
0

--24--
1

--25--
1

--26--
      2 _persistence/decisions.md 919
      2 _persistence/decisions.md 1306
      2 _persistence/tasks.md 1218
     10 _persistence/tasks.md 1937
      2 _persistence/tasks.md 1993
```

📌 **Numeracion de esta lista.** Se etiqueta cada resultado con la posicion que ocupa su orden en la
lista de arriba (1 a 28); donde una orden se repite identica mas adelante en la lista (15=1, 16=1,
17=2, 18=8), se marca con `(=N)` en vez de repetir la salida. Ninguna de las 28 lineas fallo al
reejecutarse ni devolvio algo distinto de lo que los bloques de esta sesion publican.

Procedencia por archivo (de donde sale cada orden, derivada del diff y no escrita a mano):

```
$ git diff -U0 -- _persistence _audit | awk '/^\+\+\+ /{f=$2} /^\+\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
b/_persistence/decisions.md :: +$ grep -nE '^\| `(FT|SC)-XXX`' project.md
b/_persistence/decisions.md :: +$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
b/_persistence/decisions.md :: +$ grep -rnoE "\b(VS|TC|ADR)-[0-9]{3}\b" _templates/ _phases/ _workflow/ _persistence/ _audit/ ; echo "exit=$?"
b/_persistence/decisions.md :: +$ grep -c "Variabilidad de la entrada" _workflow/005_discovery.md _workflow/010_prototype.md _workflow/020_baseline.md
b/_persistence/decisions.md :: +$ grep -l "discrepancia con la tabla de lectura" _workflow/005_discovery.md _workflow/010_prototype.md _workflow/020_baseline.md
b/_persistence/decisions.md :: +$ grep -c "no hay discrepancia que declarar" _workflow/020_baseline.md
b/_persistence/lessons.md :: +$ grep -rnoE "\b(N|T|D|A|C|I|F|L|S|R|DT)-[0-9]{3}\b" _templates/020_baseline/
b/_persistence/lessons.md :: +$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
b/_persistence/lessons.md :: +$ ls -1 _workflow/
b/_persistence/lessons.md :: +$ grep -cE "^\| \*\*([1-9]|10) · " _workflow/020_baseline.md
b/_persistence/lessons.md :: +$ grep -c "^### Paso " _phases/020_baseline.md
b/_persistence/lessons.md :: +$ grep -n "_workflow/020_baseline" _phases/020_baseline.md
b/_persistence/lessons.md :: +$ grep -rnE "RaidomAI|Proyectos_TripleS|TripleS|github.com|USUARIO" _workflow/020_baseline.md ; echo "exit=$?"
b/_persistence/lessons.md :: +$ grep -rnoE "\b(N|T|D|A|C|I|F|L|S|R|DT|FT|SC|VS|TC|ADR)-[0-9]{3}\b" _workflow/020_baseline.md ; echo "exit=$?"
b/_persistence/progress.md :: +$ grep -c 'Nota del 2026-09-03 (`T-060`, hallazgo `F-037`)' _persistence/progress.md
b/_persistence/tasks.md :: +$ grep -nE '^\| `(FT|SC)-XXX`' project.md
b/_persistence/tasks.md :: +$ grep -oE '^\| `[A-Z]+-[A-Za-z]+`' project.md | sort | uniq -d | wc -l
b/_persistence/tasks.md :: +$ grep -rnoE "\b(FT|SC|VS|TC|ADR)-[0-9]{3}\b" _templates/020_baseline/ | grep -vE "(FT|SC)-00[12]"
b/_persistence/tasks.md :: +$ grep -c 'Nota del 2026-09-03 (`T-062`, hallazgo `F-041`)' _persistence/decisions.md _persistence/tasks.md
b/_persistence/tasks.md :: +$ grep -c 'lo decide una auditoria' _persistence/lessons.md
b/_persistence/tasks.md :: +$ grep -c 'DT-003' _persistence/lessons.md _persistence/techdebt.md
b/_persistence/tasks.md :: +$ grep -nE 'seis lineas|cinco lineas|veinte apariciones|dieciocho apariciones' _persistence/lessons.md
b/_persistence/tasks.md :: +$ grep -cE '\*\*seis\*\* apariciones|\*\*cinco apariciones' _persistence/lessons.md
b/_persistence/tasks.md :: +$ grep -c 'entradas YA EXISTENTES que el commit edita' .claude/skills/protocol-close/SKILL.md
b/_persistence/tasks.md :: +$ grep -c 'no es todo lo que cambia' .claude/skills/protocol-close/SKILL.md
b/_persistence/techdebt.md :: +$ python -c "import io,re; pat=re.compile(u'[\x00-\x08\x0b\x0c\x0e-\x1f]'); [print(f,i,repr(m.group())) for f in ['_persistence/decisions.md','_persistence/tasks.md'] for i,l in enumerate(io.open(f,encoding='utf-8'),1) for m in pat.finditer(l)]" | awk '{print $1, $2}' | uniq -c
```

La procedencia por archivo coincide con la seccion 1: cada orden nace en el archivo que la publica
como parte de su propio bloque de verificacion.
